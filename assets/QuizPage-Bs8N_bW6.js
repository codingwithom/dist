const __vite__mapDeps=(i,m=__vite__mapDeps,d=(m.f||(m.f=["assets/index-CSKiqNtJ.js","assets/vendor-charts-Du7SI2xx.js","assets/vendor-firebase-C_hAKoH5.js","assets/index-5Zz1WMpG.css"])))=>i.map(i=>d[i]);
import { c as createLucideIcon, j as jsxRuntimeExports, B as Button, y as ArrowLeft, z as FileText, X, b as cn, aq as House, r as Lock, Z as BookOpen, ar as Sparkles, W as User, A as AnimatePresence, a0 as Layers, m as motion, I as Input, S as Search, o as Play, as as useControllableState, at as composeEventHandlers, au as Primitive, av as useComposedRefs, aw as createCollection, ax as useSize, ay as createContextScope, P as Pause, f as Check, az as Award, aA as CircleQuestionMark, i as useAppContext, $ as BrainCircuit, g as ChevronLeft, ab as VolumeX, V as Volume2, _ as __vitePreload, w as useWorkspaceContext, a6 as CircleAlert, U as Trophy, h as ChevronRight, a5 as Eye } from "./index-CSKiqNtJ.js";
import { b as reactExports, h as getDefaultExportFromCjs } from "./vendor-charts-Du7SI2xx.js";
import { G as Globe } from "./globe-UxNVB4Qz.js";
import { P as Plus, a as Pencil } from "./plus-DAVR1Obx.js";
import { A as ArrowRight, I as Info } from "./info-0rwtQfyI.js";
import { E as EllipsisVertical } from "./ellipsis-vertical-mcnznuEr.js";
import { I as Image$1, a as ZoomIn, Z as ZoomOut, S as Save } from "./zoom-out-B3GslGUK.js";
import { E as ExternalLink, B as Badge } from "./badge-OjNuXFZ9.js";
import { s as svg, h as html$2, o as ok, f as find, a as stringify, b as stringify$1, c as hastToReact, d as asciiAlphanumeric, e as factorySpace, m as markdownLineEnding, g as markdownLineEndingOrSpace, u as unicodeWhitespace, i as unicodePunctuation, j as asciiAlpha, k as asciiControl, l as asciiAtext, n as markdownSpace, p as asciiPunctuation, q as asciiHexDigit, r as asciiDigit, v as visitParents, t as unreachable, w as convert, x as longestStreak, E as EXIT, y as katex, z as rehypeKatex, A as remarkMath } from "./vendor-math-DSzbeUdl.js";
import { R as RotateCcw } from "./rotate-ccw-DjT8jNnP.js";
import { a as Copy, C as CircleCheckBig } from "./copy-CWwXm7p_.js";
import { C as CircleCheck } from "./circle-check-_7gsecGi.js";
import { C as CircleX } from "./circle-x-D9WO7NmT.js";
import { T as Trash2 } from "./trash-2-DUaqGf45.js";
import { S as Settings, M as Mic } from "./settings-DW2CZk4t.js";
import { D as Download } from "./download-ca0rhp0K.js";
import { F as FilePlay } from "./file-play-B6kygM-F.js";
import { R as RefreshCw } from "./refresh-cw-m_nwo0Ny.js";
import { S as Square } from "./square-C_SLYSjx.js";
import { T as Target } from "./target-kimGvUoM.js";
import { Z as Zap } from "./zap-er3ixULL.js";
import "./vendor-firebase-C_hAKoH5.js";
const __iconNode$b = [
  ["path", { d: "M12 20v2", key: "1lh1kg" }],
  ["path", { d: "M12 2v2", key: "tus03m" }],
  ["path", { d: "M17 20v2", key: "1rnc9c" }],
  ["path", { d: "M17 2v2", key: "11trls" }],
  ["path", { d: "M2 12h2", key: "1t8f8n" }],
  ["path", { d: "M2 17h2", key: "7oei6x" }],
  ["path", { d: "M2 7h2", key: "asdhe0" }],
  ["path", { d: "M20 12h2", key: "1q8mjw" }],
  ["path", { d: "M20 17h2", key: "1fpfkl" }],
  ["path", { d: "M20 7h2", key: "1o8tra" }],
  ["path", { d: "M7 20v2", key: "4gnj0m" }],
  ["path", { d: "M7 2v2", key: "1i4yhu" }],
  ["rect", { x: "4", y: "4", width: "16", height: "16", rx: "2", key: "1vbyd7" }],
  ["rect", { x: "8", y: "8", width: "8", height: "8", rx: "1", key: "z9xiuo" }]
];
const Cpu = createLucideIcon("cpu", __iconNode$b);
const __iconNode$a = [
  ["path", { d: "M12 19v3", key: "npa21l" }],
  ["path", { d: "M15 9.34V5a3 3 0 0 0-5.68-1.33", key: "1gzdoj" }],
  ["path", { d: "M16.95 16.95A7 7 0 0 1 5 12v-2", key: "cqa7eg" }],
  ["path", { d: "M18.89 13.23A7 7 0 0 0 19 12v-2", key: "16hl24" }],
  ["path", { d: "m2 2 20 20", key: "1ooewy" }],
  ["path", { d: "M9 9v3a3 3 0 0 0 5.12 2.12", key: "r2i35w" }]
];
const MicOff = createLucideIcon("mic-off", __iconNode$a);
const __iconNode$9 = [
  ["path", { d: "M15 18h-5", key: "95g1m2" }],
  ["path", { d: "M18 14h-8", key: "sponae" }],
  [
    "path",
    {
      d: "M4 22h16a2 2 0 0 0 2-2V4a2 2 0 0 0-2-2H8a2 2 0 0 0-2 2v16a2 2 0 0 1-4 0v-9a2 2 0 0 1 2-2h2",
      key: "39pd36"
    }
  ],
  ["rect", { width: "8", height: "4", x: "10", y: "6", rx: "1", key: "aywv1n" }]
];
const Newspaper = createLucideIcon("newspaper", __iconNode$9);
const __iconNode$8 = [
  ["rect", { width: "18", height: "18", x: "3", y: "3", rx: "2", key: "afitv7" }],
  ["path", { d: "M9 3v18", key: "fh3hqa" }],
  ["path", { d: "m16 15-3-3 3-3", key: "14y99z" }]
];
const PanelLeftClose = createLucideIcon("panel-left-close", __iconNode$8);
const __iconNode$7 = [
  ["rect", { width: "18", height: "18", x: "3", y: "3", rx: "2", key: "afitv7" }],
  ["path", { d: "M9 3v18", key: "fh3hqa" }]
];
const PanelLeft = createLucideIcon("panel-left", __iconNode$7);
const __iconNode$6 = [
  ["path", { d: "M21 12a9 9 0 1 1-9-9c2.52 0 4.93 1 6.74 2.74L21 8", key: "1p45f6" }],
  ["path", { d: "M21 3v5h-5", key: "1q7to0" }]
];
const RotateCw = createLucideIcon("rotate-cw", __iconNode$6);
const __iconNode$5 = [
  ["path", { d: "M16 10a4 4 0 0 1-8 0", key: "1ltviw" }],
  ["path", { d: "M3.103 6.034h17.794", key: "awc11p" }],
  [
    "path",
    {
      d: "M3.4 5.467a2 2 0 0 0-.4 1.2V20a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2V6.667a2 2 0 0 0-.4-1.2l-2-2.667A2 2 0 0 0 17 2H7a2 2 0 0 0-1.6.8z",
      key: "o988cm"
    }
  ]
];
const ShoppingBag = createLucideIcon("shopping-bag", __iconNode$5);
const __iconNode$4 = [
  ["path", { d: "M10 8h4", key: "1sr2af" }],
  ["path", { d: "M12 21v-9", key: "17s77i" }],
  ["path", { d: "M12 8V3", key: "13r4qs" }],
  ["path", { d: "M17 16h4", key: "h1uq16" }],
  ["path", { d: "M19 12V3", key: "o1uvq1" }],
  ["path", { d: "M19 21v-5", key: "qua636" }],
  ["path", { d: "M3 14h4", key: "bcjad9" }],
  ["path", { d: "M5 10V3", key: "cb8scm" }],
  ["path", { d: "M5 21v-7", key: "1w1uti" }]
];
const SlidersVertical = createLucideIcon("sliders-vertical", __iconNode$4);
const __iconNode$3 = [
  [
    "path",
    {
      d: "M11.525 2.295a.53.53 0 0 1 .95 0l2.31 4.679a2.123 2.123 0 0 0 1.595 1.16l5.166.756a.53.53 0 0 1 .294.904l-3.736 3.638a2.123 2.123 0 0 0-.611 1.878l.882 5.14a.53.53 0 0 1-.771.56l-4.618-2.428a2.122 2.122 0 0 0-1.973 0L6.396 21.01a.53.53 0 0 1-.77-.56l.881-5.139a2.122 2.122 0 0 0-.611-1.879L2.16 9.795a.53.53 0 0 1 .294-.906l5.165-.755a2.122 2.122 0 0 0 1.597-1.16z",
      key: "r04s7s"
    }
  ]
];
const Star = createLucideIcon("star", __iconNode$3);
const __iconNode$2 = [
  ["path", { d: "M17 14V2", key: "8ymqnk" }],
  [
    "path",
    {
      d: "M9 18.12 10 14H4.17a2 2 0 0 1-1.92-2.56l2.33-8A2 2 0 0 1 6.5 2H20a2 2 0 0 1 2 2v8a2 2 0 0 1-2 2h-2.76a2 2 0 0 0-1.79 1.11L12 22a3.13 3.13 0 0 1-3-3.88Z",
      key: "m61m77"
    }
  ]
];
const ThumbsDown = createLucideIcon("thumbs-down", __iconNode$2);
const __iconNode$1 = [
  ["path", { d: "M7 10v12", key: "1qc93n" }],
  [
    "path",
    {
      d: "M15 5.88 14 10h5.83a2 2 0 0 1 1.92 2.56l-2.33 8A2 2 0 0 1 17.5 22H4a2 2 0 0 1-2-2v-8a2 2 0 0 1 2-2h2.76a2 2 0 0 0 1.79-1.11L12 2a3.13 3.13 0 0 1 3 3.88Z",
      key: "emmmcr"
    }
  ]
];
const ThumbsUp = createLucideIcon("thumbs-up", __iconNode$1);
const __iconNode = [
  ["path", { d: "m17 2-5 5-5-5", key: "16satq" }],
  ["rect", { width: "20", height: "15", x: "2", y: "7", rx: "2", key: "1e6viu" }]
];
const Tv = createLucideIcon("tv", __iconNode);
const DEFAULT_SHORTCUTS = [
  { id: "1", name: "Google", url: "https://www.google.com" },
  { id: "2", name: "Wikipedia", url: "https://en.wikipedia.org" },
  { id: "3", name: "JEE Main", url: "https://jeemain.nta.ac.in" },
  { id: "4", name: "Maths Help", url: "https://www.wolframalpha.com" },
  { id: "5", name: "Physics Classroom", url: "https://www.physicsclassroom.com" },
  { id: "6", name: "Chemistry Notes", url: "https://www.masterorganicchemistry.com" }
];
function AIEngin({ onBack }) {
  const [isMobile, setIsMobile] = reactExports.useState(false);
  const [tabs, setTabs] = reactExports.useState(() => {
    return [
      {
        id: "initial-tab",
        title: "New Tab",
        url: "",
        browserMode: "newtab",
        query: "",
        searchResults: [],
        iframeUrl: "",
        history: [""],
        historyIndex: 0,
        readerMode: false,
        scrapedData: null,
        loading: false
      }
    ];
  });
  const [activeTabId, setActiveTabId] = reactExports.useState("initial-tab");
  const [showTabSwitcherMobile, setShowTabSwitcherMobile] = reactExports.useState(false);
  const [showMenuDropdown, setShowMenuDropdown] = reactExports.useState(false);
  const [showInfoDropdown, setShowInfoDropdown] = reactExports.useState(false);
  const [fullscreen, setFullscreen] = reactExports.useState(false);
  const iframeRef = reactExports.useRef(null);
  const [shortcuts, setShortcuts] = reactExports.useState(() => {
    const saved = localStorage.getItem("jee_browser_shortcuts");
    return saved ? JSON.parse(saved) : DEFAULT_SHORTCUTS;
  });
  const [showAddShortcut, setShowAddShortcut] = reactExports.useState(false);
  const [newShortcutName, setNewShortcutName] = reactExports.useState("");
  const [newShortcutUrl, setNewShortcutUrl] = reactExports.useState("");
  reactExports.useEffect(() => {
    localStorage.setItem("jee_browser_shortcuts", JSON.stringify(shortcuts));
  }, [shortcuts]);
  reactExports.useEffect(() => {
    const handleResize = () => {
      setIsMobile(window.innerWidth < 768);
    };
    handleResize();
    window.addEventListener("resize", handleResize);
    return () => window.removeEventListener("resize", handleResize);
  }, []);
  const activeTab = tabs.find((t) => t.id === activeTabId) || tabs[0];
  const updateActiveTab = (updates) => {
    setTabs((prevTabs) => prevTabs.map((t) => t.id === activeTabId ? { ...t, ...updates } : t));
  };
  const handleAddShortcut = () => {
    if (!newShortcutName.trim() || !newShortcutUrl.trim()) return;
    let url = newShortcutUrl.trim();
    if (!/^https?:\/\//i.test(url)) {
      url = "https://" + url;
    }
    const newShortcut = {
      id: Date.now().toString(),
      name: newShortcutName.trim(),
      url
    };
    setShortcuts([...shortcuts, newShortcut]);
    setNewShortcutName("");
    setNewShortcutUrl("");
    setShowAddShortcut(false);
  };
  const handleRemoveShortcut = (id, e) => {
    e.stopPropagation();
    setShortcuts(shortcuts.filter((s) => s.id !== id));
  };
  const createNewTab = (url = "") => {
    const newId = Date.now().toString();
    const newTab = {
      id: newId,
      title: url ? url.replace(/^https?:\/\/(www\.)?/i, "") : "New Tab",
      url,
      browserMode: url ? "browse" : "newtab",
      query: "",
      searchResults: [],
      iframeUrl: url ? `/api/proxy?url=${encodeURIComponent(url)}` : "",
      history: [url],
      historyIndex: 0,
      readerMode: false,
      scrapedData: null,
      loading: false
    };
    setTabs([...tabs, newTab]);
    setActiveTabId(newId);
    setShowTabSwitcherMobile(false);
  };
  const closeTab = (tabId, e) => {
    if (e) e.stopPropagation();
    if (tabs.length === 1) {
      setTabs([
        {
          id: "initial-tab",
          title: "New Tab",
          url: "",
          browserMode: "newtab",
          query: "",
          searchResults: [],
          iframeUrl: "",
          history: [""],
          historyIndex: 0,
          readerMode: false,
          scrapedData: null,
          loading: false
        }
      ]);
      setActiveTabId("initial-tab");
      return;
    }
    const index2 = tabs.findIndex((t) => t.id === tabId);
    const newTabs = tabs.filter((t) => t.id !== tabId);
    setTabs(newTabs);
    if (activeTabId === tabId) {
      const nextActiveIndex = Math.max(0, index2 - 1);
      setActiveTabId(newTabs[nextActiveIndex].id);
    }
  };
  const navigateTo = (url, addToHistory = true) => {
    let target = url.trim();
    if (!target) return;
    const isUrl2 = /^(https?:\/\/)?([\da-z.-]+)\.([a-z.]{2,6})([/\w .-]*)*\/?$/i.test(target) || target.includes("localhost") || target.startsWith("http://") || target.startsWith("https://");
    if (!isUrl2) {
      handleSearch(target);
      return;
    }
    if (!/^https?:\/\//i.test(target)) {
      target = "https://" + target;
    }
    let newHistory = activeTab.history;
    let newIndex = activeTab.historyIndex;
    if (addToHistory) {
      newHistory = activeTab.history.slice(0, activeTab.historyIndex + 1);
      newHistory.push(target);
      newIndex = newHistory.length - 1;
    }
    updateActiveTab({
      url: target,
      title: target.replace(/^https?:\/\/(www\.)?/i, ""),
      browserMode: "browse",
      iframeUrl: `/api/proxy?url=${encodeURIComponent(target)}`,
      history: newHistory,
      historyIndex: newIndex,
      readerMode: false,
      // reset reader mode on navigate
      scrapedData: null,
      loading: false
    });
  };
  const handleSearch = async (searchQuery) => {
    if (!searchQuery.trim()) return;
    let newHistory = activeTab.history.slice(0, activeTab.historyIndex + 1);
    const searchUrlLabel = `Search: ${searchQuery}`;
    newHistory.push(searchUrlLabel);
    updateActiveTab({
      loading: true,
      query: searchQuery,
      url: searchUrlLabel,
      title: searchQuery,
      browserMode: "search",
      history: newHistory,
      historyIndex: newHistory.length - 1,
      readerMode: false
    });
    try {
      const res = await fetch(`/api/search?q=${encodeURIComponent(searchQuery)}`);
      if (res.ok) {
        const data = await res.json();
        setTabs(
          (prevTabs) => prevTabs.map(
            (t) => t.id === activeTabId ? { ...t, searchResults: data.results || [], loading: false } : t
          )
        );
      } else {
        setTabs(
          (prevTabs) => prevTabs.map(
            (t) => t.id === activeTabId ? { ...t, searchResults: [], loading: false } : t
          )
        );
      }
    } catch (e) {
      console.error("Search API call failed:", e);
      setTabs(
        (prevTabs) => prevTabs.map(
          (t) => t.id === activeTabId ? { ...t, searchResults: [], loading: false } : t
        )
      );
    }
  };
  const handleGoBack = () => {
    if (activeTab.historyIndex > 0) {
      const prevIndex = activeTab.historyIndex - 1;
      const prevUrl = activeTab.history[prevIndex];
      if (prevUrl === "") {
        updateActiveTab({
          browserMode: "newtab",
          url: "",
          title: "New Tab",
          historyIndex: prevIndex,
          readerMode: false,
          scrapedData: null
        });
      } else if (prevUrl.startsWith("Search: ")) {
        const q = prevUrl.replace("Search: ", "");
        updateActiveTab({
          browserMode: "search",
          url: prevUrl,
          title: q,
          query: q,
          historyIndex: prevIndex,
          readerMode: false,
          scrapedData: null,
          loading: true
        });
        fetch(`/api/search?q=${encodeURIComponent(q)}`).then((res) => res.ok ? res.json() : { results: [] }).then((data) => {
          setTabs(
            (prevTabs) => prevTabs.map(
              (t) => t.id === activeTabId ? { ...t, searchResults: data.results || [], loading: false } : t
            )
          );
        });
      } else {
        updateActiveTab({
          browserMode: "browse",
          url: prevUrl,
          title: prevUrl.replace(/^https?:\/\/(www\.)?/i, ""),
          iframeUrl: `/api/proxy?url=${encodeURIComponent(prevUrl)}`,
          historyIndex: prevIndex,
          readerMode: false,
          scrapedData: null
        });
      }
    }
  };
  const handleGoForward = () => {
    if (activeTab.historyIndex < activeTab.history.length - 1) {
      const nextIndex = activeTab.historyIndex + 1;
      const nextUrl = activeTab.history[nextIndex];
      if (nextUrl === "") {
        updateActiveTab({
          browserMode: "newtab",
          url: "",
          title: "New Tab",
          historyIndex: nextIndex,
          readerMode: false,
          scrapedData: null
        });
      } else if (nextUrl.startsWith("Search: ")) {
        const q = nextUrl.replace("Search: ", "");
        updateActiveTab({
          browserMode: "search",
          url: nextUrl,
          title: q,
          query: q,
          historyIndex: nextIndex,
          readerMode: false,
          scrapedData: null,
          loading: true
        });
        fetch(`/api/search?q=${encodeURIComponent(q)}`).then((res) => res.ok ? res.json() : { results: [] }).then((data) => {
          setTabs(
            (prevTabs) => prevTabs.map(
              (t) => t.id === activeTabId ? { ...t, searchResults: data.results || [], loading: false } : t
            )
          );
        });
      } else {
        updateActiveTab({
          browserMode: "browse",
          url: nextUrl,
          title: nextUrl.replace(/^https?:\/\/(www\.)?/i, ""),
          iframeUrl: `/api/proxy?url=${encodeURIComponent(nextUrl)}`,
          historyIndex: nextIndex,
          readerMode: false,
          scrapedData: null
        });
      }
    }
  };
  const handleReload = () => {
    if (activeTab.readerMode) {
      toggleReaderMode(true);
    } else if (iframeRef.current) {
      iframeRef.current.src = iframeRef.current.src;
    }
  };
  const handleGoHome = () => {
    const newHistory = activeTab.history.slice(0, activeTab.historyIndex + 1);
    newHistory.push("");
    updateActiveTab({
      browserMode: "newtab",
      url: "",
      title: "New Tab",
      query: "",
      searchResults: [],
      iframeUrl: "",
      history: newHistory,
      historyIndex: newHistory.length - 1,
      readerMode: false,
      scrapedData: null
    });
  };
  const toggleReaderMode = async (forceReload = false) => {
    const shouldActivate = !activeTab.readerMode || forceReload;
    if (shouldActivate) {
      updateActiveTab({ readerMode: true, loading: true });
      try {
        const res = await fetch(
          `/api/scrape?url=${encodeURIComponent(activeTab.url)}`
        );
        if (res.ok) {
          const data = await res.json();
          setTabs(
            (prevTabs) => prevTabs.map(
              (t) => t.id === activeTabId ? { ...t, scrapedData: data, loading: false } : t
            )
          );
        } else {
          setTabs(
            (prevTabs) => prevTabs.map(
              (t) => t.id === activeTabId ? {
                ...t,
                loading: false,
                scrapedData: { error: "Failed to scrape page content." }
              } : t
            )
          );
        }
      } catch (e) {
        setTabs(
          (prevTabs) => prevTabs.map(
            (t) => t.id === activeTabId ? {
              ...t,
              loading: false,
              scrapedData: { error: "Could not fetch readable text format." }
            } : t
          )
        );
      }
    } else {
      updateActiveTab({ readerMode: false });
    }
  };
  const renderKnowledgePanel = (query, results) => {
    const title = results[0]?.title ? results[0].title.split(" - ")[0].split(" | ")[0] : query;
    let subtitle = "Search Topic";
    const qLower = query.toLowerCase();
    if (qLower.includes("mrbeast") || qLower.includes("mr beast")) {
      subtitle = "American YouTuber";
    } else if (qLower.includes("wikipedia") || qLower.includes("wiki")) {
      subtitle = "Online Encyclopedia";
    } else if (qLower.includes("physics") || qLower.includes("chemistry") || qLower.includes("math")) {
      subtitle = "Academic Subject";
    } else if (results[0]?.url) {
      try {
        subtitle = new URL(results[0].url).hostname.replace("www.", "");
      } catch (e) {
      }
    }
    const desc = results[0]?.snippet || "Information collected from indexed resources regarding the search query.";
    const images = results.map((r) => r.thumbnail).filter((t) => t && t.startsWith("http"));
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "border border-border bg-card p-5 rounded-2xl shadow-sm space-y-4 max-w-sm w-full sticky top-4 font-sans select-text", children: [
      images.length > 0 ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex gap-2 overflow-x-auto scrollbar-none rounded-xl h-36", children: images.map((img, idx) => /* @__PURE__ */ jsxRuntimeExports.jsx(
        "img",
        {
          src: img,
          alt: "thumbnail",
          className: "h-full object-cover rounded-lg shrink-0 w-36 border border-border/30 hover:scale-[1.02] transition-transform duration-200"
        },
        idx
      )) }) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "h-32 rounded-xl bg-gradient-to-br from-indigo-500/10 to-purple-500/10 flex items-center justify-center border border-indigo-500/10", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Sparkles, { className: "w-8 h-8 text-indigo-500/35" }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "text-xl font-bold tracking-tight text-foreground", children: title }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground mt-0.5", children: subtitle })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex gap-2 flex-wrap", children: [
        results[0] && /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            onClick: () => navigateTo(results[0].url, true),
            className: "px-3.5 py-1.5 rounded-full text-xs font-semibold bg-primary text-primary-foreground hover:bg-primary/90 transition-colors cursor-pointer shadow-sm",
            children: "Overview"
          }
        ),
        results[1] && /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            onClick: () => navigateTo(results[1].url, true),
            className: "px-3.5 py-1.5 rounded-full text-xs font-semibold bg-muted hover:bg-muted/80 text-foreground transition-colors cursor-pointer border border-border/50",
            children: "Alternative link"
          }
        )
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "border-t border-border/60 pt-4 space-y-2", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("h4", { className: "text-xs font-bold text-muted-foreground uppercase tracking-wider", children: "About" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs leading-relaxed text-foreground/90 font-light line-clamp-6", children: desc }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2.5 pt-2 flex-wrap", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[10px] font-bold text-muted-foreground uppercase", children: "Profiles:" }),
          results.slice(0, 4).map((r, idx) => {
            let label = "Website";
            if (r.url.includes("youtube.com")) label = "YouTube";
            else if (r.url.includes("wikipedia.org")) label = "Wikipedia";
            else if (r.url.includes("instagram.com")) label = "Instagram";
            else if (r.url.includes("twitter.com")) label = "Twitter";
            else if (r.url.includes("github.com")) label = "GitHub";
            return /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "button",
              {
                onClick: () => navigateTo(r.url, true),
                className: "px-2 py-0.5 rounded border border-border text-[9px] hover:bg-muted text-muted-foreground hover:text-foreground font-semibold flex items-center gap-1 transition-colors",
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(Globe, { className: "w-2.5 h-2.5" }),
                  label
                ]
              },
              idx
            );
          })
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "border-t border-border/60 pt-4 text-xs space-y-2.5", children: qLower.includes("mrbeast") || qLower.includes("mr beast") ? /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between items-center py-0.5 border-b border-border/20", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-muted-foreground", children: "Born:" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-medium text-foreground text-right", children: "7 May 1998 (age 28 years), Wichita, KS" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between items-center py-0.5 border-b border-border/20", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-muted-foreground", children: "Full name:" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-medium text-foreground text-right", children: "James Stephen Donaldson" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between items-center py-0.5 border-b border-border/20", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-muted-foreground", children: "Height:" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-medium text-foreground text-right", children: "1.96 m" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between items-center py-0.5", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-muted-foreground", children: "YouTube Channel:" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-semibold text-red-500 hover:underline cursor-pointer", onClick: () => navigateTo("https://www.youtube.com/@MrBeast"), children: "MrBeast (50.3Cr+ followers)" })
        ] })
      ] }) : /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between items-center py-0.5 border-b border-border/20", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-muted-foreground", children: "Domain:" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-medium text-foreground truncate max-w-[200px]", children: results[0]?.url ? new URL(results[0].url).hostname : "N/A" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between items-center py-0.5 border-b border-border/20", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-muted-foreground", children: "Category:" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-medium text-foreground", children: subtitle })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between items-center py-0.5 border-b border-border/20", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-muted-foreground", children: "Search Source:" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-medium text-foreground", children: "Strome Web indexing" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between items-center py-0.5", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-muted-foreground", children: "Safety status:" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "font-medium text-green-500 flex items-center gap-0.5", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Lock, { className: "w-3 h-3" }),
            " Secure proxy"
          ] })
        ] })
      ] }) })
    ] });
  };
  const renderPeopleAlsoAsk = (query) => {
    const questions = [
      {
        q: `What is the primary topic of ${query}?`,
        a: `${query} refers to the popular search subject currently queried on the Strome Browser. You can load direct pages from the results to explore comprehensive textbooks, guides, and academic papers.`
      },
      {
        q: `How do I view websites directly for ${query}?`,
        a: `Simply click any of the blue page titles in the search results list. Strome will automatically route the site through a secure local proxy to bypass browser iframe blockers (X-Frame-Options/CORS) and load it natively.`
      },
      {
        q: `Is ${query} related information secure?`,
        a: `Yes! All websites loaded via the Strome Search engine run through a sandboxed local proxy node (0.0.0.0), stripping tracking cookies and frame-blocking scripts while keeping your main browsing session protected.`
      }
    ];
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "border border-border/60 rounded-2xl p-4 bg-muted/10 space-y-3 font-sans w-full", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("h4", { className: "text-sm font-semibold text-foreground", children: "People also ask" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "divide-y divide-border/50", children: questions.map((item, idx) => /* @__PURE__ */ jsxRuntimeExports.jsxs("details", { className: "group py-2.5 outline-none cursor-pointer", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("summary", { className: "flex justify-between items-center text-xs font-medium text-foreground/90 hover:text-primary transition-colors select-none list-none [&::-webkit-details-marker]:hidden", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: item.q }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-muted-foreground transition-transform group-open:rotate-180 text-[9px]", children: "▼" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground/80 mt-2 leading-relaxed pl-1 font-light cursor-text", children: item.a })
      ] }, idx)) })
    ] });
  };
  const renderSearchResultsView = (tab2) => {
    if (tab2.loading) {
      return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col items-center justify-center h-full p-8 text-center bg-background", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(RotateCw, { className: "w-8 h-8 text-primary animate-spin mb-3" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-sm text-muted-foreground animate-pulse", children: "Strome indexing educational resources..." })
      ] });
    }
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "h-full overflow-y-auto bg-background text-foreground select-text pb-12", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "border-b border-border/60 bg-muted/15 px-4 md:px-36 py-2.5 flex items-center gap-6 text-xs text-muted-foreground overflow-x-auto shrink-0 scrollbar-none select-none", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-primary font-bold border-b-2 border-primary pb-2 px-1 cursor-default shrink-0 flex items-center gap-1.5", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Search, { className: "w-3.5 h-3.5" }),
          " All"
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "pb-2 hover:text-foreground cursor-pointer shrink-0 flex items-center gap-1.5", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Image$1, { className: "w-3.5 h-3.5 text-muted-foreground/80" }),
          " Images"
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "pb-2 hover:text-foreground cursor-pointer shrink-0 flex items-center gap-1.5", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Play, { className: "w-3.5 h-3.5 text-muted-foreground/80" }),
          " Videos"
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "pb-2 hover:text-foreground cursor-pointer shrink-0 flex items-center gap-1.5", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Newspaper, { className: "w-3.5 h-3.5 text-muted-foreground/80" }),
          " News"
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "pb-2 hover:text-foreground cursor-pointer shrink-0 flex items-center gap-1.5", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(ShoppingBag, { className: "w-3.5 h-3.5 text-muted-foreground/80" }),
          " Shopping"
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "pb-2 hover:text-foreground cursor-pointer shrink-0 flex items-center gap-1.5", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(EllipsisVertical, { className: "w-3.5 h-3.5 text-muted-foreground/80" }),
          " More"
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "max-w-7xl mx-auto px-4 md:px-36 py-4 flex flex-col md:flex-row gap-8 items-start", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 w-full space-y-6", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-xs text-muted-foreground font-light", children: [
            "About ",
            tab2.searchResults.length,
            " results (",
            (tab2.searchResults.length * 28 / 1e3).toFixed(2),
            " seconds)"
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "p-5 rounded-2xl border border-indigo-500/20 bg-indigo-500/5 dark:bg-indigo-950/20 shadow-sm relative overflow-hidden", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute right-0 top-0 w-24 h-24 bg-indigo-500/10 rounded-full blur-2xl pointer-events-none" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 mb-2", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Sparkles, { className: "w-4 h-4 text-indigo-500 dark:text-indigo-400" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("h4", { className: "text-xs font-bold text-indigo-500 dark:text-indigo-400 uppercase tracking-wider", children: "Strome Answer" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-sm leading-relaxed text-foreground/90 font-sans", children: [
              "Here are search results for ",
              /* @__PURE__ */ jsxRuntimeExports.jsxs("strong", { className: "font-semibold text-indigo-500", children: [
                '"',
                tab2.query,
                '"'
              ] }),
              ". Clicking a title will open the website. If a site fails to render due to iframe blockades, click the ",
              /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { className: "font-semibold text-indigo-500", children: "Reader Mode" }),
              " book icon at the top of the browser to view clean scraped text."
            ] })
          ] }),
          tab2.searchResults.length > 0 && renderPeopleAlsoAsk(tab2.query),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "space-y-6 pt-2", children: tab2.searchResults.length === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "py-12 text-center text-muted-foreground", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Globe, { className: "w-12 h-12 text-muted-foreground/35 mx-auto mb-3" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "font-bold text-sm", children: "No indexed pages found" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs mt-1", children: "Try typing a direct website URL or changing your search terms." })
          ] }) : tab2.searchResults.map((item, idx) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "group space-y-1.5 max-w-2xl select-text", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 text-xs text-muted-foreground/80 truncate", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-4.5 h-4.5 rounded-full bg-muted border border-border flex items-center justify-center text-[9px] font-bold text-foreground", children: item.url ? new URL(item.url).hostname.charAt(0).toUpperCase() : "?" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "span",
                {
                  className: "truncate hover:underline cursor-pointer font-sans",
                  onClick: () => navigateTo(item.url, true),
                  children: item.url
                }
              )
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "text-lg md:text-xl font-medium text-blue-600 dark:text-[#8ab4f8] group-hover:underline cursor-pointer leading-tight", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
              "button",
              {
                onClick: () => navigateTo(item.url, true),
                className: "text-left font-sans font-normal",
                children: item.title
              }
            ) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm text-muted-foreground/90 leading-relaxed font-sans font-light line-clamp-3", children: item.snippet })
          ] }, idx)) })
        ] }),
        tab2.searchResults.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-full md:w-80 lg:w-96 shrink-0 md:sticky md:top-4", children: renderKnowledgePanel(tab2.query, tab2.searchResults) })
      ] })
    ] });
  };
  const renderReaderView = (tab2) => {
    if (tab2.loading) {
      return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col items-center justify-center h-full p-8 text-center bg-card", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(RotateCw, { className: "w-8 h-8 text-primary animate-spin mb-3" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-sm text-muted-foreground animate-pulse", children: "Extracting main web article text and images..." })
      ] });
    }
    const data = tab2.scrapedData;
    if (!data || data.error) {
      return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col items-center justify-center h-full p-8 text-center bg-card", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(BookOpen, { className: "w-12 h-12 text-destructive mb-3 opacity-60" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "text-base font-bold mb-1", children: "Reader Mode Unavailable" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground max-w-sm mb-4", children: data?.error || "We couldn't scrape readable content from this site. Standard Web View might work." }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: () => updateActiveTab({ readerMode: false }), children: "Switch to Web View" })
      ] });
    }
    return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "h-full overflow-y-auto bg-[#fafafa] dark:bg-[#18181c] text-[#333] dark:text-[#e3e3e6] transition-colors p-6 md:p-12", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "max-w-2xl mx-auto space-y-6 select-text pb-12", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between border-b border-border/80 pb-3 mb-6", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 text-xs text-muted-foreground font-sans", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(BookOpen, { className: "w-4 h-4 text-primary" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Reader Mode Active" })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          Button,
          {
            variant: "outline",
            size: "sm",
            onClick: () => updateActiveTab({ readerMode: false }),
            className: "h-7 text-xs gap-1.5",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Globe, { className: "w-3.5 h-3.5" }),
              " Web View"
            ]
          }
        )
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("h1", { className: "text-3xl md:text-4.5xl font-extrabold leading-tight tracking-tight font-serif text-foreground", children: data.title || tab2.title }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "text-xs text-muted-foreground font-mono", children: [
        "Origin:",
        " ",
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "a",
          {
            href: tab2.url,
            target: "_blank",
            rel: "noopener noreferrer",
            className: "text-primary hover:underline",
            children: tab2.url
          }
        )
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "font-serif leading-relaxed text-base md:text-lg space-y-5 pt-5 border-t border-border/40 text-foreground/90", children: data.text ? data.text.split("\n\n").map((para, idx) => {
        const trimmed = para.trim();
        if (!trimmed) return null;
        if (trimmed.startsWith("###")) {
          return /* @__PURE__ */ jsxRuntimeExports.jsx(
            "h3",
            {
              className: "text-lg font-bold pt-4 text-foreground font-sans",
              children: trimmed.replace("###", "")
            },
            idx
          );
        } else if (trimmed.startsWith("##")) {
          return /* @__PURE__ */ jsxRuntimeExports.jsx(
            "h2",
            {
              className: "text-xl font-bold pt-6 text-foreground font-sans",
              children: trimmed.replace("##", "")
            },
            idx
          );
        } else if (trimmed.startsWith("#")) {
          return /* @__PURE__ */ jsxRuntimeExports.jsx(
            "h1",
            {
              className: "text-2xl font-bold pt-8 text-foreground font-sans",
              children: trimmed.replace("#", "")
            },
            idx
          );
        }
        return /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "indent-0 md:indent-4", children: trimmed }, idx);
      }) : /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-muted-foreground italic", children: "No article text detected." }) }),
      data.links && data.links.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "pt-8 mt-8 border-t border-border/80 space-y-3", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "text-xs font-semibold text-muted-foreground uppercase tracking-wider font-sans", children: "Extracted Links" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "grid grid-cols-1 md:grid-cols-2 gap-2", children: data.links.map((link2, idx) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "button",
          {
            onClick: () => navigateTo(link2.url, true),
            className: "flex items-start gap-2 p-2.5 rounded-xl border border-border/40 bg-card hover:bg-muted text-left text-xs transition-colors",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(ExternalLink, { className: "w-3.5 h-3.5 text-muted-foreground shrink-0 mt-0.5" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "min-w-0 flex-1", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "font-semibold text-foreground truncate", children: link2.text || "Untitled Hyperlink" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-muted-foreground/60 truncate font-mono text-[9px]", children: link2.url })
              ] })
            ]
          },
          idx
        )) })
      ] })
    ] }) });
  };
  const renderNTP = (tab2) => {
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "absolute inset-0 flex flex-col items-center justify-center p-6 bg-background overflow-y-auto select-none", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mb-8 flex items-center justify-center font-bold tracking-tight text-5.5xl md:text-7xl select-none font-sans drop-shadow-sm", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[#4285F4]", children: "S" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[#EA4335]", children: "t" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[#FBBC05]", children: "r" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[#4285F4]", children: "o" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[#34A853]", children: "m" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[#EA4335]", children: "e" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "w-full max-w-xl mb-9 relative", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Search, { className: "absolute left-4.5 top-3.5 h-5 w-5 text-muted-foreground" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "input",
          {
            type: "text",
            value: tab2.query,
            onChange: (e) => updateActiveTab({ query: e.target.value }),
            onKeyDown: (e) => e.key === "Enter" && handleSearch(tab2.query),
            placeholder: "Search Google or enter web URL",
            className: "h-12 pl-12 pr-12 rounded-full border border-border bg-card shadow-sm hover:shadow-md focus:shadow-md focus:outline-none focus:ring-1 focus:ring-primary text-sm font-normal w-full transition-all text-foreground font-sans"
          }
        ),
        tab2.query.trim().length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            onClick: () => updateActiveTab({ query: "" }),
            className: "absolute right-4.5 top-3.5 text-muted-foreground hover:text-foreground",
            children: /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "h-5 w-5" })
          }
        )
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "w-full max-w-xl flex flex-wrap gap-5 justify-center", children: [
        shortcuts.map((shortcut) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "div",
          {
            onClick: () => navigateTo(shortcut.url),
            className: "group w-20 h-20 flex flex-col items-center justify-center bg-card hover:bg-accent/40 border border-border/40 hover:border-border rounded-2xl cursor-pointer p-2 relative shadow-sm hover:shadow-md transition-all duration-150",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "button",
                {
                  onClick: (e) => handleRemoveShortcut(shortcut.id, e),
                  className: "absolute top-1 right-1 p-0.5 rounded-full bg-muted/80 text-muted-foreground hover:text-destructive opacity-0 group-hover:opacity-100 transition-opacity z-10",
                  children: /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "w-3.5 h-3.5" })
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-9 h-9 rounded-full bg-accent text-accent-foreground font-semibold text-xs flex items-center justify-center mb-1.5 shadow-inner group-hover:scale-105 transition-transform duration-200", children: shortcut.name.charAt(0).toUpperCase() }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[10px] font-medium text-center truncate w-full text-foreground/80 group-hover:text-foreground", children: shortcut.name })
            ]
          },
          shortcut.id
        )),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "button",
          {
            onClick: () => setShowAddShortcut(true),
            className: "w-20 h-20 flex flex-col items-center justify-center bg-muted/20 hover:bg-muted/40 border border-dashed border-border rounded-2xl cursor-pointer p-2 transition-all duration-150 text-muted-foreground hover:text-foreground",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-9 h-9 rounded-full bg-card border border-border flex items-center justify-center mb-1.5 shadow-sm", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Plus, { className: "w-4 h-4" }) }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[10px] font-medium text-center", children: "Add shortcut" })
            ]
          }
        )
      ] })
    ] });
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "div",
    {
      className: cn(
        "w-full h-full flex flex-col bg-background text-foreground select-none relative overflow-hidden w-full h-full",
        fullscreen && "fixed inset-0 z-[250]"
      ),
      children: [
        !isMobile && /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "h-10 bg-[#dee1e6] dark:bg-[#1e1f22] border-b border-border/80 flex items-end px-3 justify-between gap-4 flex-shrink-0 z-20", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-end gap-1 overflow-x-auto scrollbar-none flex-1 max-w-[85%]", children: [
              onBack && /* @__PURE__ */ jsxRuntimeExports.jsxs(
                Button,
                {
                  variant: "ghost",
                  size: "sm",
                  onClick: onBack,
                  className: "h-8 px-2 font-bold text-xs gap-1 text-muted-foreground hover:text-foreground hover:bg-muted/50 rounded-lg mb-1 shrink-0 transition-colors",
                  children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(ArrowLeft, { className: "h-3.5 w-3.5" }),
                    " Back"
                  ]
                }
              ),
              tabs.map((tab2) => {
                const isTabActive = tab2.id === activeTabId;
                return /* @__PURE__ */ jsxRuntimeExports.jsxs(
                  "div",
                  {
                    onClick: () => {
                      setActiveTabId(tab2.id);
                      setShowTabSwitcherMobile(false);
                    },
                    className: cn(
                      "relative h-[32px] px-3.5 flex items-center gap-2 text-xs font-normal cursor-pointer select-none transition-all duration-100 truncate group border-r border-[#cfd2d6] dark:border-border/30 rounded-t-lg",
                      isTabActive ? "bg-background text-foreground border-t border-x border-border/60 shadow-sm z-10" : "text-muted-foreground hover:bg-muted/50 hover:text-foreground"
                    ),
                    style: { minWidth: "120px", maxWidth: "170px" },
                    children: [
                      tab2.browserMode === "newtab" ? /* @__PURE__ */ jsxRuntimeExports.jsx(Globe, { className: "w-3.5 h-3.5 text-muted-foreground shrink-0" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(FileText, { className: "w-3.5 h-3.5 text-muted-foreground shrink-0" }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "truncate flex-1 font-medium select-none", children: tab2.title }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx(
                        "button",
                        {
                          onClick: (e) => closeTab(tab2.id, e),
                          className: "p-0.5 rounded-full hover:bg-muted-foreground/20 opacity-0 group-hover:opacity-100 transition-opacity ml-1.5 shrink-0",
                          children: /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "w-3 h-3 text-muted-foreground" })
                        }
                      )
                    ]
                  },
                  tab2.id
                );
              }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "button",
                {
                  onClick: () => createNewTab(),
                  className: "h-7 w-7 rounded-full flex items-center justify-center hover:bg-muted-foreground/15 mb-1 shrink-0 text-muted-foreground hover:text-foreground transition-colors",
                  children: /* @__PURE__ */ jsxRuntimeExports.jsx(Plus, { className: "w-4 h-4" })
                }
              )
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-1.5 mb-2 shrink-0", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "div",
                {
                  className: "w-3 h-3 rounded-full bg-red-500/80 hover:bg-red-600 transition-colors cursor-pointer",
                  onClick: onBack,
                  title: "Exit Browser"
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "div",
                {
                  className: "w-3 h-3 rounded-full bg-yellow-500/80 hover:bg-yellow-600 transition-colors cursor-pointer",
                  onClick: () => createNewTab(),
                  title: "Minimize / New Tab"
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "div",
                {
                  className: "w-3 h-3 rounded-full bg-green-500/80 hover:bg-green-600 transition-colors cursor-pointer",
                  onClick: () => setFullscreen(!fullscreen),
                  title: "Fullscreen Toggle"
                }
              )
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "h-11 bg-background border-b border-border/80 flex items-center px-3.5 justify-between gap-3 flex-shrink-0 z-20", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-1", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                Button,
                {
                  variant: "ghost",
                  size: "icon",
                  className: "h-7 w-7 text-muted-foreground",
                  onClick: handleGoBack,
                  disabled: activeTab.historyIndex <= 0,
                  children: /* @__PURE__ */ jsxRuntimeExports.jsx(ArrowLeft, { className: "h-4 w-4" })
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                Button,
                {
                  variant: "ghost",
                  size: "icon",
                  className: "h-7 w-7 text-muted-foreground",
                  onClick: handleGoForward,
                  disabled: activeTab.historyIndex >= activeTab.history.length - 1,
                  children: /* @__PURE__ */ jsxRuntimeExports.jsx(ArrowRight, { className: "h-4 w-4" })
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                Button,
                {
                  variant: "ghost",
                  size: "icon",
                  className: "h-7 w-7 text-muted-foreground",
                  onClick: handleReload,
                  disabled: activeTab.browserMode === "newtab",
                  children: /* @__PURE__ */ jsxRuntimeExports.jsx(RotateCw, { className: "h-3.5 w-3.5" })
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                Button,
                {
                  variant: "ghost",
                  size: "icon",
                  className: "h-7 w-7 text-muted-foreground",
                  onClick: handleGoHome,
                  children: /* @__PURE__ */ jsxRuntimeExports.jsx(House, { className: "h-4 w-4" })
                }
              )
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 max-w-3xl flex items-center relative", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "button",
                {
                  onClick: () => setShowInfoDropdown(!showInfoDropdown),
                  className: "absolute left-3 p-1 rounded hover:bg-muted text-muted-foreground shrink-0 z-10",
                  children: /* @__PURE__ */ jsxRuntimeExports.jsx(Lock, { className: "h-3.5 w-3.5" })
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "input",
                {
                  type: "text",
                  value: activeTab.browserMode === "newtab" ? activeTab.query : activeTab.query || activeTab.url,
                  onChange: (e) => {
                    updateActiveTab({ query: e.target.value });
                    if (activeTab.browserMode === "newtab") {
                      updateActiveTab({ url: e.target.value });
                    }
                  },
                  onKeyDown: (e) => e.key === "Enter" && navigateTo(activeTab.query || activeTab.url),
                  placeholder: "Search Google or enter web URL",
                  className: "h-8 pl-9 pr-24 bg-[#f1f3f4] dark:bg-[#202124] border border-transparent rounded-full focus-visible:ring-1 focus-visible:ring-primary w-full text-xs font-normal shadow-sm focus:outline-none focus:ring-0 text-foreground font-sans"
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "absolute right-3 flex items-center gap-1.5", children: [
                activeTab.loading ? /* @__PURE__ */ jsxRuntimeExports.jsx(RotateCw, { className: "w-3.5 h-3.5 text-muted-foreground animate-spin" }) : activeTab.browserMode !== "newtab" ? /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "button",
                  {
                    onClick: () => toggleReaderMode(),
                    className: "p-1 rounded hover:bg-muted text-muted-foreground hover:text-primary transition-colors",
                    title: activeTab.readerMode ? "Switch to Web View" : "Switch to Reader Mode",
                    children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                      BookOpen,
                      {
                        className: cn(
                          "w-4 h-4",
                          activeTab.readerMode && "text-primary fill-primary/10"
                        )
                      }
                    )
                  }
                ) : null,
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "button",
                  {
                    onClick: () => {
                      if (activeTab.browserMode !== "newtab") {
                        const exists = shortcuts.find(
                          (s) => s.url === activeTab.url
                        );
                        if (!exists) {
                          setShortcuts([
                            ...shortcuts,
                            {
                              id: Date.now().toString(),
                              name: activeTab.title,
                              url: activeTab.url
                            }
                          ]);
                        }
                      }
                    },
                    className: "p-1 rounded hover:bg-muted text-muted-foreground hover:text-yellow-500 transition-colors",
                    title: "Bookmark this page",
                    children: /* @__PURE__ */ jsxRuntimeExports.jsx(Star, { className: "w-4 h-4" })
                  }
                )
              ] }),
              showInfoDropdown && /* @__PURE__ */ jsxRuntimeExports.jsxs(
                "div",
                {
                  className: "absolute left-0 top-10 z-[300] bg-card border border-border rounded-xl shadow-2xl p-4 w-72 flex flex-col gap-2 font-sans",
                  onClick: (e) => e.stopPropagation(),
                  children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-start gap-2.5 pb-2 border-b border-border/40", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx(Lock, { className: "w-4 h-4 text-green-500 shrink-0 mt-0.5" }),
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsx("h4", { className: "font-semibold text-xs text-foreground", children: "Connection is secure" }),
                        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-[10px] text-muted-foreground", children: "Your information is private when loaded through the Strome Proxy." })
                      ] })
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "text-[10px] text-muted-foreground/80 flex flex-col gap-1 mt-1", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "URL:" }),
                        " ",
                        activeTab.url || "New Tab"
                      ] }),
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsx("strong", { children: "Proxy Node:" }),
                        " 0.0.0.0 (Local Sandbox)"
                      ] })
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx(
                      Button,
                      {
                        variant: "outline",
                        size: "sm",
                        className: "h-7 text-[10px] mt-1.5 rounded-lg",
                        onClick: () => setShowInfoDropdown(false),
                        children: "Close info"
                      }
                    )
                  ]
                }
              )
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-1.5 relative", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "ghost", size: "icon", className: "h-7 w-7 text-muted-foreground", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Sparkles, { className: "h-4 w-4" }) }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "ghost", size: "icon", className: "h-7 w-7 text-muted-foreground", children: /* @__PURE__ */ jsxRuntimeExports.jsx(User, { className: "h-4 w-4" }) }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                Button,
                {
                  variant: "ghost",
                  size: "icon",
                  className: "h-7 w-7 text-muted-foreground",
                  onClick: () => setShowMenuDropdown(!showMenuDropdown),
                  children: /* @__PURE__ */ jsxRuntimeExports.jsx(EllipsisVertical, { className: "h-4 w-4" })
                }
              ),
              showMenuDropdown && /* @__PURE__ */ jsxRuntimeExports.jsxs(
                "div",
                {
                  className: "absolute right-0 top-8 z-[300] bg-card border border-border rounded-xl shadow-2xl py-1.5 w-52 flex flex-col font-sans text-xs",
                  onClick: (e) => e.stopPropagation(),
                  children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsxs(
                      "button",
                      {
                        onClick: () => {
                          createNewTab();
                          setShowMenuDropdown(false);
                        },
                        className: "w-full text-left px-4 py-2 hover:bg-muted flex items-center justify-between",
                        children: [
                          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "New tab" }),
                          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[10px] text-muted-foreground", children: "Ctrl+T" })
                        ]
                      }
                    ),
                    activeTab.browserMode !== "newtab" && /* @__PURE__ */ jsxRuntimeExports.jsxs(
                      "button",
                      {
                        onClick: () => {
                          toggleReaderMode();
                          setShowMenuDropdown(false);
                        },
                        className: "w-full text-left px-4 py-2 hover:bg-muted flex items-center justify-between",
                        children: [
                          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Toggle Reader Mode" }),
                          /* @__PURE__ */ jsxRuntimeExports.jsx(BookOpen, { className: "w-3.5 h-3.5 text-muted-foreground" })
                        ]
                      }
                    ),
                    /* @__PURE__ */ jsxRuntimeExports.jsx(
                      "button",
                      {
                        onClick: () => {
                          handleGoHome();
                          setShowMenuDropdown(false);
                        },
                        className: "w-full text-left px-4 py-2 hover:bg-muted",
                        children: "Go to Homepage"
                      }
                    ),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs(
                      "button",
                      {
                        onClick: () => {
                          setFullscreen(!fullscreen);
                          setShowMenuDropdown(false);
                        },
                        className: "w-full text-left px-4 py-2 hover:bg-muted flex items-center justify-between",
                        children: [
                          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Fullscreen" }),
                          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[10px] text-muted-foreground", children: "F11" })
                        ]
                      }
                    ),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("hr", { className: "border-border/40 my-1" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx(
                      "button",
                      {
                        onClick: () => {
                          setTabs(tabs.filter((t) => t.id === activeTabId));
                          setShowMenuDropdown(false);
                        },
                        className: "w-full text-left px-4 py-2 hover:bg-muted text-destructive hover:bg-destructive/5",
                        children: "Close other tabs"
                      }
                    )
                  ]
                }
              )
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "h-8 bg-background border-b border-border/40 px-4 flex items-center gap-4 flex-shrink-0 z-10 select-none text-[11px] text-muted-foreground overflow-x-auto scrollbar-none font-sans font-medium", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "flex items-center gap-1 text-foreground border-r border-border/40 pr-3 mr-1", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Star, { className: "w-3 h-3 text-yellow-500 fill-yellow-500" }),
              " Bookmarks:"
            ] }),
            shortcuts.slice(0, 7).map((s) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "button",
              {
                onClick: () => navigateTo(s.url),
                className: "hover:text-foreground flex items-center gap-1.5 hover:underline shrink-0 max-w-[120px] truncate",
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(Globe, { className: "w-3 h-3 text-muted-foreground/60 shrink-0" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "truncate", children: s.name })
                ]
              },
              s.id
            ))
          ] })
        ] }),
        isMobile && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "h-14 bg-background border-b border-border flex items-center px-3 justify-between gap-2.5 flex-shrink-0 z-20", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "button",
            {
              onClick: handleGoHome,
              className: "p-1 text-muted-foreground hover:text-foreground shrink-0",
              children: /* @__PURE__ */ jsxRuntimeExports.jsx(House, { className: "w-5 h-5" })
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 flex items-center bg-[#f1f3f4] dark:bg-[#202124] rounded-full border border-transparent px-3 py-1.5 gap-2 relative", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Lock, { className: "w-3.5 h-3.5 text-muted-foreground/75 shrink-0" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "input",
              {
                type: "text",
                value: activeTab.browserMode === "newtab" ? activeTab.query : activeTab.query || activeTab.url,
                onChange: (e) => {
                  updateActiveTab({ query: e.target.value });
                  if (activeTab.browserMode === "newtab") {
                    updateActiveTab({ url: e.target.value });
                  }
                },
                onKeyDown: (e) => e.key === "Enter" && navigateTo(activeTab.query || activeTab.url),
                placeholder: "Search or type URL",
                className: "bg-transparent border-none text-xs w-full focus:outline-none focus:ring-0 text-foreground font-normal placeholder:text-muted-foreground/60 p-0 font-sans"
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex items-center gap-2 shrink-0", children: activeTab.loading ? /* @__PURE__ */ jsxRuntimeExports.jsx(RotateCw, { className: "w-3.5 h-3.5 text-muted-foreground animate-spin" }) : activeTab.browserMode !== "newtab" ? /* @__PURE__ */ jsxRuntimeExports.jsx(
              "button",
              {
                onClick: () => toggleReaderMode(),
                className: "text-muted-foreground hover:text-primary",
                children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                  BookOpen,
                  {
                    className: cn(
                      "w-3.5 h-3.5",
                      activeTab.readerMode && "text-primary fill-primary/10"
                    )
                  }
                )
              }
            ) : null })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "button",
            {
              onClick: () => setShowTabSwitcherMobile(!showTabSwitcherMobile),
              className: "w-6 h-6 border-2 border-foreground/60 rounded-md flex items-center justify-center text-xs font-bold text-foreground/80 shrink-0 hover:bg-muted select-none",
              children: tabs.length
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "button",
            {
              onClick: () => setShowMenuDropdown(!showMenuDropdown),
              className: "p-1 text-muted-foreground hover:text-foreground shrink-0",
              children: /* @__PURE__ */ jsxRuntimeExports.jsx(EllipsisVertical, { className: "w-5 h-5" })
            }
          ),
          showMenuDropdown && /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "div",
            {
              className: "absolute right-3 top-12 z-[300] bg-card border border-border rounded-xl shadow-2xl py-1.5 w-48 flex flex-col font-sans text-xs",
              onClick: (e) => e.stopPropagation(),
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs(
                  "button",
                  {
                    onClick: () => {
                      createNewTab();
                      setShowMenuDropdown(false);
                    },
                    className: "w-full text-left px-4 py-2 hover:bg-muted flex items-center justify-between",
                    children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "New tab" }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx(Plus, { className: "w-3.5 h-3.5 text-muted-foreground" })
                    ]
                  }
                ),
                activeTab.browserMode !== "newtab" && /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "button",
                  {
                    onClick: () => {
                      toggleReaderMode();
                      setShowMenuDropdown(false);
                    },
                    className: "w-full text-left px-4 py-2 hover:bg-muted",
                    children: "Reader View"
                  }
                ),
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "button",
                  {
                    onClick: () => {
                      handleGoHome();
                      setShowMenuDropdown(false);
                    },
                    className: "w-full text-left px-4 py-2 hover:bg-muted",
                    children: "Go Home"
                  }
                ),
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "button",
                  {
                    onClick: () => {
                      if (onBack) onBack();
                      setShowMenuDropdown(false);
                    },
                    className: "w-full text-left px-4 py-2 hover:bg-muted border-t border-border/40 text-destructive",
                    children: "Exit Browser"
                  }
                )
              ]
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(AnimatePresence, { children: isMobile && showTabSwitcherMobile && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "absolute inset-0 bg-[#eef1f6] dark:bg-[#111214] z-[999] flex flex-col p-4", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between border-b border-border/60 pb-3 mb-4 shrink-0 font-sans", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Layers, { className: "w-4 h-4 text-primary" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "font-bold text-sm", children: [
                "Open Tabs (",
                tabs.length,
                ")"
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "button",
              {
                onClick: () => setShowTabSwitcherMobile(false),
                className: "p-1 rounded-full bg-card shadow-sm text-muted-foreground hover:text-foreground",
                children: /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "w-5 h-5" })
              }
            )
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex-1 overflow-y-auto grid grid-cols-2 gap-3 pb-20 select-none", children: tabs.map((tab2) => {
            const isTabActive = tab2.id === activeTabId;
            return /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "div",
              {
                onClick: () => {
                  setActiveTabId(tab2.id);
                  setShowTabSwitcherMobile(false);
                },
                className: cn(
                  "p-3 rounded-2xl border border-border bg-card hover:border-primary/50 cursor-pointer flex flex-col justify-between h-32 relative shadow-sm hover:shadow-md transition-all duration-200",
                  isTabActive && "border-2 border-primary ring-1 ring-primary/20"
                ),
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    "button",
                    {
                      onClick: (e) => closeTab(tab2.id, e),
                      className: "absolute top-2 right-2 p-1 rounded-full bg-muted/80 text-muted-foreground hover:text-destructive z-10 shadow-sm",
                      children: /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "w-3.5 h-3.5" })
                    }
                  ),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-1 pr-6 flex-1", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-6 h-6 rounded-full bg-accent flex items-center justify-center text-[10px] font-bold text-accent-foreground shrink-0 shadow-inner", children: tab2.browserMode === "newtab" ? /* @__PURE__ */ jsxRuntimeExports.jsx(House, { className: "w-3 h-3 text-muted-foreground" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Globe, { className: "w-3 h-3 text-muted-foreground" }) }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("h4", { className: "text-[10px] font-semibold text-foreground leading-tight line-clamp-2 pt-1 font-sans", children: tab2.title })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[9px] text-muted-foreground truncate w-full pt-1.5 border-t border-border/30 font-sans font-light", children: tab2.browserMode === "newtab" ? "New Tab" : tab2.url })
                ]
              },
              tab2.id
            );
          }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute bottom-4 left-4 right-4 shrink-0 font-sans", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
            Button,
            {
              onClick: () => createNewTab(),
              className: "w-full rounded-full gap-2 font-bold py-5 text-sm shadow-lg hover:shadow-xl",
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Plus, { className: "w-4 h-4" }),
                " New Tab"
              ]
            }
          ) })
        ] }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 relative w-full overflow-hidden bg-background", children: [
          activeTab.browserMode === "browse" && !activeTab.readerMode && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "h-6.5 bg-muted/20 border-b border-border/40 px-3 flex items-center justify-between text-[10px] text-muted-foreground flex-shrink-0 font-sans font-light", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "flex items-center gap-1", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Info, { className: "w-3.5 h-3.5 text-primary shrink-0" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "If this website appears blank, click the Book icon in the URL bar to launch Reader Mode." })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "button",
              {
                onClick: () => toggleReaderMode(),
                className: "text-primary hover:underline font-semibold",
                children: "Enable Reader Mode"
              }
            )
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(AnimatePresence, { mode: "wait", children: activeTab.browserMode === "newtab" ? (
            /* NTP Tab Homepage */
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              motion.div,
              {
                initial: { opacity: 0 },
                animate: { opacity: 1 },
                exit: { opacity: 0 },
                className: "absolute inset-0 w-full h-full",
                children: renderNTP(activeTab)
              },
              `${activeTabId}-newtab`
            )
          ) : activeTab.browserMode === "search" ? (
            /* Search Results Tab View */
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              motion.div,
              {
                initial: { opacity: 0 },
                animate: { opacity: 1 },
                exit: { opacity: 0 },
                className: "absolute inset-0 w-full h-full",
                children: renderSearchResultsView(activeTab)
              },
              `${activeTabId}-search`
            )
          ) : (
            /* Browse Iframe View or Reader View */
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              motion.div,
              {
                initial: { opacity: 0 },
                animate: { opacity: 1 },
                exit: { opacity: 0 },
                className: "absolute inset-0 w-full h-full flex",
                children: activeTab.readerMode ? renderReaderView(activeTab) : /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 h-full relative bg-card flex flex-col", children: [
                  activeTab.loading && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute inset-0 flex items-center justify-center bg-card/65 backdrop-blur-sm z-10", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col items-center gap-3", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(RotateCw, { className: "w-8 h-8 text-primary animate-spin" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-xs text-muted-foreground", children: "Loading page through secure proxy..." })
                  ] }) }),
                  activeTab.iframeUrl ? /* @__PURE__ */ jsxRuntimeExports.jsx(
                    "iframe",
                    {
                      ref: iframeRef,
                      src: activeTab.iframeUrl,
                      className: "w-full flex-1 border-0 bg-white",
                      title: "Browser viewport",
                      sandbox: "allow-same-origin allow-scripts allow-forms allow-popups"
                    }
                  ) : /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-grow flex flex-col items-center justify-center text-center p-8", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(Globe, { className: "w-12 h-12 text-muted-foreground/35 mb-3" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "font-semibold text-sm mb-1 text-foreground/80", children: "No Page Loaded" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground max-w-sm leading-relaxed", children: "Enter a search query or URL in the bar at the top to browse." })
                  ] })
                ] })
              },
              `${activeTabId}-browse`
            )
          ) })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(AnimatePresence, { children: showAddShortcut && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "fixed inset-0 z-[500] flex items-center justify-center bg-background/60 backdrop-blur-sm p-4 font-sans text-xs", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
          motion.div,
          {
            initial: { scale: 0.95, opacity: 0 },
            animate: { scale: 1, opacity: 1 },
            exit: { scale: 0.95, opacity: 0 },
            className: "bg-card border border-border rounded-2xl shadow-2xl p-5 w-full max-w-sm flex flex-col gap-4",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between items-center border-b border-border/40 pb-2.5", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "font-bold text-sm text-foreground", children: "Add shortcut" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  Button,
                  {
                    variant: "ghost",
                    size: "icon",
                    className: "h-6 w-6",
                    onClick: () => setShowAddShortcut(false),
                    children: /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "w-4 h-4" })
                  }
                )
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-3", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-1", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("label", { className: "text-[10px] uppercase font-bold text-muted-foreground", children: "Name" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    Input,
                    {
                      value: newShortcutName,
                      onChange: (e) => setNewShortcutName(e.target.value),
                      placeholder: "e.g. Google",
                      className: "h-9 text-xs"
                    }
                  )
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-1", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("label", { className: "text-[10px] uppercase font-bold text-muted-foreground", children: "URL" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    Input,
                    {
                      value: newShortcutUrl,
                      onChange: (e) => setNewShortcutUrl(e.target.value),
                      placeholder: "e.g. google.com",
                      className: "h-9 text-xs"
                    }
                  )
                ] })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-end gap-2 mt-2", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  Button,
                  {
                    variant: "outline",
                    size: "sm",
                    onClick: () => setShowAddShortcut(false),
                    children: "Cancel"
                  }
                ),
                /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", onClick: handleAddShortcut, children: "Add shortcut" })
              ] })
            ]
          }
        ) }) })
      ]
    }
  );
}
const nameRe = /^[$_\p{ID_Start}][$_\u{200C}\u{200D}\p{ID_Continue}]*$/u;
const nameReJsx = /^[$_\p{ID_Start}][-$_\u{200C}\u{200D}\p{ID_Continue}]*$/u;
const emptyOptions$3 = {};
function name(name2, options) {
  const settings = emptyOptions$3;
  const re2 = settings.jsx ? nameReJsx : nameRe;
  return re2.test(name2);
}
const re = /[ \t\n\f\r]/g;
function whitespace(thing) {
  return typeof thing === "object" ? thing.type === "text" ? empty(thing.value) : false : empty(thing);
}
function empty(value) {
  return value.replace(re, "") === "";
}
var cjs$2 = {};
var cjs$1;
var hasRequiredCjs$2;
function requireCjs$2() {
  if (hasRequiredCjs$2) return cjs$1;
  hasRequiredCjs$2 = 1;
  var COMMENT_REGEX = /\/\*[^*]*\*+([^/*][^*]*\*+)*\//g;
  var NEWLINE_REGEX = /\n/g;
  var WHITESPACE_REGEX = /^\s*/;
  var PROPERTY_REGEX = /^(\*?[-#/*\\\w]+(\[[0-9a-z_-]+\])?)\s*/;
  var COLON_REGEX = /^:\s*/;
  var VALUE_REGEX = /^((?:'(?:\\'|.)*?'|"(?:\\"|.)*?"|\([^)]*?\)|[^};])+)/;
  var SEMICOLON_REGEX = /^[;\s]*/;
  var TRIM_REGEX = /^\s+|\s+$/g;
  var NEWLINE = "\n";
  var FORWARD_SLASH = "/";
  var ASTERISK = "*";
  var EMPTY_STRING = "";
  var TYPE_COMMENT = "comment";
  var TYPE_DECLARATION = "declaration";
  function index2(style, options) {
    if (typeof style !== "string") {
      throw new TypeError("First argument must be a string");
    }
    if (!style) return [];
    options = options || {};
    var lineno = 1;
    var column = 1;
    function updatePosition(str) {
      var lines = str.match(NEWLINE_REGEX);
      if (lines) lineno += lines.length;
      var i = str.lastIndexOf(NEWLINE);
      column = ~i ? str.length - i : column + str.length;
    }
    function position2() {
      var start = { line: lineno, column };
      return function(node2) {
        node2.position = new Position(start);
        whitespace2();
        return node2;
      };
    }
    function Position(start) {
      this.start = start;
      this.end = { line: lineno, column };
      this.source = options.source;
    }
    Position.prototype.content = style;
    function error(msg) {
      var err = new Error(
        options.source + ":" + lineno + ":" + column + ": " + msg
      );
      err.reason = msg;
      err.filename = options.source;
      err.line = lineno;
      err.column = column;
      err.source = style;
      if (options.silent) ;
      else {
        throw err;
      }
    }
    function match(re2) {
      var m = re2.exec(style);
      if (!m) return;
      var str = m[0];
      updatePosition(str);
      style = style.slice(str.length);
      return m;
    }
    function whitespace2() {
      match(WHITESPACE_REGEX);
    }
    function comments(rules) {
      var c;
      rules = rules || [];
      while (c = comment()) {
        if (c !== false) {
          rules.push(c);
        }
      }
      return rules;
    }
    function comment() {
      var pos = position2();
      if (FORWARD_SLASH != style.charAt(0) || ASTERISK != style.charAt(1)) return;
      var i = 2;
      while (EMPTY_STRING != style.charAt(i) && (ASTERISK != style.charAt(i) || FORWARD_SLASH != style.charAt(i + 1))) {
        ++i;
      }
      i += 2;
      if (EMPTY_STRING === style.charAt(i - 1)) {
        return error("End of comment missing");
      }
      var str = style.slice(2, i - 2);
      column += 2;
      updatePosition(str);
      style = style.slice(i);
      column += 2;
      return pos({
        type: TYPE_COMMENT,
        comment: str
      });
    }
    function declaration() {
      var pos = position2();
      var prop = match(PROPERTY_REGEX);
      if (!prop) return;
      comment();
      if (!match(COLON_REGEX)) return error("property missing ':'");
      var val = match(VALUE_REGEX);
      var ret = pos({
        type: TYPE_DECLARATION,
        property: trim(prop[0].replace(COMMENT_REGEX, EMPTY_STRING)),
        value: val ? trim(val[0].replace(COMMENT_REGEX, EMPTY_STRING)) : EMPTY_STRING
      });
      match(SEMICOLON_REGEX);
      return ret;
    }
    function declarations() {
      var decls = [];
      comments(decls);
      var decl;
      while (decl = declaration()) {
        if (decl !== false) {
          decls.push(decl);
          comments(decls);
        }
      }
      return decls;
    }
    whitespace2();
    return declarations();
  }
  function trim(str) {
    return str ? str.replace(TRIM_REGEX, EMPTY_STRING) : EMPTY_STRING;
  }
  cjs$1 = index2;
  return cjs$1;
}
var hasRequiredCjs$1;
function requireCjs$1() {
  if (hasRequiredCjs$1) return cjs$2;
  hasRequiredCjs$1 = 1;
  var __importDefault = cjs$2 && cjs$2.__importDefault || function(mod) {
    return mod && mod.__esModule ? mod : { "default": mod };
  };
  Object.defineProperty(cjs$2, "__esModule", { value: true });
  cjs$2.default = StyleToObject;
  const inline_style_parser_1 = __importDefault(requireCjs$2());
  function StyleToObject(style, iterator) {
    let styleObject = null;
    if (!style || typeof style !== "string") {
      return styleObject;
    }
    const declarations = (0, inline_style_parser_1.default)(style);
    const hasIterator = typeof iterator === "function";
    declarations.forEach((declaration) => {
      if (declaration.type !== "declaration") {
        return;
      }
      const { property, value } = declaration;
      if (hasIterator) {
        iterator(property, value, declaration);
      } else if (value) {
        styleObject = styleObject || {};
        styleObject[property] = value;
      }
    });
    return styleObject;
  }
  return cjs$2;
}
var utilities = {};
var hasRequiredUtilities;
function requireUtilities() {
  if (hasRequiredUtilities) return utilities;
  hasRequiredUtilities = 1;
  Object.defineProperty(utilities, "__esModule", { value: true });
  utilities.camelCase = void 0;
  var CUSTOM_PROPERTY_REGEX = /^--[a-zA-Z0-9_-]+$/;
  var HYPHEN_REGEX = /-([a-z])/g;
  var NO_HYPHEN_REGEX = /^[^-]+$/;
  var VENDOR_PREFIX_REGEX = /^-(webkit|moz|ms|o|khtml)-/;
  var MS_VENDOR_PREFIX_REGEX = /^-(ms)-/;
  var skipCamelCase = function(property) {
    return !property || NO_HYPHEN_REGEX.test(property) || CUSTOM_PROPERTY_REGEX.test(property);
  };
  var capitalize = function(match, character) {
    return character.toUpperCase();
  };
  var trimHyphen = function(match, prefix) {
    return "".concat(prefix, "-");
  };
  var camelCase = function(property, options) {
    if (options === void 0) {
      options = {};
    }
    if (skipCamelCase(property)) {
      return property;
    }
    property = property.toLowerCase();
    if (options.reactCompat) {
      property = property.replace(MS_VENDOR_PREFIX_REGEX, trimHyphen);
    } else {
      property = property.replace(VENDOR_PREFIX_REGEX, trimHyphen);
    }
    return property.replace(HYPHEN_REGEX, capitalize);
  };
  utilities.camelCase = camelCase;
  return utilities;
}
var cjs;
var hasRequiredCjs;
function requireCjs() {
  if (hasRequiredCjs) return cjs;
  hasRequiredCjs = 1;
  var __importDefault = cjs && cjs.__importDefault || function(mod) {
    return mod && mod.__esModule ? mod : { "default": mod };
  };
  var style_to_object_1 = __importDefault(requireCjs$1());
  var utilities_1 = requireUtilities();
  function StyleToJS(style, options) {
    var output = {};
    if (!style || typeof style !== "string") {
      return output;
    }
    (0, style_to_object_1.default)(style, function(property, value) {
      if (property && value) {
        output[(0, utilities_1.camelCase)(property, options)] = value;
      }
    });
    return output;
  }
  StyleToJS.default = StyleToJS;
  cjs = StyleToJS;
  return cjs;
}
var cjsExports = requireCjs();
const styleToJs = /* @__PURE__ */ getDefaultExportFromCjs(cjsExports);
const pointEnd = point$2("end");
const pointStart = point$2("start");
function point$2(type) {
  return point2;
  function point2(node2) {
    const point3 = node2 && node2.position && node2.position[type] || {};
    if (typeof point3.line === "number" && point3.line > 0 && typeof point3.column === "number" && point3.column > 0) {
      return {
        line: point3.line,
        column: point3.column,
        offset: typeof point3.offset === "number" && point3.offset > -1 ? point3.offset : void 0
      };
    }
  }
}
function position$1(node2) {
  const start = pointStart(node2);
  const end = pointEnd(node2);
  if (start && end) {
    return { start, end };
  }
}
function stringifyPosition(value) {
  if (!value || typeof value !== "object") {
    return "";
  }
  if ("position" in value || "type" in value) {
    return position(value.position);
  }
  if ("start" in value || "end" in value) {
    return position(value);
  }
  if ("line" in value || "column" in value) {
    return point$1(value);
  }
  return "";
}
function point$1(point2) {
  return index(point2 && point2.line) + ":" + index(point2 && point2.column);
}
function position(pos) {
  return point$1(pos && pos.start) + "-" + point$1(pos && pos.end);
}
function index(value) {
  return value && typeof value === "number" ? value : 1;
}
class VFileMessage extends Error {
  /**
   * Create a message for `reason`.
   *
   * > 🪦 **Note**: also has obsolete signatures.
   *
   * @overload
   * @param {string} reason
   * @param {Options | null | undefined} [options]
   * @returns
   *
   * @overload
   * @param {string} reason
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns
   *
   * @overload
   * @param {string} reason
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns
   *
   * @overload
   * @param {string} reason
   * @param {string | null | undefined} [origin]
   * @returns
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {string | null | undefined} [origin]
   * @returns
   *
   * @param {Error | VFileMessage | string} causeOrReason
   *   Reason for message, should use markdown.
   * @param {Node | NodeLike | Options | Point | Position | string | null | undefined} [optionsOrParentOrPlace]
   *   Configuration (optional).
   * @param {string | null | undefined} [origin]
   *   Place in code where the message originates (example:
   *   `'my-package:my-rule'` or `'my-rule'`).
   * @returns
   *   Instance of `VFileMessage`.
   */
  // eslint-disable-next-line complexity
  constructor(causeOrReason, optionsOrParentOrPlace, origin) {
    super();
    if (typeof optionsOrParentOrPlace === "string") {
      origin = optionsOrParentOrPlace;
      optionsOrParentOrPlace = void 0;
    }
    let reason = "";
    let options = {};
    let legacyCause = false;
    if (optionsOrParentOrPlace) {
      if ("line" in optionsOrParentOrPlace && "column" in optionsOrParentOrPlace) {
        options = { place: optionsOrParentOrPlace };
      } else if ("start" in optionsOrParentOrPlace && "end" in optionsOrParentOrPlace) {
        options = { place: optionsOrParentOrPlace };
      } else if ("type" in optionsOrParentOrPlace) {
        options = {
          ancestors: [optionsOrParentOrPlace],
          place: optionsOrParentOrPlace.position
        };
      } else {
        options = { ...optionsOrParentOrPlace };
      }
    }
    if (typeof causeOrReason === "string") {
      reason = causeOrReason;
    } else if (!options.cause && causeOrReason) {
      legacyCause = true;
      reason = causeOrReason.message;
      options.cause = causeOrReason;
    }
    if (!options.ruleId && !options.source && typeof origin === "string") {
      const index2 = origin.indexOf(":");
      if (index2 === -1) {
        options.ruleId = origin;
      } else {
        options.source = origin.slice(0, index2);
        options.ruleId = origin.slice(index2 + 1);
      }
    }
    if (!options.place && options.ancestors && options.ancestors) {
      const parent = options.ancestors[options.ancestors.length - 1];
      if (parent) {
        options.place = parent.position;
      }
    }
    const start = options.place && "start" in options.place ? options.place.start : options.place;
    this.ancestors = options.ancestors || void 0;
    this.cause = options.cause || void 0;
    this.column = start ? start.column : void 0;
    this.fatal = void 0;
    this.file = "";
    this.message = reason;
    this.line = start ? start.line : void 0;
    this.name = stringifyPosition(options.place) || "1:1";
    this.place = options.place || void 0;
    this.reason = this.message;
    this.ruleId = options.ruleId || void 0;
    this.source = options.source || void 0;
    this.stack = legacyCause && options.cause && typeof options.cause.stack === "string" ? options.cause.stack : "";
    this.actual = void 0;
    this.expected = void 0;
    this.note = void 0;
    this.url = void 0;
  }
}
VFileMessage.prototype.file = "";
VFileMessage.prototype.name = "";
VFileMessage.prototype.reason = "";
VFileMessage.prototype.message = "";
VFileMessage.prototype.stack = "";
VFileMessage.prototype.column = void 0;
VFileMessage.prototype.line = void 0;
VFileMessage.prototype.ancestors = void 0;
VFileMessage.prototype.cause = void 0;
VFileMessage.prototype.fatal = void 0;
VFileMessage.prototype.place = void 0;
VFileMessage.prototype.ruleId = void 0;
VFileMessage.prototype.source = void 0;
const own$3 = {}.hasOwnProperty;
const emptyMap = /* @__PURE__ */ new Map();
const cap = /[A-Z]/g;
const tableElements = /* @__PURE__ */ new Set(["table", "tbody", "thead", "tfoot", "tr"]);
const tableCellElement = /* @__PURE__ */ new Set(["td", "th"]);
const docs = "https://github.com/syntax-tree/hast-util-to-jsx-runtime";
function toJsxRuntime(tree, options) {
  if (!options || options.Fragment === void 0) {
    throw new TypeError("Expected `Fragment` in options");
  }
  const filePath = options.filePath || void 0;
  let create;
  if (options.development) {
    if (typeof options.jsxDEV !== "function") {
      throw new TypeError(
        "Expected `jsxDEV` in options when `development: true`"
      );
    }
    create = developmentCreate(filePath, options.jsxDEV);
  } else {
    if (typeof options.jsx !== "function") {
      throw new TypeError("Expected `jsx` in production options");
    }
    if (typeof options.jsxs !== "function") {
      throw new TypeError("Expected `jsxs` in production options");
    }
    create = productionCreate(filePath, options.jsx, options.jsxs);
  }
  const state = {
    Fragment: options.Fragment,
    ancestors: [],
    components: options.components || {},
    create,
    elementAttributeNameCase: options.elementAttributeNameCase || "react",
    evaluater: options.createEvaluater ? options.createEvaluater() : void 0,
    filePath,
    ignoreInvalidStyle: options.ignoreInvalidStyle || false,
    passKeys: options.passKeys !== false,
    passNode: options.passNode || false,
    schema: options.space === "svg" ? svg : html$2,
    stylePropertyNameCase: options.stylePropertyNameCase || "dom",
    tableCellAlignToStyle: options.tableCellAlignToStyle !== false
  };
  const result = one$1(state, tree, void 0);
  if (result && typeof result !== "string") {
    return result;
  }
  return state.create(
    tree,
    state.Fragment,
    { children: result || void 0 },
    void 0
  );
}
function one$1(state, node2, key) {
  if (node2.type === "element") {
    return element$1(state, node2, key);
  }
  if (node2.type === "mdxFlowExpression" || node2.type === "mdxTextExpression") {
    return mdxExpression(state, node2);
  }
  if (node2.type === "mdxJsxFlowElement" || node2.type === "mdxJsxTextElement") {
    return mdxJsxElement(state, node2, key);
  }
  if (node2.type === "mdxjsEsm") {
    return mdxEsm(state, node2);
  }
  if (node2.type === "root") {
    return root$2(state, node2, key);
  }
  if (node2.type === "text") {
    return text$5(state, node2);
  }
}
function element$1(state, node2, key) {
  const parentSchema = state.schema;
  let schema = parentSchema;
  if (node2.tagName.toLowerCase() === "svg" && parentSchema.space === "html") {
    schema = svg;
    state.schema = schema;
  }
  state.ancestors.push(node2);
  const type = findComponentFromName(state, node2.tagName, false);
  const props = createElementProps(state, node2);
  let children = createChildren(state, node2);
  if (tableElements.has(node2.tagName)) {
    children = children.filter(function(child) {
      return typeof child === "string" ? !whitespace(child) : true;
    });
  }
  addNode(state, props, type, node2);
  addChildren(props, children);
  state.ancestors.pop();
  state.schema = parentSchema;
  return state.create(node2, type, props, key);
}
function mdxExpression(state, node2) {
  if (node2.data && node2.data.estree && state.evaluater) {
    const program = node2.data.estree;
    const expression = program.body[0];
    ok(expression.type === "ExpressionStatement");
    return (
      /** @type {Child | undefined} */
      state.evaluater.evaluateExpression(expression.expression)
    );
  }
  crashEstree(state, node2.position);
}
function mdxEsm(state, node2) {
  if (node2.data && node2.data.estree && state.evaluater) {
    return (
      /** @type {Child | undefined} */
      state.evaluater.evaluateProgram(node2.data.estree)
    );
  }
  crashEstree(state, node2.position);
}
function mdxJsxElement(state, node2, key) {
  const parentSchema = state.schema;
  let schema = parentSchema;
  if (node2.name === "svg" && parentSchema.space === "html") {
    schema = svg;
    state.schema = schema;
  }
  state.ancestors.push(node2);
  const type = node2.name === null ? state.Fragment : findComponentFromName(state, node2.name, true);
  const props = createJsxElementProps(state, node2);
  const children = createChildren(state, node2);
  addNode(state, props, type, node2);
  addChildren(props, children);
  state.ancestors.pop();
  state.schema = parentSchema;
  return state.create(node2, type, props, key);
}
function root$2(state, node2, key) {
  const props = {};
  addChildren(props, createChildren(state, node2));
  return state.create(node2, state.Fragment, props, key);
}
function text$5(_, node2) {
  return node2.value;
}
function addNode(state, props, type, node2) {
  if (typeof type !== "string" && type !== state.Fragment && state.passNode) {
    props.node = node2;
  }
}
function addChildren(props, children) {
  if (children.length > 0) {
    const value = children.length > 1 ? children : children[0];
    if (value) {
      props.children = value;
    }
  }
}
function productionCreate(_, jsx, jsxs) {
  return create;
  function create(_2, type, props, key) {
    const isStaticChildren = Array.isArray(props.children);
    const fn = isStaticChildren ? jsxs : jsx;
    return key ? fn(type, props, key) : fn(type, props);
  }
}
function developmentCreate(filePath, jsxDEV) {
  return create;
  function create(node2, type, props, key) {
    const isStaticChildren = Array.isArray(props.children);
    const point2 = pointStart(node2);
    return jsxDEV(
      type,
      props,
      key,
      isStaticChildren,
      {
        columnNumber: point2 ? point2.column - 1 : void 0,
        fileName: filePath,
        lineNumber: point2 ? point2.line : void 0
      },
      void 0
    );
  }
}
function createElementProps(state, node2) {
  const props = {};
  let alignValue;
  let prop;
  for (prop in node2.properties) {
    if (prop !== "children" && own$3.call(node2.properties, prop)) {
      const result = createProperty(state, prop, node2.properties[prop]);
      if (result) {
        const [key, value] = result;
        if (state.tableCellAlignToStyle && key === "align" && typeof value === "string" && tableCellElement.has(node2.tagName)) {
          alignValue = value;
        } else {
          props[key] = value;
        }
      }
    }
  }
  if (alignValue) {
    const style = (
      /** @type {Style} */
      props.style || (props.style = {})
    );
    style[state.stylePropertyNameCase === "css" ? "text-align" : "textAlign"] = alignValue;
  }
  return props;
}
function createJsxElementProps(state, node2) {
  const props = {};
  for (const attribute of node2.attributes) {
    if (attribute.type === "mdxJsxExpressionAttribute") {
      if (attribute.data && attribute.data.estree && state.evaluater) {
        const program = attribute.data.estree;
        const expression = program.body[0];
        ok(expression.type === "ExpressionStatement");
        const objectExpression = expression.expression;
        ok(objectExpression.type === "ObjectExpression");
        const property = objectExpression.properties[0];
        ok(property.type === "SpreadElement");
        Object.assign(
          props,
          state.evaluater.evaluateExpression(property.argument)
        );
      } else {
        crashEstree(state, node2.position);
      }
    } else {
      const name2 = attribute.name;
      let value;
      if (attribute.value && typeof attribute.value === "object") {
        if (attribute.value.data && attribute.value.data.estree && state.evaluater) {
          const program = attribute.value.data.estree;
          const expression = program.body[0];
          ok(expression.type === "ExpressionStatement");
          value = state.evaluater.evaluateExpression(expression.expression);
        } else {
          crashEstree(state, node2.position);
        }
      } else {
        value = attribute.value === null ? true : attribute.value;
      }
      props[name2] = /** @type {Props[keyof Props]} */
      value;
    }
  }
  return props;
}
function createChildren(state, node2) {
  const children = [];
  let index2 = -1;
  const countsByName = state.passKeys ? /* @__PURE__ */ new Map() : emptyMap;
  while (++index2 < node2.children.length) {
    const child = node2.children[index2];
    let key;
    if (state.passKeys) {
      const name2 = child.type === "element" ? child.tagName : child.type === "mdxJsxFlowElement" || child.type === "mdxJsxTextElement" ? child.name : void 0;
      if (name2) {
        const count = countsByName.get(name2) || 0;
        key = name2 + "-" + count;
        countsByName.set(name2, count + 1);
      }
    }
    const result = one$1(state, child, key);
    if (result !== void 0) children.push(result);
  }
  return children;
}
function createProperty(state, prop, value) {
  const info = find(state.schema, prop);
  if (value === null || value === void 0 || typeof value === "number" && Number.isNaN(value)) {
    return;
  }
  if (Array.isArray(value)) {
    value = info.commaSeparated ? stringify(value) : stringify$1(value);
  }
  if (info.property === "style") {
    let styleObject = typeof value === "object" ? value : parseStyle(state, String(value));
    if (state.stylePropertyNameCase === "css") {
      styleObject = transformStylesToCssCasing(styleObject);
    }
    return ["style", styleObject];
  }
  return [
    state.elementAttributeNameCase === "react" && info.space ? hastToReact[info.property] || info.property : info.attribute,
    value
  ];
}
function parseStyle(state, value) {
  try {
    return styleToJs(value, { reactCompat: true });
  } catch (error) {
    if (state.ignoreInvalidStyle) {
      return {};
    }
    const cause = (
      /** @type {Error} */
      error
    );
    const message = new VFileMessage("Cannot parse `style` attribute", {
      ancestors: state.ancestors,
      cause,
      ruleId: "style",
      source: "hast-util-to-jsx-runtime"
    });
    message.file = state.filePath || void 0;
    message.url = docs + "#cannot-parse-style-attribute";
    throw message;
  }
}
function findComponentFromName(state, name$1, allowExpression) {
  let result;
  if (!allowExpression) {
    result = { type: "Literal", value: name$1 };
  } else if (name$1.includes(".")) {
    const identifiers = name$1.split(".");
    let index2 = -1;
    let node2;
    while (++index2 < identifiers.length) {
      const prop = name(identifiers[index2]) ? { type: "Identifier", name: identifiers[index2] } : { type: "Literal", value: identifiers[index2] };
      node2 = node2 ? {
        type: "MemberExpression",
        object: node2,
        property: prop,
        computed: Boolean(index2 && prop.type === "Literal"),
        optional: false
      } : prop;
    }
    result = node2;
  } else {
    result = name(name$1) && !/^[a-z]/.test(name$1) ? { type: "Identifier", name: name$1 } : { type: "Literal", value: name$1 };
  }
  if (result.type === "Literal") {
    const name2 = (
      /** @type {string | number} */
      result.value
    );
    return own$3.call(state.components, name2) ? state.components[name2] : name2;
  }
  if (state.evaluater) {
    return state.evaluater.evaluateExpression(result);
  }
  crashEstree(state);
}
function crashEstree(state, place) {
  const message = new VFileMessage(
    "Cannot handle MDX estrees without `createEvaluater`",
    {
      ancestors: state.ancestors,
      place,
      ruleId: "mdx-estree",
      source: "hast-util-to-jsx-runtime"
    }
  );
  message.file = state.filePath || void 0;
  message.url = docs + "#cannot-handle-mdx-estrees-without-createevaluater";
  throw message;
}
function transformStylesToCssCasing(domCasing) {
  const cssCasing = {};
  let from;
  for (from in domCasing) {
    if (own$3.call(domCasing, from)) {
      cssCasing[transformStyleToCssCasing(from)] = domCasing[from];
    }
  }
  return cssCasing;
}
function transformStyleToCssCasing(from) {
  let to = from.replace(cap, toDash);
  if (to.slice(0, 3) === "ms-") to = "-" + to;
  return to;
}
function toDash($0) {
  return "-" + $0.toLowerCase();
}
const urlAttributes = {
  action: ["form"],
  cite: ["blockquote", "del", "ins", "q"],
  data: ["object"],
  formAction: ["button", "input"],
  href: ["a", "area", "base", "link"],
  icon: ["menuitem"],
  itemId: null,
  manifest: ["html"],
  ping: ["a", "area"],
  poster: ["video"],
  src: [
    "audio",
    "embed",
    "iframe",
    "img",
    "input",
    "script",
    "source",
    "track",
    "video"
  ]
};
const emptyOptions$2 = {};
function toString$1(value, options) {
  const settings = emptyOptions$2;
  const includeImageAlt = typeof settings.includeImageAlt === "boolean" ? settings.includeImageAlt : true;
  const includeHtml = typeof settings.includeHtml === "boolean" ? settings.includeHtml : true;
  return one(value, includeImageAlt, includeHtml);
}
function one(value, includeImageAlt, includeHtml) {
  if (node(value)) {
    if ("value" in value) {
      return value.type === "html" && !includeHtml ? "" : value.value;
    }
    if (includeImageAlt && "alt" in value && value.alt) {
      return value.alt;
    }
    if ("children" in value) {
      return all(value.children, includeImageAlt, includeHtml);
    }
  }
  if (Array.isArray(value)) {
    return all(value, includeImageAlt, includeHtml);
  }
  return "";
}
function all(values, includeImageAlt, includeHtml) {
  const result = [];
  let index2 = -1;
  while (++index2 < values.length) {
    result[index2] = one(values[index2], includeImageAlt, includeHtml);
  }
  return result.join("");
}
function node(value) {
  return Boolean(value && typeof value === "object");
}
const element = document.createElement("i");
function decodeNamedCharacterReference(value) {
  const characterReference2 = "&" + value + ";";
  element.innerHTML = characterReference2;
  const character = element.textContent;
  if (character.charCodeAt(character.length - 1) === 59 && value !== "semi") {
    return false;
  }
  return character === characterReference2 ? false : character;
}
function splice(list2, start, remove, items) {
  const end = list2.length;
  let chunkStart = 0;
  let parameters;
  if (start < 0) {
    start = -start > end ? 0 : end + start;
  } else {
    start = start > end ? end : start;
  }
  remove = remove > 0 ? remove : 0;
  if (items.length < 1e4) {
    parameters = Array.from(items);
    parameters.unshift(start, remove);
    list2.splice(...parameters);
  } else {
    if (remove) list2.splice(start, remove);
    while (chunkStart < items.length) {
      parameters = items.slice(chunkStart, chunkStart + 1e4);
      parameters.unshift(start, 0);
      list2.splice(...parameters);
      chunkStart += 1e4;
      start += 1e4;
    }
  }
}
function push(list2, items) {
  if (list2.length > 0) {
    splice(list2, list2.length, 0, items);
    return list2;
  }
  return items;
}
const hasOwnProperty = {}.hasOwnProperty;
function combineExtensions(extensions) {
  const all2 = {};
  let index2 = -1;
  while (++index2 < extensions.length) {
    syntaxExtension(all2, extensions[index2]);
  }
  return all2;
}
function syntaxExtension(all2, extension2) {
  let hook;
  for (hook in extension2) {
    const maybe = hasOwnProperty.call(all2, hook) ? all2[hook] : void 0;
    const left = maybe || (all2[hook] = {});
    const right = extension2[hook];
    let code2;
    if (right) {
      for (code2 in right) {
        if (!hasOwnProperty.call(left, code2)) left[code2] = [];
        const value = right[code2];
        constructs(
          // @ts-expect-error Looks like a list.
          left[code2],
          Array.isArray(value) ? value : value ? [value] : []
        );
      }
    }
  }
}
function constructs(existing, list2) {
  let index2 = -1;
  const before = [];
  while (++index2 < list2.length) {
    (list2[index2].add === "after" ? existing : before).push(list2[index2]);
  }
  splice(existing, 0, 0, before);
}
function decodeNumericCharacterReference(value, base) {
  const code2 = Number.parseInt(value, base);
  if (
    // C0 except for HT, LF, FF, CR, space.
    code2 < 9 || code2 === 11 || code2 > 13 && code2 < 32 || // Control character (DEL) of C0, and C1 controls.
    code2 > 126 && code2 < 160 || // Lone high surrogates and low surrogates.
    code2 > 55295 && code2 < 57344 || // Noncharacters.
    code2 > 64975 && code2 < 65008 || /* eslint-disable no-bitwise */
    (code2 & 65535) === 65535 || (code2 & 65535) === 65534 || /* eslint-enable no-bitwise */
    // Out of range
    code2 > 1114111
  ) {
    return "�";
  }
  return String.fromCodePoint(code2);
}
function normalizeIdentifier(value) {
  return value.replace(/[\t\n\r ]+/g, " ").replace(/^ | $/g, "").toLowerCase().toUpperCase();
}
function normalizeUri(value) {
  const result = [];
  let index2 = -1;
  let start = 0;
  let skip = 0;
  while (++index2 < value.length) {
    const code2 = value.charCodeAt(index2);
    let replace2 = "";
    if (code2 === 37 && asciiAlphanumeric(value.charCodeAt(index2 + 1)) && asciiAlphanumeric(value.charCodeAt(index2 + 2))) {
      skip = 2;
    } else if (code2 < 128) {
      if (!/[!#$&-;=?-Z_a-z~]/.test(String.fromCharCode(code2))) {
        replace2 = String.fromCharCode(code2);
      }
    } else if (code2 > 55295 && code2 < 57344) {
      const next = value.charCodeAt(index2 + 1);
      if (code2 < 56320 && next > 56319 && next < 57344) {
        replace2 = String.fromCharCode(code2, next);
        skip = 1;
      } else {
        replace2 = "�";
      }
    } else {
      replace2 = String.fromCharCode(code2);
    }
    if (replace2) {
      result.push(value.slice(start, index2), encodeURIComponent(replace2));
      start = index2 + skip + 1;
      replace2 = "";
    }
    if (skip) {
      index2 += skip;
      skip = 0;
    }
  }
  return result.join("") + value.slice(start);
}
const content$1 = {
  tokenize: initializeContent
};
function initializeContent(effects) {
  const contentStart = effects.attempt(this.parser.constructs.contentInitial, afterContentStartConstruct, paragraphInitial);
  let previous2;
  return contentStart;
  function afterContentStartConstruct(code2) {
    if (code2 === null) {
      effects.consume(code2);
      return;
    }
    effects.enter("lineEnding");
    effects.consume(code2);
    effects.exit("lineEnding");
    return factorySpace(effects, contentStart, "linePrefix");
  }
  function paragraphInitial(code2) {
    effects.enter("paragraph");
    return lineStart(code2);
  }
  function lineStart(code2) {
    const token = effects.enter("chunkText", {
      contentType: "text",
      previous: previous2
    });
    if (previous2) {
      previous2.next = token;
    }
    previous2 = token;
    return data(code2);
  }
  function data(code2) {
    if (code2 === null) {
      effects.exit("chunkText");
      effects.exit("paragraph");
      effects.consume(code2);
      return;
    }
    if (markdownLineEnding(code2)) {
      effects.consume(code2);
      effects.exit("chunkText");
      return lineStart;
    }
    effects.consume(code2);
    return data;
  }
}
const document$2 = {
  tokenize: initializeDocument
};
const containerConstruct = {
  tokenize: tokenizeContainer
};
function initializeDocument(effects) {
  const self2 = this;
  const stack = [];
  let continued = 0;
  let childFlow;
  let childToken;
  let lineStartOffset;
  return start;
  function start(code2) {
    if (continued < stack.length) {
      const item = stack[continued];
      self2.containerState = item[1];
      return effects.attempt(item[0].continuation, documentContinue, checkNewContainers)(code2);
    }
    return checkNewContainers(code2);
  }
  function documentContinue(code2) {
    continued++;
    if (self2.containerState._closeFlow) {
      self2.containerState._closeFlow = void 0;
      if (childFlow) {
        closeFlow();
      }
      const indexBeforeExits = self2.events.length;
      let indexBeforeFlow = indexBeforeExits;
      let point2;
      while (indexBeforeFlow--) {
        if (self2.events[indexBeforeFlow][0] === "exit" && self2.events[indexBeforeFlow][1].type === "chunkFlow") {
          point2 = self2.events[indexBeforeFlow][1].end;
          break;
        }
      }
      exitContainers(continued);
      let index2 = indexBeforeExits;
      while (index2 < self2.events.length) {
        self2.events[index2][1].end = {
          ...point2
        };
        index2++;
      }
      splice(self2.events, indexBeforeFlow + 1, 0, self2.events.slice(indexBeforeExits));
      self2.events.length = index2;
      return checkNewContainers(code2);
    }
    return start(code2);
  }
  function checkNewContainers(code2) {
    if (continued === stack.length) {
      if (!childFlow) {
        return documentContinued(code2);
      }
      if (childFlow.currentConstruct && childFlow.currentConstruct.concrete) {
        return flowStart(code2);
      }
      self2.interrupt = Boolean(childFlow.currentConstruct && !childFlow._gfmTableDynamicInterruptHack);
    }
    self2.containerState = {};
    return effects.check(containerConstruct, thereIsANewContainer, thereIsNoNewContainer)(code2);
  }
  function thereIsANewContainer(code2) {
    if (childFlow) closeFlow();
    exitContainers(continued);
    return documentContinued(code2);
  }
  function thereIsNoNewContainer(code2) {
    self2.parser.lazy[self2.now().line] = continued !== stack.length;
    lineStartOffset = self2.now().offset;
    return flowStart(code2);
  }
  function documentContinued(code2) {
    self2.containerState = {};
    return effects.attempt(containerConstruct, containerContinue, flowStart)(code2);
  }
  function containerContinue(code2) {
    continued++;
    stack.push([self2.currentConstruct, self2.containerState]);
    return documentContinued(code2);
  }
  function flowStart(code2) {
    if (code2 === null) {
      if (childFlow) closeFlow();
      exitContainers(0);
      effects.consume(code2);
      return;
    }
    childFlow = childFlow || self2.parser.flow(self2.now());
    effects.enter("chunkFlow", {
      _tokenizer: childFlow,
      contentType: "flow",
      previous: childToken
    });
    return flowContinue(code2);
  }
  function flowContinue(code2) {
    if (code2 === null) {
      writeToChild(effects.exit("chunkFlow"), true);
      exitContainers(0);
      effects.consume(code2);
      return;
    }
    if (markdownLineEnding(code2)) {
      effects.consume(code2);
      writeToChild(effects.exit("chunkFlow"));
      continued = 0;
      self2.interrupt = void 0;
      return start;
    }
    effects.consume(code2);
    return flowContinue;
  }
  function writeToChild(token, endOfFile) {
    const stream = self2.sliceStream(token);
    if (endOfFile) stream.push(null);
    token.previous = childToken;
    if (childToken) childToken.next = token;
    childToken = token;
    childFlow.defineSkip(token.start);
    childFlow.write(stream);
    if (self2.parser.lazy[token.start.line]) {
      let index2 = childFlow.events.length;
      while (index2--) {
        if (
          // The token starts before the line ending…
          childFlow.events[index2][1].start.offset < lineStartOffset && // …and either is not ended yet…
          (!childFlow.events[index2][1].end || // …or ends after it.
          childFlow.events[index2][1].end.offset > lineStartOffset)
        ) {
          return;
        }
      }
      const indexBeforeExits = self2.events.length;
      let indexBeforeFlow = indexBeforeExits;
      let seen;
      let point2;
      while (indexBeforeFlow--) {
        if (self2.events[indexBeforeFlow][0] === "exit" && self2.events[indexBeforeFlow][1].type === "chunkFlow") {
          if (seen) {
            point2 = self2.events[indexBeforeFlow][1].end;
            break;
          }
          seen = true;
        }
      }
      exitContainers(continued);
      index2 = indexBeforeExits;
      while (index2 < self2.events.length) {
        self2.events[index2][1].end = {
          ...point2
        };
        index2++;
      }
      splice(self2.events, indexBeforeFlow + 1, 0, self2.events.slice(indexBeforeExits));
      self2.events.length = index2;
    }
  }
  function exitContainers(size) {
    let index2 = stack.length;
    while (index2-- > size) {
      const entry = stack[index2];
      self2.containerState = entry[1];
      entry[0].exit.call(self2, effects);
    }
    stack.length = size;
  }
  function closeFlow() {
    childFlow.write([null]);
    childToken = void 0;
    childFlow = void 0;
    self2.containerState._closeFlow = void 0;
  }
}
function tokenizeContainer(effects, ok2, nok) {
  return factorySpace(effects, effects.attempt(this.parser.constructs.document, ok2, nok), "linePrefix", this.parser.constructs.disable.null.includes("codeIndented") ? void 0 : 4);
}
function classifyCharacter(code2) {
  if (code2 === null || markdownLineEndingOrSpace(code2) || unicodeWhitespace(code2)) {
    return 1;
  }
  if (unicodePunctuation(code2)) {
    return 2;
  }
}
function resolveAll(constructs2, events, context) {
  const called = [];
  let index2 = -1;
  while (++index2 < constructs2.length) {
    const resolve = constructs2[index2].resolveAll;
    if (resolve && !called.includes(resolve)) {
      events = resolve(events, context);
      called.push(resolve);
    }
  }
  return events;
}
const attention = {
  name: "attention",
  resolveAll: resolveAllAttention,
  tokenize: tokenizeAttention
};
function resolveAllAttention(events, context) {
  let index2 = -1;
  let open;
  let group;
  let text2;
  let openingSequence;
  let closingSequence;
  let use;
  let nextEvents;
  let offset;
  while (++index2 < events.length) {
    if (events[index2][0] === "enter" && events[index2][1].type === "attentionSequence" && events[index2][1]._close) {
      open = index2;
      while (open--) {
        if (events[open][0] === "exit" && events[open][1].type === "attentionSequence" && events[open][1]._open && // If the markers are the same:
        context.sliceSerialize(events[open][1]).charCodeAt(0) === context.sliceSerialize(events[index2][1]).charCodeAt(0)) {
          if ((events[open][1]._close || events[index2][1]._open) && (events[index2][1].end.offset - events[index2][1].start.offset) % 3 && !((events[open][1].end.offset - events[open][1].start.offset + events[index2][1].end.offset - events[index2][1].start.offset) % 3)) {
            continue;
          }
          use = events[open][1].end.offset - events[open][1].start.offset > 1 && events[index2][1].end.offset - events[index2][1].start.offset > 1 ? 2 : 1;
          const start = {
            ...events[open][1].end
          };
          const end = {
            ...events[index2][1].start
          };
          movePoint(start, -use);
          movePoint(end, use);
          openingSequence = {
            type: use > 1 ? "strongSequence" : "emphasisSequence",
            start,
            end: {
              ...events[open][1].end
            }
          };
          closingSequence = {
            type: use > 1 ? "strongSequence" : "emphasisSequence",
            start: {
              ...events[index2][1].start
            },
            end
          };
          text2 = {
            type: use > 1 ? "strongText" : "emphasisText",
            start: {
              ...events[open][1].end
            },
            end: {
              ...events[index2][1].start
            }
          };
          group = {
            type: use > 1 ? "strong" : "emphasis",
            start: {
              ...openingSequence.start
            },
            end: {
              ...closingSequence.end
            }
          };
          events[open][1].end = {
            ...openingSequence.start
          };
          events[index2][1].start = {
            ...closingSequence.end
          };
          nextEvents = [];
          if (events[open][1].end.offset - events[open][1].start.offset) {
            nextEvents = push(nextEvents, [["enter", events[open][1], context], ["exit", events[open][1], context]]);
          }
          nextEvents = push(nextEvents, [["enter", group, context], ["enter", openingSequence, context], ["exit", openingSequence, context], ["enter", text2, context]]);
          nextEvents = push(nextEvents, resolveAll(context.parser.constructs.insideSpan.null, events.slice(open + 1, index2), context));
          nextEvents = push(nextEvents, [["exit", text2, context], ["enter", closingSequence, context], ["exit", closingSequence, context], ["exit", group, context]]);
          if (events[index2][1].end.offset - events[index2][1].start.offset) {
            offset = 2;
            nextEvents = push(nextEvents, [["enter", events[index2][1], context], ["exit", events[index2][1], context]]);
          } else {
            offset = 0;
          }
          splice(events, open - 1, index2 - open + 3, nextEvents);
          index2 = open + nextEvents.length - offset - 2;
          break;
        }
      }
    }
  }
  index2 = -1;
  while (++index2 < events.length) {
    if (events[index2][1].type === "attentionSequence") {
      events[index2][1].type = "data";
    }
  }
  return events;
}
function tokenizeAttention(effects, ok2) {
  const attentionMarkers2 = this.parser.constructs.attentionMarkers.null;
  const previous2 = this.previous;
  const before = classifyCharacter(previous2);
  let marker;
  return start;
  function start(code2) {
    marker = code2;
    effects.enter("attentionSequence");
    return inside(code2);
  }
  function inside(code2) {
    if (code2 === marker) {
      effects.consume(code2);
      return inside;
    }
    const token = effects.exit("attentionSequence");
    const after = classifyCharacter(code2);
    const open = !after || after === 2 && before || attentionMarkers2.includes(code2);
    const close = !before || before === 2 && after || attentionMarkers2.includes(previous2);
    token._open = Boolean(marker === 42 ? open : open && (before || !close));
    token._close = Boolean(marker === 42 ? close : close && (after || !open));
    return ok2(code2);
  }
}
function movePoint(point2, offset) {
  point2.column += offset;
  point2.offset += offset;
  point2._bufferIndex += offset;
}
const autolink = {
  name: "autolink",
  tokenize: tokenizeAutolink
};
function tokenizeAutolink(effects, ok2, nok) {
  let size = 0;
  return start;
  function start(code2) {
    effects.enter("autolink");
    effects.enter("autolinkMarker");
    effects.consume(code2);
    effects.exit("autolinkMarker");
    effects.enter("autolinkProtocol");
    return open;
  }
  function open(code2) {
    if (asciiAlpha(code2)) {
      effects.consume(code2);
      return schemeOrEmailAtext;
    }
    if (code2 === 64) {
      return nok(code2);
    }
    return emailAtext(code2);
  }
  function schemeOrEmailAtext(code2) {
    if (code2 === 43 || code2 === 45 || code2 === 46 || asciiAlphanumeric(code2)) {
      size = 1;
      return schemeInsideOrEmailAtext(code2);
    }
    return emailAtext(code2);
  }
  function schemeInsideOrEmailAtext(code2) {
    if (code2 === 58) {
      effects.consume(code2);
      size = 0;
      return urlInside;
    }
    if ((code2 === 43 || code2 === 45 || code2 === 46 || asciiAlphanumeric(code2)) && size++ < 32) {
      effects.consume(code2);
      return schemeInsideOrEmailAtext;
    }
    size = 0;
    return emailAtext(code2);
  }
  function urlInside(code2) {
    if (code2 === 62) {
      effects.exit("autolinkProtocol");
      effects.enter("autolinkMarker");
      effects.consume(code2);
      effects.exit("autolinkMarker");
      effects.exit("autolink");
      return ok2;
    }
    if (code2 === null || code2 === 32 || code2 === 60 || asciiControl(code2)) {
      return nok(code2);
    }
    effects.consume(code2);
    return urlInside;
  }
  function emailAtext(code2) {
    if (code2 === 64) {
      effects.consume(code2);
      return emailAtSignOrDot;
    }
    if (asciiAtext(code2)) {
      effects.consume(code2);
      return emailAtext;
    }
    return nok(code2);
  }
  function emailAtSignOrDot(code2) {
    return asciiAlphanumeric(code2) ? emailLabel(code2) : nok(code2);
  }
  function emailLabel(code2) {
    if (code2 === 46) {
      effects.consume(code2);
      size = 0;
      return emailAtSignOrDot;
    }
    if (code2 === 62) {
      effects.exit("autolinkProtocol").type = "autolinkEmail";
      effects.enter("autolinkMarker");
      effects.consume(code2);
      effects.exit("autolinkMarker");
      effects.exit("autolink");
      return ok2;
    }
    return emailValue(code2);
  }
  function emailValue(code2) {
    if ((code2 === 45 || asciiAlphanumeric(code2)) && size++ < 63) {
      const next = code2 === 45 ? emailValue : emailLabel;
      effects.consume(code2);
      return next;
    }
    return nok(code2);
  }
}
const blankLine = {
  partial: true,
  tokenize: tokenizeBlankLine
};
function tokenizeBlankLine(effects, ok2, nok) {
  return start;
  function start(code2) {
    return markdownSpace(code2) ? factorySpace(effects, after, "linePrefix")(code2) : after(code2);
  }
  function after(code2) {
    return code2 === null || markdownLineEnding(code2) ? ok2(code2) : nok(code2);
  }
}
const blockQuote = {
  continuation: {
    tokenize: tokenizeBlockQuoteContinuation
  },
  exit: exit$1,
  name: "blockQuote",
  tokenize: tokenizeBlockQuoteStart
};
function tokenizeBlockQuoteStart(effects, ok2, nok) {
  const self2 = this;
  return start;
  function start(code2) {
    if (code2 === 62) {
      const state = self2.containerState;
      if (!state.open) {
        effects.enter("blockQuote", {
          _container: true
        });
        state.open = true;
      }
      effects.enter("blockQuotePrefix");
      effects.enter("blockQuoteMarker");
      effects.consume(code2);
      effects.exit("blockQuoteMarker");
      return after;
    }
    return nok(code2);
  }
  function after(code2) {
    if (markdownSpace(code2)) {
      effects.enter("blockQuotePrefixWhitespace");
      effects.consume(code2);
      effects.exit("blockQuotePrefixWhitespace");
      effects.exit("blockQuotePrefix");
      return ok2;
    }
    effects.exit("blockQuotePrefix");
    return ok2(code2);
  }
}
function tokenizeBlockQuoteContinuation(effects, ok2, nok) {
  const self2 = this;
  return contStart;
  function contStart(code2) {
    if (markdownSpace(code2)) {
      return factorySpace(effects, contBefore, "linePrefix", self2.parser.constructs.disable.null.includes("codeIndented") ? void 0 : 4)(code2);
    }
    return contBefore(code2);
  }
  function contBefore(code2) {
    return effects.attempt(blockQuote, ok2, nok)(code2);
  }
}
function exit$1(effects) {
  effects.exit("blockQuote");
}
const characterEscape = {
  name: "characterEscape",
  tokenize: tokenizeCharacterEscape
};
function tokenizeCharacterEscape(effects, ok2, nok) {
  return start;
  function start(code2) {
    effects.enter("characterEscape");
    effects.enter("escapeMarker");
    effects.consume(code2);
    effects.exit("escapeMarker");
    return inside;
  }
  function inside(code2) {
    if (asciiPunctuation(code2)) {
      effects.enter("characterEscapeValue");
      effects.consume(code2);
      effects.exit("characterEscapeValue");
      effects.exit("characterEscape");
      return ok2;
    }
    return nok(code2);
  }
}
const characterReference = {
  name: "characterReference",
  tokenize: tokenizeCharacterReference
};
function tokenizeCharacterReference(effects, ok2, nok) {
  const self2 = this;
  let size = 0;
  let max;
  let test;
  return start;
  function start(code2) {
    effects.enter("characterReference");
    effects.enter("characterReferenceMarker");
    effects.consume(code2);
    effects.exit("characterReferenceMarker");
    return open;
  }
  function open(code2) {
    if (code2 === 35) {
      effects.enter("characterReferenceMarkerNumeric");
      effects.consume(code2);
      effects.exit("characterReferenceMarkerNumeric");
      return numeric;
    }
    effects.enter("characterReferenceValue");
    max = 31;
    test = asciiAlphanumeric;
    return value(code2);
  }
  function numeric(code2) {
    if (code2 === 88 || code2 === 120) {
      effects.enter("characterReferenceMarkerHexadecimal");
      effects.consume(code2);
      effects.exit("characterReferenceMarkerHexadecimal");
      effects.enter("characterReferenceValue");
      max = 6;
      test = asciiHexDigit;
      return value;
    }
    effects.enter("characterReferenceValue");
    max = 7;
    test = asciiDigit;
    return value(code2);
  }
  function value(code2) {
    if (code2 === 59 && size) {
      const token = effects.exit("characterReferenceValue");
      if (test === asciiAlphanumeric && !decodeNamedCharacterReference(self2.sliceSerialize(token))) {
        return nok(code2);
      }
      effects.enter("characterReferenceMarker");
      effects.consume(code2);
      effects.exit("characterReferenceMarker");
      effects.exit("characterReference");
      return ok2;
    }
    if (test(code2) && size++ < max) {
      effects.consume(code2);
      return value;
    }
    return nok(code2);
  }
}
const nonLazyContinuation = {
  partial: true,
  tokenize: tokenizeNonLazyContinuation
};
const codeFenced = {
  concrete: true,
  name: "codeFenced",
  tokenize: tokenizeCodeFenced
};
function tokenizeCodeFenced(effects, ok2, nok) {
  const self2 = this;
  const closeStart = {
    partial: true,
    tokenize: tokenizeCloseStart
  };
  let initialPrefix = 0;
  let sizeOpen = 0;
  let marker;
  return start;
  function start(code2) {
    return beforeSequenceOpen(code2);
  }
  function beforeSequenceOpen(code2) {
    const tail = self2.events[self2.events.length - 1];
    initialPrefix = tail && tail[1].type === "linePrefix" ? tail[2].sliceSerialize(tail[1], true).length : 0;
    marker = code2;
    effects.enter("codeFenced");
    effects.enter("codeFencedFence");
    effects.enter("codeFencedFenceSequence");
    return sequenceOpen(code2);
  }
  function sequenceOpen(code2) {
    if (code2 === marker) {
      sizeOpen++;
      effects.consume(code2);
      return sequenceOpen;
    }
    if (sizeOpen < 3) {
      return nok(code2);
    }
    effects.exit("codeFencedFenceSequence");
    return markdownSpace(code2) ? factorySpace(effects, infoBefore, "whitespace")(code2) : infoBefore(code2);
  }
  function infoBefore(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("codeFencedFence");
      return self2.interrupt ? ok2(code2) : effects.check(nonLazyContinuation, atNonLazyBreak, after)(code2);
    }
    effects.enter("codeFencedFenceInfo");
    effects.enter("chunkString", {
      contentType: "string"
    });
    return info(code2);
  }
  function info(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("chunkString");
      effects.exit("codeFencedFenceInfo");
      return infoBefore(code2);
    }
    if (markdownSpace(code2)) {
      effects.exit("chunkString");
      effects.exit("codeFencedFenceInfo");
      return factorySpace(effects, metaBefore, "whitespace")(code2);
    }
    if (code2 === 96 && code2 === marker) {
      return nok(code2);
    }
    effects.consume(code2);
    return info;
  }
  function metaBefore(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      return infoBefore(code2);
    }
    effects.enter("codeFencedFenceMeta");
    effects.enter("chunkString", {
      contentType: "string"
    });
    return meta(code2);
  }
  function meta(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("chunkString");
      effects.exit("codeFencedFenceMeta");
      return infoBefore(code2);
    }
    if (code2 === 96 && code2 === marker) {
      return nok(code2);
    }
    effects.consume(code2);
    return meta;
  }
  function atNonLazyBreak(code2) {
    return effects.attempt(closeStart, after, contentBefore)(code2);
  }
  function contentBefore(code2) {
    effects.enter("lineEnding");
    effects.consume(code2);
    effects.exit("lineEnding");
    return contentStart;
  }
  function contentStart(code2) {
    return initialPrefix > 0 && markdownSpace(code2) ? factorySpace(effects, beforeContentChunk, "linePrefix", initialPrefix + 1)(code2) : beforeContentChunk(code2);
  }
  function beforeContentChunk(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      return effects.check(nonLazyContinuation, atNonLazyBreak, after)(code2);
    }
    effects.enter("codeFlowValue");
    return contentChunk(code2);
  }
  function contentChunk(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("codeFlowValue");
      return beforeContentChunk(code2);
    }
    effects.consume(code2);
    return contentChunk;
  }
  function after(code2) {
    effects.exit("codeFenced");
    return ok2(code2);
  }
  function tokenizeCloseStart(effects2, ok3, nok2) {
    let size = 0;
    return startBefore;
    function startBefore(code2) {
      effects2.enter("lineEnding");
      effects2.consume(code2);
      effects2.exit("lineEnding");
      return start2;
    }
    function start2(code2) {
      effects2.enter("codeFencedFence");
      return markdownSpace(code2) ? factorySpace(effects2, beforeSequenceClose, "linePrefix", self2.parser.constructs.disable.null.includes("codeIndented") ? void 0 : 4)(code2) : beforeSequenceClose(code2);
    }
    function beforeSequenceClose(code2) {
      if (code2 === marker) {
        effects2.enter("codeFencedFenceSequence");
        return sequenceClose(code2);
      }
      return nok2(code2);
    }
    function sequenceClose(code2) {
      if (code2 === marker) {
        size++;
        effects2.consume(code2);
        return sequenceClose;
      }
      if (size >= sizeOpen) {
        effects2.exit("codeFencedFenceSequence");
        return markdownSpace(code2) ? factorySpace(effects2, sequenceCloseAfter, "whitespace")(code2) : sequenceCloseAfter(code2);
      }
      return nok2(code2);
    }
    function sequenceCloseAfter(code2) {
      if (code2 === null || markdownLineEnding(code2)) {
        effects2.exit("codeFencedFence");
        return ok3(code2);
      }
      return nok2(code2);
    }
  }
}
function tokenizeNonLazyContinuation(effects, ok2, nok) {
  const self2 = this;
  return start;
  function start(code2) {
    if (code2 === null) {
      return nok(code2);
    }
    effects.enter("lineEnding");
    effects.consume(code2);
    effects.exit("lineEnding");
    return lineStart;
  }
  function lineStart(code2) {
    return self2.parser.lazy[self2.now().line] ? nok(code2) : ok2(code2);
  }
}
const codeIndented = {
  name: "codeIndented",
  tokenize: tokenizeCodeIndented
};
const furtherStart = {
  partial: true,
  tokenize: tokenizeFurtherStart
};
function tokenizeCodeIndented(effects, ok2, nok) {
  const self2 = this;
  return start;
  function start(code2) {
    effects.enter("codeIndented");
    return factorySpace(effects, afterPrefix, "linePrefix", 4 + 1)(code2);
  }
  function afterPrefix(code2) {
    const tail = self2.events[self2.events.length - 1];
    return tail && tail[1].type === "linePrefix" && tail[2].sliceSerialize(tail[1], true).length >= 4 ? atBreak(code2) : nok(code2);
  }
  function atBreak(code2) {
    if (code2 === null) {
      return after(code2);
    }
    if (markdownLineEnding(code2)) {
      return effects.attempt(furtherStart, atBreak, after)(code2);
    }
    effects.enter("codeFlowValue");
    return inside(code2);
  }
  function inside(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("codeFlowValue");
      return atBreak(code2);
    }
    effects.consume(code2);
    return inside;
  }
  function after(code2) {
    effects.exit("codeIndented");
    return ok2(code2);
  }
}
function tokenizeFurtherStart(effects, ok2, nok) {
  const self2 = this;
  return furtherStart2;
  function furtherStart2(code2) {
    if (self2.parser.lazy[self2.now().line]) {
      return nok(code2);
    }
    if (markdownLineEnding(code2)) {
      effects.enter("lineEnding");
      effects.consume(code2);
      effects.exit("lineEnding");
      return furtherStart2;
    }
    return factorySpace(effects, afterPrefix, "linePrefix", 4 + 1)(code2);
  }
  function afterPrefix(code2) {
    const tail = self2.events[self2.events.length - 1];
    return tail && tail[1].type === "linePrefix" && tail[2].sliceSerialize(tail[1], true).length >= 4 ? ok2(code2) : markdownLineEnding(code2) ? furtherStart2(code2) : nok(code2);
  }
}
const codeText = {
  name: "codeText",
  previous: previous$1,
  resolve: resolveCodeText,
  tokenize: tokenizeCodeText
};
function resolveCodeText(events) {
  let tailExitIndex = events.length - 4;
  let headEnterIndex = 3;
  let index2;
  let enter;
  if ((events[headEnterIndex][1].type === "lineEnding" || events[headEnterIndex][1].type === "space") && (events[tailExitIndex][1].type === "lineEnding" || events[tailExitIndex][1].type === "space")) {
    index2 = headEnterIndex;
    while (++index2 < tailExitIndex) {
      if (events[index2][1].type === "codeTextData") {
        events[headEnterIndex][1].type = "codeTextPadding";
        events[tailExitIndex][1].type = "codeTextPadding";
        headEnterIndex += 2;
        tailExitIndex -= 2;
        break;
      }
    }
  }
  index2 = headEnterIndex - 1;
  tailExitIndex++;
  while (++index2 <= tailExitIndex) {
    if (enter === void 0) {
      if (index2 !== tailExitIndex && events[index2][1].type !== "lineEnding") {
        enter = index2;
      }
    } else if (index2 === tailExitIndex || events[index2][1].type === "lineEnding") {
      events[enter][1].type = "codeTextData";
      if (index2 !== enter + 2) {
        events[enter][1].end = events[index2 - 1][1].end;
        events.splice(enter + 2, index2 - enter - 2);
        tailExitIndex -= index2 - enter - 2;
        index2 = enter + 2;
      }
      enter = void 0;
    }
  }
  return events;
}
function previous$1(code2) {
  return code2 !== 96 || this.events[this.events.length - 1][1].type === "characterEscape";
}
function tokenizeCodeText(effects, ok2, nok) {
  let sizeOpen = 0;
  let size;
  let token;
  return start;
  function start(code2) {
    effects.enter("codeText");
    effects.enter("codeTextSequence");
    return sequenceOpen(code2);
  }
  function sequenceOpen(code2) {
    if (code2 === 96) {
      effects.consume(code2);
      sizeOpen++;
      return sequenceOpen;
    }
    effects.exit("codeTextSequence");
    return between(code2);
  }
  function between(code2) {
    if (code2 === null) {
      return nok(code2);
    }
    if (code2 === 32) {
      effects.enter("space");
      effects.consume(code2);
      effects.exit("space");
      return between;
    }
    if (code2 === 96) {
      token = effects.enter("codeTextSequence");
      size = 0;
      return sequenceClose(code2);
    }
    if (markdownLineEnding(code2)) {
      effects.enter("lineEnding");
      effects.consume(code2);
      effects.exit("lineEnding");
      return between;
    }
    effects.enter("codeTextData");
    return data(code2);
  }
  function data(code2) {
    if (code2 === null || code2 === 32 || code2 === 96 || markdownLineEnding(code2)) {
      effects.exit("codeTextData");
      return between(code2);
    }
    effects.consume(code2);
    return data;
  }
  function sequenceClose(code2) {
    if (code2 === 96) {
      effects.consume(code2);
      size++;
      return sequenceClose;
    }
    if (size === sizeOpen) {
      effects.exit("codeTextSequence");
      effects.exit("codeText");
      return ok2(code2);
    }
    token.type = "codeTextData";
    return data(code2);
  }
}
class SpliceBuffer {
  /**
   * @param {ReadonlyArray<T> | null | undefined} [initial]
   *   Initial items (optional).
   * @returns
   *   Splice buffer.
   */
  constructor(initial) {
    this.left = initial ? [...initial] : [];
    this.right = [];
  }
  /**
   * Array access;
   * does not move the cursor.
   *
   * @param {number} index
   *   Index.
   * @return {T}
   *   Item.
   */
  get(index2) {
    if (index2 < 0 || index2 >= this.left.length + this.right.length) {
      throw new RangeError("Cannot access index `" + index2 + "` in a splice buffer of size `" + (this.left.length + this.right.length) + "`");
    }
    if (index2 < this.left.length) return this.left[index2];
    return this.right[this.right.length - index2 + this.left.length - 1];
  }
  /**
   * The length of the splice buffer, one greater than the largest index in the
   * array.
   */
  get length() {
    return this.left.length + this.right.length;
  }
  /**
   * Remove and return `list[0]`;
   * moves the cursor to `0`.
   *
   * @returns {T | undefined}
   *   Item, optional.
   */
  shift() {
    this.setCursor(0);
    return this.right.pop();
  }
  /**
   * Slice the buffer to get an array;
   * does not move the cursor.
   *
   * @param {number} start
   *   Start.
   * @param {number | null | undefined} [end]
   *   End (optional).
   * @returns {Array<T>}
   *   Array of items.
   */
  slice(start, end) {
    const stop = end === null || end === void 0 ? Number.POSITIVE_INFINITY : end;
    if (stop < this.left.length) {
      return this.left.slice(start, stop);
    }
    if (start > this.left.length) {
      return this.right.slice(this.right.length - stop + this.left.length, this.right.length - start + this.left.length).reverse();
    }
    return this.left.slice(start).concat(this.right.slice(this.right.length - stop + this.left.length).reverse());
  }
  /**
   * Mimics the behavior of Array.prototype.splice() except for the change of
   * interface necessary to avoid segfaults when patching in very large arrays.
   *
   * This operation moves cursor is moved to `start` and results in the cursor
   * placed after any inserted items.
   *
   * @param {number} start
   *   Start;
   *   zero-based index at which to start changing the array;
   *   negative numbers count backwards from the end of the array and values
   *   that are out-of bounds are clamped to the appropriate end of the array.
   * @param {number | null | undefined} [deleteCount=0]
   *   Delete count (default: `0`);
   *   maximum number of elements to delete, starting from start.
   * @param {Array<T> | null | undefined} [items=[]]
   *   Items to include in place of the deleted items (default: `[]`).
   * @return {Array<T>}
   *   Any removed items.
   */
  splice(start, deleteCount, items) {
    const count = deleteCount || 0;
    this.setCursor(Math.trunc(start));
    const removed = this.right.splice(this.right.length - count, Number.POSITIVE_INFINITY);
    if (items) chunkedPush(this.left, items);
    return removed.reverse();
  }
  /**
   * Remove and return the highest-numbered item in the array, so
   * `list[list.length - 1]`;
   * Moves the cursor to `length`.
   *
   * @returns {T | undefined}
   *   Item, optional.
   */
  pop() {
    this.setCursor(Number.POSITIVE_INFINITY);
    return this.left.pop();
  }
  /**
   * Inserts a single item to the high-numbered side of the array;
   * moves the cursor to `length`.
   *
   * @param {T} item
   *   Item.
   * @returns {undefined}
   *   Nothing.
   */
  push(item) {
    this.setCursor(Number.POSITIVE_INFINITY);
    this.left.push(item);
  }
  /**
   * Inserts many items to the high-numbered side of the array.
   * Moves the cursor to `length`.
   *
   * @param {Array<T>} items
   *   Items.
   * @returns {undefined}
   *   Nothing.
   */
  pushMany(items) {
    this.setCursor(Number.POSITIVE_INFINITY);
    chunkedPush(this.left, items);
  }
  /**
   * Inserts a single item to the low-numbered side of the array;
   * Moves the cursor to `0`.
   *
   * @param {T} item
   *   Item.
   * @returns {undefined}
   *   Nothing.
   */
  unshift(item) {
    this.setCursor(0);
    this.right.push(item);
  }
  /**
   * Inserts many items to the low-numbered side of the array;
   * moves the cursor to `0`.
   *
   * @param {Array<T>} items
   *   Items.
   * @returns {undefined}
   *   Nothing.
   */
  unshiftMany(items) {
    this.setCursor(0);
    chunkedPush(this.right, items.reverse());
  }
  /**
   * Move the cursor to a specific position in the array. Requires
   * time proportional to the distance moved.
   *
   * If `n < 0`, the cursor will end up at the beginning.
   * If `n > length`, the cursor will end up at the end.
   *
   * @param {number} n
   *   Position.
   * @return {undefined}
   *   Nothing.
   */
  setCursor(n) {
    if (n === this.left.length || n > this.left.length && this.right.length === 0 || n < 0 && this.left.length === 0) return;
    if (n < this.left.length) {
      const removed = this.left.splice(n, Number.POSITIVE_INFINITY);
      chunkedPush(this.right, removed.reverse());
    } else {
      const removed = this.right.splice(this.left.length + this.right.length - n, Number.POSITIVE_INFINITY);
      chunkedPush(this.left, removed.reverse());
    }
  }
}
function chunkedPush(list2, right) {
  let chunkStart = 0;
  if (right.length < 1e4) {
    list2.push(...right);
  } else {
    while (chunkStart < right.length) {
      list2.push(...right.slice(chunkStart, chunkStart + 1e4));
      chunkStart += 1e4;
    }
  }
}
function subtokenize(eventsArray) {
  const jumps = {};
  let index2 = -1;
  let event;
  let lineIndex;
  let otherIndex;
  let otherEvent;
  let parameters;
  let subevents;
  let more;
  const events = new SpliceBuffer(eventsArray);
  while (++index2 < events.length) {
    while (index2 in jumps) {
      index2 = jumps[index2];
    }
    event = events.get(index2);
    if (index2 && event[1].type === "chunkFlow" && events.get(index2 - 1)[1].type === "listItemPrefix") {
      subevents = event[1]._tokenizer.events;
      otherIndex = 0;
      if (otherIndex < subevents.length && subevents[otherIndex][1].type === "lineEndingBlank") {
        otherIndex += 2;
      }
      if (otherIndex < subevents.length && subevents[otherIndex][1].type === "content") {
        while (++otherIndex < subevents.length) {
          if (subevents[otherIndex][1].type === "content") {
            break;
          }
          if (subevents[otherIndex][1].type === "chunkText") {
            subevents[otherIndex][1]._isInFirstContentOfListItem = true;
            otherIndex++;
          }
        }
      }
    }
    if (event[0] === "enter") {
      if (event[1].contentType) {
        Object.assign(jumps, subcontent(events, index2));
        index2 = jumps[index2];
        more = true;
      }
    } else if (event[1]._container) {
      otherIndex = index2;
      lineIndex = void 0;
      while (otherIndex--) {
        otherEvent = events.get(otherIndex);
        if (otherEvent[1].type === "lineEnding" || otherEvent[1].type === "lineEndingBlank") {
          if (otherEvent[0] === "enter") {
            if (lineIndex) {
              events.get(lineIndex)[1].type = "lineEndingBlank";
            }
            otherEvent[1].type = "lineEnding";
            lineIndex = otherIndex;
          }
        } else if (otherEvent[1].type === "linePrefix" || otherEvent[1].type === "listItemIndent") ;
        else {
          break;
        }
      }
      if (lineIndex) {
        event[1].end = {
          ...events.get(lineIndex)[1].start
        };
        parameters = events.slice(lineIndex, index2);
        parameters.unshift(event);
        events.splice(lineIndex, index2 - lineIndex + 1, parameters);
      }
    }
  }
  splice(eventsArray, 0, Number.POSITIVE_INFINITY, events.slice(0));
  return !more;
}
function subcontent(events, eventIndex) {
  const token = events.get(eventIndex)[1];
  const context = events.get(eventIndex)[2];
  let startPosition = eventIndex - 1;
  const startPositions = [];
  let tokenizer = token._tokenizer;
  if (!tokenizer) {
    tokenizer = context.parser[token.contentType](token.start);
    if (token._contentTypeTextTrailing) {
      tokenizer._contentTypeTextTrailing = true;
    }
  }
  const childEvents = tokenizer.events;
  const jumps = [];
  const gaps = {};
  let stream;
  let previous2;
  let index2 = -1;
  let current = token;
  let adjust = 0;
  let start = 0;
  const breaks = [start];
  while (current) {
    while (events.get(++startPosition)[1] !== current) {
    }
    startPositions.push(startPosition);
    if (!current._tokenizer) {
      stream = context.sliceStream(current);
      if (!current.next) {
        stream.push(null);
      }
      if (previous2) {
        tokenizer.defineSkip(current.start);
      }
      if (current._isInFirstContentOfListItem) {
        tokenizer._gfmTasklistFirstContentOfListItem = true;
      }
      tokenizer.write(stream);
      if (current._isInFirstContentOfListItem) {
        tokenizer._gfmTasklistFirstContentOfListItem = void 0;
      }
    }
    previous2 = current;
    current = current.next;
  }
  current = token;
  while (++index2 < childEvents.length) {
    if (
      // Find a void token that includes a break.
      childEvents[index2][0] === "exit" && childEvents[index2 - 1][0] === "enter" && childEvents[index2][1].type === childEvents[index2 - 1][1].type && childEvents[index2][1].start.line !== childEvents[index2][1].end.line
    ) {
      start = index2 + 1;
      breaks.push(start);
      current._tokenizer = void 0;
      current.previous = void 0;
      current = current.next;
    }
  }
  tokenizer.events = [];
  if (current) {
    current._tokenizer = void 0;
    current.previous = void 0;
  } else {
    breaks.pop();
  }
  index2 = breaks.length;
  while (index2--) {
    const slice = childEvents.slice(breaks[index2], breaks[index2 + 1]);
    const start2 = startPositions.pop();
    jumps.push([start2, start2 + slice.length - 1]);
    events.splice(start2, 2, slice);
  }
  jumps.reverse();
  index2 = -1;
  while (++index2 < jumps.length) {
    gaps[adjust + jumps[index2][0]] = adjust + jumps[index2][1];
    adjust += jumps[index2][1] - jumps[index2][0] - 1;
  }
  return gaps;
}
const content = {
  resolve: resolveContent,
  tokenize: tokenizeContent
};
const continuationConstruct = {
  partial: true,
  tokenize: tokenizeContinuation
};
function resolveContent(events) {
  subtokenize(events);
  return events;
}
function tokenizeContent(effects, ok2) {
  let previous2;
  return chunkStart;
  function chunkStart(code2) {
    effects.enter("content");
    previous2 = effects.enter("chunkContent", {
      contentType: "content"
    });
    return chunkInside(code2);
  }
  function chunkInside(code2) {
    if (code2 === null) {
      return contentEnd(code2);
    }
    if (markdownLineEnding(code2)) {
      return effects.check(continuationConstruct, contentContinue, contentEnd)(code2);
    }
    effects.consume(code2);
    return chunkInside;
  }
  function contentEnd(code2) {
    effects.exit("chunkContent");
    effects.exit("content");
    return ok2(code2);
  }
  function contentContinue(code2) {
    effects.consume(code2);
    effects.exit("chunkContent");
    previous2.next = effects.enter("chunkContent", {
      contentType: "content",
      previous: previous2
    });
    previous2 = previous2.next;
    return chunkInside;
  }
}
function tokenizeContinuation(effects, ok2, nok) {
  const self2 = this;
  return startLookahead;
  function startLookahead(code2) {
    effects.exit("chunkContent");
    effects.enter("lineEnding");
    effects.consume(code2);
    effects.exit("lineEnding");
    return factorySpace(effects, prefixed, "linePrefix");
  }
  function prefixed(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      return nok(code2);
    }
    const tail = self2.events[self2.events.length - 1];
    if (!self2.parser.constructs.disable.null.includes("codeIndented") && tail && tail[1].type === "linePrefix" && tail[2].sliceSerialize(tail[1], true).length >= 4) {
      return ok2(code2);
    }
    return effects.interrupt(self2.parser.constructs.flow, nok, ok2)(code2);
  }
}
function factoryDestination(effects, ok2, nok, type, literalType, literalMarkerType, rawType, stringType, max) {
  const limit = max || Number.POSITIVE_INFINITY;
  let balance = 0;
  return start;
  function start(code2) {
    if (code2 === 60) {
      effects.enter(type);
      effects.enter(literalType);
      effects.enter(literalMarkerType);
      effects.consume(code2);
      effects.exit(literalMarkerType);
      return enclosedBefore;
    }
    if (code2 === null || code2 === 32 || code2 === 41 || asciiControl(code2)) {
      return nok(code2);
    }
    effects.enter(type);
    effects.enter(rawType);
    effects.enter(stringType);
    effects.enter("chunkString", {
      contentType: "string"
    });
    return raw(code2);
  }
  function enclosedBefore(code2) {
    if (code2 === 62) {
      effects.enter(literalMarkerType);
      effects.consume(code2);
      effects.exit(literalMarkerType);
      effects.exit(literalType);
      effects.exit(type);
      return ok2;
    }
    effects.enter(stringType);
    effects.enter("chunkString", {
      contentType: "string"
    });
    return enclosed(code2);
  }
  function enclosed(code2) {
    if (code2 === 62) {
      effects.exit("chunkString");
      effects.exit(stringType);
      return enclosedBefore(code2);
    }
    if (code2 === null || code2 === 60 || markdownLineEnding(code2)) {
      return nok(code2);
    }
    effects.consume(code2);
    return code2 === 92 ? enclosedEscape : enclosed;
  }
  function enclosedEscape(code2) {
    if (code2 === 60 || code2 === 62 || code2 === 92) {
      effects.consume(code2);
      return enclosed;
    }
    return enclosed(code2);
  }
  function raw(code2) {
    if (!balance && (code2 === null || code2 === 41 || markdownLineEndingOrSpace(code2))) {
      effects.exit("chunkString");
      effects.exit(stringType);
      effects.exit(rawType);
      effects.exit(type);
      return ok2(code2);
    }
    if (balance < limit && code2 === 40) {
      effects.consume(code2);
      balance++;
      return raw;
    }
    if (code2 === 41) {
      effects.consume(code2);
      balance--;
      return raw;
    }
    if (code2 === null || code2 === 32 || code2 === 40 || asciiControl(code2)) {
      return nok(code2);
    }
    effects.consume(code2);
    return code2 === 92 ? rawEscape : raw;
  }
  function rawEscape(code2) {
    if (code2 === 40 || code2 === 41 || code2 === 92) {
      effects.consume(code2);
      return raw;
    }
    return raw(code2);
  }
}
function factoryLabel(effects, ok2, nok, type, markerType, stringType) {
  const self2 = this;
  let size = 0;
  let seen;
  return start;
  function start(code2) {
    effects.enter(type);
    effects.enter(markerType);
    effects.consume(code2);
    effects.exit(markerType);
    effects.enter(stringType);
    return atBreak;
  }
  function atBreak(code2) {
    if (size > 999 || code2 === null || code2 === 91 || code2 === 93 && !seen || // To do: remove in the future once we’ve switched from
    // `micromark-extension-footnote` to `micromark-extension-gfm-footnote`,
    // which doesn’t need this.
    // Hidden footnotes hook.
    /* c8 ignore next 3 */
    code2 === 94 && !size && "_hiddenFootnoteSupport" in self2.parser.constructs) {
      return nok(code2);
    }
    if (code2 === 93) {
      effects.exit(stringType);
      effects.enter(markerType);
      effects.consume(code2);
      effects.exit(markerType);
      effects.exit(type);
      return ok2;
    }
    if (markdownLineEnding(code2)) {
      effects.enter("lineEnding");
      effects.consume(code2);
      effects.exit("lineEnding");
      return atBreak;
    }
    effects.enter("chunkString", {
      contentType: "string"
    });
    return labelInside(code2);
  }
  function labelInside(code2) {
    if (code2 === null || code2 === 91 || code2 === 93 || markdownLineEnding(code2) || size++ > 999) {
      effects.exit("chunkString");
      return atBreak(code2);
    }
    effects.consume(code2);
    if (!seen) seen = !markdownSpace(code2);
    return code2 === 92 ? labelEscape : labelInside;
  }
  function labelEscape(code2) {
    if (code2 === 91 || code2 === 92 || code2 === 93) {
      effects.consume(code2);
      size++;
      return labelInside;
    }
    return labelInside(code2);
  }
}
function factoryTitle(effects, ok2, nok, type, markerType, stringType) {
  let marker;
  return start;
  function start(code2) {
    if (code2 === 34 || code2 === 39 || code2 === 40) {
      effects.enter(type);
      effects.enter(markerType);
      effects.consume(code2);
      effects.exit(markerType);
      marker = code2 === 40 ? 41 : code2;
      return begin;
    }
    return nok(code2);
  }
  function begin(code2) {
    if (code2 === marker) {
      effects.enter(markerType);
      effects.consume(code2);
      effects.exit(markerType);
      effects.exit(type);
      return ok2;
    }
    effects.enter(stringType);
    return atBreak(code2);
  }
  function atBreak(code2) {
    if (code2 === marker) {
      effects.exit(stringType);
      return begin(marker);
    }
    if (code2 === null) {
      return nok(code2);
    }
    if (markdownLineEnding(code2)) {
      effects.enter("lineEnding");
      effects.consume(code2);
      effects.exit("lineEnding");
      return factorySpace(effects, atBreak, "linePrefix");
    }
    effects.enter("chunkString", {
      contentType: "string"
    });
    return inside(code2);
  }
  function inside(code2) {
    if (code2 === marker || code2 === null || markdownLineEnding(code2)) {
      effects.exit("chunkString");
      return atBreak(code2);
    }
    effects.consume(code2);
    return code2 === 92 ? escape : inside;
  }
  function escape(code2) {
    if (code2 === marker || code2 === 92) {
      effects.consume(code2);
      return inside;
    }
    return inside(code2);
  }
}
function factoryWhitespace(effects, ok2) {
  let seen;
  return start;
  function start(code2) {
    if (markdownLineEnding(code2)) {
      effects.enter("lineEnding");
      effects.consume(code2);
      effects.exit("lineEnding");
      seen = true;
      return start;
    }
    if (markdownSpace(code2)) {
      return factorySpace(effects, start, seen ? "linePrefix" : "lineSuffix")(code2);
    }
    return ok2(code2);
  }
}
const definition$1 = {
  name: "definition",
  tokenize: tokenizeDefinition
};
const titleBefore = {
  partial: true,
  tokenize: tokenizeTitleBefore
};
function tokenizeDefinition(effects, ok2, nok) {
  const self2 = this;
  let identifier;
  return start;
  function start(code2) {
    effects.enter("definition");
    return before(code2);
  }
  function before(code2) {
    return factoryLabel.call(
      self2,
      effects,
      labelAfter,
      // Note: we don’t need to reset the way `markdown-rs` does.
      nok,
      "definitionLabel",
      "definitionLabelMarker",
      "definitionLabelString"
    )(code2);
  }
  function labelAfter(code2) {
    identifier = normalizeIdentifier(self2.sliceSerialize(self2.events[self2.events.length - 1][1]).slice(1, -1));
    if (code2 === 58) {
      effects.enter("definitionMarker");
      effects.consume(code2);
      effects.exit("definitionMarker");
      return markerAfter;
    }
    return nok(code2);
  }
  function markerAfter(code2) {
    return markdownLineEndingOrSpace(code2) ? factoryWhitespace(effects, destinationBefore)(code2) : destinationBefore(code2);
  }
  function destinationBefore(code2) {
    return factoryDestination(
      effects,
      destinationAfter,
      // Note: we don’t need to reset the way `markdown-rs` does.
      nok,
      "definitionDestination",
      "definitionDestinationLiteral",
      "definitionDestinationLiteralMarker",
      "definitionDestinationRaw",
      "definitionDestinationString"
    )(code2);
  }
  function destinationAfter(code2) {
    return effects.attempt(titleBefore, after, after)(code2);
  }
  function after(code2) {
    return markdownSpace(code2) ? factorySpace(effects, afterWhitespace, "whitespace")(code2) : afterWhitespace(code2);
  }
  function afterWhitespace(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("definition");
      self2.parser.defined.push(identifier);
      return ok2(code2);
    }
    return nok(code2);
  }
}
function tokenizeTitleBefore(effects, ok2, nok) {
  return titleBefore2;
  function titleBefore2(code2) {
    return markdownLineEndingOrSpace(code2) ? factoryWhitespace(effects, beforeMarker)(code2) : nok(code2);
  }
  function beforeMarker(code2) {
    return factoryTitle(effects, titleAfter, nok, "definitionTitle", "definitionTitleMarker", "definitionTitleString")(code2);
  }
  function titleAfter(code2) {
    return markdownSpace(code2) ? factorySpace(effects, titleAfterOptionalWhitespace, "whitespace")(code2) : titleAfterOptionalWhitespace(code2);
  }
  function titleAfterOptionalWhitespace(code2) {
    return code2 === null || markdownLineEnding(code2) ? ok2(code2) : nok(code2);
  }
}
const hardBreakEscape = {
  name: "hardBreakEscape",
  tokenize: tokenizeHardBreakEscape
};
function tokenizeHardBreakEscape(effects, ok2, nok) {
  return start;
  function start(code2) {
    effects.enter("hardBreakEscape");
    effects.consume(code2);
    return after;
  }
  function after(code2) {
    if (markdownLineEnding(code2)) {
      effects.exit("hardBreakEscape");
      return ok2(code2);
    }
    return nok(code2);
  }
}
const headingAtx = {
  name: "headingAtx",
  resolve: resolveHeadingAtx,
  tokenize: tokenizeHeadingAtx
};
function resolveHeadingAtx(events, context) {
  let contentEnd = events.length - 2;
  let contentStart = 3;
  let content2;
  let text2;
  if (events[contentStart][1].type === "whitespace") {
    contentStart += 2;
  }
  if (contentEnd - 2 > contentStart && events[contentEnd][1].type === "whitespace") {
    contentEnd -= 2;
  }
  if (events[contentEnd][1].type === "atxHeadingSequence" && (contentStart === contentEnd - 1 || contentEnd - 4 > contentStart && events[contentEnd - 2][1].type === "whitespace")) {
    contentEnd -= contentStart + 1 === contentEnd ? 2 : 4;
  }
  if (contentEnd > contentStart) {
    content2 = {
      type: "atxHeadingText",
      start: events[contentStart][1].start,
      end: events[contentEnd][1].end
    };
    text2 = {
      type: "chunkText",
      start: events[contentStart][1].start,
      end: events[contentEnd][1].end,
      contentType: "text"
    };
    splice(events, contentStart, contentEnd - contentStart + 1, [["enter", content2, context], ["enter", text2, context], ["exit", text2, context], ["exit", content2, context]]);
  }
  return events;
}
function tokenizeHeadingAtx(effects, ok2, nok) {
  let size = 0;
  return start;
  function start(code2) {
    effects.enter("atxHeading");
    return before(code2);
  }
  function before(code2) {
    effects.enter("atxHeadingSequence");
    return sequenceOpen(code2);
  }
  function sequenceOpen(code2) {
    if (code2 === 35 && size++ < 6) {
      effects.consume(code2);
      return sequenceOpen;
    }
    if (code2 === null || markdownLineEndingOrSpace(code2)) {
      effects.exit("atxHeadingSequence");
      return atBreak(code2);
    }
    return nok(code2);
  }
  function atBreak(code2) {
    if (code2 === 35) {
      effects.enter("atxHeadingSequence");
      return sequenceFurther(code2);
    }
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("atxHeading");
      return ok2(code2);
    }
    if (markdownSpace(code2)) {
      return factorySpace(effects, atBreak, "whitespace")(code2);
    }
    effects.enter("atxHeadingText");
    return data(code2);
  }
  function sequenceFurther(code2) {
    if (code2 === 35) {
      effects.consume(code2);
      return sequenceFurther;
    }
    effects.exit("atxHeadingSequence");
    return atBreak(code2);
  }
  function data(code2) {
    if (code2 === null || code2 === 35 || markdownLineEndingOrSpace(code2)) {
      effects.exit("atxHeadingText");
      return atBreak(code2);
    }
    effects.consume(code2);
    return data;
  }
}
const htmlBlockNames = [
  "address",
  "article",
  "aside",
  "base",
  "basefont",
  "blockquote",
  "body",
  "caption",
  "center",
  "col",
  "colgroup",
  "dd",
  "details",
  "dialog",
  "dir",
  "div",
  "dl",
  "dt",
  "fieldset",
  "figcaption",
  "figure",
  "footer",
  "form",
  "frame",
  "frameset",
  "h1",
  "h2",
  "h3",
  "h4",
  "h5",
  "h6",
  "head",
  "header",
  "hr",
  "html",
  "iframe",
  "legend",
  "li",
  "link",
  "main",
  "menu",
  "menuitem",
  "nav",
  "noframes",
  "ol",
  "optgroup",
  "option",
  "p",
  "param",
  "search",
  "section",
  "summary",
  "table",
  "tbody",
  "td",
  "tfoot",
  "th",
  "thead",
  "title",
  "tr",
  "track",
  "ul"
];
const htmlRawNames = ["pre", "script", "style", "textarea"];
const htmlFlow = {
  concrete: true,
  name: "htmlFlow",
  resolveTo: resolveToHtmlFlow,
  tokenize: tokenizeHtmlFlow
};
const blankLineBefore = {
  partial: true,
  tokenize: tokenizeBlankLineBefore
};
const nonLazyContinuationStart = {
  partial: true,
  tokenize: tokenizeNonLazyContinuationStart
};
function resolveToHtmlFlow(events) {
  let index2 = events.length;
  while (index2--) {
    if (events[index2][0] === "enter" && events[index2][1].type === "htmlFlow") {
      break;
    }
  }
  if (index2 > 1 && events[index2 - 2][1].type === "linePrefix") {
    events[index2][1].start = events[index2 - 2][1].start;
    events[index2 + 1][1].start = events[index2 - 2][1].start;
    events.splice(index2 - 2, 2);
  }
  return events;
}
function tokenizeHtmlFlow(effects, ok2, nok) {
  const self2 = this;
  let marker;
  let closingTag;
  let buffer;
  let index2;
  let markerB;
  return start;
  function start(code2) {
    return before(code2);
  }
  function before(code2) {
    effects.enter("htmlFlow");
    effects.enter("htmlFlowData");
    effects.consume(code2);
    return open;
  }
  function open(code2) {
    if (code2 === 33) {
      effects.consume(code2);
      return declarationOpen;
    }
    if (code2 === 47) {
      effects.consume(code2);
      closingTag = true;
      return tagCloseStart;
    }
    if (code2 === 63) {
      effects.consume(code2);
      marker = 3;
      return self2.interrupt ? ok2 : continuationDeclarationInside;
    }
    if (asciiAlpha(code2)) {
      effects.consume(code2);
      buffer = String.fromCharCode(code2);
      return tagName;
    }
    return nok(code2);
  }
  function declarationOpen(code2) {
    if (code2 === 45) {
      effects.consume(code2);
      marker = 2;
      return commentOpenInside;
    }
    if (code2 === 91) {
      effects.consume(code2);
      marker = 5;
      index2 = 0;
      return cdataOpenInside;
    }
    if (asciiAlpha(code2)) {
      effects.consume(code2);
      marker = 4;
      return self2.interrupt ? ok2 : continuationDeclarationInside;
    }
    return nok(code2);
  }
  function commentOpenInside(code2) {
    if (code2 === 45) {
      effects.consume(code2);
      return self2.interrupt ? ok2 : continuationDeclarationInside;
    }
    return nok(code2);
  }
  function cdataOpenInside(code2) {
    const value = "CDATA[";
    if (code2 === value.charCodeAt(index2++)) {
      effects.consume(code2);
      if (index2 === value.length) {
        return self2.interrupt ? ok2 : continuation;
      }
      return cdataOpenInside;
    }
    return nok(code2);
  }
  function tagCloseStart(code2) {
    if (asciiAlpha(code2)) {
      effects.consume(code2);
      buffer = String.fromCharCode(code2);
      return tagName;
    }
    return nok(code2);
  }
  function tagName(code2) {
    if (code2 === null || code2 === 47 || code2 === 62 || markdownLineEndingOrSpace(code2)) {
      const slash = code2 === 47;
      const name2 = buffer.toLowerCase();
      if (!slash && !closingTag && htmlRawNames.includes(name2)) {
        marker = 1;
        return self2.interrupt ? ok2(code2) : continuation(code2);
      }
      if (htmlBlockNames.includes(buffer.toLowerCase())) {
        marker = 6;
        if (slash) {
          effects.consume(code2);
          return basicSelfClosing;
        }
        return self2.interrupt ? ok2(code2) : continuation(code2);
      }
      marker = 7;
      return self2.interrupt && !self2.parser.lazy[self2.now().line] ? nok(code2) : closingTag ? completeClosingTagAfter(code2) : completeAttributeNameBefore(code2);
    }
    if (code2 === 45 || asciiAlphanumeric(code2)) {
      effects.consume(code2);
      buffer += String.fromCharCode(code2);
      return tagName;
    }
    return nok(code2);
  }
  function basicSelfClosing(code2) {
    if (code2 === 62) {
      effects.consume(code2);
      return self2.interrupt ? ok2 : continuation;
    }
    return nok(code2);
  }
  function completeClosingTagAfter(code2) {
    if (markdownSpace(code2)) {
      effects.consume(code2);
      return completeClosingTagAfter;
    }
    return completeEnd(code2);
  }
  function completeAttributeNameBefore(code2) {
    if (code2 === 47) {
      effects.consume(code2);
      return completeEnd;
    }
    if (code2 === 58 || code2 === 95 || asciiAlpha(code2)) {
      effects.consume(code2);
      return completeAttributeName;
    }
    if (markdownSpace(code2)) {
      effects.consume(code2);
      return completeAttributeNameBefore;
    }
    return completeEnd(code2);
  }
  function completeAttributeName(code2) {
    if (code2 === 45 || code2 === 46 || code2 === 58 || code2 === 95 || asciiAlphanumeric(code2)) {
      effects.consume(code2);
      return completeAttributeName;
    }
    return completeAttributeNameAfter(code2);
  }
  function completeAttributeNameAfter(code2) {
    if (code2 === 61) {
      effects.consume(code2);
      return completeAttributeValueBefore;
    }
    if (markdownSpace(code2)) {
      effects.consume(code2);
      return completeAttributeNameAfter;
    }
    return completeAttributeNameBefore(code2);
  }
  function completeAttributeValueBefore(code2) {
    if (code2 === null || code2 === 60 || code2 === 61 || code2 === 62 || code2 === 96) {
      return nok(code2);
    }
    if (code2 === 34 || code2 === 39) {
      effects.consume(code2);
      markerB = code2;
      return completeAttributeValueQuoted;
    }
    if (markdownSpace(code2)) {
      effects.consume(code2);
      return completeAttributeValueBefore;
    }
    return completeAttributeValueUnquoted(code2);
  }
  function completeAttributeValueQuoted(code2) {
    if (code2 === markerB) {
      effects.consume(code2);
      markerB = null;
      return completeAttributeValueQuotedAfter;
    }
    if (code2 === null || markdownLineEnding(code2)) {
      return nok(code2);
    }
    effects.consume(code2);
    return completeAttributeValueQuoted;
  }
  function completeAttributeValueUnquoted(code2) {
    if (code2 === null || code2 === 34 || code2 === 39 || code2 === 47 || code2 === 60 || code2 === 61 || code2 === 62 || code2 === 96 || markdownLineEndingOrSpace(code2)) {
      return completeAttributeNameAfter(code2);
    }
    effects.consume(code2);
    return completeAttributeValueUnquoted;
  }
  function completeAttributeValueQuotedAfter(code2) {
    if (code2 === 47 || code2 === 62 || markdownSpace(code2)) {
      return completeAttributeNameBefore(code2);
    }
    return nok(code2);
  }
  function completeEnd(code2) {
    if (code2 === 62) {
      effects.consume(code2);
      return completeAfter;
    }
    return nok(code2);
  }
  function completeAfter(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      return continuation(code2);
    }
    if (markdownSpace(code2)) {
      effects.consume(code2);
      return completeAfter;
    }
    return nok(code2);
  }
  function continuation(code2) {
    if (code2 === 45 && marker === 2) {
      effects.consume(code2);
      return continuationCommentInside;
    }
    if (code2 === 60 && marker === 1) {
      effects.consume(code2);
      return continuationRawTagOpen;
    }
    if (code2 === 62 && marker === 4) {
      effects.consume(code2);
      return continuationClose;
    }
    if (code2 === 63 && marker === 3) {
      effects.consume(code2);
      return continuationDeclarationInside;
    }
    if (code2 === 93 && marker === 5) {
      effects.consume(code2);
      return continuationCdataInside;
    }
    if (markdownLineEnding(code2) && (marker === 6 || marker === 7)) {
      effects.exit("htmlFlowData");
      return effects.check(blankLineBefore, continuationAfter, continuationStart)(code2);
    }
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("htmlFlowData");
      return continuationStart(code2);
    }
    effects.consume(code2);
    return continuation;
  }
  function continuationStart(code2) {
    return effects.check(nonLazyContinuationStart, continuationStartNonLazy, continuationAfter)(code2);
  }
  function continuationStartNonLazy(code2) {
    effects.enter("lineEnding");
    effects.consume(code2);
    effects.exit("lineEnding");
    return continuationBefore;
  }
  function continuationBefore(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      return continuationStart(code2);
    }
    effects.enter("htmlFlowData");
    return continuation(code2);
  }
  function continuationCommentInside(code2) {
    if (code2 === 45) {
      effects.consume(code2);
      return continuationDeclarationInside;
    }
    return continuation(code2);
  }
  function continuationRawTagOpen(code2) {
    if (code2 === 47) {
      effects.consume(code2);
      buffer = "";
      return continuationRawEndTag;
    }
    return continuation(code2);
  }
  function continuationRawEndTag(code2) {
    if (code2 === 62) {
      const name2 = buffer.toLowerCase();
      if (htmlRawNames.includes(name2)) {
        effects.consume(code2);
        return continuationClose;
      }
      return continuation(code2);
    }
    if (asciiAlpha(code2) && buffer.length < 8) {
      effects.consume(code2);
      buffer += String.fromCharCode(code2);
      return continuationRawEndTag;
    }
    return continuation(code2);
  }
  function continuationCdataInside(code2) {
    if (code2 === 93) {
      effects.consume(code2);
      return continuationDeclarationInside;
    }
    return continuation(code2);
  }
  function continuationDeclarationInside(code2) {
    if (code2 === 62) {
      effects.consume(code2);
      return continuationClose;
    }
    if (code2 === 45 && marker === 2) {
      effects.consume(code2);
      return continuationDeclarationInside;
    }
    return continuation(code2);
  }
  function continuationClose(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("htmlFlowData");
      return continuationAfter(code2);
    }
    effects.consume(code2);
    return continuationClose;
  }
  function continuationAfter(code2) {
    effects.exit("htmlFlow");
    return ok2(code2);
  }
}
function tokenizeNonLazyContinuationStart(effects, ok2, nok) {
  const self2 = this;
  return start;
  function start(code2) {
    if (markdownLineEnding(code2)) {
      effects.enter("lineEnding");
      effects.consume(code2);
      effects.exit("lineEnding");
      return after;
    }
    return nok(code2);
  }
  function after(code2) {
    return self2.parser.lazy[self2.now().line] ? nok(code2) : ok2(code2);
  }
}
function tokenizeBlankLineBefore(effects, ok2, nok) {
  return start;
  function start(code2) {
    effects.enter("lineEnding");
    effects.consume(code2);
    effects.exit("lineEnding");
    return effects.attempt(blankLine, ok2, nok);
  }
}
const htmlText = {
  name: "htmlText",
  tokenize: tokenizeHtmlText
};
function tokenizeHtmlText(effects, ok2, nok) {
  const self2 = this;
  let marker;
  let index2;
  let returnState;
  return start;
  function start(code2) {
    effects.enter("htmlText");
    effects.enter("htmlTextData");
    effects.consume(code2);
    return open;
  }
  function open(code2) {
    if (code2 === 33) {
      effects.consume(code2);
      return declarationOpen;
    }
    if (code2 === 47) {
      effects.consume(code2);
      return tagCloseStart;
    }
    if (code2 === 63) {
      effects.consume(code2);
      return instruction;
    }
    if (asciiAlpha(code2)) {
      effects.consume(code2);
      return tagOpen;
    }
    return nok(code2);
  }
  function declarationOpen(code2) {
    if (code2 === 45) {
      effects.consume(code2);
      return commentOpenInside;
    }
    if (code2 === 91) {
      effects.consume(code2);
      index2 = 0;
      return cdataOpenInside;
    }
    if (asciiAlpha(code2)) {
      effects.consume(code2);
      return declaration;
    }
    return nok(code2);
  }
  function commentOpenInside(code2) {
    if (code2 === 45) {
      effects.consume(code2);
      return commentEnd;
    }
    return nok(code2);
  }
  function comment(code2) {
    if (code2 === null) {
      return nok(code2);
    }
    if (code2 === 45) {
      effects.consume(code2);
      return commentClose;
    }
    if (markdownLineEnding(code2)) {
      returnState = comment;
      return lineEndingBefore(code2);
    }
    effects.consume(code2);
    return comment;
  }
  function commentClose(code2) {
    if (code2 === 45) {
      effects.consume(code2);
      return commentEnd;
    }
    return comment(code2);
  }
  function commentEnd(code2) {
    return code2 === 62 ? end(code2) : code2 === 45 ? commentClose(code2) : comment(code2);
  }
  function cdataOpenInside(code2) {
    const value = "CDATA[";
    if (code2 === value.charCodeAt(index2++)) {
      effects.consume(code2);
      return index2 === value.length ? cdata : cdataOpenInside;
    }
    return nok(code2);
  }
  function cdata(code2) {
    if (code2 === null) {
      return nok(code2);
    }
    if (code2 === 93) {
      effects.consume(code2);
      return cdataClose;
    }
    if (markdownLineEnding(code2)) {
      returnState = cdata;
      return lineEndingBefore(code2);
    }
    effects.consume(code2);
    return cdata;
  }
  function cdataClose(code2) {
    if (code2 === 93) {
      effects.consume(code2);
      return cdataEnd;
    }
    return cdata(code2);
  }
  function cdataEnd(code2) {
    if (code2 === 62) {
      return end(code2);
    }
    if (code2 === 93) {
      effects.consume(code2);
      return cdataEnd;
    }
    return cdata(code2);
  }
  function declaration(code2) {
    if (code2 === null || code2 === 62) {
      return end(code2);
    }
    if (markdownLineEnding(code2)) {
      returnState = declaration;
      return lineEndingBefore(code2);
    }
    effects.consume(code2);
    return declaration;
  }
  function instruction(code2) {
    if (code2 === null) {
      return nok(code2);
    }
    if (code2 === 63) {
      effects.consume(code2);
      return instructionClose;
    }
    if (markdownLineEnding(code2)) {
      returnState = instruction;
      return lineEndingBefore(code2);
    }
    effects.consume(code2);
    return instruction;
  }
  function instructionClose(code2) {
    return code2 === 62 ? end(code2) : instruction(code2);
  }
  function tagCloseStart(code2) {
    if (asciiAlpha(code2)) {
      effects.consume(code2);
      return tagClose;
    }
    return nok(code2);
  }
  function tagClose(code2) {
    if (code2 === 45 || asciiAlphanumeric(code2)) {
      effects.consume(code2);
      return tagClose;
    }
    return tagCloseBetween(code2);
  }
  function tagCloseBetween(code2) {
    if (markdownLineEnding(code2)) {
      returnState = tagCloseBetween;
      return lineEndingBefore(code2);
    }
    if (markdownSpace(code2)) {
      effects.consume(code2);
      return tagCloseBetween;
    }
    return end(code2);
  }
  function tagOpen(code2) {
    if (code2 === 45 || asciiAlphanumeric(code2)) {
      effects.consume(code2);
      return tagOpen;
    }
    if (code2 === 47 || code2 === 62 || markdownLineEndingOrSpace(code2)) {
      return tagOpenBetween(code2);
    }
    return nok(code2);
  }
  function tagOpenBetween(code2) {
    if (code2 === 47) {
      effects.consume(code2);
      return end;
    }
    if (code2 === 58 || code2 === 95 || asciiAlpha(code2)) {
      effects.consume(code2);
      return tagOpenAttributeName;
    }
    if (markdownLineEnding(code2)) {
      returnState = tagOpenBetween;
      return lineEndingBefore(code2);
    }
    if (markdownSpace(code2)) {
      effects.consume(code2);
      return tagOpenBetween;
    }
    return end(code2);
  }
  function tagOpenAttributeName(code2) {
    if (code2 === 45 || code2 === 46 || code2 === 58 || code2 === 95 || asciiAlphanumeric(code2)) {
      effects.consume(code2);
      return tagOpenAttributeName;
    }
    return tagOpenAttributeNameAfter(code2);
  }
  function tagOpenAttributeNameAfter(code2) {
    if (code2 === 61) {
      effects.consume(code2);
      return tagOpenAttributeValueBefore;
    }
    if (markdownLineEnding(code2)) {
      returnState = tagOpenAttributeNameAfter;
      return lineEndingBefore(code2);
    }
    if (markdownSpace(code2)) {
      effects.consume(code2);
      return tagOpenAttributeNameAfter;
    }
    return tagOpenBetween(code2);
  }
  function tagOpenAttributeValueBefore(code2) {
    if (code2 === null || code2 === 60 || code2 === 61 || code2 === 62 || code2 === 96) {
      return nok(code2);
    }
    if (code2 === 34 || code2 === 39) {
      effects.consume(code2);
      marker = code2;
      return tagOpenAttributeValueQuoted;
    }
    if (markdownLineEnding(code2)) {
      returnState = tagOpenAttributeValueBefore;
      return lineEndingBefore(code2);
    }
    if (markdownSpace(code2)) {
      effects.consume(code2);
      return tagOpenAttributeValueBefore;
    }
    effects.consume(code2);
    return tagOpenAttributeValueUnquoted;
  }
  function tagOpenAttributeValueQuoted(code2) {
    if (code2 === marker) {
      effects.consume(code2);
      marker = void 0;
      return tagOpenAttributeValueQuotedAfter;
    }
    if (code2 === null) {
      return nok(code2);
    }
    if (markdownLineEnding(code2)) {
      returnState = tagOpenAttributeValueQuoted;
      return lineEndingBefore(code2);
    }
    effects.consume(code2);
    return tagOpenAttributeValueQuoted;
  }
  function tagOpenAttributeValueUnquoted(code2) {
    if (code2 === null || code2 === 34 || code2 === 39 || code2 === 60 || code2 === 61 || code2 === 96) {
      return nok(code2);
    }
    if (code2 === 47 || code2 === 62 || markdownLineEndingOrSpace(code2)) {
      return tagOpenBetween(code2);
    }
    effects.consume(code2);
    return tagOpenAttributeValueUnquoted;
  }
  function tagOpenAttributeValueQuotedAfter(code2) {
    if (code2 === 47 || code2 === 62 || markdownLineEndingOrSpace(code2)) {
      return tagOpenBetween(code2);
    }
    return nok(code2);
  }
  function end(code2) {
    if (code2 === 62) {
      effects.consume(code2);
      effects.exit("htmlTextData");
      effects.exit("htmlText");
      return ok2;
    }
    return nok(code2);
  }
  function lineEndingBefore(code2) {
    effects.exit("htmlTextData");
    effects.enter("lineEnding");
    effects.consume(code2);
    effects.exit("lineEnding");
    return lineEndingAfter;
  }
  function lineEndingAfter(code2) {
    return markdownSpace(code2) ? factorySpace(effects, lineEndingAfterPrefix, "linePrefix", self2.parser.constructs.disable.null.includes("codeIndented") ? void 0 : 4)(code2) : lineEndingAfterPrefix(code2);
  }
  function lineEndingAfterPrefix(code2) {
    effects.enter("htmlTextData");
    return returnState(code2);
  }
}
const labelEnd = {
  name: "labelEnd",
  resolveAll: resolveAllLabelEnd,
  resolveTo: resolveToLabelEnd,
  tokenize: tokenizeLabelEnd
};
const resourceConstruct = {
  tokenize: tokenizeResource
};
const referenceFullConstruct = {
  tokenize: tokenizeReferenceFull
};
const referenceCollapsedConstruct = {
  tokenize: tokenizeReferenceCollapsed
};
function resolveAllLabelEnd(events) {
  let index2 = -1;
  const newEvents = [];
  while (++index2 < events.length) {
    const token = events[index2][1];
    newEvents.push(events[index2]);
    if (token.type === "labelImage" || token.type === "labelLink" || token.type === "labelEnd") {
      const offset = token.type === "labelImage" ? 4 : 2;
      token.type = "data";
      index2 += offset;
    }
  }
  if (events.length !== newEvents.length) {
    splice(events, 0, events.length, newEvents);
  }
  return events;
}
function resolveToLabelEnd(events, context) {
  let index2 = events.length;
  let offset = 0;
  let token;
  let open;
  let close;
  let media;
  while (index2--) {
    token = events[index2][1];
    if (open) {
      if (token.type === "link" || token.type === "labelLink" && token._inactive) {
        break;
      }
      if (events[index2][0] === "enter" && token.type === "labelLink") {
        token._inactive = true;
      }
    } else if (close) {
      if (events[index2][0] === "enter" && (token.type === "labelImage" || token.type === "labelLink") && !token._balanced) {
        open = index2;
        if (token.type !== "labelLink") {
          offset = 2;
          break;
        }
      }
    } else if (token.type === "labelEnd") {
      close = index2;
    }
  }
  const group = {
    type: events[open][1].type === "labelLink" ? "link" : "image",
    start: {
      ...events[open][1].start
    },
    end: {
      ...events[events.length - 1][1].end
    }
  };
  const label = {
    type: "label",
    start: {
      ...events[open][1].start
    },
    end: {
      ...events[close][1].end
    }
  };
  const text2 = {
    type: "labelText",
    start: {
      ...events[open + offset + 2][1].end
    },
    end: {
      ...events[close - 2][1].start
    }
  };
  media = [["enter", group, context], ["enter", label, context]];
  media = push(media, events.slice(open + 1, open + offset + 3));
  media = push(media, [["enter", text2, context]]);
  media = push(media, resolveAll(context.parser.constructs.insideSpan.null, events.slice(open + offset + 4, close - 3), context));
  media = push(media, [["exit", text2, context], events[close - 2], events[close - 1], ["exit", label, context]]);
  media = push(media, events.slice(close + 1));
  media = push(media, [["exit", group, context]]);
  splice(events, open, events.length, media);
  return events;
}
function tokenizeLabelEnd(effects, ok2, nok) {
  const self2 = this;
  let index2 = self2.events.length;
  let labelStart;
  let defined;
  while (index2--) {
    if ((self2.events[index2][1].type === "labelImage" || self2.events[index2][1].type === "labelLink") && !self2.events[index2][1]._balanced) {
      labelStart = self2.events[index2][1];
      break;
    }
  }
  return start;
  function start(code2) {
    if (!labelStart) {
      return nok(code2);
    }
    if (labelStart._inactive) {
      return labelEndNok(code2);
    }
    defined = self2.parser.defined.includes(normalizeIdentifier(self2.sliceSerialize({
      start: labelStart.end,
      end: self2.now()
    })));
    effects.enter("labelEnd");
    effects.enter("labelMarker");
    effects.consume(code2);
    effects.exit("labelMarker");
    effects.exit("labelEnd");
    return after;
  }
  function after(code2) {
    if (code2 === 40) {
      return effects.attempt(resourceConstruct, labelEndOk, defined ? labelEndOk : labelEndNok)(code2);
    }
    if (code2 === 91) {
      return effects.attempt(referenceFullConstruct, labelEndOk, defined ? referenceNotFull : labelEndNok)(code2);
    }
    return defined ? labelEndOk(code2) : labelEndNok(code2);
  }
  function referenceNotFull(code2) {
    return effects.attempt(referenceCollapsedConstruct, labelEndOk, labelEndNok)(code2);
  }
  function labelEndOk(code2) {
    return ok2(code2);
  }
  function labelEndNok(code2) {
    labelStart._balanced = true;
    return nok(code2);
  }
}
function tokenizeResource(effects, ok2, nok) {
  return resourceStart;
  function resourceStart(code2) {
    effects.enter("resource");
    effects.enter("resourceMarker");
    effects.consume(code2);
    effects.exit("resourceMarker");
    return resourceBefore;
  }
  function resourceBefore(code2) {
    return markdownLineEndingOrSpace(code2) ? factoryWhitespace(effects, resourceOpen)(code2) : resourceOpen(code2);
  }
  function resourceOpen(code2) {
    if (code2 === 41) {
      return resourceEnd(code2);
    }
    return factoryDestination(effects, resourceDestinationAfter, resourceDestinationMissing, "resourceDestination", "resourceDestinationLiteral", "resourceDestinationLiteralMarker", "resourceDestinationRaw", "resourceDestinationString", 32)(code2);
  }
  function resourceDestinationAfter(code2) {
    return markdownLineEndingOrSpace(code2) ? factoryWhitespace(effects, resourceBetween)(code2) : resourceEnd(code2);
  }
  function resourceDestinationMissing(code2) {
    return nok(code2);
  }
  function resourceBetween(code2) {
    if (code2 === 34 || code2 === 39 || code2 === 40) {
      return factoryTitle(effects, resourceTitleAfter, nok, "resourceTitle", "resourceTitleMarker", "resourceTitleString")(code2);
    }
    return resourceEnd(code2);
  }
  function resourceTitleAfter(code2) {
    return markdownLineEndingOrSpace(code2) ? factoryWhitespace(effects, resourceEnd)(code2) : resourceEnd(code2);
  }
  function resourceEnd(code2) {
    if (code2 === 41) {
      effects.enter("resourceMarker");
      effects.consume(code2);
      effects.exit("resourceMarker");
      effects.exit("resource");
      return ok2;
    }
    return nok(code2);
  }
}
function tokenizeReferenceFull(effects, ok2, nok) {
  const self2 = this;
  return referenceFull;
  function referenceFull(code2) {
    return factoryLabel.call(self2, effects, referenceFullAfter, referenceFullMissing, "reference", "referenceMarker", "referenceString")(code2);
  }
  function referenceFullAfter(code2) {
    return self2.parser.defined.includes(normalizeIdentifier(self2.sliceSerialize(self2.events[self2.events.length - 1][1]).slice(1, -1))) ? ok2(code2) : nok(code2);
  }
  function referenceFullMissing(code2) {
    return nok(code2);
  }
}
function tokenizeReferenceCollapsed(effects, ok2, nok) {
  return referenceCollapsedStart;
  function referenceCollapsedStart(code2) {
    effects.enter("reference");
    effects.enter("referenceMarker");
    effects.consume(code2);
    effects.exit("referenceMarker");
    return referenceCollapsedOpen;
  }
  function referenceCollapsedOpen(code2) {
    if (code2 === 93) {
      effects.enter("referenceMarker");
      effects.consume(code2);
      effects.exit("referenceMarker");
      effects.exit("reference");
      return ok2;
    }
    return nok(code2);
  }
}
const labelStartImage = {
  name: "labelStartImage",
  resolveAll: labelEnd.resolveAll,
  tokenize: tokenizeLabelStartImage
};
function tokenizeLabelStartImage(effects, ok2, nok) {
  const self2 = this;
  return start;
  function start(code2) {
    effects.enter("labelImage");
    effects.enter("labelImageMarker");
    effects.consume(code2);
    effects.exit("labelImageMarker");
    return open;
  }
  function open(code2) {
    if (code2 === 91) {
      effects.enter("labelMarker");
      effects.consume(code2);
      effects.exit("labelMarker");
      effects.exit("labelImage");
      return after;
    }
    return nok(code2);
  }
  function after(code2) {
    return code2 === 94 && "_hiddenFootnoteSupport" in self2.parser.constructs ? nok(code2) : ok2(code2);
  }
}
const labelStartLink = {
  name: "labelStartLink",
  resolveAll: labelEnd.resolveAll,
  tokenize: tokenizeLabelStartLink
};
function tokenizeLabelStartLink(effects, ok2, nok) {
  const self2 = this;
  return start;
  function start(code2) {
    effects.enter("labelLink");
    effects.enter("labelMarker");
    effects.consume(code2);
    effects.exit("labelMarker");
    effects.exit("labelLink");
    return after;
  }
  function after(code2) {
    return code2 === 94 && "_hiddenFootnoteSupport" in self2.parser.constructs ? nok(code2) : ok2(code2);
  }
}
const lineEnding = {
  name: "lineEnding",
  tokenize: tokenizeLineEnding
};
function tokenizeLineEnding(effects, ok2) {
  return start;
  function start(code2) {
    effects.enter("lineEnding");
    effects.consume(code2);
    effects.exit("lineEnding");
    return factorySpace(effects, ok2, "linePrefix");
  }
}
const thematicBreak$2 = {
  name: "thematicBreak",
  tokenize: tokenizeThematicBreak
};
function tokenizeThematicBreak(effects, ok2, nok) {
  let size = 0;
  let marker;
  return start;
  function start(code2) {
    effects.enter("thematicBreak");
    return before(code2);
  }
  function before(code2) {
    marker = code2;
    return atBreak(code2);
  }
  function atBreak(code2) {
    if (code2 === marker) {
      effects.enter("thematicBreakSequence");
      return sequence(code2);
    }
    if (size >= 3 && (code2 === null || markdownLineEnding(code2))) {
      effects.exit("thematicBreak");
      return ok2(code2);
    }
    return nok(code2);
  }
  function sequence(code2) {
    if (code2 === marker) {
      effects.consume(code2);
      size++;
      return sequence;
    }
    effects.exit("thematicBreakSequence");
    return markdownSpace(code2) ? factorySpace(effects, atBreak, "whitespace")(code2) : atBreak(code2);
  }
}
const list$2 = {
  continuation: {
    tokenize: tokenizeListContinuation
  },
  exit: tokenizeListEnd,
  name: "list",
  tokenize: tokenizeListStart
};
const listItemPrefixWhitespaceConstruct = {
  partial: true,
  tokenize: tokenizeListItemPrefixWhitespace
};
const indentConstruct = {
  partial: true,
  tokenize: tokenizeIndent$1
};
function tokenizeListStart(effects, ok2, nok) {
  const self2 = this;
  const tail = self2.events[self2.events.length - 1];
  let initialSize = tail && tail[1].type === "linePrefix" ? tail[2].sliceSerialize(tail[1], true).length : 0;
  let size = 0;
  return start;
  function start(code2) {
    const kind = self2.containerState.type || (code2 === 42 || code2 === 43 || code2 === 45 ? "listUnordered" : "listOrdered");
    if (kind === "listUnordered" ? !self2.containerState.marker || code2 === self2.containerState.marker : asciiDigit(code2)) {
      if (!self2.containerState.type) {
        self2.containerState.type = kind;
        effects.enter(kind, {
          _container: true
        });
      }
      if (kind === "listUnordered") {
        effects.enter("listItemPrefix");
        return code2 === 42 || code2 === 45 ? effects.check(thematicBreak$2, nok, atMarker)(code2) : atMarker(code2);
      }
      if (!self2.interrupt || code2 === 49) {
        effects.enter("listItemPrefix");
        effects.enter("listItemValue");
        return inside(code2);
      }
    }
    return nok(code2);
  }
  function inside(code2) {
    if (asciiDigit(code2) && ++size < 10) {
      effects.consume(code2);
      return inside;
    }
    if ((!self2.interrupt || size < 2) && (self2.containerState.marker ? code2 === self2.containerState.marker : code2 === 41 || code2 === 46)) {
      effects.exit("listItemValue");
      return atMarker(code2);
    }
    return nok(code2);
  }
  function atMarker(code2) {
    effects.enter("listItemMarker");
    effects.consume(code2);
    effects.exit("listItemMarker");
    self2.containerState.marker = self2.containerState.marker || code2;
    return effects.check(
      blankLine,
      // Can’t be empty when interrupting.
      self2.interrupt ? nok : onBlank,
      effects.attempt(listItemPrefixWhitespaceConstruct, endOfPrefix, otherPrefix)
    );
  }
  function onBlank(code2) {
    self2.containerState.initialBlankLine = true;
    initialSize++;
    return endOfPrefix(code2);
  }
  function otherPrefix(code2) {
    if (markdownSpace(code2)) {
      effects.enter("listItemPrefixWhitespace");
      effects.consume(code2);
      effects.exit("listItemPrefixWhitespace");
      return endOfPrefix;
    }
    return nok(code2);
  }
  function endOfPrefix(code2) {
    self2.containerState.size = initialSize + self2.sliceSerialize(effects.exit("listItemPrefix"), true).length;
    return ok2(code2);
  }
}
function tokenizeListContinuation(effects, ok2, nok) {
  const self2 = this;
  self2.containerState._closeFlow = void 0;
  return effects.check(blankLine, onBlank, notBlank);
  function onBlank(code2) {
    self2.containerState.furtherBlankLines = self2.containerState.furtherBlankLines || self2.containerState.initialBlankLine;
    return factorySpace(effects, ok2, "listItemIndent", self2.containerState.size + 1)(code2);
  }
  function notBlank(code2) {
    if (self2.containerState.furtherBlankLines || !markdownSpace(code2)) {
      self2.containerState.furtherBlankLines = void 0;
      self2.containerState.initialBlankLine = void 0;
      return notInCurrentItem(code2);
    }
    self2.containerState.furtherBlankLines = void 0;
    self2.containerState.initialBlankLine = void 0;
    return effects.attempt(indentConstruct, ok2, notInCurrentItem)(code2);
  }
  function notInCurrentItem(code2) {
    self2.containerState._closeFlow = true;
    self2.interrupt = void 0;
    return factorySpace(effects, effects.attempt(list$2, ok2, nok), "linePrefix", self2.parser.constructs.disable.null.includes("codeIndented") ? void 0 : 4)(code2);
  }
}
function tokenizeIndent$1(effects, ok2, nok) {
  const self2 = this;
  return factorySpace(effects, afterPrefix, "listItemIndent", self2.containerState.size + 1);
  function afterPrefix(code2) {
    const tail = self2.events[self2.events.length - 1];
    return tail && tail[1].type === "listItemIndent" && tail[2].sliceSerialize(tail[1], true).length === self2.containerState.size ? ok2(code2) : nok(code2);
  }
}
function tokenizeListEnd(effects) {
  effects.exit(this.containerState.type);
}
function tokenizeListItemPrefixWhitespace(effects, ok2, nok) {
  const self2 = this;
  return factorySpace(effects, afterPrefix, "listItemPrefixWhitespace", self2.parser.constructs.disable.null.includes("codeIndented") ? void 0 : 4 + 1);
  function afterPrefix(code2) {
    const tail = self2.events[self2.events.length - 1];
    return !markdownSpace(code2) && tail && tail[1].type === "listItemPrefixWhitespace" ? ok2(code2) : nok(code2);
  }
}
const setextUnderline = {
  name: "setextUnderline",
  resolveTo: resolveToSetextUnderline,
  tokenize: tokenizeSetextUnderline
};
function resolveToSetextUnderline(events, context) {
  let index2 = events.length;
  let content2;
  let text2;
  let definition2;
  while (index2--) {
    if (events[index2][0] === "enter") {
      if (events[index2][1].type === "content") {
        content2 = index2;
        break;
      }
      if (events[index2][1].type === "paragraph") {
        text2 = index2;
      }
    } else {
      if (events[index2][1].type === "content") {
        events.splice(index2, 1);
      }
      if (!definition2 && events[index2][1].type === "definition") {
        definition2 = index2;
      }
    }
  }
  const heading2 = {
    type: "setextHeading",
    start: {
      ...events[content2][1].start
    },
    end: {
      ...events[events.length - 1][1].end
    }
  };
  events[text2][1].type = "setextHeadingText";
  if (definition2) {
    events.splice(text2, 0, ["enter", heading2, context]);
    events.splice(definition2 + 1, 0, ["exit", events[content2][1], context]);
    events[content2][1].end = {
      ...events[definition2][1].end
    };
  } else {
    events[content2][1] = heading2;
  }
  events.push(["exit", heading2, context]);
  return events;
}
function tokenizeSetextUnderline(effects, ok2, nok) {
  const self2 = this;
  let marker;
  return start;
  function start(code2) {
    let index2 = self2.events.length;
    let paragraph2;
    while (index2--) {
      if (self2.events[index2][1].type !== "lineEnding" && self2.events[index2][1].type !== "linePrefix" && self2.events[index2][1].type !== "content") {
        paragraph2 = self2.events[index2][1].type === "paragraph";
        break;
      }
    }
    if (!self2.parser.lazy[self2.now().line] && (self2.interrupt || paragraph2)) {
      effects.enter("setextHeadingLine");
      marker = code2;
      return before(code2);
    }
    return nok(code2);
  }
  function before(code2) {
    effects.enter("setextHeadingLineSequence");
    return inside(code2);
  }
  function inside(code2) {
    if (code2 === marker) {
      effects.consume(code2);
      return inside;
    }
    effects.exit("setextHeadingLineSequence");
    return markdownSpace(code2) ? factorySpace(effects, after, "lineSuffix")(code2) : after(code2);
  }
  function after(code2) {
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("setextHeadingLine");
      return ok2(code2);
    }
    return nok(code2);
  }
}
const flow$1 = {
  tokenize: initializeFlow
};
function initializeFlow(effects) {
  const self2 = this;
  const initial = effects.attempt(
    // Try to parse a blank line.
    blankLine,
    atBlankEnding,
    // Try to parse initial flow (essentially, only code).
    effects.attempt(this.parser.constructs.flowInitial, afterConstruct, factorySpace(effects, effects.attempt(this.parser.constructs.flow, afterConstruct, effects.attempt(content, afterConstruct)), "linePrefix"))
  );
  return initial;
  function atBlankEnding(code2) {
    if (code2 === null) {
      effects.consume(code2);
      return;
    }
    effects.enter("lineEndingBlank");
    effects.consume(code2);
    effects.exit("lineEndingBlank");
    self2.currentConstruct = void 0;
    return initial;
  }
  function afterConstruct(code2) {
    if (code2 === null) {
      effects.consume(code2);
      return;
    }
    effects.enter("lineEnding");
    effects.consume(code2);
    effects.exit("lineEnding");
    self2.currentConstruct = void 0;
    return initial;
  }
}
const resolver = {
  resolveAll: createResolver()
};
const string$1 = initializeFactory("string");
const text$4 = initializeFactory("text");
function initializeFactory(field) {
  return {
    resolveAll: createResolver(field === "text" ? resolveAllLineSuffixes : void 0),
    tokenize: initializeText
  };
  function initializeText(effects) {
    const self2 = this;
    const constructs2 = this.parser.constructs[field];
    const text2 = effects.attempt(constructs2, start, notText);
    return start;
    function start(code2) {
      return atBreak(code2) ? text2(code2) : notText(code2);
    }
    function notText(code2) {
      if (code2 === null) {
        effects.consume(code2);
        return;
      }
      effects.enter("data");
      effects.consume(code2);
      return data;
    }
    function data(code2) {
      if (atBreak(code2)) {
        effects.exit("data");
        return text2(code2);
      }
      effects.consume(code2);
      return data;
    }
    function atBreak(code2) {
      if (code2 === null) {
        return true;
      }
      const list2 = constructs2[code2];
      let index2 = -1;
      if (list2) {
        while (++index2 < list2.length) {
          const item = list2[index2];
          if (!item.previous || item.previous.call(self2, self2.previous)) {
            return true;
          }
        }
      }
      return false;
    }
  }
}
function createResolver(extraResolver) {
  return resolveAllText;
  function resolveAllText(events, context) {
    let index2 = -1;
    let enter;
    while (++index2 <= events.length) {
      if (enter === void 0) {
        if (events[index2] && events[index2][1].type === "data") {
          enter = index2;
          index2++;
        }
      } else if (!events[index2] || events[index2][1].type !== "data") {
        if (index2 !== enter + 2) {
          events[enter][1].end = events[index2 - 1][1].end;
          events.splice(enter + 2, index2 - enter - 2);
          index2 = enter + 2;
        }
        enter = void 0;
      }
    }
    return extraResolver ? extraResolver(events, context) : events;
  }
}
function resolveAllLineSuffixes(events, context) {
  let eventIndex = 0;
  while (++eventIndex <= events.length) {
    if ((eventIndex === events.length || events[eventIndex][1].type === "lineEnding") && events[eventIndex - 1][1].type === "data") {
      const data = events[eventIndex - 1][1];
      const chunks = context.sliceStream(data);
      let index2 = chunks.length;
      let bufferIndex = -1;
      let size = 0;
      let tabs;
      while (index2--) {
        const chunk = chunks[index2];
        if (typeof chunk === "string") {
          bufferIndex = chunk.length;
          while (chunk.charCodeAt(bufferIndex - 1) === 32) {
            size++;
            bufferIndex--;
          }
          if (bufferIndex) break;
          bufferIndex = -1;
        } else if (chunk === -2) {
          tabs = true;
          size++;
        } else if (chunk === -1) ;
        else {
          index2++;
          break;
        }
      }
      if (context._contentTypeTextTrailing && eventIndex === events.length) {
        size = 0;
      }
      if (size) {
        const token = {
          type: eventIndex === events.length || tabs || size < 2 ? "lineSuffix" : "hardBreakTrailing",
          start: {
            _bufferIndex: index2 ? bufferIndex : data.start._bufferIndex + bufferIndex,
            _index: data.start._index + index2,
            line: data.end.line,
            column: data.end.column - size,
            offset: data.end.offset - size
          },
          end: {
            ...data.end
          }
        };
        data.end = {
          ...token.start
        };
        if (data.start.offset === data.end.offset) {
          Object.assign(data, token);
        } else {
          events.splice(eventIndex, 0, ["enter", token, context], ["exit", token, context]);
          eventIndex += 2;
        }
      }
      eventIndex++;
    }
  }
  return events;
}
const document$1 = {
  [42]: list$2,
  [43]: list$2,
  [45]: list$2,
  [48]: list$2,
  [49]: list$2,
  [50]: list$2,
  [51]: list$2,
  [52]: list$2,
  [53]: list$2,
  [54]: list$2,
  [55]: list$2,
  [56]: list$2,
  [57]: list$2,
  [62]: blockQuote
};
const contentInitial = {
  [91]: definition$1
};
const flowInitial = {
  [-2]: codeIndented,
  [-1]: codeIndented,
  [32]: codeIndented
};
const flow = {
  [35]: headingAtx,
  [42]: thematicBreak$2,
  [45]: [setextUnderline, thematicBreak$2],
  [60]: htmlFlow,
  [61]: setextUnderline,
  [95]: thematicBreak$2,
  [96]: codeFenced,
  [126]: codeFenced
};
const string = {
  [38]: characterReference,
  [92]: characterEscape
};
const text$3 = {
  [-5]: lineEnding,
  [-4]: lineEnding,
  [-3]: lineEnding,
  [33]: labelStartImage,
  [38]: characterReference,
  [42]: attention,
  [60]: [autolink, htmlText],
  [91]: labelStartLink,
  [92]: [hardBreakEscape, characterEscape],
  [93]: labelEnd,
  [95]: attention,
  [96]: codeText
};
const insideSpan = {
  null: [attention, resolver]
};
const attentionMarkers = {
  null: [42, 95]
};
const disable = {
  null: []
};
const defaultConstructs = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  attentionMarkers,
  contentInitial,
  disable,
  document: document$1,
  flow,
  flowInitial,
  insideSpan,
  string,
  text: text$3
}, Symbol.toStringTag, { value: "Module" }));
function createTokenizer(parser, initialize, from) {
  let point2 = {
    _bufferIndex: -1,
    _index: 0,
    line: from && from.line || 1,
    column: from && from.column || 1,
    offset: from && from.offset || 0
  };
  const columnStart = {};
  const resolveAllConstructs = [];
  let chunks = [];
  let stack = [];
  const effects = {
    attempt: constructFactory(onsuccessfulconstruct),
    check: constructFactory(onsuccessfulcheck),
    consume,
    enter,
    exit: exit2,
    interrupt: constructFactory(onsuccessfulcheck, {
      interrupt: true
    })
  };
  const context = {
    code: null,
    containerState: {},
    defineSkip,
    events: [],
    now,
    parser,
    previous: null,
    sliceSerialize,
    sliceStream,
    write
  };
  let state = initialize.tokenize.call(context, effects);
  if (initialize.resolveAll) {
    resolveAllConstructs.push(initialize);
  }
  return context;
  function write(slice) {
    chunks = push(chunks, slice);
    main();
    if (chunks[chunks.length - 1] !== null) {
      return [];
    }
    addResult(initialize, 0);
    context.events = resolveAll(resolveAllConstructs, context.events, context);
    return context.events;
  }
  function sliceSerialize(token, expandTabs) {
    return serializeChunks(sliceStream(token), expandTabs);
  }
  function sliceStream(token) {
    return sliceChunks(chunks, token);
  }
  function now() {
    const {
      _bufferIndex,
      _index,
      line,
      column,
      offset
    } = point2;
    return {
      _bufferIndex,
      _index,
      line,
      column,
      offset
    };
  }
  function defineSkip(value) {
    columnStart[value.line] = value.column;
    accountForPotentialSkip();
  }
  function main() {
    let chunkIndex;
    while (point2._index < chunks.length) {
      const chunk = chunks[point2._index];
      if (typeof chunk === "string") {
        chunkIndex = point2._index;
        if (point2._bufferIndex < 0) {
          point2._bufferIndex = 0;
        }
        while (point2._index === chunkIndex && point2._bufferIndex < chunk.length) {
          go(chunk.charCodeAt(point2._bufferIndex));
        }
      } else {
        go(chunk);
      }
    }
  }
  function go(code2) {
    state = state(code2);
  }
  function consume(code2) {
    if (markdownLineEnding(code2)) {
      point2.line++;
      point2.column = 1;
      point2.offset += code2 === -3 ? 2 : 1;
      accountForPotentialSkip();
    } else if (code2 !== -1) {
      point2.column++;
      point2.offset++;
    }
    if (point2._bufferIndex < 0) {
      point2._index++;
    } else {
      point2._bufferIndex++;
      if (point2._bufferIndex === // Points w/ non-negative `_bufferIndex` reference
      // strings.
      /** @type {string} */
      chunks[point2._index].length) {
        point2._bufferIndex = -1;
        point2._index++;
      }
    }
    context.previous = code2;
  }
  function enter(type, fields) {
    const token = fields || {};
    token.type = type;
    token.start = now();
    context.events.push(["enter", token, context]);
    stack.push(token);
    return token;
  }
  function exit2(type) {
    const token = stack.pop();
    token.end = now();
    context.events.push(["exit", token, context]);
    return token;
  }
  function onsuccessfulconstruct(construct, info) {
    addResult(construct, info.from);
  }
  function onsuccessfulcheck(_, info) {
    info.restore();
  }
  function constructFactory(onreturn, fields) {
    return hook;
    function hook(constructs2, returnState, bogusState) {
      let listOfConstructs;
      let constructIndex;
      let currentConstruct;
      let info;
      return Array.isArray(constructs2) ? (
        /* c8 ignore next 1 */
        handleListOfConstructs(constructs2)
      ) : "tokenize" in constructs2 ? (
        // Looks like a construct.
        handleListOfConstructs([
          /** @type {Construct} */
          constructs2
        ])
      ) : handleMapOfConstructs(constructs2);
      function handleMapOfConstructs(map2) {
        return start;
        function start(code2) {
          const left = code2 !== null && map2[code2];
          const all2 = code2 !== null && map2.null;
          const list2 = [
            // To do: add more extension tests.
            /* c8 ignore next 2 */
            ...Array.isArray(left) ? left : left ? [left] : [],
            ...Array.isArray(all2) ? all2 : all2 ? [all2] : []
          ];
          return handleListOfConstructs(list2)(code2);
        }
      }
      function handleListOfConstructs(list2) {
        listOfConstructs = list2;
        constructIndex = 0;
        if (list2.length === 0) {
          return bogusState;
        }
        return handleConstruct(list2[constructIndex]);
      }
      function handleConstruct(construct) {
        return start;
        function start(code2) {
          info = store();
          currentConstruct = construct;
          if (!construct.partial) {
            context.currentConstruct = construct;
          }
          if (construct.name && context.parser.constructs.disable.null.includes(construct.name)) {
            return nok();
          }
          return construct.tokenize.call(
            // If we do have fields, create an object w/ `context` as its
            // prototype.
            // This allows a “live binding”, which is needed for `interrupt`.
            fields ? Object.assign(Object.create(context), fields) : context,
            effects,
            ok2,
            nok
          )(code2);
        }
      }
      function ok2(code2) {
        onreturn(currentConstruct, info);
        return returnState;
      }
      function nok(code2) {
        info.restore();
        if (++constructIndex < listOfConstructs.length) {
          return handleConstruct(listOfConstructs[constructIndex]);
        }
        return bogusState;
      }
    }
  }
  function addResult(construct, from2) {
    if (construct.resolveAll && !resolveAllConstructs.includes(construct)) {
      resolveAllConstructs.push(construct);
    }
    if (construct.resolve) {
      splice(context.events, from2, context.events.length - from2, construct.resolve(context.events.slice(from2), context));
    }
    if (construct.resolveTo) {
      context.events = construct.resolveTo(context.events, context);
    }
  }
  function store() {
    const startPoint = now();
    const startPrevious = context.previous;
    const startCurrentConstruct = context.currentConstruct;
    const startEventsIndex = context.events.length;
    const startStack = Array.from(stack);
    return {
      from: startEventsIndex,
      restore
    };
    function restore() {
      point2 = startPoint;
      context.previous = startPrevious;
      context.currentConstruct = startCurrentConstruct;
      context.events.length = startEventsIndex;
      stack = startStack;
      accountForPotentialSkip();
    }
  }
  function accountForPotentialSkip() {
    if (point2.line in columnStart && point2.column < 2) {
      point2.column = columnStart[point2.line];
      point2.offset += columnStart[point2.line] - 1;
    }
  }
}
function sliceChunks(chunks, token) {
  const startIndex = token.start._index;
  const startBufferIndex = token.start._bufferIndex;
  const endIndex = token.end._index;
  const endBufferIndex = token.end._bufferIndex;
  let view;
  if (startIndex === endIndex) {
    view = [chunks[startIndex].slice(startBufferIndex, endBufferIndex)];
  } else {
    view = chunks.slice(startIndex, endIndex);
    if (startBufferIndex > -1) {
      const head = view[0];
      if (typeof head === "string") {
        view[0] = head.slice(startBufferIndex);
      } else {
        view.shift();
      }
    }
    if (endBufferIndex > 0) {
      view.push(chunks[endIndex].slice(0, endBufferIndex));
    }
  }
  return view;
}
function serializeChunks(chunks, expandTabs) {
  let index2 = -1;
  const result = [];
  let atTab;
  while (++index2 < chunks.length) {
    const chunk = chunks[index2];
    let value;
    if (typeof chunk === "string") {
      value = chunk;
    } else switch (chunk) {
      case -5: {
        value = "\r";
        break;
      }
      case -4: {
        value = "\n";
        break;
      }
      case -3: {
        value = "\r\n";
        break;
      }
      case -2: {
        value = expandTabs ? " " : "	";
        break;
      }
      case -1: {
        if (!expandTabs && atTab) continue;
        value = " ";
        break;
      }
      default: {
        value = String.fromCharCode(chunk);
      }
    }
    atTab = chunk === -2;
    result.push(value);
  }
  return result.join("");
}
function parse(options) {
  const settings = options || {};
  const constructs2 = (
    /** @type {FullNormalizedExtension} */
    combineExtensions([defaultConstructs, ...settings.extensions || []])
  );
  const parser = {
    constructs: constructs2,
    content: create(content$1),
    defined: [],
    document: create(document$2),
    flow: create(flow$1),
    lazy: {},
    string: create(string$1),
    text: create(text$4)
  };
  return parser;
  function create(initial) {
    return creator;
    function creator(from) {
      return createTokenizer(parser, initial, from);
    }
  }
}
function postprocess(events) {
  while (!subtokenize(events)) {
  }
  return events;
}
const search = /[\0\t\n\r]/g;
function preprocess() {
  let column = 1;
  let buffer = "";
  let start = true;
  let atCarriageReturn;
  return preprocessor;
  function preprocessor(value, encoding, end) {
    const chunks = [];
    let match;
    let next;
    let startPosition;
    let endPosition;
    let code2;
    value = buffer + (typeof value === "string" ? value.toString() : new TextDecoder(encoding || void 0).decode(value));
    startPosition = 0;
    buffer = "";
    if (start) {
      if (value.charCodeAt(0) === 65279) {
        startPosition++;
      }
      start = void 0;
    }
    while (startPosition < value.length) {
      search.lastIndex = startPosition;
      match = search.exec(value);
      endPosition = match && match.index !== void 0 ? match.index : value.length;
      code2 = value.charCodeAt(endPosition);
      if (!match) {
        buffer = value.slice(startPosition);
        break;
      }
      if (code2 === 10 && startPosition === endPosition && atCarriageReturn) {
        chunks.push(-3);
        atCarriageReturn = void 0;
      } else {
        if (atCarriageReturn) {
          chunks.push(-5);
          atCarriageReturn = void 0;
        }
        if (startPosition < endPosition) {
          chunks.push(value.slice(startPosition, endPosition));
          column += endPosition - startPosition;
        }
        switch (code2) {
          case 0: {
            chunks.push(65533);
            column++;
            break;
          }
          case 9: {
            next = Math.ceil(column / 4) * 4;
            chunks.push(-2);
            while (column++ < next) chunks.push(-1);
            break;
          }
          case 10: {
            chunks.push(-4);
            column = 1;
            break;
          }
          default: {
            atCarriageReturn = true;
            column = 1;
          }
        }
      }
      startPosition = endPosition + 1;
    }
    if (end) {
      if (atCarriageReturn) chunks.push(-5);
      if (buffer) chunks.push(buffer);
      chunks.push(null);
    }
    return chunks;
  }
}
const characterEscapeOrReference = /\\([!-/:-@[-`{-~])|&(#(?:\d{1,7}|x[\da-f]{1,6})|[\da-z]{1,31});/gi;
function decodeString(value) {
  return value.replace(characterEscapeOrReference, decode);
}
function decode($0, $1, $2) {
  if ($1) {
    return $1;
  }
  const head = $2.charCodeAt(0);
  if (head === 35) {
    const head2 = $2.charCodeAt(1);
    const hex = head2 === 120 || head2 === 88;
    return decodeNumericCharacterReference($2.slice(hex ? 2 : 1), hex ? 16 : 10);
  }
  return decodeNamedCharacterReference($2) || $0;
}
const own$2 = {}.hasOwnProperty;
function fromMarkdown(value, encoding, options) {
  if (encoding && typeof encoding === "object") {
    options = encoding;
    encoding = void 0;
  }
  return compiler(options)(postprocess(parse(options).document().write(preprocess()(value, encoding, true))));
}
function compiler(options) {
  const config = {
    transforms: [],
    canContainEols: ["emphasis", "fragment", "heading", "paragraph", "strong"],
    enter: {
      autolink: opener(link2),
      autolinkProtocol: onenterdata,
      autolinkEmail: onenterdata,
      atxHeading: opener(heading2),
      blockQuote: opener(blockQuote2),
      characterEscape: onenterdata,
      characterReference: onenterdata,
      codeFenced: opener(codeFlow),
      codeFencedFenceInfo: buffer,
      codeFencedFenceMeta: buffer,
      codeIndented: opener(codeFlow, buffer),
      codeText: opener(codeText2, buffer),
      codeTextData: onenterdata,
      data: onenterdata,
      codeFlowValue: onenterdata,
      definition: opener(definition2),
      definitionDestinationString: buffer,
      definitionLabelString: buffer,
      definitionTitleString: buffer,
      emphasis: opener(emphasis2),
      hardBreakEscape: opener(hardBreak2),
      hardBreakTrailing: opener(hardBreak2),
      htmlFlow: opener(html2, buffer),
      htmlFlowData: onenterdata,
      htmlText: opener(html2, buffer),
      htmlTextData: onenterdata,
      image: opener(image2),
      label: buffer,
      link: opener(link2),
      listItem: opener(listItem2),
      listItemValue: onenterlistitemvalue,
      listOrdered: opener(list2, onenterlistordered),
      listUnordered: opener(list2),
      paragraph: opener(paragraph2),
      reference: onenterreference,
      referenceString: buffer,
      resourceDestinationString: buffer,
      resourceTitleString: buffer,
      setextHeading: opener(heading2),
      strong: opener(strong2),
      thematicBreak: opener(thematicBreak2)
    },
    exit: {
      atxHeading: closer(),
      atxHeadingSequence: onexitatxheadingsequence,
      autolink: closer(),
      autolinkEmail: onexitautolinkemail,
      autolinkProtocol: onexitautolinkprotocol,
      blockQuote: closer(),
      characterEscapeValue: onexitdata,
      characterReferenceMarkerHexadecimal: onexitcharacterreferencemarker,
      characterReferenceMarkerNumeric: onexitcharacterreferencemarker,
      characterReferenceValue: onexitcharacterreferencevalue,
      characterReference: onexitcharacterreference,
      codeFenced: closer(onexitcodefenced),
      codeFencedFence: onexitcodefencedfence,
      codeFencedFenceInfo: onexitcodefencedfenceinfo,
      codeFencedFenceMeta: onexitcodefencedfencemeta,
      codeFlowValue: onexitdata,
      codeIndented: closer(onexitcodeindented),
      codeText: closer(onexitcodetext),
      codeTextData: onexitdata,
      data: onexitdata,
      definition: closer(),
      definitionDestinationString: onexitdefinitiondestinationstring,
      definitionLabelString: onexitdefinitionlabelstring,
      definitionTitleString: onexitdefinitiontitlestring,
      emphasis: closer(),
      hardBreakEscape: closer(onexithardbreak),
      hardBreakTrailing: closer(onexithardbreak),
      htmlFlow: closer(onexithtmlflow),
      htmlFlowData: onexitdata,
      htmlText: closer(onexithtmltext),
      htmlTextData: onexitdata,
      image: closer(onexitimage),
      label: onexitlabel,
      labelText: onexitlabeltext,
      lineEnding: onexitlineending,
      link: closer(onexitlink),
      listItem: closer(),
      listOrdered: closer(),
      listUnordered: closer(),
      paragraph: closer(),
      referenceString: onexitreferencestring,
      resourceDestinationString: onexitresourcedestinationstring,
      resourceTitleString: onexitresourcetitlestring,
      resource: onexitresource,
      setextHeading: closer(onexitsetextheading),
      setextHeadingLineSequence: onexitsetextheadinglinesequence,
      setextHeadingText: onexitsetextheadingtext,
      strong: closer(),
      thematicBreak: closer()
    }
  };
  configure(config, (options || {}).mdastExtensions || []);
  const data = {};
  return compile;
  function compile(events) {
    let tree = {
      type: "root",
      children: []
    };
    const context = {
      stack: [tree],
      tokenStack: [],
      config,
      enter,
      exit: exit2,
      buffer,
      resume,
      data
    };
    const listStack = [];
    let index2 = -1;
    while (++index2 < events.length) {
      if (events[index2][1].type === "listOrdered" || events[index2][1].type === "listUnordered") {
        if (events[index2][0] === "enter") {
          listStack.push(index2);
        } else {
          const tail = listStack.pop();
          index2 = prepareList(events, tail, index2);
        }
      }
    }
    index2 = -1;
    while (++index2 < events.length) {
      const handler = config[events[index2][0]];
      if (own$2.call(handler, events[index2][1].type)) {
        handler[events[index2][1].type].call(Object.assign({
          sliceSerialize: events[index2][2].sliceSerialize
        }, context), events[index2][1]);
      }
    }
    if (context.tokenStack.length > 0) {
      const tail = context.tokenStack[context.tokenStack.length - 1];
      const handler = tail[1] || defaultOnError;
      handler.call(context, void 0, tail[0]);
    }
    tree.position = {
      start: point(events.length > 0 ? events[0][1].start : {
        line: 1,
        column: 1,
        offset: 0
      }),
      end: point(events.length > 0 ? events[events.length - 2][1].end : {
        line: 1,
        column: 1,
        offset: 0
      })
    };
    index2 = -1;
    while (++index2 < config.transforms.length) {
      tree = config.transforms[index2](tree) || tree;
    }
    return tree;
  }
  function prepareList(events, start, length) {
    let index2 = start - 1;
    let containerBalance = -1;
    let listSpread = false;
    let listItem3;
    let lineIndex;
    let firstBlankLineIndex;
    let atMarker;
    while (++index2 <= length) {
      const event = events[index2];
      switch (event[1].type) {
        case "listUnordered":
        case "listOrdered":
        case "blockQuote": {
          if (event[0] === "enter") {
            containerBalance++;
          } else {
            containerBalance--;
          }
          atMarker = void 0;
          break;
        }
        case "lineEndingBlank": {
          if (event[0] === "enter") {
            if (listItem3 && !atMarker && !containerBalance && !firstBlankLineIndex) {
              firstBlankLineIndex = index2;
            }
            atMarker = void 0;
          }
          break;
        }
        case "linePrefix":
        case "listItemValue":
        case "listItemMarker":
        case "listItemPrefix":
        case "listItemPrefixWhitespace": {
          break;
        }
        default: {
          atMarker = void 0;
        }
      }
      if (!containerBalance && event[0] === "enter" && event[1].type === "listItemPrefix" || containerBalance === -1 && event[0] === "exit" && (event[1].type === "listUnordered" || event[1].type === "listOrdered")) {
        if (listItem3) {
          let tailIndex = index2;
          lineIndex = void 0;
          while (tailIndex--) {
            const tailEvent = events[tailIndex];
            if (tailEvent[1].type === "lineEnding" || tailEvent[1].type === "lineEndingBlank") {
              if (tailEvent[0] === "exit") continue;
              if (lineIndex) {
                events[lineIndex][1].type = "lineEndingBlank";
                listSpread = true;
              }
              tailEvent[1].type = "lineEnding";
              lineIndex = tailIndex;
            } else if (tailEvent[1].type === "linePrefix" || tailEvent[1].type === "blockQuotePrefix" || tailEvent[1].type === "blockQuotePrefixWhitespace" || tailEvent[1].type === "blockQuoteMarker" || tailEvent[1].type === "listItemIndent") ;
            else {
              break;
            }
          }
          if (firstBlankLineIndex && (!lineIndex || firstBlankLineIndex < lineIndex)) {
            listItem3._spread = true;
          }
          listItem3.end = Object.assign({}, lineIndex ? events[lineIndex][1].start : event[1].end);
          events.splice(lineIndex || index2, 0, ["exit", listItem3, event[2]]);
          index2++;
          length++;
        }
        if (event[1].type === "listItemPrefix") {
          const item = {
            type: "listItem",
            _spread: false,
            start: Object.assign({}, event[1].start),
            // @ts-expect-error: we’ll add `end` in a second.
            end: void 0
          };
          listItem3 = item;
          events.splice(index2, 0, ["enter", item, event[2]]);
          index2++;
          length++;
          firstBlankLineIndex = void 0;
          atMarker = true;
        }
      }
    }
    events[start][1]._spread = listSpread;
    return length;
  }
  function opener(create, and) {
    return open;
    function open(token) {
      enter.call(this, create(token), token);
      if (and) and.call(this, token);
    }
  }
  function buffer() {
    this.stack.push({
      type: "fragment",
      children: []
    });
  }
  function enter(node2, token, errorHandler) {
    const parent = this.stack[this.stack.length - 1];
    const siblings = parent.children;
    siblings.push(node2);
    this.stack.push(node2);
    this.tokenStack.push([token, errorHandler || void 0]);
    node2.position = {
      start: point(token.start),
      // @ts-expect-error: `end` will be patched later.
      end: void 0
    };
  }
  function closer(and) {
    return close;
    function close(token) {
      if (and) and.call(this, token);
      exit2.call(this, token);
    }
  }
  function exit2(token, onExitError) {
    const node2 = this.stack.pop();
    const open = this.tokenStack.pop();
    if (!open) {
      throw new Error("Cannot close `" + token.type + "` (" + stringifyPosition({
        start: token.start,
        end: token.end
      }) + "): it’s not open");
    } else if (open[0].type !== token.type) {
      if (onExitError) {
        onExitError.call(this, token, open[0]);
      } else {
        const handler = open[1] || defaultOnError;
        handler.call(this, token, open[0]);
      }
    }
    node2.position.end = point(token.end);
  }
  function resume() {
    return toString$1(this.stack.pop());
  }
  function onenterlistordered() {
    this.data.expectingFirstListItemValue = true;
  }
  function onenterlistitemvalue(token) {
    if (this.data.expectingFirstListItemValue) {
      const ancestor = this.stack[this.stack.length - 2];
      ancestor.start = Number.parseInt(this.sliceSerialize(token), 10);
      this.data.expectingFirstListItemValue = void 0;
    }
  }
  function onexitcodefencedfenceinfo() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.lang = data2;
  }
  function onexitcodefencedfencemeta() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.meta = data2;
  }
  function onexitcodefencedfence() {
    if (this.data.flowCodeInside) return;
    this.buffer();
    this.data.flowCodeInside = true;
  }
  function onexitcodefenced() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.value = data2.replace(/^(\r?\n|\r)|(\r?\n|\r)$/g, "");
    this.data.flowCodeInside = void 0;
  }
  function onexitcodeindented() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.value = data2.replace(/(\r?\n|\r)$/g, "");
  }
  function onexitdefinitionlabelstring(token) {
    const label = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.label = label;
    node2.identifier = normalizeIdentifier(this.sliceSerialize(token)).toLowerCase();
  }
  function onexitdefinitiontitlestring() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.title = data2;
  }
  function onexitdefinitiondestinationstring() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.url = data2;
  }
  function onexitatxheadingsequence(token) {
    const node2 = this.stack[this.stack.length - 1];
    if (!node2.depth) {
      const depth = this.sliceSerialize(token).length;
      node2.depth = depth;
    }
  }
  function onexitsetextheadingtext() {
    this.data.setextHeadingSlurpLineEnding = true;
  }
  function onexitsetextheadinglinesequence(token) {
    const node2 = this.stack[this.stack.length - 1];
    node2.depth = this.sliceSerialize(token).codePointAt(0) === 61 ? 1 : 2;
  }
  function onexitsetextheading() {
    this.data.setextHeadingSlurpLineEnding = void 0;
  }
  function onenterdata(token) {
    const node2 = this.stack[this.stack.length - 1];
    const siblings = node2.children;
    let tail = siblings[siblings.length - 1];
    if (!tail || tail.type !== "text") {
      tail = text2();
      tail.position = {
        start: point(token.start),
        // @ts-expect-error: we’ll add `end` later.
        end: void 0
      };
      siblings.push(tail);
    }
    this.stack.push(tail);
  }
  function onexitdata(token) {
    const tail = this.stack.pop();
    tail.value += this.sliceSerialize(token);
    tail.position.end = point(token.end);
  }
  function onexitlineending(token) {
    const context = this.stack[this.stack.length - 1];
    if (this.data.atHardBreak) {
      const tail = context.children[context.children.length - 1];
      tail.position.end = point(token.end);
      this.data.atHardBreak = void 0;
      return;
    }
    if (!this.data.setextHeadingSlurpLineEnding && config.canContainEols.includes(context.type)) {
      onenterdata.call(this, token);
      onexitdata.call(this, token);
    }
  }
  function onexithardbreak() {
    this.data.atHardBreak = true;
  }
  function onexithtmlflow() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.value = data2;
  }
  function onexithtmltext() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.value = data2;
  }
  function onexitcodetext() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.value = data2;
  }
  function onexitlink() {
    const node2 = this.stack[this.stack.length - 1];
    if (this.data.inReference) {
      const referenceType = this.data.referenceType || "shortcut";
      node2.type += "Reference";
      node2.referenceType = referenceType;
      delete node2.url;
      delete node2.title;
    } else {
      delete node2.identifier;
      delete node2.label;
    }
    this.data.referenceType = void 0;
  }
  function onexitimage() {
    const node2 = this.stack[this.stack.length - 1];
    if (this.data.inReference) {
      const referenceType = this.data.referenceType || "shortcut";
      node2.type += "Reference";
      node2.referenceType = referenceType;
      delete node2.url;
      delete node2.title;
    } else {
      delete node2.identifier;
      delete node2.label;
    }
    this.data.referenceType = void 0;
  }
  function onexitlabeltext(token) {
    const string2 = this.sliceSerialize(token);
    const ancestor = this.stack[this.stack.length - 2];
    ancestor.label = decodeString(string2);
    ancestor.identifier = normalizeIdentifier(string2).toLowerCase();
  }
  function onexitlabel() {
    const fragment = this.stack[this.stack.length - 1];
    const value = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    this.data.inReference = true;
    if (node2.type === "link") {
      const children = fragment.children;
      node2.children = children;
    } else {
      node2.alt = value;
    }
  }
  function onexitresourcedestinationstring() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.url = data2;
  }
  function onexitresourcetitlestring() {
    const data2 = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.title = data2;
  }
  function onexitresource() {
    this.data.inReference = void 0;
  }
  function onenterreference() {
    this.data.referenceType = "collapsed";
  }
  function onexitreferencestring(token) {
    const label = this.resume();
    const node2 = this.stack[this.stack.length - 1];
    node2.label = label;
    node2.identifier = normalizeIdentifier(this.sliceSerialize(token)).toLowerCase();
    this.data.referenceType = "full";
  }
  function onexitcharacterreferencemarker(token) {
    this.data.characterReferenceType = token.type;
  }
  function onexitcharacterreferencevalue(token) {
    const data2 = this.sliceSerialize(token);
    const type = this.data.characterReferenceType;
    let value;
    if (type) {
      value = decodeNumericCharacterReference(data2, type === "characterReferenceMarkerNumeric" ? 10 : 16);
      this.data.characterReferenceType = void 0;
    } else {
      const result = decodeNamedCharacterReference(data2);
      value = result;
    }
    const tail = this.stack[this.stack.length - 1];
    tail.value += value;
  }
  function onexitcharacterreference(token) {
    const tail = this.stack.pop();
    tail.position.end = point(token.end);
  }
  function onexitautolinkprotocol(token) {
    onexitdata.call(this, token);
    const node2 = this.stack[this.stack.length - 1];
    node2.url = this.sliceSerialize(token);
  }
  function onexitautolinkemail(token) {
    onexitdata.call(this, token);
    const node2 = this.stack[this.stack.length - 1];
    node2.url = "mailto:" + this.sliceSerialize(token);
  }
  function blockQuote2() {
    return {
      type: "blockquote",
      children: []
    };
  }
  function codeFlow() {
    return {
      type: "code",
      lang: null,
      meta: null,
      value: ""
    };
  }
  function codeText2() {
    return {
      type: "inlineCode",
      value: ""
    };
  }
  function definition2() {
    return {
      type: "definition",
      identifier: "",
      label: null,
      title: null,
      url: ""
    };
  }
  function emphasis2() {
    return {
      type: "emphasis",
      children: []
    };
  }
  function heading2() {
    return {
      type: "heading",
      // @ts-expect-error `depth` will be set later.
      depth: 0,
      children: []
    };
  }
  function hardBreak2() {
    return {
      type: "break"
    };
  }
  function html2() {
    return {
      type: "html",
      value: ""
    };
  }
  function image2() {
    return {
      type: "image",
      title: null,
      url: "",
      alt: null
    };
  }
  function link2() {
    return {
      type: "link",
      title: null,
      url: "",
      children: []
    };
  }
  function list2(token) {
    return {
      type: "list",
      ordered: token.type === "listOrdered",
      start: null,
      spread: token._spread,
      children: []
    };
  }
  function listItem2(token) {
    return {
      type: "listItem",
      spread: token._spread,
      checked: null,
      children: []
    };
  }
  function paragraph2() {
    return {
      type: "paragraph",
      children: []
    };
  }
  function strong2() {
    return {
      type: "strong",
      children: []
    };
  }
  function text2() {
    return {
      type: "text",
      value: ""
    };
  }
  function thematicBreak2() {
    return {
      type: "thematicBreak"
    };
  }
}
function point(d) {
  return {
    line: d.line,
    column: d.column,
    offset: d.offset
  };
}
function configure(combined, extensions) {
  let index2 = -1;
  while (++index2 < extensions.length) {
    const value = extensions[index2];
    if (Array.isArray(value)) {
      configure(combined, value);
    } else {
      extension(combined, value);
    }
  }
}
function extension(combined, extension2) {
  let key;
  for (key in extension2) {
    if (own$2.call(extension2, key)) {
      switch (key) {
        case "canContainEols": {
          const right = extension2[key];
          if (right) {
            combined[key].push(...right);
          }
          break;
        }
        case "transforms": {
          const right = extension2[key];
          if (right) {
            combined[key].push(...right);
          }
          break;
        }
        case "enter":
        case "exit": {
          const right = extension2[key];
          if (right) {
            Object.assign(combined[key], right);
          }
          break;
        }
      }
    }
  }
}
function defaultOnError(left, right) {
  if (left) {
    throw new Error("Cannot close `" + left.type + "` (" + stringifyPosition({
      start: left.start,
      end: left.end
    }) + "): a different token (`" + right.type + "`, " + stringifyPosition({
      start: right.start,
      end: right.end
    }) + ") is open");
  } else {
    throw new Error("Cannot close document, a token (`" + right.type + "`, " + stringifyPosition({
      start: right.start,
      end: right.end
    }) + ") is still open");
  }
}
function remarkParse(options) {
  const self2 = this;
  self2.parser = parser;
  function parser(doc) {
    return fromMarkdown(doc, {
      ...self2.data("settings"),
      ...options,
      // Note: these options are not in the readme.
      // The goal is for them to be set by plugins on `data` instead of being
      // passed by users.
      extensions: self2.data("micromarkExtensions") || [],
      mdastExtensions: self2.data("fromMarkdownExtensions") || []
    });
  }
}
function blockquote$1(state, node2) {
  const result = {
    type: "element",
    tagName: "blockquote",
    properties: {},
    children: state.wrap(state.all(node2), true)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function hardBreak$1(state, node2) {
  const result = { type: "element", tagName: "br", properties: {}, children: [] };
  state.patch(node2, result);
  return [state.applyData(node2, result), { type: "text", value: "\n" }];
}
function code$2(state, node2) {
  const value = node2.value ? node2.value + "\n" : "";
  const properties = {};
  const language = node2.lang ? node2.lang.split(/\s+/) : [];
  if (language.length > 0) {
    properties.className = ["language-" + language[0]];
  }
  let result = {
    type: "element",
    tagName: "code",
    properties,
    children: [{ type: "text", value }]
  };
  if (node2.meta) {
    result.data = { meta: node2.meta };
  }
  state.patch(node2, result);
  result = state.applyData(node2, result);
  result = { type: "element", tagName: "pre", properties: {}, children: [result] };
  state.patch(node2, result);
  return result;
}
function strikethrough(state, node2) {
  const result = {
    type: "element",
    tagName: "del",
    properties: {},
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function emphasis$1(state, node2) {
  const result = {
    type: "element",
    tagName: "em",
    properties: {},
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function footnoteReference$1(state, node2) {
  const clobberPrefix = typeof state.options.clobberPrefix === "string" ? state.options.clobberPrefix : "user-content-";
  const id = String(node2.identifier).toUpperCase();
  const safeId = normalizeUri(id.toLowerCase());
  const index2 = state.footnoteOrder.indexOf(id);
  let counter;
  let reuseCounter = state.footnoteCounts.get(id);
  if (reuseCounter === void 0) {
    reuseCounter = 0;
    state.footnoteOrder.push(id);
    counter = state.footnoteOrder.length;
  } else {
    counter = index2 + 1;
  }
  reuseCounter += 1;
  state.footnoteCounts.set(id, reuseCounter);
  const link2 = {
    type: "element",
    tagName: "a",
    properties: {
      href: "#" + clobberPrefix + "fn-" + safeId,
      id: clobberPrefix + "fnref-" + safeId + (reuseCounter > 1 ? "-" + reuseCounter : ""),
      dataFootnoteRef: true,
      ariaDescribedBy: ["footnote-label"]
    },
    children: [{ type: "text", value: String(counter) }]
  };
  state.patch(node2, link2);
  const sup = {
    type: "element",
    tagName: "sup",
    properties: {},
    children: [link2]
  };
  state.patch(node2, sup);
  return state.applyData(node2, sup);
}
function heading$1(state, node2) {
  const result = {
    type: "element",
    tagName: "h" + node2.depth,
    properties: {},
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function html$1(state, node2) {
  if (state.options.allowDangerousHtml) {
    const result = { type: "raw", value: node2.value };
    state.patch(node2, result);
    return state.applyData(node2, result);
  }
  return void 0;
}
function revert(state, node2) {
  const subtype = node2.referenceType;
  let suffix = "]";
  if (subtype === "collapsed") {
    suffix += "[]";
  } else if (subtype === "full") {
    suffix += "[" + (node2.label || node2.identifier) + "]";
  }
  if (node2.type === "imageReference") {
    return [{ type: "text", value: "![" + node2.alt + suffix }];
  }
  const contents = state.all(node2);
  const head = contents[0];
  if (head && head.type === "text") {
    head.value = "[" + head.value;
  } else {
    contents.unshift({ type: "text", value: "[" });
  }
  const tail = contents[contents.length - 1];
  if (tail && tail.type === "text") {
    tail.value += suffix;
  } else {
    contents.push({ type: "text", value: suffix });
  }
  return contents;
}
function imageReference$1(state, node2) {
  const id = String(node2.identifier).toUpperCase();
  const definition2 = state.definitionById.get(id);
  if (!definition2) {
    return revert(state, node2);
  }
  const properties = { src: normalizeUri(definition2.url || ""), alt: node2.alt };
  if (definition2.title !== null && definition2.title !== void 0) {
    properties.title = definition2.title;
  }
  const result = { type: "element", tagName: "img", properties, children: [] };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function image$1(state, node2) {
  const properties = { src: normalizeUri(node2.url) };
  if (node2.alt !== null && node2.alt !== void 0) {
    properties.alt = node2.alt;
  }
  if (node2.title !== null && node2.title !== void 0) {
    properties.title = node2.title;
  }
  const result = { type: "element", tagName: "img", properties, children: [] };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function inlineCode$1(state, node2) {
  const text2 = { type: "text", value: node2.value.replace(/\r?\n|\r/g, " ") };
  state.patch(node2, text2);
  const result = {
    type: "element",
    tagName: "code",
    properties: {},
    children: [text2]
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function linkReference$1(state, node2) {
  const id = String(node2.identifier).toUpperCase();
  const definition2 = state.definitionById.get(id);
  if (!definition2) {
    return revert(state, node2);
  }
  const properties = { href: normalizeUri(definition2.url || "") };
  if (definition2.title !== null && definition2.title !== void 0) {
    properties.title = definition2.title;
  }
  const result = {
    type: "element",
    tagName: "a",
    properties,
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function link$1(state, node2) {
  const properties = { href: normalizeUri(node2.url) };
  if (node2.title !== null && node2.title !== void 0) {
    properties.title = node2.title;
  }
  const result = {
    type: "element",
    tagName: "a",
    properties,
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function listItem$1(state, node2, parent) {
  const results = state.all(node2);
  const loose = parent ? listLoose(parent) : listItemLoose(node2);
  const properties = {};
  const children = [];
  if (typeof node2.checked === "boolean") {
    const head = results[0];
    let paragraph2;
    if (head && head.type === "element" && head.tagName === "p") {
      paragraph2 = head;
    } else {
      paragraph2 = { type: "element", tagName: "p", properties: {}, children: [] };
      results.unshift(paragraph2);
    }
    if (paragraph2.children.length > 0) {
      paragraph2.children.unshift({ type: "text", value: " " });
    }
    paragraph2.children.unshift({
      type: "element",
      tagName: "input",
      properties: { type: "checkbox", checked: node2.checked, disabled: true },
      children: []
    });
    properties.className = ["task-list-item"];
  }
  let index2 = -1;
  while (++index2 < results.length) {
    const child = results[index2];
    if (loose || index2 !== 0 || child.type !== "element" || child.tagName !== "p") {
      children.push({ type: "text", value: "\n" });
    }
    if (child.type === "element" && child.tagName === "p" && !loose) {
      children.push(...child.children);
    } else {
      children.push(child);
    }
  }
  const tail = results[results.length - 1];
  if (tail && (loose || tail.type !== "element" || tail.tagName !== "p")) {
    children.push({ type: "text", value: "\n" });
  }
  const result = { type: "element", tagName: "li", properties, children };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function listLoose(node2) {
  let loose = false;
  if (node2.type === "list") {
    loose = node2.spread || false;
    const children = node2.children;
    let index2 = -1;
    while (!loose && ++index2 < children.length) {
      loose = listItemLoose(children[index2]);
    }
  }
  return loose;
}
function listItemLoose(node2) {
  const spread = node2.spread;
  return spread === null || spread === void 0 ? node2.children.length > 1 : spread;
}
function list$1(state, node2) {
  const properties = {};
  const results = state.all(node2);
  let index2 = -1;
  if (typeof node2.start === "number" && node2.start !== 1) {
    properties.start = node2.start;
  }
  while (++index2 < results.length) {
    const child = results[index2];
    if (child.type === "element" && child.tagName === "li" && child.properties && Array.isArray(child.properties.className) && child.properties.className.includes("task-list-item")) {
      properties.className = ["contains-task-list"];
      break;
    }
  }
  const result = {
    type: "element",
    tagName: node2.ordered ? "ol" : "ul",
    properties,
    children: state.wrap(results, true)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function paragraph$1(state, node2) {
  const result = {
    type: "element",
    tagName: "p",
    properties: {},
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function root$1(state, node2) {
  const result = { type: "root", children: state.wrap(state.all(node2)) };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function strong$1(state, node2) {
  const result = {
    type: "element",
    tagName: "strong",
    properties: {},
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function table(state, node2) {
  const rows = state.all(node2);
  const firstRow = rows.shift();
  const tableContent = [];
  if (firstRow) {
    const head = {
      type: "element",
      tagName: "thead",
      properties: {},
      children: state.wrap([firstRow], true)
    };
    state.patch(node2.children[0], head);
    tableContent.push(head);
  }
  if (rows.length > 0) {
    const body = {
      type: "element",
      tagName: "tbody",
      properties: {},
      children: state.wrap(rows, true)
    };
    const start = pointStart(node2.children[1]);
    const end = pointEnd(node2.children[node2.children.length - 1]);
    if (start && end) body.position = { start, end };
    tableContent.push(body);
  }
  const result = {
    type: "element",
    tagName: "table",
    properties: {},
    children: state.wrap(tableContent, true)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function tableRow(state, node2, parent) {
  const siblings = parent ? parent.children : void 0;
  const rowIndex = siblings ? siblings.indexOf(node2) : 1;
  const tagName = rowIndex === 0 ? "th" : "td";
  const align = parent && parent.type === "table" ? parent.align : void 0;
  const length = align ? align.length : node2.children.length;
  let cellIndex = -1;
  const cells = [];
  while (++cellIndex < length) {
    const cell = node2.children[cellIndex];
    const properties = {};
    const alignValue = align ? align[cellIndex] : void 0;
    if (alignValue) {
      properties.align = alignValue;
    }
    let result2 = { type: "element", tagName, properties, children: [] };
    if (cell) {
      result2.children = state.all(cell);
      state.patch(cell, result2);
      result2 = state.applyData(cell, result2);
    }
    cells.push(result2);
  }
  const result = {
    type: "element",
    tagName: "tr",
    properties: {},
    children: state.wrap(cells, true)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function tableCell(state, node2) {
  const result = {
    type: "element",
    tagName: "td",
    // Assume body cell.
    properties: {},
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
const tab = 9;
const space = 32;
function trimLines(value) {
  const source = String(value);
  const search2 = /\r?\n|\r/g;
  let match = search2.exec(source);
  let last = 0;
  const lines = [];
  while (match) {
    lines.push(
      trimLine(source.slice(last, match.index), last > 0, true),
      match[0]
    );
    last = match.index + match[0].length;
    match = search2.exec(source);
  }
  lines.push(trimLine(source.slice(last), last > 0, false));
  return lines.join("");
}
function trimLine(value, start, end) {
  let startIndex = 0;
  let endIndex = value.length;
  if (start) {
    let code2 = value.codePointAt(startIndex);
    while (code2 === tab || code2 === space) {
      startIndex++;
      code2 = value.codePointAt(startIndex);
    }
  }
  if (end) {
    let code2 = value.codePointAt(endIndex - 1);
    while (code2 === tab || code2 === space) {
      endIndex--;
      code2 = value.codePointAt(endIndex - 1);
    }
  }
  return endIndex > startIndex ? value.slice(startIndex, endIndex) : "";
}
function text$2(state, node2) {
  const result = { type: "text", value: trimLines(String(node2.value)) };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function thematicBreak$1(state, node2) {
  const result = {
    type: "element",
    tagName: "hr",
    properties: {},
    children: []
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
const handlers = {
  blockquote: blockquote$1,
  break: hardBreak$1,
  code: code$2,
  delete: strikethrough,
  emphasis: emphasis$1,
  footnoteReference: footnoteReference$1,
  heading: heading$1,
  html: html$1,
  imageReference: imageReference$1,
  image: image$1,
  inlineCode: inlineCode$1,
  linkReference: linkReference$1,
  link: link$1,
  listItem: listItem$1,
  list: list$1,
  paragraph: paragraph$1,
  // @ts-expect-error: root is different, but hard to type.
  root: root$1,
  strong: strong$1,
  table,
  tableCell,
  tableRow,
  text: text$2,
  thematicBreak: thematicBreak$1,
  toml: ignore,
  yaml: ignore,
  definition: ignore,
  footnoteDefinition: ignore
};
function ignore() {
  return void 0;
}
const VOID = -1;
const PRIMITIVE = 0;
const ARRAY = 1;
const OBJECT = 2;
const DATE = 3;
const REGEXP = 4;
const MAP = 5;
const SET = 6;
const ERROR = 7;
const BIGINT = 8;
const env = typeof self === "object" ? self : globalThis;
const guard = (name2, init) => {
  switch (name2) {
    case "Function":
    case "SharedWorker":
    case "Worker":
    case "eval":
    case "setInterval":
    case "setTimeout":
      throw new TypeError("unable to deserialize " + name2);
  }
  return new env[name2](init);
};
const deserializer = ($, _) => {
  const as = (out, index2) => {
    $.set(index2, out);
    return out;
  };
  const unpair = (index2) => {
    if ($.has(index2))
      return $.get(index2);
    const [type, value] = _[index2];
    switch (type) {
      case PRIMITIVE:
      case VOID:
        return as(value, index2);
      case ARRAY: {
        const arr = as([], index2);
        for (const index3 of value)
          arr.push(unpair(index3));
        return arr;
      }
      case OBJECT: {
        const object = as({}, index2);
        for (const [key, index3] of value)
          object[unpair(key)] = unpair(index3);
        return object;
      }
      case DATE:
        return as(new Date(value), index2);
      case REGEXP: {
        const { source, flags } = value;
        return as(new RegExp(source, flags), index2);
      }
      case MAP: {
        const map2 = as(/* @__PURE__ */ new Map(), index2);
        for (const [key, index3] of value)
          map2.set(unpair(key), unpair(index3));
        return map2;
      }
      case SET: {
        const set = as(/* @__PURE__ */ new Set(), index2);
        for (const index3 of value)
          set.add(unpair(index3));
        return set;
      }
      case ERROR: {
        const { name: name2, message } = value;
        return as(guard(name2, message), index2);
      }
      case BIGINT:
        return as(BigInt(value), index2);
      case "BigInt":
        return as(Object(BigInt(value)), index2);
      case "ArrayBuffer":
        return as(new Uint8Array(value).buffer, value);
      case "DataView": {
        const { buffer } = new Uint8Array(value);
        return as(new DataView(buffer), value);
      }
    }
    return as(guard(type, value), index2);
  };
  return unpair;
};
const deserialize = (serialized) => deserializer(/* @__PURE__ */ new Map(), serialized)(0);
const EMPTY = "";
const { toString } = {};
const { keys } = Object;
const typeOf = (value) => {
  const type = typeof value;
  if (type !== "object" || !value)
    return [PRIMITIVE, type];
  const asString = toString.call(value).slice(8, -1);
  switch (asString) {
    case "Array":
      return [ARRAY, EMPTY];
    case "Object":
      return [OBJECT, EMPTY];
    case "Date":
      return [DATE, EMPTY];
    case "RegExp":
      return [REGEXP, EMPTY];
    case "Map":
      return [MAP, EMPTY];
    case "Set":
      return [SET, EMPTY];
    case "DataView":
      return [ARRAY, asString];
  }
  if (asString.includes("Array"))
    return [ARRAY, asString];
  if (asString.includes("Error"))
    return [ERROR, asString];
  return [OBJECT, asString];
};
const shouldSkip = ([TYPE, type]) => TYPE === PRIMITIVE && (type === "function" || type === "symbol");
const serializer = (strict, json, $, _) => {
  const as = (out, value) => {
    const index2 = _.push(out) - 1;
    $.set(value, index2);
    return index2;
  };
  const pair = (value) => {
    if ($.has(value))
      return $.get(value);
    let [TYPE, type] = typeOf(value);
    switch (TYPE) {
      case PRIMITIVE: {
        let entry = value;
        switch (type) {
          case "bigint":
            TYPE = BIGINT;
            entry = value.toString();
            break;
          case "function":
          case "symbol":
            if (strict)
              throw new TypeError("unable to serialize " + type);
            entry = null;
            break;
          case "undefined":
            return as([VOID], value);
        }
        return as([TYPE, entry], value);
      }
      case ARRAY: {
        if (type) {
          let spread = value;
          if (type === "DataView") {
            spread = new Uint8Array(value.buffer);
          } else if (type === "ArrayBuffer") {
            spread = new Uint8Array(value);
          }
          return as([type, [...spread]], value);
        }
        const arr = [];
        const index2 = as([TYPE, arr], value);
        for (const entry of value)
          arr.push(pair(entry));
        return index2;
      }
      case OBJECT: {
        if (type) {
          switch (type) {
            case "BigInt":
              return as([type, value.toString()], value);
            case "Boolean":
            case "Number":
            case "String":
              return as([type, value.valueOf()], value);
          }
        }
        if (json && "toJSON" in value)
          return pair(value.toJSON());
        const entries = [];
        const index2 = as([TYPE, entries], value);
        for (const key of keys(value)) {
          if (strict || !shouldSkip(typeOf(value[key])))
            entries.push([pair(key), pair(value[key])]);
        }
        return index2;
      }
      case DATE:
        return as([TYPE, value.toISOString()], value);
      case REGEXP: {
        const { source, flags } = value;
        return as([TYPE, { source, flags }], value);
      }
      case MAP: {
        const entries = [];
        const index2 = as([TYPE, entries], value);
        for (const [key, entry] of value) {
          if (strict || !(shouldSkip(typeOf(key)) || shouldSkip(typeOf(entry))))
            entries.push([pair(key), pair(entry)]);
        }
        return index2;
      }
      case SET: {
        const entries = [];
        const index2 = as([TYPE, entries], value);
        for (const entry of value) {
          if (strict || !shouldSkip(typeOf(entry)))
            entries.push(pair(entry));
        }
        return index2;
      }
    }
    const { message } = value;
    return as([TYPE, { name: type, message }], value);
  };
  return pair;
};
const serialize$1 = (value, { json, lossy } = {}) => {
  const _ = [];
  return serializer(!(json || lossy), !!json, /* @__PURE__ */ new Map(), _)(value), _;
};
const structuredClone$1 = typeof structuredClone === "function" ? (
  /* c8 ignore start */
  (any, options) => options && ("json" in options || "lossy" in options) ? deserialize(serialize$1(any, options)) : structuredClone(any)
) : (any, options) => deserialize(serialize$1(any, options));
function defaultFootnoteBackContent(_, rereferenceIndex) {
  const result = [{ type: "text", value: "↩" }];
  if (rereferenceIndex > 1) {
    result.push({
      type: "element",
      tagName: "sup",
      properties: {},
      children: [{ type: "text", value: String(rereferenceIndex) }]
    });
  }
  return result;
}
function defaultFootnoteBackLabel(referenceIndex, rereferenceIndex) {
  return "Back to reference " + (referenceIndex + 1) + (rereferenceIndex > 1 ? "-" + rereferenceIndex : "");
}
function footer(state) {
  const clobberPrefix = typeof state.options.clobberPrefix === "string" ? state.options.clobberPrefix : "user-content-";
  const footnoteBackContent = state.options.footnoteBackContent || defaultFootnoteBackContent;
  const footnoteBackLabel = state.options.footnoteBackLabel || defaultFootnoteBackLabel;
  const footnoteLabel = state.options.footnoteLabel || "Footnotes";
  const footnoteLabelTagName = state.options.footnoteLabelTagName || "h2";
  const footnoteLabelProperties = state.options.footnoteLabelProperties || {
    className: ["sr-only"]
  };
  const listItems = [];
  let referenceIndex = -1;
  while (++referenceIndex < state.footnoteOrder.length) {
    const definition2 = state.footnoteById.get(
      state.footnoteOrder[referenceIndex]
    );
    if (!definition2) {
      continue;
    }
    const content2 = state.all(definition2);
    const id = String(definition2.identifier).toUpperCase();
    const safeId = normalizeUri(id.toLowerCase());
    let rereferenceIndex = 0;
    const backReferences = [];
    const counts = state.footnoteCounts.get(id);
    while (counts !== void 0 && ++rereferenceIndex <= counts) {
      if (backReferences.length > 0) {
        backReferences.push({ type: "text", value: " " });
      }
      let children = typeof footnoteBackContent === "string" ? footnoteBackContent : footnoteBackContent(referenceIndex, rereferenceIndex);
      if (typeof children === "string") {
        children = { type: "text", value: children };
      }
      backReferences.push({
        type: "element",
        tagName: "a",
        properties: {
          href: "#" + clobberPrefix + "fnref-" + safeId + (rereferenceIndex > 1 ? "-" + rereferenceIndex : ""),
          dataFootnoteBackref: "",
          ariaLabel: typeof footnoteBackLabel === "string" ? footnoteBackLabel : footnoteBackLabel(referenceIndex, rereferenceIndex),
          className: ["data-footnote-backref"]
        },
        children: Array.isArray(children) ? children : [children]
      });
    }
    const tail = content2[content2.length - 1];
    if (tail && tail.type === "element" && tail.tagName === "p") {
      const tailTail = tail.children[tail.children.length - 1];
      if (tailTail && tailTail.type === "text") {
        tailTail.value += " ";
      } else {
        tail.children.push({ type: "text", value: " " });
      }
      tail.children.push(...backReferences);
    } else {
      content2.push(...backReferences);
    }
    const listItem2 = {
      type: "element",
      tagName: "li",
      properties: { id: clobberPrefix + "fn-" + safeId },
      children: state.wrap(content2, true)
    };
    state.patch(definition2, listItem2);
    listItems.push(listItem2);
  }
  if (listItems.length === 0) {
    return;
  }
  return {
    type: "element",
    tagName: "section",
    properties: { dataFootnotes: true, className: ["footnotes"] },
    children: [
      {
        type: "element",
        tagName: footnoteLabelTagName,
        properties: {
          ...structuredClone$1(footnoteLabelProperties),
          id: "footnote-label"
        },
        children: [{ type: "text", value: footnoteLabel }]
      },
      { type: "text", value: "\n" },
      {
        type: "element",
        tagName: "ol",
        properties: {},
        children: state.wrap(listItems, true)
      },
      { type: "text", value: "\n" }
    ]
  };
}
function visit(tree, testOrVisitor, visitorOrReverse, maybeReverse) {
  let reverse;
  let test;
  let visitor;
  if (typeof testOrVisitor === "function" && typeof visitorOrReverse !== "function") {
    test = void 0;
    visitor = testOrVisitor;
    reverse = visitorOrReverse;
  } else {
    test = testOrVisitor;
    visitor = visitorOrReverse;
    reverse = maybeReverse;
  }
  visitParents(tree, test, overload, reverse);
  function overload(node2, parents) {
    const parent = parents[parents.length - 1];
    const index2 = parent ? parent.children.indexOf(node2) : void 0;
    return visitor(node2, index2, parent);
  }
}
const own$1 = {}.hasOwnProperty;
const emptyOptions$1 = {};
function createState(tree, options) {
  const settings = options || emptyOptions$1;
  const definitionById = /* @__PURE__ */ new Map();
  const footnoteById = /* @__PURE__ */ new Map();
  const footnoteCounts = /* @__PURE__ */ new Map();
  const handlers$1 = { ...handlers, ...settings.handlers };
  const state = {
    all: all2,
    applyData,
    definitionById,
    footnoteById,
    footnoteCounts,
    footnoteOrder: [],
    handlers: handlers$1,
    one: one2,
    options: settings,
    patch,
    wrap: wrap$1
  };
  visit(tree, function(node2) {
    if (node2.type === "definition" || node2.type === "footnoteDefinition") {
      const map2 = node2.type === "definition" ? definitionById : footnoteById;
      const id = String(node2.identifier).toUpperCase();
      if (!map2.has(id)) {
        map2.set(id, node2);
      }
    }
  });
  return state;
  function one2(node2, parent) {
    const type = node2.type;
    const handle2 = state.handlers[type];
    if (own$1.call(state.handlers, type) && handle2) {
      return handle2(state, node2, parent);
    }
    if (state.options.passThrough && state.options.passThrough.includes(type)) {
      if ("children" in node2) {
        const { children, ...shallow } = node2;
        const result = structuredClone$1(shallow);
        result.children = state.all(node2);
        return result;
      }
      return structuredClone$1(node2);
    }
    const unknown = state.options.unknownHandler || defaultUnknownHandler;
    return unknown(state, node2, parent);
  }
  function all2(parent) {
    const values = [];
    if ("children" in parent) {
      const nodes = parent.children;
      let index2 = -1;
      while (++index2 < nodes.length) {
        const result = state.one(nodes[index2], parent);
        if (result) {
          if (index2 && nodes[index2 - 1].type === "break") {
            if (!Array.isArray(result) && result.type === "text") {
              result.value = trimMarkdownSpaceStart(result.value);
            }
            if (!Array.isArray(result) && result.type === "element") {
              const head = result.children[0];
              if (head && head.type === "text") {
                head.value = trimMarkdownSpaceStart(head.value);
              }
            }
          }
          if (Array.isArray(result)) {
            values.push(...result);
          } else {
            values.push(result);
          }
        }
      }
    }
    return values;
  }
}
function patch(from, to) {
  if (from.position) to.position = position$1(from);
}
function applyData(from, to) {
  let result = to;
  if (from && from.data) {
    const hName = from.data.hName;
    const hChildren = from.data.hChildren;
    const hProperties = from.data.hProperties;
    if (typeof hName === "string") {
      if (result.type === "element") {
        result.tagName = hName;
      } else {
        const children = "children" in result ? result.children : [result];
        result = { type: "element", tagName: hName, properties: {}, children };
      }
    }
    if (result.type === "element" && hProperties) {
      Object.assign(result.properties, structuredClone$1(hProperties));
    }
    if ("children" in result && result.children && hChildren !== null && hChildren !== void 0) {
      result.children = hChildren;
    }
  }
  return result;
}
function defaultUnknownHandler(state, node2) {
  const data = node2.data || {};
  const result = "value" in node2 && !(own$1.call(data, "hProperties") || own$1.call(data, "hChildren")) ? { type: "text", value: node2.value } : {
    type: "element",
    tagName: "div",
    properties: {},
    children: state.all(node2)
  };
  state.patch(node2, result);
  return state.applyData(node2, result);
}
function wrap$1(nodes, loose) {
  const result = [];
  let index2 = -1;
  if (loose) {
    result.push({ type: "text", value: "\n" });
  }
  while (++index2 < nodes.length) {
    if (index2) result.push({ type: "text", value: "\n" });
    result.push(nodes[index2]);
  }
  if (loose && nodes.length > 0) {
    result.push({ type: "text", value: "\n" });
  }
  return result;
}
function trimMarkdownSpaceStart(value) {
  let index2 = 0;
  let code2 = value.charCodeAt(index2);
  while (code2 === 9 || code2 === 32) {
    index2++;
    code2 = value.charCodeAt(index2);
  }
  return value.slice(index2);
}
function toHast(tree, options) {
  const state = createState(tree, options);
  const node2 = state.one(tree, void 0);
  const foot = footer(state);
  const result = Array.isArray(node2) ? { type: "root", children: node2 } : node2 || { type: "root", children: [] };
  if (foot) {
    result.children.push({ type: "text", value: "\n" }, foot);
  }
  return result;
}
function remarkRehype(destination, options) {
  if (destination && "run" in destination) {
    return async function(tree, file) {
      const hastTree = (
        /** @type {HastRoot} */
        toHast(tree, { file, ...options })
      );
      await destination.run(hastTree, file);
    };
  }
  return function(tree, file) {
    return (
      /** @type {HastRoot} */
      toHast(tree, { file, ...destination || options })
    );
  };
}
function bail(error) {
  if (error) {
    throw error;
  }
}
var extend$1;
var hasRequiredExtend;
function requireExtend() {
  if (hasRequiredExtend) return extend$1;
  hasRequiredExtend = 1;
  var hasOwn = Object.prototype.hasOwnProperty;
  var toStr = Object.prototype.toString;
  var defineProperty = Object.defineProperty;
  var gOPD = Object.getOwnPropertyDescriptor;
  var isArray = function isArray2(arr) {
    if (typeof Array.isArray === "function") {
      return Array.isArray(arr);
    }
    return toStr.call(arr) === "[object Array]";
  };
  var isPlainObject2 = function isPlainObject3(obj) {
    if (!obj || toStr.call(obj) !== "[object Object]") {
      return false;
    }
    var hasOwnConstructor = hasOwn.call(obj, "constructor");
    var hasIsPrototypeOf = obj.constructor && obj.constructor.prototype && hasOwn.call(obj.constructor.prototype, "isPrototypeOf");
    if (obj.constructor && !hasOwnConstructor && !hasIsPrototypeOf) {
      return false;
    }
    var key;
    for (key in obj) {
    }
    return typeof key === "undefined" || hasOwn.call(obj, key);
  };
  var setProperty = function setProperty2(target, options) {
    if (defineProperty && options.name === "__proto__") {
      defineProperty(target, options.name, {
        enumerable: true,
        configurable: true,
        value: options.newValue,
        writable: true
      });
    } else {
      target[options.name] = options.newValue;
    }
  };
  var getProperty = function getProperty2(obj, name2) {
    if (name2 === "__proto__") {
      if (!hasOwn.call(obj, name2)) {
        return void 0;
      } else if (gOPD) {
        return gOPD(obj, name2).value;
      }
    }
    return obj[name2];
  };
  extend$1 = function extend2() {
    var options, name2, src, copy, copyIsArray, clone;
    var target = arguments[0];
    var i = 1;
    var length = arguments.length;
    var deep = false;
    if (typeof target === "boolean") {
      deep = target;
      target = arguments[1] || {};
      i = 2;
    }
    if (target == null || typeof target !== "object" && typeof target !== "function") {
      target = {};
    }
    for (; i < length; ++i) {
      options = arguments[i];
      if (options != null) {
        for (name2 in options) {
          src = getProperty(target, name2);
          copy = getProperty(options, name2);
          if (target !== copy) {
            if (deep && copy && (isPlainObject2(copy) || (copyIsArray = isArray(copy)))) {
              if (copyIsArray) {
                copyIsArray = false;
                clone = src && isArray(src) ? src : [];
              } else {
                clone = src && isPlainObject2(src) ? src : {};
              }
              setProperty(target, { name: name2, newValue: extend2(deep, clone, copy) });
            } else if (typeof copy !== "undefined") {
              setProperty(target, { name: name2, newValue: copy });
            }
          }
        }
      }
    }
    return target;
  };
  return extend$1;
}
var extendExports = requireExtend();
const extend = /* @__PURE__ */ getDefaultExportFromCjs(extendExports);
function isPlainObject(value) {
  if (typeof value !== "object" || value === null) {
    return false;
  }
  const prototype = Object.getPrototypeOf(value);
  return (prototype === null || prototype === Object.prototype || Object.getPrototypeOf(prototype) === null) && !(Symbol.toStringTag in value) && !(Symbol.iterator in value);
}
function trough() {
  const fns = [];
  const pipeline = { run, use };
  return pipeline;
  function run(...values) {
    let middlewareIndex = -1;
    const callback = values.pop();
    if (typeof callback !== "function") {
      throw new TypeError("Expected function as last argument, not " + callback);
    }
    next(null, ...values);
    function next(error, ...output) {
      const fn = fns[++middlewareIndex];
      let index2 = -1;
      if (error) {
        callback(error);
        return;
      }
      while (++index2 < values.length) {
        if (output[index2] === null || output[index2] === void 0) {
          output[index2] = values[index2];
        }
      }
      values = output;
      if (fn) {
        wrap(fn, next)(...output);
      } else {
        callback(null, ...output);
      }
    }
  }
  function use(middelware) {
    if (typeof middelware !== "function") {
      throw new TypeError(
        "Expected `middelware` to be a function, not " + middelware
      );
    }
    fns.push(middelware);
    return pipeline;
  }
}
function wrap(middleware, callback) {
  let called;
  return wrapped;
  function wrapped(...parameters) {
    const fnExpectsCallback = middleware.length > parameters.length;
    let result;
    if (fnExpectsCallback) {
      parameters.push(done);
    }
    try {
      result = middleware.apply(this, parameters);
    } catch (error) {
      const exception = (
        /** @type {Error} */
        error
      );
      if (fnExpectsCallback && called) {
        throw exception;
      }
      return done(exception);
    }
    if (!fnExpectsCallback) {
      if (result && result.then && typeof result.then === "function") {
        result.then(then, done);
      } else if (result instanceof Error) {
        done(result);
      } else {
        then(result);
      }
    }
  }
  function done(error, ...output) {
    if (!called) {
      called = true;
      callback(error, ...output);
    }
  }
  function then(value) {
    done(null, value);
  }
}
const minpath = { basename, dirname, extname, join, sep: "/" };
function basename(path2, extname2) {
  if (extname2 !== void 0 && typeof extname2 !== "string") {
    throw new TypeError('"ext" argument must be a string');
  }
  assertPath$1(path2);
  let start = 0;
  let end = -1;
  let index2 = path2.length;
  let seenNonSlash;
  if (extname2 === void 0 || extname2.length === 0 || extname2.length > path2.length) {
    while (index2--) {
      if (path2.codePointAt(index2) === 47) {
        if (seenNonSlash) {
          start = index2 + 1;
          break;
        }
      } else if (end < 0) {
        seenNonSlash = true;
        end = index2 + 1;
      }
    }
    return end < 0 ? "" : path2.slice(start, end);
  }
  if (extname2 === path2) {
    return "";
  }
  let firstNonSlashEnd = -1;
  let extnameIndex = extname2.length - 1;
  while (index2--) {
    if (path2.codePointAt(index2) === 47) {
      if (seenNonSlash) {
        start = index2 + 1;
        break;
      }
    } else {
      if (firstNonSlashEnd < 0) {
        seenNonSlash = true;
        firstNonSlashEnd = index2 + 1;
      }
      if (extnameIndex > -1) {
        if (path2.codePointAt(index2) === extname2.codePointAt(extnameIndex--)) {
          if (extnameIndex < 0) {
            end = index2;
          }
        } else {
          extnameIndex = -1;
          end = firstNonSlashEnd;
        }
      }
    }
  }
  if (start === end) {
    end = firstNonSlashEnd;
  } else if (end < 0) {
    end = path2.length;
  }
  return path2.slice(start, end);
}
function dirname(path2) {
  assertPath$1(path2);
  if (path2.length === 0) {
    return ".";
  }
  let end = -1;
  let index2 = path2.length;
  let unmatchedSlash;
  while (--index2) {
    if (path2.codePointAt(index2) === 47) {
      if (unmatchedSlash) {
        end = index2;
        break;
      }
    } else if (!unmatchedSlash) {
      unmatchedSlash = true;
    }
  }
  return end < 0 ? path2.codePointAt(0) === 47 ? "/" : "." : end === 1 && path2.codePointAt(0) === 47 ? "//" : path2.slice(0, end);
}
function extname(path2) {
  assertPath$1(path2);
  let index2 = path2.length;
  let end = -1;
  let startPart = 0;
  let startDot = -1;
  let preDotState = 0;
  let unmatchedSlash;
  while (index2--) {
    const code2 = path2.codePointAt(index2);
    if (code2 === 47) {
      if (unmatchedSlash) {
        startPart = index2 + 1;
        break;
      }
      continue;
    }
    if (end < 0) {
      unmatchedSlash = true;
      end = index2 + 1;
    }
    if (code2 === 46) {
      if (startDot < 0) {
        startDot = index2;
      } else if (preDotState !== 1) {
        preDotState = 1;
      }
    } else if (startDot > -1) {
      preDotState = -1;
    }
  }
  if (startDot < 0 || end < 0 || // We saw a non-dot character immediately before the dot.
  preDotState === 0 || // The (right-most) trimmed path component is exactly `..`.
  preDotState === 1 && startDot === end - 1 && startDot === startPart + 1) {
    return "";
  }
  return path2.slice(startDot, end);
}
function join(...segments) {
  let index2 = -1;
  let joined;
  while (++index2 < segments.length) {
    assertPath$1(segments[index2]);
    if (segments[index2]) {
      joined = joined === void 0 ? segments[index2] : joined + "/" + segments[index2];
    }
  }
  return joined === void 0 ? "." : normalize(joined);
}
function normalize(path2) {
  assertPath$1(path2);
  const absolute = path2.codePointAt(0) === 47;
  let value = normalizeString(path2, !absolute);
  if (value.length === 0 && !absolute) {
    value = ".";
  }
  if (value.length > 0 && path2.codePointAt(path2.length - 1) === 47) {
    value += "/";
  }
  return absolute ? "/" + value : value;
}
function normalizeString(path2, allowAboveRoot) {
  let result = "";
  let lastSegmentLength = 0;
  let lastSlash = -1;
  let dots = 0;
  let index2 = -1;
  let code2;
  let lastSlashIndex;
  while (++index2 <= path2.length) {
    if (index2 < path2.length) {
      code2 = path2.codePointAt(index2);
    } else if (code2 === 47) {
      break;
    } else {
      code2 = 47;
    }
    if (code2 === 47) {
      if (lastSlash === index2 - 1 || dots === 1) ;
      else if (lastSlash !== index2 - 1 && dots === 2) {
        if (result.length < 2 || lastSegmentLength !== 2 || result.codePointAt(result.length - 1) !== 46 || result.codePointAt(result.length - 2) !== 46) {
          if (result.length > 2) {
            lastSlashIndex = result.lastIndexOf("/");
            if (lastSlashIndex !== result.length - 1) {
              if (lastSlashIndex < 0) {
                result = "";
                lastSegmentLength = 0;
              } else {
                result = result.slice(0, lastSlashIndex);
                lastSegmentLength = result.length - 1 - result.lastIndexOf("/");
              }
              lastSlash = index2;
              dots = 0;
              continue;
            }
          } else if (result.length > 0) {
            result = "";
            lastSegmentLength = 0;
            lastSlash = index2;
            dots = 0;
            continue;
          }
        }
        if (allowAboveRoot) {
          result = result.length > 0 ? result + "/.." : "..";
          lastSegmentLength = 2;
        }
      } else {
        if (result.length > 0) {
          result += "/" + path2.slice(lastSlash + 1, index2);
        } else {
          result = path2.slice(lastSlash + 1, index2);
        }
        lastSegmentLength = index2 - lastSlash - 1;
      }
      lastSlash = index2;
      dots = 0;
    } else if (code2 === 46 && dots > -1) {
      dots++;
    } else {
      dots = -1;
    }
  }
  return result;
}
function assertPath$1(path2) {
  if (typeof path2 !== "string") {
    throw new TypeError(
      "Path must be a string. Received " + JSON.stringify(path2)
    );
  }
}
const minproc = { cwd };
function cwd() {
  return "/";
}
function isUrl(fileUrlOrPath) {
  return Boolean(
    fileUrlOrPath !== null && typeof fileUrlOrPath === "object" && "href" in fileUrlOrPath && fileUrlOrPath.href && "protocol" in fileUrlOrPath && fileUrlOrPath.protocol && // @ts-expect-error: indexing is fine.
    fileUrlOrPath.auth === void 0
  );
}
function urlToPath(path2) {
  if (typeof path2 === "string") {
    path2 = new URL(path2);
  } else if (!isUrl(path2)) {
    const error = new TypeError(
      'The "path" argument must be of type string or an instance of URL. Received `' + path2 + "`"
    );
    error.code = "ERR_INVALID_ARG_TYPE";
    throw error;
  }
  if (path2.protocol !== "file:") {
    const error = new TypeError("The URL must be of scheme file");
    error.code = "ERR_INVALID_URL_SCHEME";
    throw error;
  }
  return getPathFromURLPosix(path2);
}
function getPathFromURLPosix(url) {
  if (url.hostname !== "") {
    const error = new TypeError(
      'File URL host must be "localhost" or empty on darwin'
    );
    error.code = "ERR_INVALID_FILE_URL_HOST";
    throw error;
  }
  const pathname = url.pathname;
  let index2 = -1;
  while (++index2 < pathname.length) {
    if (pathname.codePointAt(index2) === 37 && pathname.codePointAt(index2 + 1) === 50) {
      const third = pathname.codePointAt(index2 + 2);
      if (third === 70 || third === 102) {
        const error = new TypeError(
          "File URL path must not include encoded / characters"
        );
        error.code = "ERR_INVALID_FILE_URL_PATH";
        throw error;
      }
    }
  }
  return decodeURIComponent(pathname);
}
const order = (
  /** @type {const} */
  [
    "history",
    "path",
    "basename",
    "stem",
    "extname",
    "dirname"
  ]
);
class VFile {
  /**
   * Create a new virtual file.
   *
   * `options` is treated as:
   *
   * *   `string` or `Uint8Array` — `{value: options}`
   * *   `URL` — `{path: options}`
   * *   `VFile` — shallow copies its data over to the new file
   * *   `object` — all fields are shallow copied over to the new file
   *
   * Path related fields are set in the following order (least specific to
   * most specific): `history`, `path`, `basename`, `stem`, `extname`,
   * `dirname`.
   *
   * You cannot set `dirname` or `extname` without setting either `history`,
   * `path`, `basename`, or `stem` too.
   *
   * @param {Compatible | null | undefined} [value]
   *   File value.
   * @returns
   *   New instance.
   */
  constructor(value) {
    let options;
    if (!value) {
      options = {};
    } else if (isUrl(value)) {
      options = { path: value };
    } else if (typeof value === "string" || isUint8Array$1(value)) {
      options = { value };
    } else {
      options = value;
    }
    this.cwd = "cwd" in options ? "" : minproc.cwd();
    this.data = {};
    this.history = [];
    this.messages = [];
    this.value;
    this.map;
    this.result;
    this.stored;
    let index2 = -1;
    while (++index2 < order.length) {
      const field2 = order[index2];
      if (field2 in options && options[field2] !== void 0 && options[field2] !== null) {
        this[field2] = field2 === "history" ? [...options[field2]] : options[field2];
      }
    }
    let field;
    for (field in options) {
      if (!order.includes(field)) {
        this[field] = options[field];
      }
    }
  }
  /**
   * Get the basename (including extname) (example: `'index.min.js'`).
   *
   * @returns {string | undefined}
   *   Basename.
   */
  get basename() {
    return typeof this.path === "string" ? minpath.basename(this.path) : void 0;
  }
  /**
   * Set basename (including extname) (`'index.min.js'`).
   *
   * Cannot contain path separators (`'/'` on unix, macOS, and browsers, `'\'`
   * on windows).
   * Cannot be nullified (use `file.path = file.dirname` instead).
   *
   * @param {string} basename
   *   Basename.
   * @returns {undefined}
   *   Nothing.
   */
  set basename(basename2) {
    assertNonEmpty(basename2, "basename");
    assertPart(basename2, "basename");
    this.path = minpath.join(this.dirname || "", basename2);
  }
  /**
   * Get the parent path (example: `'~'`).
   *
   * @returns {string | undefined}
   *   Dirname.
   */
  get dirname() {
    return typeof this.path === "string" ? minpath.dirname(this.path) : void 0;
  }
  /**
   * Set the parent path (example: `'~'`).
   *
   * Cannot be set if there’s no `path` yet.
   *
   * @param {string | undefined} dirname
   *   Dirname.
   * @returns {undefined}
   *   Nothing.
   */
  set dirname(dirname2) {
    assertPath(this.basename, "dirname");
    this.path = minpath.join(dirname2 || "", this.basename);
  }
  /**
   * Get the extname (including dot) (example: `'.js'`).
   *
   * @returns {string | undefined}
   *   Extname.
   */
  get extname() {
    return typeof this.path === "string" ? minpath.extname(this.path) : void 0;
  }
  /**
   * Set the extname (including dot) (example: `'.js'`).
   *
   * Cannot contain path separators (`'/'` on unix, macOS, and browsers, `'\'`
   * on windows).
   * Cannot be set if there’s no `path` yet.
   *
   * @param {string | undefined} extname
   *   Extname.
   * @returns {undefined}
   *   Nothing.
   */
  set extname(extname2) {
    assertPart(extname2, "extname");
    assertPath(this.dirname, "extname");
    if (extname2) {
      if (extname2.codePointAt(0) !== 46) {
        throw new Error("`extname` must start with `.`");
      }
      if (extname2.includes(".", 1)) {
        throw new Error("`extname` cannot contain multiple dots");
      }
    }
    this.path = minpath.join(this.dirname, this.stem + (extname2 || ""));
  }
  /**
   * Get the full path (example: `'~/index.min.js'`).
   *
   * @returns {string}
   *   Path.
   */
  get path() {
    return this.history[this.history.length - 1];
  }
  /**
   * Set the full path (example: `'~/index.min.js'`).
   *
   * Cannot be nullified.
   * You can set a file URL (a `URL` object with a `file:` protocol) which will
   * be turned into a path with `url.fileURLToPath`.
   *
   * @param {URL | string} path
   *   Path.
   * @returns {undefined}
   *   Nothing.
   */
  set path(path2) {
    if (isUrl(path2)) {
      path2 = urlToPath(path2);
    }
    assertNonEmpty(path2, "path");
    if (this.path !== path2) {
      this.history.push(path2);
    }
  }
  /**
   * Get the stem (basename w/o extname) (example: `'index.min'`).
   *
   * @returns {string | undefined}
   *   Stem.
   */
  get stem() {
    return typeof this.path === "string" ? minpath.basename(this.path, this.extname) : void 0;
  }
  /**
   * Set the stem (basename w/o extname) (example: `'index.min'`).
   *
   * Cannot contain path separators (`'/'` on unix, macOS, and browsers, `'\'`
   * on windows).
   * Cannot be nullified (use `file.path = file.dirname` instead).
   *
   * @param {string} stem
   *   Stem.
   * @returns {undefined}
   *   Nothing.
   */
  set stem(stem) {
    assertNonEmpty(stem, "stem");
    assertPart(stem, "stem");
    this.path = minpath.join(this.dirname || "", stem + (this.extname || ""));
  }
  // Normal prototypal methods.
  /**
   * Create a fatal message for `reason` associated with the file.
   *
   * The `fatal` field of the message is set to `true` (error; file not usable)
   * and the `file` field is set to the current file path.
   * The message is added to the `messages` field on `file`.
   *
   * > 🪦 **Note**: also has obsolete signatures.
   *
   * @overload
   * @param {string} reason
   * @param {MessageOptions | null | undefined} [options]
   * @returns {never}
   *
   * @overload
   * @param {string} reason
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns {never}
   *
   * @overload
   * @param {string} reason
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns {never}
   *
   * @overload
   * @param {string} reason
   * @param {string | null | undefined} [origin]
   * @returns {never}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns {never}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns {never}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {string | null | undefined} [origin]
   * @returns {never}
   *
   * @param {Error | VFileMessage | string} causeOrReason
   *   Reason for message, should use markdown.
   * @param {Node | NodeLike | MessageOptions | Point | Position | string | null | undefined} [optionsOrParentOrPlace]
   *   Configuration (optional).
   * @param {string | null | undefined} [origin]
   *   Place in code where the message originates (example:
   *   `'my-package:my-rule'` or `'my-rule'`).
   * @returns {never}
   *   Never.
   * @throws {VFileMessage}
   *   Message.
   */
  fail(causeOrReason, optionsOrParentOrPlace, origin) {
    const message = this.message(causeOrReason, optionsOrParentOrPlace, origin);
    message.fatal = true;
    throw message;
  }
  /**
   * Create an info message for `reason` associated with the file.
   *
   * The `fatal` field of the message is set to `undefined` (info; change
   * likely not needed) and the `file` field is set to the current file path.
   * The message is added to the `messages` field on `file`.
   *
   * > 🪦 **Note**: also has obsolete signatures.
   *
   * @overload
   * @param {string} reason
   * @param {MessageOptions | null | undefined} [options]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {string} reason
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {string} reason
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {string} reason
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @param {Error | VFileMessage | string} causeOrReason
   *   Reason for message, should use markdown.
   * @param {Node | NodeLike | MessageOptions | Point | Position | string | null | undefined} [optionsOrParentOrPlace]
   *   Configuration (optional).
   * @param {string | null | undefined} [origin]
   *   Place in code where the message originates (example:
   *   `'my-package:my-rule'` or `'my-rule'`).
   * @returns {VFileMessage}
   *   Message.
   */
  info(causeOrReason, optionsOrParentOrPlace, origin) {
    const message = this.message(causeOrReason, optionsOrParentOrPlace, origin);
    message.fatal = void 0;
    return message;
  }
  /**
   * Create a message for `reason` associated with the file.
   *
   * The `fatal` field of the message is set to `false` (warning; change may be
   * needed) and the `file` field is set to the current file path.
   * The message is added to the `messages` field on `file`.
   *
   * > 🪦 **Note**: also has obsolete signatures.
   *
   * @overload
   * @param {string} reason
   * @param {MessageOptions | null | undefined} [options]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {string} reason
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {string} reason
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {string} reason
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Node | NodeLike | null | undefined} parent
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {Point | Position | null | undefined} place
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @overload
   * @param {Error | VFileMessage} cause
   * @param {string | null | undefined} [origin]
   * @returns {VFileMessage}
   *
   * @param {Error | VFileMessage | string} causeOrReason
   *   Reason for message, should use markdown.
   * @param {Node | NodeLike | MessageOptions | Point | Position | string | null | undefined} [optionsOrParentOrPlace]
   *   Configuration (optional).
   * @param {string | null | undefined} [origin]
   *   Place in code where the message originates (example:
   *   `'my-package:my-rule'` or `'my-rule'`).
   * @returns {VFileMessage}
   *   Message.
   */
  message(causeOrReason, optionsOrParentOrPlace, origin) {
    const message = new VFileMessage(
      // @ts-expect-error: the overloads are fine.
      causeOrReason,
      optionsOrParentOrPlace,
      origin
    );
    if (this.path) {
      message.name = this.path + ":" + message.name;
      message.file = this.path;
    }
    message.fatal = false;
    this.messages.push(message);
    return message;
  }
  /**
   * Serialize the file.
   *
   * > **Note**: which encodings are supported depends on the engine.
   * > For info on Node.js, see:
   * > <https://nodejs.org/api/util.html#whatwg-supported-encodings>.
   *
   * @param {string | null | undefined} [encoding='utf8']
   *   Character encoding to understand `value` as when it’s a `Uint8Array`
   *   (default: `'utf-8'`).
   * @returns {string}
   *   Serialized file.
   */
  toString(encoding) {
    if (this.value === void 0) {
      return "";
    }
    if (typeof this.value === "string") {
      return this.value;
    }
    const decoder = new TextDecoder(encoding || void 0);
    return decoder.decode(this.value);
  }
}
function assertPart(part, name2) {
  if (part && part.includes(minpath.sep)) {
    throw new Error(
      "`" + name2 + "` cannot be a path: did not expect `" + minpath.sep + "`"
    );
  }
}
function assertNonEmpty(part, name2) {
  if (!part) {
    throw new Error("`" + name2 + "` cannot be empty");
  }
}
function assertPath(path2, name2) {
  if (!path2) {
    throw new Error("Setting `" + name2 + "` requires `path` to be set too");
  }
}
function isUint8Array$1(value) {
  return Boolean(
    value && typeof value === "object" && "byteLength" in value && "byteOffset" in value
  );
}
const CallableInstance = (
  /**
   * @type {new <Parameters extends Array<unknown>, Result>(property: string | symbol) => (...parameters: Parameters) => Result}
   */
  /** @type {unknown} */
  /**
   * @this {Function}
   * @param {string | symbol} property
   * @returns {(...parameters: Array<unknown>) => unknown}
   */
  (function(property) {
    const self2 = this;
    const constr = self2.constructor;
    const proto = (
      /** @type {Record<string | symbol, Function>} */
      // Prototypes do exist.
      // type-coverage:ignore-next-line
      constr.prototype
    );
    const value = proto[property];
    const apply = function() {
      return value.apply(apply, arguments);
    };
    Object.setPrototypeOf(apply, proto);
    return apply;
  })
);
const own = {}.hasOwnProperty;
class Processor extends CallableInstance {
  /**
   * Create a processor.
   */
  constructor() {
    super("copy");
    this.Compiler = void 0;
    this.Parser = void 0;
    this.attachers = [];
    this.compiler = void 0;
    this.freezeIndex = -1;
    this.frozen = void 0;
    this.namespace = {};
    this.parser = void 0;
    this.transformers = trough();
  }
  /**
   * Copy a processor.
   *
   * @deprecated
   *   This is a private internal method and should not be used.
   * @returns {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>}
   *   New *unfrozen* processor ({@linkcode Processor}) that is
   *   configured to work the same as its ancestor.
   *   When the descendant processor is configured in the future it does not
   *   affect the ancestral processor.
   */
  copy() {
    const destination = (
      /** @type {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>} */
      new Processor()
    );
    let index2 = -1;
    while (++index2 < this.attachers.length) {
      const attacher = this.attachers[index2];
      destination.use(...attacher);
    }
    destination.data(extend(true, {}, this.namespace));
    return destination;
  }
  /**
   * Configure the processor with info available to all plugins.
   * Information is stored in an object.
   *
   * Typically, options can be given to a specific plugin, but sometimes it
   * makes sense to have information shared with several plugins.
   * For example, a list of HTML elements that are self-closing, which is
   * needed during all phases.
   *
   * > **Note**: setting information cannot occur on *frozen* processors.
   * > Call the processor first to create a new unfrozen processor.
   *
   * > **Note**: to register custom data in TypeScript, augment the
   * > {@linkcode Data} interface.
   *
   * @example
   *   This example show how to get and set info:
   *
   *   ```js
   *   import {unified} from 'unified'
   *
   *   const processor = unified().data('alpha', 'bravo')
   *
   *   processor.data('alpha') // => 'bravo'
   *
   *   processor.data() // => {alpha: 'bravo'}
   *
   *   processor.data({charlie: 'delta'})
   *
   *   processor.data() // => {charlie: 'delta'}
   *   ```
   *
   * @template {keyof Data} Key
   *
   * @overload
   * @returns {Data}
   *
   * @overload
   * @param {Data} dataset
   * @returns {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>}
   *
   * @overload
   * @param {Key} key
   * @returns {Data[Key]}
   *
   * @overload
   * @param {Key} key
   * @param {Data[Key]} value
   * @returns {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>}
   *
   * @param {Data | Key} [key]
   *   Key to get or set, or entire dataset to set, or nothing to get the
   *   entire dataset (optional).
   * @param {Data[Key]} [value]
   *   Value to set (optional).
   * @returns {unknown}
   *   The current processor when setting, the value at `key` when getting, or
   *   the entire dataset when getting without key.
   */
  data(key, value) {
    if (typeof key === "string") {
      if (arguments.length === 2) {
        assertUnfrozen("data", this.frozen);
        this.namespace[key] = value;
        return this;
      }
      return own.call(this.namespace, key) && this.namespace[key] || void 0;
    }
    if (key) {
      assertUnfrozen("data", this.frozen);
      this.namespace = key;
      return this;
    }
    return this.namespace;
  }
  /**
   * Freeze a processor.
   *
   * Frozen processors are meant to be extended and not to be configured
   * directly.
   *
   * When a processor is frozen it cannot be unfrozen.
   * New processors working the same way can be created by calling the
   * processor.
   *
   * It’s possible to freeze processors explicitly by calling `.freeze()`.
   * Processors freeze automatically when `.parse()`, `.run()`, `.runSync()`,
   * `.stringify()`, `.process()`, or `.processSync()` are called.
   *
   * @returns {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>}
   *   The current processor.
   */
  freeze() {
    if (this.frozen) {
      return this;
    }
    const self2 = (
      /** @type {Processor} */
      /** @type {unknown} */
      this
    );
    while (++this.freezeIndex < this.attachers.length) {
      const [attacher, ...options] = this.attachers[this.freezeIndex];
      if (options[0] === false) {
        continue;
      }
      if (options[0] === true) {
        options[0] = void 0;
      }
      const transformer = attacher.call(self2, ...options);
      if (typeof transformer === "function") {
        this.transformers.use(transformer);
      }
    }
    this.frozen = true;
    this.freezeIndex = Number.POSITIVE_INFINITY;
    return this;
  }
  /**
   * Parse text to a syntax tree.
   *
   * > **Note**: `parse` freezes the processor if not already *frozen*.
   *
   * > **Note**: `parse` performs the parse phase, not the run phase or other
   * > phases.
   *
   * @param {Compatible | undefined} [file]
   *   file to parse (optional); typically `string` or `VFile`; any value
   *   accepted as `x` in `new VFile(x)`.
   * @returns {ParseTree extends undefined ? Node : ParseTree}
   *   Syntax tree representing `file`.
   */
  parse(file) {
    this.freeze();
    const realFile = vfile(file);
    const parser = this.parser || this.Parser;
    assertParser("parse", parser);
    return parser(String(realFile), realFile);
  }
  /**
   * Process the given file as configured on the processor.
   *
   * > **Note**: `process` freezes the processor if not already *frozen*.
   *
   * > **Note**: `process` performs the parse, run, and stringify phases.
   *
   * @overload
   * @param {Compatible | undefined} file
   * @param {ProcessCallback<VFileWithOutput<CompileResult>>} done
   * @returns {undefined}
   *
   * @overload
   * @param {Compatible | undefined} [file]
   * @returns {Promise<VFileWithOutput<CompileResult>>}
   *
   * @param {Compatible | undefined} [file]
   *   File (optional); typically `string` or `VFile`]; any value accepted as
   *   `x` in `new VFile(x)`.
   * @param {ProcessCallback<VFileWithOutput<CompileResult>> | undefined} [done]
   *   Callback (optional).
   * @returns {Promise<VFile> | undefined}
   *   Nothing if `done` is given.
   *   Otherwise a promise, rejected with a fatal error or resolved with the
   *   processed file.
   *
   *   The parsed, transformed, and compiled value is available at
   *   `file.value` (see note).
   *
   *   > **Note**: unified typically compiles by serializing: most
   *   > compilers return `string` (or `Uint8Array`).
   *   > Some compilers, such as the one configured with
   *   > [`rehype-react`][rehype-react], return other values (in this case, a
   *   > React tree).
   *   > If you’re using a compiler that doesn’t serialize, expect different
   *   > result values.
   *   >
   *   > To register custom results in TypeScript, add them to
   *   > {@linkcode CompileResultMap}.
   *
   *   [rehype-react]: https://github.com/rehypejs/rehype-react
   */
  process(file, done) {
    const self2 = this;
    this.freeze();
    assertParser("process", this.parser || this.Parser);
    assertCompiler("process", this.compiler || this.Compiler);
    return done ? executor(void 0, done) : new Promise(executor);
    function executor(resolve, reject) {
      const realFile = vfile(file);
      const parseTree = (
        /** @type {HeadTree extends undefined ? Node : HeadTree} */
        /** @type {unknown} */
        self2.parse(realFile)
      );
      self2.run(parseTree, realFile, function(error, tree, file2) {
        if (error || !tree || !file2) {
          return realDone(error);
        }
        const compileTree = (
          /** @type {CompileTree extends undefined ? Node : CompileTree} */
          /** @type {unknown} */
          tree
        );
        const compileResult = self2.stringify(compileTree, file2);
        if (looksLikeAValue(compileResult)) {
          file2.value = compileResult;
        } else {
          file2.result = compileResult;
        }
        realDone(
          error,
          /** @type {VFileWithOutput<CompileResult>} */
          file2
        );
      });
      function realDone(error, file2) {
        if (error || !file2) {
          reject(error);
        } else if (resolve) {
          resolve(file2);
        } else {
          done(void 0, file2);
        }
      }
    }
  }
  /**
   * Process the given file as configured on the processor.
   *
   * An error is thrown if asynchronous transforms are configured.
   *
   * > **Note**: `processSync` freezes the processor if not already *frozen*.
   *
   * > **Note**: `processSync` performs the parse, run, and stringify phases.
   *
   * @param {Compatible | undefined} [file]
   *   File (optional); typically `string` or `VFile`; any value accepted as
   *   `x` in `new VFile(x)`.
   * @returns {VFileWithOutput<CompileResult>}
   *   The processed file.
   *
   *   The parsed, transformed, and compiled value is available at
   *   `file.value` (see note).
   *
   *   > **Note**: unified typically compiles by serializing: most
   *   > compilers return `string` (or `Uint8Array`).
   *   > Some compilers, such as the one configured with
   *   > [`rehype-react`][rehype-react], return other values (in this case, a
   *   > React tree).
   *   > If you’re using a compiler that doesn’t serialize, expect different
   *   > result values.
   *   >
   *   > To register custom results in TypeScript, add them to
   *   > {@linkcode CompileResultMap}.
   *
   *   [rehype-react]: https://github.com/rehypejs/rehype-react
   */
  processSync(file) {
    let complete = false;
    let result;
    this.freeze();
    assertParser("processSync", this.parser || this.Parser);
    assertCompiler("processSync", this.compiler || this.Compiler);
    this.process(file, realDone);
    assertDone("processSync", "process", complete);
    return result;
    function realDone(error, file2) {
      complete = true;
      bail(error);
      result = file2;
    }
  }
  /**
   * Run *transformers* on a syntax tree.
   *
   * > **Note**: `run` freezes the processor if not already *frozen*.
   *
   * > **Note**: `run` performs the run phase, not other phases.
   *
   * @overload
   * @param {HeadTree extends undefined ? Node : HeadTree} tree
   * @param {RunCallback<TailTree extends undefined ? Node : TailTree>} done
   * @returns {undefined}
   *
   * @overload
   * @param {HeadTree extends undefined ? Node : HeadTree} tree
   * @param {Compatible | undefined} file
   * @param {RunCallback<TailTree extends undefined ? Node : TailTree>} done
   * @returns {undefined}
   *
   * @overload
   * @param {HeadTree extends undefined ? Node : HeadTree} tree
   * @param {Compatible | undefined} [file]
   * @returns {Promise<TailTree extends undefined ? Node : TailTree>}
   *
   * @param {HeadTree extends undefined ? Node : HeadTree} tree
   *   Tree to transform and inspect.
   * @param {(
   *   RunCallback<TailTree extends undefined ? Node : TailTree> |
   *   Compatible
   * )} [file]
   *   File associated with `node` (optional); any value accepted as `x` in
   *   `new VFile(x)`.
   * @param {RunCallback<TailTree extends undefined ? Node : TailTree>} [done]
   *   Callback (optional).
   * @returns {Promise<TailTree extends undefined ? Node : TailTree> | undefined}
   *   Nothing if `done` is given.
   *   Otherwise, a promise rejected with a fatal error or resolved with the
   *   transformed tree.
   */
  run(tree, file, done) {
    assertNode(tree);
    this.freeze();
    const transformers = this.transformers;
    if (!done && typeof file === "function") {
      done = file;
      file = void 0;
    }
    return done ? executor(void 0, done) : new Promise(executor);
    function executor(resolve, reject) {
      const realFile = vfile(file);
      transformers.run(tree, realFile, realDone);
      function realDone(error, outputTree, file2) {
        const resultingTree = (
          /** @type {TailTree extends undefined ? Node : TailTree} */
          outputTree || tree
        );
        if (error) {
          reject(error);
        } else if (resolve) {
          resolve(resultingTree);
        } else {
          done(void 0, resultingTree, file2);
        }
      }
    }
  }
  /**
   * Run *transformers* on a syntax tree.
   *
   * An error is thrown if asynchronous transforms are configured.
   *
   * > **Note**: `runSync` freezes the processor if not already *frozen*.
   *
   * > **Note**: `runSync` performs the run phase, not other phases.
   *
   * @param {HeadTree extends undefined ? Node : HeadTree} tree
   *   Tree to transform and inspect.
   * @param {Compatible | undefined} [file]
   *   File associated with `node` (optional); any value accepted as `x` in
   *   `new VFile(x)`.
   * @returns {TailTree extends undefined ? Node : TailTree}
   *   Transformed tree.
   */
  runSync(tree, file) {
    let complete = false;
    let result;
    this.run(tree, file, realDone);
    assertDone("runSync", "run", complete);
    return result;
    function realDone(error, tree2) {
      bail(error);
      result = tree2;
      complete = true;
    }
  }
  /**
   * Compile a syntax tree.
   *
   * > **Note**: `stringify` freezes the processor if not already *frozen*.
   *
   * > **Note**: `stringify` performs the stringify phase, not the run phase
   * > or other phases.
   *
   * @param {CompileTree extends undefined ? Node : CompileTree} tree
   *   Tree to compile.
   * @param {Compatible | undefined} [file]
   *   File associated with `node` (optional); any value accepted as `x` in
   *   `new VFile(x)`.
   * @returns {CompileResult extends undefined ? Value : CompileResult}
   *   Textual representation of the tree (see note).
   *
   *   > **Note**: unified typically compiles by serializing: most compilers
   *   > return `string` (or `Uint8Array`).
   *   > Some compilers, such as the one configured with
   *   > [`rehype-react`][rehype-react], return other values (in this case, a
   *   > React tree).
   *   > If you’re using a compiler that doesn’t serialize, expect different
   *   > result values.
   *   >
   *   > To register custom results in TypeScript, add them to
   *   > {@linkcode CompileResultMap}.
   *
   *   [rehype-react]: https://github.com/rehypejs/rehype-react
   */
  stringify(tree, file) {
    this.freeze();
    const realFile = vfile(file);
    const compiler2 = this.compiler || this.Compiler;
    assertCompiler("stringify", compiler2);
    assertNode(tree);
    return compiler2(tree, realFile);
  }
  /**
   * Configure the processor to use a plugin, a list of usable values, or a
   * preset.
   *
   * If the processor is already using a plugin, the previous plugin
   * configuration is changed based on the options that are passed in.
   * In other words, the plugin is not added a second time.
   *
   * > **Note**: `use` cannot be called on *frozen* processors.
   * > Call the processor first to create a new unfrozen processor.
   *
   * @example
   *   There are many ways to pass plugins to `.use()`.
   *   This example gives an overview:
   *
   *   ```js
   *   import {unified} from 'unified'
   *
   *   unified()
   *     // Plugin with options:
   *     .use(pluginA, {x: true, y: true})
   *     // Passing the same plugin again merges configuration (to `{x: true, y: false, z: true}`):
   *     .use(pluginA, {y: false, z: true})
   *     // Plugins:
   *     .use([pluginB, pluginC])
   *     // Two plugins, the second with options:
   *     .use([pluginD, [pluginE, {}]])
   *     // Preset with plugins and settings:
   *     .use({plugins: [pluginF, [pluginG, {}]], settings: {position: false}})
   *     // Settings only:
   *     .use({settings: {position: false}})
   *   ```
   *
   * @template {Array<unknown>} [Parameters=[]]
   * @template {Node | string | undefined} [Input=undefined]
   * @template [Output=Input]
   *
   * @overload
   * @param {Preset | null | undefined} [preset]
   * @returns {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>}
   *
   * @overload
   * @param {PluggableList} list
   * @returns {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>}
   *
   * @overload
   * @param {Plugin<Parameters, Input, Output>} plugin
   * @param {...(Parameters | [boolean])} parameters
   * @returns {UsePlugin<ParseTree, HeadTree, TailTree, CompileTree, CompileResult, Input, Output>}
   *
   * @param {PluggableList | Plugin | Preset | null | undefined} value
   *   Usable value.
   * @param {...unknown} parameters
   *   Parameters, when a plugin is given as a usable value.
   * @returns {Processor<ParseTree, HeadTree, TailTree, CompileTree, CompileResult>}
   *   Current processor.
   */
  use(value, ...parameters) {
    const attachers = this.attachers;
    const namespace = this.namespace;
    assertUnfrozen("use", this.frozen);
    if (value === null || value === void 0) ;
    else if (typeof value === "function") {
      addPlugin(value, parameters);
    } else if (typeof value === "object") {
      if (Array.isArray(value)) {
        addList(value);
      } else {
        addPreset(value);
      }
    } else {
      throw new TypeError("Expected usable value, not `" + value + "`");
    }
    return this;
    function add(value2) {
      if (typeof value2 === "function") {
        addPlugin(value2, []);
      } else if (typeof value2 === "object") {
        if (Array.isArray(value2)) {
          const [plugin, ...parameters2] = (
            /** @type {PluginTuple<Array<unknown>>} */
            value2
          );
          addPlugin(plugin, parameters2);
        } else {
          addPreset(value2);
        }
      } else {
        throw new TypeError("Expected usable value, not `" + value2 + "`");
      }
    }
    function addPreset(result) {
      if (!("plugins" in result) && !("settings" in result)) {
        throw new Error(
          "Expected usable value but received an empty preset, which is probably a mistake: presets typically come with `plugins` and sometimes with `settings`, but this has neither"
        );
      }
      addList(result.plugins);
      if (result.settings) {
        namespace.settings = extend(true, namespace.settings, result.settings);
      }
    }
    function addList(plugins) {
      let index2 = -1;
      if (plugins === null || plugins === void 0) ;
      else if (Array.isArray(plugins)) {
        while (++index2 < plugins.length) {
          const thing = plugins[index2];
          add(thing);
        }
      } else {
        throw new TypeError("Expected a list of plugins, not `" + plugins + "`");
      }
    }
    function addPlugin(plugin, parameters2) {
      let index2 = -1;
      let entryIndex = -1;
      while (++index2 < attachers.length) {
        if (attachers[index2][0] === plugin) {
          entryIndex = index2;
          break;
        }
      }
      if (entryIndex === -1) {
        attachers.push([plugin, ...parameters2]);
      } else if (parameters2.length > 0) {
        let [primary, ...rest] = parameters2;
        const currentPrimary = attachers[entryIndex][1];
        if (isPlainObject(currentPrimary) && isPlainObject(primary)) {
          primary = extend(true, currentPrimary, primary);
        }
        attachers[entryIndex] = [plugin, primary, ...rest];
      }
    }
  }
}
const unified = new Processor().freeze();
function assertParser(name2, value) {
  if (typeof value !== "function") {
    throw new TypeError("Cannot `" + name2 + "` without `parser`");
  }
}
function assertCompiler(name2, value) {
  if (typeof value !== "function") {
    throw new TypeError("Cannot `" + name2 + "` without `compiler`");
  }
}
function assertUnfrozen(name2, frozen) {
  if (frozen) {
    throw new Error(
      "Cannot call `" + name2 + "` on a frozen processor.\nCreate a new processor first, by calling it: use `processor()` instead of `processor`."
    );
  }
}
function assertNode(node2) {
  if (!isPlainObject(node2) || typeof node2.type !== "string") {
    throw new TypeError("Expected node, got `" + node2 + "`");
  }
}
function assertDone(name2, asyncName, complete) {
  if (!complete) {
    throw new Error(
      "`" + name2 + "` finished async. Use `" + asyncName + "` instead"
    );
  }
}
function vfile(value) {
  return looksLikeAVFile(value) ? value : new VFile(value);
}
function looksLikeAVFile(value) {
  return Boolean(
    value && typeof value === "object" && "message" in value && "messages" in value
  );
}
function looksLikeAValue(value) {
  return typeof value === "string" || isUint8Array(value);
}
function isUint8Array(value) {
  return Boolean(
    value && typeof value === "object" && "byteLength" in value && "byteOffset" in value
  );
}
const changelog = "https://github.com/remarkjs/react-markdown/blob/main/changelog.md";
const emptyPlugins = [];
const emptyRemarkRehypeOptions = { allowDangerousHtml: true };
const safeProtocol = /^(https?|ircs?|mailto|xmpp)$/i;
const deprecations = [
  { from: "astPlugins", id: "remove-buggy-html-in-markdown-parser" },
  { from: "allowDangerousHtml", id: "remove-buggy-html-in-markdown-parser" },
  {
    from: "allowNode",
    id: "replace-allownode-allowedtypes-and-disallowedtypes",
    to: "allowElement"
  },
  {
    from: "allowedTypes",
    id: "replace-allownode-allowedtypes-and-disallowedtypes",
    to: "allowedElements"
  },
  { from: "className", id: "remove-classname" },
  {
    from: "disallowedTypes",
    id: "replace-allownode-allowedtypes-and-disallowedtypes",
    to: "disallowedElements"
  },
  { from: "escapeHtml", id: "remove-buggy-html-in-markdown-parser" },
  { from: "includeElementIndex", id: "#remove-includeelementindex" },
  {
    from: "includeNodeIndex",
    id: "change-includenodeindex-to-includeelementindex"
  },
  { from: "linkTarget", id: "remove-linktarget" },
  { from: "plugins", id: "change-plugins-to-remarkplugins", to: "remarkPlugins" },
  { from: "rawSourcePos", id: "#remove-rawsourcepos" },
  { from: "renderers", id: "change-renderers-to-components", to: "components" },
  { from: "source", id: "change-source-to-children", to: "children" },
  { from: "sourcePos", id: "#remove-sourcepos" },
  { from: "transformImageUri", id: "#add-urltransform", to: "urlTransform" },
  { from: "transformLinkUri", id: "#add-urltransform", to: "urlTransform" }
];
function Markdown(options) {
  const processor = createProcessor(options);
  const file = createFile(options);
  return post(processor.runSync(processor.parse(file), file), options);
}
function createProcessor(options) {
  const rehypePlugins = options.rehypePlugins || emptyPlugins;
  const remarkPlugins = options.remarkPlugins || emptyPlugins;
  const remarkRehypeOptions = options.remarkRehypeOptions ? { ...options.remarkRehypeOptions, ...emptyRemarkRehypeOptions } : emptyRemarkRehypeOptions;
  const processor = unified().use(remarkParse).use(remarkPlugins).use(remarkRehype, remarkRehypeOptions).use(rehypePlugins);
  return processor;
}
function createFile(options) {
  const children = options.children || "";
  const file = new VFile();
  if (typeof children === "string") {
    file.value = children;
  }
  return file;
}
function post(tree, options) {
  const allowedElements = options.allowedElements;
  const allowElement = options.allowElement;
  const components = options.components;
  const disallowedElements = options.disallowedElements;
  const skipHtml = options.skipHtml;
  const unwrapDisallowed = options.unwrapDisallowed;
  const urlTransform = options.urlTransform || defaultUrlTransform;
  for (const deprecation of deprecations) {
    if (Object.hasOwn(options, deprecation.from)) {
      unreachable(
        "Unexpected `" + deprecation.from + "` prop, " + (deprecation.to ? "use `" + deprecation.to + "` instead" : "remove it") + " (see <" + changelog + "#" + deprecation.id + "> for more info)"
      );
    }
  }
  visit(tree, transform);
  return toJsxRuntime(tree, {
    Fragment: jsxRuntimeExports.Fragment,
    components,
    ignoreInvalidStyle: true,
    jsx: jsxRuntimeExports.jsx,
    jsxs: jsxRuntimeExports.jsxs,
    passKeys: true,
    passNode: true
  });
  function transform(node2, index2, parent) {
    if (node2.type === "raw" && parent && typeof index2 === "number") {
      if (skipHtml) {
        parent.children.splice(index2, 1);
      } else {
        parent.children[index2] = { type: "text", value: node2.value };
      }
      return index2;
    }
    if (node2.type === "element") {
      let key;
      for (key in urlAttributes) {
        if (Object.hasOwn(urlAttributes, key) && Object.hasOwn(node2.properties, key)) {
          const value = node2.properties[key];
          const test = urlAttributes[key];
          if (test === null || test.includes(node2.tagName)) {
            node2.properties[key] = urlTransform(String(value || ""), key, node2);
          }
        }
      }
    }
    if (node2.type === "element") {
      let remove = allowedElements ? !allowedElements.includes(node2.tagName) : disallowedElements ? disallowedElements.includes(node2.tagName) : false;
      if (!remove && allowElement && typeof index2 === "number") {
        remove = !allowElement(node2, index2, parent);
      }
      if (remove && parent && typeof index2 === "number") {
        if (unwrapDisallowed && node2.children) {
          parent.children.splice(index2, 1, ...node2.children);
        } else {
          parent.children.splice(index2, 1);
        }
        return index2;
      }
    }
  }
}
function defaultUrlTransform(value) {
  const colon = value.indexOf(":");
  const questionMark = value.indexOf("?");
  const numberSign = value.indexOf("#");
  const slash = value.indexOf("/");
  if (
    // If there is no protocol, it’s relative.
    colon === -1 || // If the first colon is after a `?`, `#`, or `/`, it’s not a protocol.
    slash !== -1 && colon > slash || questionMark !== -1 && colon > questionMark || numberSign !== -1 && colon > numberSign || // It is a protocol, it should be allowed.
    safeProtocol.test(value.slice(0, colon))
  ) {
    return value;
  }
  return "";
}
function ccount(value, character) {
  const source = String(value);
  if (typeof character !== "string") {
    throw new TypeError("Expected character");
  }
  let count = 0;
  let index2 = source.indexOf(character);
  while (index2 !== -1) {
    count++;
    index2 = source.indexOf(character, index2 + character.length);
  }
  return count;
}
function escapeStringRegexp(string2) {
  if (typeof string2 !== "string") {
    throw new TypeError("Expected a string");
  }
  return string2.replace(/[|\\{}()[\]^$+*?.]/g, "\\$&").replace(/-/g, "\\x2d");
}
function findAndReplace(tree, list2, options) {
  const settings = options || {};
  const ignored = convert(settings.ignore || []);
  const pairs = toPairs(list2);
  let pairIndex = -1;
  while (++pairIndex < pairs.length) {
    visitParents(tree, "text", visitor);
  }
  function visitor(node2, parents) {
    let index2 = -1;
    let grandparent;
    while (++index2 < parents.length) {
      const parent = parents[index2];
      const siblings = grandparent ? grandparent.children : void 0;
      if (ignored(
        parent,
        siblings ? siblings.indexOf(parent) : void 0,
        grandparent
      )) {
        return;
      }
      grandparent = parent;
    }
    if (grandparent) {
      return handler(node2, parents);
    }
  }
  function handler(node2, parents) {
    const parent = parents[parents.length - 1];
    const find2 = pairs[pairIndex][0];
    const replace2 = pairs[pairIndex][1];
    let start = 0;
    const siblings = parent.children;
    const index2 = siblings.indexOf(node2);
    let change = false;
    let nodes = [];
    find2.lastIndex = 0;
    let match = find2.exec(node2.value);
    while (match) {
      const position2 = match.index;
      const matchObject = {
        index: match.index,
        input: match.input,
        stack: [...parents, node2]
      };
      let value = replace2(...match, matchObject);
      if (typeof value === "string") {
        value = value.length > 0 ? { type: "text", value } : void 0;
      }
      if (value === false) {
        find2.lastIndex = position2 + 1;
      } else {
        if (start !== position2) {
          nodes.push({
            type: "text",
            value: node2.value.slice(start, position2)
          });
        }
        if (Array.isArray(value)) {
          nodes.push(...value);
        } else if (value) {
          nodes.push(value);
        }
        start = position2 + match[0].length;
        change = true;
      }
      if (!find2.global) {
        break;
      }
      match = find2.exec(node2.value);
    }
    if (change) {
      if (start < node2.value.length) {
        nodes.push({ type: "text", value: node2.value.slice(start) });
      }
      parent.children.splice(index2, 1, ...nodes);
    } else {
      nodes = [node2];
    }
    return index2 + nodes.length;
  }
}
function toPairs(tupleOrList) {
  const result = [];
  if (!Array.isArray(tupleOrList)) {
    throw new TypeError("Expected find and replace tuple or list of tuples");
  }
  const list2 = !tupleOrList[0] || Array.isArray(tupleOrList[0]) ? tupleOrList : [tupleOrList];
  let index2 = -1;
  while (++index2 < list2.length) {
    const tuple = list2[index2];
    result.push([toExpression(tuple[0]), toFunction(tuple[1])]);
  }
  return result;
}
function toExpression(find2) {
  return typeof find2 === "string" ? new RegExp(escapeStringRegexp(find2), "g") : find2;
}
function toFunction(replace2) {
  return typeof replace2 === "function" ? replace2 : function() {
    return replace2;
  };
}
const inConstruct = "phrasing";
const notInConstruct = ["autolink", "link", "image", "label"];
function gfmAutolinkLiteralFromMarkdown() {
  return {
    transforms: [transformGfmAutolinkLiterals],
    enter: {
      literalAutolink: enterLiteralAutolink,
      literalAutolinkEmail: enterLiteralAutolinkValue,
      literalAutolinkHttp: enterLiteralAutolinkValue,
      literalAutolinkWww: enterLiteralAutolinkValue
    },
    exit: {
      literalAutolink: exitLiteralAutolink,
      literalAutolinkEmail: exitLiteralAutolinkEmail,
      literalAutolinkHttp: exitLiteralAutolinkHttp,
      literalAutolinkWww: exitLiteralAutolinkWww
    }
  };
}
function gfmAutolinkLiteralToMarkdown() {
  return {
    unsafe: [
      {
        character: "@",
        before: "[+\\-.\\w]",
        after: "[\\-.\\w]",
        inConstruct,
        notInConstruct
      },
      {
        character: ".",
        before: "[Ww]",
        after: "[\\-.\\w]",
        inConstruct,
        notInConstruct
      },
      {
        character: ":",
        before: "[ps]",
        after: "\\/",
        inConstruct,
        notInConstruct
      }
    ]
  };
}
function enterLiteralAutolink(token) {
  this.enter({ type: "link", title: null, url: "", children: [] }, token);
}
function enterLiteralAutolinkValue(token) {
  this.config.enter.autolinkProtocol.call(this, token);
}
function exitLiteralAutolinkHttp(token) {
  this.config.exit.autolinkProtocol.call(this, token);
}
function exitLiteralAutolinkWww(token) {
  this.config.exit.data.call(this, token);
  const node2 = this.stack[this.stack.length - 1];
  ok(node2.type === "link");
  node2.url = "http://" + this.sliceSerialize(token);
}
function exitLiteralAutolinkEmail(token) {
  this.config.exit.autolinkEmail.call(this, token);
}
function exitLiteralAutolink(token) {
  this.exit(token);
}
function transformGfmAutolinkLiterals(tree) {
  findAndReplace(
    tree,
    [
      [/(https?:\/\/|www(?=\.))([-.\w]+)([^ \t\r\n]*)/gi, findUrl],
      [new RegExp("(?<=^|\\s|\\p{P}|\\p{S})([-.\\w+]+)@([-\\w]+(?:\\.[-\\w]+)+)", "gu"), findEmail]
    ],
    { ignore: ["link", "linkReference"] }
  );
}
function findUrl(_, protocol, domain2, path2, match) {
  let prefix = "";
  if (!previous(match)) {
    return false;
  }
  if (/^w/i.test(protocol)) {
    domain2 = protocol + domain2;
    protocol = "";
    prefix = "http://";
  }
  if (!isCorrectDomain(domain2)) {
    return false;
  }
  const parts = splitUrl(domain2 + path2);
  if (!parts[0]) return false;
  const result = {
    type: "link",
    title: null,
    url: prefix + protocol + parts[0],
    children: [{ type: "text", value: protocol + parts[0] }]
  };
  if (parts[1]) {
    return [result, { type: "text", value: parts[1] }];
  }
  return result;
}
function findEmail(_, atext, label, match) {
  if (
    // Not an expected previous character.
    !previous(match, true) || // Label ends in not allowed character.
    /[-\d_]$/.test(label)
  ) {
    return false;
  }
  return {
    type: "link",
    title: null,
    url: "mailto:" + atext + "@" + label,
    children: [{ type: "text", value: atext + "@" + label }]
  };
}
function isCorrectDomain(domain2) {
  const parts = domain2.split(".");
  if (parts.length < 2 || parts[parts.length - 1] && (/_/.test(parts[parts.length - 1]) || !/[a-zA-Z\d]/.test(parts[parts.length - 1])) || parts[parts.length - 2] && (/_/.test(parts[parts.length - 2]) || !/[a-zA-Z\d]/.test(parts[parts.length - 2]))) {
    return false;
  }
  return true;
}
function splitUrl(url) {
  const trailExec = /[!"&'),.:;<>?\]}]+$/.exec(url);
  if (!trailExec) {
    return [url, void 0];
  }
  url = url.slice(0, trailExec.index);
  let trail2 = trailExec[0];
  let closingParenIndex = trail2.indexOf(")");
  const openingParens = ccount(url, "(");
  let closingParens = ccount(url, ")");
  while (closingParenIndex !== -1 && openingParens > closingParens) {
    url += trail2.slice(0, closingParenIndex + 1);
    trail2 = trail2.slice(closingParenIndex + 1);
    closingParenIndex = trail2.indexOf(")");
    closingParens++;
  }
  return [url, trail2];
}
function previous(match, email) {
  const code2 = match.input.charCodeAt(match.index - 1);
  return (match.index === 0 || unicodeWhitespace(code2) || unicodePunctuation(code2)) && // If it’s an email, the previous character should not be a slash.
  (!email || code2 !== 47);
}
footnoteReference.peek = footnoteReferencePeek;
function enterFootnoteCallString() {
  this.buffer();
}
function enterFootnoteCall(token) {
  this.enter({ type: "footnoteReference", identifier: "", label: "" }, token);
}
function enterFootnoteDefinitionLabelString() {
  this.buffer();
}
function enterFootnoteDefinition(token) {
  this.enter(
    { type: "footnoteDefinition", identifier: "", label: "", children: [] },
    token
  );
}
function exitFootnoteCallString(token) {
  const label = this.resume();
  const node2 = this.stack[this.stack.length - 1];
  ok(node2.type === "footnoteReference");
  node2.identifier = normalizeIdentifier(
    this.sliceSerialize(token)
  ).toLowerCase();
  node2.label = label;
}
function exitFootnoteCall(token) {
  this.exit(token);
}
function exitFootnoteDefinitionLabelString(token) {
  const label = this.resume();
  const node2 = this.stack[this.stack.length - 1];
  ok(node2.type === "footnoteDefinition");
  node2.identifier = normalizeIdentifier(
    this.sliceSerialize(token)
  ).toLowerCase();
  node2.label = label;
}
function exitFootnoteDefinition(token) {
  this.exit(token);
}
function footnoteReferencePeek() {
  return "[";
}
function footnoteReference(node2, _, state, info) {
  const tracker = state.createTracker(info);
  let value = tracker.move("[^");
  const exit2 = state.enter("footnoteReference");
  const subexit = state.enter("reference");
  value += tracker.move(
    state.safe(state.associationId(node2), { after: "]", before: value })
  );
  subexit();
  exit2();
  value += tracker.move("]");
  return value;
}
function gfmFootnoteFromMarkdown() {
  return {
    enter: {
      gfmFootnoteCallString: enterFootnoteCallString,
      gfmFootnoteCall: enterFootnoteCall,
      gfmFootnoteDefinitionLabelString: enterFootnoteDefinitionLabelString,
      gfmFootnoteDefinition: enterFootnoteDefinition
    },
    exit: {
      gfmFootnoteCallString: exitFootnoteCallString,
      gfmFootnoteCall: exitFootnoteCall,
      gfmFootnoteDefinitionLabelString: exitFootnoteDefinitionLabelString,
      gfmFootnoteDefinition: exitFootnoteDefinition
    }
  };
}
function gfmFootnoteToMarkdown(options) {
  let firstLineBlank = false;
  if (options && options.firstLineBlank) {
    firstLineBlank = true;
  }
  return {
    handlers: { footnoteDefinition, footnoteReference },
    // This is on by default already.
    unsafe: [{ character: "[", inConstruct: ["label", "phrasing", "reference"] }]
  };
  function footnoteDefinition(node2, _, state, info) {
    const tracker = state.createTracker(info);
    let value = tracker.move("[^");
    const exit2 = state.enter("footnoteDefinition");
    const subexit = state.enter("label");
    value += tracker.move(
      state.safe(state.associationId(node2), { before: value, after: "]" })
    );
    subexit();
    value += tracker.move("]:");
    if (node2.children && node2.children.length > 0) {
      tracker.shift(4);
      value += tracker.move(
        (firstLineBlank ? "\n" : " ") + state.indentLines(
          state.containerFlow(node2, tracker.current()),
          firstLineBlank ? mapAll : mapExceptFirst
        )
      );
    }
    exit2();
    return value;
  }
}
function mapExceptFirst(line, index2, blank) {
  return index2 === 0 ? line : mapAll(line, index2, blank);
}
function mapAll(line, index2, blank) {
  return (blank ? "" : "    ") + line;
}
const constructsWithoutStrikethrough = [
  "autolink",
  "destinationLiteral",
  "destinationRaw",
  "reference",
  "titleQuote",
  "titleApostrophe"
];
handleDelete.peek = peekDelete;
function gfmStrikethroughFromMarkdown() {
  return {
    canContainEols: ["delete"],
    enter: { strikethrough: enterStrikethrough },
    exit: { strikethrough: exitStrikethrough }
  };
}
function gfmStrikethroughToMarkdown() {
  return {
    unsafe: [
      {
        character: "~",
        inConstruct: "phrasing",
        notInConstruct: constructsWithoutStrikethrough
      }
    ],
    handlers: { delete: handleDelete }
  };
}
function enterStrikethrough(token) {
  this.enter({ type: "delete", children: [] }, token);
}
function exitStrikethrough(token) {
  this.exit(token);
}
function handleDelete(node2, _, state, info) {
  const tracker = state.createTracker(info);
  const exit2 = state.enter("strikethrough");
  let value = tracker.move("~~");
  value += state.containerPhrasing(node2, {
    ...tracker.current(),
    before: value,
    after: "~"
  });
  value += tracker.move("~~");
  exit2();
  return value;
}
function peekDelete() {
  return "~";
}
function defaultStringLength(value) {
  return value.length;
}
function markdownTable(table2, options) {
  const settings = options || {};
  const align = (settings.align || []).concat();
  const stringLength = settings.stringLength || defaultStringLength;
  const alignments = [];
  const cellMatrix = [];
  const sizeMatrix = [];
  const longestCellByColumn = [];
  let mostCellsPerRow = 0;
  let rowIndex = -1;
  while (++rowIndex < table2.length) {
    const row2 = [];
    const sizes2 = [];
    let columnIndex2 = -1;
    if (table2[rowIndex].length > mostCellsPerRow) {
      mostCellsPerRow = table2[rowIndex].length;
    }
    while (++columnIndex2 < table2[rowIndex].length) {
      const cell = serialize(table2[rowIndex][columnIndex2]);
      if (settings.alignDelimiters !== false) {
        const size = stringLength(cell);
        sizes2[columnIndex2] = size;
        if (longestCellByColumn[columnIndex2] === void 0 || size > longestCellByColumn[columnIndex2]) {
          longestCellByColumn[columnIndex2] = size;
        }
      }
      row2.push(cell);
    }
    cellMatrix[rowIndex] = row2;
    sizeMatrix[rowIndex] = sizes2;
  }
  let columnIndex = -1;
  if (typeof align === "object" && "length" in align) {
    while (++columnIndex < mostCellsPerRow) {
      alignments[columnIndex] = toAlignment(align[columnIndex]);
    }
  } else {
    const code2 = toAlignment(align);
    while (++columnIndex < mostCellsPerRow) {
      alignments[columnIndex] = code2;
    }
  }
  columnIndex = -1;
  const row = [];
  const sizes = [];
  while (++columnIndex < mostCellsPerRow) {
    const code2 = alignments[columnIndex];
    let before = "";
    let after = "";
    if (code2 === 99) {
      before = ":";
      after = ":";
    } else if (code2 === 108) {
      before = ":";
    } else if (code2 === 114) {
      after = ":";
    }
    let size = settings.alignDelimiters === false ? 1 : Math.max(
      1,
      longestCellByColumn[columnIndex] - before.length - after.length
    );
    const cell = before + "-".repeat(size) + after;
    if (settings.alignDelimiters !== false) {
      size = before.length + size + after.length;
      if (size > longestCellByColumn[columnIndex]) {
        longestCellByColumn[columnIndex] = size;
      }
      sizes[columnIndex] = size;
    }
    row[columnIndex] = cell;
  }
  cellMatrix.splice(1, 0, row);
  sizeMatrix.splice(1, 0, sizes);
  rowIndex = -1;
  const lines = [];
  while (++rowIndex < cellMatrix.length) {
    const row2 = cellMatrix[rowIndex];
    const sizes2 = sizeMatrix[rowIndex];
    columnIndex = -1;
    const line = [];
    while (++columnIndex < mostCellsPerRow) {
      const cell = row2[columnIndex] || "";
      let before = "";
      let after = "";
      if (settings.alignDelimiters !== false) {
        const size = longestCellByColumn[columnIndex] - (sizes2[columnIndex] || 0);
        const code2 = alignments[columnIndex];
        if (code2 === 114) {
          before = " ".repeat(size);
        } else if (code2 === 99) {
          if (size % 2) {
            before = " ".repeat(size / 2 + 0.5);
            after = " ".repeat(size / 2 - 0.5);
          } else {
            before = " ".repeat(size / 2);
            after = before;
          }
        } else {
          after = " ".repeat(size);
        }
      }
      if (settings.delimiterStart !== false && !columnIndex) {
        line.push("|");
      }
      if (settings.padding !== false && // Don’t add the opening space if we’re not aligning and the cell is
      // empty: there will be a closing space.
      !(settings.alignDelimiters === false && cell === "") && (settings.delimiterStart !== false || columnIndex)) {
        line.push(" ");
      }
      if (settings.alignDelimiters !== false) {
        line.push(before);
      }
      line.push(cell);
      if (settings.alignDelimiters !== false) {
        line.push(after);
      }
      if (settings.padding !== false) {
        line.push(" ");
      }
      if (settings.delimiterEnd !== false || columnIndex !== mostCellsPerRow - 1) {
        line.push("|");
      }
    }
    lines.push(
      settings.delimiterEnd === false ? line.join("").replace(/ +$/, "") : line.join("")
    );
  }
  return lines.join("\n");
}
function serialize(value) {
  return value === null || value === void 0 ? "" : String(value);
}
function toAlignment(value) {
  const code2 = typeof value === "string" ? value.codePointAt(0) : 0;
  return code2 === 67 || code2 === 99 ? 99 : code2 === 76 || code2 === 108 ? 108 : code2 === 82 || code2 === 114 ? 114 : 0;
}
function blockquote(node2, _, state, info) {
  const exit2 = state.enter("blockquote");
  const tracker = state.createTracker(info);
  tracker.move("> ");
  tracker.shift(2);
  const value = state.indentLines(
    state.containerFlow(node2, tracker.current()),
    map$1
  );
  exit2();
  return value;
}
function map$1(line, _, blank) {
  return ">" + (blank ? "" : " ") + line;
}
function patternInScope(stack, pattern) {
  return listInScope(stack, pattern.inConstruct, true) && !listInScope(stack, pattern.notInConstruct, false);
}
function listInScope(stack, list2, none) {
  if (typeof list2 === "string") {
    list2 = [list2];
  }
  if (!list2 || list2.length === 0) {
    return none;
  }
  let index2 = -1;
  while (++index2 < list2.length) {
    if (stack.includes(list2[index2])) {
      return true;
    }
  }
  return false;
}
function hardBreak(_, _1, state, info) {
  let index2 = -1;
  while (++index2 < state.unsafe.length) {
    if (state.unsafe[index2].character === "\n" && patternInScope(state.stack, state.unsafe[index2])) {
      return /[ \t]/.test(info.before) ? "" : " ";
    }
  }
  return "\\\n";
}
function formatCodeAsIndented(node2, state) {
  return Boolean(
    state.options.fences === false && node2.value && // If there’s no info…
    !node2.lang && // And there’s a non-whitespace character…
    /[^ \r\n]/.test(node2.value) && // And the value doesn’t start or end in a blank…
    !/^[\t ]*(?:[\r\n]|$)|(?:^|[\r\n])[\t ]*$/.test(node2.value)
  );
}
function checkFence(state) {
  const marker = state.options.fence || "`";
  if (marker !== "`" && marker !== "~") {
    throw new Error(
      "Cannot serialize code with `" + marker + "` for `options.fence`, expected `` ` `` or `~`"
    );
  }
  return marker;
}
function code$1(node2, _, state, info) {
  const marker = checkFence(state);
  const raw = node2.value || "";
  const suffix = marker === "`" ? "GraveAccent" : "Tilde";
  if (formatCodeAsIndented(node2, state)) {
    const exit3 = state.enter("codeIndented");
    const value2 = state.indentLines(raw, map);
    exit3();
    return value2;
  }
  const tracker = state.createTracker(info);
  const sequence = marker.repeat(Math.max(longestStreak(raw, marker) + 1, 3));
  const exit2 = state.enter("codeFenced");
  let value = tracker.move(sequence);
  if (node2.lang) {
    const subexit = state.enter(`codeFencedLang${suffix}`);
    value += tracker.move(
      state.safe(node2.lang, {
        before: value,
        after: " ",
        encode: ["`"],
        ...tracker.current()
      })
    );
    subexit();
  }
  if (node2.lang && node2.meta) {
    const subexit = state.enter(`codeFencedMeta${suffix}`);
    value += tracker.move(" ");
    value += tracker.move(
      state.safe(node2.meta, {
        before: value,
        after: "\n",
        encode: ["`"],
        ...tracker.current()
      })
    );
    subexit();
  }
  value += tracker.move("\n");
  if (raw) {
    value += tracker.move(raw + "\n");
  }
  value += tracker.move(sequence);
  exit2();
  return value;
}
function map(line, _, blank) {
  return (blank ? "" : "    ") + line;
}
function checkQuote(state) {
  const marker = state.options.quote || '"';
  if (marker !== '"' && marker !== "'") {
    throw new Error(
      "Cannot serialize title with `" + marker + "` for `options.quote`, expected `\"`, or `'`"
    );
  }
  return marker;
}
function definition(node2, _, state, info) {
  const quote = checkQuote(state);
  const suffix = quote === '"' ? "Quote" : "Apostrophe";
  const exit2 = state.enter("definition");
  let subexit = state.enter("label");
  const tracker = state.createTracker(info);
  let value = tracker.move("[");
  value += tracker.move(
    state.safe(state.associationId(node2), {
      before: value,
      after: "]",
      ...tracker.current()
    })
  );
  value += tracker.move("]: ");
  subexit();
  if (
    // If there’s no url, or…
    !node2.url || // If there are control characters or whitespace.
    /[\0- \u007F]/.test(node2.url)
  ) {
    subexit = state.enter("destinationLiteral");
    value += tracker.move("<");
    value += tracker.move(
      state.safe(node2.url, { before: value, after: ">", ...tracker.current() })
    );
    value += tracker.move(">");
  } else {
    subexit = state.enter("destinationRaw");
    value += tracker.move(
      state.safe(node2.url, {
        before: value,
        after: node2.title ? " " : "\n",
        ...tracker.current()
      })
    );
  }
  subexit();
  if (node2.title) {
    subexit = state.enter(`title${suffix}`);
    value += tracker.move(" " + quote);
    value += tracker.move(
      state.safe(node2.title, {
        before: value,
        after: quote,
        ...tracker.current()
      })
    );
    value += tracker.move(quote);
    subexit();
  }
  exit2();
  return value;
}
function checkEmphasis(state) {
  const marker = state.options.emphasis || "*";
  if (marker !== "*" && marker !== "_") {
    throw new Error(
      "Cannot serialize emphasis with `" + marker + "` for `options.emphasis`, expected `*`, or `_`"
    );
  }
  return marker;
}
function encodeCharacterReference(code2) {
  return "&#x" + code2.toString(16).toUpperCase() + ";";
}
function encodeInfo(outside, inside, marker) {
  const outsideKind = classifyCharacter(outside);
  const insideKind = classifyCharacter(inside);
  if (outsideKind === void 0) {
    return insideKind === void 0 ? (
      // Letter inside:
      // we have to encode *both* letters for `_` as it is looser.
      // it already forms for `*` (and GFMs `~`).
      marker === "_" ? { inside: true, outside: true } : { inside: false, outside: false }
    ) : insideKind === 1 ? (
      // Whitespace inside: encode both (letter, whitespace).
      { inside: true, outside: true }
    ) : (
      // Punctuation inside: encode outer (letter)
      { inside: false, outside: true }
    );
  }
  if (outsideKind === 1) {
    return insideKind === void 0 ? (
      // Letter inside: already forms.
      { inside: false, outside: false }
    ) : insideKind === 1 ? (
      // Whitespace inside: encode both (whitespace).
      { inside: true, outside: true }
    ) : (
      // Punctuation inside: already forms.
      { inside: false, outside: false }
    );
  }
  return insideKind === void 0 ? (
    // Letter inside: already forms.
    { inside: false, outside: false }
  ) : insideKind === 1 ? (
    // Whitespace inside: encode inner (whitespace).
    { inside: true, outside: false }
  ) : (
    // Punctuation inside: already forms.
    { inside: false, outside: false }
  );
}
emphasis.peek = emphasisPeek;
function emphasis(node2, _, state, info) {
  const marker = checkEmphasis(state);
  const exit2 = state.enter("emphasis");
  const tracker = state.createTracker(info);
  const before = tracker.move(marker);
  let between = tracker.move(
    state.containerPhrasing(node2, {
      after: marker,
      before,
      ...tracker.current()
    })
  );
  const betweenHead = between.charCodeAt(0);
  const open = encodeInfo(
    info.before.charCodeAt(info.before.length - 1),
    betweenHead,
    marker
  );
  if (open.inside) {
    between = encodeCharacterReference(betweenHead) + between.slice(1);
  }
  const betweenTail = between.charCodeAt(between.length - 1);
  const close = encodeInfo(info.after.charCodeAt(0), betweenTail, marker);
  if (close.inside) {
    between = between.slice(0, -1) + encodeCharacterReference(betweenTail);
  }
  const after = tracker.move(marker);
  exit2();
  state.attentionEncodeSurroundingInfo = {
    after: close.outside,
    before: open.outside
  };
  return before + between + after;
}
function emphasisPeek(_, _1, state) {
  return state.options.emphasis || "*";
}
function formatHeadingAsSetext(node2, state) {
  let literalWithBreak = false;
  visit(node2, function(node3) {
    if ("value" in node3 && /\r?\n|\r/.test(node3.value) || node3.type === "break") {
      literalWithBreak = true;
      return EXIT;
    }
  });
  return Boolean(
    (!node2.depth || node2.depth < 3) && toString$1(node2) && (state.options.setext || literalWithBreak)
  );
}
function heading(node2, _, state, info) {
  const rank = Math.max(Math.min(6, node2.depth || 1), 1);
  const tracker = state.createTracker(info);
  if (formatHeadingAsSetext(node2, state)) {
    const exit3 = state.enter("headingSetext");
    const subexit2 = state.enter("phrasing");
    const value2 = state.containerPhrasing(node2, {
      ...tracker.current(),
      before: "\n",
      after: "\n"
    });
    subexit2();
    exit3();
    return value2 + "\n" + (rank === 1 ? "=" : "-").repeat(
      // The whole size…
      value2.length - // Minus the position of the character after the last EOL (or
      // 0 if there is none)…
      (Math.max(value2.lastIndexOf("\r"), value2.lastIndexOf("\n")) + 1)
    );
  }
  const sequence = "#".repeat(rank);
  const exit2 = state.enter("headingAtx");
  const subexit = state.enter("phrasing");
  tracker.move(sequence + " ");
  let value = state.containerPhrasing(node2, {
    before: "# ",
    after: "\n",
    ...tracker.current()
  });
  if (/^[\t ]/.test(value)) {
    value = encodeCharacterReference(value.charCodeAt(0)) + value.slice(1);
  }
  value = value ? sequence + " " + value : sequence;
  if (state.options.closeAtx) {
    value += " " + sequence;
  }
  subexit();
  exit2();
  return value;
}
html.peek = htmlPeek;
function html(node2) {
  return node2.value || "";
}
function htmlPeek() {
  return "<";
}
image.peek = imagePeek;
function image(node2, _, state, info) {
  const quote = checkQuote(state);
  const suffix = quote === '"' ? "Quote" : "Apostrophe";
  const exit2 = state.enter("image");
  let subexit = state.enter("label");
  const tracker = state.createTracker(info);
  let value = tracker.move("![");
  value += tracker.move(
    state.safe(node2.alt, { before: value, after: "]", ...tracker.current() })
  );
  value += tracker.move("](");
  subexit();
  if (
    // If there’s no url but there is a title…
    !node2.url && node2.title || // If there are control characters or whitespace.
    /[\0- \u007F]/.test(node2.url)
  ) {
    subexit = state.enter("destinationLiteral");
    value += tracker.move("<");
    value += tracker.move(
      state.safe(node2.url, { before: value, after: ">", ...tracker.current() })
    );
    value += tracker.move(">");
  } else {
    subexit = state.enter("destinationRaw");
    value += tracker.move(
      state.safe(node2.url, {
        before: value,
        after: node2.title ? " " : ")",
        ...tracker.current()
      })
    );
  }
  subexit();
  if (node2.title) {
    subexit = state.enter(`title${suffix}`);
    value += tracker.move(" " + quote);
    value += tracker.move(
      state.safe(node2.title, {
        before: value,
        after: quote,
        ...tracker.current()
      })
    );
    value += tracker.move(quote);
    subexit();
  }
  value += tracker.move(")");
  exit2();
  return value;
}
function imagePeek() {
  return "!";
}
imageReference.peek = imageReferencePeek;
function imageReference(node2, _, state, info) {
  const type = node2.referenceType;
  const exit2 = state.enter("imageReference");
  let subexit = state.enter("label");
  const tracker = state.createTracker(info);
  let value = tracker.move("![");
  const alt = state.safe(node2.alt, {
    before: value,
    after: "]",
    ...tracker.current()
  });
  value += tracker.move(alt + "][");
  subexit();
  const stack = state.stack;
  state.stack = [];
  subexit = state.enter("reference");
  const reference = state.safe(state.associationId(node2), {
    before: value,
    after: "]",
    ...tracker.current()
  });
  subexit();
  state.stack = stack;
  exit2();
  if (type === "full" || !alt || alt !== reference) {
    value += tracker.move(reference + "]");
  } else if (type === "shortcut") {
    value = value.slice(0, -1);
  } else {
    value += tracker.move("]");
  }
  return value;
}
function imageReferencePeek() {
  return "!";
}
inlineCode.peek = inlineCodePeek;
function inlineCode(node2, _, state) {
  let value = node2.value || "";
  let sequence = "`";
  let index2 = -1;
  while (new RegExp("(^|[^`])" + sequence + "([^`]|$)").test(value)) {
    sequence += "`";
  }
  if (/[^ \r\n]/.test(value) && (/^[ \r\n]/.test(value) && /[ \r\n]$/.test(value) || /^`|`$/.test(value))) {
    value = " " + value + " ";
  }
  while (++index2 < state.unsafe.length) {
    const pattern = state.unsafe[index2];
    const expression = state.compilePattern(pattern);
    let match;
    if (!pattern.atBreak) continue;
    while (match = expression.exec(value)) {
      let position2 = match.index;
      if (value.charCodeAt(position2) === 10 && value.charCodeAt(position2 - 1) === 13) {
        position2--;
      }
      value = value.slice(0, position2) + " " + value.slice(match.index + 1);
    }
  }
  return sequence + value + sequence;
}
function inlineCodePeek() {
  return "`";
}
function formatLinkAsAutolink(node2, state) {
  const raw = toString$1(node2);
  return Boolean(
    !state.options.resourceLink && // If there’s a url…
    node2.url && // And there’s a no title…
    !node2.title && // And the content of `node` is a single text node…
    node2.children && node2.children.length === 1 && node2.children[0].type === "text" && // And if the url is the same as the content…
    (raw === node2.url || "mailto:" + raw === node2.url) && // And that starts w/ a protocol…
    /^[a-z][a-z+.-]+:/i.test(node2.url) && // And that doesn’t contain ASCII control codes (character escapes and
    // references don’t work), space, or angle brackets…
    !/[\0- <>\u007F]/.test(node2.url)
  );
}
link.peek = linkPeek;
function link(node2, _, state, info) {
  const quote = checkQuote(state);
  const suffix = quote === '"' ? "Quote" : "Apostrophe";
  const tracker = state.createTracker(info);
  let exit2;
  let subexit;
  if (formatLinkAsAutolink(node2, state)) {
    const stack = state.stack;
    state.stack = [];
    exit2 = state.enter("autolink");
    let value2 = tracker.move("<");
    value2 += tracker.move(
      state.containerPhrasing(node2, {
        before: value2,
        after: ">",
        ...tracker.current()
      })
    );
    value2 += tracker.move(">");
    exit2();
    state.stack = stack;
    return value2;
  }
  exit2 = state.enter("link");
  subexit = state.enter("label");
  let value = tracker.move("[");
  value += tracker.move(
    state.containerPhrasing(node2, {
      before: value,
      after: "](",
      ...tracker.current()
    })
  );
  value += tracker.move("](");
  subexit();
  if (
    // If there’s no url but there is a title…
    !node2.url && node2.title || // If there are control characters or whitespace.
    /[\0- \u007F]/.test(node2.url)
  ) {
    subexit = state.enter("destinationLiteral");
    value += tracker.move("<");
    value += tracker.move(
      state.safe(node2.url, { before: value, after: ">", ...tracker.current() })
    );
    value += tracker.move(">");
  } else {
    subexit = state.enter("destinationRaw");
    value += tracker.move(
      state.safe(node2.url, {
        before: value,
        after: node2.title ? " " : ")",
        ...tracker.current()
      })
    );
  }
  subexit();
  if (node2.title) {
    subexit = state.enter(`title${suffix}`);
    value += tracker.move(" " + quote);
    value += tracker.move(
      state.safe(node2.title, {
        before: value,
        after: quote,
        ...tracker.current()
      })
    );
    value += tracker.move(quote);
    subexit();
  }
  value += tracker.move(")");
  exit2();
  return value;
}
function linkPeek(node2, _, state) {
  return formatLinkAsAutolink(node2, state) ? "<" : "[";
}
linkReference.peek = linkReferencePeek;
function linkReference(node2, _, state, info) {
  const type = node2.referenceType;
  const exit2 = state.enter("linkReference");
  let subexit = state.enter("label");
  const tracker = state.createTracker(info);
  let value = tracker.move("[");
  const text2 = state.containerPhrasing(node2, {
    before: value,
    after: "]",
    ...tracker.current()
  });
  value += tracker.move(text2 + "][");
  subexit();
  const stack = state.stack;
  state.stack = [];
  subexit = state.enter("reference");
  const reference = state.safe(state.associationId(node2), {
    before: value,
    after: "]",
    ...tracker.current()
  });
  subexit();
  state.stack = stack;
  exit2();
  if (type === "full" || !text2 || text2 !== reference) {
    value += tracker.move(reference + "]");
  } else if (type === "shortcut") {
    value = value.slice(0, -1);
  } else {
    value += tracker.move("]");
  }
  return value;
}
function linkReferencePeek() {
  return "[";
}
function checkBullet(state) {
  const marker = state.options.bullet || "*";
  if (marker !== "*" && marker !== "+" && marker !== "-") {
    throw new Error(
      "Cannot serialize items with `" + marker + "` for `options.bullet`, expected `*`, `+`, or `-`"
    );
  }
  return marker;
}
function checkBulletOther(state) {
  const bullet = checkBullet(state);
  const bulletOther = state.options.bulletOther;
  if (!bulletOther) {
    return bullet === "*" ? "-" : "*";
  }
  if (bulletOther !== "*" && bulletOther !== "+" && bulletOther !== "-") {
    throw new Error(
      "Cannot serialize items with `" + bulletOther + "` for `options.bulletOther`, expected `*`, `+`, or `-`"
    );
  }
  if (bulletOther === bullet) {
    throw new Error(
      "Expected `bullet` (`" + bullet + "`) and `bulletOther` (`" + bulletOther + "`) to be different"
    );
  }
  return bulletOther;
}
function checkBulletOrdered(state) {
  const marker = state.options.bulletOrdered || ".";
  if (marker !== "." && marker !== ")") {
    throw new Error(
      "Cannot serialize items with `" + marker + "` for `options.bulletOrdered`, expected `.` or `)`"
    );
  }
  return marker;
}
function checkRule(state) {
  const marker = state.options.rule || "*";
  if (marker !== "*" && marker !== "-" && marker !== "_") {
    throw new Error(
      "Cannot serialize rules with `" + marker + "` for `options.rule`, expected `*`, `-`, or `_`"
    );
  }
  return marker;
}
function list(node2, parent, state, info) {
  const exit2 = state.enter("list");
  const bulletCurrent = state.bulletCurrent;
  let bullet = node2.ordered ? checkBulletOrdered(state) : checkBullet(state);
  const bulletOther = node2.ordered ? bullet === "." ? ")" : "." : checkBulletOther(state);
  let useDifferentMarker = parent && state.bulletLastUsed ? bullet === state.bulletLastUsed : false;
  if (!node2.ordered) {
    const firstListItem = node2.children ? node2.children[0] : void 0;
    if (
      // Bullet could be used as a thematic break marker:
      (bullet === "*" || bullet === "-") && // Empty first list item:
      firstListItem && (!firstListItem.children || !firstListItem.children[0]) && // Directly in two other list items:
      state.stack[state.stack.length - 1] === "list" && state.stack[state.stack.length - 2] === "listItem" && state.stack[state.stack.length - 3] === "list" && state.stack[state.stack.length - 4] === "listItem" && // That are each the first child.
      state.indexStack[state.indexStack.length - 1] === 0 && state.indexStack[state.indexStack.length - 2] === 0 && state.indexStack[state.indexStack.length - 3] === 0
    ) {
      useDifferentMarker = true;
    }
    if (checkRule(state) === bullet && firstListItem) {
      let index2 = -1;
      while (++index2 < node2.children.length) {
        const item = node2.children[index2];
        if (item && item.type === "listItem" && item.children && item.children[0] && item.children[0].type === "thematicBreak") {
          useDifferentMarker = true;
          break;
        }
      }
    }
  }
  if (useDifferentMarker) {
    bullet = bulletOther;
  }
  state.bulletCurrent = bullet;
  const value = state.containerFlow(node2, info);
  state.bulletLastUsed = bullet;
  state.bulletCurrent = bulletCurrent;
  exit2();
  return value;
}
function checkListItemIndent(state) {
  const style = state.options.listItemIndent || "one";
  if (style !== "tab" && style !== "one" && style !== "mixed") {
    throw new Error(
      "Cannot serialize items with `" + style + "` for `options.listItemIndent`, expected `tab`, `one`, or `mixed`"
    );
  }
  return style;
}
function listItem(node2, parent, state, info) {
  const listItemIndent = checkListItemIndent(state);
  let bullet = state.bulletCurrent || checkBullet(state);
  if (parent && parent.type === "list" && parent.ordered) {
    bullet = (typeof parent.start === "number" && parent.start > -1 ? parent.start : 1) + (state.options.incrementListMarker === false ? 0 : parent.children.indexOf(node2)) + bullet;
  }
  let size = bullet.length + 1;
  if (listItemIndent === "tab" || listItemIndent === "mixed" && (parent && parent.type === "list" && parent.spread || node2.spread)) {
    size = Math.ceil(size / 4) * 4;
  }
  const tracker = state.createTracker(info);
  tracker.move(bullet + " ".repeat(size - bullet.length));
  tracker.shift(size);
  const exit2 = state.enter("listItem");
  const value = state.indentLines(
    state.containerFlow(node2, tracker.current()),
    map2
  );
  exit2();
  return value;
  function map2(line, index2, blank) {
    if (index2) {
      return (blank ? "" : " ".repeat(size)) + line;
    }
    return (blank ? bullet : bullet + " ".repeat(size - bullet.length)) + line;
  }
}
function paragraph(node2, _, state, info) {
  const exit2 = state.enter("paragraph");
  const subexit = state.enter("phrasing");
  const value = state.containerPhrasing(node2, info);
  subexit();
  exit2();
  return value;
}
const phrasing = (
  /** @type {(node?: unknown) => node is Exclude<PhrasingContent, Html>} */
  convert([
    "break",
    "delete",
    "emphasis",
    // To do: next major: removed since footnotes were added to GFM.
    "footnote",
    "footnoteReference",
    "image",
    "imageReference",
    "inlineCode",
    // Enabled by `mdast-util-math`:
    "inlineMath",
    "link",
    "linkReference",
    // Enabled by `mdast-util-mdx`:
    "mdxJsxTextElement",
    // Enabled by `mdast-util-mdx`:
    "mdxTextExpression",
    "strong",
    "text",
    // Enabled by `mdast-util-directive`:
    "textDirective"
  ])
);
function root(node2, _, state, info) {
  const hasPhrasing = node2.children.some(function(d) {
    return phrasing(d);
  });
  const container = hasPhrasing ? state.containerPhrasing : state.containerFlow;
  return container.call(state, node2, info);
}
function checkStrong(state) {
  const marker = state.options.strong || "*";
  if (marker !== "*" && marker !== "_") {
    throw new Error(
      "Cannot serialize strong with `" + marker + "` for `options.strong`, expected `*`, or `_`"
    );
  }
  return marker;
}
strong.peek = strongPeek;
function strong(node2, _, state, info) {
  const marker = checkStrong(state);
  const exit2 = state.enter("strong");
  const tracker = state.createTracker(info);
  const before = tracker.move(marker + marker);
  let between = tracker.move(
    state.containerPhrasing(node2, {
      after: marker,
      before,
      ...tracker.current()
    })
  );
  const betweenHead = between.charCodeAt(0);
  const open = encodeInfo(
    info.before.charCodeAt(info.before.length - 1),
    betweenHead,
    marker
  );
  if (open.inside) {
    between = encodeCharacterReference(betweenHead) + between.slice(1);
  }
  const betweenTail = between.charCodeAt(between.length - 1);
  const close = encodeInfo(info.after.charCodeAt(0), betweenTail, marker);
  if (close.inside) {
    between = between.slice(0, -1) + encodeCharacterReference(betweenTail);
  }
  const after = tracker.move(marker + marker);
  exit2();
  state.attentionEncodeSurroundingInfo = {
    after: close.outside,
    before: open.outside
  };
  return before + between + after;
}
function strongPeek(_, _1, state) {
  return state.options.strong || "*";
}
function text$1(node2, _, state, info) {
  return state.safe(node2.value, info);
}
function checkRuleRepetition(state) {
  const repetition = state.options.ruleRepetition || 3;
  if (repetition < 3) {
    throw new Error(
      "Cannot serialize rules with repetition `" + repetition + "` for `options.ruleRepetition`, expected `3` or more"
    );
  }
  return repetition;
}
function thematicBreak(_, _1, state) {
  const value = (checkRule(state) + (state.options.ruleSpaces ? " " : "")).repeat(checkRuleRepetition(state));
  return state.options.ruleSpaces ? value.slice(0, -1) : value;
}
const handle = {
  blockquote,
  break: hardBreak,
  code: code$1,
  definition,
  emphasis,
  hardBreak,
  heading,
  html,
  image,
  imageReference,
  inlineCode,
  link,
  linkReference,
  list,
  listItem,
  paragraph,
  root,
  strong,
  text: text$1,
  thematicBreak
};
function gfmTableFromMarkdown() {
  return {
    enter: {
      table: enterTable,
      tableData: enterCell,
      tableHeader: enterCell,
      tableRow: enterRow
    },
    exit: {
      codeText: exitCodeText,
      table: exitTable,
      tableData: exit,
      tableHeader: exit,
      tableRow: exit
    }
  };
}
function enterTable(token) {
  const align = token._align;
  this.enter(
    {
      type: "table",
      align: align.map(function(d) {
        return d === "none" ? null : d;
      }),
      children: []
    },
    token
  );
  this.data.inTable = true;
}
function exitTable(token) {
  this.exit(token);
  this.data.inTable = void 0;
}
function enterRow(token) {
  this.enter({ type: "tableRow", children: [] }, token);
}
function exit(token) {
  this.exit(token);
}
function enterCell(token) {
  this.enter({ type: "tableCell", children: [] }, token);
}
function exitCodeText(token) {
  let value = this.resume();
  if (this.data.inTable) {
    value = value.replace(/\\([\\|])/g, replace);
  }
  const node2 = this.stack[this.stack.length - 1];
  ok(node2.type === "inlineCode");
  node2.value = value;
  this.exit(token);
}
function replace($0, $1) {
  return $1 === "|" ? $1 : $0;
}
function gfmTableToMarkdown(options) {
  const settings = options || {};
  const padding = settings.tableCellPadding;
  const alignDelimiters = settings.tablePipeAlign;
  const stringLength = settings.stringLength;
  const around = padding ? " " : "|";
  return {
    unsafe: [
      { character: "\r", inConstruct: "tableCell" },
      { character: "\n", inConstruct: "tableCell" },
      // A pipe, when followed by a tab or space (padding), or a dash or colon
      // (unpadded delimiter row), could result in a table.
      { atBreak: true, character: "|", after: "[	 :-]" },
      // A pipe in a cell must be encoded.
      { character: "|", inConstruct: "tableCell" },
      // A colon must be followed by a dash, in which case it could start a
      // delimiter row.
      { atBreak: true, character: ":", after: "-" },
      // A delimiter row can also start with a dash, when followed by more
      // dashes, a colon, or a pipe.
      // This is a stricter version than the built in check for lists, thematic
      // breaks, and setex heading underlines though:
      // <https://github.com/syntax-tree/mdast-util-to-markdown/blob/51a2038/lib/unsafe.js#L57>
      { atBreak: true, character: "-", after: "[:|-]" }
    ],
    handlers: {
      inlineCode: inlineCodeWithTable,
      table: handleTable,
      tableCell: handleTableCell,
      tableRow: handleTableRow
    }
  };
  function handleTable(node2, _, state, info) {
    return serializeData(handleTableAsData(node2, state, info), node2.align);
  }
  function handleTableRow(node2, _, state, info) {
    const row = handleTableRowAsData(node2, state, info);
    const value = serializeData([row]);
    return value.slice(0, value.indexOf("\n"));
  }
  function handleTableCell(node2, _, state, info) {
    const exit2 = state.enter("tableCell");
    const subexit = state.enter("phrasing");
    const value = state.containerPhrasing(node2, {
      ...info,
      before: around,
      after: around
    });
    subexit();
    exit2();
    return value;
  }
  function serializeData(matrix, align) {
    return markdownTable(matrix, {
      align,
      // @ts-expect-error: `markdown-table` types should support `null`.
      alignDelimiters,
      // @ts-expect-error: `markdown-table` types should support `null`.
      padding,
      // @ts-expect-error: `markdown-table` types should support `null`.
      stringLength
    });
  }
  function handleTableAsData(node2, state, info) {
    const children = node2.children;
    let index2 = -1;
    const result = [];
    const subexit = state.enter("table");
    while (++index2 < children.length) {
      result[index2] = handleTableRowAsData(children[index2], state, info);
    }
    subexit();
    return result;
  }
  function handleTableRowAsData(node2, state, info) {
    const children = node2.children;
    let index2 = -1;
    const result = [];
    const subexit = state.enter("tableRow");
    while (++index2 < children.length) {
      result[index2] = handleTableCell(children[index2], node2, state, info);
    }
    subexit();
    return result;
  }
  function inlineCodeWithTable(node2, parent, state) {
    let value = handle.inlineCode(node2, parent, state);
    if (state.stack.includes("tableCell")) {
      value = value.replace(/\|/g, "\\$&");
    }
    return value;
  }
}
function gfmTaskListItemFromMarkdown() {
  return {
    exit: {
      taskListCheckValueChecked: exitCheck,
      taskListCheckValueUnchecked: exitCheck,
      paragraph: exitParagraphWithTaskListItem
    }
  };
}
function gfmTaskListItemToMarkdown() {
  return {
    unsafe: [{ atBreak: true, character: "-", after: "[:|-]" }],
    handlers: { listItem: listItemWithTaskListItem }
  };
}
function exitCheck(token) {
  const node2 = this.stack[this.stack.length - 2];
  ok(node2.type === "listItem");
  node2.checked = token.type === "taskListCheckValueChecked";
}
function exitParagraphWithTaskListItem(token) {
  const parent = this.stack[this.stack.length - 2];
  if (parent && parent.type === "listItem" && typeof parent.checked === "boolean") {
    const node2 = this.stack[this.stack.length - 1];
    ok(node2.type === "paragraph");
    const head = node2.children[0];
    if (head && head.type === "text") {
      const siblings = parent.children;
      let index2 = -1;
      let firstParaghraph;
      while (++index2 < siblings.length) {
        const sibling = siblings[index2];
        if (sibling.type === "paragraph") {
          firstParaghraph = sibling;
          break;
        }
      }
      if (firstParaghraph === node2) {
        head.value = head.value.slice(1);
        if (head.value.length === 0) {
          node2.children.shift();
        } else if (node2.position && head.position && typeof head.position.start.offset === "number") {
          head.position.start.column++;
          head.position.start.offset++;
          node2.position.start = Object.assign({}, head.position.start);
        }
      }
    }
  }
  this.exit(token);
}
function listItemWithTaskListItem(node2, parent, state, info) {
  const head = node2.children[0];
  const checkable = typeof node2.checked === "boolean" && head && head.type === "paragraph";
  const checkbox = "[" + (node2.checked ? "x" : " ") + "] ";
  const tracker = state.createTracker(info);
  if (checkable) {
    tracker.move(checkbox);
  }
  let value = handle.listItem(node2, parent, state, {
    ...info,
    ...tracker.current()
  });
  if (checkable) {
    value = value.replace(/^(?:[*+-]|\d+\.)([\r\n]| {1,3})/, check);
  }
  return value;
  function check($0) {
    return $0 + checkbox;
  }
}
function gfmFromMarkdown() {
  return [
    gfmAutolinkLiteralFromMarkdown(),
    gfmFootnoteFromMarkdown(),
    gfmStrikethroughFromMarkdown(),
    gfmTableFromMarkdown(),
    gfmTaskListItemFromMarkdown()
  ];
}
function gfmToMarkdown(options) {
  return {
    extensions: [
      gfmAutolinkLiteralToMarkdown(),
      gfmFootnoteToMarkdown(options),
      gfmStrikethroughToMarkdown(),
      gfmTableToMarkdown(options),
      gfmTaskListItemToMarkdown()
    ]
  };
}
const wwwPrefix = {
  tokenize: tokenizeWwwPrefix,
  partial: true
};
const domain = {
  tokenize: tokenizeDomain,
  partial: true
};
const path = {
  tokenize: tokenizePath,
  partial: true
};
const trail = {
  tokenize: tokenizeTrail,
  partial: true
};
const emailDomainDotTrail = {
  tokenize: tokenizeEmailDomainDotTrail,
  partial: true
};
const wwwAutolink = {
  name: "wwwAutolink",
  tokenize: tokenizeWwwAutolink,
  previous: previousWww
};
const protocolAutolink = {
  name: "protocolAutolink",
  tokenize: tokenizeProtocolAutolink,
  previous: previousProtocol
};
const emailAutolink = {
  name: "emailAutolink",
  tokenize: tokenizeEmailAutolink,
  previous: previousEmail
};
const text = {};
function gfmAutolinkLiteral() {
  return {
    text
  };
}
let code = 48;
while (code < 123) {
  text[code] = emailAutolink;
  code++;
  if (code === 58) code = 65;
  else if (code === 91) code = 97;
}
text[43] = emailAutolink;
text[45] = emailAutolink;
text[46] = emailAutolink;
text[95] = emailAutolink;
text[72] = [emailAutolink, protocolAutolink];
text[104] = [emailAutolink, protocolAutolink];
text[87] = [emailAutolink, wwwAutolink];
text[119] = [emailAutolink, wwwAutolink];
function tokenizeEmailAutolink(effects, ok2, nok) {
  const self2 = this;
  let dot;
  let data;
  return start;
  function start(code2) {
    if (!gfmAtext(code2) || !previousEmail.call(self2, self2.previous) || previousUnbalanced(self2.events)) {
      return nok(code2);
    }
    effects.enter("literalAutolink");
    effects.enter("literalAutolinkEmail");
    return atext(code2);
  }
  function atext(code2) {
    if (gfmAtext(code2)) {
      effects.consume(code2);
      return atext;
    }
    if (code2 === 64) {
      effects.consume(code2);
      return emailDomain;
    }
    return nok(code2);
  }
  function emailDomain(code2) {
    if (code2 === 46) {
      return effects.check(emailDomainDotTrail, emailDomainAfter, emailDomainDot)(code2);
    }
    if (code2 === 45 || code2 === 95 || asciiAlphanumeric(code2)) {
      data = true;
      effects.consume(code2);
      return emailDomain;
    }
    return emailDomainAfter(code2);
  }
  function emailDomainDot(code2) {
    effects.consume(code2);
    dot = true;
    return emailDomain;
  }
  function emailDomainAfter(code2) {
    if (data && dot && asciiAlpha(self2.previous)) {
      effects.exit("literalAutolinkEmail");
      effects.exit("literalAutolink");
      return ok2(code2);
    }
    return nok(code2);
  }
}
function tokenizeWwwAutolink(effects, ok2, nok) {
  const self2 = this;
  return wwwStart;
  function wwwStart(code2) {
    if (code2 !== 87 && code2 !== 119 || !previousWww.call(self2, self2.previous) || previousUnbalanced(self2.events)) {
      return nok(code2);
    }
    effects.enter("literalAutolink");
    effects.enter("literalAutolinkWww");
    return effects.check(wwwPrefix, effects.attempt(domain, effects.attempt(path, wwwAfter), nok), nok)(code2);
  }
  function wwwAfter(code2) {
    effects.exit("literalAutolinkWww");
    effects.exit("literalAutolink");
    return ok2(code2);
  }
}
function tokenizeProtocolAutolink(effects, ok2, nok) {
  const self2 = this;
  let buffer = "";
  let seen = false;
  return protocolStart;
  function protocolStart(code2) {
    if ((code2 === 72 || code2 === 104) && previousProtocol.call(self2, self2.previous) && !previousUnbalanced(self2.events)) {
      effects.enter("literalAutolink");
      effects.enter("literalAutolinkHttp");
      buffer += String.fromCodePoint(code2);
      effects.consume(code2);
      return protocolPrefixInside;
    }
    return nok(code2);
  }
  function protocolPrefixInside(code2) {
    if (asciiAlpha(code2) && buffer.length < 5) {
      buffer += String.fromCodePoint(code2);
      effects.consume(code2);
      return protocolPrefixInside;
    }
    if (code2 === 58) {
      const protocol = buffer.toLowerCase();
      if (protocol === "http" || protocol === "https") {
        effects.consume(code2);
        return protocolSlashesInside;
      }
    }
    return nok(code2);
  }
  function protocolSlashesInside(code2) {
    if (code2 === 47) {
      effects.consume(code2);
      if (seen) {
        return afterProtocol;
      }
      seen = true;
      return protocolSlashesInside;
    }
    return nok(code2);
  }
  function afterProtocol(code2) {
    return code2 === null || asciiControl(code2) || markdownLineEndingOrSpace(code2) || unicodeWhitespace(code2) || unicodePunctuation(code2) ? nok(code2) : effects.attempt(domain, effects.attempt(path, protocolAfter), nok)(code2);
  }
  function protocolAfter(code2) {
    effects.exit("literalAutolinkHttp");
    effects.exit("literalAutolink");
    return ok2(code2);
  }
}
function tokenizeWwwPrefix(effects, ok2, nok) {
  let size = 0;
  return wwwPrefixInside;
  function wwwPrefixInside(code2) {
    if ((code2 === 87 || code2 === 119) && size < 3) {
      size++;
      effects.consume(code2);
      return wwwPrefixInside;
    }
    if (code2 === 46 && size === 3) {
      effects.consume(code2);
      return wwwPrefixAfter;
    }
    return nok(code2);
  }
  function wwwPrefixAfter(code2) {
    return code2 === null ? nok(code2) : ok2(code2);
  }
}
function tokenizeDomain(effects, ok2, nok) {
  let underscoreInLastSegment;
  let underscoreInLastLastSegment;
  let seen;
  return domainInside;
  function domainInside(code2) {
    if (code2 === 46 || code2 === 95) {
      return effects.check(trail, domainAfter, domainAtPunctuation)(code2);
    }
    if (code2 === null || markdownLineEndingOrSpace(code2) || unicodeWhitespace(code2) || code2 !== 45 && unicodePunctuation(code2)) {
      return domainAfter(code2);
    }
    seen = true;
    effects.consume(code2);
    return domainInside;
  }
  function domainAtPunctuation(code2) {
    if (code2 === 95) {
      underscoreInLastSegment = true;
    } else {
      underscoreInLastLastSegment = underscoreInLastSegment;
      underscoreInLastSegment = void 0;
    }
    effects.consume(code2);
    return domainInside;
  }
  function domainAfter(code2) {
    if (underscoreInLastLastSegment || underscoreInLastSegment || !seen) {
      return nok(code2);
    }
    return ok2(code2);
  }
}
function tokenizePath(effects, ok2) {
  let sizeOpen = 0;
  let sizeClose = 0;
  return pathInside;
  function pathInside(code2) {
    if (code2 === 40) {
      sizeOpen++;
      effects.consume(code2);
      return pathInside;
    }
    if (code2 === 41 && sizeClose < sizeOpen) {
      return pathAtPunctuation(code2);
    }
    if (code2 === 33 || code2 === 34 || code2 === 38 || code2 === 39 || code2 === 41 || code2 === 42 || code2 === 44 || code2 === 46 || code2 === 58 || code2 === 59 || code2 === 60 || code2 === 63 || code2 === 93 || code2 === 95 || code2 === 126) {
      return effects.check(trail, ok2, pathAtPunctuation)(code2);
    }
    if (code2 === null || markdownLineEndingOrSpace(code2) || unicodeWhitespace(code2)) {
      return ok2(code2);
    }
    effects.consume(code2);
    return pathInside;
  }
  function pathAtPunctuation(code2) {
    if (code2 === 41) {
      sizeClose++;
    }
    effects.consume(code2);
    return pathInside;
  }
}
function tokenizeTrail(effects, ok2, nok) {
  return trail2;
  function trail2(code2) {
    if (code2 === 33 || code2 === 34 || code2 === 39 || code2 === 41 || code2 === 42 || code2 === 44 || code2 === 46 || code2 === 58 || code2 === 59 || code2 === 63 || code2 === 95 || code2 === 126) {
      effects.consume(code2);
      return trail2;
    }
    if (code2 === 38) {
      effects.consume(code2);
      return trailCharacterReferenceStart;
    }
    if (code2 === 93) {
      effects.consume(code2);
      return trailBracketAfter;
    }
    if (
      // `<` is an end.
      code2 === 60 || // So is whitespace.
      code2 === null || markdownLineEndingOrSpace(code2) || unicodeWhitespace(code2)
    ) {
      return ok2(code2);
    }
    return nok(code2);
  }
  function trailBracketAfter(code2) {
    if (code2 === null || code2 === 40 || code2 === 91 || markdownLineEndingOrSpace(code2) || unicodeWhitespace(code2)) {
      return ok2(code2);
    }
    return trail2(code2);
  }
  function trailCharacterReferenceStart(code2) {
    return asciiAlpha(code2) ? trailCharacterReferenceInside(code2) : nok(code2);
  }
  function trailCharacterReferenceInside(code2) {
    if (code2 === 59) {
      effects.consume(code2);
      return trail2;
    }
    if (asciiAlpha(code2)) {
      effects.consume(code2);
      return trailCharacterReferenceInside;
    }
    return nok(code2);
  }
}
function tokenizeEmailDomainDotTrail(effects, ok2, nok) {
  return start;
  function start(code2) {
    effects.consume(code2);
    return after;
  }
  function after(code2) {
    return asciiAlphanumeric(code2) ? nok(code2) : ok2(code2);
  }
}
function previousWww(code2) {
  return code2 === null || code2 === 40 || code2 === 42 || code2 === 95 || code2 === 91 || code2 === 93 || code2 === 126 || markdownLineEndingOrSpace(code2);
}
function previousProtocol(code2) {
  return !asciiAlpha(code2);
}
function previousEmail(code2) {
  return !(code2 === 47 || gfmAtext(code2));
}
function gfmAtext(code2) {
  return code2 === 43 || code2 === 45 || code2 === 46 || code2 === 95 || asciiAlphanumeric(code2);
}
function previousUnbalanced(events) {
  let index2 = events.length;
  let result = false;
  while (index2--) {
    const token = events[index2][1];
    if ((token.type === "labelLink" || token.type === "labelImage") && !token._balanced) {
      result = true;
      break;
    }
    if (token._gfmAutolinkLiteralWalkedInto) {
      result = false;
      break;
    }
  }
  if (events.length > 0 && !result) {
    events[events.length - 1][1]._gfmAutolinkLiteralWalkedInto = true;
  }
  return result;
}
const indent = {
  tokenize: tokenizeIndent,
  partial: true
};
function gfmFootnote() {
  return {
    document: {
      [91]: {
        name: "gfmFootnoteDefinition",
        tokenize: tokenizeDefinitionStart,
        continuation: {
          tokenize: tokenizeDefinitionContinuation
        },
        exit: gfmFootnoteDefinitionEnd
      }
    },
    text: {
      [91]: {
        name: "gfmFootnoteCall",
        tokenize: tokenizeGfmFootnoteCall
      },
      [93]: {
        name: "gfmPotentialFootnoteCall",
        add: "after",
        tokenize: tokenizePotentialGfmFootnoteCall,
        resolveTo: resolveToPotentialGfmFootnoteCall
      }
    }
  };
}
function tokenizePotentialGfmFootnoteCall(effects, ok2, nok) {
  const self2 = this;
  let index2 = self2.events.length;
  const defined = self2.parser.gfmFootnotes || (self2.parser.gfmFootnotes = []);
  let labelStart;
  while (index2--) {
    const token = self2.events[index2][1];
    if (token.type === "labelImage") {
      labelStart = token;
      break;
    }
    if (token.type === "gfmFootnoteCall" || token.type === "labelLink" || token.type === "label" || token.type === "image" || token.type === "link") {
      break;
    }
  }
  return start;
  function start(code2) {
    if (!labelStart || !labelStart._balanced) {
      return nok(code2);
    }
    const id = normalizeIdentifier(self2.sliceSerialize({
      start: labelStart.end,
      end: self2.now()
    }));
    if (id.codePointAt(0) !== 94 || !defined.includes(id.slice(1))) {
      return nok(code2);
    }
    effects.enter("gfmFootnoteCallLabelMarker");
    effects.consume(code2);
    effects.exit("gfmFootnoteCallLabelMarker");
    return ok2(code2);
  }
}
function resolveToPotentialGfmFootnoteCall(events, context) {
  let index2 = events.length;
  while (index2--) {
    if (events[index2][1].type === "labelImage" && events[index2][0] === "enter") {
      events[index2][1];
      break;
    }
  }
  events[index2 + 1][1].type = "data";
  events[index2 + 3][1].type = "gfmFootnoteCallLabelMarker";
  const call = {
    type: "gfmFootnoteCall",
    start: Object.assign({}, events[index2 + 3][1].start),
    end: Object.assign({}, events[events.length - 1][1].end)
  };
  const marker = {
    type: "gfmFootnoteCallMarker",
    start: Object.assign({}, events[index2 + 3][1].end),
    end: Object.assign({}, events[index2 + 3][1].end)
  };
  marker.end.column++;
  marker.end.offset++;
  marker.end._bufferIndex++;
  const string2 = {
    type: "gfmFootnoteCallString",
    start: Object.assign({}, marker.end),
    end: Object.assign({}, events[events.length - 1][1].start)
  };
  const chunk = {
    type: "chunkString",
    contentType: "string",
    start: Object.assign({}, string2.start),
    end: Object.assign({}, string2.end)
  };
  const replacement = [
    // Take the `labelImageMarker` (now `data`, the `!`)
    events[index2 + 1],
    events[index2 + 2],
    ["enter", call, context],
    // The `[`
    events[index2 + 3],
    events[index2 + 4],
    // The `^`.
    ["enter", marker, context],
    ["exit", marker, context],
    // Everything in between.
    ["enter", string2, context],
    ["enter", chunk, context],
    ["exit", chunk, context],
    ["exit", string2, context],
    // The ending (`]`, properly parsed and labelled).
    events[events.length - 2],
    events[events.length - 1],
    ["exit", call, context]
  ];
  events.splice(index2, events.length - index2 + 1, ...replacement);
  return events;
}
function tokenizeGfmFootnoteCall(effects, ok2, nok) {
  const self2 = this;
  const defined = self2.parser.gfmFootnotes || (self2.parser.gfmFootnotes = []);
  let size = 0;
  let data;
  return start;
  function start(code2) {
    effects.enter("gfmFootnoteCall");
    effects.enter("gfmFootnoteCallLabelMarker");
    effects.consume(code2);
    effects.exit("gfmFootnoteCallLabelMarker");
    return callStart;
  }
  function callStart(code2) {
    if (code2 !== 94) return nok(code2);
    effects.enter("gfmFootnoteCallMarker");
    effects.consume(code2);
    effects.exit("gfmFootnoteCallMarker");
    effects.enter("gfmFootnoteCallString");
    effects.enter("chunkString").contentType = "string";
    return callData;
  }
  function callData(code2) {
    if (
      // Too long.
      size > 999 || // Closing brace with nothing.
      code2 === 93 && !data || // Space or tab is not supported by GFM for some reason.
      // `\n` and `[` not being supported makes sense.
      code2 === null || code2 === 91 || markdownLineEndingOrSpace(code2)
    ) {
      return nok(code2);
    }
    if (code2 === 93) {
      effects.exit("chunkString");
      const token = effects.exit("gfmFootnoteCallString");
      if (!defined.includes(normalizeIdentifier(self2.sliceSerialize(token)))) {
        return nok(code2);
      }
      effects.enter("gfmFootnoteCallLabelMarker");
      effects.consume(code2);
      effects.exit("gfmFootnoteCallLabelMarker");
      effects.exit("gfmFootnoteCall");
      return ok2;
    }
    if (!markdownLineEndingOrSpace(code2)) {
      data = true;
    }
    size++;
    effects.consume(code2);
    return code2 === 92 ? callEscape : callData;
  }
  function callEscape(code2) {
    if (code2 === 91 || code2 === 92 || code2 === 93) {
      effects.consume(code2);
      size++;
      return callData;
    }
    return callData(code2);
  }
}
function tokenizeDefinitionStart(effects, ok2, nok) {
  const self2 = this;
  const defined = self2.parser.gfmFootnotes || (self2.parser.gfmFootnotes = []);
  let identifier;
  let size = 0;
  let data;
  return start;
  function start(code2) {
    effects.enter("gfmFootnoteDefinition")._container = true;
    effects.enter("gfmFootnoteDefinitionLabel");
    effects.enter("gfmFootnoteDefinitionLabelMarker");
    effects.consume(code2);
    effects.exit("gfmFootnoteDefinitionLabelMarker");
    return labelAtMarker;
  }
  function labelAtMarker(code2) {
    if (code2 === 94) {
      effects.enter("gfmFootnoteDefinitionMarker");
      effects.consume(code2);
      effects.exit("gfmFootnoteDefinitionMarker");
      effects.enter("gfmFootnoteDefinitionLabelString");
      effects.enter("chunkString").contentType = "string";
      return labelInside;
    }
    return nok(code2);
  }
  function labelInside(code2) {
    if (
      // Too long.
      size > 999 || // Closing brace with nothing.
      code2 === 93 && !data || // Space or tab is not supported by GFM for some reason.
      // `\n` and `[` not being supported makes sense.
      code2 === null || code2 === 91 || markdownLineEndingOrSpace(code2)
    ) {
      return nok(code2);
    }
    if (code2 === 93) {
      effects.exit("chunkString");
      const token = effects.exit("gfmFootnoteDefinitionLabelString");
      identifier = normalizeIdentifier(self2.sliceSerialize(token));
      effects.enter("gfmFootnoteDefinitionLabelMarker");
      effects.consume(code2);
      effects.exit("gfmFootnoteDefinitionLabelMarker");
      effects.exit("gfmFootnoteDefinitionLabel");
      return labelAfter;
    }
    if (!markdownLineEndingOrSpace(code2)) {
      data = true;
    }
    size++;
    effects.consume(code2);
    return code2 === 92 ? labelEscape : labelInside;
  }
  function labelEscape(code2) {
    if (code2 === 91 || code2 === 92 || code2 === 93) {
      effects.consume(code2);
      size++;
      return labelInside;
    }
    return labelInside(code2);
  }
  function labelAfter(code2) {
    if (code2 === 58) {
      effects.enter("definitionMarker");
      effects.consume(code2);
      effects.exit("definitionMarker");
      if (!defined.includes(identifier)) {
        defined.push(identifier);
      }
      return factorySpace(effects, whitespaceAfter, "gfmFootnoteDefinitionWhitespace");
    }
    return nok(code2);
  }
  function whitespaceAfter(code2) {
    return ok2(code2);
  }
}
function tokenizeDefinitionContinuation(effects, ok2, nok) {
  return effects.check(blankLine, ok2, effects.attempt(indent, ok2, nok));
}
function gfmFootnoteDefinitionEnd(effects) {
  effects.exit("gfmFootnoteDefinition");
}
function tokenizeIndent(effects, ok2, nok) {
  const self2 = this;
  return factorySpace(effects, afterPrefix, "gfmFootnoteDefinitionIndent", 4 + 1);
  function afterPrefix(code2) {
    const tail = self2.events[self2.events.length - 1];
    return tail && tail[1].type === "gfmFootnoteDefinitionIndent" && tail[2].sliceSerialize(tail[1], true).length === 4 ? ok2(code2) : nok(code2);
  }
}
function gfmStrikethrough(options) {
  const options_ = options || {};
  let single = options_.singleTilde;
  const tokenizer = {
    name: "strikethrough",
    tokenize: tokenizeStrikethrough,
    resolveAll: resolveAllStrikethrough
  };
  if (single === null || single === void 0) {
    single = true;
  }
  return {
    text: {
      [126]: tokenizer
    },
    insideSpan: {
      null: [tokenizer]
    },
    attentionMarkers: {
      null: [126]
    }
  };
  function resolveAllStrikethrough(events, context) {
    let index2 = -1;
    while (++index2 < events.length) {
      if (events[index2][0] === "enter" && events[index2][1].type === "strikethroughSequenceTemporary" && events[index2][1]._close) {
        let open = index2;
        while (open--) {
          if (events[open][0] === "exit" && events[open][1].type === "strikethroughSequenceTemporary" && events[open][1]._open && // If the sizes are the same:
          events[index2][1].end.offset - events[index2][1].start.offset === events[open][1].end.offset - events[open][1].start.offset) {
            events[index2][1].type = "strikethroughSequence";
            events[open][1].type = "strikethroughSequence";
            const strikethrough2 = {
              type: "strikethrough",
              start: Object.assign({}, events[open][1].start),
              end: Object.assign({}, events[index2][1].end)
            };
            const text2 = {
              type: "strikethroughText",
              start: Object.assign({}, events[open][1].end),
              end: Object.assign({}, events[index2][1].start)
            };
            const nextEvents = [["enter", strikethrough2, context], ["enter", events[open][1], context], ["exit", events[open][1], context], ["enter", text2, context]];
            const insideSpan2 = context.parser.constructs.insideSpan.null;
            if (insideSpan2) {
              splice(nextEvents, nextEvents.length, 0, resolveAll(insideSpan2, events.slice(open + 1, index2), context));
            }
            splice(nextEvents, nextEvents.length, 0, [["exit", text2, context], ["enter", events[index2][1], context], ["exit", events[index2][1], context], ["exit", strikethrough2, context]]);
            splice(events, open - 1, index2 - open + 3, nextEvents);
            index2 = open + nextEvents.length - 2;
            break;
          }
        }
      }
    }
    index2 = -1;
    while (++index2 < events.length) {
      if (events[index2][1].type === "strikethroughSequenceTemporary") {
        events[index2][1].type = "data";
      }
    }
    return events;
  }
  function tokenizeStrikethrough(effects, ok2, nok) {
    const previous2 = this.previous;
    const events = this.events;
    let size = 0;
    return start;
    function start(code2) {
      if (previous2 === 126 && events[events.length - 1][1].type !== "characterEscape") {
        return nok(code2);
      }
      effects.enter("strikethroughSequenceTemporary");
      return more(code2);
    }
    function more(code2) {
      const before = classifyCharacter(previous2);
      if (code2 === 126) {
        if (size > 1) return nok(code2);
        effects.consume(code2);
        size++;
        return more;
      }
      if (size < 2 && !single) return nok(code2);
      const token = effects.exit("strikethroughSequenceTemporary");
      const after = classifyCharacter(code2);
      token._open = !after || after === 2 && Boolean(before);
      token._close = !before || before === 2 && Boolean(after);
      return ok2(code2);
    }
  }
}
class EditMap {
  /**
   * Create a new edit map.
   */
  constructor() {
    this.map = [];
  }
  /**
   * Create an edit: a remove and/or add at a certain place.
   *
   * @param {number} index
   * @param {number} remove
   * @param {Array<Event>} add
   * @returns {undefined}
   */
  add(index2, remove, add) {
    addImplementation(this, index2, remove, add);
  }
  // To do: add this when moving to `micromark`.
  // /**
  //  * Create an edit: but insert `add` before existing additions.
  //  *
  //  * @param {number} index
  //  * @param {number} remove
  //  * @param {Array<Event>} add
  //  * @returns {undefined}
  //  */
  // addBefore(index, remove, add) {
  //   addImplementation(this, index, remove, add, true)
  // }
  /**
   * Done, change the events.
   *
   * @param {Array<Event>} events
   * @returns {undefined}
   */
  consume(events) {
    this.map.sort(function(a, b) {
      return a[0] - b[0];
    });
    if (this.map.length === 0) {
      return;
    }
    let index2 = this.map.length;
    const vecs = [];
    while (index2 > 0) {
      index2 -= 1;
      vecs.push(events.slice(this.map[index2][0] + this.map[index2][1]), this.map[index2][2]);
      events.length = this.map[index2][0];
    }
    vecs.push(events.slice());
    events.length = 0;
    let slice = vecs.pop();
    while (slice) {
      for (const element2 of slice) {
        events.push(element2);
      }
      slice = vecs.pop();
    }
    this.map.length = 0;
  }
}
function addImplementation(editMap, at, remove, add) {
  let index2 = 0;
  if (remove === 0 && add.length === 0) {
    return;
  }
  while (index2 < editMap.map.length) {
    if (editMap.map[index2][0] === at) {
      editMap.map[index2][1] += remove;
      editMap.map[index2][2].push(...add);
      return;
    }
    index2 += 1;
  }
  editMap.map.push([at, remove, add]);
}
function gfmTableAlign(events, index2) {
  let inDelimiterRow = false;
  const align = [];
  while (index2 < events.length) {
    const event = events[index2];
    if (inDelimiterRow) {
      if (event[0] === "enter") {
        if (event[1].type === "tableContent") {
          align.push(events[index2 + 1][1].type === "tableDelimiterMarker" ? "left" : "none");
        }
      } else if (event[1].type === "tableContent") {
        if (events[index2 - 1][1].type === "tableDelimiterMarker") {
          const alignIndex = align.length - 1;
          align[alignIndex] = align[alignIndex] === "left" ? "center" : "right";
        }
      } else if (event[1].type === "tableDelimiterRow") {
        break;
      }
    } else if (event[0] === "enter" && event[1].type === "tableDelimiterRow") {
      inDelimiterRow = true;
    }
    index2 += 1;
  }
  return align;
}
function gfmTable() {
  return {
    flow: {
      null: {
        name: "table",
        tokenize: tokenizeTable,
        resolveAll: resolveTable
      }
    }
  };
}
function tokenizeTable(effects, ok2, nok) {
  const self2 = this;
  let size = 0;
  let sizeB = 0;
  let seen;
  return start;
  function start(code2) {
    let index2 = self2.events.length - 1;
    while (index2 > -1) {
      const type = self2.events[index2][1].type;
      if (type === "lineEnding" || // Note: markdown-rs uses `whitespace` instead of `linePrefix`
      type === "linePrefix") index2--;
      else break;
    }
    const tail = index2 > -1 ? self2.events[index2][1].type : null;
    const next = tail === "tableHead" || tail === "tableRow" ? bodyRowStart : headRowBefore;
    if (next === bodyRowStart && self2.parser.lazy[self2.now().line]) {
      return nok(code2);
    }
    return next(code2);
  }
  function headRowBefore(code2) {
    effects.enter("tableHead");
    effects.enter("tableRow");
    return headRowStart(code2);
  }
  function headRowStart(code2) {
    if (code2 === 124) {
      return headRowBreak(code2);
    }
    seen = true;
    sizeB += 1;
    return headRowBreak(code2);
  }
  function headRowBreak(code2) {
    if (code2 === null) {
      return nok(code2);
    }
    if (markdownLineEnding(code2)) {
      if (sizeB > 1) {
        sizeB = 0;
        self2.interrupt = true;
        effects.exit("tableRow");
        effects.enter("lineEnding");
        effects.consume(code2);
        effects.exit("lineEnding");
        return headDelimiterStart;
      }
      return nok(code2);
    }
    if (markdownSpace(code2)) {
      return factorySpace(effects, headRowBreak, "whitespace")(code2);
    }
    sizeB += 1;
    if (seen) {
      seen = false;
      size += 1;
    }
    if (code2 === 124) {
      effects.enter("tableCellDivider");
      effects.consume(code2);
      effects.exit("tableCellDivider");
      seen = true;
      return headRowBreak;
    }
    effects.enter("data");
    return headRowData(code2);
  }
  function headRowData(code2) {
    if (code2 === null || code2 === 124 || markdownLineEndingOrSpace(code2)) {
      effects.exit("data");
      return headRowBreak(code2);
    }
    effects.consume(code2);
    return code2 === 92 ? headRowEscape : headRowData;
  }
  function headRowEscape(code2) {
    if (code2 === 92 || code2 === 124) {
      effects.consume(code2);
      return headRowData;
    }
    return headRowData(code2);
  }
  function headDelimiterStart(code2) {
    self2.interrupt = false;
    if (self2.parser.lazy[self2.now().line]) {
      return nok(code2);
    }
    effects.enter("tableDelimiterRow");
    seen = false;
    if (markdownSpace(code2)) {
      return factorySpace(effects, headDelimiterBefore, "linePrefix", self2.parser.constructs.disable.null.includes("codeIndented") ? void 0 : 4)(code2);
    }
    return headDelimiterBefore(code2);
  }
  function headDelimiterBefore(code2) {
    if (code2 === 45 || code2 === 58) {
      return headDelimiterValueBefore(code2);
    }
    if (code2 === 124) {
      seen = true;
      effects.enter("tableCellDivider");
      effects.consume(code2);
      effects.exit("tableCellDivider");
      return headDelimiterCellBefore;
    }
    return headDelimiterNok(code2);
  }
  function headDelimiterCellBefore(code2) {
    if (markdownSpace(code2)) {
      return factorySpace(effects, headDelimiterValueBefore, "whitespace")(code2);
    }
    return headDelimiterValueBefore(code2);
  }
  function headDelimiterValueBefore(code2) {
    if (code2 === 58) {
      sizeB += 1;
      seen = true;
      effects.enter("tableDelimiterMarker");
      effects.consume(code2);
      effects.exit("tableDelimiterMarker");
      return headDelimiterLeftAlignmentAfter;
    }
    if (code2 === 45) {
      sizeB += 1;
      return headDelimiterLeftAlignmentAfter(code2);
    }
    if (code2 === null || markdownLineEnding(code2)) {
      return headDelimiterCellAfter(code2);
    }
    return headDelimiterNok(code2);
  }
  function headDelimiterLeftAlignmentAfter(code2) {
    if (code2 === 45) {
      effects.enter("tableDelimiterFiller");
      return headDelimiterFiller(code2);
    }
    return headDelimiterNok(code2);
  }
  function headDelimiterFiller(code2) {
    if (code2 === 45) {
      effects.consume(code2);
      return headDelimiterFiller;
    }
    if (code2 === 58) {
      seen = true;
      effects.exit("tableDelimiterFiller");
      effects.enter("tableDelimiterMarker");
      effects.consume(code2);
      effects.exit("tableDelimiterMarker");
      return headDelimiterRightAlignmentAfter;
    }
    effects.exit("tableDelimiterFiller");
    return headDelimiterRightAlignmentAfter(code2);
  }
  function headDelimiterRightAlignmentAfter(code2) {
    if (markdownSpace(code2)) {
      return factorySpace(effects, headDelimiterCellAfter, "whitespace")(code2);
    }
    return headDelimiterCellAfter(code2);
  }
  function headDelimiterCellAfter(code2) {
    if (code2 === 124) {
      return headDelimiterBefore(code2);
    }
    if (code2 === null || markdownLineEnding(code2)) {
      if (!seen || size !== sizeB) {
        return headDelimiterNok(code2);
      }
      effects.exit("tableDelimiterRow");
      effects.exit("tableHead");
      return ok2(code2);
    }
    return headDelimiterNok(code2);
  }
  function headDelimiterNok(code2) {
    return nok(code2);
  }
  function bodyRowStart(code2) {
    effects.enter("tableRow");
    return bodyRowBreak(code2);
  }
  function bodyRowBreak(code2) {
    if (code2 === 124) {
      effects.enter("tableCellDivider");
      effects.consume(code2);
      effects.exit("tableCellDivider");
      return bodyRowBreak;
    }
    if (code2 === null || markdownLineEnding(code2)) {
      effects.exit("tableRow");
      return ok2(code2);
    }
    if (markdownSpace(code2)) {
      return factorySpace(effects, bodyRowBreak, "whitespace")(code2);
    }
    effects.enter("data");
    return bodyRowData(code2);
  }
  function bodyRowData(code2) {
    if (code2 === null || code2 === 124 || markdownLineEndingOrSpace(code2)) {
      effects.exit("data");
      return bodyRowBreak(code2);
    }
    effects.consume(code2);
    return code2 === 92 ? bodyRowEscape : bodyRowData;
  }
  function bodyRowEscape(code2) {
    if (code2 === 92 || code2 === 124) {
      effects.consume(code2);
      return bodyRowData;
    }
    return bodyRowData(code2);
  }
}
function resolveTable(events, context) {
  let index2 = -1;
  let inFirstCellAwaitingPipe = true;
  let rowKind = 0;
  let lastCell = [0, 0, 0, 0];
  let cell = [0, 0, 0, 0];
  let afterHeadAwaitingFirstBodyRow = false;
  let lastTableEnd = 0;
  let currentTable;
  let currentBody;
  let currentCell;
  const map2 = new EditMap();
  while (++index2 < events.length) {
    const event = events[index2];
    const token = event[1];
    if (event[0] === "enter") {
      if (token.type === "tableHead") {
        afterHeadAwaitingFirstBodyRow = false;
        if (lastTableEnd !== 0) {
          flushTableEnd(map2, context, lastTableEnd, currentTable, currentBody);
          currentBody = void 0;
          lastTableEnd = 0;
        }
        currentTable = {
          type: "table",
          start: Object.assign({}, token.start),
          // Note: correct end is set later.
          end: Object.assign({}, token.end)
        };
        map2.add(index2, 0, [["enter", currentTable, context]]);
      } else if (token.type === "tableRow" || token.type === "tableDelimiterRow") {
        inFirstCellAwaitingPipe = true;
        currentCell = void 0;
        lastCell = [0, 0, 0, 0];
        cell = [0, index2 + 1, 0, 0];
        if (afterHeadAwaitingFirstBodyRow) {
          afterHeadAwaitingFirstBodyRow = false;
          currentBody = {
            type: "tableBody",
            start: Object.assign({}, token.start),
            // Note: correct end is set later.
            end: Object.assign({}, token.end)
          };
          map2.add(index2, 0, [["enter", currentBody, context]]);
        }
        rowKind = token.type === "tableDelimiterRow" ? 2 : currentBody ? 3 : 1;
      } else if (rowKind && (token.type === "data" || token.type === "tableDelimiterMarker" || token.type === "tableDelimiterFiller")) {
        inFirstCellAwaitingPipe = false;
        if (cell[2] === 0) {
          if (lastCell[1] !== 0) {
            cell[0] = cell[1];
            currentCell = flushCell(map2, context, lastCell, rowKind, void 0, currentCell);
            lastCell = [0, 0, 0, 0];
          }
          cell[2] = index2;
        }
      } else if (token.type === "tableCellDivider") {
        if (inFirstCellAwaitingPipe) {
          inFirstCellAwaitingPipe = false;
        } else {
          if (lastCell[1] !== 0) {
            cell[0] = cell[1];
            currentCell = flushCell(map2, context, lastCell, rowKind, void 0, currentCell);
          }
          lastCell = cell;
          cell = [lastCell[1], index2, 0, 0];
        }
      }
    } else if (token.type === "tableHead") {
      afterHeadAwaitingFirstBodyRow = true;
      lastTableEnd = index2;
    } else if (token.type === "tableRow" || token.type === "tableDelimiterRow") {
      lastTableEnd = index2;
      if (lastCell[1] !== 0) {
        cell[0] = cell[1];
        currentCell = flushCell(map2, context, lastCell, rowKind, index2, currentCell);
      } else if (cell[1] !== 0) {
        currentCell = flushCell(map2, context, cell, rowKind, index2, currentCell);
      }
      rowKind = 0;
    } else if (rowKind && (token.type === "data" || token.type === "tableDelimiterMarker" || token.type === "tableDelimiterFiller")) {
      cell[3] = index2;
    }
  }
  if (lastTableEnd !== 0) {
    flushTableEnd(map2, context, lastTableEnd, currentTable, currentBody);
  }
  map2.consume(context.events);
  index2 = -1;
  while (++index2 < context.events.length) {
    const event = context.events[index2];
    if (event[0] === "enter" && event[1].type === "table") {
      event[1]._align = gfmTableAlign(context.events, index2);
    }
  }
  return events;
}
function flushCell(map2, context, range, rowKind, rowEnd, previousCell) {
  const groupName = rowKind === 1 ? "tableHeader" : rowKind === 2 ? "tableDelimiter" : "tableData";
  const valueName = "tableContent";
  if (range[0] !== 0) {
    previousCell.end = Object.assign({}, getPoint(context.events, range[0]));
    map2.add(range[0], 0, [["exit", previousCell, context]]);
  }
  const now = getPoint(context.events, range[1]);
  previousCell = {
    type: groupName,
    start: Object.assign({}, now),
    // Note: correct end is set later.
    end: Object.assign({}, now)
  };
  map2.add(range[1], 0, [["enter", previousCell, context]]);
  if (range[2] !== 0) {
    const relatedStart = getPoint(context.events, range[2]);
    const relatedEnd = getPoint(context.events, range[3]);
    const valueToken = {
      type: valueName,
      start: Object.assign({}, relatedStart),
      end: Object.assign({}, relatedEnd)
    };
    map2.add(range[2], 0, [["enter", valueToken, context]]);
    if (rowKind !== 2) {
      const start = context.events[range[2]];
      const end = context.events[range[3]];
      start[1].end = Object.assign({}, end[1].end);
      start[1].type = "chunkText";
      start[1].contentType = "text";
      if (range[3] > range[2] + 1) {
        const a = range[2] + 1;
        const b = range[3] - range[2] - 1;
        map2.add(a, b, []);
      }
    }
    map2.add(range[3] + 1, 0, [["exit", valueToken, context]]);
  }
  if (rowEnd !== void 0) {
    previousCell.end = Object.assign({}, getPoint(context.events, rowEnd));
    map2.add(rowEnd, 0, [["exit", previousCell, context]]);
    previousCell = void 0;
  }
  return previousCell;
}
function flushTableEnd(map2, context, index2, table2, tableBody) {
  const exits = [];
  const related = getPoint(context.events, index2);
  if (tableBody) {
    tableBody.end = Object.assign({}, related);
    exits.push(["exit", tableBody, context]);
  }
  table2.end = Object.assign({}, related);
  exits.push(["exit", table2, context]);
  map2.add(index2 + 1, 0, exits);
}
function getPoint(events, index2) {
  const event = events[index2];
  const side = event[0] === "enter" ? "start" : "end";
  return event[1][side];
}
const tasklistCheck = {
  name: "tasklistCheck",
  tokenize: tokenizeTasklistCheck
};
function gfmTaskListItem() {
  return {
    text: {
      [91]: tasklistCheck
    }
  };
}
function tokenizeTasklistCheck(effects, ok2, nok) {
  const self2 = this;
  return open;
  function open(code2) {
    if (
      // Exit if there’s stuff before.
      self2.previous !== null || // Exit if not in the first content that is the first child of a list
      // item.
      !self2._gfmTasklistFirstContentOfListItem
    ) {
      return nok(code2);
    }
    effects.enter("taskListCheck");
    effects.enter("taskListCheckMarker");
    effects.consume(code2);
    effects.exit("taskListCheckMarker");
    return inside;
  }
  function inside(code2) {
    if (markdownLineEndingOrSpace(code2)) {
      effects.enter("taskListCheckValueUnchecked");
      effects.consume(code2);
      effects.exit("taskListCheckValueUnchecked");
      return close;
    }
    if (code2 === 88 || code2 === 120) {
      effects.enter("taskListCheckValueChecked");
      effects.consume(code2);
      effects.exit("taskListCheckValueChecked");
      return close;
    }
    return nok(code2);
  }
  function close(code2) {
    if (code2 === 93) {
      effects.enter("taskListCheckMarker");
      effects.consume(code2);
      effects.exit("taskListCheckMarker");
      effects.exit("taskListCheck");
      return after;
    }
    return nok(code2);
  }
  function after(code2) {
    if (markdownLineEnding(code2)) {
      return ok2(code2);
    }
    if (markdownSpace(code2)) {
      return effects.check({
        tokenize: spaceThenNonSpace
      }, ok2, nok)(code2);
    }
    return nok(code2);
  }
}
function spaceThenNonSpace(effects, ok2, nok) {
  return factorySpace(effects, after, "whitespace");
  function after(code2) {
    return code2 === null ? nok(code2) : ok2(code2);
  }
}
function gfm(options) {
  return combineExtensions([
    gfmAutolinkLiteral(),
    gfmFootnote(),
    gfmStrikethrough(options),
    gfmTable(),
    gfmTaskListItem()
  ]);
}
const emptyOptions = {};
function remarkGfm(options) {
  const self2 = (
    /** @type {Processor<Root>} */
    this
  );
  const settings = options || emptyOptions;
  const data = self2.data();
  const micromarkExtensions = data.micromarkExtensions || (data.micromarkExtensions = []);
  const fromMarkdownExtensions = data.fromMarkdownExtensions || (data.fromMarkdownExtensions = []);
  const toMarkdownExtensions = data.toMarkdownExtensions || (data.toMarkdownExtensions = []);
  micromarkExtensions.push(gfm(settings));
  fromMarkdownExtensions.push(gfmFromMarkdown());
  toMarkdownExtensions.push(gfmToMarkdown(settings));
}
function clamp(value, [min, max]) {
  return Math.min(max, Math.max(min, value));
}
var DirectionContext = reactExports.createContext(void 0);
function useDirection(localDir) {
  const globalDir = reactExports.useContext(DirectionContext);
  return localDir || globalDir || "ltr";
}
function usePrevious(value) {
  const ref = reactExports.useRef({ value, previous: value });
  return reactExports.useMemo(() => {
    if (ref.current.value !== value) {
      ref.current.previous = ref.current.value;
      ref.current.value = value;
    }
    return ref.current.previous;
  }, [value]);
}
var PAGE_KEYS = ["PageUp", "PageDown"];
var ARROW_KEYS = ["ArrowUp", "ArrowDown", "ArrowLeft", "ArrowRight"];
var BACK_KEYS = {
  "from-left": ["Home", "PageDown", "ArrowDown", "ArrowLeft"],
  "from-right": ["Home", "PageDown", "ArrowDown", "ArrowRight"],
  "from-bottom": ["Home", "PageDown", "ArrowDown", "ArrowLeft"],
  "from-top": ["Home", "PageDown", "ArrowUp", "ArrowLeft"]
};
var SLIDER_NAME = "Slider";
var [Collection, useCollection, createCollectionScope] = createCollection(SLIDER_NAME);
var [createSliderContext] = createContextScope(SLIDER_NAME, [
  createCollectionScope
]);
var [SliderProvider, useSliderContext] = createSliderContext(SLIDER_NAME);
var Slider$1 = reactExports.forwardRef(
  (props, forwardedRef) => {
    const {
      name: name2,
      min = 0,
      max = 100,
      step = 1,
      orientation = "horizontal",
      disabled = false,
      minStepsBetweenThumbs = 0,
      defaultValue = [min],
      value,
      onValueChange = () => {
      },
      onValueCommit = () => {
      },
      inverted = false,
      form,
      ...sliderProps
    } = props;
    const thumbRefs = reactExports.useRef(/* @__PURE__ */ new Set());
    const valueIndexToChangeRef = reactExports.useRef(0);
    const isHorizontal = orientation === "horizontal";
    const SliderOrientation = isHorizontal ? SliderHorizontal : SliderVertical;
    const [values = [], setValues] = useControllableState({
      prop: value,
      defaultProp: defaultValue,
      onChange: (value2) => {
        const thumbs = [...thumbRefs.current];
        thumbs[valueIndexToChangeRef.current]?.focus();
        onValueChange(value2);
      }
    });
    const valuesBeforeSlideStartRef = reactExports.useRef(values);
    function handleSlideStart(value2) {
      const closestIndex = getClosestValueIndex(values, value2);
      updateValues(value2, closestIndex);
    }
    function handleSlideMove(value2) {
      updateValues(value2, valueIndexToChangeRef.current);
    }
    function handleSlideEnd() {
      const prevValue = valuesBeforeSlideStartRef.current[valueIndexToChangeRef.current];
      const nextValue = values[valueIndexToChangeRef.current];
      const hasChanged = nextValue !== prevValue;
      if (hasChanged) onValueCommit(values);
    }
    function updateValues(value2, atIndex, { commit } = { commit: false }) {
      const decimalCount = getDecimalCount(step);
      const snapToStep = roundValue(Math.round((value2 - min) / step) * step + min, decimalCount);
      const nextValue = clamp(snapToStep, [min, max]);
      setValues((prevValues = []) => {
        const nextValues = getNextSortedValues(prevValues, nextValue, atIndex);
        if (hasMinStepsBetweenValues(nextValues, minStepsBetweenThumbs * step)) {
          valueIndexToChangeRef.current = nextValues.indexOf(nextValue);
          const hasChanged = String(nextValues) !== String(prevValues);
          if (hasChanged && commit) onValueCommit(nextValues);
          return hasChanged ? nextValues : prevValues;
        } else {
          return prevValues;
        }
      });
    }
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      SliderProvider,
      {
        scope: props.__scopeSlider,
        name: name2,
        disabled,
        min,
        max,
        valueIndexToChangeRef,
        thumbs: thumbRefs.current,
        values,
        orientation,
        form,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(Collection.Provider, { scope: props.__scopeSlider, children: /* @__PURE__ */ jsxRuntimeExports.jsx(Collection.Slot, { scope: props.__scopeSlider, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          SliderOrientation,
          {
            "aria-disabled": disabled,
            "data-disabled": disabled ? "" : void 0,
            ...sliderProps,
            ref: forwardedRef,
            onPointerDown: composeEventHandlers(sliderProps.onPointerDown, () => {
              if (!disabled) valuesBeforeSlideStartRef.current = values;
            }),
            min,
            max,
            inverted,
            onSlideStart: disabled ? void 0 : handleSlideStart,
            onSlideMove: disabled ? void 0 : handleSlideMove,
            onSlideEnd: disabled ? void 0 : handleSlideEnd,
            onHomeKeyDown: () => !disabled && updateValues(min, 0, { commit: true }),
            onEndKeyDown: () => !disabled && updateValues(max, values.length - 1, { commit: true }),
            onStepKeyDown: ({ event, direction: stepDirection }) => {
              if (!disabled) {
                const isPageKey = PAGE_KEYS.includes(event.key);
                const isSkipKey = isPageKey || event.shiftKey && ARROW_KEYS.includes(event.key);
                const multiplier = isSkipKey ? 10 : 1;
                const atIndex = valueIndexToChangeRef.current;
                const value2 = values[atIndex];
                const stepInDirection = step * multiplier * stepDirection;
                updateValues(value2 + stepInDirection, atIndex, { commit: true });
              }
            }
          }
        ) }) })
      }
    );
  }
);
Slider$1.displayName = SLIDER_NAME;
var [SliderOrientationProvider, useSliderOrientationContext] = createSliderContext(SLIDER_NAME, {
  startEdge: "left",
  endEdge: "right",
  size: "width",
  direction: 1
});
var SliderHorizontal = reactExports.forwardRef(
  (props, forwardedRef) => {
    const {
      min,
      max,
      dir,
      inverted,
      onSlideStart,
      onSlideMove,
      onSlideEnd,
      onStepKeyDown,
      ...sliderProps
    } = props;
    const [slider, setSlider] = reactExports.useState(null);
    const composedRefs = useComposedRefs(forwardedRef, (node2) => setSlider(node2));
    const rectRef = reactExports.useRef(void 0);
    const direction = useDirection(dir);
    const isDirectionLTR = direction === "ltr";
    const isSlidingFromLeft = isDirectionLTR && !inverted || !isDirectionLTR && inverted;
    function getValueFromPointer(pointerPosition) {
      const rect = rectRef.current || slider.getBoundingClientRect();
      const input = [0, rect.width];
      const output = isSlidingFromLeft ? [min, max] : [max, min];
      const value = linearScale(input, output);
      rectRef.current = rect;
      return value(pointerPosition - rect.left);
    }
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      SliderOrientationProvider,
      {
        scope: props.__scopeSlider,
        startEdge: isSlidingFromLeft ? "left" : "right",
        endEdge: isSlidingFromLeft ? "right" : "left",
        direction: isSlidingFromLeft ? 1 : -1,
        size: "width",
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          SliderImpl,
          {
            dir: direction,
            "data-orientation": "horizontal",
            ...sliderProps,
            ref: composedRefs,
            style: {
              ...sliderProps.style,
              ["--radix-slider-thumb-transform"]: "translateX(-50%)"
            },
            onSlideStart: (event) => {
              const value = getValueFromPointer(event.clientX);
              onSlideStart?.(value);
            },
            onSlideMove: (event) => {
              const value = getValueFromPointer(event.clientX);
              onSlideMove?.(value);
            },
            onSlideEnd: () => {
              rectRef.current = void 0;
              onSlideEnd?.();
            },
            onStepKeyDown: (event) => {
              const slideDirection = isSlidingFromLeft ? "from-left" : "from-right";
              const isBackKey = BACK_KEYS[slideDirection].includes(event.key);
              onStepKeyDown?.({ event, direction: isBackKey ? -1 : 1 });
            }
          }
        )
      }
    );
  }
);
var SliderVertical = reactExports.forwardRef(
  (props, forwardedRef) => {
    const {
      min,
      max,
      inverted,
      onSlideStart,
      onSlideMove,
      onSlideEnd,
      onStepKeyDown,
      ...sliderProps
    } = props;
    const sliderRef = reactExports.useRef(null);
    const ref = useComposedRefs(forwardedRef, sliderRef);
    const rectRef = reactExports.useRef(void 0);
    const isSlidingFromBottom = !inverted;
    function getValueFromPointer(pointerPosition) {
      const rect = rectRef.current || sliderRef.current.getBoundingClientRect();
      const input = [0, rect.height];
      const output = isSlidingFromBottom ? [max, min] : [min, max];
      const value = linearScale(input, output);
      rectRef.current = rect;
      return value(pointerPosition - rect.top);
    }
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      SliderOrientationProvider,
      {
        scope: props.__scopeSlider,
        startEdge: isSlidingFromBottom ? "bottom" : "top",
        endEdge: isSlidingFromBottom ? "top" : "bottom",
        size: "height",
        direction: isSlidingFromBottom ? 1 : -1,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx(
          SliderImpl,
          {
            "data-orientation": "vertical",
            ...sliderProps,
            ref,
            style: {
              ...sliderProps.style,
              ["--radix-slider-thumb-transform"]: "translateY(50%)"
            },
            onSlideStart: (event) => {
              const value = getValueFromPointer(event.clientY);
              onSlideStart?.(value);
            },
            onSlideMove: (event) => {
              const value = getValueFromPointer(event.clientY);
              onSlideMove?.(value);
            },
            onSlideEnd: () => {
              rectRef.current = void 0;
              onSlideEnd?.();
            },
            onStepKeyDown: (event) => {
              const slideDirection = isSlidingFromBottom ? "from-bottom" : "from-top";
              const isBackKey = BACK_KEYS[slideDirection].includes(event.key);
              onStepKeyDown?.({ event, direction: isBackKey ? -1 : 1 });
            }
          }
        )
      }
    );
  }
);
var SliderImpl = reactExports.forwardRef(
  (props, forwardedRef) => {
    const {
      __scopeSlider,
      onSlideStart,
      onSlideMove,
      onSlideEnd,
      onHomeKeyDown,
      onEndKeyDown,
      onStepKeyDown,
      ...sliderProps
    } = props;
    const context = useSliderContext(SLIDER_NAME, __scopeSlider);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive.span,
      {
        ...sliderProps,
        ref: forwardedRef,
        onKeyDown: composeEventHandlers(props.onKeyDown, (event) => {
          if (event.key === "Home") {
            onHomeKeyDown(event);
            event.preventDefault();
          } else if (event.key === "End") {
            onEndKeyDown(event);
            event.preventDefault();
          } else if (PAGE_KEYS.concat(ARROW_KEYS).includes(event.key)) {
            onStepKeyDown(event);
            event.preventDefault();
          }
        }),
        onPointerDown: composeEventHandlers(props.onPointerDown, (event) => {
          const target = event.target;
          target.setPointerCapture(event.pointerId);
          event.preventDefault();
          if (context.thumbs.has(target)) {
            target.focus();
          } else {
            onSlideStart(event);
          }
        }),
        onPointerMove: composeEventHandlers(props.onPointerMove, (event) => {
          const target = event.target;
          if (target.hasPointerCapture(event.pointerId)) onSlideMove(event);
        }),
        onPointerUp: composeEventHandlers(props.onPointerUp, (event) => {
          const target = event.target;
          if (target.hasPointerCapture(event.pointerId)) {
            target.releasePointerCapture(event.pointerId);
            onSlideEnd(event);
          }
        })
      }
    );
  }
);
var TRACK_NAME = "SliderTrack";
var SliderTrack = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeSlider, ...trackProps } = props;
    const context = useSliderContext(TRACK_NAME, __scopeSlider);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive.span,
      {
        "data-disabled": context.disabled ? "" : void 0,
        "data-orientation": context.orientation,
        ...trackProps,
        ref: forwardedRef
      }
    );
  }
);
SliderTrack.displayName = TRACK_NAME;
var RANGE_NAME = "SliderRange";
var SliderRange = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeSlider, ...rangeProps } = props;
    const context = useSliderContext(RANGE_NAME, __scopeSlider);
    const orientation = useSliderOrientationContext(RANGE_NAME, __scopeSlider);
    const ref = reactExports.useRef(null);
    const composedRefs = useComposedRefs(forwardedRef, ref);
    const valuesCount = context.values.length;
    const percentages = context.values.map(
      (value) => convertValueToPercentage(value, context.min, context.max)
    );
    const offsetStart = valuesCount > 1 ? Math.min(...percentages) : 0;
    const offsetEnd = 100 - Math.max(...percentages);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive.span,
      {
        "data-orientation": context.orientation,
        "data-disabled": context.disabled ? "" : void 0,
        ...rangeProps,
        ref: composedRefs,
        style: {
          ...props.style,
          [orientation.startEdge]: offsetStart + "%",
          [orientation.endEdge]: offsetEnd + "%"
        }
      }
    );
  }
);
SliderRange.displayName = RANGE_NAME;
var THUMB_NAME = "SliderThumb";
var SliderThumb = reactExports.forwardRef(
  (props, forwardedRef) => {
    const getItems = useCollection(props.__scopeSlider);
    const [thumb, setThumb] = reactExports.useState(null);
    const composedRefs = useComposedRefs(forwardedRef, (node2) => setThumb(node2));
    const index2 = reactExports.useMemo(
      () => thumb ? getItems().findIndex((item) => item.ref.current === thumb) : -1,
      [getItems, thumb]
    );
    return /* @__PURE__ */ jsxRuntimeExports.jsx(SliderThumbImpl, { ...props, ref: composedRefs, index: index2 });
  }
);
var SliderThumbImpl = reactExports.forwardRef(
  (props, forwardedRef) => {
    const { __scopeSlider, index: index2, name: name2, ...thumbProps } = props;
    const context = useSliderContext(THUMB_NAME, __scopeSlider);
    const orientation = useSliderOrientationContext(THUMB_NAME, __scopeSlider);
    const [thumb, setThumb] = reactExports.useState(null);
    const composedRefs = useComposedRefs(forwardedRef, (node2) => setThumb(node2));
    const isFormControl = thumb ? context.form || !!thumb.closest("form") : true;
    const size = useSize(thumb);
    const value = context.values[index2];
    const percent = value === void 0 ? 0 : convertValueToPercentage(value, context.min, context.max);
    const label = getLabel(index2, context.values.length);
    const orientationSize = size?.[orientation.size];
    const thumbInBoundsOffset = orientationSize ? getThumbInBoundsOffset(orientationSize, percent, orientation.direction) : 0;
    reactExports.useEffect(() => {
      if (thumb) {
        context.thumbs.add(thumb);
        return () => {
          context.thumbs.delete(thumb);
        };
      }
    }, [thumb, context.thumbs]);
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "span",
      {
        style: {
          transform: "var(--radix-slider-thumb-transform)",
          position: "absolute",
          [orientation.startEdge]: `calc(${percent}% + ${thumbInBoundsOffset}px)`
        },
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Collection.ItemSlot, { scope: props.__scopeSlider, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
            Primitive.span,
            {
              role: "slider",
              "aria-label": props["aria-label"] || label,
              "aria-valuemin": context.min,
              "aria-valuenow": value,
              "aria-valuemax": context.max,
              "aria-orientation": context.orientation,
              "data-orientation": context.orientation,
              "data-disabled": context.disabled ? "" : void 0,
              tabIndex: context.disabled ? void 0 : 0,
              ...thumbProps,
              ref: composedRefs,
              style: value === void 0 ? { display: "none" } : props.style,
              onFocus: composeEventHandlers(props.onFocus, () => {
                context.valueIndexToChangeRef.current = index2;
              })
            }
          ) }),
          isFormControl && /* @__PURE__ */ jsxRuntimeExports.jsx(
            SliderBubbleInput,
            {
              name: name2 ?? (context.name ? context.name + (context.values.length > 1 ? "[]" : "") : void 0),
              form: context.form,
              value
            },
            index2
          )
        ]
      }
    );
  }
);
SliderThumb.displayName = THUMB_NAME;
var BUBBLE_INPUT_NAME = "RadioBubbleInput";
var SliderBubbleInput = reactExports.forwardRef(
  ({ __scopeSlider, value, ...props }, forwardedRef) => {
    const ref = reactExports.useRef(null);
    const composedRefs = useComposedRefs(ref, forwardedRef);
    const prevValue = usePrevious(value);
    reactExports.useEffect(() => {
      const input = ref.current;
      if (!input) return;
      const inputProto = window.HTMLInputElement.prototype;
      const descriptor = Object.getOwnPropertyDescriptor(inputProto, "value");
      const setValue = descriptor.set;
      if (prevValue !== value && setValue) {
        const event = new Event("input", { bubbles: true });
        setValue.call(input, value);
        input.dispatchEvent(event);
      }
    }, [prevValue, value]);
    return /* @__PURE__ */ jsxRuntimeExports.jsx(
      Primitive.input,
      {
        style: { display: "none" },
        ...props,
        ref: composedRefs,
        defaultValue: value
      }
    );
  }
);
SliderBubbleInput.displayName = BUBBLE_INPUT_NAME;
function getNextSortedValues(prevValues = [], nextValue, atIndex) {
  const nextValues = [...prevValues];
  nextValues[atIndex] = nextValue;
  return nextValues.sort((a, b) => a - b);
}
function convertValueToPercentage(value, min, max) {
  const maxSteps = max - min;
  const percentPerStep = 100 / maxSteps;
  const percentage = percentPerStep * (value - min);
  return clamp(percentage, [0, 100]);
}
function getLabel(index2, totalValues) {
  if (totalValues > 2) {
    return `Value ${index2 + 1} of ${totalValues}`;
  } else if (totalValues === 2) {
    return ["Minimum", "Maximum"][index2];
  } else {
    return void 0;
  }
}
function getClosestValueIndex(values, nextValue) {
  if (values.length === 1) return 0;
  const distances = values.map((value) => Math.abs(value - nextValue));
  const closestDistance = Math.min(...distances);
  return distances.indexOf(closestDistance);
}
function getThumbInBoundsOffset(width, left, direction) {
  const halfWidth = width / 2;
  const halfPercent = 50;
  const offset = linearScale([0, halfPercent], [0, halfWidth]);
  return (halfWidth - offset(left) * direction) * direction;
}
function getStepsBetweenValues(values) {
  return values.slice(0, -1).map((value, index2) => values[index2 + 1] - value);
}
function hasMinStepsBetweenValues(values, minStepsBetweenValues) {
  if (minStepsBetweenValues > 0) {
    const stepsBetweenValues = getStepsBetweenValues(values);
    const actualMinStepsBetweenValues = Math.min(...stepsBetweenValues);
    return actualMinStepsBetweenValues >= minStepsBetweenValues;
  }
  return true;
}
function linearScale(input, output) {
  return (value) => {
    if (input[0] === input[1] || output[0] === output[1]) return output[0];
    const ratio = (output[1] - output[0]) / (input[1] - input[0]);
    return output[0] + ratio * (value - input[0]);
  };
}
function getDecimalCount(value) {
  return (String(value).split(".")[1] || "").length;
}
function roundValue(value, decimalCount) {
  const rounder = Math.pow(10, decimalCount);
  return Math.round(value * rounder) / rounder;
}
var Root = Slider$1;
var Track = SliderTrack;
var Range = SliderRange;
var Thumb = SliderThumb;
const Slider = reactExports.forwardRef(({ className, ...props }, ref) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
  Root,
  {
    ref,
    className: cn(
      "relative flex w-full touch-none select-none items-center",
      className
    ),
    ...props,
    children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Track, { className: "relative h-1.5 w-full grow overflow-hidden rounded-full bg-primary/20", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Range, { className: "absolute h-full bg-primary" }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Thumb, { className: "block h-4 w-4 rounded-full border border-primary/50 bg-background shadow transition-colors focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring disabled:pointer-events-none disabled:opacity-50" })
    ]
  }
));
Slider.displayName = Root.displayName;
function GraphWidget({
  functions: initialFunctions,
  xRange: initialXRange = [-10, 10],
  yRange: initialYRange = [-6, 6],
  sliders: initialSliders = []
}) {
  const canvasRef = reactExports.useRef(null);
  const containerRef = reactExports.useRef(null);
  const [functions, setFunctions] = reactExports.useState(() => {
    let rawFuncs = [];
    if (Array.isArray(initialFunctions)) {
      rawFuncs = initialFunctions;
    } else if (initialFunctions !== void 0 && initialFunctions !== null) {
      rawFuncs = [initialFunctions];
    } else {
      rawFuncs = ["sin(x)"];
    }
    return rawFuncs.map((f) => f !== null && f !== void 0 ? String(f).trim() : "").filter((f) => f.length > 0);
  });
  const [newFuncInput, setNewFuncInput] = reactExports.useState("");
  const [xRange, setXRange] = reactExports.useState(initialXRange);
  const [yRange, setYRange] = reactExports.useState(initialYRange);
  const [isClosed, setIsClosed] = reactExports.useState(false);
  const [isCopied, setIsCopied] = reactExports.useState(false);
  const [isLiked, setIsLiked] = reactExports.useState(null);
  const [showEquationPanel, setShowEquationPanel] = reactExports.useState(true);
  const [resizeCount, setResizeCount] = reactExports.useState(0);
  reactExports.useEffect(() => {
    const handleResize = () => setResizeCount((prev) => prev + 1);
    window.addEventListener("resize", handleResize);
    return () => window.removeEventListener("resize", handleResize);
  }, []);
  const [sliderVals, setSliderVals] = reactExports.useState(() => {
    const vals = {};
    initialSliders.forEach((s) => {
      vals[s.name] = s.defaultValue;
    });
    return vals;
  });
  const [mousePos, setMousePos] = reactExports.useState(null);
  const [isPanning, setIsPanning] = reactExports.useState(false);
  const panStart = reactExports.useRef({
    x: 0,
    y: 0,
    xRange: [...initialXRange],
    yRange: [...initialYRange]
  });
  const parseAndEval = (expr, xVal, sVals, yVal) => {
    let cleaned = expr.toLowerCase();
    cleaned = cleaned.replace(/\\/g, "");
    cleaned = cleaned.replace(/^y\s*=\s*/, "");
    cleaned = cleaned.replace(/(\d+)([a-z(θ])/g, "$1*$2");
    cleaned = cleaned.replace(/\)\(/g, ")*(");
    const mathFuncs = ["sin", "cos", "tan", "cot", "sec", "csc", "log", "ln", "sqrt"];
    mathFuncs.forEach((func) => {
      const regexSpace = new RegExp(`\\b${func}\\s+([a-z0-9_().**+\\-/^θ]+)`, "g");
      cleaned = cleaned.replace(regexSpace, `${func}($1)`);
      const regexVar = new RegExp(`\\b${func}([xθt]|theta)\\b`, "g");
      cleaned = cleaned.replace(regexVar, `${func}($1)`);
    });
    Object.entries(sVals).forEach(([name2, val]) => {
      const regex = new RegExp(`\\b${name2}\\b`, "g");
      cleaned = cleaned.replace(regex, `(${val})`);
    });
    cleaned = cleaned.replace(/\bpi\b/g, "Math.PI");
    cleaned = cleaned.replace(/\be\b/g, "Math.E");
    cleaned = cleaned.replace(/sin\(/g, "Math.sin(");
    cleaned = cleaned.replace(/cos\(/g, "Math.cos(");
    cleaned = cleaned.replace(/tan\(/g, "Math.tan(");
    cleaned = cleaned.replace(/cot\(/g, "1/Math.tan(");
    cleaned = cleaned.replace(/sec\(/g, "1/Math.cos(");
    cleaned = cleaned.replace(/csc\(/g, "1/Math.sin(");
    cleaned = cleaned.replace(/log\(/g, "Math.log10(");
    cleaned = cleaned.replace(/ln\(/g, "Math.log(");
    cleaned = cleaned.replace(/exp\(/g, "Math.exp(");
    cleaned = cleaned.replace(/sqrt\(/g, "Math.sqrt(");
    cleaned = cleaned.replace(/abs\(/g, "Math.abs(");
    cleaned = cleaned.replace(/asin\(/g, "Math.asin(");
    cleaned = cleaned.replace(/acos\(/g, "Math.acos(");
    cleaned = cleaned.replace(/atan\(/g, "Math.atan(");
    cleaned = cleaned.replace(/\^/g, "**");
    cleaned = cleaned.replace(/\bx\b/g, `(${xVal})`);
    cleaned = cleaned.replace(/\bt\b/g, `(${xVal})`);
    cleaned = cleaned.replace(/\btheta\b/g, `(${xVal})`);
    cleaned = cleaned.replace(/θ/g, `(${xVal})`);
    if (typeof yVal === "number") {
      cleaned = cleaned.replace(/\by\b/g, `(${yVal})`);
    }
    try {
      const fn = new Function(`return ${cleaned};`);
      const res = fn();
      return typeof res === "number" && !isNaN(res) && isFinite(res) ? res : NaN;
    } catch (e) {
      return NaN;
    }
  };
  const handleZoom = (factor) => {
    const xMid = (xRange[0] + xRange[1]) / 2;
    const xSpan = (xRange[1] - xRange[0]) * factor;
    const yMid = (yRange[0] + yRange[1]) / 2;
    const ySpan = (yRange[1] - yRange[0]) * factor;
    setXRange([xMid - xSpan / 2, xMid + xSpan / 2]);
    setYRange([yMid - ySpan / 2, yMid + ySpan / 2]);
  };
  const handleReset = () => {
    setXRange(initialXRange);
    setYRange(initialYRange);
    const resetVals = {};
    initialSliders.forEach((s) => {
      resetVals[s.name] = s.defaultValue;
    });
    setSliderVals(resetVals);
  };
  const handleMouseDown = (e) => {
    if (e.button !== 0) return;
    const canvas = canvasRef.current;
    if (!canvas) return;
    setIsPanning(true);
    const rect = canvas.getBoundingClientRect();
    panStart.current = {
      x: e.clientX - rect.left,
      y: e.clientY - rect.top,
      xRange: [...xRange],
      yRange: [...yRange]
    };
  };
  const handleMouseMove = (e) => {
    const canvas = canvasRef.current;
    if (!canvas) return;
    const rect = canvas.getBoundingClientRect();
    const x = e.clientX - rect.left;
    const y = e.clientY - rect.top;
    const width = canvas.width;
    const height = canvas.height;
    const graphX = xRange[0] + x / width * (xRange[1] - xRange[0]);
    const graphY = yRange[1] - y / height * (yRange[1] - yRange[0]);
    setMousePos({ x, y, graphX, graphY });
    if (!isPanning) return;
    const dx = x - panStart.current.x;
    const dy = y - panStart.current.y;
    const xDiff = dx / width * (panStart.current.xRange[1] - panStart.current.xRange[0]);
    const yDiff = dy / height * (panStart.current.yRange[1] - panStart.current.yRange[0]);
    setXRange([panStart.current.xRange[0] - xDiff, panStart.current.xRange[1] - xDiff]);
    setYRange([panStart.current.yRange[0] + yDiff, panStart.current.yRange[1] + yDiff]);
  };
  const handleMouseUp = () => {
    setIsPanning(false);
  };
  reactExports.useEffect(() => {
    const canvas = canvasRef.current;
    if (!canvas) return;
    const ctx = canvas.getContext("2d");
    if (!ctx) return;
    const dpr = window.devicePixelRatio || 1;
    const rect = canvas.getBoundingClientRect();
    canvas.width = rect.width * dpr;
    canvas.height = rect.height * dpr;
    ctx.scale(dpr, dpr);
    const width = rect.width;
    const height = rect.height;
    const toScreenX = (x) => (x - xRange[0]) / (xRange[1] - xRange[0]) * width;
    const toScreenY = (y) => height - (y - yRange[0]) / (yRange[1] - yRange[0]) * height;
    ctx.fillStyle = "#09090b";
    ctx.fillRect(0, 0, width, height);
    ctx.strokeStyle = "#1e1e24";
    ctx.lineWidth = 0.5;
    const zeroX = (0 - xRange[0]) / (xRange[1] - xRange[0]) * width;
    const zeroY = height - (0 - yRange[0]) / (yRange[1] - yRange[0]) * height;
    const xDiff = xRange[1] - xRange[0];
    let gridSpacing = 1;
    if (xDiff > 100) gridSpacing = 20;
    else if (xDiff > 50) gridSpacing = 10;
    else if (xDiff > 20) gridSpacing = 5;
    else if (xDiff > 5) gridSpacing = 1;
    else if (xDiff > 1) gridSpacing = 0.2;
    else gridSpacing = 0.05;
    const startGridX = Math.floor(xRange[0] / gridSpacing) * gridSpacing;
    for (let gx = startGridX; gx <= xRange[1]; gx += gridSpacing) {
      const px = (gx - xRange[0]) / (xRange[1] - xRange[0]) * width;
      ctx.beginPath();
      ctx.moveTo(px, 0);
      ctx.lineTo(px, height);
      ctx.stroke();
      if (Math.abs(gx) > 1e-4) {
        ctx.fillStyle = "#71717a";
        ctx.font = "10px sans-serif";
        ctx.textAlign = "center";
        const labelY = zeroY > height - 15 ? height - 5 : zeroY < 15 ? 12 : zeroY + 14;
        ctx.fillText(gx.toFixed(gridSpacing < 1 ? 2 : 0), px, labelY);
      }
    }
    const startGridY = Math.floor(yRange[0] / gridSpacing) * gridSpacing;
    for (let gy = startGridY; gy <= yRange[1]; gy += gridSpacing) {
      const py = height - (gy - yRange[0]) / (yRange[1] - yRange[0]) * height;
      ctx.beginPath();
      ctx.moveTo(0, py);
      ctx.lineTo(width, py);
      ctx.stroke();
      if (Math.abs(gy) > 1e-4) {
        ctx.fillStyle = "#71717a";
        ctx.font = "10px sans-serif";
        ctx.textAlign = "right";
        const labelX = zeroX < 20 ? 25 : zeroX > width - 10 ? width - 12 : zeroX - 6;
        ctx.fillText(gy.toFixed(gridSpacing < 1 ? 2 : 0), labelX, py + 3);
      }
    }
    ctx.strokeStyle = "#3f3f46";
    ctx.lineWidth = 1.2;
    if (zeroY >= 0 && zeroY <= height) {
      ctx.beginPath();
      ctx.moveTo(0, zeroY);
      ctx.lineTo(width, zeroY);
      ctx.stroke();
    }
    if (zeroX >= 0 && zeroX <= width) {
      ctx.beginPath();
      ctx.moveTo(zeroX, 0);
      ctx.lineTo(zeroX, height);
      ctx.stroke();
    }
    if (zeroX >= 0 && zeroX <= width && zeroY >= 0 && zeroY <= height) {
      ctx.fillStyle = "#71717a";
      ctx.fillText("0", zeroX - 6, zeroY + 12);
    }
    const funcColors = ["#00f0ff", "#38bdf8", "#06b6d4", "#60a5fa"];
    const convertFrac = (str) => {
      let s = str;
      let index2;
      const findMatchingBrace = (tempStr, startIdx) => {
        let depth = 1;
        for (let i = startIdx; i < tempStr.length; i++) {
          if (tempStr[i] === "{") depth++;
          else if (tempStr[i] === "}") depth--;
          if (depth === 0) return i;
        }
        return -1;
      };
      while ((index2 = s.indexOf("\\frac{")) !== -1) {
        let numStart = index2 + 6;
        let numEnd = findMatchingBrace(s, numStart);
        if (numEnd === -1) break;
        let numerator = s.substring(numStart, numEnd);
        let denStart = s.indexOf("{", numEnd + 1);
        if (denStart === -1) break;
        let denEnd = findMatchingBrace(s, denStart + 1);
        if (denEnd === -1) break;
        let denominator = s.substring(denStart + 1, denEnd);
        s = s.substring(0, index2) + `((${numerator})/(${denominator}))` + s.substring(denEnd + 1);
      }
      return s;
    };
    const cleanLatex = (str) => {
      let s = str.trim();
      if (s.startsWith("{") && s.endsWith("}")) {
        s = s.substring(1, s.length - 1).trim();
      }
      s = s.replace(/\\cdot/g, "*");
      s = s.replace(/\\left\(/g, "(").replace(/\\right\)/g, ")");
      s = s.replace(/\\left\[/g, "[").replace(/\\right\]/g, ")");
      s = s.replace(/\\left\\{/g, "{").replace(/\\right\\}/g, "}");
      s = s.replace(/\\div/g, "/");
      s = s.replace(/\\times/g, "*");
      s = convertFrac(s);
      s = s.replace(/\\(sin|cos|tan|cot|sec|csc|ln|log|sqrt|pi|theta|alpha|beta|gamma|phi|omega|t|x|y|r)/g, "$1");
      return s;
    };
    const parseConstExpr = (str) => {
      let cleaned = str.toLowerCase().replace(/\\/g, "").replace(/\bpi\b/g, "Math.PI").replace(/\be\b/g, "Math.E").replace(/\^/g, "**");
      try {
        const fn = new Function(`return ${cleaned};`);
        return fn();
      } catch (e) {
        return NaN;
      }
    };
    functions.forEach((rawExpr, fIdx) => {
      if (!rawExpr || typeof rawExpr !== "string" || !rawExpr.trim()) return;
      ctx.strokeStyle = funcColors[fIdx % funcColors.length];
      ctx.lineWidth = 2.5;
      let cleanExpr = cleanLatex(rawExpr);
      let customRange = null;
      const rangeMatch = cleanExpr.match(/,\s*([^,<=≤]+)\s*(?:<=|\\leq|≤)\s*([^,<=≤]+)\s*(?:<=|\\leq|≤)\s*([^,]+)/i);
      if (rangeMatch) {
        const lowVal = parseConstExpr(rangeMatch[1]);
        const highVal = parseConstExpr(rangeMatch[3]);
        if (!isNaN(lowVal) && !isNaN(highVal)) {
          customRange = [lowVal, highVal];
        }
      }
      const commaIdx = cleanExpr.indexOf(",");
      if (commaIdx !== -1) {
        cleanExpr = cleanExpr.substring(0, commaIdx).trim();
      }
      const isParametric = cleanExpr.includes("x=") && cleanExpr.includes("y=") && (cleanExpr.includes("t") || cleanExpr.includes("	"));
      const isPolar = cleanExpr.startsWith("r=") || cleanExpr.includes("theta") || cleanExpr.includes("θ");
      const isImplicit = !isParametric && !isPolar && cleanExpr.includes("y") && cleanExpr.includes("=");
      if (isParametric) {
        let xExpr = "";
        let yExpr = "";
        const xMatch = cleanExpr.match(/x\s*=\s*([^,;]+)/);
        const yMatch = cleanExpr.match(/y\s*=\s*([^,;]+)/);
        if (xMatch && yMatch) {
          xExpr = xMatch[1].trim();
          yExpr = yMatch[1].trim();
        } else {
          const parts = cleanExpr.split(/[;]+/);
          if (parts.length >= 2) {
            xExpr = parts[0].replace(/^x\s*=\s*/, "").trim();
            yExpr = parts[1].replace(/^y\s*=\s*/, "").trim();
          }
        }
        if (xExpr && yExpr) {
          const tMin = customRange ? customRange[0] : 0;
          const tMax = customRange ? customRange[1] : 2 * Math.PI;
          const steps = 1e3;
          ctx.beginPath();
          let isFirst = true;
          const pointsList = [];
          for (let i = 0; i <= steps; i++) {
            const t = tMin + i / steps * (tMax - tMin);
            const xVal = parseAndEval(xExpr, t, sliderVals);
            const yVal = parseAndEval(yExpr, t, sliderVals);
            if (!isNaN(xVal) && !isNaN(yVal)) {
              const px = toScreenX(xVal);
              const py = toScreenY(yVal);
              if (px >= -100 && px <= width + 100 && py >= -100 && py <= height + 100) {
                if (isFirst) {
                  ctx.moveTo(px, py);
                  isFirst = false;
                } else {
                  ctx.lineTo(px, py);
                }
                pointsList.push({ x: px, y: py });
              } else {
                isFirst = true;
              }
            } else {
              isFirst = true;
            }
          }
          ctx.stroke();
          ctx.fillStyle = funcColors[fIdx % funcColors.length];
          const nodeInterval = 40;
          pointsList.forEach((pt, ptIdx) => {
            if (ptIdx % nodeInterval === 0) {
              ctx.beginPath();
              ctx.arc(pt.x, pt.y, 3.5, 0, Math.PI * 2);
              ctx.fill();
              ctx.strokeStyle = "#ffffff";
              ctx.lineWidth = 1;
              ctx.stroke();
            }
          });
        }
      } else if (isPolar) {
        let polarExpr = cleanExpr;
        if (polarExpr.startsWith("r=")) {
          polarExpr = polarExpr.substring(2).trim();
        }
        const thetaMin = customRange ? customRange[0] : -Math.PI;
        const thetaMax = customRange ? customRange[1] : Math.PI;
        const steps = 1200;
        ctx.beginPath();
        let isFirst = true;
        const pointsList = [];
        for (let i = 0; i <= steps; i++) {
          const theta = thetaMin + i / steps * (thetaMax - thetaMin);
          const rVal = parseAndEval(polarExpr, theta, sliderVals);
          if (!isNaN(rVal)) {
            const xVal = rVal * Math.cos(theta);
            const yVal = rVal * Math.sin(theta);
            const px = toScreenX(xVal);
            const py = toScreenY(yVal);
            if (px >= -100 && px <= width + 100 && py >= -100 && py <= height + 100) {
              if (isFirst) {
                ctx.moveTo(px, py);
                isFirst = false;
              } else {
                ctx.lineTo(px, py);
              }
              pointsList.push({ x: px, y: py });
            } else {
              isFirst = true;
            }
          } else {
            isFirst = true;
          }
        }
        ctx.stroke();
        ctx.fillStyle = funcColors[fIdx % funcColors.length];
        const nodeInterval = 40;
        pointsList.forEach((pt, ptIdx) => {
          if (ptIdx % nodeInterval === 0) {
            ctx.beginPath();
            ctx.arc(pt.x, pt.y, 3.5, 0, Math.PI * 2);
            ctx.fill();
            ctx.strokeStyle = "#ffffff";
            ctx.lineWidth = 1;
            ctx.stroke();
          }
        });
      } else if (isImplicit) {
        const gridCols = 85;
        const gridRows = 85;
        const grid = [];
        for (let i = 0; i <= gridCols; i++) {
          grid[i] = [];
          const gx = xRange[0] + i / gridCols * (xRange[1] - xRange[0]);
          for (let j = 0; j <= gridRows; j++) {
            const gy = yRange[0] + j / gridRows * (yRange[1] - yRange[0]);
            let val = NaN;
            if (cleanExpr.includes("=")) {
              const sides = cleanExpr.split("=");
              const lhs = parseAndEval(sides[0], gx, sliderVals, gy);
              const rhs = parseAndEval(sides[1], gx, sliderVals, gy);
              val = lhs - rhs;
            } else {
              val = parseAndEval(cleanExpr, gx, sliderVals, gy);
            }
            grid[i][j] = val;
          }
        }
        for (let i = 0; i < gridCols; i++) {
          const gx1 = xRange[0] + i / gridCols * (xRange[1] - xRange[0]);
          const gx2 = xRange[0] + (i + 1) / gridCols * (xRange[1] - xRange[0]);
          for (let j = 0; j < gridRows; j++) {
            const gy1 = yRange[0] + j / gridRows * (yRange[1] - yRange[0]);
            const gy2 = yRange[0] + (j + 1) / gridRows * (yRange[1] - yRange[0]);
            const v1 = grid[i][j + 1];
            const v2 = grid[i + 1][j + 1];
            const v3 = grid[i][j];
            const v4 = grid[i + 1][j];
            const pts = [];
            if (!isNaN(v1) && !isNaN(v2) && v1 * v2 < 0) {
              pts.push({ x: gx1 + (gx2 - gx1) * (v1 / (v1 - v2)), y: gy2 });
            }
            if (!isNaN(v3) && !isNaN(v4) && v3 * v4 < 0) {
              pts.push({ x: gx1 + (gx2 - gx1) * (v3 / (v3 - v4)), y: gy1 });
            }
            if (!isNaN(v3) && !isNaN(v1) && v3 * v1 < 0) {
              pts.push({ x: gx1, y: gy1 + (gy2 - gy1) * (v3 / (v3 - v1)) });
            }
            if (!isNaN(v4) && !isNaN(v2) && v4 * v2 < 0) {
              pts.push({ x: gx2, y: gy1 + (gy2 - gy1) * (v4 / (v4 - v2)) });
            }
            if (pts.length >= 2) {
              ctx.beginPath();
              ctx.moveTo(toScreenX(pts[0].x), toScreenY(pts[0].y));
              ctx.lineTo(toScreenX(pts[1].x), toScreenY(pts[1].y));
              ctx.stroke();
              if ((i + j) % 6 === 0) {
                ctx.fillStyle = funcColors[fIdx % funcColors.length];
                ctx.beginPath();
                ctx.arc(toScreenX(pts[0].x), toScreenY(pts[0].y), 3.5, 0, Math.PI * 2);
                ctx.fill();
                ctx.strokeStyle = "#ffffff";
                ctx.lineWidth = 1;
                ctx.stroke();
              }
            }
          }
        }
      } else {
        ctx.beginPath();
        let isFirst = true;
        const steps = width;
        const pointsList = [];
        for (let px = 0; px <= steps; px++) {
          const xVal = xRange[0] + px / width * (xRange[1] - xRange[0]);
          const yVal = parseAndEval(cleanExpr, xVal, sliderVals);
          if (!isNaN(yVal)) {
            const py = toScreenY(yVal);
            if (py >= -100 && py <= height + 100) {
              if (isFirst) {
                ctx.moveTo(px, py);
                isFirst = false;
              } else {
                ctx.lineTo(px, py);
              }
              pointsList.push({ x: px, y: py });
            } else {
              isFirst = true;
            }
          } else {
            isFirst = true;
          }
        }
        ctx.stroke();
        ctx.fillStyle = funcColors[fIdx % funcColors.length];
        const nodeInterval = 30;
        pointsList.forEach((pt, ptIdx) => {
          if (ptIdx % nodeInterval === 0) {
            ctx.beginPath();
            ctx.arc(pt.x, pt.y, 3.5, 0, Math.PI * 2);
            ctx.fill();
            ctx.strokeStyle = "#ffffff";
            ctx.lineWidth = 1;
            ctx.stroke();
          }
        });
      }
    });
    if (mousePos && mousePos.x >= 0 && mousePos.x <= width && mousePos.y >= 0 && mousePos.y <= height) {
      ctx.strokeStyle = "rgba(255, 255, 255, 0.25)";
      ctx.lineWidth = 1;
      ctx.setLineDash([4, 4]);
      ctx.beginPath();
      ctx.moveTo(mousePos.x, 0);
      ctx.lineTo(mousePos.x, height);
      ctx.moveTo(0, mousePos.y);
      ctx.lineTo(width, mousePos.y);
      ctx.stroke();
      ctx.setLineDash([]);
      functions.forEach((rawExpr, fIdx) => {
        if (!rawExpr || typeof rawExpr !== "string" || !rawExpr.trim()) return;
        let cleanExpr = cleanLatex(rawExpr);
        let customRange = null;
        const rangeMatch = cleanExpr.match(/,\s*([^,<=≤]+)\s*(?:<=|\\leq|≤)\s*([^,<=≤]+)\s*(?:<=|\\leq|≤)\s*([^,]+)/i);
        if (rangeMatch) {
          const lowVal = parseConstExpr(rangeMatch[1]);
          const highVal = parseConstExpr(rangeMatch[3]);
          if (!isNaN(lowVal) && !isNaN(highVal)) {
            customRange = [lowVal, highVal];
          }
        }
        const commaIdx = cleanExpr.indexOf(",");
        if (commaIdx !== -1) {
          cleanExpr = cleanExpr.substring(0, commaIdx).trim();
        }
        const isParametric = cleanExpr.includes("x=") && cleanExpr.includes("y=") && (cleanExpr.includes("t") || cleanExpr.includes("	"));
        const isPolar = cleanExpr.startsWith("r=") || cleanExpr.includes("theta") || cleanExpr.includes("θ");
        let bestPt = null;
        let minDist = Infinity;
        const checkPoint = (gX, gY) => {
          const px = toScreenX(gX);
          const py = toScreenY(gY);
          const dx = px - mousePos.x;
          const dy = py - mousePos.y;
          const dist = Math.sqrt(dx * dx + dy * dy);
          if (dist < minDist) {
            minDist = dist;
            bestPt = { x: px, y: py, graphX: gX, graphY: gY };
          }
        };
        if (isParametric) {
          let xExpr = "";
          let yExpr = "";
          const xMatch = cleanExpr.match(/x\s*=\s*([^,;]+)/);
          const yMatch = cleanExpr.match(/y\s*=\s*([^,;]+)/);
          if (xMatch && yMatch) {
            xExpr = xMatch[1].trim();
            yExpr = yMatch[1].trim();
          } else {
            const parts = cleanExpr.split(/[;]+/);
            if (parts.length >= 2) {
              xExpr = parts[0].replace(/^x\s*=\s*/, "").trim();
              yExpr = parts[1].replace(/^y\s*=\s*/, "").trim();
            }
          }
          if (xExpr && yExpr) {
            const tMin = customRange ? customRange[0] : 0;
            const tMax = customRange ? customRange[1] : 2 * Math.PI;
            for (let i = 0; i <= 200; i++) {
              const t = tMin + i / 200 * (tMax - tMin);
              const gX = parseAndEval(xExpr, t, sliderVals);
              const gY = parseAndEval(yExpr, t, sliderVals);
              if (!isNaN(gX) && !isNaN(gY)) {
                checkPoint(gX, gY);
              }
            }
          }
        } else if (isPolar) {
          let polarExpr = cleanExpr;
          if (polarExpr.startsWith("r=")) {
            polarExpr = polarExpr.substring(2).trim();
          }
          const thetaMin = customRange ? customRange[0] : -Math.PI;
          const thetaMax = customRange ? customRange[1] : Math.PI;
          for (let i = 0; i <= 200; i++) {
            const theta = thetaMin + i / 200 * (thetaMax - thetaMin);
            const rVal = parseAndEval(polarExpr, theta, sliderVals);
            if (!isNaN(rVal)) {
              checkPoint(rVal * Math.cos(theta), rVal * Math.sin(theta));
            }
          }
        } else {
          const xStep = (xRange[1] - xRange[0]) / width;
          for (let dx = -10; dx <= 10; dx++) {
            const gX = mousePos.graphX + dx * xStep;
            const gY = parseAndEval(cleanExpr, gX, sliderVals);
            if (!isNaN(gY)) {
              checkPoint(gX, gY);
            }
          }
        }
        if (bestPt && (minDist < 40 || !isPolar && !isParametric && minDist < 150)) {
          const pt = bestPt;
          ctx.fillStyle = funcColors[fIdx % funcColors.length];
          ctx.beginPath();
          ctx.arc(pt.x, pt.y, 5, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = "#ffffff";
          ctx.lineWidth = 1;
          ctx.stroke();
          ctx.fillStyle = "#ffffff";
          ctx.font = "bold 11px sans-serif";
          const coordText = `(${pt.graphX.toFixed(2)}, ${pt.graphY.toFixed(2)})`;
          const textWidth = ctx.measureText(coordText).width;
          ctx.fillStyle = "rgba(15, 23, 42, 0.85)";
          ctx.fillRect(pt.x + 8, pt.y - 18, textWidth + 12, 22);
          ctx.strokeStyle = funcColors[fIdx % funcColors.length];
          ctx.strokeRect(pt.x + 8, pt.y - 18, textWidth + 12, 22);
          ctx.fillStyle = "#ffffff";
          ctx.fillText(coordText, pt.x + 14, pt.y - 3);
        }
      });
    }
  }, [functions, xRange, yRange, sliderVals, mousePos, isPanning, showEquationPanel, resizeCount]);
  if (isClosed) return null;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { ref: containerRef, className: "w-full h-[320px] sm:h-[365px] bg-[#09090b] border border-zinc-800 rounded-2xl overflow-hidden shadow-2xl flex flex-col md:flex-row text-white font-sans my-4 relative", children: [
    showEquationPanel && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "w-full md:w-2/5 bg-[#18181b] p-4 flex flex-col justify-between relative border-b md:border-b-0 md:border-r border-zinc-800 animate-in fade-in slide-in-from-left duration-250", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between w-full", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            onClick: () => setShowEquationPanel(false),
            className: "p-1 hover:bg-zinc-800 rounded-lg text-zinc-400 hover:text-white transition-all",
            title: "Hide Equations",
            children: /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "h-4 w-4" })
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-1.5", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "button",
            {
              onClick: () => {
                navigator.clipboard.writeText(functions[0] || "");
                setIsCopied(true);
                setTimeout(() => setIsCopied(false), 2e3);
              },
              className: "p-1.5 hover:bg-zinc-800 rounded-lg text-zinc-400 hover:text-white transition-all",
              title: "Copy Formula",
              children: isCopied ? /* @__PURE__ */ jsxRuntimeExports.jsx(Check, { className: "h-3.5 w-3.5 text-green-500" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Copy, { className: "h-3.5 w-3.5" })
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "button",
            {
              onClick: () => setIsLiked((prev) => prev === "like" ? null : "like"),
              className: cn(
                "p-1.5 hover:bg-zinc-800 rounded-lg transition-all",
                isLiked === "like" ? "text-green-400" : "text-zinc-400 hover:text-white"
              ),
              title: "Like Graph",
              children: /* @__PURE__ */ jsxRuntimeExports.jsx(ThumbsUp, { className: "h-3.5 w-3.5" })
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "button",
            {
              onClick: () => setIsLiked((prev) => prev === "dislike" ? null : "dislike"),
              className: cn(
                "p-1.5 hover:bg-zinc-800 rounded-lg transition-all",
                isLiked === "dislike" ? "text-red-400" : "text-zinc-400 hover:text-white"
              ),
              title: "Dislike Graph",
              children: /* @__PURE__ */ jsxRuntimeExports.jsx(ThumbsDown, { className: "h-3.5 w-3.5" })
            }
          )
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex-1 flex items-center justify-center py-6 px-3", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        "div",
        {
          className: "text-lg sm:text-xl font-semibold overflow-x-auto max-w-full text-center scrollbar-none font-mono py-2 text-zinc-100",
          dangerouslySetInnerHTML: {
            __html: (() => {
              try {
                const formula = functions[0] || "y = sin(x)";
                let latex = typeof formula === "string" ? formula.trim() : String(formula).trim();
                if (!latex.startsWith("y") && !latex.includes("=")) {
                  latex = "y = " + latex;
                }
                latex = latex.replace(/\\/g, "");
                latex = latex.replace(/\*/g, " ");
                latex = latex.replace(/\b(sin|cos|tan|cot|sec|csc|log|ln|sqrt|pi|theta|alpha|beta|gamma|lambda|phi)\b/g, "\\$1");
                return katex.renderToString(latex, { throwOnError: false, displayMode: true });
              } catch (e) {
                return functions[0] || "y = sin(x)";
              }
            })()
          }
        }
      ) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-[10px] text-zinc-500 text-center font-mono tracking-wider", children: "MathVerse 2D Graph Engine" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: cn("relative bg-[#09090b] h-full flex-1 transition-all duration-300", showEquationPanel ? "w-full md:w-3/5" : "w-full"), children: [
      !showEquationPanel && /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "button",
        {
          onClick: () => setShowEquationPanel(true),
          className: "absolute top-4 left-4 p-2 bg-zinc-900/90 hover:bg-zinc-805 border border-zinc-800 rounded-lg text-zinc-400 hover:text-white shadow-lg transition-all backdrop-blur-sm z-10 flex items-center gap-1.5 text-xs font-semibold px-3 animate-in fade-in duration-200",
          title: "Show Equations Panel",
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(SlidersVertical, { className: "h-3.5 w-3.5" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Show Equations" })
          ]
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "button",
        {
          onClick: () => setIsClosed(true),
          className: "absolute top-4 right-4 p-2 bg-zinc-900/90 hover:bg-zinc-805 border border-zinc-800 rounded-full text-zinc-400 hover:text-white shadow-lg transition-all backdrop-blur-sm z-10",
          title: "Close Graph",
          children: /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "h-3.5 w-3.5" })
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "canvas",
        {
          ref: canvasRef,
          onMouseDown: handleMouseDown,
          onMouseMove: handleMouseMove,
          onMouseUp: handleMouseUp,
          onMouseLeave: handleMouseUp,
          className: "w-full h-full cursor-grab active:cursor-grabbing block"
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "button",
        {
          onClick: handleReset,
          className: "absolute bottom-4 left-4 p-2 bg-zinc-900/90 hover:bg-zinc-805 border border-zinc-800 rounded-full text-zinc-400 hover:text-white shadow-lg transition-all backdrop-blur-sm z-10",
          title: "Reset Graph View",
          children: /* @__PURE__ */ jsxRuntimeExports.jsx(RotateCcw, { className: "h-4 w-4" })
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "absolute bottom-4 right-4 flex items-center gap-1 p-1 bg-zinc-900/90 border border-zinc-800 rounded-full shadow-lg backdrop-blur-sm z-10", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            onClick: () => handleZoom(0.8),
            className: "p-1.5 hover:bg-zinc-805 rounded-full text-zinc-400 hover:text-white transition-all",
            title: "Zoom In",
            children: /* @__PURE__ */ jsxRuntimeExports.jsx(ZoomIn, { className: "h-3.5 w-3.5" })
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            onClick: () => handleZoom(1.2),
            className: "p-1.5 hover:bg-zinc-805 rounded-full text-zinc-400 hover:text-white transition-all",
            title: "Zoom Out",
            children: /* @__PURE__ */ jsxRuntimeExports.jsx(ZoomOut, { className: "h-3.5 w-3.5" })
          }
        )
      ] })
    ] })
  ] });
}
function SimulationWidget({ type }) {
  const canvasRef = reactExports.useRef(null);
  const [isPlaying, setIsPlaying] = reactExports.useState(false);
  const [simTime, setSimTime] = reactExports.useState(0);
  const [pAngle, setPAngle] = reactExports.useState(45);
  const [pVelocity, setPVelocity] = reactExports.useState(20);
  const [pGravity, setPGravity] = reactExports.useState(9.8);
  const [pHeight, setPHeight] = reactExports.useState(0);
  const [dMass, setDMass] = reactExports.useState(8);
  const [dVolume, setDVolume] = reactExports.useState(10);
  const [dDensity, setDDensity] = reactExports.useState(1);
  const [eVoltage, setEVoltage] = reactExports.useState(12);
  const [eResistance, setEResistance] = reactExports.useState(20);
  const [bohrLevel, setBohrLevel] = reactExports.useState(1);
  const [bohrTargetLevel, setBohrTargetLevel] = reactExports.useState(1);
  const [bohrTransitioning, setBohrTransitioning] = reactExports.useState(false);
  const [bohrPhoton, setBohrPhoton] = reactExports.useState(null);
  const [bondingMolecule, setBondingMolecule] = reactExports.useState("h2o");
  const [shmMode, setShmMode] = reactExports.useState("pendulum");
  const [shmLength, setShmLength] = reactExports.useState(3);
  const [shmMass, setShmMass] = reactExports.useState(2);
  const [shmK, setShmK] = reactExports.useState(20);
  const [shmAmplitude, setShmAmplitude] = reactExports.useState(30);
  const requestRef = reactExports.useRef(null);
  const lastTimeRef = reactExports.useRef(0);
  const projCalc = reactExports.useMemo(() => {
    const rad = pAngle * Math.PI / 180;
    const v0x = pVelocity * Math.cos(rad);
    const v0y = pVelocity * Math.sin(rad);
    const a = 0.5 * pGravity;
    const b = -v0y;
    const c = -pHeight;
    const tFlight = (-b + Math.sqrt(b * b - 4 * a * c)) / (2 * a);
    const maxH = pHeight + v0y * v0y / (2 * pGravity);
    const range = v0x * tFlight;
    return { tFlight, maxH, range, v0x, v0y };
  }, [pAngle, pVelocity, pGravity, pHeight]);
  const densityCalc = reactExports.useMemo(() => {
    const blockDensity = dMass / dVolume;
    const g = 9.8;
    const Fg = dMass * g;
    let Vdisp = dVolume;
    let isFloating = false;
    if (blockDensity < dDensity) {
      Vdisp = dVolume * (blockDensity / dDensity);
      isFloating = true;
    }
    const Fb = dDensity * Vdisp * g;
    const netForce = Fb - Fg;
    return { blockDensity, Fg, Fb, Vdisp, isFloating, netForce };
  }, [dMass, dVolume, dDensity]);
  const elecCalc = reactExports.useMemo(() => {
    const current = eVoltage / eResistance;
    const power = current * current * eResistance;
    return { current, power };
  }, [eVoltage, eResistance]);
  const animate = (time) => {
    if (lastTimeRef.current !== void 0) {
      const dt = (time - lastTimeRef.current) / 1e3;
      if (isPlaying) {
        setSimTime((prev) => prev + dt);
      }
    }
    lastTimeRef.current = time;
    requestRef.current = requestAnimationFrame(animate);
  };
  reactExports.useEffect(() => {
    requestRef.current = requestAnimationFrame(animate);
    return () => {
      if (requestRef.current) cancelAnimationFrame(requestRef.current);
    };
  }, [isPlaying]);
  const triggerBohrTransition = (toLevel) => {
    if (toLevel === bohrLevel || bohrTransitioning) return;
    setBohrTargetLevel(toLevel);
    setBohrTransitioning(true);
    const fromEnergy = -13.6 / (bohrLevel * bohrLevel);
    const toEnergy = -13.6 / (toLevel * toLevel);
    const dE = Math.abs(toEnergy - fromEnergy);
    const lambda = 1240 / dE;
    setBohrPhoton({
      active: true,
      x: toLevel > bohrLevel ? -150 : 0,
      // starts outside if absorbing, center if emitting
      isAbsorbed: toLevel > bohrLevel,
      lambda
    });
  };
  reactExports.useEffect(() => {
    const canvas = canvasRef.current;
    if (!canvas) return;
    const ctx = canvas.getContext("2d");
    if (!ctx) return;
    const width = canvas.width = 500;
    const height = canvas.height = 300;
    ctx.clearRect(0, 0, width, height);
    ctx.fillStyle = "#090d16";
    ctx.fillRect(0, 0, width, height);
    if (type === "projectile") {
      ctx.strokeStyle = "#161b26";
      ctx.lineWidth = 0.5;
      for (let x = 0; x < width; x += 40) {
        ctx.beginPath();
        ctx.moveTo(x, 0);
        ctx.lineTo(x, height);
        ctx.stroke();
      }
      for (let y = 0; y < height; y += 40) {
        ctx.beginPath();
        ctx.moveTo(0, y);
        ctx.lineTo(width, y);
        ctx.stroke();
      }
      const scale = 5;
      const originX = 50;
      const originY = height - 50;
      ctx.strokeStyle = "#334155";
      ctx.lineWidth = 3;
      ctx.beginPath();
      ctx.moveTo(0, originY);
      ctx.lineTo(width, originY);
      ctx.stroke();
      if (pHeight > 0) {
        ctx.fillStyle = "#1e293b";
        ctx.fillRect(originX - 10, originY - pHeight * scale, 20, pHeight * scale);
        ctx.strokeStyle = "#475569";
        ctx.strokeRect(originX - 10, originY - pHeight * scale, 20, pHeight * scale);
      }
      ctx.strokeStyle = "rgba(59, 130, 246, 0.4)";
      ctx.lineWidth = 1.5;
      ctx.setLineDash([3, 3]);
      ctx.beginPath();
      let first = true;
      for (let t2 = 0; t2 <= projCalc.tFlight; t2 += 0.05) {
        const x = originX + projCalc.v0x * t2 * scale;
        const y = originY - pHeight * scale - (projCalc.v0y * t2 - 0.5 * pGravity * t2 * t2) * scale;
        if (first) {
          ctx.moveTo(x, y);
          first = false;
        } else ctx.lineTo(x, y);
      }
      const finalX = originX + projCalc.v0x * projCalc.tFlight * scale;
      const finalY = originY;
      ctx.lineTo(finalX, finalY);
      ctx.stroke();
      ctx.setLineDash([]);
      const t = isPlaying ? simTime % (projCalc.tFlight + 1) : 0;
      if (t <= projCalc.tFlight) {
        const ballX = originX + projCalc.v0x * t * scale;
        const ballY = originY - pHeight * scale - (projCalc.v0y * t - 0.5 * pGravity * t * t) * scale;
        ctx.fillStyle = "#f59e0b";
        ctx.beginPath();
        ctx.arc(ballX, ballY, 6, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = "#ffffff";
        ctx.lineWidth = 1;
        ctx.stroke();
      } else {
        ctx.fillStyle = "#ef4444";
        ctx.beginPath();
        ctx.arc(finalX, finalY, 6, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = "#ffffff";
        ctx.stroke();
      }
    } else if (type === "density") {
      const tankWidth = 220;
      const tankHeight = 180;
      const tankX = width / 2 - tankWidth / 2;
      const tankY = height - 50 - tankHeight;
      ctx.fillStyle = "#1e293b";
      ctx.fillRect(tankX, tankY, tankWidth, tankHeight);
      const fluidHeight = tankHeight * 0.7;
      const fluidTopY = tankY + (tankHeight - fluidHeight);
      ctx.fillStyle = "rgba(14, 165, 233, 0.4)";
      ctx.fillRect(tankX, fluidTopY, tankWidth, fluidHeight);
      ctx.strokeStyle = "#64748b";
      ctx.lineWidth = 4;
      ctx.beginPath();
      ctx.moveTo(tankX, tankY);
      ctx.lineTo(tankX, tankY + tankHeight);
      ctx.lineTo(tankX + tankWidth, tankY + tankHeight);
      ctx.lineTo(tankX + tankWidth, tankY);
      ctx.stroke();
      const blockSide = 30 + dVolume * 2;
      let blockY = fluidTopY - blockSide / 2;
      if (densityCalc.blockDensity >= dDensity) {
        const bottomY = tankY + tankHeight - blockSide;
        const sinkDuration = 2;
        const sinkProgress = isPlaying ? Math.min(simTime / sinkDuration, 1) : 0;
        blockY = fluidTopY + (bottomY - fluidTopY) * sinkProgress;
      } else {
        const submergeRatio = densityCalc.blockDensity / dDensity;
        const floatProgress = isPlaying ? Math.min(simTime / 1.5, 1) : 0;
        const targetEquilibriumY = fluidTopY - blockSide * (1 - submergeRatio);
        blockY = fluidTopY - blockSide + (targetEquilibriumY - (fluidTopY - blockSide)) * floatProgress;
      }
      const blockX = width / 2 - blockSide / 2;
      ctx.fillStyle = "#d97706";
      ctx.fillRect(blockX, blockY, blockSide, blockSide);
      ctx.strokeStyle = "#f59e0b";
      ctx.lineWidth = 2;
      ctx.strokeRect(blockX, blockY, blockSide, blockSide);
      ctx.fillStyle = "#ffffff";
      ctx.font = "bold 9px sans-serif";
      ctx.textAlign = "center";
      ctx.fillText(`${dMass} kg`, blockX + blockSide / 2, blockY + blockSide / 2 + 3);
      const centerX = blockX + blockSide / 2;
      const centerY = blockY + blockSide / 2;
      const forceScale = 0.5;
      const fgLen = Math.min(densityCalc.Fg * forceScale, 80);
      ctx.strokeStyle = "#ef4444";
      ctx.lineWidth = 3;
      ctx.beginPath();
      ctx.moveTo(centerX, centerY);
      ctx.lineTo(centerX, centerY + fgLen);
      ctx.stroke();
      ctx.fillStyle = "#ef4444";
      ctx.beginPath();
      ctx.moveTo(centerX - 5, centerY + fgLen - 5);
      ctx.lineTo(centerX + 5, centerY + fgLen - 5);
      ctx.lineTo(centerX, centerY + fgLen);
      ctx.fill();
      const fbLen = Math.min(densityCalc.Fb * forceScale, 80);
      if (fbLen > 5) {
        ctx.strokeStyle = "#10b981";
        ctx.lineWidth = 3;
        ctx.beginPath();
        ctx.moveTo(centerX, centerY);
        ctx.lineTo(centerX, centerY - fbLen);
        ctx.stroke();
        ctx.fillStyle = "#10b981";
        ctx.beginPath();
        ctx.moveTo(centerX - 5, centerY - fbLen + 5);
        ctx.lineTo(centerX + 5, centerY - fbLen + 5);
        ctx.lineTo(centerX, centerY - fbLen);
        ctx.fill();
      }
      ctx.fillStyle = "#ef4444";
      ctx.font = "11px sans-serif";
      ctx.textAlign = "left";
      ctx.fillText(`Gravity Fg: ${densityCalc.Fg.toFixed(1)} N`, 30, 50);
      ctx.fillStyle = "#10b981";
      ctx.fillText(`Buoyant Force Fb: ${densityCalc.Fb.toFixed(1)} N`, 30, 70);
      ctx.fillStyle = "#38bdf8";
      ctx.fillText(`Block Density: ${densityCalc.blockDensity.toFixed(2)} kg/L`, 30, 90);
    } else if (type === "electricity") {
      const pad = 60;
      const left = pad;
      const right = width - pad;
      const top = pad;
      const bottom = height - pad;
      ctx.strokeStyle = "#475569";
      ctx.lineWidth = 3;
      ctx.beginPath();
      ctx.moveTo(left, top);
      ctx.lineTo(right, top);
      ctx.lineTo(right, bottom);
      ctx.lineTo(left, bottom);
      ctx.closePath();
      ctx.stroke();
      const batCenterY = height / 2;
      ctx.fillStyle = "#090d16";
      ctx.fillRect(left - 10, batCenterY - 25, 20, 50);
      ctx.strokeStyle = "#10b981";
      ctx.lineWidth = 4;
      ctx.beginPath();
      ctx.moveTo(left - 15, batCenterY - 15);
      ctx.lineTo(left + 15, batCenterY - 15);
      ctx.moveTo(left - 8, batCenterY - 5);
      ctx.lineTo(left + 8, batCenterY - 5);
      ctx.moveTo(left - 15, batCenterY + 5);
      ctx.lineTo(left + 15, batCenterY + 5);
      ctx.moveTo(left - 8, batCenterY + 15);
      ctx.lineTo(left + 8, batCenterY + 15);
      ctx.stroke();
      ctx.fillStyle = "#10b981";
      ctx.font = "bold 12px sans-serif";
      ctx.fillText(`+`, left + 20, batCenterY - 15);
      ctx.fillText(`-`, left + 20, batCenterY + 20);
      const resCenterY = height / 2;
      ctx.fillStyle = "#090d16";
      ctx.fillRect(right - 15, resCenterY - 30, 30, 60);
      const heatFactor = Math.min(elecCalc.power / 50, 1);
      const colorRed = Math.round(59 + heatFactor * 196);
      const colorBlue = Math.round(130 - heatFactor * 100);
      const colorGreen = Math.round(246 - heatFactor * 200);
      ctx.fillStyle = `rgb(${colorRed}, ${colorGreen}, ${colorBlue})`;
      ctx.fillRect(right - 10, resCenterY - 25, 20, 50);
      ctx.strokeStyle = "#ffffff";
      ctx.lineWidth = 1.5;
      ctx.strokeRect(right - 10, resCenterY - 25, 20, 50);
      ctx.strokeStyle = "rgba(0, 0, 0, 0.4)";
      ctx.beginPath();
      ctx.moveTo(right - 10, resCenterY - 20);
      for (let y = resCenterY - 15; y <= resCenterY + 20; y += 10) {
        ctx.lineTo(right + 10, y - 5);
        ctx.lineTo(right - 10, y);
      }
      ctx.stroke();
      ctx.fillStyle = "#ffffff";
      ctx.font = "bold 11px sans-serif";
      ctx.textAlign = "center";
      ctx.fillText(`${eResistance} Ω`, right, resCenterY + 40);
      const ammeterX = width / 2;
      ctx.fillStyle = "#090d16";
      ctx.beginPath();
      ctx.arc(ammeterX, top, 18, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = "#3b82f6";
      ctx.lineWidth = 2;
      ctx.stroke();
      ctx.fillStyle = "#3b82f6";
      ctx.font = "bold 12px sans-serif";
      ctx.fillText(`${elecCalc.current.toFixed(2)}A`, ammeterX, top + 4);
      if (isPlaying) {
        ctx.fillStyle = "#e0f2fe";
        const electronSpeed = elecCalc.current * 80;
        const perimeter = (right - left) * 2 + (bottom - top) * 2;
        const numElectrons = 15;
        for (let i = 0; i < numElectrons; i++) {
          const startDist = i / numElectrons * perimeter + simTime * electronSpeed;
          const dist = startDist % perimeter;
          let ex = left;
          let ey = top;
          if (dist < right - left) {
            ex = left + dist;
            ey = top;
          } else if (dist < right - left + (bottom - top)) {
            ex = right;
            ey = top + (dist - (right - left));
          } else if (dist < (right - left) * 2 + (bottom - top)) {
            ex = right - (dist - (right - left) - (bottom - top));
            ey = bottom;
          } else {
            ex = left;
            ey = bottom - (dist - (right - left) * 2 - (bottom - top));
          }
          ctx.beginPath();
          ctx.arc(ex, ey, 3.5, 0, Math.PI * 2);
          ctx.fill();
        }
      }
      ctx.fillStyle = "#f59e0b";
      ctx.font = "12px sans-serif";
      ctx.textAlign = "left";
      ctx.fillText(`Power Dissipated: ${elecCalc.power.toFixed(1)} W`, 30, 40);
    } else if (type === "bohr") {
      const centerX = width / 2;
      const centerY = height / 2;
      ctx.fillStyle = "#ef4444";
      ctx.beginPath();
      ctx.arc(centerX - 3, centerY - 2, 5, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = "#3b82f6";
      ctx.beginPath();
      ctx.arc(centerX + 3, centerY + 2, 5, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = "#f59e0b";
      ctx.beginPath();
      ctx.arc(centerX, centerY - 1, 4, 0, Math.PI * 2);
      ctx.fill();
      const orbitScales = [0, 35, 65, 95, 125];
      for (let n = 1; n <= 4; n++) {
        ctx.strokeStyle = n === bohrLevel ? "rgba(168, 85, 247, 0.6)" : "#1e293b";
        ctx.lineWidth = n === bohrLevel ? 1.5 : 0.8;
        ctx.beginPath();
        ctx.arc(centerX, centerY, orbitScales[n], 0, Math.PI * 2);
        ctx.stroke();
        ctx.fillStyle = "#64748b";
        ctx.font = "10px sans-serif";
        ctx.fillText(`n=${n}`, centerX + orbitScales[n] - 8, centerY - 6);
      }
      if (bohrTransitioning) {
        const speed = 4;
        const fromRadius = orbitScales[bohrLevel];
        const toRadius = orbitScales[bohrTargetLevel];
        const transitionProgress = Math.min(simTime % 1.5 * speed, 1);
        const currentRadius = fromRadius + (toRadius - fromRadius) * transitionProgress;
        const theta = simTime * 3;
        const ex = centerX + Math.cos(theta) * currentRadius;
        const ey = centerY + Math.sin(theta) * currentRadius;
        ctx.fillStyle = "#a855f7";
        ctx.beginPath();
        ctx.arc(ex, ey, 6, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = "#ffffff";
        ctx.stroke();
        if (transitionProgress >= 1) {
          setBohrLevel(bohrTargetLevel);
          setBohrTransitioning(false);
          setBohrPhoton(null);
        }
      } else {
        const currentRadius = orbitScales[bohrLevel];
        const theta = simTime * (4 / bohrLevel);
        const ex = centerX + Math.cos(theta) * currentRadius;
        const ey = centerY + Math.sin(theta) * currentRadius;
        ctx.fillStyle = "#06b6d4";
        ctx.beginPath();
        ctx.arc(ex, ey, 5.5, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = "#ffffff";
        ctx.stroke();
      }
      if (bohrPhoton && bohrPhoton.active) {
        ctx.strokeStyle = bohrPhoton.lambda > 500 ? "#f43f5e" : "#3b82f6";
        ctx.lineWidth = 2;
        ctx.beginPath();
        const photonY = centerY - 10;
        const waveXStart = bohrPhoton.isAbsorbed ? -150 + simTime * 120 : centerX - simTime * 120;
        for (let dx = 0; dx < 40; dx++) {
          const wx = waveXStart + dx;
          const wy = photonY + Math.sin(dx * 0.4) * 6;
          if (dx === 0) ctx.moveTo(wx, wy);
          else ctx.lineTo(wx, wy);
        }
        ctx.stroke();
        ctx.fillStyle = "#ffffff";
        ctx.font = "bold 9px sans-serif";
        ctx.fillText(`Photon (${Math.round(bohrPhoton.lambda)} nm)`, waveXStart + 10, photonY - 12);
      }
      const energy = -13.6 / (bohrLevel * bohrLevel);
      ctx.fillStyle = "#ffffff";
      ctx.font = "bold 11px sans-serif";
      ctx.textAlign = "left";
      ctx.fillText(`Energy Level (E_${bohrLevel}): ${energy.toFixed(2)} eV`, 20, 30);
    } else if (type === "bonding") {
      const centerX = width / 2;
      const centerY = height / 2;
      if (bondingMolecule === "h2o") {
        ctx.fillStyle = "#ef4444";
        ctx.beginPath();
        ctx.arc(centerX, centerY - 20, 22, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = "#ffffff";
        ctx.lineWidth = 1;
        ctx.stroke();
        ctx.fillStyle = "#ffffff";
        ctx.font = "bold 12px sans-serif";
        ctx.textAlign = "center";
        ctx.fillText("O", centerX, centerY - 16);
        const bondLen = 60;
        const angleRad = 104.5 * Math.PI / 180;
        const h1x = centerX + Math.sin(angleRad / 2) * bondLen;
        const h1y = centerY - 20 + Math.cos(angleRad / 2) * bondLen;
        const h2x = centerX - Math.sin(angleRad / 2) * bondLen;
        const h2y = centerY - 20 + Math.cos(angleRad / 2) * bondLen;
        ctx.strokeStyle = "#475569";
        ctx.lineWidth = 5;
        ctx.beginPath();
        ctx.moveTo(centerX, centerY - 20);
        ctx.lineTo(h1x, h1y);
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(centerX, centerY - 20);
        ctx.lineTo(h2x, h2y);
        ctx.stroke();
        ctx.fillStyle = "#e2e8f0";
        ctx.beginPath();
        ctx.arc(h1x, h1y, 14, 0, Math.PI * 2);
        ctx.fill();
        ctx.stroke();
        ctx.fillStyle = "#0f172a";
        ctx.fillText("H", h1x, h1y + 4);
        ctx.fillStyle = "#e2e8f0";
        ctx.beginPath();
        ctx.arc(h2x, h2y, 14, 0, Math.PI * 2);
        ctx.fill();
        ctx.stroke();
        ctx.fillStyle = "#0f172a";
        ctx.fillText("H", h2x, h2y + 4);
        ctx.strokeStyle = "#a855f7";
        ctx.lineWidth = 1.5;
        ctx.setLineDash([2, 2]);
        ctx.beginPath();
        ctx.arc(centerX, centerY - 20, 35, Math.PI / 2 - angleRad / 2, Math.PI / 2 + angleRad / 2);
        ctx.stroke();
        ctx.setLineDash([]);
        ctx.fillStyle = "#a855f7";
        ctx.font = "bold 11px sans-serif";
        ctx.fillText("104.5°", centerX, centerY + 30);
        ctx.fillStyle = "rgba(168, 85, 247, 0.15)";
        ctx.strokeStyle = "rgba(168, 85, 247, 0.5)";
        ctx.lineWidth = 1;
        ctx.beginPath();
        ctx.ellipse(centerX - 15, centerY - 50, 10, 16, -Math.PI / 4, 0, Math.PI * 2);
        ctx.fill();
        ctx.stroke();
        ctx.beginPath();
        ctx.ellipse(centerX + 15, centerY - 50, 10, 16, Math.PI / 4, 0, Math.PI * 2);
        ctx.fill();
        ctx.stroke();
      } else if (bondingMolecule === "co2") {
        ctx.fillStyle = "#4b5563";
        ctx.beginPath();
        ctx.arc(centerX, centerY, 20, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = "#ffffff";
        ctx.stroke();
        ctx.fillStyle = "#ffffff";
        ctx.font = "bold 12px sans-serif";
        ctx.textAlign = "center";
        ctx.fillText("C", centerX, centerY + 4);
        const bondLen = 70;
        const o1x = centerX - bondLen;
        const o2x = centerX + bondLen;
        ctx.strokeStyle = "#6b7280";
        ctx.lineWidth = 6;
        ctx.beginPath();
        ctx.moveTo(centerX - 18, centerY - 4);
        ctx.lineTo(o1x + 18, centerY - 4);
        ctx.moveTo(centerX - 18, centerY + 4);
        ctx.lineTo(o1x + 18, centerY + 4);
        ctx.moveTo(centerX + 18, centerY - 4);
        ctx.lineTo(o2x - 18, centerY - 4);
        ctx.moveTo(centerX + 18, centerY + 4);
        ctx.lineTo(o2x - 18, centerY + 4);
        ctx.stroke();
        ctx.fillStyle = "#ef4444";
        ctx.beginPath();
        ctx.arc(o1x, centerY, 18, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = "#ffffff";
        ctx.stroke();
        ctx.fillStyle = "#ffffff";
        ctx.fillText("O", o1x, centerY + 4);
        ctx.fillStyle = "#ef4444";
        ctx.beginPath();
        ctx.arc(o2x, centerY, 18, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = "#ffffff";
        ctx.stroke();
        ctx.fillStyle = "#ffffff";
        ctx.fillText("O", o2x, centerY + 4);
        ctx.fillStyle = "#a855f7";
        ctx.font = "bold 11px sans-serif";
        ctx.fillText("Linear (180°)", centerX, centerY - 35);
      } else if (bondingMolecule === "nh3") {
        ctx.fillStyle = "#2563eb";
        ctx.beginPath();
        ctx.arc(centerX, centerY - 15, 21, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = "#ffffff";
        ctx.stroke();
        ctx.fillStyle = "#ffffff";
        ctx.font = "bold 12px sans-serif";
        ctx.textAlign = "center";
        ctx.fillText("N", centerX, centerY - 11);
        const h1x = centerX;
        const h1y = centerY + 40;
        const h2x = centerX - 55;
        const h2y = centerY + 15;
        const h3x = centerX + 55;
        const h3y = centerY + 15;
        ctx.strokeStyle = "#475569";
        ctx.lineWidth = 4;
        ctx.beginPath();
        ctx.moveTo(centerX, centerY - 15);
        ctx.lineTo(h1x, h1y);
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(centerX, centerY - 15);
        ctx.lineTo(h2x, h2y);
        ctx.stroke();
        ctx.beginPath();
        ctx.moveTo(centerX, centerY - 15);
        ctx.lineTo(h3x, h3y);
        ctx.stroke();
        ctx.fillStyle = "#e2e8f0";
        [[h1x, h1y], [h2x, h2y], [h3x, h3y]].forEach(([hx, hy]) => {
          ctx.beginPath();
          ctx.arc(hx, hy, 13, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = "#ffffff";
          ctx.stroke();
          ctx.fillStyle = "#0f172a";
          ctx.fillText("H", hx, hy + 4);
          ctx.fillStyle = "#e2e8f0";
        });
        ctx.fillStyle = "rgba(168, 85, 247, 0.15)";
        ctx.strokeStyle = "rgba(168, 85, 247, 0.5)";
        ctx.beginPath();
        ctx.ellipse(centerX, centerY - 45, 12, 18, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.stroke();
        ctx.fillStyle = "#a855f7";
        ctx.font = "bold 11px sans-serif";
        ctx.fillText("Trigonal Pyramidal (107°)", centerX, centerY + 70);
      }
    } else if (type === "shm") {
      const scale = 25;
      const centerX = width / 2;
      const pivotY = 50;
      ctx.fillStyle = "#334155";
      ctx.fillRect(centerX - 100, pivotY - 10, 200, 10);
      ctx.strokeStyle = "#475569";
      ctx.strokeRect(centerX - 100, pivotY - 10, 200, 10);
      if (shmMode === "pendulum") {
        const g = 9.8;
        const omega = Math.sqrt(g / shmLength);
        const thetaMax = shmAmplitude * Math.PI / 180;
        const theta = isPlaying ? thetaMax * Math.sin(omega * simTime) : thetaMax;
        const bobX = centerX + Math.sin(theta) * (shmLength * scale * 2);
        const bobY = pivotY + Math.cos(theta) * (shmLength * scale * 2);
        ctx.strokeStyle = "#94a3b8";
        ctx.lineWidth = 1.5;
        ctx.beginPath();
        ctx.moveTo(centerX, pivotY);
        ctx.lineTo(bobX, bobY);
        ctx.stroke();
        const r = 8 + shmMass * 1.5;
        ctx.fillStyle = "#ec4899";
        ctx.beginPath();
        ctx.arc(bobX, bobY, r, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = "#ffffff";
        ctx.stroke();
        const maxPE = shmMass * g * shmLength * (1 - Math.cos(thetaMax));
        const curPE = shmMass * g * shmLength * (1 - Math.cos(theta));
        const curKE = maxPE - curPE;
        const barX = width - 120;
        const barY = 100;
        const barH = 100;
        ctx.fillStyle = "#1e293b";
        ctx.fillRect(barX, barY, 80, barH);
        const peH = curPE / maxPE * barH;
        ctx.fillStyle = "#a855f7";
        ctx.fillRect(barX + 10, barY + barH - peH, 20, peH);
        const keH = curKE / maxPE * barH;
        ctx.fillStyle = "#06b6d4";
        ctx.fillRect(barX + 45, barY + barH - keH, 20, keH);
        ctx.fillStyle = "#ffffff";
        ctx.font = "9px sans-serif";
        ctx.textAlign = "center";
        ctx.fillText("PE", barX + 20, barY + barH + 12);
        ctx.fillText("KE", barX + 55, barY + barH + 12);
        ctx.fillText(`Period: ${(2 * Math.PI / omega).toFixed(2)}s`, barX + 38, barY - 15);
      } else {
        const omega = Math.sqrt(shmK / shmMass);
        const ampPixels = shmAmplitude * 1.2;
        const yOffset = isPlaying ? ampPixels * Math.sin(omega * simTime) : ampPixels;
        const eqLength = 110;
        const currentLength = eqLength + yOffset;
        ctx.strokeStyle = "#94a3b8";
        ctx.lineWidth = 2.5;
        ctx.beginPath();
        ctx.moveTo(centerX, pivotY);
        const coils = 14;
        for (let i = 0; i <= coils; i++) {
          const cy = pivotY + currentLength * (i / coils);
          const cx = centerX + (i % 2 === 0 && i > 0 && i < coils ? 18 : i % 2 !== 0 && i < coils ? -18 : 0);
          ctx.lineTo(cx, cy);
        }
        ctx.stroke();
        const blockSide = 26 + shmMass * 2.5;
        const blockX = centerX - blockSide / 2;
        const blockY = pivotY + currentLength;
        ctx.fillStyle = "#ec4899";
        ctx.fillRect(blockX, blockY, blockSide, blockSide);
        ctx.strokeStyle = "#ffffff";
        ctx.strokeRect(blockX, blockY, blockSide, blockSide);
        const maxE = 0.5 * shmK * (shmAmplitude * 0.01) * (shmAmplitude * 0.01);
        const curPE = 0.5 * shmK * (yOffset * 0.01) * (yOffset * 0.01);
        const curKE = maxE - curPE;
        const barX = width - 120;
        const barY = 100;
        const barH = 100;
        ctx.fillStyle = "#1e293b";
        ctx.fillRect(barX, barY, 80, barH);
        const peH = maxE > 0 ? curPE / maxE * barH : 0;
        ctx.fillStyle = "#a855f7";
        ctx.fillRect(barX + 10, barY + barH - peH, 20, peH);
        const keH = maxE > 0 ? curKE / maxE * barH : 0;
        ctx.fillStyle = "#06b6d4";
        ctx.fillRect(barX + 45, barY + barH - keH, 20, keH);
        ctx.fillStyle = "#ffffff";
        ctx.font = "9px sans-serif";
        ctx.textAlign = "center";
        ctx.fillText("PE", barX + 20, barY + barH + 12);
        ctx.fillText("KE", barX + 55, barY + barH + 12);
        ctx.fillText(`Period: ${(2 * Math.PI / omega).toFixed(2)}s`, barX + 38, barY - 15);
      }
    }
  }, [type, simTime, isPlaying, pAngle, pVelocity, pGravity, pHeight, dMass, dVolume, dDensity, eVoltage, eResistance, bohrLevel, bohrTargetLevel, bohrPhoton, bohrTransitioning, bondingMolecule, shmMode, shmLength, shmMass, shmK, shmAmplitude]);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "w-full bg-slate-900 border border-slate-800 rounded-2xl p-4 overflow-hidden shadow-2xl flex flex-col gap-4 text-white", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between pb-2 border-b border-slate-800", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Cpu, { className: "h-5 w-5 text-indigo-400" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("h3", { className: "font-bold text-base bg-gradient-to-r from-purple-400 to-indigo-400 bg-clip-text text-transparent", children: [
          type.toUpperCase(),
          " Interactive Simulation"
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Button,
          {
            size: "sm",
            onClick: () => setIsPlaying(!isPlaying),
            className: "h-8 rounded-full bg-indigo-600 hover:bg-indigo-700 font-bold px-3 gap-1.5",
            children: isPlaying ? /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Pause, { className: "h-3.5 w-3.5 fill-current" }),
              " Pause"
            ] }) : /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Play, { className: "h-3.5 w-3.5 fill-current" }),
              " Run Simulation"
            ] })
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          Button,
          {
            size: "sm",
            variant: "outline",
            onClick: () => {
              setSimTime(0);
              setIsPlaying(false);
              setBohrPhoton(null);
            },
            className: "h-8 w-8 p-0 rounded-full border-slate-700 text-slate-300 hover:text-white",
            children: /* @__PURE__ */ jsxRuntimeExports.jsx(RotateCcw, { className: "h-3.5 w-3.5" })
          }
        )
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col md:flex-row gap-4", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex-1 bg-slate-950 rounded-2xl border border-slate-800 overflow-hidden relative flex items-center justify-center h-[300px]", children: /* @__PURE__ */ jsxRuntimeExports.jsx("canvas", { ref: canvasRef, className: "block w-[500px] h-[300px] max-w-full" }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "w-full md:w-60 shrink-0 flex flex-col gap-3.5 bg-slate-950/40 p-3.5 rounded-2xl border border-slate-800", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("label", { className: "text-xs font-bold text-slate-400 uppercase tracking-wider", children: "Parameters" }),
        type === "projectile" && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-3", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-1.5", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between text-xs font-semibold", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Launch Angle:" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-indigo-400", children: [
                pAngle,
                "°"
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Slider, { min: 0, max: 90, step: 1, value: [pAngle], onValueChange: (val) => setPAngle(val[0]) })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-1.5", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between text-xs font-semibold", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Velocity (v₀):" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-indigo-400", children: [
                pVelocity,
                " m/s"
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Slider, { min: 1, max: 50, step: 1, value: [pVelocity], onValueChange: (val) => setPVelocity(val[0]) })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-1.5", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between text-xs font-semibold", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Height (h₀):" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-indigo-400", children: [
                pHeight,
                " m"
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Slider, { min: 0, max: 40, step: 1, value: [pHeight], onValueChange: (val) => setPHeight(val[0]) })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-1.5", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between text-xs font-semibold", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Gravity (g):" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-indigo-400", children: [
                pGravity,
                " m/s²"
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Slider, { min: 1.6, max: 25, step: 0.1, value: [pGravity], onValueChange: (val) => setPGravity(val[0]) })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mt-2 text-xs font-medium text-slate-300 space-y-1 bg-slate-950 p-2 rounded-lg border border-slate-800", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              "Range: ",
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-yellow-400 font-mono", children: [
                projCalc.range.toFixed(2),
                " m"
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              "Max Height: ",
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-yellow-400 font-mono", children: [
                projCalc.maxH.toFixed(2),
                " m"
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              "Time of Flight: ",
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-yellow-400 font-mono", children: [
                projCalc.tFlight.toFixed(2),
                " s"
              ] })
            ] })
          ] })
        ] }),
        type === "density" && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-3", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-1.5", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between text-xs font-semibold", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Block Mass:" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-amber-500", children: [
                dMass,
                " kg"
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Slider, { min: 1, max: 20, step: 0.5, value: [dMass], onValueChange: (val) => setDMass(val[0]) })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-1.5", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between text-xs font-semibold", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Block Volume:" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-amber-500", children: [
                dVolume,
                " L"
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Slider, { min: 1, max: 20, step: 0.5, value: [dVolume], onValueChange: (val) => setDVolume(val[0]) })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-1.5", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between text-xs font-semibold", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Fluid Density:" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-sky-400", children: [
                dDensity,
                " kg/L"
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Slider, { min: 0.5, max: 2, step: 0.05, value: [dDensity], onValueChange: (val) => setDDensity(val[0]) })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mt-2 text-xs font-medium text-slate-300 space-y-1 bg-slate-950 p-2.5 rounded-lg border border-slate-800", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-1.5 justify-between", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Status:" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { variant: densityCalc.blockDensity > dDensity ? "destructive" : "default", className: "text-[9px] h-4", children: densityCalc.blockDensity > dDensity ? "Sinks" : "Floats" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              "Displaced: ",
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-sky-400 font-mono", children: [
                densityCalc.Vdisp.toFixed(2),
                " L"
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              "Net Force: ",
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-pink-400 font-mono", children: [
                densityCalc.netForce.toFixed(1),
                " N"
              ] })
            ] })
          ] })
        ] }),
        type === "electricity" && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-3", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-1.5", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between text-xs font-semibold", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Battery Voltage:" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-emerald-400", children: [
                eVoltage,
                " V"
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Slider, { min: 1, max: 30, step: 0.5, value: [eVoltage], onValueChange: (val) => setEVoltage(val[0]) })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-1.5", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between text-xs font-semibold", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Resistance:" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-amber-500", children: [
                eResistance,
                " Ω"
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Slider, { min: 5, max: 100, step: 1, value: [eResistance], onValueChange: (val) => setEResistance(val[0]) })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mt-2 text-xs font-medium text-slate-300 space-y-1 bg-slate-950 p-2.5 rounded-lg border border-slate-800", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              "Current (I): ",
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-sky-400 font-mono", children: [
                elecCalc.current.toFixed(3),
                " A"
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              "Heat Power (P): ",
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-orange-400 font-mono", children: [
                elecCalc.power.toFixed(1),
                " W"
              ] })
            ] })
          ] })
        ] }),
        type === "bohr" && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-3.5", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-xs text-slate-400", children: "Select an orbit to trigger energy absorption/emission transitions:" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "grid grid-cols-2 gap-2", children: [1, 2, 3, 4].map((n) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "button",
            {
              onClick: () => triggerBohrTransition(n),
              disabled: bohrTransitioning,
              className: `py-2 text-xs font-bold rounded-xl transition-all border ${bohrLevel === n ? "bg-purple-600/30 border-purple-500 text-purple-200" : "bg-slate-950 hover:bg-slate-900 border-slate-800 text-slate-400"} disabled:opacity-50 disabled:cursor-not-allowed`,
              children: [
                "Orbit n=",
                n
              ]
            },
            n
          )) }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "text-[11px] text-slate-400 leading-relaxed bg-slate-950 p-2.5 rounded-lg border border-slate-800", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "font-bold text-slate-300 mb-1", children: "Spectral Lines Info:" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              "• n=3 → n=2: Emits ",
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-red-400", children: "Red (656nm)" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              "• n=4 → n=2: Emits ",
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-cyan-400", children: "Cyan (486nm)" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              "• n=2 → n=1: Emits ",
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-purple-400", children: "UV (121nm)" })
            ] })
          ] })
        ] }),
        type === "bonding" && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-3", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-xs text-slate-400", children: "Select Molecule:" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "select",
            {
              value: bondingMolecule,
              onChange: (e) => setBondingMolecule(e.target.value),
              className: "bg-slate-950 border border-slate-800 rounded-xl px-3 py-2 text-xs text-slate-200 focus:outline-none focus:border-indigo-500 cursor-pointer font-bold",
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "h2o", children: "H2O (Water) - Bent" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "co2", children: "CO2 (Carbon Dioxide) - Linear" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "nh3", children: "NH3 (Ammonia) - Trigonal Pyramidal" })
              ]
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "text-[11px] text-slate-400 leading-relaxed bg-slate-950 p-2.5 rounded-lg border border-slate-800 mt-2", children: [
            bondingMolecule === "h2o" && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-bold text-red-400", children: "H2O (sp³ hybridized):" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "mt-1", children: "Oxygen has 2 bonding pairs & 2 lone pairs. Repulsion from lone pairs squeezes H-O-H angle from tetrahedral 109.5° to 104.5°." })
            ] }),
            bondingMolecule === "co2" && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-bold text-grey-400", children: "CO2 (sp hybridized):" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "mt-1", children: "Carbon forms 2 double bonds with Oxygen. The linear arrangement minimizes electron repulsion (180° angle)." })
            ] }),
            bondingMolecule === "nh3" && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-bold text-blue-400", children: "NH3 (sp³ hybridized):" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "mt-1", children: "Nitrogen has 3 bond pairs & 1 lone pair. The lone pair pushes the bonds down, resulting in 107° bond angle." })
            ] })
          ] })
        ] }),
        type === "shm" && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-3", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex gap-1.5 p-1 bg-slate-950 rounded-xl border border-slate-800", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "button",
              {
                onClick: () => setShmMode("pendulum"),
                className: `w-full py-1 text-[10px] font-bold rounded-lg transition-colors ${shmMode === "pendulum" ? "bg-indigo-600 text-white" : "text-slate-400 hover:text-slate-200"}`,
                children: "Pendulum"
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "button",
              {
                onClick: () => setShmMode("spring"),
                className: `w-full py-1 text-[10px] font-bold rounded-lg transition-colors ${shmMode === "spring" ? "bg-indigo-600 text-white" : "text-slate-400 hover:text-slate-200"}`,
                children: "Spring-Mass"
              }
            )
          ] }),
          shmMode === "pendulum" ? /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-1.5", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between text-xs font-semibold", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Length (L):" }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-pink-400", children: [
                  shmLength,
                  " m"
                ] })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(Slider, { min: 0.5, max: 5, step: 0.1, value: [shmLength], onValueChange: (val) => setShmLength(val[0]) })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-1.5", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between text-xs font-semibold", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Bob Mass (m):" }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-pink-400", children: [
                  shmMass,
                  " kg"
                ] })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(Slider, { min: 0.5, max: 8, step: 0.1, value: [shmMass], onValueChange: (val) => setShmMass(val[0]) })
            ] })
          ] }) : /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-1.5", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between text-xs font-semibold", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Spring Constant (k):" }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-pink-400", children: [
                  shmK,
                  " N/m"
                ] })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(Slider, { min: 5, max: 50, step: 1, value: [shmK], onValueChange: (val) => setShmK(val[0]) })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-1.5", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between text-xs font-semibold", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Block Mass (m):" }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-pink-400", children: [
                  shmMass,
                  " kg"
                ] })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(Slider, { min: 0.5, max: 8, step: 0.1, value: [shmMass], onValueChange: (val) => setShmMass(val[0]) })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-1.5", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between text-xs font-semibold", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Amplitude:" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-pink-400", children: [
                shmAmplitude,
                shmMode === "pendulum" ? "°" : " cm"
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(Slider, { min: 5, max: 60, step: 1, value: [shmAmplitude], onValueChange: (val) => setShmAmplitude(val[0]) })
          ] })
        ] })
      ] })
    ] })
  ] });
}
function YouTubeCardWidget({
  videoId,
  title,
  creator,
  views,
  uploadDate,
  rating = "4.9",
  whyRecommend
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "my-5 w-full bg-slate-900 border border-slate-800 rounded-2xl overflow-hidden shadow-2xl transition-all duration-300 hover:scale-[1.01] hover:border-slate-700 flex flex-col md:flex-row max-w-2xl", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "relative w-full md:w-80 h-44 md:h-auto bg-black shrink-0", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
      "iframe",
      {
        src: `https://www.youtube.com/embed/${videoId}`,
        className: "absolute top-0 left-0 w-full h-full border-0",
        allowFullScreen: true,
        allow: "accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
      }
    ) }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "p-4 flex flex-col justify-between flex-1 text-white gap-3.5", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-1.5", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs(Badge, { className: "bg-red-600/90 text-white font-bold tracking-wide rounded-full text-[9px] hover:bg-red-700 uppercase flex items-center gap-1 w-fit", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Tv, { className: "h-2.5 w-2.5" }),
          " Featured Lecture"
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("h4", { className: "font-extrabold text-sm md:text-base leading-tight text-slate-100 hover:text-red-400 transition-colors line-clamp-2", children: title }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-[11px] font-bold text-slate-400 flex items-center gap-1", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
            "By ",
            creator
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "•" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
            views,
            " views"
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "•" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: uploadDate })
        ] })
      ] }),
      whyRecommend && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-slate-950/60 p-2.5 rounded-xl border border-slate-800/80 text-[11px] text-slate-300 relative", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "font-bold text-slate-400 uppercase tracking-wider text-[9px] mb-1 flex items-center gap-1.5", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Sparkles, { className: "h-3 w-3 text-yellow-400" }),
          " Why OM Tutors Recommend This:"
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "italic leading-relaxed", children: [
          '"',
          whyRecommend,
          '"'
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between border-t border-slate-800/80 pt-2.5", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-1", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Award, { className: "h-4 w-4 text-yellow-500 fill-yellow-500/10" }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-[11px] font-extrabold text-yellow-500", children: [
            rating,
            " / 5.0 Rating"
          ] })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "a",
          {
            href: `https://www.youtube.com/watch?v=${videoId}`,
            target: "_blank",
            rel: "noopener noreferrer",
            className: "text-[11px] font-bold text-blue-400 hover:text-blue-300 transition-all flex items-center gap-1 hover:underline",
            children: [
              "Open in YT ",
              /* @__PURE__ */ jsxRuntimeExports.jsx(ExternalLink, { className: "h-3 w-3" })
            ]
          }
        )
      ] })
    ] })
  ] });
}
function NewsFeedWidget({ items, recommendations }) {
  const [activeCategory, setActiveCategory] = reactExports.useState("technology");
  const [selectedQuizAns, setSelectedQuizAns] = reactExports.useState({});
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "my-5 w-full bg-slate-900 border border-slate-800 rounded-2xl overflow-hidden shadow-2xl text-white max-w-2xl flex flex-col", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "p-4 bg-slate-950 border-b border-slate-800 flex items-center justify-between", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { className: "bg-blue-600 text-white rounded-full", children: "LIVE" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-extrabold text-sm uppercase tracking-wider text-slate-200", children: "Educational News & Academic Bulletin" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[10px] font-semibold text-slate-500 font-mono", children: "2026-06-16" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "divide-y divide-slate-800 max-h-80 overflow-y-auto px-4", children: items.map((it, idx) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "py-3.5 space-y-1.5 hover:bg-slate-800/10 transition-all px-1.5 rounded-lg my-1", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between gap-2", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[10px] font-extrabold text-blue-400 uppercase tracking-widest", children: it.source }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { variant: "outline", className: "text-[9px] h-4 text-yellow-500 border-yellow-500/20 bg-yellow-500/5", children: it.badge })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("h5", { className: "font-bold text-sm leading-tight text-slate-100 hover:text-blue-400 transition-colors", children: it.title }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-slate-400 leading-relaxed font-medium", children: it.summary })
    ] }, idx)) }),
    recommendations && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "p-4 bg-slate-950 border-t border-slate-800/60 mt-auto", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex border-b border-slate-800 pb-2 mb-3 overflow-x-auto gap-2 shrink-0", children: [
        { id: "technology", label: "Tech" },
        { id: "sports", label: "Sports" },
        { id: "politics", label: "Politics" },
        { id: "business", label: "Business" },
        { id: "assembly", label: "School Assembly" }
      ].map((tab2) => /* @__PURE__ */ jsxRuntimeExports.jsx(
        "button",
        {
          onClick: () => setActiveCategory(tab2.id),
          className: `px-3 py-1 text-xs font-bold rounded-full transition-all shrink-0 ${activeCategory === tab2.id ? "bg-blue-600/20 text-blue-400 border border-blue-600/40" : "text-slate-400 hover:text-slate-200"}`,
          children: tab2.label
        },
        tab2.id
      )) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "min-h-12 text-xs leading-relaxed text-slate-300 font-medium bg-slate-900/60 p-3 rounded-xl border border-slate-800/80", children: [
        activeCategory === "technology" && recommendations.technology && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: recommendations.technology }),
        activeCategory === "sports" && recommendations.sports && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: recommendations.sports }),
        activeCategory === "politics" && recommendations.politics && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: recommendations.politics }),
        activeCategory === "business" && recommendations.business && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: recommendations.business }),
        activeCategory === "assembly" && recommendations.schoolAssembly && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-bold text-slate-400 block mb-1", children: "Assembly Bulletins:" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: recommendations.schoolAssembly })
        ] })
      ] }),
      recommendations.quiz && recommendations.quiz.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mt-4 border-t border-slate-800/60 pt-4", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-xs font-extrabold text-indigo-400 uppercase tracking-wider mb-2 flex items-center gap-1.5", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(CircleQuestionMark, { className: "h-3.5 w-3.5 text-indigo-400" }),
          " Current Affairs Quiz"
        ] }),
        recommendations.quiz.map((q, qIdx) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-slate-900 p-3.5 rounded-2xl border border-slate-800 flex flex-col gap-2.5", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-xs font-bold text-slate-200", children: [
            "Q: ",
            q.question
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "grid grid-cols-2 gap-2", children: q.options.map((opt) => /* @__PURE__ */ jsxRuntimeExports.jsx(
            "button",
            {
              onClick: () => setSelectedQuizAns((prev) => ({ ...prev, [qIdx]: opt })),
              className: `p-2 text-xs rounded-xl font-bold border text-left transition-all ${selectedQuizAns[qIdx] === opt ? opt === q.answer ? "bg-green-600/20 border-green-500 text-green-300" : "bg-red-600/20 border-red-500 text-red-300" : selectedQuizAns[qIdx] && opt === q.answer ? "bg-green-600/20 border-green-500 text-green-300" : "bg-slate-950 hover:bg-slate-800 border-slate-800 text-slate-300"}`,
              children: opt
            },
            opt
          )) }),
          selectedQuizAns[qIdx] && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex items-center gap-1.5 text-[10px] font-bold", children: selectedQuizAns[qIdx] === q.answer ? /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-green-400 flex items-center gap-1", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(CircleCheck, { className: "h-3.5 w-3.5 fill-green-500/10" }),
            " Correct Answer! Excellent."
          ] }) : /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-red-400 flex items-center gap-1", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(CircleX, { className: "h-3.5 w-3.5 fill-red-500/10" }),
            " Wrong answer. Correct was: ",
            q.answer
          ] }) })
        ] }, qIdx))
      ] })
    ] })
  ] });
}
function InteractiveQuizWidget({
  question,
  options,
  answer,
  explanation
}) {
  const [selectedOption, setSelectedOption] = reactExports.useState(null);
  const [showExplanation, setShowExplanation] = reactExports.useState(false);
  const handleSelect = (opt) => {
    if (selectedOption) return;
    setSelectedOption(opt);
    setShowExplanation(true);
  };
  const handleReset = () => {
    setSelectedOption(null);
    setShowExplanation(false);
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "my-5 w-full bg-slate-900 border border-slate-800 rounded-2xl overflow-hidden shadow-2xl text-white max-w-2xl flex flex-col", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "p-4 bg-slate-950 border-b border-slate-800 flex items-center justify-between", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(BookOpen, { className: "h-4.5 w-4.5 text-blue-400" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-extrabold text-xs uppercase tracking-wider text-slate-300", children: "Interactive Practice Question" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Badge, { className: "bg-indigo-600/90 text-white hover:bg-indigo-700", children: "JEE Prep" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "p-5 flex flex-col gap-4", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("h4", { className: "font-bold text-sm md:text-base leading-relaxed text-slate-100", children: question }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex flex-col gap-2", children: options.map((opt) => {
        const isSelected = selectedOption === opt;
        const isCorrect = opt === answer;
        const hasChosen = selectedOption !== null;
        let btnStyle = "bg-slate-950 border-slate-800 text-slate-300 hover:bg-slate-850 hover:border-slate-700";
        if (hasChosen) {
          if (isCorrect) {
            btnStyle = "bg-green-600/15 border-green-500 text-green-200 shadow-inner";
          } else if (isSelected) {
            btnStyle = "bg-red-600/15 border-red-500 text-red-200 shadow-inner";
          } else {
            btnStyle = "bg-slate-950/40 border-slate-850 text-slate-500 opacity-60";
          }
        }
        return /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "button",
          {
            disabled: hasChosen,
            onClick: () => handleSelect(opt),
            className: `p-3.5 text-xs md:text-sm font-semibold rounded-xl border text-left transition-all flex items-center justify-between gap-3 ${btnStyle}`,
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: opt }),
              hasChosen && isCorrect && /* @__PURE__ */ jsxRuntimeExports.jsx(CircleCheck, { className: "h-4 w-4 text-green-500 shrink-0" }),
              hasChosen && isSelected && !isCorrect && /* @__PURE__ */ jsxRuntimeExports.jsx(CircleX, { className: "h-4 w-4 text-red-500 shrink-0" })
            ]
          },
          opt
        );
      }) }),
      showExplanation && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mt-2 space-y-3 animate-fadeIn", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-slate-950/60 p-4 rounded-xl border border-slate-800/80", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "font-bold text-indigo-400 text-xs mb-1.5 uppercase tracking-wider flex items-center gap-1.5", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Info, { className: "h-3.5 w-3.5" }),
            " Concept Explanation"
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs md:text-[13px] text-slate-300 leading-relaxed font-medium", children: explanation })
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex justify-end", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
          Button,
          {
            size: "sm",
            onClick: handleReset,
            variant: "outline",
            className: "h-8 text-xs font-bold text-slate-300 border-slate-800 hover:bg-slate-800 hover:text-white rounded-full px-3.5 gap-1",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(RotateCcw, { className: "h-3 w-3" }),
              " Try Again"
            ]
          }
        ) })
      ] })
    ] })
  ] });
}
const loadHtml2Canvas = () => {
  return new Promise((resolve, reject) => {
    if (window.html2canvas && window.html2canvasIsPro) {
      resolve(window.html2canvas);
      return;
    }
    const script = document.createElement("script");
    script.src = "https://cdn.jsdelivr.net/npm/html2canvas-pro@2.0.4/dist/html2canvas-pro.min.js";
    script.onload = () => {
      window.html2canvasIsPro = true;
      resolve(window.html2canvas);
    };
    script.onerror = () => reject(new Error("Failed to load html2canvas-pro"));
    document.body.appendChild(script);
  });
};
function YoutubePreview({ videoId, href, children }) {
  const [play, setPlay] = reactExports.useState(false);
  const containerRef = reactExports.useRef(null);
  const playerRef = reactExports.useRef(null);
  const [thumbUrl, setThumbUrl] = reactExports.useState(`https://img.youtube.com/vi/${videoId}/maxresdefault.jpg`);
  reactExports.useEffect(() => {
    setThumbUrl(`https://img.youtube.com/vi/${videoId}/maxresdefault.jpg`);
  }, [videoId]);
  reactExports.useEffect(() => {
    if (!play || !containerRef.current) return;
    let cancelled = false;
    __vitePreload(async () => {
      const { loadYouTubeApi } = await import("./index-CSKiqNtJ.js").then((n) => n.aG);
      return { loadYouTubeApi };
    }, true ? __vite__mapDeps([0,1,2,3]) : void 0).then(({ loadYouTubeApi }) => {
      loadYouTubeApi().then(() => {
        if (cancelled || !containerRef.current) return;
        if (playerRef.current) {
          try {
            playerRef.current.destroy();
          } catch {
          }
          playerRef.current = null;
        }
        playerRef.current = new window.YT.Player(containerRef.current, {
          videoId,
          playerVars: {
            autoplay: 1,
            controls: 1,
            showinfo: 0,
            rel: 0,
            modestbranding: 1,
            iv_load_policy: 3,
            playsinline: 1,
            origin: window.location.origin
          }
        });
      });
    });
    return () => {
      cancelled = true;
      if (playerRef.current) {
        try {
          playerRef.current.destroy();
        } catch {
        }
        playerRef.current = null;
      }
    };
  }, [play, videoId]);
  let channelName = "";
  let videoTitle = "";
  if (children) {
    const textStr = String(children).trim();
    const parts = textStr.split(/ - (.+)/);
    if (parts.length >= 2) {
      channelName = parts[0].trim();
      videoTitle = parts[1].trim().replace(/^["']|["']$/g, "");
    } else {
      videoTitle = textStr;
    }
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "my-5 rounded-2xl overflow-hidden border border-slate-800 w-full max-w-2xl bg-[#0b0f19] hover:border-slate-700 transition-all duration-300 shadow-xl group", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        className: "relative pt-[56.25%] bg-black cursor-pointer overflow-hidden",
        onClick: () => setPlay(true),
        children: play ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute inset-0 w-full h-full", children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { ref: containerRef, className: "w-full h-full" }) }) : /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "img",
            {
              src: thumbUrl,
              alt: "YouTube Video Thumbnail",
              className: "absolute top-0 left-0 w-full h-full object-cover opacity-90 group-hover:scale-[1.02] group-hover:opacity-100 transition-all duration-500",
              loading: "lazy",
              onError: () => {
                if (thumbUrl.includes("maxresdefault.jpg")) {
                  setThumbUrl(`https://img.youtube.com/vi/${videoId}/hqdefault.jpg`);
                } else if (thumbUrl.includes("hqdefault.jpg")) {
                  setThumbUrl(`https://img.youtube.com/vi/${videoId}/mqdefault.jpg`);
                }
              }
            }
          ),
          videoTitle && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute top-0 left-0 right-0 p-4 bg-gradient-to-b from-black/80 via-black/30 to-transparent text-white z-10 pointer-events-none", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2.5", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "h-9 w-9 rounded-full bg-slate-900 border border-white/10 flex items-center justify-center shrink-0 overflow-hidden shadow", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
              "img",
              {
                src: `https://www.google.com/s2/favicons?domain=youtube.com&sz=64`,
                alt: "",
                className: "h-5 w-5 object-contain"
              }
            ) }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "min-w-0 flex-1", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("h5", { className: "text-[14px] font-bold text-white leading-tight truncate drop-shadow-md", children: videoTitle }),
              channelName && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-[11px] text-slate-300 font-semibold truncate drop-shadow", children: channelName })
            ] })
          ] }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute inset-0 bg-black/10 group-hover:bg-black/25 transition-colors pointer-events-none" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute inset-0 flex items-center justify-center pointer-events-none", children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-16 h-11 rounded-2xl bg-[#FF0000] flex items-center justify-center group-hover:bg-[#FF0000]/90 transition-all duration-300 shadow-2xl scale-95 group-hover:scale-105", children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-0 h-0 border-t-[8px] border-t-transparent border-l-[14px] border-l-white border-b-[8px] border-b-transparent ml-1" }) }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "a",
            {
              href,
              target: "_blank",
              rel: "noopener noreferrer",
              onClick: (e) => e.stopPropagation(),
              className: "absolute bottom-3 right-3 px-3 py-1.5 rounded-full bg-black/75 hover:bg-black/95 text-white text-[11px] font-bold transition-all flex items-center gap-1.5 backdrop-blur border border-white/10 shadow-lg active:scale-95",
              children: "Watch on YouTube"
            }
          )
        ] })
      }
    ),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "p-4 bg-slate-950/40 border-t border-slate-900 flex justify-between items-center gap-4", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "min-w-0 flex-1", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-[12px] font-bold text-slate-300 truncate", children: videoTitle || `YouTube Video (${videoId})` }),
        channelName && /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-[10px] text-slate-500 font-semibold truncate mt-0.5", children: [
          "Channel: ",
          channelName
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "a",
        {
          href,
          target: "_blank",
          rel: "noopener noreferrer",
          className: "text-[11px] font-bold text-blue-400 hover:text-blue-300 transition-colors shrink-0 flex items-center gap-1 hover:underline",
          onClick: (e) => e.stopPropagation(),
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Open Link" }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(ExternalLink, { className: "h-3 w-3" })
          ]
        }
      )
    ] })
  ] });
}
function SourceCitationBadge({ href, children, sources }) {
  const [isHovered, setIsHovered] = reactExports.useState(false);
  const badgeRef = reactExports.useRef(null);
  const [isMobile, setIsMobile] = reactExports.useState(false);
  const [coords, setCoords] = reactExports.useState(null);
  const hoverTimeoutRef = reactExports.useRef(null);
  reactExports.useEffect(() => {
    const checkMobile = () => {
      setIsMobile(window.innerWidth < 768);
    };
    checkMobile();
    window.addEventListener("resize", checkMobile);
    return () => window.removeEventListener("resize", checkMobile);
  }, []);
  const updateCoords = () => {
    if (badgeRef.current) {
      const rect = badgeRef.current.getBoundingClientRect();
      setCoords({
        top: rect.top,
        left: rect.left + rect.width / 2
      });
    }
  };
  reactExports.useEffect(() => {
    if (!isHovered) return;
    const handleScrollOrResize = () => {
      updateCoords();
    };
    window.addEventListener("scroll", handleScrollOrResize, { capture: true, passive: true });
    window.addEventListener("resize", handleScrollOrResize);
    return () => {
      window.removeEventListener("scroll", handleScrollOrResize, { capture: true });
      window.removeEventListener("resize", handleScrollOrResize);
    };
  }, [isHovered]);
  reactExports.useEffect(() => {
    if (!isHovered || !isMobile) return;
    const handleDocumentClick = (e) => {
      if (badgeRef.current && !badgeRef.current.contains(e.target)) {
        setIsHovered(false);
      }
    };
    document.addEventListener("click", handleDocumentClick);
    return () => document.removeEventListener("click", handleDocumentClick);
  }, [isHovered, isMobile]);
  const handleMouseEnter = () => {
    if (hoverTimeoutRef.current) clearTimeout(hoverTimeoutRef.current);
    updateCoords();
    setIsHovered(true);
  };
  const handleMouseLeave = () => {
    hoverTimeoutRef.current = setTimeout(() => {
      setIsHovered(false);
    }, 200);
  };
  const handleBadgeClick = (e) => {
    e.preventDefault();
    e.stopPropagation();
    if (isMobile) {
      updateCoords();
      setIsHovered((prev) => !prev);
    } else {
      window.open(href, "_blank", "noopener,noreferrer");
    }
  };
  const matchingSource = sources?.find((s) => {
    try {
      const url1 = new URL(s.uri).href;
      const url2 = new URL(href).href;
      return url1 === url2 || url1.replace(/\/$/, "") === url2.replace(/\/$/, "");
    } catch (e) {
      return s.uri === href;
    }
  });
  let hostname = "";
  try {
    hostname = new URL(href).hostname;
  } catch (e) {
    hostname = href;
  }
  const cleanHostname = hostname.replace(/^www\./i, "");
  const faviconUrl = matchingSource?.favicon || `https://www.google.com/s2/favicons?domain=${hostname}&sz=64`;
  const titleText = matchingSource?.title || cleanHostname;
  const snippetText = matchingSource?.snippet || "Explore this source for more details and verified information.";
  let sourceName = children ? String(children).trim() : "";
  const isUrl2 = sourceName.startsWith("http") || sourceName.includes("/") || sourceName.includes(".");
  if (!sourceName || /^\d+$/.test(sourceName) || /^source/i.test(sourceName) || sourceName.startsWith("[") || isUrl2) {
    if (matchingSource?.title) {
      const parts = matchingSource.title.split(/[-|—]/);
      sourceName = parts[parts.length - 1]?.trim() || cleanHostname;
      if (sourceName.length > 25) {
        sourceName = cleanHostname;
      }
    } else {
      sourceName = cleanHostname;
    }
  }
  const cleanName = getSourceName(href, sourceName);
  const isPdf = href.toLowerCase().endsWith(".pdf") || href.toLowerCase().includes(".pdf?");
  let displayFavicon = faviconUrl;
  if (isPdf) {
    displayFavicon = "https://cdn-icons-png.flaticon.com/512/337/337946.png";
  }
  let displayTitle = titleText;
  let displaySnippet = snippetText;
  let displayPublisher = cleanName;
  if (isPdf) {
    displayPublisher = "PDF Document";
    try {
      const filename = decodeURIComponent(href.split("/").pop() || "document.pdf").split("?")[0];
      displayTitle = filename;
    } catch (e) {
      displayTitle = "PDF Document";
    }
    displaySnippet = "Portable Document Format (PDF) file containing prep notes or reference material.";
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "span",
    {
      className: cn("ms-1 inline-flex max-w-full items-center select-none relative top-[-0.094rem] translate-y-0.5", isHovered ? "z-[30]" : "z-0"),
      onMouseEnter: handleMouseEnter,
      onMouseLeave: handleMouseLeave,
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "a",
          {
            ref: badgeRef,
            href,
            target: "_blank",
            rel: "noopener noreferrer",
            onClick: handleBadgeClick,
            className: "citation-badge flex items-center overflow-hidden rounded-xl text-[9px] font-medium h-[18px] ps-1 pe-2 transition-colors duration-150 ease-in-out bg-[#F4F4F4] dark:bg-[#303030] text-[#555] dark:text-[#c4c4c4] select-none",
            children: /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "flex items-center gap-1", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "inline-flex shrink-0 items-center justify-center overflow-hidden rounded-full", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                "img",
                {
                  src: displayFavicon,
                  alt: "",
                  className: "h-3 w-3 rounded-full",
                  onError: (e) => {
                    e.currentTarget.src = isPdf ? "https://cdn-icons-png.flaticon.com/512/337/337946.png" : "https://www.google.com/s2/favicons?domain=wikipedia.org";
                  }
                }
              ) }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "max-w-[15ch] grow truncate overflow-hidden text-center leading-none", children: displayPublisher })
            ] })
          }
        ),
        isHovered && /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "div",
          {
            "data-html2canvas-ignore": "true",
            className: cn(
              "fixed bg-[#1a1a1a]/95 backdrop-blur-md border border-[#2d2d2d] rounded-2xl p-4 shadow-[0_10px_30px_rgba(0,0,0,0.6)] z-[9999] text-left pointer-events-auto animate-in fade-in duration-200",
              isMobile ? "bottom-24 left-4 right-4 max-w-sm mx-auto slide-in-from-bottom-2" : "w-80 slide-in-from-bottom-1"
            ),
            style: !isMobile && coords ? {
              top: `${coords.top - 8}px`,
              left: `${coords.left}px`,
              transform: "translate(-50%, -100%)"
            } : void 0,
            onMouseEnter: handleMouseEnter,
            onMouseLeave: handleMouseLeave,
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 mb-2", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "h-5 w-5 rounded bg-white border border-slate-800 flex items-center justify-center overflow-hidden shrink-0", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "img",
                  {
                    src: displayFavicon,
                    alt: "",
                    className: "h-3.5 w-3.5 object-contain",
                    onError: (e) => {
                      e.currentTarget.src = "https://www.google.com/s2/favicons?domain=wikipedia.org";
                    }
                  }
                ) }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[11px] font-bold text-slate-400 uppercase tracking-wider", children: displayPublisher }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-[10px] text-slate-500 ml-auto flex items-center gap-0.5", children: [
                  "Verified Link ",
                  /* @__PURE__ */ jsxRuntimeExports.jsx(ExternalLink, { className: "h-2.5 w-2.5 inline" })
                ] })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "a",
                {
                  href,
                  target: "_blank",
                  rel: "noopener noreferrer",
                  className: "text-xs font-bold text-white hover:text-blue-400 transition-colors leading-snug mb-1.5 line-clamp-2 block",
                  children: displayTitle
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-[11px] text-slate-400 leading-relaxed line-clamp-3", children: displaySnippet })
            ]
          }
        )
      ]
    }
  );
}
const getMarkdownComponents = (setFullScreenImage, sources) => ({
  table: ({ node: node2, children, ...props }) => {
    return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-full overflow-x-auto my-4 border border-border/80 rounded-xl scrollbar-hide", children: /* @__PURE__ */ jsxRuntimeExports.jsx("table", { className: "w-full border-collapse", ...props, children }) });
  },
  a: ({ node: node2, children, href, ...props }) => {
    const isImageLink = node2?.children?.length === 1 && node2.children[0].tagName === "img";
    if (isImageLink) {
      return /* @__PURE__ */ jsxRuntimeExports.jsxs("a", { href, target: "_blank", rel: "noopener noreferrer", className: "inline-block transition-transform hover:scale-[1.02] m-2 align-top group relative", ...props, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "img",
          {
            src: node2.children[0].properties.src,
            alt: node2.children[0].properties.alt || "Thumbnail",
            className: "w-full h-auto object-cover max-h-[350px] bg-muted rounded-xl border border-border"
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute inset-0 bg-black/40 opacity-0 group-hover:opacity-100 transition-opacity rounded-xl flex items-center justify-center pointer-events-none", children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-white font-bold text-sm bg-black/60 px-3 py-1.5 rounded-full backdrop-blur-sm", children: "Open Link" }) })
      ] });
    }
    const ytRegex = /(?:https?:\/\/)?(?:www\.)?(?:youtube\.com\/(?:[^\/\n\s]+\/\S+\/|(?:v|e(?:mbed)?)\/|\S*?[?&]v=)|youtu\.be\/)([a-zA-Z0-9_-]{11})/;
    const ytMatch = href?.match(ytRegex);
    if (ytMatch && ytMatch[1]) {
      const videoId = ytMatch[1];
      const textContent = children ? String(children).trim() : "";
      const isInlineCitation = !textContent || textContent.toLowerCase() === "youtube" || /^(youtube\s*\+\d+|source\s*\d+|\d+)$/i.test(textContent) || textContent.length < 15;
      if (isInlineCitation) {
        return /* @__PURE__ */ jsxRuntimeExports.jsx(SourceCitationBadge, { href, sources, children: children || "YouTube" });
      }
      return /* @__PURE__ */ jsxRuntimeExports.jsx(YoutubePreview, { videoId, href, children });
    }
    if (href && (href.startsWith("http://") || href.startsWith("https://"))) {
      return /* @__PURE__ */ jsxRuntimeExports.jsx(SourceCitationBadge, { href, sources, children });
    }
    return /* @__PURE__ */ jsxRuntimeExports.jsx("a", { href, target: "_blank", rel: "noopener noreferrer", className: "text-blue-500 hover:text-blue-600 underline transition-colors font-medium break-words", ...props, children });
  },
  img: ({ src, alt, ...props }) => {
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "relative group inline-flex m-2 align-top max-w-full", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "img",
        {
          src,
          alt,
          className: "w-full h-auto object-cover max-h-[350px] bg-muted rounded-xl border border-border cursor-pointer transition-transform hover:scale-[1.02]",
          onClick: (e) => {
            e.preventDefault();
            e.stopPropagation();
            if (setFullScreenImage) setFullScreenImage(src);
          },
          ...props
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "a",
        {
          href: src,
          download: `image_${Date.now()}.png`,
          target: "_blank",
          rel: "noreferrer",
          onClick: (e) => e.stopPropagation(),
          className: "absolute top-2 right-2 p-2 bg-black/60 rounded-full opacity-0 group-hover:opacity-100 transition-opacity hover:bg-black/80 text-white shadow-md backdrop-blur-sm z-10",
          children: /* @__PURE__ */ jsxRuntimeExports.jsx(Download, { className: "h-4 w-4" })
        }
      )
    ] });
  },
  code: ({ node: node2, className, children, ...props }) => {
    const match = /language-(\w+)/.exec(className || "");
    const codeContent = String(children).replace(/\n$/, "");
    let lang = match ? match[1] : "";
    if (!lang || lang === "json") {
      try {
        const data = JSON.parse(codeContent);
        if (typeof data === "object" && data !== null) {
          if (data.type === "simulation" || data.type && ["projectile", "density", "electricity", "bohr", "bonding", "shm"].includes(data.type)) {
            lang = "simulation";
          } else if (data.functions || data.formula || data.type === "plot") {
            lang = "graph";
          } else if (data.videoId || data.creator) {
            lang = "youtube-card";
          } else if (data.items || data.recommendations) {
            lang = "news-feed";
          } else if (data.question && data.options && data.answer) {
            lang = "interactive-quiz";
          }
        }
      } catch (e) {
      }
    }
    if (lang) {
      if (lang === "simulation") {
        try {
          const data = JSON.parse(codeContent);
          return /* @__PURE__ */ jsxRuntimeExports.jsx(SimulationWidget, { type: data.type });
        } catch (e) {
          return /* @__PURE__ */ jsxRuntimeExports.jsx("pre", { className: "bg-slate-900 border border-slate-800 p-4 rounded-xl overflow-x-auto", children: /* @__PURE__ */ jsxRuntimeExports.jsx("code", { className, ...props, children }) });
        }
      }
      if (lang === "graph" || lang === "plot") {
        try {
          const data = JSON.parse(codeContent);
          let funcs = data.functions;
          if (!funcs && data.formula) {
            funcs = [data.formula.replace(/^y\s*=\s*/i, "")];
          }
          if (!funcs && typeof data === "object" && data !== null) {
            const values = Object.values(data).filter((v) => typeof v === "string" && (v.includes("x") || v.includes("sin") || v.includes("cos") || v.includes("tan")));
            if (values.length > 0) {
              funcs = [values[0].replace(/^y\s*=\s*/i, "")];
            }
          }
          return /* @__PURE__ */ jsxRuntimeExports.jsx(
            GraphWidget,
            {
              functions: funcs || ["sin(x)"],
              xRange: data.xRange || [-10, 10],
              yRange: data.yRange || [-6, 6],
              sliders: data.sliders || []
            }
          );
        } catch (e) {
          return /* @__PURE__ */ jsxRuntimeExports.jsx("pre", { className: "bg-slate-900 border border-slate-800 p-4 rounded-xl overflow-x-auto", children: /* @__PURE__ */ jsxRuntimeExports.jsx("code", { className, ...props, children }) });
        }
      }
      if (lang === "youtube-card") {
        try {
          const data = JSON.parse(codeContent);
          return /* @__PURE__ */ jsxRuntimeExports.jsx(
            YouTubeCardWidget,
            {
              videoId: data.videoId,
              title: data.title,
              creator: data.creator,
              views: data.views,
              uploadDate: data.uploadDate,
              rating: data.rating,
              whyRecommend: data.whyRecommend
            }
          );
        } catch (e) {
          return /* @__PURE__ */ jsxRuntimeExports.jsx("pre", { className: "bg-slate-900 border border-slate-800 p-4 rounded-xl overflow-x-auto", children: /* @__PURE__ */ jsxRuntimeExports.jsx("code", { className, ...props, children }) });
        }
      }
      if (lang === "news-feed") {
        try {
          const data = JSON.parse(codeContent);
          return /* @__PURE__ */ jsxRuntimeExports.jsx(NewsFeedWidget, { items: data.items, recommendations: data.recommendations });
        } catch (e) {
          return /* @__PURE__ */ jsxRuntimeExports.jsx("pre", { className: "bg-slate-900 border border-slate-800 p-4 rounded-xl overflow-x-auto", children: /* @__PURE__ */ jsxRuntimeExports.jsx("code", { className, ...props, children }) });
        }
      }
      if (lang === "interactive-quiz") {
        try {
          const data = JSON.parse(codeContent);
          return /* @__PURE__ */ jsxRuntimeExports.jsx(
            InteractiveQuizWidget,
            {
              question: data.question,
              options: data.options,
              answer: data.answer,
              explanation: data.explanation
            }
          );
        } catch (e) {
          return /* @__PURE__ */ jsxRuntimeExports.jsx("pre", { className: "bg-slate-900 border border-slate-800 p-4 rounded-xl overflow-x-auto", children: /* @__PURE__ */ jsxRuntimeExports.jsx("code", { className, ...props, children }) });
        }
      }
    }
    const isInline = !className;
    return isInline ? /* @__PURE__ */ jsxRuntimeExports.jsx("code", { className: "bg-muted px-1.5 py-0.5 rounded font-mono text-xs text-rose-500 dark:text-rose-400", ...props, children }) : /* @__PURE__ */ jsxRuntimeExports.jsx("pre", { className: "bg-muted border border-border p-4 rounded-xl overflow-x-auto max-w-full my-2", children: /* @__PURE__ */ jsxRuntimeExports.jsx("code", { className, ...props, children }) });
  }
});
function ChatImageEditor({
  attachment,
  onSave,
  onClose
}) {
  const canvasRef = reactExports.useRef(null);
  const [penColor, setPenColor] = reactExports.useState("#EF4444");
  const [penSize, setPenSize] = reactExports.useState(3);
  const [drawing, setDrawing] = reactExports.useState(false);
  const lastPos = reactExports.useRef(null);
  reactExports.useEffect(() => {
    if (!canvasRef.current) return;
    const canvas = canvasRef.current;
    const ctx = canvas.getContext("2d");
    const img = new Image();
    img.crossOrigin = "anonymous";
    img.src = attachment.url;
    img.onload = () => {
      const maxWidth = window.innerWidth * 0.8;
      const maxHeight = window.innerHeight * 0.7;
      let width = img.width;
      let height = img.height;
      if (width > maxWidth) {
        height = Math.round(height * maxWidth / width);
        width = maxWidth;
      }
      if (height > maxHeight) {
        width = Math.round(width * maxHeight / height);
        height = maxHeight;
      }
      canvas.width = width;
      canvas.height = height;
      ctx.drawImage(img, 0, 0, width, height);
    };
  }, [attachment.url]);
  const getPos = (e) => {
    const rect = canvasRef.current.getBoundingClientRect();
    const scaleX = canvasRef.current.width / rect.width;
    const scaleY = canvasRef.current.height / rect.height;
    let clientX, clientY;
    if ("touches" in e) {
      clientX = e.touches[0].clientX;
      clientY = e.touches[0].clientY;
    } else {
      clientX = e.clientX;
      clientY = e.clientY;
    }
    return {
      x: (clientX - rect.left) * scaleX,
      y: (clientY - rect.top) * scaleY
    };
  };
  const onPointerDown = (e) => {
    setDrawing(true);
    lastPos.current = getPos(e);
  };
  const onPointerMove = (e) => {
    if (!drawing || !canvasRef.current || !lastPos.current) return;
    const ctx = canvasRef.current.getContext("2d");
    const pos = getPos(e);
    ctx.strokeStyle = penColor;
    ctx.lineWidth = penSize;
    ctx.lineCap = "round";
    ctx.lineJoin = "round";
    ctx.beginPath();
    ctx.moveTo(lastPos.current.x, lastPos.current.y);
    ctx.lineTo(pos.x, pos.y);
    ctx.stroke();
    lastPos.current = pos;
  };
  const onPointerUp = () => {
    setDrawing(false);
    lastPos.current = null;
  };
  const handleSave = () => {
    if (!canvasRef.current) return;
    canvasRef.current.toBlob((blob) => {
      if (blob) onSave(blob);
    }, attachment.file.type || "image/png");
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "fixed inset-0 z-[500] flex items-center justify-center p-4", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute inset-0 bg-black/80 backdrop-blur-sm", onClick: () => onClose() }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(
      motion.div,
      {
        initial: { opacity: 0, scale: 0.95 },
        animate: { opacity: 1, scale: 1 },
        exit: { opacity: 0, scale: 0.95 },
        className: "relative bg-card border border-border rounded-2xl shadow-2xl z-10 flex flex-col max-h-[90vh]",
        onClick: (e) => e.stopPropagation(),
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between p-3 border-b border-border shrink-0", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-sm font-bold text-foreground flex items-center gap-2", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(Pencil, { className: "h-4 w-4 text-primary" }),
              "Draw & Annotate"
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-4 hidden sm:flex", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex items-center gap-1.5", children: ["#EF4444", "#3B82F6", "#22C55E", "#F59E0B", "#FFFFFF", "#000000"].map((c) => /* @__PURE__ */ jsxRuntimeExports.jsx(
                "button",
                {
                  onClick: () => setPenColor(c),
                  className: "w-5 h-5 rounded-full border-2 transition-transform hover:scale-110",
                  style: { backgroundColor: c, borderColor: penColor === c ? "#8B5CF6" : "transparent" }
                },
                c
              )) }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-1.5 h-1.5 rounded-full bg-foreground" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "input",
                  {
                    type: "range",
                    min: "1",
                    max: "20",
                    value: penSize,
                    onChange: (e) => setPenSize(+e.target.value),
                    className: "w-20 accent-primary"
                  }
                ),
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-3 h-3 rounded-full bg-foreground" })
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex gap-2", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs(Button, { size: "sm", onClick: handleSave, className: "h-8 text-xs gap-1.5", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Check, { className: "h-3 w-3" }),
                " Save"
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { onClick: onClose, className: "p-1 hover:bg-muted rounded text-muted-foreground hover:text-foreground", children: /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "h-5 w-5" }) })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex-1 overflow-auto p-4 flex items-center justify-center bg-muted/30 touch-none", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
            "canvas",
            {
              ref: canvasRef,
              className: "rounded-lg shadow-md cursor-crosshair border border-border bg-white",
              style: { touchAction: "none", maxWidth: "100%", maxHeight: "100%" },
              onMouseDown: onPointerDown,
              onMouseMove: onPointerMove,
              onMouseUp: onPointerUp,
              onMouseLeave: onPointerUp,
              onTouchStart: onPointerDown,
              onTouchMove: onPointerMove,
              onTouchEnd: onPointerUp,
              onTouchCancel: onPointerUp
            }
          ) }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "sm:hidden p-3 border-t border-border flex items-center justify-between gap-2 shrink-0 overflow-x-auto", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex items-center gap-1.5", children: ["#EF4444", "#3B82F6", "#22C55E", "#F59E0B", "#FFFFFF", "#000000"].map((c) => /* @__PURE__ */ jsxRuntimeExports.jsx(
              "button",
              {
                onClick: () => setPenColor(c),
                className: "w-5 h-5 rounded-full border-2 shrink-0 transition-transform hover:scale-110",
                style: { backgroundColor: c, borderColor: penColor === c ? "#8B5CF6" : "transparent" }
              },
              c
            )) }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex items-center gap-2", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
              "input",
              {
                type: "range",
                min: "1",
                max: "20",
                value: penSize,
                onChange: (e) => setPenSize(+e.target.value),
                className: "w-20 accent-primary"
              }
            ) })
          ] })
        ]
      }
    )
  ] });
}
function getSourceName(uri, title) {
  let hostname = "";
  try {
    hostname = new URL(uri).hostname.toLowerCase();
  } catch (e) {
    return title || "Source";
  }
  if (hostname.includes("indianexpress.com")) return "The Indian Express";
  if (hostname.includes("abplive.com")) return "ABP Live";
  if (hostname.includes("aajtak.in")) return "Aaj Tak";
  if (hostname.includes("republicworld.com")) return "Republic World";
  if (hostname.includes("tv9bharatvarsh.com")) return "TV9 Bharatvarsh";
  if (hostname.includes("ddnews.gov.in")) return "DD News";
  if (hostname.includes("foxnews.com")) return "Fox News";
  if (hostname.includes("bbc.com") || hostname.includes("bbc.co.uk")) return "BBC News";
  if (hostname.includes("indiatvnews.com")) return "India TV";
  if (hostname.includes("thehindu.com")) return "The Hindu";
  if (hostname.includes("news18.com")) return "News18";
  if (hostname.includes("zeenews.india.com")) return "Zee News";
  if (hostname.includes("moneycontrol.com")) return "Moneycontrol";
  if (hostname.includes("jagran.com")) return "Dainik Jagran";
  if (hostname.includes("bhaskar.com")) return "Dainik Bhaskar";
  if (hostname.includes("careers360.com")) return "Careers360";
  if (hostname.includes("sarvgyan.com")) return "SarvGyan";
  if (hostname.includes("josaa.nic.in")) return "JoSAA";
  if (hostname.includes("collegedunia.com")) return "Collegedunia";
  if (hostname.includes("timesofindia")) return "Times of India";
  if (hostname.includes("hindustantimes.com")) return "Hindustan Times";
  if (hostname.includes("ndtv.com")) return "NDTV";
  if (hostname.includes("theprint.in")) return "ThePrint";
  if (hostname.includes("livemint.com")) return "Mint";
  if (hostname.includes("businesstoday.in")) return "Business Today";
  if (hostname.includes("business-standard.com")) return "Business Standard";
  if (hostname.includes("nta.ac.in")) return "NTA";
  if (hostname.includes("jeeadv.ac.in")) return "JEE Advanced";
  if (hostname.includes("wikipedia.org")) return "Wikipedia";
  if (hostname.includes("youtube.com")) return "YouTube";
  if (hostname.includes("eduniti.in")) return "Eduniti";
  const parts = hostname.replace("www.", "").split(".");
  if (parts.length > 0) {
    return parts[0].charAt(0).toUpperCase() + parts[0].slice(1);
  }
  return "Source";
}
const preprocessMarkdown = (content2, sources) => {
  if (!content2) return content2;
  let cleaned = content2;
  if (sources && sources.length > 0) {
    cleaned = cleaned.replace(/\[Source\s+(\d+)\]/gi, (match, numStr) => {
      const idx = parseInt(numStr, 10) - 1;
      if (idx >= 0 && idx < sources.length) {
        return `[Source ${idx + 1}](${sources[idx].uri})`;
      }
      return match;
    });
    cleaned = cleaned.replace(/\[(\d+)\]/g, (match, numStr) => {
      const idx = parseInt(numStr, 10) - 1;
      if (idx >= 0 && idx < sources.length) {
        let title = "";
        try {
          const hostname = new URL(sources[idx].uri).hostname;
          title = hostname.replace(/^www\./i, "");
        } catch (e) {
          title = sources[idx].title || `Source ${idx + 1}`;
        }
        return `[${title}](${sources[idx].uri})`;
      }
      return match;
    });
  }
  const toolCallRegex = /<\|tool_call_start\|>\[calculus\([\s\S]*?code="([^"]+)"[\s\S]*?\)\](?:<\|tool_call_end\|>)?/g;
  cleaned = cleaned.replace(toolCallRegex, (match, codeAttr) => {
    let func = "sin(x)";
    const mathMatch = codeAttr.match(/,\s*([a-z0-9_().**+\-/^]+)\s*,/i);
    if (mathMatch) {
      func = mathMatch[1];
    } else {
      const params = codeAttr.split(",");
      if (params.length > 1) {
        func = params[1].trim();
      }
    }
    return `
\`\`\`graph
{
  "functions": ["${func}"]
}
\`\`\`
`;
  });
  const calculusRegex = /\[calculus\s+question="([^"]+)"[\s\S]*?code="([^"]+)"[\s\S]*?\]/g;
  cleaned = cleaned.replace(calculusRegex, (match, questionAttr, codeAttr) => {
    let func = "sin(x)";
    const eqMatch = questionAttr.match(/y\s*=\s*([a-z0-9_().**+\-/^ ]+)/i);
    if (eqMatch) {
      func = eqMatch[1].trim();
    } else {
      const params = codeAttr.split(",");
      if (params.length > 1) {
        func = params[1].trim();
      }
    }
    return `
\`\`\`graph
{
  "functions": ["${func}"]
}
\`\`\`
`;
  });
  let scanIndex = 0;
  while (true) {
    const openBraceIdx = cleaned.indexOf("{", scanIndex);
    if (openBraceIdx === -1) break;
    const beforeText = cleaned.substring(0, openBraceIdx);
    const backtickCount = (beforeText.match(/```/g) || []).length;
    if (backtickCount % 2 !== 0) {
      scanIndex = openBraceIdx + 1;
      continue;
    }
    let depth = 1;
    let closeBraceIdx = -1;
    let inString = false;
    let escape = false;
    for (let i = openBraceIdx + 1; i < cleaned.length; i++) {
      const char = cleaned[i];
      if (escape) {
        escape = false;
        continue;
      }
      if (char === "\\") {
        escape = true;
        continue;
      }
      if (char === '"') {
        inString = !inString;
        continue;
      }
      if (!inString) {
        if (char === "{") depth++;
        else if (char === "}") depth--;
        if (depth === 0) {
          closeBraceIdx = i;
          break;
        }
      }
    }
    if (closeBraceIdx === -1) {
      scanIndex = openBraceIdx + 1;
      continue;
    }
    const jsonStr = cleaned.substring(openBraceIdx, closeBraceIdx + 1);
    try {
      const parsed = JSON.parse(jsonStr);
      if (typeof parsed === "object" && parsed !== null) {
        let lang = "";
        if (parsed.functions || parsed.formula || parsed.type === "plot") {
          lang = "graph";
        } else if (parsed.type === "simulation" || parsed.type && ["projectile", "density", "electricity", "bohr", "bonding", "shm"].includes(parsed.type)) {
          lang = "simulation";
        } else if (parsed.videoId || parsed.creator) {
          lang = "youtube-card";
        } else if (parsed.items || parsed.recommendations) {
          lang = "news-feed";
        } else if (parsed.question && parsed.options && parsed.answer) {
          lang = "interactive-quiz";
        }
        if (lang) {
          const replacement = `
\`\`\`${lang}
${jsonStr.trim()}
\`\`\`
`;
          let startReplaceIdx = openBraceIdx;
          while (startReplaceIdx > 0 && (cleaned[startReplaceIdx - 1] === " " || cleaned[startReplaceIdx - 1] === "	")) {
            startReplaceIdx--;
          }
          cleaned = cleaned.substring(0, startReplaceIdx) + replacement + cleaned.substring(closeBraceIdx + 1);
          scanIndex = startReplaceIdx + replacement.length;
          continue;
        }
      }
    } catch (e) {
    }
    scanIndex = openBraceIdx + 1;
  }
  cleaned = cleaned.replace(/<\|tool_call_start\|>/g, "");
  cleaned = cleaned.replace(/<\|tool_call_end\|>/g, "");
  return cleaned;
};
function TypewriterMarkdown({ content: content2, isTyping, onComplete, setFullScreenImage, sources }) {
  const [displayed, setDisplayed] = reactExports.useState(isTyping ? "" : content2);
  reactExports.useEffect(() => {
    if (!isTyping) {
      setDisplayed(content2);
      return;
    }
    let i = 0;
    const interval = setInterval(() => {
      setDisplayed(content2.slice(0, i));
      i += 12;
      window.dispatchEvent(new Event("chat-typing"));
      if (i >= content2.length + 12) {
        clearInterval(interval);
        setDisplayed(content2);
        onComplete?.();
      }
    }, 15);
    return () => clearInterval(interval);
  }, [content2, isTyping, onComplete]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Markdown, { remarkPlugins: [remarkMath, remarkGfm], rehypePlugins: [rehypeKatex], components: getMarkdownComponents(setFullScreenImage, sources), children: preprocessMarkdown(displayed, sources) });
}
const POPULAR_NEWS_SOURCES = [
  { names: ["aajtak", "aaj tak"], domain: "aajtak.in" },
  { names: ["republic world", "republicworld", "republic tv", "republic"], domain: "republicworld.com" },
  { names: ["abp news", "abplive", "abp live", "abpnews", "abp"], domain: "abplive.com" },
  { names: ["indiatv", "india tv", "indiatvnews"], domain: "indiatvnews.com" },
  { names: ["tv9 bharatverse", "tv9 bharatvarsh", "tv9"], domain: "tv9bharatvarsh.com" },
  { names: ["bbc", "bbc news", "bbcnews"], domain: "bbc.com" },
  { names: ["fox news", "foxnews", "fox"], domain: "foxnews.com" },
  { names: ["dd news", "ddnews", "dd"], domain: "ddnews.gov.in" },
  { names: ["hindustan times", "hindustantimes", "ht"], domain: "hindustantimes.com" },
  { names: ["ndtv"], domain: "ndtv.com" },
  { names: ["times of india", "toi"], domain: "timesofindia.indiatimes.com" },
  { names: ["indian express", "indianexpress"], domain: "indianexpress.com" },
  { names: ["the hindu"], domain: "thehindu.com" },
  { names: ["news18"], domain: "news18.com" },
  { names: ["zee news", "zeenews"], domain: "zeenews.india.com" },
  { names: ["livemint", "mint"], domain: "livemint.com" },
  { names: ["moneycontrol"], domain: "moneycontrol.com" },
  { names: ["jagran", "dainik jagran"], domain: "jagran.com" },
  { names: ["bhaskar", "dainik bhaskar"], domain: "bhaskar.com" },
  { names: ["cnn"], domain: "cnn.com" },
  { names: ["nytimes", "new york times"], domain: "nytimes.com" },
  { names: ["reuters"], domain: "reuters.com" },
  { names: ["ap news", "apnews"], domain: "apnews.com" },
  { names: ["al jazeera", "aljazeera"], domain: "aljazeera.com" },
  { names: ["guardian", "the guardian"], domain: "theguardian.com" },
  { names: ["washington post", "washingtonpost"], domain: "washingtonpost.com" },
  { names: ["bloomberg"], domain: "bloomberg.com" },
  { names: ["cnbc"], domain: "cnbc.com" },
  { names: ["forbes"], domain: "forbes.com" },
  { names: ["wsj", "wall street journal"], domain: "wsj.com" }
];
function getCleanQueryAndSources(prompt) {
  const lowercasePrompt = prompt.toLowerCase();
  const matchedDomains = /* @__PURE__ */ new Set();
  for (const src of POPULAR_NEWS_SOURCES) {
    for (const name2 of src.names) {
      const regex = new RegExp(`\\b${name2.replace(/[-\/\\^$*+?.()|[\]{}]/g, "\\$&")}\\b`, "gi");
      if (regex.test(lowercasePrompt)) {
        matchedDomains.add(src.domain);
        break;
      }
    }
  }
  const domainRegex = /\b([a-z0-9-]+\.[a-z]{2,6})\b/gi;
  let match;
  while ((match = domainRegex.exec(prompt)) !== null) {
    const dom = match[1].toLowerCase();
    matchedDomains.add(dom);
  }
  const listMatches = prompt.match(/(?:sources|websites|sites)\s*:\s*([^.?!]+)/i);
  if (listMatches) {
    const listContent = listMatches[1];
    const items = listContent.split(/,|\band\b/).map((s) => s.trim().toLowerCase()).filter(Boolean);
    for (const item of items) {
      if (item.length > 2 && item.length < 30 && !["other news source", "other news sources", "other websites", "others", "other"].includes(item)) {
        let found = false;
        for (const src of POPULAR_NEWS_SOURCES) {
          if (src.names.includes(item) || item.includes(src.domain)) {
            matchedDomains.add(src.domain);
            found = true;
            break;
          }
        }
        if (!found) {
          matchedDomains.add(item);
        }
      }
    }
  }
  let cleanQuery = prompt;
  cleanQuery = cleanQuery.replace(/(?:use\s+these\s+websites?\s+as\s+sources?|using\s+these\s+websites?\s+as\s+sources?|sources?\s*:\s*|websites?\s*:\s*)/gi, "");
  for (const src of POPULAR_NEWS_SOURCES) {
    for (const name2 of src.names) {
      const regex = new RegExp(`\\b${name2.replace(/[-\/\\^$*+?.()|[\]{}]/g, "\\$&")}\\b`, "gi");
      cleanQuery = cleanQuery.replace(regex, "");
    }
  }
  cleanQuery = cleanQuery.replace(/and\s+other\s+news\s+sources?/gi, "");
  cleanQuery = cleanQuery.replace(/and\s+other\s+sources?/gi, "");
  cleanQuery = cleanQuery.replace(/and\s+other\s+websites?/gi, "");
  cleanQuery = cleanQuery.replace(/[\s,;.-]+/g, " ").trim();
  if (cleanQuery.split(/\s+/).filter(Boolean).length < 2) {
    cleanQuery = prompt;
  }
  return {
    cleanQuery,
    sources: Array.from(matchedDomains)
  };
}
async function fetchWebSearchResults(query, timeFilter) {
  try {
    let url = `/api/search?q=${encodeURIComponent(query)}`;
    if (timeFilter) {
      url += `&df=${timeFilter}`;
    }
    const res = await fetch(url);
    if (!res.ok) throw new Error("Search request failed");
    const data = await res.json();
    const results = data.results || [];
    if (results.length === 0) {
      return { results: [], text: "No search results found." };
    }
    let searchSummaries = "";
    results.forEach((item, idx) => {
      searchSummaries += `[Source ${idx + 1}] Title: ${item.title}
URL: ${item.url}
Snippet: ${item.snippet}
Thumbnail: ${item.thumbnail || ""}

`;
    });
    const mappedResults = results.map((item) => ({
      url: item.url,
      title: item.title,
      snippet: item.snippet,
      thumbnail: item.thumbnail
    }));
    return { results: mappedResults, text: searchSummaries };
  } catch (error) {
    console.warn("Local search API failed or backend server is not running. Falling back to direct client-side search via CORS proxy:", error);
    try {
      const encoded = encodeURIComponent(query);
      let searchUrl = `https://html.duckduckgo.com/html/?q=${encoded}`;
      if (timeFilter) {
        searchUrl += `&df=${timeFilter}`;
      }
      const proxyUrl = `https://corsproxy.io/?${encodeURIComponent(searchUrl)}`;
      const res = await fetch(proxyUrl);
      if (!res.ok) throw new Error("CORS proxy search request failed");
      const html2 = await res.text();
      if (!html2.includes("result results_links")) {
        return { results: [], text: "No search results found." };
      }
      const decodeHTMLEntities = (str) => {
        return str.replace(/&amp;/g, "&").replace(/&lt;/g, "<").replace(/&gt;/g, ">").replace(/&quot;/g, '"').replace(/&#39;/g, "'").replace(/&#x27;/g, "'").replace(/&#x2F;/g, "/").replace(/&ndash;/g, "–").replace(/&mdash;/g, "—").replace(/&nbsp;/g, " ");
      };
      const parts = html2.split('class="result results_links');
      const results = parts.slice(1).map((part) => {
        const hrefMatch = part.match(/class="result__a"[^>]*href="([^"]+)"/);
        const titleMatch = part.match(/class="result__a"[^>]*>([\s\S]*?)<\/a>/);
        const snippetMatch = part.match(/class="result__snippet"[^>]*>([\s\S]*?)<\/a>/);
        let url = hrefMatch ? hrefMatch[1] : "";
        if (url.startsWith("//")) {
          url = "https:" + url;
        }
        if (url.startsWith("/l/") || url.includes("uddg=")) {
          const match = url.match(/[?&]uddg=([^&]+)/);
          if (match) {
            url = decodeURIComponent(match[1]);
          }
        }
        let title = titleMatch ? titleMatch[1].replace(/<[^>]*>/g, "").trim() : "";
        let snippet = snippetMatch ? snippetMatch[1].replace(/<[^>]*>/g, "").trim() : "";
        title = decodeHTMLEntities(title);
        snippet = decodeHTMLEntities(snippet);
        return { url, title, snippet };
      });
      if (results.length === 0) {
        return { results: [], text: "No search results found." };
      }
      let searchSummaries = "";
      results.forEach((item, idx) => {
        searchSummaries += `[Source ${idx + 1}] Title: ${item.title}
URL: ${item.url}
Snippet: ${item.snippet}
Thumbnail: 

`;
      });
      return { results, text: searchSummaries };
    } catch (fallbackError) {
      console.error("Fallback direct web search failed:", fallbackError);
      return { results: [], text: "Failed to retrieve search results." };
    }
  }
}
async function fetchThirdPartyAiVideoAnalysis(videoId, rawText) {
  const apiKey = localStorage.getItem("jee_openrouter_api_key") || "";
  if (!apiKey) return "";
  const prompt = `You are a YouTube video analyzer and multi-source verification expert.
VIDEO URL: https://www.youtube.com/watch?v=${videoId}
RAW DATA:
${rawText}

Please perform the following verification and analysis:
1. **Identify the True Video Topic:** Determine the exact show, anime, or topic based on the video title and description metadata (e.g. "SPY×FAMILY - Episode 36 (S2E11) [Hindi dub]").
2. **Cross-Validate Across Sources:** Analyze all the web search results in the RAW DATA. Cross-reference the plot, characters (e.g., Loid, Yor, Anya, Becky, Fiona Frost), and events to verify which details are consistent across multiple sources. Ignore and filter out any search matches or summaries that belong to different shows or topics.
3. **Generate AI Analyzer Reports:** Using ONLY the verified and cross-checked facts for the identified video topic, output the following structured reports:
   - **YTSummary.app (Core Insights):** A concise bullet outline of verified highlights and key takeaways.
   - **NoteGPT.io (Structured Notes & Plot Progression):** Detailed, verified summaries of chapters, themes, and characters.
   - **Recall.ai (Multi-Source Verification Audit):** A checklist of verified facts, noting the sources they match, to ensure 100% correct, hallucination-free summaries.

Do not complain about missing transcripts. Retain only verified, consistent facts. Format the output with clear markdown headers for each source.`;
  const reqBody = {
    model: "google/gemma-2-9b-it:free",
    messages: [{ role: "user", content: prompt }]
  };
  try {
    let response = await fetch("https://openrouter.ai/api/v1/chat/completions", {
      method: "POST",
      headers: {
        "Authorization": `Bearer ${apiKey.trim()}`,
        "HTTP-Referer": window.location.href,
        "X-Title": "StudE",
        "Content-Type": "application/json"
      },
      body: JSON.stringify(reqBody)
    });
    if (!response.ok) {
      response = await fetch(`https://corsproxy.io/?${encodeURIComponent("https://openrouter.ai/api/v1/chat/completions")}`, {
        method: "POST",
        headers: {
          "Authorization": `Bearer ${apiKey.trim()}`,
          "HTTP-Referer": window.location.href,
          "X-Title": "StudE",
          "Content-Type": "application/json"
        },
        body: JSON.stringify(reqBody)
      });
    }
    if (response.ok) {
      const data = await response.json();
      return data.choices?.[0]?.message?.content || "";
    }
  } catch (e) {
    console.error("Failed to fetch 3rd party AI video analysis:", e);
  }
  return "";
}
async function fetchCrawlResults(targetUrl, deep) {
  let combinedText = "";
  const crawledUrls = [targetUrl];
  const ytRegex = /(?:https?:\/\/)?(?:www\.)?(?:youtube\.com\/(?:[^\/\n\s]+\/\S+\/|(?:v|e(?:mbed)?)\/|\S*?[?&]v=)|youtu\.be\/)([a-zA-Z0-9_-]{11})/;
  const ytMatch = targetUrl.match(ytRegex);
  if (ytMatch && ytMatch[1]) {
    const videoId = ytMatch[1];
    let rawText = "";
    try {
      const res = await fetch(`/api/scrape?url=${encodeURIComponent(targetUrl)}`);
      if (res.ok) {
        const data = await res.json();
        if (data.text && !data.text.includes("Failed to scrape") && !data.text.includes("Failed to retrieve")) {
          rawText = data.text;
        }
      }
    } catch (err) {
      console.warn("Backend scraper failed for YouTube URL, attempting client-side extraction...", err);
    }
    if (!rawText) {
      let metaText = "";
      let transcriptText = "";
      let htmlTitle = "";
      let htmlAuthor = "";
      let htmlKeywords = "";
      try {
        const mirrors = [
          "https://inv.thepixora.com",
          "https://invidious.f5.si",
          "https://invidious.tiekoetter.com"
        ];
        for (const mirror of mirrors) {
          try {
            const vres = await fetch(`${mirror}/api/v1/videos/${videoId}`, { signal: AbortSignal.timeout(5e3) });
            if (vres.ok) {
              const data = await vres.json();
              if (data && data.title) {
                metaText = `=== YOUTUBE VIDEO METADATA ===
URL: ${targetUrl}
Title: ${data.title}
Channel Name: ${data.author || "Unknown"} (URL: ${data.authorUrl || "N/A"})
Upload Date: ${data.publishedText || "N/A"}
Duration: ${data.lengthSeconds || 0} seconds
Views: ${data.viewCount || 0}
Likes: ${data.likeCount || 0}
Description:
${data.description || "No description provided."}
`;
                break;
              }
            }
          } catch (e) {
          }
        }
      } catch (e) {
      }
      try {
        const tres = await fetch(`https://youtube-transcript.ai/transcript/${videoId}.txt`, { signal: AbortSignal.timeout(8e3) });
        if (tres.ok) {
          const contentType = tres.headers.get("content-type") || "";
          const body = await tres.text();
          if (!contentType.includes("text/html") && !body.trim().startsWith("<!DOCTYPE")) {
            transcriptText = body;
          }
        }
      } catch (e) {
      }
      if (!metaText || !transcriptText) {
        try {
          const proxyUrl = `https://corsproxy.io/?${encodeURIComponent(`https://www.youtube.com/watch?v=${videoId}`)}`;
          const ytPageRes = await fetch(proxyUrl, { signal: AbortSignal.timeout(6e3) });
          if (ytPageRes.ok) {
            const html2 = await ytPageRes.text();
            const titleMatch = html2.match(/<meta\s+name=["']title["']\s+content=["']([^"']+)["']/i) || html2.match(/<meta\s+property=["']og:title["']\s+content=["']([^"']+)["']/i) || html2.match(/<title>([\s\S]*?)<\/title>/i);
            const descMatch = html2.match(/<meta\s+name=["']description["']\s+content=["']([^"']+)["']/i) || html2.match(/<meta\s+property=["']og:description["']\s+content=["']([^"']+)["']/i);
            const keyMatch = html2.match(/<meta\s+name=["']keywords["']\s+content=["']([^"']+)["']/i);
            const authorMatch = html2.match(/<link\s+itemprop=["']name["']\s+content=["']([^"']+)["']/i);
            htmlTitle = titleMatch ? titleMatch[1].replace(/ - YouTube$/, "").trim() : "";
            htmlAuthor = authorMatch ? authorMatch[1].trim() : "";
            htmlKeywords = keyMatch ? keyMatch[1].trim() : "";
            const description = descMatch ? descMatch[1].trim() : "No description provided.";
            if (htmlTitle) {
              if (!metaText) {
                metaText = `=== YOUTUBE VIDEO METADATA (EXTRACTED VIA CLIENT HTML) ===
URL: ${targetUrl}
Title: ${htmlTitle}
Channel Name: ${htmlAuthor || "Unknown"}
Keywords: ${htmlKeywords}
Description:
${description}
`;
              }
            }
            if (!transcriptText) {
              const index2 = html2.indexOf("ytInitialPlayerResponse");
              if (index2 !== -1) {
                const startIndex = html2.indexOf("{", index2);
                if (startIndex !== -1) {
                  let braceCount = 0;
                  let inString = false;
                  let escaped = false;
                  let jsonStr = "";
                  for (let i = startIndex; i < html2.length; i++) {
                    const char = html2[i];
                    if (inString) {
                      if (escaped) {
                        escaped = false;
                      } else if (char === "\\") {
                        escaped = true;
                      } else if (char === '"') {
                        inString = false;
                      }
                    } else {
                      if (char === '"') {
                        inString = true;
                      } else if (char === "{") {
                        braceCount++;
                      } else if (char === "}") {
                        braceCount--;
                        if (braceCount === 0) {
                          jsonStr = html2.substring(startIndex, i + 1);
                          break;
                        }
                      }
                    }
                  }
                  if (jsonStr) {
                    try {
                      const data = JSON.parse(jsonStr);
                      const capTracks = data.captions?.playerCaptionsTracklistRenderer?.captionTracks;
                      if (capTracks && capTracks.length > 0) {
                        const track = capTracks.find((t) => t.languageCode === "en") || capTracks.find((t) => t.languageCode === "hi") || capTracks[0];
                        if (track && track.baseUrl) {
                          const subtitleProxy = `https://corsproxy.io/?${encodeURIComponent(track.baseUrl + "&fmt=json3")}`;
                          const subtitleRes = await fetch(subtitleProxy, { signal: AbortSignal.timeout(6e3) });
                          if (subtitleRes.ok) {
                            const subJson = await subtitleRes.json();
                            if (subJson && subJson.events) {
                              const textLines = subJson.events.map((ev) => ev.segs?.map((s) => s.utf8).join("").trim() || "").filter(Boolean);
                              if (textLines.length > 0) {
                                transcriptText = textLines.join(" ");
                              }
                            }
                          }
                        }
                      }
                    } catch (e) {
                    }
                  }
                }
              }
            }
          }
        } catch (err) {
          console.warn("Client-side direct HTML scrape failed:", err);
        }
      }
      let combinedTextResult = "";
      if (metaText) combinedTextResult += metaText + "\n";
      if (transcriptText) {
        combinedTextResult += `=== SPOKEN DIALOGUE / TRANSCRIPT ===
${transcriptText}
`;
      }
      if (!combinedTextResult) {
        try {
          const noembedRes = await fetch(`https://noembed.com/embed?url=${encodeURIComponent(targetUrl)}`);
          if (noembedRes.ok) {
            const noembedData = await noembedRes.json();
            if (noembedData && noembedData.title) {
              htmlTitle = noembedData.title;
              htmlAuthor = noembedData.author_name;
              combinedTextResult = `=== YOUTUBE VIDEO METADATA (RESOLVED VIA NOEMBED) ===
URL: ${targetUrl}
Title: ${noembedData.title}
Channel Name: ${noembedData.author_name}
Description: This video is titled "${noembedData.title}" by channel "${noembedData.author_name}". (No captions or transcripts are available for this video, but you can explain or discuss its topic using this metadata or your knowledge).
`;
            }
          }
        } catch (neErr) {
        }
      }
      const isTranscriptShort = !transcriptText || transcriptText.trim().length < 200;
      if (isTranscriptShort && (htmlTitle || metaText)) {
        let videoTitle = htmlTitle;
        let videoAuthor = htmlAuthor;
        if (!videoTitle && metaText) {
          const titleMatch = metaText.match(/Title:\s*(.+)/);
          const authorMatch = metaText.match(/Channel Name:\s*(.+)/);
          if (titleMatch) videoTitle = titleMatch[1].trim();
          if (authorMatch) videoAuthor = authorMatch[1].trim();
        }
        if (videoTitle) {
          const queries = [];
          queries.push(`${videoTitle} plot OR summary`);
          if (videoAuthor && videoAuthor !== "Unknown Channel") {
            queries.push(`${videoAuthor} ${videoTitle} summary`);
          }
          if (htmlKeywords) {
            const firstKeys = htmlKeywords.split(",").slice(0, 3).map((k) => k.trim()).filter(Boolean).join(" ");
            if (firstKeys) {
              queries.push(`${firstKeys} ${videoTitle} summary`);
            }
          }
          const searchResultsMap = /* @__PURE__ */ new Map();
          await Promise.all(
            queries.slice(0, 2).map(async (qStr) => {
              try {
                const encoded = encodeURIComponent(qStr);
                const searchUrl = `https://html.duckduckgo.com/html/?q=${encoded}`;
                const proxyUrl = `https://corsproxy.io/?${encodeURIComponent(searchUrl)}`;
                const searchRes = await fetch(proxyUrl, { signal: AbortSignal.timeout(5e3) });
                if (searchRes.ok) {
                  const searchHtml = await searchRes.text();
                  const parts = searchHtml.split('class="result results_links');
                  parts.slice(1, 4).forEach((part) => {
                    const titleMatch = part.match(/class="result__a"[^>]*>([\s\S]*?)<\/a>/);
                    const snippetMatch = part.match(/class="result__snippet"[^>]*>([\s\S]*?)<\/a>/);
                    const hrefMatch = part.match(/class="result__a"[^>]*href="([^"]+)"/);
                    let title = titleMatch ? titleMatch[1].replace(/<[^>]*>/g, "").trim() : "";
                    let snippet = snippetMatch ? snippetMatch[1].replace(/<[^>]*>/g, "").trim() : "";
                    let href = hrefMatch ? hrefMatch[1] : "";
                    if (href.startsWith("//")) href = "https:" + href;
                    if (href.startsWith("/l/") || href.includes("uddg=")) {
                      const m = href.match(/[?&]uddg=([^&]+)/);
                      if (m) href = decodeURIComponent(m[1]);
                    }
                    title = title.replace(/&amp;/g, "&").replace(/&lt;/g, "<").replace(/&gt;/g, ">").replace(/&quot;/g, '"').replace(/&#39;/g, "'");
                    snippet = snippet.replace(/&amp;/g, "&").replace(/&lt;/g, "<").replace(/&gt;/g, ">").replace(/&quot;/g, '"').replace(/&#39;/g, "'");
                    if (title && snippet && !searchResultsMap.has(href)) {
                      searchResultsMap.set(href, `- [${title}](${href}): ${snippet}`);
                    }
                  });
                }
              } catch (e) {
              }
            })
          );
          if (searchResultsMap.size > 0) {
            const scrapedSummaries = Array.from(searchResultsMap.values()).join("\n");
            combinedTextResult += `
=== 3RD-PARTY AI YOUTUBE VIDEO ANALYZER REPORT ===
Below is the aggregated analysis, plot details, and topic summaries fetched from multiple high-authority web databases for this video to ensure correctness:
${scrapedSummaries}
`;
          }
        }
      }
      if (!combinedTextResult) {
        combinedTextResult = `YouTube Video URL: ${targetUrl}
Video ID: ${videoId}
(Failed to retrieve transcript or metadata due to network restrictions)`;
      }
      rawText = combinedTextResult;
    }
    let aiAnalysis = "";
    try {
      aiAnalysis = await fetchThirdPartyAiVideoAnalysis(videoId, rawText);
    } catch (e) {
      console.warn("Failed to generate simulated AI video analyzer report:", e);
    }
    let finalCrawlText = rawText;
    if (aiAnalysis) {
      finalCrawlText += `

=== 3RD-PARTY AI YOUTUBE VIDEO ANALYZER REPORTS ===
Below is the detailed analysis, key takeaways, chapter notes, and cross-referenced summaries generated by querying this video URL through YTSummary.app, NoteGPT.io, and Recall.ai:

${aiAnalysis}
`;
    }
    return { text: finalCrawlText, crawledUrls };
  }
  if (targetUrl.toLowerCase().endsWith(".pdf") || targetUrl.toLowerCase().includes(".pdf?")) {
    try {
      console.log(`[Crawler] Parsing PDF file client-side: ${targetUrl}`);
      try {
        const res = await fetch(`/api/scrape?url=${encodeURIComponent(targetUrl)}`);
        if (res.ok) {
          const data = await res.json();
          if (data.text && !data.text.includes("Failed to scrape")) {
            return { text: data.text, crawledUrls };
          }
        }
      } catch (err) {
      }
      try {
        const proxyUrl = `https://corsproxy.io/?${encodeURIComponent(targetUrl)}`;
        const response = await fetch(proxyUrl);
        if (!response.ok) throw new Error(`HTTP ${response.status}`);
        const buffer = await response.arrayBuffer();
        const pdfjsLib = await __vitePreload(() => import("./vendor-pdf-DJ2ymul_.js"), true ? [] : void 0);
        pdfjsLib.GlobalWorkerOptions.workerSrc = `https://unpkg.com/pdfjs-dist@${pdfjsLib.version}/build/pdf.worker.min.mjs`;
        const doc = await pdfjsLib.getDocument({ data: buffer }).promise;
        let parsedText = `=== CRAWLED PDF FILE: ${targetUrl} ===
`;
        const numPages = Math.min(doc.numPages, 50);
        for (let i = 1; i <= numPages; i++) {
          const page = await doc.getPage(i);
          const textContent = await page.getTextContent();
          const pageText = textContent.items.map((item) => item.str).join(" ");
          parsedText += `--- PAGE ${i} ---
${pageText}

`;
        }
        return { text: parsedText, crawledUrls };
      } catch (clientPdfErr) {
        console.error("Browser PDF.js extraction failed:", clientPdfErr);
        return { text: `Failed to fetch and parse PDF contents: ${clientPdfErr.message || clientPdfErr}`, crawledUrls };
      }
    } catch (e) {
      return { text: `Failed to load PDF file: ${e.message || e}`, crawledUrls };
    }
  }
  const scrapeSingle = async (url) => {
    try {
      const res = await fetch(`/api/scrape?url=${encodeURIComponent(url)}`);
      if (res.ok) {
        const data = await res.json();
        return { text: data.text || "", links: data.links || [] };
      }
    } catch (e) {
      console.warn(`Local API scrape failed for ${url}, falling back to client-side proxy...`, e);
    }
    try {
      const proxyUrl = `https://corsproxy.io/?${encodeURIComponent(url)}`;
      const res = await fetch(proxyUrl);
      if (!res.ok) throw new Error(`HTTP status ${res.status}`);
      const html2 = await res.text();
      const cleanHtmlToText = (htmlStr) => {
        let clean = htmlStr.replace(/<script\b[^<]*(?:(?!<\/script>)<[^<]*)*<\/script>/gi, "");
        clean = clean.replace(/<style\b[^<]*(?:(?!<\/style>)<[^<]*)*<\/style>/gi, "");
        clean = clean.replace(/<[^>]+>/g, " ");
        clean = clean.replace(/&amp;/g, "&").replace(/&lt;/g, "<").replace(/&gt;/g, ">").replace(/&quot;/g, '"').replace(/&#39;/g, "'").replace(/&apos;/g, "'").replace(/&nbsp;/g, " ");
        return clean.replace(/\s+/g, " ").trim();
      };
      let text2 = cleanHtmlToText(html2);
      if (text2.length > 12e3) {
        text2 = text2.slice(0, 12e3) + "... [Content truncated for length]";
      }
      const links = [];
      const linkRegex = /<a\s+[^>]*href=["']([^"']+)["']/gi;
      const parsedUrl = new URL(url);
      const hostname = parsedUrl.hostname;
      let match;
      while ((match = linkRegex.exec(html2)) !== null) {
        const rawHref = match[1].trim();
        try {
          const resolvedUrl = new URL(rawHref, url);
          resolvedUrl.hash = "";
          const href = resolvedUrl.href;
          if (resolvedUrl.hostname === hostname && !links.includes(href) && href !== url) {
            links.push(href);
          }
        } catch (e) {
        }
      }
      return { text: text2, links: links.slice(0, 15) };
    } catch (err) {
      console.error(`Client fallback scraping failed for ${url}:`, err);
      return { text: `Failed to fetch page content: ${err.message || String(err)}`, links: [] };
    }
  };
  const primaryResult = await scrapeSingle(targetUrl);
  combinedText += `[CRAWLED PAGE: ${targetUrl}]
${primaryResult.text}

`;
  if (deep && primaryResult.links.length > 0) {
    const keywords = ["about", "feature", "service", "product", "contact", "info"];
    const subpageLinks = primaryResult.links.filter((link2) => {
      const lower = link2.toLowerCase();
      return keywords.some((kw) => lower.includes(kw));
    });
    const targets = (subpageLinks.length > 0 ? subpageLinks : primaryResult.links).slice(0, 2);
    for (const link2 of targets) {
      crawledUrls.push(link2);
      const subResult = await scrapeSingle(link2);
      combinedText += `[CRAWLED PAGE: ${link2}]
${subResult.text}

`;
    }
  }
  return { text: combinedText, crawledUrls };
}
const SLASH_COMMANDS = [
  { name: "/clear", description: "Clear current chat history", icon: Trash2 },
  { name: "/export", description: "Export chat as PNG image", icon: Image$1 },
  { name: "/features", description: "View AI features and capabilities", icon: BrainCircuit },
  { name: "/help", description: "Get help with AI assistant", icon: CircleQuestionMark },
  { name: "/internet", description: "Enable web search capabilities", icon: Globe },
  { name: "/system", description: "View system status and configuration", icon: Settings }
];
const fixMath = (str) => {
  if (!str) return str;
  str = str.replace(/&lt;br\s*\/?&gt;/gi, "\n\n");
  str = str.replace(/<br\s*\/?>/gi, "\n\n");
  str = str.replace(/&nbsp;/gi, " ");
  str = str.replace(/&lt;/g, "<");
  str = str.replace(/&gt;/g, ">");
  str = str.replace(/&amp;/g, "&");
  str = str.replace(/\\\(([\s\S]*?)\\\)/g, "$$$1$");
  str = str.replace(/\\\[([\s\S]*?)\\\]/g, "$$$$$1$$$$");
  str = str.replace(/(?:\\text\{sp\}|sp)\s*\^?\s*\{?(\d+)\}?\s*(?:extd|\\text\{d\}|\\textd)\s*\^?\s*\{?(\d+)\}?/g, "sp^$1d^$2");
  str = str.replace(/(?:\\text\{sp\}|sp)\s*\^?\s*\{?(\d+)\}?\s*(?:extd|\\text\{d\}|\\textd)/g, "sp^$1d");
  str = str.replace(/(?:extd|\\text\{d\}|\\textd)\s*\^?\s*\{?(\d+)\}?\s*(?:\\text\{sp\}|sp)\s*\^?\s*\{?(\d+)\}?/g, "d^$1sp^$2");
  str = str.replace(/sp\s*<\s*sup\s*>\s*(\d+)\s*<\s*\/\s*sup\s*>\s*(?:extd|\\text\{d\}|\\textd)\s*<\s*sup\s*>\s*(\d+)\s*<\s*\/\s*sup\s*>/g, "sp<sup>$1</sup>d<sup>$2</sup>");
  str = str.replace(/sp\s*<\s*sup\s*>\s*(\d+)\s*<\s*\/\s*sup\s*>\s*(?:extd|\\text\{d\}|\\textd)/g, "sp<sup>$1</sup>d");
  str = str.replace(/(?:extd|\\text\{d\}|\\textd)\s*<\s*sup\s*>\s*(\d+)\s*<\s*\/\s*sup\s*>\s*sp\s*<\s*sup\s*>\s*(\d+)\s*<\s*\/\s*sup\s*>/g, "d<sup>$1</sup>sp<sup>$2</sup>");
  str = str.replace(/\\begin\{align\*?\}([\s\S]*?)\\end\{align\*?\}/g, "\\begin{aligned}$1\\end{aligned}");
  str = str.replace(/(?:\xFF\xFF|\$)?\s*\\begin\{([a-zA-Z*]+)\}([\s\S]*?)\\end\{\1\}\s*(?:\xFF\xFF|\$)?/g, (match, env2, inner) => {
    const mathEnvs = ["aligned", "pmatrix", "bmatrix", "vmatrix", "matrix", "cases", "array", "eqnarray", "equation", "equation*"];
    if (mathEnvs.includes(env2)) {
      let cleaned = inner.replace(/\$\$/g, "").replace(/\$/g, "");
      return `
$$
\\begin{${env2}}${cleaned}\\end{${env2}}
$$
`;
    }
    return match;
  });
  str = str.replace(/\((\\text\{[^}]+\}.*?)\)/g, "$$$1$");
  str = str.replace(/\((\\displaystyle.*?)\)/g, "$$$1$");
  str = str.replace(/^\[\s+([\s\S]*?[_^\\][\s\S]*?)\s+\]$/gm, "$$$$ $1 $$$$");
  str = str.replace(/\$\$\s*([a-zA-Z\\{}_0-9]+)\s*\]\s*=/g, "$$$$ [$1] =");
  let tempStr = str.replace(/\\\$/g, "___ESCAPED_DOLLAR___");
  let tokens = tempStr.split(/(\$\$?)/);
  let inBlockMath = false;
  let inInlineMath = false;
  let result = "";
  for (let i = 0; i < tokens.length; i++) {
    let token = tokens[i];
    if (token === "$$") {
      if (!inInlineMath) inBlockMath = !inBlockMath;
      result += token;
    } else if (token === "$") {
      if (!inBlockMath) inInlineMath = !inInlineMath;
      result += token;
    } else {
      if (inBlockMath) {
        const disruptMatch = token.match(/(\*\*|\n\s*\n)/);
        if (disruptMatch) {
          const idx = disruptMatch.index;
          result += token.substring(0, idx) + "$$" + token.substring(idx);
          inBlockMath = false;
        } else {
          result += token;
        }
      } else if (inInlineMath) {
        const disruptMatch = token.match(/(\*\*|\n\s*\n)/);
        if (disruptMatch) {
          const idx = disruptMatch.index;
          result += token.substring(0, idx) + "$" + token.substring(idx);
          inInlineMath = false;
        } else {
          result += token;
        }
      } else {
        result += token;
      }
    }
  }
  if (inBlockMath) result += "$$";
  if (inInlineMath) result += "$";
  str = result.replace(/___ESCAPED_DOLLAR___/g, "\\$");
  return str;
};
function parseImageRequest(msg) {
  const lower = msg.toLowerCase();
  let resolution = "768x768";
  if (/\blandscape\b/i.test(lower)) {
    resolution = "768x512";
  } else if (/\bportrait\b/i.test(lower)) {
    resolution = "512x768";
  } else if (/\bsquare\b/i.test(lower)) {
    resolution = "768x768";
  }
  let count = 1;
  const countMatch = lower.match(/(\d+)\s*(?:images?|pictures?|photos?|generations?|drawings?)/i) || lower.match(/(?:generate|make|create|imagine|draw|get|want)\s*(\d+)\s*(?:images?|pictures?|photos?|generations?|drawings?)/i);
  if (countMatch && countMatch[1]) {
    const val = parseInt(countMatch[1], 10);
    if (!isNaN(val) && val > 0) {
      count = Math.min(val, 10);
    }
  }
  let styleName = "No style";
  let promptPrefix = "";
  let promptSuffix = "";
  let negativePrompt = "";
  if (/\banime\b/i.test(lower) || /\bwaifu\b/i.test(lower)) {
    if (/\bwaifu\b/i.test(lower)) {
      styleName = "Waifu";
      promptSuffix = ", waifu character portrait, 1girl, very high resolution, semi-realistic anime art. Intricate and impressive anime artwork with stunningly beautiful composition and style.\n\nOverall, it's an absolute world-class masterpiece anime artwork. It's an aesthetically pleasing waifu artwork with impeccable attention to detail and impressive composition. It's an aesthetically pleasing character portrait. It's an aesthetically pleasing image.";
    } else {
      styleName = "Painted Anime";
      const isMale = /\bmale\b|\bboy\b|\bman\b/i.test(lower);
      if (isMale) {
        promptSuffix = ", art in the style of atey ghailan, painterly anime style at pixiv, art in the style of kantoku, in art style of redjuice/necömi/rella/tiv pixiv collab, your name anime art style, masterpiece digital painting, exquisite lighting and composition, inspired by wlop art style, 8k, sharp, very detailed, high resolution, illustration.\n\nOverall, it's an absolute world-class masterpiece painterly anime-inspired digital art. It's an aesthetically pleasing painterly anime-inspired digital artwork with impeccable attention to detail and impressive composition.";
      } else {
        promptPrefix = "painterly anime artwork, ";
        promptSuffix = ", world-class masterpiece, fine details, breathtaking artwork, painterly art style, high quality, 8k, very detailed, high resolution, exquisite composition and lighting.\n\nOverall, it's an absolute world-class masterpiece painterly anime artwork. It's an aesthetically pleasing painterly anime artwork with impeccable attention to detail and impressive composition.";
      }
    }
  } else if (/\bphoto\b/i.test(lower) || /\brealistic\b/i.test(lower) || /\bphotorealistic\b/i.test(lower)) {
    styleName = "Casual Photo";
    promptPrefix = "A casual photo. A casual photo of ";
    promptSuffix = ". It's a casual photo.";
  } else if (/\bfantasy\s*map\b/i.test(lower) || /\bworld\s*map\b/i.test(lower)) {
    styleName = "Fantasy World Map";
    promptPrefix = "beautiful fantasy map of ";
    promptSuffix = ", beautiful fantasy map inspired by middle earth and azeroth and discworld and westeros and essos and the witcher world and tamriel and faerûn and thedas, 4k, beautiful colors, crisp, high-resolution artistic map, topographic 3D terrain, artistic map.\n\nOverall, it's an absolute world-class masterpiece fantasy world map. It's an aesthetically pleasing fantasy world map artwork with impeccable attention to detail and impressive composition.";
  } else if (/\bisometric\b/i.test(lower)) {
    styleName = "3D Isometric Icon";
    promptSuffix = ", 3D isometric render of cute, 3D app icon, clean isometric design, beautiful design, soft gradient background, soft colors, centered, 3D blender render, masterpiece, best quality, high resolution, 8k octane render, beautiful color scheme, soft smooth lighting, physically based rendering, square image, high polycount";
  } else if (/\bflat style icon\b/i.test(lower) || /\bflat icon\b/i.test(lower)) {
    styleName = "Flat Style Icon";
    promptSuffix = ", creative icon, flat style icon, masterpiece, high resolution, crisp, beautiful composition and color choice, beautiful flat painted style, behance contest-winner, award winning icon illustration, 8k, best quality";
  } else if (/\bflat style logo\b/i.test(lower) || /\bflat logo\b/i.test(lower)) {
    styleName = "Flat Style Logo";
    promptPrefix = "beautiful flat-style logo design depicting ";
    promptSuffix = ", creative flat-style logo design, trending on dribbble, featured on behance, portfolio piece, minimal flat design, breathtaking graphic design, 8k, high resolution vector logo, plain background, amazingly beautiful logo design, winner of best logo award";
  } else if (/\bgame art\b/i.test(lower) || /\bgame icon\b/i.test(lower)) {
    styleName = "Game Art Icon";
    promptSuffix = ", a concept art icon for league of legends, a digital art logo, illustration, league of legends style icon, inspired by wlop style, 8k, dota 2 style icon, fine details, sharp, very detailed icon, high resolution rpg ability/spell/item icon";
  } else if (/\bclaymation\b/i.test(lower)) {
    styleName = "Claymation";
    promptSuffix = ", claymation, incredible claymation style, kubo and the two strings style art, Missing Link 2019 art style, clay texture, clay animation, stop-motion clay";
  } else if (/\bcartoon\b/i.test(lower)) {
    styleName = "Cartoon";
    promptSuffix = ", cartoon-style art, superb linework, nice colors and composition, bold linework, masterpiece, cute art in the style of Dana Terrace, in the style of Rebecca Sugar, in the style of ry-spirit, amazing and wholesome cartoon-style art, cute art style, (trending on artstation)";
  } else if (/\bpencil\b/i.test(lower) || /\bsketch\b/i.test(lower)) {
    styleName = "Pencil";
    promptPrefix = "black and white pencil drawing, ";
    promptSuffix = ", black and white, breathtaking pencil illustration, highly detailed, 4k, textured paper, pencil texture, sketch";
  } else if (/\btattoo\b/i.test(lower)) {
    styleName = "Tattoo Design";
    promptPrefix = "amazing tattoo design, ";
    promptSuffix = ", breathtaking tattoo design, incredible tattoo design";
  } else if (/\bcursed\b/i.test(lower)) {
    styleName = "Cursed Photo";
    promptPrefix = "cursed photo of ";
    promptSuffix = ", creepy and cursed, absolutely cursed photo, nope nope nope nope, what the actual f, unsettling photo, cursed_thing, cursedimages, no context, cursed image, bad photo, weird photo, very strange, color photo, creepy photo, nightmare fuel";
  }
  let corePrompt = msg;
  corePrompt = corePrompt.replace(/\b(generate|create|make|imagine|draw|picture|photo|illustration|paint|imaging|imagin)\s*(?:an?|the)?\s*(?:images?|pictures?|photos?)?\s*(?:of|for)?\b/i, "");
  corePrompt = corePrompt.replace(/\b(landscape|portrait|square)\b/i, "");
  corePrompt = corePrompt.replace(/\b\d+\s*(?:images?|pictures?|photos?|generations?|drawings?)\b/i, "");
  corePrompt = corePrompt.replace(/\b(anime|waifu|realistic|photorealistic|fantasy map|isometric|flat style icon|flat icon|flat style logo|flat logo|game art|game icon|claymation|cartoon|pencil|sketch|tattoo|cursed)\b/i, "");
  corePrompt = corePrompt.replace(/\b(art\s+)?style\b/i, "");
  corePrompt = corePrompt.replace(/^[,\s.!?\-\/]+|[,\s.!?\-\/]+$/g, "");
  if (!corePrompt.trim()) {
    corePrompt = msg;
  }
  let finalPrompt = corePrompt;
  if (styleName === "Casual Photo") {
    const promptCleaned = corePrompt.replace(/\b(realistic)\b/i, "actual");
    finalPrompt = `${promptPrefix}${promptCleaned}${promptSuffix}`;
  } else {
    finalPrompt = `${promptPrefix}${corePrompt}${promptSuffix}`;
  }
  return {
    prompt: finalPrompt,
    originalPrompt: corePrompt,
    resolution,
    count,
    styleName,
    negativePrompt
  };
}
class AIChatBackgroundManager {
  activeJobs = /* @__PURE__ */ new Map();
  listeners = /* @__PURE__ */ new Set();
  subscribe(cb) {
    this.listeners.add(cb);
    return () => {
      this.listeners.delete(cb);
    };
  }
  notify() {
    this.listeners.forEach((cb) => cb());
  }
  isGenerating(sessionId) {
    return this.activeJobs.has(sessionId);
  }
  isGeneratingImageType(sessionId) {
    const job = this.activeJobs.get(sessionId);
    return job ? job.generatingImageType : false;
  }
  stopJob(sessionId) {
    const job = this.activeJobs.get(sessionId);
    if (job) {
      job.abortController.abort();
      this.activeJobs.delete(sessionId);
      try {
        const raw = localStorage.getItem("jee_ai_chats");
        if (raw) {
          const sessions = JSON.parse(raw);
          const updated = sessions.map((s) => {
            if (s.id !== sessionId) return s;
            const msgs = [...s.messages];
            const lastMsg = msgs[msgs.length - 1];
            if (lastMsg && lastMsg.role === "model") {
              msgs[msgs.length - 1] = {
                role: "model",
                content: "*You stopped this response*",
                isTyping: false,
                isStopped: true
              };
            } else {
              msgs.push({
                role: "model",
                content: "*You stopped this response*",
                isTyping: false,
                isStopped: true
              });
            }
            return { ...s, messages: msgs, updatedAt: Date.now() };
          });
          localStorage.setItem("jee_ai_chats", JSON.stringify(updated));
        }
      } catch (e) {
      }
      this.notify();
    }
  }
  async autoGenerateTitle(sessionId, chatHistory) {
    if (chatHistory.length !== 2 && (chatHistory.length - 2) % 4 !== 0) return;
    try {
      const apiKey = localStorage.getItem("jee_openrouter_api_key") || "";
      if (!apiKey) return;
      const chatText = chatHistory.map((m) => `${m.role}: ${m.content}`).join("\n").slice(-3e3);
      const promptText = `Summarize the core topic of the following conversation in a short, catchy title (maximum 4 words). Respond ONLY with the title, without quotes or punctuation or any explanation.

${chatText}`;
      let newTitle = "";
      const models = ["meta-llama/llama-3.3-70b-instruct:free", "qwen/qwen-2.5-coder-32b-instruct:free", "openai/gpt-oss-120b:free"];
      for (const modelName of models) {
        try {
          const payload = { model: modelName, messages: [{ role: "user", content: promptText }] };
          let res;
          try {
            res = await fetch("https://openrouter.ai/api/v1/chat/completions", {
              method: "POST",
              headers: { "Authorization": `Bearer ${apiKey.trim()}`, "Content-Type": "application/json" },
              body: JSON.stringify(payload)
            });
          } catch (e) {
            res = await fetch(`https://corsproxy.io/?${encodeURIComponent("https://openrouter.ai/api/v1/chat/completions")}`, {
              method: "POST",
              headers: { "Authorization": `Bearer ${apiKey.trim()}`, "Content-Type": "application/json" },
              body: JSON.stringify(payload)
            });
          }
          if (res.ok) {
            const data = await res.json();
            newTitle = data.choices?.[0]?.message?.content?.trim();
            if (newTitle) break;
          }
        } catch (e) {
        }
      }
      if (newTitle) {
        newTitle = newTitle.replace(/^["']|["']$/g, "").replace(/\n/g, " ").trim();
        const raw = localStorage.getItem("jee_ai_chats");
        if (raw) {
          const sessions = JSON.parse(raw);
          const updated = sessions.map((s) => s.id === sessionId ? { ...s, title: newTitle } : s);
          localStorage.setItem("jee_ai_chats", JSON.stringify(updated));
          this.notify();
        }
      }
    } catch (err) {
      console.warn("Failed to auto-generate title:", err);
    }
  }
  async generateResponse(params) {
    const { sessionId, messagesToSent, filePayloads, selectedGoal, selectedSettings } = params;
    this.stopJob(sessionId);
    const abortController = new AbortController();
    const signal = abortController.signal;
    const lastMsg = messagesToSent[messagesToSent.length - 1].content;
    const hasImages = filePayloads && filePayloads.length > 0;
    selectedSettings?.internet ?? true;
    selectedSettings?.ai ?? true;
    const useVerify = selectedSettings?.verify ?? true;
    let extractedQuestionText = "";
    let diagramDescriptionText = "";
    if (hasImages) {
      try {
        const apiKey = localStorage.getItem("jee_openrouter_api_key") || "";
        if (apiKey) {
          const ocrPrompt = `Analyze the academic/study question shown in this image. Do not solve it.
Your job is to extract all details from the image:
1. QUESTION TEXT: Transcribe the clean, exact text of the question. If there are multiple-choice options (A, B, C, D), include them verbatim.
2. VISUAL DIAGRAM DESCRIPTION: If there is any diagram, graph, schematic, circuit, chemical reaction/mechanism, chemical structure, or geometry figure, describe it in absolute structural detail. Mention every label, numerical value, coordinate, component, force arrow, node, battery voltage, resistance, chemical symbol, chemical bond, and angle. Be extremely precise so that a solver could understand the exact structure and solve the question based only on your text description.

Output in this exact format:
[QUESTION TEXT]
(insert transcribed text here)

[DIAGRAM DESCRIPTION]
(insert detailed description here or write "None" if there is no diagram)`;
          const ocrPayload = [
            {
              role: "user",
              content: [
                { type: "text", text: ocrPrompt },
                ...filePayloads.map((fp) => ({ type: "image_url", image_url: { url: `data:${fp.inlineData.mimeType};base64,${fp.inlineData.data}` } }))
              ]
            }
          ];
          const ocrModels = [
            "google/gemini-2.5-flash",
            "meta-llama/llama-3.2-11b-vision-instruct:free",
            "google/gemma-4-26b-a4b-it:free"
          ];
          for (const model of ocrModels) {
            try {
              const res = await fetch("https://openrouter.ai/api/v1/chat/completions", {
                method: "POST",
                headers: {
                  "Authorization": `Bearer ${apiKey.trim()}`,
                  "HTTP-Referer": window.location.href,
                  "X-Title": "StudE",
                  "Content-Type": "application/json"
                },
                body: JSON.stringify({ model, messages: ocrPayload }),
                signal
              });
              if (res.ok) {
                const ocrData = await res.json();
                const ocrResponseText = ocrData.choices?.[0]?.message?.content?.trim() || "";
                if (ocrResponseText) {
                  const questionMatch = ocrResponseText.match(/\[QUESTION TEXT\]([\s\S]*?)(?:\[DIAGRAM DESCRIPTION\]|$)/i);
                  const diagramMatch = ocrResponseText.match(/\[DIAGRAM DESCRIPTION\]([\s\S]*?)$/i);
                  extractedQuestionText = questionMatch ? questionMatch[1].trim() : ocrResponseText;
                  const rawDiag = diagramMatch ? diagramMatch[1].trim() : "";
                  if (rawDiag && !/^none/i.test(rawDiag)) {
                    diagramDescriptionText = rawDiag;
                  }
                  break;
                }
              }
            } catch (e) {
            }
          }
        }
      } catch (ocrErr) {
        console.warn("Failed to transcribe image question via OCR:", ocrErr);
      }
    }
    let searchQuery = lastMsg;
    if (extractedQuestionText) {
      if (lastMsg.length < 25 || /solve|answer|what is|calculate|explain|this|image|picture|photo/i.test(lastMsg)) {
        searchQuery = extractedQuestionText;
      } else {
        searchQuery = `${lastMsg} ${extractedQuestionText}`;
      }
    }
    const isImageRequest = !hasImages && /generate.*image|create.*image|draw\b|make.*image|picture.*of|image.*of|create.*picture|make.*picture|generate.*picture|imagin.*image|imagine.*image|imagine\b/i.test(lastMsg);
    this.activeJobs.set(sessionId, {
      sessionId,
      abortController,
      generatingImageType: isImageRequest
    });
    this.notify();
    if (isImageRequest) {
      try {
        const options = parseImageRequest(lastMsg);
        const imageUrls = [];
        const responseContent = `Generating ${options.count} image(s) using **Pollinations AI**...
- **Prompt**: "${options.originalPrompt}"
- **Style**: ${options.styleName}
- **Shape**: ${options.resolution === "768x512" ? "Landscape" : options.resolution === "512x768" ? "Portrait" : "Square"}`;
        const intermediateHistory = [...messagesToSent, {
          role: "model",
          content: responseContent,
          isTyping: true,
          attachments: []
        }];
        try {
          const raw = localStorage.getItem("jee_ai_chats");
          if (raw) {
            const sessions = JSON.parse(raw);
            const updated = sessions.map((s) => s.id === sessionId ? {
              ...s,
              messages: intermediateHistory,
              updatedAt: Date.now()
            } : s);
            localStorage.setItem("jee_ai_chats", JSON.stringify(updated));
          }
        } catch (e) {
        }
        this.notify();
        for (let i = 0; i < options.count; i++) {
          const seed = Math.floor(Math.random() * 1e6);
          let model = "flux";
          if (options.styleName === "Painted Anime" || options.styleName === "Waifu") {
            model = "flux-anime";
          } else if (options.styleName === "Casual Photo") {
            model = "flux-realism";
          }
          let w = 768, h = 768;
          if (options.resolution === "768x512") {
            w = 1024;
            h = 768;
          } else if (options.resolution === "512x768") {
            w = 768;
            h = 1024;
          }
          const sanitizedPrompt = options.prompt.replace(/[\r\n]+/g, " ").trim();
          const url = `https://image.pollinations.ai/prompt/${encodeURIComponent(sanitizedPrompt)}?width=${w}&height=${h}&model=${model}&seed=${seed}&nologo=true`;
          try {
            const resp = await fetch(url);
            if (!resp.ok) throw new Error("Status " + resp.status);
            const blob = await resp.blob();
            const base64 = await new Promise((resolve) => {
              const reader = new FileReader();
              reader.onloadend = () => resolve(reader.result);
              reader.readAsDataURL(blob);
            });
            imageUrls.push(base64);
            try {
              const raw = localStorage.getItem("jee_ai_chats");
              if (raw) {
                const sessions = JSON.parse(raw);
                const updated = sessions.map((s) => {
                  if (s.id !== sessionId) return s;
                  const msgs = [...s.messages];
                  const last = msgs[msgs.length - 1];
                  if (last && last.role === "model") {
                    last.attachments = imageUrls.map((img, idx) => ({
                      url: img,
                      type: "image",
                      name: `Generated Image ${idx + 1}`
                    }));
                  }
                  return { ...s, messages: msgs, updatedAt: Date.now() };
                });
                localStorage.setItem("jee_ai_chats", JSON.stringify(updated));
              }
            } catch (e) {
            }
            this.notify();
          } catch (e) {
            console.error("Pollinations generation failed:", e);
          }
        }
        const finalResponseContent = imageUrls.length > 0 ? "" : `Failed to generate any images from the Pollinations AI generator. Please try again.`;
        let finalHistory = [];
        try {
          const raw = localStorage.getItem("jee_ai_chats");
          if (raw) {
            const sessions = JSON.parse(raw);
            const updated = sessions.map((s) => {
              if (s.id !== sessionId) return s;
              const msgs = [...s.messages];
              const last = msgs[msgs.length - 1];
              if (last && last.role === "model") {
                last.content = finalResponseContent;
                last.isTyping = false;
                last.attachments = imageUrls.map((img, idx) => ({
                  url: img,
                  type: "image",
                  name: `Generated Image ${idx + 1}`
                }));
              }
              finalHistory = msgs;
              return { ...s, messages: msgs, updatedAt: Date.now() };
            });
            localStorage.setItem("jee_ai_chats", JSON.stringify(updated));
          }
        } catch (e) {
        }
        this.notify();
        this.autoGenerateTitle(sessionId, finalHistory);
        return;
      } catch (err) {
        console.error("Perchance image generation error:", err);
        const errorContent = `Failed to generate image: ${err.message}`;
        try {
          const raw = localStorage.getItem("jee_ai_chats");
          if (raw) {
            const sessions = JSON.parse(raw);
            const updated = sessions.map((s) => s.id === sessionId ? {
              ...s,
              messages: [...messagesToSent, { role: "model", content: errorContent, isTyping: false }],
              updatedAt: Date.now()
            } : s);
            localStorage.setItem("jee_ai_chats", JSON.stringify(updated));
          }
        } catch (e) {
        }
        this.notify();
        return;
      } finally {
        this.activeJobs.delete(sessionId);
        this.notify();
      }
    }
    let responseText = "";
    let generatedAttachments = [];
    let generatedSources = [];
    try {
      const apiKey = localStorage.getItem("jee_openrouter_api_key") || "";
      if (!apiKey) {
        throw new Error("Please set your OpenRouter API Key in the Admin Panel first!");
      }
      const goalCategory = selectedGoal?.category || "JEE";
      const goalName = selectedGoal?.displayName || "JEE Prep";
      const goalPath = selectedGoal ? selectedGoal.path.join(" -> ") : "JEE";
      let goalSpecificInstruction = "";
      if (goalCategory === "JEE") {
        goalSpecificInstruction = `EDUCATIONAL SUPER MODE & JEE MASTER MODE
For all academic subjects: Physics, Chemistry, Mathematics.
JEE Knowledge Base: NCERT, HC Verma, Cengage, Arihant, Irodov, Black Book, Pathfinder, N Avasthi, MS Chauhan, JD Lee.
Capabilities: JEE Main, JEE Advanced level concepts.
Can generate: Daily plans, Mock tests, PYQ analysis, Rank improvement strategies for IIT-JEE.`;
      } else if (goalCategory === "NEET") {
        goalSpecificInstruction = `EDUCATIONAL SUPER MODE & NEET MASTER MODE
For all medical prep subjects: Physics, Chemistry, Biology (Botany & Zoology).
NEET Knowledge Base: NCERT Biology/Chemistry/Physics, HC Verma, DC Pandey, MS Chauhan, MTG, Trueman's Biology.
Capabilities: NEET level concepts, diagrams, medical entrance problem solving.
Can generate: Biology mock tests, organic chemistry reaction sheet, physics numerical walkthroughs, revision schedules.`;
      } else if (goalCategory === "UPSC") {
        goalSpecificInstruction = `EDUCATIONAL SUPER MODE & UPSC CIVIL SERVICES MASTER MODE
For all UPSC IAS preparation: General Studies (GS1, GS2, GS3, GS4), CSAT, Essay, and optional subjects.
UPSC Knowledge Base: NCERT, Laxmikanth (Polity), Bipin Chandra & Rajiv Ahir (History), Ramesh Singh (Economy), GC Leong & Majid Husain (Geography), Current Affairs, Yojana & Kurukshetra magazines.
Capabilities: General Studies topics, answer structure analysis, essay drafting, ethics case studies.
Can generate: Daily study schedules, GS test series questions, essay prompts, current affairs summaries.`;
      } else if (goalCategory === "School") {
        goalSpecificInstruction = `EDUCATIONAL SUPER MODE & SCHOOL EXAM MASTER MODE
For grade school education (specifically targetting ${goalPath}).
School Knowledge Base: NCERT, CBSE / ICSE textbook solutions, state boards syllabus, RS Aggarwal, RD Sharma, Lakhmir Singh & Manjit Kaur.
Capabilities: Grade-appropriate explanations, board preparation (10th/12th if applicable), homework help, interactive quiz creation.
Can generate: Custom study plans, school chapter notes, worksheets, sample board exam papers.`;
      } else if (goalCategory === "Olympiads") {
        goalSpecificInstruction = `EDUCATIONAL SUPER MODE & OLYMPIAD MASTER MODE
For advanced academic olympiad prep (specifically targetting ${goalPath}).
Olympiad Knowledge Base: Advanced mathematics, physics, chemistry, biology, English, computers. PRMO, IOQM, RMO, INMO, SOF IMO/NSO/IEO/NCO syllabus.
Capabilities: Deep Olympiad-level conceptual problems, logic, proofs, aptitude.
Can generate: Olympiad practice tests, past paper solutions, concept worksheets.`;
      } else if (goalCategory === "Skills") {
        goalSpecificInstruction = `SKILLS DEVELOPMENT & PRACTICAL LEARNING MASTER MODE
For skills training (specifically targetting ${goalPath}).
Skills Knowledge Base: Online courses, tutorials, practical learning, industry best practices, project-building guides.
Capabilities: Step-by-step learning roadmaps, coding challenges, music sheets, painting techniques, trading strategies, hacking lab setups.
Can generate: Skills learning roadmap, action items, practice projects, portfolio development guidelines.`;
      } else if (goalCategory === "Ed-Tech") {
        goalSpecificInstruction = `ED-TECH COMPANION & PLATFORM STUDY MODE
Specifically customized for study material of ${goalName}.
Knowledge Base: Coaching notes, test series, standard modules, lectures.
Capabilities: Doubt resolution, summarizing coaching videos/lectures, coaching syllabus mapping, custom quizzes.
Can generate: Lecture summary templates, study calendars mapped to coaching test series.`;
      } else {
        goalSpecificInstruction = `EDUCATIONAL SUPER MODE & ${goalCategory.toUpperCase()} MASTER MODE
For preparation of ${goalName} (${goalPath}).
Knowledge Base: Standard syllabus, textbooks, online tutorials, exams preparation resources.
Capabilities: Custom study plans, topic explanation, question bank generation.
Can generate: Mock exams, daily timetables, flashcards, concept sheets.`;
      }
      const systemInstruction = `SYSTEM IDENTITY

You are Calculus AI made by OM. Ultimate, an advanced multimodal AI educational operating system designed to rival and exceed the capabilities of ChatGPT, Gemini, Claude, Perplexity, Grok, Wolfram Alpha, Desmos, GeoGebra, Khan Academy, and the world's best educational platforms.

You are not merely a chatbot.

You are:
- AI Tutor
- Research Assistant
- Problem Solver
- Coding Assistant
- Internet Research Agent
- YouTube Research Agent
- Mathematical Engine
- Scientific Calculator
- Interactive Simulator
- Graph Visualizer
- Academic Planner
- Productivity Coach
- Knowledge Base
- File Analyzer
- Vision AI
- Learning Companion

CORE OBJECTIVE
For every user request:
1. Understand intent.
2. Decide required tools.
3. Gather information.
4. Verify accuracy.
5. Think step-by-step.
6. Generate rich response.
7. Add visual elements if useful.
8. Add simulations if possible.
9. Add references if internet used.
10. Ensure final answer is educational and correct.

UNIVERSAL CAPABILITIES
- Conversation: Natural conversations, long-term context awareness, multi-turn discussions, follow-up understanding, context memory, personalized responses.
- Reasoning: Logical reasoning, mathematical reasoning, scientific reasoning, multi-step reasoning, comparative analysis, critical thinking.
- Research: Internet research, source verification, fact checking, citation generation, data aggregation, latest information retrieval.
- Education: Teaching concepts, creating notes, making summaries, solving problems, generating quizzes, exam preparation.
- Programming: Code generation, code debugging, code explanation, architecture design, full-stack development, AI development.
- Multimedia: Image understanding, document understanding, graph understanding, diagram analysis, video recommendations, visual explanations.

INTERNET RESEARCH MODE
Whenever a question contains words like: latest, today, recent, current, trending, updated, news, live, ongoing, recently:
1. Search internet.
2. Collect data.
3. Verify sources.
4. Cross-check information.
5. Generate answer.
Never rely solely on stored knowledge for time-sensitive information.

SOURCE CITATION SYSTEM
Whenever internet data is used, display:
- Source logo
- Source name
- Website link`;
      const currentYear = (/* @__PURE__ */ new Date()).getFullYear();
      const dateInstruction = `

DATE REFERENCE
The current local date is ${(/* @__PURE__ */ new Date()).toLocaleDateString("en-US", { weekday: "long", year: "numeric", month: "long", day: "numeric" })}. You MUST evaluate all notifications, syllabus changes, and news queries with this date context.`;
      const imageGenerationInstruction = isImageRequest ? `

IMAGE GENERATION CAPABILITY
You have visual generation capabilities. Since the user is requesting an image, respond ONLY with a single JSON block wrapped inside a markdown code block matching this schema:
\`\`\`json
{
  "type": "image",
  "prompt": "highly detailed description of the scene to generate",
  "aspect_ratio": "1:1"
}
\`\`\`
Do not include any explanation or markdown outside the code block.` : "";
      const useInternet2 = selectedSettings?.internet ?? true;
      const useAI2 = selectedSettings?.ai ?? true;
      let searchContext = "";
      let scrapedContext = "";
      if (useInternet2) {
        const hasHindi = /[\u0900-\u097F]/.test(searchQuery);
        let englishQuery = searchQuery;
        if (hasHindi) {
          const hindiToEng = {
            "नया": "new",
            "नये": "new",
            "नई": "new",
            "ताजा": "latest",
            "ताज़ा": "latest",
            "लेटेस्ट": "latest",
            "अपडेट": "update",
            "सिलेबस": "syllabus",
            "पैटर्न": "pattern",
            "खबर": "news",
            "समाचार": "news",
            "परीक्षा": "exam",
            "तैयारी": "preparation",
            "बोर्ड": "board",
            "किताब": "book",
            "सवाल": "question",
            "जवाब": "answer",
            "कहानी": "story",
            "एपिसोड": "episode",
            "वीडियो": "video",
            "क्या है": "what is",
            "क्या": "what",
            "कब": "when",
            "कैसे": "how"
          };
          let eng = searchQuery;
          for (const [hindi, english] of Object.entries(hindiToEng)) {
            eng = eng.replace(new RegExp(hindi, "gi"), english);
          }
          const cleanWords = eng.split(/\s+/).filter((w) => !/[\u0900-\u097F]/.test(w));
          if (cleanWords.length > 0) {
            englishQuery = cleanWords.join(" ");
          }
        }
        const isLatest = /latest|latets|today|recent|current|trending|updated|updates|new|news|situation|real-time|realtime|up-to-date|recently/i.test(searchQuery);
        const query1 = isLatest ? searchQuery + " " + currentYear : searchQuery;
        const query2 = isLatest ? englishQuery + " " + currentYear : englishQuery;
        const fetchSearchWithFallbacks = async (q) => {
          try {
            let resObj = await fetchWebSearchResults(q, "m");
            if (!resObj || !resObj.results || resObj.results.length === 0) {
              resObj = await fetchWebSearchResults(q, "y");
            }
            if (!resObj || !resObj.results || resObj.results.length === 0) {
              resObj = await fetchWebSearchResults(q);
            }
            return resObj;
          } catch (e) {
            try {
              return await fetchWebSearchResults(q);
            } catch (err) {
              return null;
            }
          }
        };
        const resultsMap = /* @__PURE__ */ new Map();
        const searchPromises = [fetchSearchWithFallbacks(query1)];
        if (query1 !== query2) {
          searchPromises.push(fetchSearchWithFallbacks(query2));
        }
        const { cleanQuery, sources: targetedSources } = getCleanQueryAndSources(searchQuery);
        if (targetedSources.length > 0) {
          const groupSize = 5;
          for (let i = 0; i < targetedSources.length; i += groupSize) {
            const group = targetedSources.slice(i, i + groupSize);
            const siteOperators = group.map((src) => {
              if (src.includes(".")) {
                return `site:${src}`;
              } else {
                return src;
              }
            });
            const targetedQuery = `${cleanQuery} (${siteOperators.join(" OR ")})` + (isLatest ? ` ${currentYear}` : "");
            searchPromises.push(fetchSearchWithFallbacks(targetedQuery));
          }
        }
        const searchObjs = await Promise.all(searchPromises);
        searchObjs.forEach((searchObj) => {
          if (searchObj && searchObj.results) {
            searchObj.results.forEach((item) => {
              if (item.url && !resultsMap.has(item.url)) {
                resultsMap.set(item.url, item);
              }
            });
          }
        });
        if (resultsMap.size > 0) {
          const mergedResults = Array.from(resultsMap.values());
          let searchSummaries = "";
          mergedResults.forEach((item, idx) => {
            searchSummaries += `[Source ${idx + 1}] Title: ${item.title}
URL: ${item.url}
Snippet: ${item.snippet}
Thumbnail: ${item.thumbnail || ""}

`;
            let hostname = "";
            try {
              hostname = new URL(item.url).hostname;
            } catch (e) {
            }
            generatedSources.push({
              uri: item.url,
              title: item.title,
              favicon: hostname ? `https://www.google.com/s2/favicons?domain=${hostname}` : "",
              snippet: item.snippet
            });
          });
          searchContext = `

[CRITICAL DIRECTIVE: REAL-TIME RAG MODE ACTIVATED]
The user is requesting information that requires real-time data. You MUST answer this query using the verified web search results provided below. Cite all facts by referencing the relevant [Source X] link.

REAL-TIME WEB SEARCH RESULTS:
${searchSummaries}

INSTRUCTIONS: Write a comprehensive, precise response synthesizing the search results. Cite sources exactly.`;
          if (useVerify) {
            const isAcademicSite = (url) => {
              const academicDomains = [
                "doubtnut.com",
                "toppr.com",
                "byjus.com",
                "brainly.in",
                "vedantu.com",
                "sarthaks.com",
                "shiksha.com",
                "unacademy.com",
                "embibe.com",
                "stackexchange.com",
                "stackoverflow.com",
                "geeksforgeeks.org",
                "selfstudys.com",
                "collegedunia.com",
                "careers360.com",
                "physics.org",
                "khanacademy.org",
                "wikipedia.org",
                "chemwatch.net",
                "libretexts.org",
                "wolframalpha.com"
              ];
              try {
                const hostname = new URL(url).hostname.toLowerCase();
                return academicDomains.some((domain2) => hostname.includes(domain2));
              } catch (e) {
                return false;
              }
            };
            const sortedResults = [...mergedResults].sort((a, b) => {
              const aIsAcad = isAcademicSite(a.url) ? 1 : 0;
              const bIsAcad = isAcademicSite(b.url) ? 1 : 0;
              return bIsAcad - aIsAcad;
            });
            const scrapeTargets = sortedResults.slice(0, 3);
            console.log(`[Validation] Scraping ${scrapeTargets.length} academic/web pages for solution verification...`);
            const scrapeWithTimeout = async (url, timeoutMs = 6e3) => {
              return Promise.race([
                fetchCrawlResults(url, false),
                new Promise(
                  (_, reject) => setTimeout(() => reject(new Error("Timeout scraping")), timeoutMs)
                )
              ]).catch((err) => {
                console.warn(`[Validation] Scraping timed out/failed for ${url}:`, err);
                return { text: `[Failed to retrieve full contents of ${url}]`, crawledUrls: [url] };
              });
            };
            try {
              const scrapeResults = await Promise.all(
                scrapeTargets.map((item) => scrapeWithTimeout(item.url))
              );
              scrapeResults.forEach((scraped, index2) => {
                const item = scrapeTargets[index2];
                if (scraped && scraped.text && !scraped.text.includes("[Failed to retrieve")) {
                  scrapedContext += `
--- SOURCE ${index2 + 1}: ${item.title} (${item.url}) ---
`;
                  const solutionText = scraped.text.slice(0, 4e3);
                  scrapedContext += `${solutionText}

`;
                  const matchingGenSource = generatedSources.find((gs) => gs.uri === item.url);
                  if (matchingGenSource) {
                    matchingGenSource.scrapedSolution = solutionText;
                  }
                }
              });
            } catch (err) {
              console.error("[Validation] Failed parallel scraping:", err);
            }
          }
        }
      }
      let ytContext = "";
      const containsYtUrl = /(?:youtube\.com\/(?:[^\/\n\s]+\/\S+\/|(?:v|e(?:mbed)?)\/|\S*?[?&]v=)|youtu\.be\/)/i.test(searchQuery);
      const isYtRequest = !containsYtUrl && /latest\s+video|youtube\b|yt\b|video\s+of|video\s+from|upload\s+of|upload\s+from|tell me.*video/i.test(searchQuery);
      if (useInternet2 && isYtRequest) {
        let cleanQuery = searchQuery.replace(/tell me/gi, "").replace(/latest video of/gi, "").replace(/latest video from/gi, "").replace(/video of/gi, "").replace(/video from/gi, "").replace(/upload of/gi, "").replace(/upload from/gi, "").replace(/show me/gi, "").replace(/what is/gi, "").replace(/the/gi, "").trim();
        if (!cleanQuery) {
          cleanQuery = searchQuery;
        }
        const ytSearchQuery = `${cleanQuery} latest video`;
        try {
          const ytSearchUrl = `/api/yt-search?q=${encodeURIComponent(ytSearchQuery)}`;
          const ytRes = await fetch(ytSearchUrl);
          if (ytRes.ok) {
            const ytData = await ytRes.json();
            const results = ytData.results || [];
            if (results.length > 0) {
              ytContext = `

[CRITICAL DIRECTIVE: REAL-TIME YOUTUBE VIDEO MODE ACTIVATED]
The user is requesting video suggestions, latest uploads, or channel info. You MUST use the verified YouTube search results provided below.
To present the primary video, you MUST render a YouTube Card Widget.
The YouTube Card Widget is generated by returning a single JSON block wrapped in \`\`\`youtube-card:
\`\`\`youtube-card
{
  "videoId": "${results[0].videoId}",
  "title": ${JSON.stringify(results[0].title)},
  "creator": ${JSON.stringify(results[0].author)},
  "views": "${results[0].views || "1M+"}",
  "uploadDate": "${results[0].uploadedAt || "Recently"}",
  "whyRecommend": "This is the latest video uploaded by the creator/channel matching your query."
}
\`\`\`

Here are the top search results returned from YouTube for "${searchQuery}":
`;
              results.slice(0, 5).forEach((v, idx) => {
                ytContext += `[Video ${idx + 1}] Title: ${v.title}
Link: https://www.youtube.com/watch?v=${v.videoId}
Channel/Creator: ${v.author}
Duration: ${v.length_seconds} seconds
Thumbnail: ${v.thumbnail}
Views: ${v.views}
Uploaded: ${v.uploadedAt}

`;
                generatedSources.push({
                  uri: `https://www.youtube.com/watch?v=${v.videoId}`,
                  title: v.title,
                  favicon: "https://www.google.com/s2/favicons?domain=youtube.com",
                  snippet: `YouTube Video by ${v.author}. Views: ${v.views}. Uploaded: ${v.uploadedAt}. Length: ${Math.floor(v.length_seconds / 60)}m ${v.length_seconds % 60}s.`
                });
              });
              ytContext += `
INSTRUCTIONS: Formulate a detailed, premium response describing the latest video and other recent ones. Output the primary video's \`\`\`youtube-card\`\`\` block. Under that, cite the other relevant videos as standard text or list links, which will be styled as inline sources.`;
            }
          }
        } catch (ytErr) {
          console.warn("YouTube live search query failed:", ytErr);
        }
      }
      let crawlContext = "";
      try {
        const lowerMsg2 = searchQuery.toLowerCase();
        const isCrawlIntent = lowerMsg2.includes("crawl") || lowerMsg2.includes("fetch data") || lowerMsg2.includes("scrape") || lowerMsg2.includes("go to the website") || lowerMsg2.includes("go to this website") || lowerMsg2.includes("go to website") || lowerMsg2.includes("visit the website") || lowerMsg2.includes("visit this website") || lowerMsg2.includes("visit website") || lowerMsg2.includes("look at this website") || lowerMsg2.includes("look at the website") || lowerMsg2.includes("take a look of this website") || lowerMsg2.includes("take a look at this website") || lowerMsg2.includes("analyze the website") || lowerMsg2.includes("analyze this website") || lowerMsg2.includes("read the content") || lowerMsg2.includes("website content");
        const explicitUrlRegex = /(https?:\/\/[^\s]+)/gi;
        let urlsToCrawl = [];
        const explicitMatches = searchQuery.match(explicitUrlRegex);
        if (explicitMatches) {
          urlsToCrawl = explicitMatches.map((u) => u.replace(/[.,;!?]$/, ""));
        } else {
          const words = searchQuery.split(/\s+/);
          for (const word of words) {
            const cleanWord = word.replace(/[.,;!?()'"\[\]]$/, "").replace(/^[()'"\[\]]/, "");
            if (cleanWord.includes(".") && !cleanWord.startsWith(".") && !cleanWord.endsWith(".")) {
              const dotIndex = cleanWord.indexOf(".");
              const afterDot = cleanWord.slice(dotIndex + 1);
              if (/^[a-zA-Z]{2,}/.test(afterDot)) {
                urlsToCrawl.push(cleanWord.startsWith("http") ? cleanWord : "https://" + cleanWord);
              }
            }
          }
        }
        const shouldCrawl = urlsToCrawl.length > 0;
        if (useInternet2 && shouldCrawl) {
          const isDeepCrawl = lowerMsg2.includes("feature") || lowerMsg2.includes("sitemap") || lowerMsg2.includes("all pages") || lowerMsg2.includes("other pages") || lowerMsg2.includes("sub-pages") || lowerMsg2.includes("link") || lowerMsg2.includes("deep") || lowerMsg2.includes("analyze content") || lowerMsg2.includes("tell me what is");
          console.log(`[Crawler] Starting crawl for: ${urlsToCrawl[0]} (deep: ${isDeepCrawl})`);
          const crawlResult = await fetchCrawlResults(urlsToCrawl[0], isDeepCrawl);
          if (crawlResult && crawlResult.text) {
            crawlContext = `

[CRITICAL DIRECTIVE: REAL-TIME WEB CRAWL MODE ACTIVATED]
The user has provided a link (${urlsToCrawl[0]}) to be analyzed, crawled, or read.
You MUST base your response and summary on the verified live web crawl contents provided below.
If this is a YouTube link, you are acting as an AI video summarizer. Analyze the video using the provided metadata, title, channel name, description, and transcript text. If the transcript is not fully detailed or is missing, synthesize a comprehensive summary of the video's topic using the provided title, channel name, and any search/description context. Do NOT state that the crawl failed or complain about network restrictions; explain the video content or topic as best as possible using the available info!
CRITICAL: Do NOT make up any information, do not summarize random mathematical or other unrelated topics, and do not hallucinate. Ground your response 100% on the crawl text below.

LIVE WEB CRAWL CONTENTS:
${crawlResult.text}

INSTRUCTIONS: Synthesize the live crawled text above to answer the user's request. State clearly in your response that you have successfully crawled the link and are summarizing its actual content.`;
            crawlResult.crawledUrls.forEach((url) => {
              let hostname = "";
              try {
                hostname = new URL(url).hostname;
              } catch (e) {
              }
              generatedSources.push({
                uri: url,
                title: `Crawled Page: ${url.replace(/^https?:\/\//i, "")}`,
                favicon: hostname ? `https://www.google.com/s2/favicons?domain=${hostname}` : "",
                snippet: `Direct crawled contents parsed and analyzed by AI.`
              });
            });
          }
        }
      } catch (crawlErr) {
        console.warn("Crawl process failed:", crawlErr);
      }
      const isGraphOrPlotRequest = /graph\b|plot\b|visualize.*function|graphical\b/i.test(searchQuery);
      let graphDirective = "";
      if (isGraphOrPlotRequest) {
        graphDirective = `

GRAPHING CALCULATOR PLUGIN
If the user requests to graph, plot, or visualize a mathematical function (e.g. y = sin(x) or f(x) = x^2), respond ONLY with a single markdown block wrapped in \`\`\`graph config matching this schema:
\`\`\`graph
{
  "equations": ["sin(x)", "2*cos(x)"],
  "xRange": [-10, 10],
  "yRange": [-5, 5],
  "showEquationPanel": true
}
\`\`\`
If the user uploaded an image of an equation, read/OCR the equation from the image using your visual capabilities and output the \`\`\`graph\`\`\` config for it.`;
      }
      const codeAndWidgetRestriction = `

[CRITICAL RESTRICTION - CODE AND WIDGETS]
1. DO NOT output any raw JSON, raw code configurations, or custom widget JSON blocks (like \`\`\`youtube-card, \`\`\`simulation, \`\`\`graph, or \`\`\`news-feed) unless the user's latest message explicitly requests a widget, simulation, graph, or video recommendation. Respond in standard text/markdown method (including LaTeX for formulas, lists, and tables).
2. DO NOT write code blocks (like Python, C++, Java, JS, HTML, etc.) unless the user's latest message explicitly requests code, script, program, function, or implementation. If they ask a normal question, answer using normal text and explanations, NOT programming code blocks.
`;
      let validationInstruction = "";
      let selfValidationInstruction = "";
      if (useVerify) {
        if (scrapedContext) {
          validationInstruction = `

[CRITICAL DIRECTIVE: MULTI-SOURCE SOLUTION VALIDATION & VERIFICATION LOOP]
You are acting in MULTI-SOURCE SOLUTION VALIDATION mode. Below are the detailed page contents scraped from high-authority educational resources regarding this specific problem/concept:

${scrapedContext}

Your mission is to output a 100% mathematically and scientifically accurate answer. Follow these instructions:
1. **Analyze Scraped Solutions**: Carefully read the scraped contents from the educational websites. Extract the final answer keys, options, and intermediate calculations from each source.
2. **Solve Independently**: Solve the question on your own using physical laws, chemical mechanisms, or mathematical formulas.
3. **Compare & Verify**:
   - Compare your independent solution with the scraped solutions.
   - If there's a consensus across sources and your independent work, explain it clearly.
   - If there is a contradiction (e.g., source 1 says option A, source 2 says option B, or your independent calculation yields a different answer), analyze the steps of the discrepant solutions. Identify any arithmetic errors, sign errors, incorrect assumptions, or misinterpretations of the question in those solutions.
   - Rectify the error and determine the mathematically correct option.
4. **Draft Detailed Explanation**: Write a premium, step-by-step educational breakdown using LaTeX for mathematical styling.
5. **Output the Multi-Source Validation Audit**:
   You MUST append a section titled "### 🔍 Multi-Source Validation Audit" at the very end of your response. For each source checked:
   - Identify the source URL/name.
   - State the solution/option found on that source.
   - Mention the verification result (e.g., "Verified correct", "Contains arithmetic error in Step 3, corrected here", etc.).
   - Conclude with: "✅ Final Status: Answer verified with 100% accuracy."
`;
        } else {
          selfValidationInstruction = `

[CRITICAL DIRECTIVE: DUAL-SOLVER SELF-VERIFICATION LOOP]
Since live educational website scraping yielded no direct solution hits, you MUST execute a strict self-verification workflow:
1. Solve the question using Method A (e.g. direct calculation or algebraic formulation).
2. Solve the question using Method B (e.g. dimensional analysis, special cases, graph sketching, or alternative formulas) to double-check.
3. Compare the two paths. If they lead to different results, find the mistake in your reasoning and resolve it.
4. Output a detailed explanation and add a section titled "### 🔍 Self-Verification Audit" detailing:
   - Method A verification steps.
   - Method B verification steps.
   - Final status: ✅ Checked for consistency and confirmed correct.
`;
        }
      }
      let finalSystemInstruction = "";
      if (useInternet2 && !useAI2) {
        finalSystemInstruction = `SYSTEM IDENTITY:
You are Calculus AI made by OM, running in strict SEARCH RESULTS ONLY mode.
All your pre-trained academic, general, and historical knowledge has been disabled.
You MUST compile, format, and present the response using ONLY the real-time internet search results and web crawled contents provided below.
Do NOT modify, speculate, extrapolate, or add details not found in the search/crawling data.
If the provided search results/crawled content do not contain the answer, you must respond with: "I'm sorry, but that information is not available in the real-time search results."
Do not write programming code blocks unless code is explicitly present in the search results and requested.
Always display search source logos, names, and links for citation.

${searchContext}
${crawlContext}
${ytContext}

${dateInstruction}
${imageGenerationInstruction}
${graphDirective}
${codeAndWidgetRestriction}`;
      } else if (useInternet2 && useAI2) {
        const ragInstruction = `

[CRITICAL DIRECTIVE: REAL-TIME RAG MODE ACTIVATED]
You have real-time internet search access. Combine the provided search results/crawled content with your pre-trained knowledge base and reasoning capabilities to synthesize a comprehensive, proper, finalized, and accurate response. Cite sources precisely.`;
        finalSystemInstruction = systemInstruction + goalSpecificInstruction + imageGenerationInstruction + dateInstruction + searchContext + crawlContext + ytContext + ragInstruction + validationInstruction + selfValidationInstruction + graphDirective + codeAndWidgetRestriction;
      } else {
        const aiOnlyInstruction = `

[PRE-TRAINED DATASET MODE ACTIVATED]
You must answer this query using ONLY your pre-trained dataset/knowledge base. Web search is disabled. Do not generate or refer to search citations.`;
        finalSystemInstruction = systemInstruction + goalSpecificInstruction + imageGenerationInstruction + dateInstruction + aiOnlyInstruction + selfValidationInstruction + graphDirective + codeAndWidgetRestriction;
      }
      let visionPrompt = hasImages ? "Please scan, read, and analyze the uploaded image carefully. Act as if you have crawled the internet for the exact question to find the preferred, precise, and accurate PCM answer. Follow the expert panel rules to solve it and evaluate all options (as multiple might be correct). Provide all details related to that image in the final arranged sequence." : "";
      if (hasImages && useVerify) {
        visionPrompt += " Cross-reference this image analysis with the scraped web solution data to ensure 100% accuracy.";
      }
      if (hasImages && diagramDescriptionText) {
        visionPrompt += `

[DETAILED DIAGRAM/VISUAL TRANSCRIPTION AND SPECIFICATION]:
${diagramDescriptionText}`;
      }
      const openRouterFreeModels = [
        "google/gemma-4-26b-a4b-it:free",
        "google/gemma-4-31b-it:free",
        "liquid/lfm-2.5-1.2b-thinking:free",
        "liquid/lfm-2.5-1.2b-instruct:free",
        "openai/gpt-oss-120b:free",
        "openai/gpt-oss-20b:free",
        "z-ai/glm-4.5-air:free",
        "nvidia/nemotron-3.5-content-safety:free",
        "nvidia/nemotron-3-ultra-550b-a55b:free",
        "nousresearch/hermes-3-llama-3.1-405b:free",
        "moonshotai/kimi-k2.6:free",
        "meta-llama/llama-3.3-70b-instruct:free",
        "qwen/qwen-2.5-coder-32b-instruct:free",
        "google/gemma-2-9b-it:free"
      ];
      const searchCapableModels = [
        "meta-llama/llama-3.3-70b-instruct:free",
        "qwen/qwen-2.5-coder-32b-instruct:free",
        "google/gemma-2-9b-it:free"
      ];
      const openRouterImageModels = [
        "google/gemini-2.5-flash",
        "meta-llama/llama-3.2-11b-vision-instruct:free",
        "meta-llama/llama-3.2-90b-vision-instruct:free",
        "nvidia/nemotron-3-nano-omni-30b-a3b-reasoning:free",
        "nvidia/llama-nemotron-rerank-vl-1b-v2:free",
        "nex-agi/nex-n2-pro:free",
        "nvidia/nemotron-3.5-content-safety:free",
        "google/gemma-4-31b-it:free",
        "google/gemma-4-26b-a4b-it:free"
      ];
      let success = false;
      let targetModels = [];
      const lowerMsg = lastMsg.toLowerCase();
      const isCodeRequest = lowerMsg.includes("code") || lowerMsg.includes("script") || lowerMsg.includes("program") || lowerMsg.includes("function") || lowerMsg.includes("html");
      const isMathReasoningRequest = lowerMsg.includes("math") || lowerMsg.includes("solve") || lowerMsg.includes("reasoning") || lowerMsg.includes("calculate") || lowerMsg.includes("equation") || lowerMsg.includes("physics") || lowerMsg.includes("chemistry");
      if (hasImages || isImageRequest) {
        targetModels = openRouterImageModels;
      } else if (isMathReasoningRequest) {
        const prioritized = [
          "qwen/qwen-2.5-coder-32b-instruct:free",
          "meta-llama/llama-3.3-70b-instruct:free",
          "liquid/lfm-2.5-1.2b-thinking:free",
          "openai/gpt-oss-120b:free",
          "z-ai/glm-4.5-air:free",
          "nousresearch/hermes-3-llama-3.1-405b:free",
          "moonshotai/kimi-k2.6:free"
        ];
        targetModels = [...prioritized, ...openRouterFreeModels.filter((m) => !prioritized.includes(m))];
      } else if (isCodeRequest) {
        const prioritized = [
          "qwen/qwen-2.5-coder-32b-instruct:free",
          "openai/gpt-oss-120b:free",
          "meta-llama/llama-3.3-70b-instruct:free"
        ];
        targetModels = [...prioritized, ...openRouterFreeModels.filter((m) => !prioritized.includes(m))];
      } else {
        const prioritized = [
          "google/gemma-2-9b-it:free",
          "openai/gpt-oss-20b:free",
          "liquid/lfm-2.5-1.2b-instruct:free",
          "google/gemma-4-26b-a4b-it:free"
        ];
        targetModels = [...prioritized, ...openRouterFreeModels.filter((m) => !prioritized.includes(m))];
      }
      const messagesToUse = messagesToSent.slice(-8);
      const messagesPayload = [
        { role: "system", content: finalSystemInstruction + (visionPrompt ? "\n\n" + visionPrompt : "") },
        ...messagesToUse.map((m, idx) => {
          let contentText = m.content;
          if (contentText.length > 15e4) {
            contentText = contentText.slice(0, 15e4) + "\n\n...[Content truncated to fit AI limits]...";
          }
          if (idx === messagesToUse.length - 1 && m.role === "user" && filePayloads && filePayloads.length > 0) {
            const contentArray = [{ type: "text", text: contentText || "Please analyze the uploaded image." }];
            filePayloads.forEach((fp) => contentArray.push({ type: "image_url", image_url: { url: `data:${fp.inlineData.mimeType};base64,${fp.inlineData.data}` } }));
            return { role: "user", content: contentArray };
          }
          return { role: m.role === "model" ? "assistant" : "user", content: contentText };
        })
      ];
      let primaryApiError = "";
      for (const modelName of targetModels) {
        if (signal.aborted) throw { name: "AbortError" };
        const modelAbort = new AbortController();
        const onParentAbort = () => modelAbort.abort();
        signal.addEventListener("abort", onParentAbort);
        const timeoutId = setTimeout(() => {
          modelAbort.abort();
        }, 8e3);
        try {
          const reqBody = {
            model: modelName,
            messages: messagesPayload
          };
          if (searchCapableModels.includes(modelName)) {
            reqBody.plugins = [{ id: "web", max_results: 5 }];
          }
          let response;
          try {
            response = await fetch("https://openrouter.ai/api/v1/chat/completions", {
              method: "POST",
              headers: {
                "Authorization": `Bearer ${apiKey.trim()}`,
                "HTTP-Referer": window.location.href,
                "X-Title": "StudE",
                "Content-Type": "application/json"
              },
              body: JSON.stringify(reqBody),
              signal: modelAbort.signal
            });
          } catch (e) {
            if (e.name === "AbortError" && signal.aborted) throw e;
            response = await fetch(`https://corsproxy.io/?${encodeURIComponent("https://openrouter.ai/api/v1/chat/completions")}`, {
              method: "POST",
              headers: {
                "Authorization": `Bearer ${apiKey.trim()}`,
                "HTTP-Referer": window.location.href,
                "X-Title": "StudE",
                "Content-Type": "application/json"
              },
              body: JSON.stringify(reqBody),
              signal: modelAbort.signal
            });
          }
          clearTimeout(timeoutId);
          signal.removeEventListener("abort", onParentAbort);
          if (!response.ok) {
            const errData = await response.json().catch(() => ({}));
            let errorMsg = response.statusText || String(response.status);
            if (errData.error?.message) errorMsg = errData.error.message;
            else if (typeof errData.detail === "string") errorMsg = errData.detail;
            else if (Array.isArray(errData.detail)) errorMsg = JSON.stringify(errData.detail);
            else if (errData.title) errorMsg = errData.title;
            throw new Error(`OpenRouter Error (${modelName}): ${errorMsg}`);
          }
          const data = await response.json();
          const messageObj = data.choices?.[0]?.message;
          const content2 = messageObj?.content;
          if (messageObj?.images && messageObj.images.length > 0) {
            generatedAttachments = messageObj.images.map((img) => ({
              url: img.image_url?.url || img.url || "",
              type: "image",
              name: "Generated Image"
            }));
          }
          if (messageObj?.citations && Array.isArray(messageObj.citations)) {
            messageObj.citations.forEach((cit) => {
              if (cit.url || cit.uri) {
                const uri = cit.url || cit.uri;
                let hostname = "";
                try {
                  hostname = new URL(uri).hostname;
                } catch (e) {
                }
                generatedSources.push({
                  uri,
                  title: cit.title || hostname || uri,
                  favicon: hostname ? `https://www.google.com/s2/favicons?domain=${hostname}` : ""
                });
              }
            });
          }
          if (content2 || generatedAttachments.length > 0) {
            responseText = content2 || (generatedAttachments.length > 0 ? "Here is your generated image." : "Done.");
            success = true;
            break;
          }
        } catch (err) {
          clearTimeout(timeoutId);
          signal.removeEventListener("abort", onParentAbort);
          if (err.name === "AbortError" && signal.aborted) {
            throw err;
          }
          console.warn(`Model ${modelName} failed or timed out:`, err);
          if (!primaryApiError) primaryApiError = err.message;
        }
      }
      if (!success) {
        throw new Error("AI Limits End");
      }
      generatedSources = generatedSources.filter((v, i, a) => a.findIndex((t) => t.uri === v.uri) === i);
      responseText = fixMath(responseText);
      const newMessagesHistory = [...messagesToSent, {
        role: "model",
        content: responseText,
        isTyping: true,
        attachments: generatedAttachments.length > 0 ? generatedAttachments : void 0,
        sources: generatedSources.length > 0 ? generatedSources : void 0
      }];
      try {
        const raw = localStorage.getItem("jee_ai_chats");
        if (raw) {
          const sessions = JSON.parse(raw);
          const updated = sessions.map((s) => s.id === sessionId ? {
            ...s,
            messages: newMessagesHistory,
            updatedAt: Date.now()
          } : s);
          localStorage.setItem("jee_ai_chats", JSON.stringify(updated));
        }
      } catch (e) {
      }
      this.notify();
      this.autoGenerateTitle(sessionId, newMessagesHistory);
    } catch (e) {
      if (e.name === "AbortError" || signal.aborted) {
        try {
          const raw = localStorage.getItem("jee_ai_chats");
          if (raw) {
            const sessions = JSON.parse(raw);
            const updated = sessions.map((s) => s.id === sessionId ? {
              ...s,
              messages: [...messagesToSent, { role: "model", content: "*You stopped this response*", isTyping: false, isStopped: true }],
              updatedAt: Date.now()
            } : s);
            localStorage.setItem("jee_ai_chats", JSON.stringify(updated));
          }
        } catch (err) {
        }
        this.notify();
        return;
      }
      const errorContent = e.message === "AI Limits End" ? "AI Limits End" : e.message.includes("Maintenance") ? e.message : `Error: ${e.message}`;
      try {
        const raw = localStorage.getItem("jee_ai_chats");
        if (raw) {
          const sessions = JSON.parse(raw);
          const updated = sessions.map((s) => s.id === sessionId ? {
            ...s,
            messages: [...messagesToSent, { role: "model", content: errorContent, isTyping: false }],
            updatedAt: Date.now()
          } : s);
          localStorage.setItem("jee_ai_chats", JSON.stringify(updated));
        }
      } catch (err) {
      }
      this.notify();
    } finally {
      this.activeJobs.delete(sessionId);
      this.notify();
    }
  }
}
const aiChatBackgroundManager = new AIChatBackgroundManager();
function AIChatInterface({ onBack }) {
  const { user, selectedGoal } = useAppContext();
  const [sessions, setSessions] = reactExports.useState(() => {
    try {
      return JSON.parse(localStorage.getItem("jee_ai_chats") || "[]");
    } catch {
      return [];
    }
  });
  const updateSessions = (updater) => {
    const next = typeof updater === "function" ? updater(sessions) : updater;
    localStorage.setItem("jee_ai_chats", JSON.stringify(next));
    setSessions(next);
  };
  const [activeSessionId, setActiveSessionId] = reactExports.useState(null);
  const [showSettingsMenu, setShowSettingsMenu] = reactExports.useState(false);
  const [settings, setSettings] = reactExports.useState(() => {
    try {
      const raw = localStorage.getItem("jee_ai_chat_settings");
      if (raw) {
        const parsed = JSON.parse(raw);
        if (typeof parsed.internet === "boolean" && typeof parsed.ai === "boolean") {
          return {
            internet: parsed.internet,
            ai: parsed.ai,
            verify: typeof parsed.verify === "boolean" ? parsed.verify : true
          };
        }
      }
    } catch {
    }
    return { internet: true, ai: true, verify: true };
  });
  const toggleSetting = (key) => {
    setSettings((prev) => {
      if (key !== "verify") {
        if (prev[key] && !prev[key === "internet" ? "ai" : "internet"]) {
          return prev;
        }
      }
      const next = { ...prev, [key]: !prev[key] };
      localStorage.setItem("jee_ai_chat_settings", JSON.stringify(next));
      return next;
    });
  };
  const [visibleMessagesCount, setVisibleMessagesCount] = reactExports.useState(15);
  const [input, setInput] = reactExports.useState("");
  const [showCommands, setShowCommands] = reactExports.useState(false);
  const [selectedCommandIndex, setSelectedCommandIndex] = reactExports.useState(0);
  const commandMenuRef = reactExports.useRef(null);
  const commandListRef = reactExports.useRef(null);
  const [activeSourcesDropdown, setActiveSourcesDropdown] = reactExports.useState(null);
  const [activeTtsIdx, setActiveTtsIdx] = reactExports.useState(null);
  const synthRef = reactExports.useRef(typeof window !== "undefined" ? window.speechSynthesis : null);
  const utteranceRef = reactExports.useRef(null);
  reactExports.useEffect(() => {
    return () => {
      if (synthRef.current) {
        synthRef.current.cancel();
      }
    };
  }, []);
  const cleanTextForTts = (text2) => {
    let cleaned = text2;
    cleaned = cleaned.replace(/```[\s\S]*?```/g, " [code block omitted] ");
    cleaned = cleaned.replace(/`([^`]+)`/g, "$1");
    cleaned = cleaned.replace(/\$\$([\s\S]*?)\$\$/g, (match, p1) => {
      return " [formula: " + p1.replace(/\\frac{([^}]+)}{([^}]+)}/g, "$1 over $2").replace(/\\boxed{([^}]+)}/g, "$1").replace(/\\text{([^}]+)}/g, "$1").replace(/[\\]/g, "") + "] ";
    });
    cleaned = cleaned.replace(/\$([^$]+)\$/g, (match, p1) => {
      return p1.replace(/\\frac{([^}]+)}{([^}]+)}/g, "$1 over $2").replace(/\\boxed{([^}]+)}/g, "$1").replace(/\\text{([^}]+)}/g, "$1").replace(/[\\]/g, "");
    });
    cleaned = cleaned.replace(/<[^>]*>/g, "");
    cleaned = cleaned.replace(/\*\*([^*]+)\*\*/g, "$1");
    cleaned = cleaned.replace(/\*([^*]+)\*/g, "$1");
    cleaned = cleaned.replace(/__([^_]+)__/g, "$1");
    cleaned = cleaned.replace(/_([^_]+)_/g, "$1");
    cleaned = cleaned.replace(/~~([^~]+)~~/g, "$1");
    cleaned = cleaned.replace(/\[([^\]]+)\]\([^)]+\)/g, "$1");
    cleaned = cleaned.replace(/^\s*[-*+]\s+/gm, "");
    cleaned = cleaned.replace(/^\s*\d+\.\s+/gm, "");
    cleaned = cleaned.replace(/[#*~_-]+/g, " ");
    cleaned = cleaned.replace(/\s+/g, " ").trim();
    return cleaned;
  };
  const detectLanguage = (text2) => {
    if (/[\u0900-\u097F]/.test(text2)) {
      return "hi";
    }
    const hinglishKeywords = /\b(aap|tum|tera|mera|hum|apna|apne|apni|kaise|kya|kyun|kab|kahan|kidhar|udhar|idhar|se|ko|ka|ki|ke|me|mein|par|bhi|hi|toh|to|tha|thi|the|hai|hain|ho|hoga|hogi|hoge|kar|karna|karta|karte|karti|raha|rahe|rahi|gaya|gaye|gayi|chahiye|sakte|sakta|sakti|liye|diya|de|do|le|lo|aur|ya|lekin|magar|parantu|nhi|nahi|mat|na|kuch|sab|koi|kisi|achha|acha|theek|thik|yaar|bhai|chal|chalo)\b/i;
    const words = text2.toLowerCase().split(/\s+/);
    let matchCount = 0;
    for (const word of words) {
      if (hinglishKeywords.test(word)) {
        matchCount++;
      }
    }
    if (matchCount >= 2 || words.length > 0 && matchCount / words.length > 0.05) {
      return "hinglish";
    }
    return "en";
  };
  const handleToggleTts = (text2, index2) => {
    if (!synthRef.current) return;
    if (activeTtsIdx === index2) {
      synthRef.current.cancel();
      setActiveTtsIdx(null);
      return;
    }
    synthRef.current.cancel();
    setActiveTtsIdx(index2);
    const cleanedText = cleanTextForTts(text2);
    const utterance = new SpeechSynthesisUtterance(cleanedText);
    utteranceRef.current = utterance;
    const detectedLang = detectLanguage(text2);
    console.log("[TTS] Detected language:", detectedLang);
    const voices = synthRef.current.getVoices();
    let selectedVoice = null;
    if (detectedLang === "hi") {
      selectedVoice = voices.find(
        (v) => (v.lang.startsWith("hi") || v.lang.startsWith("hi-IN") || v.lang.startsWith("hi_IN")) && (v.name.includes("Online") || v.name.includes("Natural") || v.name.includes("Google") || v.name.includes("Swara") || v.name.includes("Kalpana")) && (v.name.toLowerCase().includes("female") || v.name.toLowerCase().includes("woman") || v.name.includes("Swara") || v.name.includes("Kalpana") || v.name.includes("Google"))
      );
      if (!selectedVoice) {
        selectedVoice = voices.find(
          (v) => (v.lang.startsWith("hi") || v.lang.startsWith("hi-IN") || v.lang.startsWith("hi_IN")) && (v.name.includes("Online") || v.name.includes("Natural") || v.name.includes("Google") || v.name.includes("Swara") || v.name.includes("Kalpana"))
        );
      }
      if (!selectedVoice) {
        selectedVoice = voices.find((v) => v.lang.startsWith("hi") || v.lang.startsWith("hi-IN") || v.lang.startsWith("hi_IN"));
      }
    } else if (detectedLang === "hinglish") {
      selectedVoice = voices.find(
        (v) => (v.lang.startsWith("en-IN") || v.lang.startsWith("en_IN")) && (v.name.includes("Online") || v.name.includes("Natural") || v.name.includes("Google")) && (v.name.toLowerCase().includes("female") || v.name.toLowerCase().includes("woman") || v.name.includes("Neerja") || v.name.includes("Heera") || v.name.includes("Veena") || v.name.includes("Priya") || v.name.includes("Google"))
      );
      if (!selectedVoice) {
        selectedVoice = voices.find(
          (v) => (v.lang.startsWith("en-IN") || v.lang.startsWith("en_IN")) && (v.name.includes("Neerja") || v.name.includes("Heera") || v.name.includes("Veena") || v.name.includes("Priya"))
        );
      }
      if (!selectedVoice) {
        selectedVoice = voices.find((v) => v.lang.startsWith("en-IN") || v.lang.startsWith("en_IN"));
      }
      if (!selectedVoice) {
        selectedVoice = voices.find(
          (v) => v.lang.startsWith("en") && (v.name.includes("Online") || v.name.includes("Natural") || v.name.includes("Google")) && (v.name.toLowerCase().includes("female") || v.name.toLowerCase().includes("woman") || v.name.includes("Aria") || v.name.includes("Jenny") || v.name.includes("Sonia") || v.name.includes("Samantha") || v.name.includes("Zira"))
        );
      }
    }
    if (!selectedVoice) {
      selectedVoice = voices.find(
        (v) => v.lang.startsWith("en") && (v.name.includes("Online") || v.name.includes("Natural") || v.name.includes("Google")) && (v.name.toLowerCase().includes("female") || v.name.toLowerCase().includes("woman") || v.name.includes("Aria") || v.name.includes("Jenny") || v.name.includes("Sonia") || v.name.includes("Samantha") || v.name.includes("Zira"))
      );
      if (!selectedVoice) {
        selectedVoice = voices.find(
          (v) => v.lang.startsWith("en") && (v.name.includes("Natural") || v.name.includes("Neural") || v.name.includes("Google") || v.name.includes("Aria") || v.name.includes("Jenny") || v.name.includes("Sonia"))
        );
      }
      if (!selectedVoice) {
        selectedVoice = voices.find(
          (v) => v.lang.startsWith("en") && (v.name.includes("Samantha") || v.name.includes("Alex") || v.name.includes("Daniel") || v.name.includes("Karen") || v.name.includes("Zira"))
        );
      }
      if (!selectedVoice) {
        selectedVoice = voices.find((v) => v.lang.startsWith("en-US") || v.lang.startsWith("en_US") || v.lang.startsWith("en-GB") || v.lang.startsWith("en_GB"));
      }
      if (!selectedVoice) {
        selectedVoice = voices.find((v) => v.lang.startsWith("en"));
      }
    }
    if (selectedVoice) {
      console.log("[TTS] Selected voice:", selectedVoice.name, selectedVoice.lang);
      utterance.voice = selectedVoice;
      utterance.lang = selectedVoice.lang;
    } else {
      utterance.lang = detectedLang === "hi" ? "hi-IN" : detectedLang === "hinglish" ? "en-IN" : "en-US";
    }
    utterance.rate = detectedLang === "hi" ? 0.95 : 1;
    utterance.pitch = 1;
    utterance.volume = 1;
    utterance.onend = () => {
      setActiveTtsIdx(null);
    };
    utterance.onerror = () => {
      setActiveTtsIdx(null);
    };
    synthRef.current.speak(utterance);
  };
  const filteredCommands = reactExports.useMemo(() => {
    if (!input.startsWith("/")) return [];
    const query = input.toLowerCase();
    return SLASH_COMMANDS.filter((cmd) => cmd.name.startsWith(query));
  }, [input]);
  reactExports.useEffect(() => {
    const handleOutsideClick = (e) => {
      if (commandMenuRef.current && !commandMenuRef.current.contains(e.target)) {
        setShowCommands(false);
      }
    };
    if (showCommands) {
      document.addEventListener("mousedown", handleOutsideClick);
    }
    return () => document.removeEventListener("mousedown", handleOutsideClick);
  }, [showCommands]);
  reactExports.useEffect(() => {
    if (commandListRef.current) {
      const activeEl = commandListRef.current.children[selectedCommandIndex];
      if (activeEl) {
        const container = commandListRef.current;
        const activeTop = activeEl.offsetTop;
        const activeHeight = activeEl.offsetHeight;
        const containerHeight = container.clientHeight;
        const containerScrollTop = container.scrollTop;
        if (activeTop < containerScrollTop) {
          container.scrollTop = activeTop;
        } else if (activeTop + activeHeight > containerScrollTop + containerHeight) {
          container.scrollTop = activeTop + activeHeight - containerHeight;
        }
      }
    }
  }, [selectedCommandIndex]);
  const selectCommand = (cmd) => {
    if (cmd === "/clear") {
      if (activeSessionId) {
        updateSessions((prev) => prev.map((s) => s.id === activeSessionId ? { ...s, messages: [], updatedAt: Date.now() } : s));
      }
      setInput("");
      setAttachedFiles([]);
    } else if (cmd === "/export") {
      setInput("");
      exportAsImage();
    } else {
      setInput(cmd + " ");
      textareaRef.current?.focus();
    }
    setShowCommands(false);
  };
  const [activeJobsCount, setActiveJobsCount] = reactExports.useState(0);
  reactExports.useEffect(() => {
    const unsubscribe = aiChatBackgroundManager.subscribe(() => {
      const raw = localStorage.getItem("jee_ai_chats");
      if (raw) {
        setSessions(JSON.parse(raw));
      }
      setActiveJobsCount(aiChatBackgroundManager.activeJobs.size);
    });
    return () => {
      unsubscribe();
    };
  }, []);
  const loading = aiChatBackgroundManager.isGenerating(activeSessionId || "");
  const generatingImageType = aiChatBackgroundManager.isGeneratingImageType(activeSessionId || "");
  const [sidebarOpen, setSidebarOpen] = reactExports.useState(() => typeof window !== "undefined" ? window.innerWidth > 768 : true);
  const [editingId, setEditingId] = reactExports.useState(null);
  const [editTitle, setEditTitle] = reactExports.useState("");
  const [attachedFiles, setAttachedFiles] = reactExports.useState([]);
  const fileInputRef = reactExports.useRef(null);
  const [isDragging, setIsDragging] = reactExports.useState(false);
  const [imageToEdit, setImageToEdit] = reactExports.useState(null);
  const [fullScreenImage, setFullScreenImage] = reactExports.useState(null);
  const [chatMode, setChatMode] = reactExports.useState("academic");
  const [showManualGrapher, setShowManualGrapher] = reactExports.useState(false);
  const messagesEndRef = reactExports.useRef(null);
  const textareaRef = reactExports.useRef(null);
  const [copiedIdx, setCopiedIdx] = reactExports.useState(null);
  const [exportingImage, setExportingImage] = reactExports.useState(false);
  const [editingMessageIdx, setEditingMessageIdx] = reactExports.useState(null);
  const [editingMessageText, setEditingMessageText] = reactExports.useState("");
  const [likedMsgs, setLikedMsgs] = reactExports.useState({});
  reactExports.useEffect(() => {
    setLikedMsgs({});
  }, [activeSessionId]);
  const handleLikeMessage = (idx) => {
    setLikedMsgs((prev) => ({
      ...prev,
      [idx]: prev[idx] === "like" ? void 0 : "like"
    }));
  };
  const handleDislikeMessage = (idx) => {
    setLikedMsgs((prev) => ({
      ...prev,
      [idx]: prev[idx] === "dislike" ? void 0 : "dislike"
    }));
  };
  const copyToClipboard = (text2, idx) => {
    navigator.clipboard.writeText(text2);
    setCopiedIdx(idx);
    setTimeout(() => setCopiedIdx(null), 2e3);
  };
  const [isRecording, setIsRecording] = reactExports.useState(false);
  const [isTransliterating, setIsTransliterating] = reactExports.useState(false);
  const recognitionRef = reactExports.useRef(null);
  const speechBaseTextRef = reactExports.useRef("");
  const inputRef = reactExports.useRef(input);
  reactExports.useEffect(() => {
    inputRef.current = input;
  }, [input]);
  const processAudioTranscriptWithAI = async (text2) => {
    if (!text2.trim()) return text2;
    try {
      const apiKey = localStorage.getItem("jee_openrouter_api_key") || "";
      const headers = { "Content-Type": "application/json" };
      if (apiKey) {
        headers["Authorization"] = `Bearer ${apiKey.trim()}`;
      }
      const promptText = `You are a speech transcription post-processor. The user spoke into a microphone. The speech-to-text engine generated a raw phonetic transcript (which is written in the Latin alphabet, containing a mix of English and Hinglish/Hindi words).
Your task is to:
1. Clean up and format the transcript, correcting any misrecognized words (for example, if the speech recognizer transcribed "Jockey" instead of "jo ki", correct it to "jo ki").
2. Ensure that:
   - Any English parts remain in grammatically correct English.
   - Any Hindi parts are written in Hinglish format (Latin script, using phonetic words like "ha bhai samajh aaya na?", "h bhai tum keisaye ho", "tum kaise ho") instead of Hindi Devanagari script characters.
3. Respond ONLY with the final corrected transcription. Do not include any explanations, greetings, quotes, notes, or extra text.

Here is the raw transcription:
"${text2}"`;
      const payload = {
        model: "meta-llama/llama-3.3-70b-instruct:free",
        messages: [{ role: "user", content: promptText }]
      };
      let res;
      const url = "https://openrouter.ai/api/v1/chat/completions";
      try {
        res = await fetch(url, {
          method: "POST",
          headers,
          body: JSON.stringify(payload)
        });
      } catch (e) {
        res = await fetch(`https://corsproxy.io/?${encodeURIComponent(url)}`, {
          method: "POST",
          headers,
          body: JSON.stringify(payload)
        });
      }
      if (res.ok) {
        const data = await res.json();
        const result = data.choices?.[0]?.message?.content?.trim();
        if (result) {
          return result.replace(/^["']|["']$/g, "").trim();
        }
      }
    } catch (err) {
      console.error("Failed to process audio transcript with AI:", err);
    }
    return text2;
  };
  reactExports.useEffect(() => {
    const SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition;
    if (SpeechRecognition) {
      const rec = new SpeechRecognition();
      rec.continuous = false;
      rec.interimResults = true;
      rec.lang = "en-IN";
      rec.onstart = () => {
        setIsRecording(true);
      };
      rec.onresult = (event) => {
        let speechText = "";
        for (let i = event.resultIndex; i < event.results.length; ++i) {
          speechText += event.results[i][0].transcript;
        }
        const base = speechBaseTextRef.current;
        const separator = base && !base.endsWith(" ") ? " " : "";
        setInput(base + separator + speechText);
      };
      rec.onerror = (event) => {
        console.error("Speech recognition error:", event.error);
        if (event.error === "not-allowed") {
          alert("Microphone access was denied. Please allow microphone permissions in your browser settings.");
        }
        setIsRecording(false);
      };
      rec.onend = async () => {
        setIsRecording(false);
        const latestText = inputRef.current;
        const base = speechBaseTextRef.current;
        const newText = latestText.slice(base.length).trim();
        if (newText) {
          setIsTransliterating(true);
          try {
            const processedText = await processAudioTranscriptWithAI(newText);
            const separator = base && !base.endsWith(" ") ? " " : "";
            setInput(base + separator + processedText);
          } catch (e) {
            console.error("Processing error in onend:", e);
          } finally {
            setIsTransliterating(false);
          }
        }
      };
      recognitionRef.current = rec;
    }
    return () => {
      if (recognitionRef.current) {
        try {
          recognitionRef.current.abort();
        } catch (e) {
        }
      }
    };
  }, []);
  const handleMicClick = () => {
    const SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition;
    if (!SpeechRecognition) {
      alert("Speech recognition is not supported in this browser. Please try Chrome, Safari, or Edge.");
      return;
    }
    if (isRecording) {
      recognitionRef.current?.stop();
    } else {
      speechBaseTextRef.current = input;
      try {
        recognitionRef.current?.start();
      } catch (e) {
        console.error("Failed to start speech recognition:", e);
      }
    }
  };
  const exportAsImage = async () => {
    const element2 = document.getElementById("chat-capture-area");
    if (!element2) return;
    setExportingImage(true);
    const currentSession = sessions.find((s) => s.id === activeSessionId);
    const title = currentSession ? currentSession.title.toLowerCase().replace(/\s+/g, "_") : "chat";
    const filename = `chat_export_${title}.png`;
    let fileHandle = null;
    if ("showSaveFilePicker" in window) {
      try {
        fileHandle = await window.showSaveFilePicker({
          suggestedName: filename,
          types: [{
            description: "PNG Image",
            accept: {
              "image/png": [".png"]
            }
          }]
        });
      } catch (pickerErr) {
        if (pickerErr.name === "AbortError") {
          setExportingImage(false);
          return;
        }
        console.warn("showSaveFilePicker failed or was rejected, falling back to legacy download:", pickerErr);
      }
    }
    try {
      const html2canvas = await loadHtml2Canvas();
      await new Promise((resolve) => setTimeout(resolve, 300));
      const bodyBg = getComputedStyle(document.body).backgroundColor;
      const parsedBg = bodyBg && bodyBg !== "transparent" && bodyBg !== "rgba(0, 0, 0, 0)" ? bodyBg : getComputedStyle(element2).backgroundColor && getComputedStyle(element2).backgroundColor !== "transparent" && getComputedStyle(element2).backgroundColor !== "rgba(0, 0, 0, 0)" ? getComputedStyle(element2).backgroundColor : "#020817";
      const scrollContainer = element2.closest(".overflow-y-auto");
      const originalScrollTop = scrollContainer ? scrollContainer.scrollTop : 0;
      if (scrollContainer) {
        scrollContainer.scrollTop = 0;
        await new Promise((resolve) => setTimeout(resolve, 100));
      }
      const canvas = await html2canvas(element2, {
        useCORS: true,
        allowTaint: true,
        backgroundColor: parsedBg,
        scale: window.devicePixelRatio || 2,
        logging: false,
        height: element2.scrollHeight,
        windowHeight: element2.scrollHeight,
        scrollX: 0,
        scrollY: 0
      });
      if (scrollContainer) {
        scrollContainer.scrollTop = originalScrollTop;
      }
      if (fileHandle) {
        const blob = await new Promise((resolve) => canvas.toBlob(resolve, "image/png"));
        if (blob) {
          const writable = await fileHandle.createWritable();
          await writable.write(blob);
          await writable.close();
          return;
        }
      }
      const imgData = canvas.toDataURL("image/png");
      const link2 = document.createElement("a");
      link2.href = imgData;
      link2.download = filename;
      document.body.appendChild(link2);
      link2.click();
      document.body.removeChild(link2);
    } catch (err) {
      console.error("Failed to export chat as image:", err);
    } finally {
      setExportingImage(false);
    }
  };
  const activeSession = sessions.find((s) => s.id === activeSessionId);
  const messages = activeSession?.messages || [];
  const renderedMessages = reactExports.useMemo(() => {
    return messages.slice(-visibleMessagesCount);
  }, [messages, visibleMessagesCount]);
  const hasMoreMessages = messages.length > visibleMessagesCount;
  reactExports.useEffect(() => {
    setVisibleMessagesCount(15);
  }, [activeSessionId]);
  const isTyping = messages.length > 0 && messages[messages.length - 1].role === "model" && messages[messages.length - 1].isTyping;
  reactExports.useEffect(() => {
    localStorage.setItem("jee_ai_chats", JSON.stringify(sessions));
  }, [sessions]);
  const scrollToBottom = () => {
    messagesEndRef.current?.scrollIntoView({ behavior: "smooth" });
  };
  const prevMessagesLength = reactExports.useRef(messages.length);
  reactExports.useEffect(() => {
    if (messages.length > prevMessagesLength.current) {
      scrollToBottom();
    }
    prevMessagesLength.current = messages.length;
  }, [messages.length]);
  reactExports.useEffect(() => {
    const handleScroll = () => {
      if (messagesEndRef.current) messagesEndRef.current.scrollIntoView();
    };
    window.addEventListener("chat-typing", handleScroll);
    return () => window.removeEventListener("chat-typing", handleScroll);
  }, []);
  const handleStopGeneration = () => {
    if (activeSessionId && aiChatBackgroundManager.isGenerating(activeSessionId)) {
      aiChatBackgroundManager.stopJob(activeSessionId);
    } else if (isTyping) {
      markAsDone(messages.length - 1);
    }
  };
  reactExports.useEffect(() => {
    if (textareaRef.current) {
      textareaRef.current.style.height = "auto";
      textareaRef.current.style.height = Math.min(textareaRef.current.scrollHeight, 120) + "px";
    }
  }, [input]);
  const createNewChat = () => {
    setActiveSessionId(null);
    setInput("");
    if (window.innerWidth < 768) setSidebarOpen(false);
  };
  const markAsDone = (msgIndex) => {
    updateSessions((prev) => prev.map((s) => s.id === activeSessionId ? {
      ...s,
      messages: s.messages.map((msg, i) => i === msgIndex ? { ...msg, isTyping: false } : msg)
    } : s));
  };
  const handleDragOver = (e) => {
    e.preventDefault();
    setIsDragging(true);
  };
  const handleDragLeave = (e) => {
    e.preventDefault();
    setIsDragging(false);
  };
  const handleDrop = (e) => {
    e.preventDefault();
    setIsDragging(false);
    if (e.dataTransfer.files && e.dataTransfer.files.length > 0) {
      handleFiles(Array.from(e.dataTransfer.files));
    }
  };
  const handleFiles = (files) => {
    const newAttachments = files.map((file) => {
      let type = "other";
      if (file.type.startsWith("image/")) type = "image";
      else if (file.type === "application/pdf") type = "pdf";
      else if (file.type.startsWith("video/")) type = "video";
      else if (file.type.startsWith("text/")) type = "text";
      return { id: Date.now().toString() + Math.random().toString(36).substring(2), file, url: URL.createObjectURL(file), type };
    });
    setAttachedFiles((prev) => [...prev, ...newAttachments]);
  };
  const handleDeleteMessage = (sessionId, msgIndex) => {
    updateSessions((prev) => prev.map((s) => {
      if (s.id !== sessionId) return s;
      const newMessages = [...s.messages];
      const msgRole = newMessages[msgIndex]?.role;
      if (msgRole === "user") {
        newMessages.splice(msgIndex, 1);
        if (newMessages[msgIndex] && newMessages[msgIndex].role === "model") {
          newMessages.splice(msgIndex, 1);
        }
      } else if (msgRole === "model") {
        newMessages.splice(msgIndex, 1);
      }
      return { ...s, messages: newMessages, updatedAt: Date.now() };
    }));
  };
  const fetchAIResponse = async (sessionId, messagesToSent, filePayloads) => {
    aiChatBackgroundManager.generateResponse({
      sessionId,
      messagesToSent,
      filePayloads,
      selectedGoal,
      selectedSettings: settings
    });
  };
  const handleSend = async () => {
    if (!input.trim() && attachedFiles.length === 0 || loading || isTyping) return;
    const userMsg = input.trim();
    if (userMsg === "/clear") {
      if (activeSessionId) {
        updateSessions((prev) => prev.map((s) => s.id === activeSessionId ? { ...s, messages: [], updatedAt: Date.now() } : s));
      }
      setInput("");
      setAttachedFiles([]);
      return;
    }
    if (userMsg === "/export") {
      setInput("");
      exportAsImage();
      return;
    }
    setInput("");
    let filePayloads = [];
    for (const af of attachedFiles.filter((f) => f.type === "image")) {
      const b64 = await new Promise((resolve) => {
        const img = new Image();
        img.onload = () => {
          const canvas = document.createElement("canvas");
          let width = img.width;
          let height = img.height;
          const maxDim = 512;
          if (width > maxDim || height > maxDim) {
            if (width > height) {
              height = Math.round(height * maxDim / width);
              width = maxDim;
            } else {
              width = Math.round(width * maxDim / height);
              height = maxDim;
            }
          }
          canvas.width = width;
          canvas.height = height;
          const ctx = canvas.getContext("2d");
          if (ctx) {
            ctx.drawImage(img, 0, 0, width, height);
            const dataUrl = canvas.toDataURL(af.file.type || "image/jpeg", 0.8);
            resolve(dataUrl.split(",")[1]);
          } else {
            resolve("");
          }
        };
        img.src = af.url;
      });
      if (b64) {
        filePayloads.push({ inlineData: { data: b64, mimeType: af.file.type || "image/jpeg" } });
      }
    }
    let textFilesContent = "";
    for (const af of attachedFiles.filter((f) => f.type === "pdf")) {
      try {
        const pdfjsLib = await __vitePreload(() => import("./vendor-pdf-DJ2ymul_.js"), true ? [] : void 0);
        pdfjsLib.GlobalWorkerOptions.workerSrc = `https://unpkg.com/pdfjs-dist@${pdfjsLib.version}/build/pdf.worker.min.mjs`;
        const arrayBuffer = await af.file.arrayBuffer();
        const pdf = await pdfjsLib.getDocument({ data: arrayBuffer.slice(0) }).promise;
        let text2 = "";
        for (let i = 1; i <= Math.min(pdf.numPages, 50); i++) {
          const page = await pdf.getPage(i);
          const content2 = await page.getTextContent();
          text2 += `--- PAGE ${i} ---
` + content2.items.map((it) => it.str).join(" ") + "\n\n";
        }
        textFilesContent += `

--- Contents of ${af.file.name} (First ${Math.min(pdf.numPages, 50)} pages) ---
${text2.slice(0, 3e4)}`;
      } catch (err) {
        console.error("PDF Parsing Error: ", err);
      }
    }
    for (const af of attachedFiles.filter((f) => f.type === "text" || f.type === "other" || f.file.name.endsWith(".txt"))) {
      try {
        const text2 = await af.file.text();
        textFilesContent += `

--- Contents of ${af.file.name} ---
${text2.slice(0, 5e3)}`;
      } catch (err) {
        console.error("Text Parsing Error: ", err);
      }
    }
    const finalUserMsg = userMsg + textFilesContent;
    const attachmentsToSave = attachedFiles.map((af) => ({ url: af.url, type: af.type, name: af.file.name }));
    setAttachedFiles([]);
    let currentId = activeSessionId;
    let nextSessions = [...sessions];
    if (!currentId) {
      currentId = Date.now().toString();
      const titleMsg = finalUserMsg || "Media Upload";
      const newSession = {
        id: currentId,
        title: titleMsg.slice(0, 30) + (titleMsg.length > 30 ? "..." : ""),
        updatedAt: Date.now(),
        messages: []
      };
      nextSessions = [newSession, ...nextSessions];
    }
    const newMessages = [...activeSessionId ? sessions.find((s) => s.id === activeSessionId)?.messages || [] : [], { role: "user", content: finalUserMsg, attachments: attachmentsToSave }];
    const finalSessions = nextSessions.map((s) => s.id === currentId ? { ...s, messages: newMessages, updatedAt: Date.now() } : s);
    updateSessions(finalSessions);
    if (!activeSessionId) {
      setActiveSessionId(currentId);
    }
    await fetchAIResponse(currentId, newMessages, filePayloads);
  };
  const getImagePayloadsFromAttachments = async (attachments) => {
    const filePayloads = [];
    if (!attachments) return filePayloads;
    for (const att of attachments) {
      if (att.type === "image") {
        try {
          const b64 = await new Promise((resolve) => {
            const img = new Image();
            img.crossOrigin = "anonymous";
            img.onload = () => {
              const canvas = document.createElement("canvas");
              let width = img.width;
              let height = img.height;
              const maxDim = 512;
              if (width > maxDim || height > maxDim) {
                if (width > height) {
                  height = Math.round(height * maxDim / width);
                  width = maxDim;
                } else {
                  width = Math.round(width * maxDim / height);
                  height = maxDim;
                }
              }
              canvas.width = width;
              canvas.height = height;
              const ctx = canvas.getContext("2d");
              if (ctx) {
                ctx.drawImage(img, 0, 0, width, height);
                const dataUrl = canvas.toDataURL("image/jpeg", 0.8);
                resolve(dataUrl.split(",")[1]);
              } else {
                resolve("");
              }
            };
            img.onerror = () => resolve("");
            img.src = att.url;
          });
          if (b64) {
            filePayloads.push({ inlineData: { data: b64, mimeType: "image/jpeg" } });
          }
        } catch (e) {
          console.error("Failed to convert attachment image to base64:", e);
        }
      }
    }
    return filePayloads;
  };
  const handleRegenerate = async () => {
    if (loading || !activeSessionId) return;
    const currentSession = sessions.find((s) => s.id === activeSessionId);
    if (!currentSession || currentSession.messages.length === 0) return;
    const msgs = [...currentSession.messages];
    if (msgs[msgs.length - 1].role === "model") {
      msgs.pop();
    }
    updateSessions((prev) => prev.map((s) => s.id === activeSessionId ? { ...s, messages: msgs, updatedAt: Date.now() } : s));
    const lastUserMsg = msgs[msgs.length - 1];
    const filePayloads = await getImagePayloadsFromAttachments(lastUserMsg?.attachments);
    await fetchAIResponse(activeSessionId, msgs, filePayloads);
  };
  const handleSaveEdit = async (idx) => {
    if (!editingMessageText.trim() || !activeSessionId) return;
    const currentSession = sessions.find((s) => s.id === activeSessionId);
    if (!currentSession) return;
    const updatedMessages = [...currentSession.messages];
    updatedMessages[idx] = {
      ...updatedMessages[idx],
      content: editingMessageText.trim()
    };
    const truncatedMessages = updatedMessages.slice(0, idx + 1);
    updateSessions((prev) => prev.map((s) => s.id === activeSessionId ? {
      ...s,
      messages: truncatedMessages,
      updatedAt: Date.now()
    } : s));
    setEditingMessageIdx(null);
    setEditingMessageText("");
    const filePayloads = await getImagePayloadsFromAttachments(truncatedMessages[idx].attachments);
    await fetchAIResponse(activeSessionId, truncatedMessages, filePayloads);
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex h-full bg-background overflow-hidden relative w-full", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(AnimatePresence, { children: sidebarOpen && /* @__PURE__ */ jsxRuntimeExports.jsxs(
      motion.div,
      {
        initial: { width: 0, opacity: 0 },
        animate: { width: 280, opacity: 1 },
        exit: { width: 0, opacity: 0 },
        className: "h-full bg-muted/30 border-r border-border flex flex-col shrink-0 overflow-hidden z-20 absolute md:relative backdrop-blur-md md:backdrop-blur-none",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "p-4", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("button", { onClick: createNewChat, className: "flex items-center gap-3 w-full p-3 bg-background hover:bg-muted border border-border rounded-xl transition-colors text-sm font-semibold text-foreground shadow-sm", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Plus, { className: "h-4 w-4" }),
            " New Chat"
          ] }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 overflow-y-auto px-3 space-y-1 pb-4", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-[10px] font-bold text-muted-foreground px-2 py-2 uppercase tracking-wider", children: "Recent Chats" }),
            sessions.sort((a, b) => b.updatedAt - a.updatedAt).map((s) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: cn("group flex items-center justify-between p-2 rounded-lg cursor-pointer transition-colors", activeSessionId === s.id ? "bg-primary/10 text-primary" : "hover:bg-muted text-foreground"), children: [
              editingId === s.id ? /* @__PURE__ */ jsxRuntimeExports.jsx(
                "input",
                {
                  value: editTitle,
                  onChange: (e) => setEditTitle(e.target.value),
                  onBlur: () => {
                    if (editTitle.trim()) updateSessions((prev) => prev.map((x) => x.id === s.id ? { ...x, title: editTitle.trim() } : x));
                    setEditingId(null);
                  },
                  onKeyDown: (e) => {
                    if (e.key === "Enter") {
                      if (editTitle.trim()) updateSessions((prev) => prev.map((x) => x.id === s.id ? { ...x, title: editTitle.trim() } : x));
                      setEditingId(null);
                    }
                  },
                  autoFocus: true,
                  className: "bg-background text-sm px-1 flex-1 border border-primary/50 outline-none rounded"
                }
              ) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex-1 min-w-0 pr-2", onClick: () => {
                setActiveSessionId(s.id);
                if (window.innerWidth < 768) setSidebarOpen(false);
              }, children: /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm truncate font-medium", children: s.title }) }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-1 opacity-0 group-hover:opacity-100 transition-opacity", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("button", { onClick: (e) => {
                  e.stopPropagation();
                  setEditingId(s.id);
                  setEditTitle(s.title);
                }, className: "p-1 hover:text-primary", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Pencil, { className: "h-3.5 w-3.5" }) }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("button", { onClick: (e) => {
                  e.stopPropagation();
                  updateSessions((prev) => prev.filter((x) => x.id !== s.id));
                  if (activeSessionId === s.id) setActiveSessionId(null);
                }, className: "p-1 hover:text-destructive", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Trash2, { className: "h-3.5 w-3.5" }) })
              ] })
            ] }, s.id))
          ] })
        ]
      }
    ) }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        className: "flex-1 flex flex-col h-full relative min-w-0 bg-background",
        onDragOver: handleDragOver,
        onDragLeave: handleDragLeave,
        onDrop: handleDrop,
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(AnimatePresence, { children: isDragging && /* @__PURE__ */ jsxRuntimeExports.jsxs(
            motion.div,
            {
              initial: { opacity: 0 },
              animate: { opacity: 1 },
              exit: { opacity: 0 },
              className: "absolute inset-0 z-50 bg-primary/20 backdrop-blur-sm flex flex-col items-center justify-center border-4 border-dashed border-primary m-4 rounded-3xl pointer-events-none",
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Plus, { className: "h-16 w-16 text-primary mb-4" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "text-2xl font-bold text-primary", children: "Drop files here to upload" })
              ]
            }
          ) }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "h-14 flex items-center justify-between px-4 shrink-0 absolute top-0 left-0 z-10 w-full bg-gradient-to-b from-background via-background/90 to-transparent border-b border-border/10 backdrop-blur-md", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center", children: [
              onBack && /* @__PURE__ */ jsxRuntimeExports.jsx("button", { onClick: onBack, className: "p-2 mr-1.5 hover:bg-muted rounded-full text-muted-foreground transition-colors flex items-center justify-center", title: "Back to Options", children: /* @__PURE__ */ jsxRuntimeExports.jsx(ChevronLeft, { className: "h-5 w-5 text-foreground" }) }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "button",
                {
                  onClick: () => setSidebarOpen(!sidebarOpen),
                  className: "p-2 hover:bg-muted rounded-full text-muted-foreground transition-colors",
                  title: sidebarOpen ? "Collapse Sidebar" : "Expand Sidebar",
                  children: sidebarOpen ? /* @__PURE__ */ jsxRuntimeExports.jsx(PanelLeftClose, { className: "h-5 w-5" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(PanelLeft, { className: "h-5 w-5" })
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "ml-3 font-semibold text-lg text-foreground flex items-center gap-2", children: "Calculus AI" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 relative", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "button",
                {
                  onClick: () => setShowSettingsMenu(!showSettingsMenu),
                  className: "p-2 hover:bg-muted rounded-full text-muted-foreground hover:text-foreground transition-colors flex items-center justify-center",
                  title: "AI Chat Settings",
                  children: /* @__PURE__ */ jsxRuntimeExports.jsx(Settings, { className: "h-5 w-5" })
                }
              ),
              showSettingsMenu && /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "div",
                  {
                    className: "fixed inset-0 z-30",
                    onClick: () => setShowSettingsMenu(false)
                  }
                ),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "absolute right-0 top-full mt-2 w-56 bg-card border border-border rounded-2xl shadow-xl p-4 z-40 animate-in fade-in slide-in-from-top-2 duration-150", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "text-xs font-bold text-muted-foreground uppercase tracking-wider mb-3", children: "AI & Internet Mode" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-3", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "flex items-center gap-3 cursor-pointer group select-none", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx(
                        "input",
                        {
                          type: "checkbox",
                          checked: settings.internet,
                          onChange: () => toggleSetting("internet"),
                          className: "h-4.5 w-4.5 rounded border-border bg-muted accent-primary cursor-pointer transition-colors"
                        }
                      ),
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col", children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-sm font-semibold text-foreground group-hover:text-primary transition-colors flex items-center gap-1.5", children: [
                          /* @__PURE__ */ jsxRuntimeExports.jsx(Globe, { className: "h-3.5 w-3.5" }),
                          " Internet"
                        ] }),
                        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[10px] text-muted-foreground", children: "Realtime search & updates" })
                      ] })
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "flex items-center gap-3 cursor-pointer group select-none", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx(
                        "input",
                        {
                          type: "checkbox",
                          checked: settings.ai,
                          onChange: () => toggleSetting("ai"),
                          className: "h-4.5 w-4.5 rounded border-border bg-muted accent-primary cursor-pointer transition-colors"
                        }
                      ),
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col", children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-sm font-semibold text-foreground group-hover:text-primary transition-colors flex items-center gap-1.5", children: [
                          /* @__PURE__ */ jsxRuntimeExports.jsx(BrainCircuit, { className: "h-3.5 w-3.5" }),
                          " AI Model"
                        ] }),
                        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[10px] text-muted-foreground", children: "Pretrained knowledge base" })
                      ] })
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "flex items-center gap-3 cursor-pointer group select-none", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx(
                        "input",
                        {
                          type: "checkbox",
                          checked: settings.verify,
                          onChange: () => toggleSetting("verify"),
                          className: "h-4.5 w-4.5 rounded border-border bg-muted accent-primary cursor-pointer transition-colors"
                        }
                      ),
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col", children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-sm font-semibold text-foreground group-hover:text-primary transition-colors flex items-center gap-1.5", children: [
                          /* @__PURE__ */ jsxRuntimeExports.jsx(CircleCheckBig, { className: "h-3.5 w-3.5 text-emerald-500" }),
                          " Verify Answer"
                        ] }),
                        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[10px] text-muted-foreground", children: "Cross-checks multiple sources" })
                      ] })
                    ] })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mt-4 pt-3 border-t border-border/60 flex items-center justify-between", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[10px] text-muted-foreground font-medium", children: "Active Mode:" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-[10px] font-bold text-primary px-2 py-0.5 bg-primary/10 rounded-full", children: [
                      settings.internet && settings.ai && (settings.verify ? "Verify + Internet + AI" : "Internet + AI"),
                      settings.internet && !settings.ai && (settings.verify ? "Verify + Internet" : "Internet Only"),
                      !settings.internet && settings.ai && (settings.verify ? "Verify + AI" : "AI Only")
                    ] })
                  ] })
                ] })
              ] })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 overflow-y-auto overflow-x-hidden pt-16 pb-32 px-4 md:px-8 lg:px-20 scrollbar-hide w-full", children: [
            messages.length === 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "h-full flex flex-col items-center justify-center text-center mt-10 md:mt-20", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(motion.div, { initial: { scale: 0.9, opacity: 0 }, animate: { scale: 1, opacity: 1 }, transition: { duration: 0.8, ease: "easeOut" }, className: "mb-8 select-none", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("h1", { className: "text-4xl md:text-5xl font-medium bg-clip-text text-transparent bg-gradient-to-r from-blue-500 via-purple-500 to-red-500 tracking-tight pb-2", children: [
                "Hello, ",
                user
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "text-3xl md:text-4xl font-medium text-muted-foreground/60 tracking-tight", children: "How can I help you today?" })
            ] }) }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { id: "chat-capture-area", className: "w-full max-w-3xl mx-auto space-y-6 p-4 rounded-2xl bg-background", children: [
              hasMoreMessages && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex justify-center py-2", "data-html2canvas-ignore": "true", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
                Button,
                {
                  variant: "ghost",
                  size: "sm",
                  className: "text-xs text-muted-foreground hover:text-foreground font-medium flex items-center gap-2 cursor-pointer transition-all bg-muted/40 hover:bg-muted/80 rounded-full px-4 py-1.5 border border-border/40",
                  onClick: () => setVisibleMessagesCount((prev) => prev + 20),
                  children: [
                    "Load older messages (",
                    messages.length - visibleMessagesCount,
                    " remaining)"
                  ]
                }
              ) }),
              renderedMessages.map((m, idx) => {
                const i = messages.length - renderedMessages.length + idx;
                return /* @__PURE__ */ jsxRuntimeExports.jsxs(motion.div, { initial: { opacity: 0, y: 10 }, animate: { opacity: 1, y: 0 }, className: cn("flex w-full group relative items-start", m.role === "user" ? "justify-end" : "justify-start"), children: [
                  m.role === "model" && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-8 h-8 rounded-full bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center shrink-0 mr-4 mt-1", children: /* @__PURE__ */ jsxRuntimeExports.jsx(BrainCircuit, { className: "h-4 w-4 text-white" }) }),
                  m.role === "user" && !loading && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { "data-html2canvas-ignore": "true", className: "flex flex-col mr-2 mt-2 opacity-0 group-hover:opacity-100 transition-opacity gap-1 shrink-0", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(
                      "button",
                      {
                        onClick: () => {
                          setEditingMessageIdx(i);
                          setEditingMessageText(m.content);
                        },
                        className: "p-1.5 text-muted-foreground hover:text-foreground hover:bg-muted rounded-full h-fit",
                        title: "Edit message",
                        children: /* @__PURE__ */ jsxRuntimeExports.jsx(Pencil, { className: "h-3.5 w-3.5" })
                      }
                    ),
                    /* @__PURE__ */ jsxRuntimeExports.jsx(
                      "button",
                      {
                        onClick: () => handleDeleteMessage(activeSessionId, i),
                        className: "p-1.5 text-muted-foreground hover:text-destructive hover:bg-muted rounded-full h-fit",
                        title: "Delete message",
                        children: /* @__PURE__ */ jsxRuntimeExports.jsx(Trash2, { className: "h-3.5 w-3.5" })
                      }
                    )
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: cn("text-[15px] leading-relaxed", m.role === "user" ? "bg-muted px-5 py-3 rounded-3xl user-message-bubble max-w-[85%] sm:max-w-[80%]" : "text-foreground pt-1 w-full", m.isStopped ? "opacity-80" : ""), children: [
                    m.role === "model" && !m.isTyping && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between border-b border-border/40 pb-2 mb-3 select-none relative w-full", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsx(Sparkles, { className: "h-4 w-4 text-blue-500 animate-pulse" }),
                        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-xs font-bold text-foreground", children: "AI Overview" }),
                        /* @__PURE__ */ jsxRuntimeExports.jsx(
                          "button",
                          {
                            onClick: () => handleToggleTts(m.content, i),
                            className: cn(
                              "h-6 w-6 rounded-full hover:bg-muted flex items-center justify-center border transition-all text-muted-foreground hover:text-foreground shrink-0 cursor-pointer shadow-sm",
                              activeTtsIdx === i ? "bg-primary/10 border-primary/20 text-primary animate-pulse" : "border-border/40"
                            ),
                            title: activeTtsIdx === i ? "Stop listening" : "Read response aloud",
                            children: activeTtsIdx === i ? /* @__PURE__ */ jsxRuntimeExports.jsx(VolumeX, { className: "h-3 w-3 text-primary" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Volume2, { className: "h-3 w-3" })
                          }
                        )
                      ] }),
                      m.sources && m.sources.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-1", children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsxs(
                          "div",
                          {
                            className: "flex -space-x-1.5 items-center cursor-pointer hover:opacity-90 active:scale-95 transition-all",
                            onClick: () => setActiveSourcesDropdown(activeSourcesDropdown === i ? null : i),
                            children: [
                              m.sources.slice(0, 3).map((src, srcIdx) => {
                                let hostname = "Source";
                                try {
                                  hostname = new URL(src.uri).hostname;
                                } catch (e) {
                                }
                                const faviconUrl = `https://www.google.com/s2/favicons?domain=${hostname}&sz=64`;
                                return /* @__PURE__ */ jsxRuntimeExports.jsx(
                                  "div",
                                  {
                                    className: "h-5 w-5 rounded-full border border-border bg-card flex items-center justify-center overflow-hidden shadow-sm ring-1 ring-card hover:translate-y-[-2px] transition-transform duration-200",
                                    title: src.title || hostname,
                                    children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                                      "img",
                                      {
                                        src: faviconUrl,
                                        alt: "",
                                        className: "h-3 w-3 object-contain rounded-full",
                                        onError: (e) => {
                                          e.currentTarget.src = "https://www.google.com/s2/favicons?domain=wikipedia.org";
                                        }
                                      }
                                    )
                                  },
                                  srcIdx
                                );
                              }),
                              m.sources.length > 3 && /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-[10px] font-bold text-muted-foreground ml-1.5 bg-muted px-1.5 py-0.5 rounded-full border border-border/80 scale-90", children: [
                                "+",
                                m.sources.length - 3
                              ] })
                            ]
                          }
                        ),
                        /* @__PURE__ */ jsxRuntimeExports.jsx(
                          "button",
                          {
                            onClick: () => setActiveSourcesDropdown(activeSourcesDropdown === i ? null : i),
                            className: "h-6 w-6 rounded-full hover:bg-muted flex items-center justify-center border border-border/40 text-muted-foreground hover:text-foreground transition-all shrink-0 cursor-pointer ml-1",
                            title: "View all search sources",
                            children: /* @__PURE__ */ jsxRuntimeExports.jsx(EllipsisVertical, { className: "h-3.5 w-3.5" })
                          }
                        ),
                        activeSourcesDropdown === i && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "absolute right-0 top-full mt-2 w-80 bg-card border border-border rounded-2xl p-4 shadow-2xl z-[150] animate-in fade-in slide-in-from-top-2 duration-200", children: [
                          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between border-b border-border/60 pb-2 mb-3", children: [
                            /* @__PURE__ */ jsxRuntimeExports.jsx("h4", { className: "text-xs font-bold text-foreground", children: "All Search Sources" }),
                            /* @__PURE__ */ jsxRuntimeExports.jsx(
                              "button",
                              {
                                onClick: () => setActiveSourcesDropdown(null),
                                className: "text-muted-foreground hover:text-foreground text-[10px] font-semibold",
                                children: "Close"
                              }
                            )
                          ] }),
                          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "max-h-60 overflow-y-auto space-y-2 pr-1 no-scrollbar text-left", children: m.sources.map((src, srcIdx) => {
                            let hostname = "Source";
                            try {
                              hostname = new URL(src.uri).hostname;
                            } catch (e) {
                            }
                            const faviconUrl = `https://www.google.com/s2/favicons?domain=${hostname}&sz=64`;
                            return /* @__PURE__ */ jsxRuntimeExports.jsxs(
                              "div",
                              {
                                className: "flex flex-col p-2.5 rounded-xl hover:bg-muted/40 border border-border/10 transition-all text-left bg-muted/10",
                                children: [
                                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-start gap-2.5", children: [
                                    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "h-7 w-7 rounded-lg bg-background border border-border flex items-center justify-center shrink-0 shadow-sm mt-0.5", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                                      "img",
                                      {
                                        src: faviconUrl,
                                        alt: "",
                                        className: "h-4.5 w-4.5 object-contain rounded-full",
                                        onError: (e) => {
                                          e.currentTarget.src = "https://www.google.com/s2/favicons?domain=wikipedia.org";
                                        }
                                      }
                                    ) }),
                                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "min-w-0 flex-1 text-left", children: [
                                      /* @__PURE__ */ jsxRuntimeExports.jsx(
                                        "a",
                                        {
                                          href: src.uri,
                                          target: "_blank",
                                          rel: "noopener noreferrer",
                                          className: "text-[11px] font-bold text-foreground hover:text-blue-500 transition-colors leading-snug line-clamp-1 block",
                                          children: src.title
                                        }
                                      ),
                                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[9px] text-muted-foreground/60 truncate block mt-0.5", children: hostname })
                                    ] })
                                  ] }),
                                  src.snippet && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-[10.5px] text-muted-foreground line-clamp-2 mt-2 leading-relaxed pr-1", children: src.snippet }),
                                  src.scrapedSolution && /* @__PURE__ */ jsxRuntimeExports.jsxs("details", { className: "mt-2 text-[10px] text-muted-foreground bg-muted/40 rounded-lg p-2 border border-border/40 select-text", children: [
                                    /* @__PURE__ */ jsxRuntimeExports.jsx("summary", { className: "cursor-pointer font-semibold text-[9px] hover:text-foreground text-muted-foreground select-none", children: "View Scraped Solution Text" }),
                                    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "mt-1.5 whitespace-pre-wrap font-mono text-[9px] max-h-32 overflow-y-auto custom-scrollbar leading-relaxed", children: src.scrapedSolution })
                                  ] })
                                ]
                              },
                              srcIdx
                            );
                          }) })
                        ] })
                      ] })
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "prose dark:prose-invert prose-p:leading-relaxed prose-pre:bg-muted prose-pre:border prose-pre:border-border max-w-none w-full prose-table:w-full prose-table:border-collapse prose-th:border prose-th:border-border prose-th:p-2 prose-td:border prose-td:border-border prose-td:p-2 prose-img:rounded-xl prose-img:max-h-[350px] prose-img:w-auto prose-img:object-contain prose-a:text-blue-500 hover:prose-a:text-blue-600 transition-colors", children: [
                      m.iframeUrls && m.iframeUrls.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex flex-wrap gap-3 mb-3", children: m.iframeUrls.map((url, idx2) => /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "relative w-full max-w-[320px] h-[320px] rounded-2xl border border-border bg-muted/30 overflow-hidden shadow-sm flex items-center justify-center", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                        "iframe",
                        {
                          src: url,
                          style: {
                            width: "100%",
                            height: "100%",
                            border: "none",
                            background: "transparent"
                          },
                          allow: "clipboard-write"
                        }
                      ) }, idx2)) }),
                      m.attachments && m.attachments.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex flex-wrap gap-2 mb-3", children: m.attachments.map((a, idx2) => a.type === "image" ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "relative group inline-block", children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsx(
                          "img",
                          {
                            src: a.url,
                            alt: a.name,
                            className: "max-w-[200px] max-h-[200px] sm:max-w-[300px] sm:max-h-[300px] rounded-lg border border-border object-cover cursor-pointer transition-transform hover:scale-[1.02]",
                            onClick: () => setFullScreenImage(a.url)
                          }
                        ),
                        /* @__PURE__ */ jsxRuntimeExports.jsx(
                          "a",
                          {
                            href: a.url,
                            download: `generated_image_${idx2}.png`,
                            target: "_blank",
                            rel: "noreferrer",
                            onClick: (e) => e.stopPropagation(),
                            className: "absolute top-2 right-2 p-2 bg-black/60 rounded-full opacity-0 group-hover:opacity-100 transition-opacity hover:bg-black/80 text-white shadow-md backdrop-blur-sm",
                            children: /* @__PURE__ */ jsxRuntimeExports.jsx(Download, { className: "h-4 w-4" })
                          }
                        )
                      ] }, idx2) : /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 p-2 bg-background border border-border rounded-lg text-xs", children: [
                        a.type === "pdf" ? /* @__PURE__ */ jsxRuntimeExports.jsx(FileText, { className: "h-4 w-4" }) : a.type === "video" ? /* @__PURE__ */ jsxRuntimeExports.jsx(FilePlay, { className: "h-4 w-4" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(FileText, { className: "h-4 w-4" }),
                        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "truncate max-w-[150px]", children: a.name })
                      ] }, idx2)) }),
                      m.role === "model" ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex flex-col w-full", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                        TypewriterMarkdown,
                        {
                          content: m.content,
                          isTyping: m.isTyping,
                          onComplete: () => markAsDone(i),
                          setFullScreenImage,
                          sources: m.sources
                        }
                      ) }) : editingMessageIdx === i ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-2 min-w-[240px] sm:min-w-[450px] bg-background border border-border/80 rounded-2xl p-3 shadow-inner", children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsx(
                          "textarea",
                          {
                            value: editingMessageText,
                            onChange: (e) => setEditingMessageText(e.target.value),
                            className: "w-full bg-transparent border-0 text-sm focus:outline-none text-foreground resize-y min-h-[80px]",
                            onKeyDown: (e) => {
                              if (e.key === "Enter" && !e.shiftKey) {
                                e.preventDefault();
                                handleSaveEdit(i);
                              }
                            },
                            autoFocus: true
                          }
                        ),
                        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-end gap-2 border-t border-border/40 pt-2 shrink-0", children: [
                          /* @__PURE__ */ jsxRuntimeExports.jsx(
                            Button,
                            {
                              variant: "ghost",
                              size: "sm",
                              onClick: () => {
                                setEditingMessageIdx(null);
                                setEditingMessageText("");
                              },
                              className: "h-7 text-xs px-3 rounded-full hover:bg-muted",
                              children: "Cancel"
                            }
                          ),
                          /* @__PURE__ */ jsxRuntimeExports.jsx(
                            Button,
                            {
                              size: "sm",
                              onClick: () => handleSaveEdit(i),
                              className: "h-7 text-xs px-3 rounded-full",
                              children: "Save & Submit"
                            }
                          )
                        ] })
                      ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Markdown, { remarkPlugins: [remarkMath, remarkGfm], rehypePlugins: [rehypeKatex], components: getMarkdownComponents(setFullScreenImage), children: preprocessMarkdown(m.content) }),
                      m.role === "model" && !m.isTyping && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { "data-html2canvas-ignore": "true", className: "mt-3 flex flex-wrap items-center justify-between gap-3 border-t border-border/40 pt-2.5", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-wrap items-center gap-2", children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsx(
                          Button,
                          {
                            variant: "outline",
                            size: "sm",
                            onClick: () => copyToClipboard(m.content, i),
                            className: "h-7 text-[11px] gap-1.5 text-muted-foreground hover:text-foreground rounded-full px-3",
                            title: "Copy response content",
                            children: copiedIdx === i ? /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
                              /* @__PURE__ */ jsxRuntimeExports.jsx(Check, { className: "h-3 w-3 text-green-500" }),
                              " Copied"
                            ] }) : /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
                              /* @__PURE__ */ jsxRuntimeExports.jsx(Copy, { className: "h-3 w-3" }),
                              " Copy"
                            ] })
                          }
                        ),
                        i === messages.length - 1 && !loading && /* @__PURE__ */ jsxRuntimeExports.jsxs(
                          Button,
                          {
                            variant: "outline",
                            size: "sm",
                            onClick: handleRegenerate,
                            className: "h-7 text-[11px] gap-1.5 text-muted-foreground hover:text-foreground rounded-full px-3",
                            children: [
                              /* @__PURE__ */ jsxRuntimeExports.jsx(RefreshCw, { className: "h-3 w-3" }),
                              " Regenerate"
                            ]
                          }
                        ),
                        /* @__PURE__ */ jsxRuntimeExports.jsx(
                          Button,
                          {
                            variant: "outline",
                            size: "sm",
                            onClick: () => handleLikeMessage(i),
                            className: cn("h-7 w-7 p-0 rounded-full", likedMsgs[i] === "like" ? "text-green-500 border-green-500/30 bg-green-500/10 hover:bg-green-500/20" : "text-muted-foreground hover:text-foreground"),
                            title: "Like response",
                            children: /* @__PURE__ */ jsxRuntimeExports.jsx(ThumbsUp, { className: "h-3.5 w-3.5" })
                          }
                        ),
                        /* @__PURE__ */ jsxRuntimeExports.jsx(
                          Button,
                          {
                            variant: "outline",
                            size: "sm",
                            onClick: () => handleDislikeMessage(i),
                            className: cn("h-7 w-7 p-0 rounded-full", likedMsgs[i] === "dislike" ? "text-red-500 border-red-500/30 bg-red-500/10 hover:bg-red-500/20" : "text-muted-foreground hover:text-foreground"),
                            title: "Dislike response",
                            children: /* @__PURE__ */ jsxRuntimeExports.jsx(ThumbsDown, { className: "h-3.5 w-3.5" })
                          }
                        )
                      ] }) })
                    ] })
                  ] })
                ] }, i);
              }),
              loading && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex w-full justify-start items-start", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-8 h-8 rounded-full bg-gradient-to-br from-blue-500 to-purple-600 flex items-center justify-center shrink-0 mr-4 mt-1", children: /* @__PURE__ */ jsxRuntimeExports.jsx(BrainCircuit, { className: "h-4 w-4 text-white animate-pulse" }) }),
                generatingImageType ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-3 mt-1.5 px-4 py-2 bg-muted/50 rounded-2xl border border-border", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-4 h-4 rounded-full border-2 border-primary border-t-transparent animate-spin" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-sm font-medium bg-gradient-to-r from-primary to-purple-500 bg-clip-text text-transparent animate-pulse", children: "Creating image..." })
                ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex flex-col gap-3 mt-2.5 w-full max-w-[80%]", children: [100, 100, 80].map((w, i) => /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "h-4 rounded-full bg-muted/60 dark:bg-muted/40 relative overflow-hidden", style: { width: `${w}%` }, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                  motion.div,
                  {
                    initial: { x: "-150%" },
                    animate: { x: "350%" },
                    transition: { repeat: Infinity, duration: 1.5, ease: "linear", delay: i * 0.15 },
                    className: "absolute inset-0 w-[50%] h-full bg-gradient-to-r from-transparent via-green-500/80 dark:via-green-400/80 to-transparent"
                  }
                ) }, i)) })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { ref: messagesEndRef })
            ] })
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute bottom-0 left-0 right-0 p-4 md:p-6 bg-gradient-to-t from-background via-background to-transparent z-10", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "max-w-3xl mx-auto relative", children: [
            showCommands && filteredCommands.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx(
              "div",
              {
                ref: commandMenuRef,
                className: "absolute bottom-full left-4 mb-3 w-full max-w-xs sm:max-w-sm bg-popover/90 backdrop-blur-xl border border-border/80 rounded-2xl shadow-2xl overflow-hidden z-[1000] flex flex-col",
                style: { maxHeight: "80px" },
                children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "div",
                  {
                    ref: commandListRef,
                    className: "overflow-y-auto divide-y divide-border/40 scrollbar-hide py-1 max-h-[80px]",
                    children: filteredCommands.map((cmd, idx) => {
                      const isSelected = idx === selectedCommandIndex;
                      const IconComponent = cmd.icon;
                      return /* @__PURE__ */ jsxRuntimeExports.jsxs(
                        "button",
                        {
                          onClick: () => selectCommand(cmd.name),
                          className: cn(
                            "w-full text-left px-4 py-2 text-sm flex items-center justify-between transition-colors outline-none h-[40px]",
                            isSelected ? "bg-accent text-accent-foreground font-medium" : "text-popover-foreground hover:bg-muted/50"
                          ),
                          children: [
                            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2.5 min-w-0", children: [
                              /* @__PURE__ */ jsxRuntimeExports.jsx(IconComponent, { className: cn("h-4 w-4 shrink-0", isSelected ? "text-primary" : "text-muted-foreground") }),
                              /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: cn("font-semibold font-mono truncate", isSelected ? "text-primary" : "text-muted-foreground"), children: cmd.name })
                            ] }),
                            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-xs text-muted-foreground font-light italic truncate max-w-[150px] ml-4 shrink-0", children: cmd.description })
                          ]
                        },
                        cmd.name
                      );
                    })
                  }
                )
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex justify-end mb-2", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "select",
              {
                value: chatMode,
                onChange: (e) => setChatMode(e.target.value),
                className: "bg-card border border-border text-[11px] font-semibold rounded-full px-3 py-1 focus:outline-none text-muted-foreground hover:text-foreground shadow-sm transition-colors cursor-pointer",
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "academic", children: "Academic Mode" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "non_academic", children: "Non-Academic Mode" })
                ]
              }
            ) }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col bg-muted/70 border border-border shadow-lg rounded-[24px] overflow-hidden focus-within:bg-muted/90 focus-within:shadow-xl transition-all p-1.5 backdrop-blur-md", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(AnimatePresence, { children: attachedFiles.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsx(
                motion.div,
                {
                  initial: { opacity: 0, height: 0 },
                  animate: { opacity: 1, height: "auto" },
                  exit: { opacity: 0, height: 0 },
                  className: "flex flex-wrap gap-2 px-2 pt-2 pb-3 border-b border-border/50 w-full",
                  children: attachedFiles.map((af) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "relative group rounded-xl overflow-hidden border border-border bg-background w-16 h-16 shrink-0 flex items-center justify-center", children: [
                    af.type === "image" ? /* @__PURE__ */ jsxRuntimeExports.jsx("img", { src: af.url, alt: "attachment", className: "w-full h-full object-cover cursor-pointer", onClick: () => setImageToEdit(af) }) : /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col items-center justify-center text-muted-foreground p-1", children: [
                      af.type === "pdf" ? /* @__PURE__ */ jsxRuntimeExports.jsx(FileText, { className: "h-6 w-6" }) : af.type === "video" ? /* @__PURE__ */ jsxRuntimeExports.jsx(FilePlay, { className: "h-6 w-6" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(FileText, { className: "h-6 w-6" }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[8px] truncate w-full text-center mt-1", children: af.file.name })
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("button", { onClick: () => setAttachedFiles((prev) => prev.filter((f) => f.id !== af.id)), className: "absolute top-1 right-1 p-0.5 bg-black/60 rounded-full opacity-0 group-hover:opacity-100 transition-opacity", children: /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "h-3 w-3 text-white" }) }),
                    af.type === "image" && /* @__PURE__ */ jsxRuntimeExports.jsx("button", { onClick: () => setImageToEdit(af), className: "absolute bottom-1 left-1 p-0.5 bg-black/60 rounded-full opacity-0 group-hover:opacity-100 transition-opacity", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Pencil, { className: "h-3 w-3 text-white" }) })
                  ] }, af.id))
                }
              ) }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-end w-full", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "file", ref: fileInputRef, className: "hidden", multiple: true, onChange: (e) => {
                  if (e.target.files) handleFiles(Array.from(e.target.files));
                  e.target.value = "";
                } }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("button", { onClick: () => fileInputRef.current?.click(), className: "h-9 w-9 shrink-0 rounded-full flex items-center justify-center transition-all mx-0.5 mb-0.5 text-muted-foreground hover:bg-muted hover:text-foreground", title: "Upload files", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Plus, { className: "h-5 w-5" }) }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "relative flex-1 flex items-center min-w-0", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    "textarea",
                    {
                      ref: textareaRef,
                      value: input,
                      onChange: (e) => {
                        const val = e.target.value;
                        setInput(val);
                        if (val.startsWith("/") && !val.includes(" ")) {
                          setShowCommands(true);
                          setSelectedCommandIndex(0);
                        } else {
                          setShowCommands(false);
                        }
                      },
                      onKeyDown: (e) => {
                        if (showCommands && filteredCommands.length > 0) {
                          if (e.key === "ArrowDown") {
                            e.preventDefault();
                            setSelectedCommandIndex((prev) => (prev + 1) % filteredCommands.length);
                            return;
                          }
                          if (e.key === "ArrowUp") {
                            e.preventDefault();
                            setSelectedCommandIndex((prev) => (prev - 1 + filteredCommands.length) % filteredCommands.length);
                            return;
                          }
                          if (e.key === "Enter") {
                            e.preventDefault();
                            selectCommand(filteredCommands[selectedCommandIndex].name);
                            return;
                          }
                          if (e.key === "Escape") {
                            e.preventDefault();
                            setShowCommands(false);
                            return;
                          }
                        }
                        if (e.key === "Enter" && !e.shiftKey) {
                          e.preventDefault();
                          handleSend();
                        }
                      },
                      placeholder: isRecording ? "Listening... Speak now." : "Ask me anything...",
                      className: cn(
                        "flex-1 bg-transparent border-none resize-none max-h-48 min-h-[36px] py-1.5 px-3 text-sm focus:outline-none placeholder:text-muted-foreground/60 text-foreground",
                        isTransliterating ? "pr-40" : isRecording ? "pr-24" : "pr-3"
                      ),
                      rows: 1
                    }
                  ),
                  isRecording && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "absolute right-3 bottom-1.5 flex items-center gap-1.5 bg-background/90 backdrop-blur-sm px-2 py-1 rounded-full border border-red-500/20 text-[10px] text-red-500 font-medium select-none pointer-events-none shadow-sm animate-pulse", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "w-1.5 h-1.5 rounded-full bg-red-500 shrink-0" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Listening" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex gap-0.5 items-end h-2 w-3 shrink-0", children: [1, 2, 3].map((bar) => /* @__PURE__ */ jsxRuntimeExports.jsx(
                      motion.span,
                      {
                        className: "w-[2px] bg-red-500 rounded-full",
                        animate: { height: ["2px", "8px", "2px"] },
                        transition: { repeat: Infinity, duration: 0.6, delay: bar * 0.15 }
                      },
                      bar
                    )) })
                  ] }),
                  isTransliterating && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "absolute right-3 bottom-1.5 flex items-center gap-1.5 bg-background/90 backdrop-blur-sm px-2.5 py-1 rounded-full border border-violet-500/20 text-[10px] text-violet-400 font-medium select-none pointer-events-none shadow-sm", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "w-1.5 h-1.5 rounded-full bg-violet-400 shrink-0" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "AI processing voice..." }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx(RefreshCw, { className: "h-2.5 w-2.5 animate-spin text-violet-400 shrink-0" })
                  ] })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "button",
                  {
                    onClick: handleMicClick,
                    type: "button",
                    className: cn(
                      "h-9 w-9 shrink-0 rounded-full flex items-center justify-center transition-all mx-0.5 mb-0.5 relative overflow-visible",
                      isRecording ? "bg-red-500/20 text-red-500 border border-red-500/30 shadow-[0_0_15px_rgba(239,68,68,0.3)] hover:bg-red-500/30" : "text-muted-foreground hover:bg-muted hover:text-foreground"
                    ),
                    title: isRecording ? "Stop recording" : "Type with your voice",
                    children: isRecording ? /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx(
                        motion.span,
                        {
                          className: "absolute inset-0 rounded-full bg-red-500/20",
                          animate: { scale: [1, 1.6, 1], opacity: [0.6, 0, 0.6] },
                          transition: { repeat: Infinity, duration: 2, ease: "easeInOut" }
                        }
                      ),
                      /* @__PURE__ */ jsxRuntimeExports.jsx(MicOff, { className: "h-5 w-5" })
                    ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Mic, { className: "h-5 w-5" })
                  }
                ),
                loading || isTyping ? /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "button",
                  {
                    onClick: handleStopGeneration,
                    className: "h-9 w-9 shrink-0 rounded-full flex items-center justify-center transition-all mx-0.5 mb-0.5 bg-red-500 text-white shadow-md hover:scale-105",
                    children: /* @__PURE__ */ jsxRuntimeExports.jsx(Square, { className: "h-4 w-4 fill-current" })
                  }
                ) : /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "button",
                  {
                    onClick: handleSend,
                    disabled: !input.trim() && attachedFiles.length === 0,
                    className: cn("h-9 w-9 shrink-0 rounded-full flex items-center justify-center transition-all mx-0.5 mb-0.5", input.trim() || attachedFiles.length > 0 ? "bg-foreground text-background shadow-md hover:scale-105" : "bg-muted-foreground/20 text-muted-foreground cursor-not-allowed"),
                    children: /* @__PURE__ */ jsxRuntimeExports.jsx(ArrowRight, { className: "h-5 w-5" })
                  }
                )
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-center text-[11px] text-muted-foreground mt-3 font-medium", children: "Calculus AI can make mistakes. Consider verifying important information." })
          ] }) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(AnimatePresence, { children: imageToEdit && /* @__PURE__ */ jsxRuntimeExports.jsx(
            ChatImageEditor,
            {
              attachment: imageToEdit,
              onClose: () => setImageToEdit(null),
              onSave: (blob) => {
                const newFile = new File([blob], imageToEdit.file.name, { type: blob.type });
                const newUrl = URL.createObjectURL(blob);
                setAttachedFiles((prev) => prev.map((f) => f.id === imageToEdit.id ? { ...f, file: newFile, url: newUrl } : f));
                setImageToEdit(null);
              }
            }
          ) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(AnimatePresence, { children: fullScreenImage && /* @__PURE__ */ jsxRuntimeExports.jsxs(
            motion.div,
            {
              initial: { opacity: 0 },
              animate: { opacity: 1 },
              exit: { opacity: 0 },
              className: "fixed inset-0 z-[1000] bg-black/90 flex flex-col items-center justify-center p-4 backdrop-blur-md",
              onClick: () => setFullScreenImage(null),
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("button", { onClick: () => setFullScreenImage(null), className: "absolute top-6 right-6 p-2 bg-white/10 hover:bg-white/20 rounded-full text-white transition-colors", children: /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "w-6 h-6" }) }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "img",
                  {
                    src: fullScreenImage,
                    alt: "Full screen",
                    className: "max-w-full max-h-[85vh] object-contain rounded-xl shadow-2xl",
                    onClick: (e) => e.stopPropagation()
                  }
                ),
                /* @__PURE__ */ jsxRuntimeExports.jsxs(
                  "a",
                  {
                    href: fullScreenImage,
                    download: "generated_image.png",
                    target: "_blank",
                    rel: "noreferrer",
                    onClick: (e) => e.stopPropagation(),
                    className: "mt-6 flex items-center gap-2 px-6 py-3 bg-primary text-primary-foreground font-bold rounded-full shadow-lg hover:scale-105 transition-transform",
                    children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx(Download, { className: "w-5 h-5" }),
                      "Download Image"
                    ]
                  }
                )
              ]
            }
          ) })
        ]
      }
    )
  ] });
}
const AnsweredShape = ({ children }) => /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-8 h-8 bg-[#22c55e] text-white flex items-center justify-center font-bold text-sm shadow-sm", style: { clipPath: "polygon(0 0, 100% 0, 100% 70%, 50% 100%, 0 70%)" }, children });
const NotAnsweredShape = ({ children }) => /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-8 h-8 bg-[#ef4444] text-white flex items-center justify-center font-bold text-sm shadow-sm", style: { clipPath: "polygon(0 0, 100% 0, 100% 70%, 50% 100%, 0 70%)" }, children });
const NotVisitedShape = ({ children }) => /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-8 h-8 bg-[#f3f4f6] border border-gray-300 text-gray-700 flex items-center justify-center font-bold text-sm rounded shadow-sm", children });
const MarkedShape = ({ children }) => /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-8 h-8 bg-[#6b21a8] text-white flex items-center justify-center font-bold text-sm rounded-full shadow-sm", children });
const AnsweredMarkedShape = ({ children }) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "w-8 h-8 bg-[#6b21a8] text-white flex items-center justify-center font-bold text-sm rounded-full relative shadow-sm", children: [
  children,
  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute bottom-0 right-0 w-2.5 h-2.5 bg-[#22c55e] rounded-full border border-white" })
] });
const ReviewShape = ({ index: index2, children, answers, questions }) => {
  const ans = answers[index2];
  const q = questions[index2];
  if (ans === void 0) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(NotVisitedShape, { children });
  }
  if (ans === q.correctOptionIndex) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(AnsweredShape, { children });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx(NotAnsweredShape, { children });
};
const quizOptionComponents = { p: ({ node: node2, ...props }) => /* @__PURE__ */ jsxRuntimeExports.jsx("span", { ...props }) };
function planSubjects(brief, defaultDifficulty, totalCount, activeSources, availablePdfs, availableSaves, subjectsList) {
  const text2 = brief.trim().toLowerCase();
  const plan = {};
  subjectsList.forEach((subj) => {
    plan[subj] = { count: 0, difficulty: defaultDifficulty };
  });
  if (text2.length > 0) {
    let totalAllocated = 0;
    subjectsList.forEach((subj) => {
      const subjMatchTerm = subj.toLowerCase().replace(/s$/, "");
      const regex = new RegExp(`(\\d+)\\s*(easy|medium|hard|tough|mixed)?\\s*(?:question|q)?s?\\s*(?:of|for)?\\s*${subjMatchTerm}`, "i");
      const match = text2.match(regex);
      if (match) {
        const count = parseInt(match[1]);
        let diffStr = match[2] || "";
        let diff = defaultDifficulty;
        if (diffStr.includes("easy")) diff = "Easy";
        else if (diffStr.includes("medium")) diff = "Medium";
        else if (diffStr.includes("hard") || diffStr.includes("tough")) diff = "Hard";
        else if (diffStr.includes("mixed")) diff = "Mixed";
        plan[subj].count = count;
        plan[subj].difficulty = diff;
        totalAllocated += count;
      }
    });
    if (totalAllocated === 0) {
      const mentionedSubjects = [];
      subjectsList.forEach((subj) => {
        const subjMatchTerm = subj.toLowerCase().replace(/s$/, "");
        if (text2.includes(subjMatchTerm)) {
          mentionedSubjects.push(subj);
        }
      });
      if (mentionedSubjects.length > 0) {
        const baseCount2 = Math.floor(totalCount / mentionedSubjects.length);
        const remainder2 = totalCount % mentionedSubjects.length;
        mentionedSubjects.forEach((subj, index2) => {
          plan[subj].count = baseCount2 + (index2 < remainder2 ? 1 : 0);
          plan[subj].difficulty = defaultDifficulty;
        });
        return plan;
      }
    } else {
      if (totalAllocated === totalCount) {
        return plan;
      }
      const unallocated = totalCount - totalAllocated;
      if (unallocated > 0) {
        const mentionedInBrief = subjectsList.some((s) => text2.includes(s.toLowerCase().replace(/s$/, "")));
        const zeroSubjs = Object.keys(plan).filter((k) => {
          if (plan[k].count > 0) return false;
          if (!mentionedInBrief) return true;
          return text2.includes(k.toLowerCase().replace(/s$/, ""));
        });
        const activeZeroSubjs = zeroSubjs.length > 0 ? zeroSubjs : Object.keys(plan).filter((k) => plan[k].count > 0);
        const baseCount2 = Math.floor(unallocated / activeZeroSubjs.length);
        const remainder2 = unallocated % activeZeroSubjs.length;
        activeZeroSubjs.forEach((subj, index2) => {
          plan[subj].count += baseCount2 + (index2 < remainder2 ? 1 : 0);
        });
      }
      return plan;
    }
  }
  const activeSubjects = /* @__PURE__ */ new Set();
  activeSources.pdfs.forEach((pdfId) => {
    const item = availablePdfs.find((p) => p.id === pdfId);
    if (item) {
      const pathLower = item.path.toLowerCase();
      subjectsList.forEach((s) => {
        if (pathLower.includes(s.toLowerCase().replace(/s$/, ""))) {
          activeSubjects.add(s);
        }
      });
    }
  });
  activeSources.saves.forEach((saveId) => {
    const item = availableSaves.find((p) => p.id === saveId);
    if (item) {
      const pathLower = item.path.toLowerCase();
      subjectsList.forEach((s) => {
        if (pathLower.includes(s.toLowerCase().replace(/s$/, ""))) {
          activeSubjects.add(s);
        }
      });
    }
  });
  const finalActive = activeSubjects.size > 0 ? Array.from(activeSubjects) : subjectsList;
  const baseCount = Math.floor(totalCount / finalActive.length);
  const remainder = totalCount % finalActive.length;
  finalActive.forEach((subj, index2) => {
    plan[subj].count = baseCount + (index2 < remainder ? 1 : 0);
    plan[subj].difficulty = defaultDifficulty;
  });
  return plan;
}
function buildSubjectPrompt(params) {
  const { subject, count, difficulty, brief, contextText, internetSearch } = params;
  return `You are an expert JEE Advanced examiner. Your task is to generate high-quality, exam-style questions for the subject: "${subject}".

CRITICAL SPECIFICATIONS:
1. Target Subject: "${subject}". Every question generated MUST belong strictly to the subject "${subject}" (e.g. Physics concepts only for Physics). Do NOT mix mathematics concepts in chemistry or physics unless it is a standard interdisciplinary concept, and keep it firmly in "${subject}".
2. Number of questions to generate: EXACTLY ${count}.
3. Target Difficulty: ${difficulty} (Note: Hard questions must involve multi-step calculations, deep conceptual understanding, or advanced application of formulas typical of JEE Advanced. Easy/Medium questions should match standard JEE Main levels).

INSTRUCTIONS REGARDING USER GUIDELINES:
${brief ? `The user has specified the following custom guidelines: "${brief}". Adhere to this as closely as possible when choosing topics, difficulty ratios, or specific sub-topics for this batch.` : "Adhere to the provided source context for topic selection."}

CRITICAL FORMATTING INSTRUCTIONS FOR MATH/PHYSICS/CHEMISTRY AND TABLES:
- You MUST use LaTeX formatting for all math, physics, and chemistry equations, formulas, symbols, coordinates, vectors, and matrices.
- MUST wrap ALL inline equations, numbers, and vectors in single dollar signs: e.g., $x^2 + y^2 = r^2$ or $[2, -1, -2]$
- MUST wrap standalone block equations in double dollar signs: e.g., $$\\frac{A x^2}{B t}$$
- NEVER leave math, vectors (like [1, 2, -2]), or formulas as plain text. Always wrap them in $...$
- NEVER use [ ... ] or \\[ ... \\] or \\( ... \\) for math. ALWAYS use $$ ... $$ for block math and $ ... $ for inline math.
- For chemical equations, use LaTeX notation (e.g. $\\text{H}_2\\text{SO}_4$).
- For tables, ALWAYS use standard Markdown tables (using | and -). NEVER use LaTeX table environments like \\begin{table} or \\begin{tabular}. Ensure they are clean and properly aligned. DO NOT put block math ($$) inside a Markdown table cell; use inline math ($) inside tables.
- If the question contains multiple statements, items, or internal options (e.g., (1) ..., (2) ..., (3) ... or Statement I, Statement II), YOU MUST format them vertically by adding newlines (\\n) before each item in the "text" field so they appear on separate lines.
Do not use Unicode approximations. Output strict LaTeX.

CRITICAL JSON ESCAPING REQUIREMENT:
Because your response must be valid JSON, you MUST double-escape ALL backslashes used in your LaTeX.
For example:
- WRONG: "\\alpha + \\beta"
- CORRECT: "\\\\alpha + \\\\beta"
- WRONG: "\\text{(i)}"
- CORRECT: "\\\\text{(i)}"
If you do not double-escape backslashes, the JSON parser will fail.

CRITICAL SOURCES LINKING RULE:
- If a question from the "SAVED QUESTIONS" in the context has a diagram or image ("Has Diagram/Image: YES"), and you adapt, rewrite, or use this question in the generated quiz, you MUST output its exact "Saved Question ID" (e.g., "src_id::q_id") in the "sourceQuestionId" field of the JSON object.
- You MUST also refer to the diagram/image in your question text (e.g., "as shown in the figure" or "as shown in the diagram below") and keep it exactly linked so that the UI can render it.
- This is the ONLY way we can render the diagram/image of the quiz with options just like JEE Mains/Advance! If you fail to supply the exact "Saved Question ID", the image will not be displayed.


CRITICAL JSON FORMATTING RULES:
1. Output EXACTLY ONE valid JSON array. No extra text before or after.
2. Do NOT use literal newlines in strings. Use \\n instead.
3. Ensure no trailing commas before closing brackets.
4. Provide exactly ${count} questions.

IMPORTANT: Respond ONLY with a valid JSON array of objects. Do not wrap it in markdown. Ensure the JSON array is properly closed at the end.
Format for each object:
[
  {
    "id": "q_${subject.toLowerCase()}_${Date.now()}_[index]",
    "text": "The actual question text here with $math$...",
    "options": ["Option A", "Option B", "Option C", "Option D"],
    "correctOptionIndex": 0,
    "explanation": "Detailed step-by-step explanation including calculations with $math$.",
    "difficulty": "${difficulty}",
    "subject": "${subject}",
    "sourceQuestionId": ""
  }
]

--- CONTEXT MATERIALS START ---
${contextText}
--- CONTEXT MATERIALS END ---

${internetSearch.enabled && internetSearch.query ? `Additionally, include up-to-date, rigorous questions on this topic: ${internetSearch.query}` : ""}
`;
}
class QuizGeneratorManager {
  activeJobs = /* @__PURE__ */ new Map();
  listeners = /* @__PURE__ */ new Set();
  constructor() {
    window.addEventListener("beforeunload", (e) => {
      if (this.isGenerating) {
        e.preventDefault();
        e.returnValue = "Quiz generation is in progress. Closing the app will stop it. Are you sure?";
      }
    });
  }
  get isGenerating() {
    return this.activeJobs.size > 0;
  }
  subscribe(cb) {
    this.listeners.add(cb);
    return () => this.listeners.delete(cb);
  }
  notify() {
    this.listeners.forEach((cb) => cb());
  }
  stopJob(jobId) {
    const job = this.activeJobs.get(jobId);
    if (job && job.abortController) {
      job.abortController.abort();
    }
  }
  async generate(params) {
    const { sources, difficulty, finalQuestionCount, readMediaAsArrayBuffer, availablePdfs, availableVideos, model, useFree, brief, subjectsList } = params;
    const apiKey = localStorage.getItem("jee_openrouter_api_key") || "";
    if (!apiKey) {
      alert("Please set your OpenRouter API Key first in the Engine Settings or Admin Panel!");
      return;
    }
    const jobId = Date.now().toString() + Math.random().toString(36).slice(2);
    const setStatus = (msg) => {
      const job = this.activeJobs.get(jobId);
      if (job) {
        job.status = msg;
        this.notify();
      }
    };
    const abortController = new AbortController();
    const signal = abortController.signal;
    this.activeJobs.set(jobId, { id: jobId, status: "Planning quiz layout...", name: `Quiz (${finalQuestionCount} Qs - ${difficulty})`, abortController });
    this.notify();
    try {
      let contextText = "";
      if (sources.videos.length > 0) {
        setStatus("Gathering video context...");
        contextText += "--- VIDEO SOURCES ---\n";
        sources.videos.forEach((vidId) => {
          const item = availableVideos.find((p) => p.id === vidId);
          if (item) contextText += `Video Topic Covered: ${item.path}
`;
        });
        contextText += "\n";
      }
      if (sources.urls.length > 0) {
        contextText += `--- REFERENCE URLS ---
${sources.urls.join(", ")}

`;
      }
      if (sources.saves.length > 0) {
        setStatus("Extracting saved questions...");
        contextText += "--- SAVED QUESTIONS ---\n";
        const allSaves = JSON.parse(localStorage.getItem("jee_saves_questions_v1") || "{}");
        sources.saves.forEach((srcId) => {
          const qs = allSaves[srcId] || [];
          qs.forEach((q) => {
            const hasImage = !!(q.questionImageKey || q.questionUrl);
            contextText += `Saved Question ID: ${srcId}::${q.id}
Saved Topic: ${q.name || ""}
Details: ${q.description || ""}
Concept Answer: ${q.answerText || ""}
Has Diagram/Image: ${hasImage ? "YES" : "NO"}

`;
          });
        });
      }
      if (sources.pdfs.length > 0) {
        setStatus("Reading text directly from your PDFs...");
        contextText += "--- PDF DOCUMENTS ---\n";
        const pdfjsLib = await __vitePreload(() => import("./vendor-pdf-DJ2ymul_.js"), true ? [] : void 0);
        pdfjsLib.GlobalWorkerOptions.workerSrc = `https://unpkg.com/pdfjs-dist@${pdfjsLib.version}/build/pdf.worker.min.mjs`;
        for (const pdfId of sources.pdfs) {
          const item = availablePdfs.find((p) => p.id === pdfId);
          if (item?.mediaKey) {
            const buf = await readMediaAsArrayBuffer(item.mediaKey);
            if (buf) {
              const pdf = await pdfjsLib.getDocument({ data: buf.slice(0) }).promise;
              const maxPages = Math.min(pdf.numPages, 100);
              for (let i = 1; i <= maxPages; i++) {
                const page = await pdf.getPage(i);
                const content2 = await page.getTextContent();
                contextText += content2.items.map((it) => it.str).join(" ") + "\n";
              }
            }
          } else if (item?.url) {
            contextText += `Document Reference URL: ${item.url}
`;
          }
        }
      }
      const allSavesData = JSON.parse(localStorage.getItem("jee_saves_questions_v1") || "[]");
      const savedList = [];
      Object.entries(allSavesData).forEach(([srcId, questionsList]) => {
        if (Array.isArray(questionsList)) {
          questionsList.forEach((q) => {
            savedList.push({ id: q.id, name: q.name || "", path: srcId });
          });
        }
      });
      const subjectPlans = planSubjects(
        brief,
        difficulty,
        finalQuestionCount,
        sources,
        availablePdfs,
        savedList,
        subjectsList
      );
      let totalBatches = 0;
      for (const [subj, plan] of Object.entries(subjectPlans)) {
        if (plan.count <= 0) continue;
        totalBatches += Math.ceil(plan.count / 5);
      }
      const getContextSlice = (batchIdx, totalBCount) => {
        if (!contextText || contextText.length <= 15e3 || totalBCount <= 1) {
          return contextText.length > 12e4 ? contextText.slice(0, 12e4) + "\n\n...[Context truncated to fit AI limits]..." : contextText;
        }
        const totalLen = contextText.length;
        const sliceSize = Math.ceil(totalLen / totalBCount);
        const overlap = Math.ceil(sliceSize * 0.15);
        const start = Math.max(0, batchIdx * sliceSize - overlap);
        const end = Math.min(totalLen, (batchIdx + 1) * sliceSize + overlap);
        let slice = contextText.substring(start, end);
        if (start > 0) slice = "... [Truncated preceding text context] ...\n" + slice;
        if (end < totalLen) slice = slice + "\n... [Truncated succeeding text context] ...";
        return slice;
      };
      const extractJson = (raw) => {
        let cleanedText = raw.replace(/```(?:json)?/gi, "").replace(/```/g, "").trim();
        const firstBracket = cleanedText.indexOf("[");
        const firstBrace = cleanedText.indexOf("{");
        if (firstBracket === -1 && firstBrace === -1) {
          throw new Error("No JSON structure found");
        }
        let isArray = firstBracket !== -1 && (firstBrace === -1 || firstBracket < firstBrace);
        let startIndex = isArray ? firstBracket : firstBrace;
        cleanedText = cleanedText.substring(startIndex);
        let lastBracket = cleanedText.lastIndexOf("]");
        let lastBrace = cleanedText.lastIndexOf("}");
        let endIndex = isArray ? lastBracket : lastBrace;
        if (endIndex !== -1) {
          cleanedText = cleanedText.substring(0, endIndex + 1);
        } else {
          cleanedText += isArray ? "]" : "}";
        }
        let parsed;
        try {
          parsed = JSON.parse(cleanedText);
        } catch (e) {
          let repaired = cleanedText.replace(/\\(.)/g, (match, char, offset) => {
            if (char === '"' || char === "\\") return match;
            if (char === "n" || char === "r" || char === "t") {
              const nextChar = cleanedText[offset + 2];
              if (nextChar && /[a-zA-Z]/.test(nextChar)) {
                return "\\\\" + char;
              }
              return match;
            }
            return "\\\\" + char;
          });
          repaired = repaired.replace(/\n/g, " ").replace(/\r/g, "").replace(/,\s*]/g, "]").replace(/,\s*}/g, "}");
          try {
            parsed = JSON.parse(repaired);
          } catch (e2) {
            let lastValidEnd = repaired.lastIndexOf("},");
            if (lastValidEnd !== -1) {
              repaired = repaired.substring(0, lastValidEnd + 1) + (isArray ? "]" : "}");
              try {
                parsed = JSON.parse(repaired);
              } catch (e3) {
                return [];
              }
            } else {
              return [];
            }
          }
        }
        if (Array.isArray(parsed) && parsed.length === 1 && parsed[0] && !parsed[0].text) {
          for (let key in parsed[0]) {
            if (Array.isArray(parsed[0][key])) {
              return parsed[0][key];
            }
          }
        } else if (!Array.isArray(parsed) && typeof parsed === "object") {
          for (let key in parsed) {
            if (Array.isArray(parsed[key])) {
              return parsed[key];
            }
          }
          return [parsed];
        }
        return parsed;
      };
      const fixMath2 = (str) => {
        if (!str) return str;
        str = str.replace(/\\ce\{([a-zA-Z0-9\+\-\s\(\)]+)\}/g, (match, chem) => {
          let converted = chem.replace(/([a-zA-Z]+)(\d+)/g, "\\text{$1}_$2");
          converted = converted.replace(new RegExp("(?<!\\d|_|{)([a-zA-Z]+)(?!\\d|_|})", "g"), "\\text{$1}");
          return converted;
        });
        str = str.replace(/&lt;br\s*\/?&gt;/gi, "\n\n");
        str = str.replace(/<br\s*\/?>/gi, "\n\n");
        str = str.replace(/&nbsp;/gi, " ");
        str = str.replace(/&lt;/g, "<");
        str = str.replace(/&gt;/g, ">");
        str = str.replace(/&amp;/g, "&");
        str = str.replace(/\\\(([\s\S]*?)\\\)/g, "$$$1$");
        str = str.replace(/\\\[([\s\S]*?)\\\]/g, "$$$$$1$$$$");
        str = str.replace(/(?:\\text\{sp\}|sp)\s*\^?\s*\{?(\d+)\}?\s*(?:extd|\\text\{d\}|\\textd)\s*\^?\s*\{?(\d+)\}?/g, "sp^$1d^$2");
        str = str.replace(/(?:\\text\{sp\}|sp)\s*\^?\s*\{?(\d+)\}?\s*(?:extd|\\text\{d\}|\\textd)/g, "sp^$1d");
        str = str.replace(/(?:extd|\\text\{d\}|\\textd)\s*\^?\s*\{?(\d+)\}?\s*(?:\\text\{sp\}|sp)\s*\^?\s*\{?(\d+)\}?/g, "d^$1sp^$2");
        str = str.replace(/sp\s*<\s*sup\s*>\s*(\d+)\s*<\s*\/\s*sup\s*>\s*(?:extd|\\text\{d\}|\\textd)\s*<\s*sup\s*>\s*(\d+)\s*<\s*\/\s*sup\s*>/g, "sp<sup>$1</sup>d<sup>$2</sup>");
        str = str.replace(/sp\s*<\s*sup\s*>\s*(\d+)\s*<\s*\/\s*sup\s*>\s*(?:extd|\\text\{d\}|\\textd)/g, "sp<sup>$1</sup>d");
        str = str.replace(/(?:extd|\\text\{d\}|\\textd)\s*<\s*sup\s*>\s*(\d+)\s*<\s*\/\s*sup\s*>\s*sp\s*<\s*sup\s*>\s*(\d+)\s*<\s*\/\s*sup\s*>/g, "d<sup>$1</sup>sp<sup>$2</sup>");
        str = str.replace(/\\begin\{align\*?\}([\s\S]*?)\\end\{align\*?\}/g, "\\begin{aligned}$1\\end{aligned}");
        str = str.replace(/(?:\$\$|\$)?\s*\\begin\{([a-zA-Z*]+)\}([\s\S]*?)\\end\{\1\}\s*(?:\$\$|\$)?/g, (match, env2, inner) => {
          const mathEnvs = ["aligned", "pmatrix", "bmatrix", "vmatrix", "matrix", "cases", "array", "eqnarray", "equation", "equation*"];
          if (mathEnvs.includes(env2)) {
            let cleaned = inner.replace(/\$\$/g, "").replace(/\$/g, "");
            return `
$$
\\begin{${env2}}${cleaned}\\end{${env2}}
$$
`;
          }
          return match;
        });
        str = str.replace(/\((\\text\{[^}]+\}.*?)\)/g, "$$$1$");
        str = str.replace(/\((\\displaystyle.*?)\)/g, "$$$1$");
        str = str.replace(/^\[\s+([\s\S]*?[_^\\][\s\S]*?)\s+\]$/gm, "$$$$ $1 $$$$");
        str = str.replace(/\dots/g, "\\dots");
        str = str.replace(/\$\$\s*([a-zA-Z\\{}_0-9]+)\s*\]\s*=/g, "$$$$ [$1] =");
        let tempStr = str.replace(/\\\$/g, "___ESCAPED_DOLLAR___");
        let tokens = tempStr.split(/(\$\$?)/);
        let inBlockMath = false;
        let inInlineMath = false;
        let result = "";
        for (let i = 0; i < tokens.length; i++) {
          let token = tokens[i];
          if (token === "$$") {
            if (!inInlineMath) inBlockMath = !inBlockMath;
            result += token;
          } else if (token === "$") {
            if (!inBlockMath) inInlineMath = !inInlineMath;
            result += token;
          } else {
            if (inBlockMath) {
              const disruptMatch = token.match(/(\*\*|\n\s*\n)/);
              if (disruptMatch) {
                const idx = disruptMatch.index;
                result += token.substring(0, idx) + "$$" + token.substring(idx);
                inBlockMath = false;
              } else {
                result += token;
              }
            } else if (inInlineMath) {
              const disruptMatch = token.match(/(\*\*|\n\s*\n)/);
              if (disruptMatch) {
                const idx = disruptMatch.index;
                result += token.substring(0, idx) + "$" + token.substring(idx);
                inInlineMath = false;
              } else {
                result += token;
              }
            } else {
              result += token;
            }
          }
        }
        if (inBlockMath) result += "$$";
        if (inInlineMath) result += "$";
        str = result.replace(/___ESCAPED_DOLLAR___/g, "\\$");
        return str;
      };
      let allParsedQuestions = [];
      let primaryApiError = "";
      let batchIndex = 0;
      for (const [subj, plan] of Object.entries(subjectPlans)) {
        if (plan.count <= 0) continue;
        let subjectQuestions = [];
        let attempts = 0;
        const MAX_ATTEMPTS = Math.ceil(plan.count / 3) * 3;
        while (subjectQuestions.length < plan.count && attempts < MAX_ATTEMPTS) {
          const needed = plan.count - subjectQuestions.length;
          const batchSize = Math.min(5, needed);
          setStatus(`Formulating ${subj} questions (${subjectQuestions.length}/${plan.count} complete)...`);
          const currentSlice = getContextSlice(batchIndex, totalBatches);
          const promptText = buildSubjectPrompt({
            subject: subj,
            count: batchSize,
            difficulty: plan.difficulty,
            brief,
            contextText: currentSlice,
            internetSearch: sources.internetSearch
          });
          let success = false;
          const modelsToTry = [
            "meta-llama/llama-3.3-70b-instruct:free",
            "qwen/qwen-2.5-coder-32b-instruct:free",
            "openai/gpt-oss-120b:free",
            "openai/gpt-oss-20b:free",
            "z-ai/glm-4.5-air:free",
            "nvidia/nemotron-3.5-content-safety:free",
            "nvidia/nemotron-3-ultra-550b-a55b:free",
            "nousresearch/hermes-3-llama-3.1-405b:free",
            "moonshotai/kimi-k2.6:free",
            "google/gemma-2-9b-it:free"
          ];
          const uniqueModels = Array.from(new Set(modelsToTry));
          for (const modelName of uniqueModels) {
            try {
              const payload = {
                model: modelName,
                messages: [{ role: "user", content: promptText }]
              };
              if (sources.internetSearch.enabled && ["google/gemma-4-31b-it:free", "google/gemma-4-31b-it", "nousresearch/hermes-3-llama-3.1-405b:free", "nousresearch/hermes-3-llama-3.1-405b", "meta-llama/llama-3.3-70b-instruct:free", "meta-llama/llama-3.3-70b-instruct"].includes(modelName)) {
                payload.plugins = [{ id: "web", max_results: 5 }];
              }
              let response;
              try {
                response = await fetch("https://openrouter.ai/api/v1/chat/completions", {
                  method: "POST",
                  headers: {
                    "Authorization": `Bearer ${apiKey.trim()}`,
                    "HTTP-Referer": window.location.href,
                    "X-Title": "StudE",
                    "Content-Type": "application/json"
                  },
                  body: JSON.stringify(payload),
                  signal
                });
              } catch (e) {
                if (e.name === "AbortError") throw e;
                response = await fetch(`https://corsproxy.io/?${encodeURIComponent("https://openrouter.ai/api/v1/chat/completions")}`, {
                  method: "POST",
                  headers: {
                    "Authorization": `Bearer ${apiKey.trim()}`,
                    "HTTP-Referer": window.location.href,
                    "X-Title": "StudE",
                    "Content-Type": "application/json"
                  },
                  body: JSON.stringify(payload),
                  signal
                });
              }
              if (!response.ok) {
                const errData = await response.json().catch(() => ({}));
                let errorMsg = response.statusText || String(response.status);
                let errorCode = response.status;
                if (errData.error?.message) errorMsg = errData.error.message;
                if (errData.error?.code) errorCode = errData.error.code;
                const isCreditExhaustedError = (status, message) => {
                  const msg = message.toLowerCase();
                  return status === 402 || msg.includes("credit") || msg.includes("balance") || msg.includes("insufficient funds") || msg.includes("insufficient balance") || msg.includes("payment required");
                };
                if (isCreditExhaustedError(errorCode, errorMsg)) {
                  throw new Error(`CREDIT_EXHAUSTED: ${errorMsg}`);
                }
                throw new Error(`OpenRouter Error (${modelName}): ${errorMsg}`);
              }
              const data = await response.json();
              const rawText = data.choices?.[0]?.message?.content || "[]";
              const parsed = extractJson(rawText);
              if (Array.isArray(parsed) && parsed.length > 0) {
                const validatedQs = parsed.filter((q) => q && q.text && Array.isArray(q.options) && typeof q.correctOptionIndex === "number").map((q) => ({
                  ...q,
                  subject: subj,
                  // Enforce correct subject allocation
                  difficulty: q.difficulty || plan.difficulty,
                  text: fixMath2(q.text),
                  options: q.options.map((opt) => fixMath2(opt)),
                  explanation: fixMath2(q.explanation)
                }));
                if (validatedQs.length > 0) {
                  subjectQuestions = [...subjectQuestions, ...validatedQs];
                  success = true;
                  batchIndex++;
                  break;
                } else {
                  throw new Error(`No valid questions returned from ${modelName}`);
                }
              } else {
                throw new Error(`Invalid JSON structure from ${modelName}`);
              }
            } catch (err) {
              if (err.name === "AbortError") throw err;
              const errMsg = err.message || "";
              if (errMsg.includes("CREDIT_EXHAUSTED") || errMsg.toLowerCase().includes("credits exhausted") || errMsg.toLowerCase().includes("insufficient balance") || errMsg.toLowerCase().includes("insufficient funds")) {
                throw new Error("Credit ends: Can't able to generate quiz. Please add credits/balance to your OpenRouter account.");
              }
              console.warn(`Model ${modelName} failed:`, err);
              if (!primaryApiError) primaryApiError = err.message;
            }
          }
          if (!success) {
            throw new Error(`Could not generate ${subj} questions. Fallbacks exhausted. Error: ${primaryApiError}`);
          }
          attempts++;
        }
        if (subjectQuestions.length > plan.count) {
          subjectQuestions = subjectQuestions.slice(0, plan.count);
        }
        allParsedQuestions = [...allParsedQuestions, ...subjectQuestions];
      }
      if (allParsedQuestions.length === 0) {
        throw new Error(`AI returned no valid questions.
API Error: ${primaryApiError || "Format issue suspected."}`);
      }
      try {
        const allSaves = JSON.parse(localStorage.getItem("jee_saves_questions_v1") || "{}");
        const savedQuestionsMap = {};
        Object.entries(allSaves).forEach(([srcId, questionsList]) => {
          if (Array.isArray(questionsList)) {
            questionsList.forEach((q) => {
              savedQuestionsMap[`${srcId}::${q.id}`] = {
                imageKey: q.questionImageKey,
                imageUrl: q.questionUrl
              };
              const cleanName = (q.name || "").toLowerCase().replace(/\s+/g, "");
              if (cleanName) {
                savedQuestionsMap[cleanName] = {
                  imageKey: q.questionImageKey,
                  imageUrl: q.questionUrl
                };
              }
            });
          }
        });
        allParsedQuestions = allParsedQuestions.map((q) => {
          let imageKey = void 0;
          let imageUrl = void 0;
          if (q.sourceQuestionId && savedQuestionsMap[q.sourceQuestionId]) {
            imageKey = savedQuestionsMap[q.sourceQuestionId].imageKey;
            imageUrl = savedQuestionsMap[q.sourceQuestionId].imageUrl;
          } else {
            const cleanText = (q.text || "").toLowerCase().replace(/\s+/g, "");
            for (const [key, value] of Object.entries(savedQuestionsMap)) {
              if (key.includes("::")) continue;
              if (cleanText.includes(key) || key.includes(cleanText)) {
                imageKey = value.imageKey;
                imageUrl = value.imageUrl;
                break;
              }
            }
          }
          return {
            ...q,
            imageKey,
            imageUrl
          };
        });
      } catch (imgErr) {
        console.warn("Diagram image mapping failed:", imgErr);
      }
      allParsedQuestions.sort((a, b) => {
        const idxA = subjectsList.indexOf(a.subject);
        const idxB = subjectsList.indexOf(b.subject);
        const orderA = idxA !== -1 ? idxA : 99;
        const orderB = idxB !== -1 ? idxB : 99;
        return orderA - orderB;
      });
      const newQuiz = {
        id: Date.now().toString(),
        name: `Quiz - ${(/* @__PURE__ */ new Date()).toLocaleString()}`,
        createdAt: Date.now(),
        questions: allParsedQuestions,
        difficulty
      };
      const pastQuizzes = JSON.parse(localStorage.getItem("jee_saved_quizzes_v1") || "[]");
      const newQuizzes = [newQuiz, ...pastQuizzes];
      localStorage.setItem("jee_saved_quizzes_v1", JSON.stringify(newQuizzes));
      if (typeof Notification !== "undefined" && Notification.permission === "granted") {
        new Notification("Quiz Generated!", { body: `Your test with ${allParsedQuestions.length} questions is ready.` });
      } else {
        alert("Your Quiz is generated!");
      }
    } catch (e) {
      if (e.name === "AbortError") {
        console.log(`Job ${jobId} stopped by user.`);
        return;
      }
      console.error("Quiz Generation Failed:", e);
      alert("Failed to generate quiz with AI: \n" + e.message);
    } finally {
      this.activeJobs.delete(jobId);
      this.notify();
    }
  }
}
const quizGeneratorManager = new QuizGeneratorManager();
function QuestionImage({ imageKey, imageUrl }) {
  const { readMediaAsBlob } = useWorkspaceContext();
  const [src, setSrc] = reactExports.useState(null);
  const [loading, setLoading] = reactExports.useState(false);
  reactExports.useEffect(() => {
    if (imageUrl) {
      setSrc(imageUrl);
      return;
    }
    if (imageKey) {
      setLoading(true);
      readMediaAsBlob(imageKey).then((blob) => {
        if (blob) {
          const url = URL.createObjectURL(blob);
          setSrc(url);
        }
      }).finally(() => setLoading(false));
    } else {
      setSrc(null);
    }
  }, [imageKey, imageUrl, readMediaAsBlob]);
  if (loading) {
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "my-4 p-4 border border-border rounded-xl flex items-center justify-center bg-muted/20", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(RefreshCw, { className: "h-5 w-5 text-muted-foreground animate-spin" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-xs text-muted-foreground ml-2 font-medium", children: "Loading diagram..." })
    ] });
  }
  if (!src) return null;
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "my-4 p-2 border border-border rounded-xl bg-white dark:bg-zinc-900 inline-block max-w-full shadow-sm hover:shadow-md transition-shadow", children: /* @__PURE__ */ jsxRuntimeExports.jsx("img", { src, alt: "Question Diagram", className: "max-h-[250px] object-contain rounded-lg" }) });
}
const getSubjectsForGoal = (goal) => {
  if (!goal) return ["Physics", "Chemistry", "Maths"];
  switch (goal.category) {
    case "JEE":
      return ["Physics", "Chemistry", "Maths"];
    case "NEET":
      return ["Physics", "Chemistry", "Biology"];
    case "UPSC":
      return ["History", "Polity", "Geography", "Economy", "CSAT", "Current Affairs"];
    case "Boards":
    case "School": {
      const isArts = goal.path?.includes("Arts");
      const isCommerce = goal.path?.includes("Commerce");
      if (isArts) {
        return ["History", "Geography", "Political Science", "English"];
      } else if (isCommerce) {
        return ["Accountancy", "Business Studies", "Economics", "Maths"];
      } else {
        return ["Physics", "Chemistry", "Maths", "Biology"];
      }
    }
    case "Olympiads":
      return ["Mathematics", "Physics", "Chemistry", "Biology", "Astronomy", "Mental Ability"];
    case "Skills":
      return ["Theory", "Practice", "Tools", "Coding & Logic"];
    case "Banking":
      return ["Quantitative Aptitude", "Reasoning Ability", "English Language", "General Awareness"];
    case "Govt Exam":
      return ["General Knowledge", "Quantitative Aptitude", "Reasoning", "English", "General Science"];
    default:
      return ["Core Subject", "Secondary Subject", "General Aptitude", "Revision & Mock"];
  }
};
function AIQuizInterface({ onBack }) {
  const { user, selectedGoal } = useAppContext();
  const { readMediaAsArrayBuffer } = useWorkspaceContext();
  const [phase, setPhase] = reactExports.useState("setup");
  const [numQuestions, setNumQuestions] = reactExports.useState(10);
  const [customNum, setCustomNum] = reactExports.useState(25);
  const [difficulty, setDifficulty] = reactExports.useState("Mixed");
  const [showSourceModal, setShowSourceModal] = reactExports.useState(false);
  const [briefInstructions, setBriefInstructions] = reactExports.useState("");
  const [aiExplanations, setAiExplanations] = reactExports.useState({});
  const [sources, setSources] = reactExports.useState({
    pdfs: [],
    saves: [],
    videos: [],
    urls: [],
    internetSearch: { enabled: false, query: "" }
  });
  const [availablePdfs, setAvailablePdfs] = reactExports.useState([]);
  const [availableVideos, setAvailableVideos] = reactExports.useState([]);
  const [availableSaves, setAvailableSaves] = reactExports.useState([]);
  const [sourceTab, setSourceTab] = reactExports.useState("pdfs");
  const [urlInput, setUrlInput] = reactExports.useState("");
  const [questions, setQuestions] = reactExports.useState([]);
  const [currentIndex, setCurrentIndex] = reactExports.useState(0);
  const [score, setScore] = reactExports.useState(0);
  const [questionStatuses, setQuestionStatuses] = reactExports.useState({});
  const [answers, setAnswers] = reactExports.useState({});
  const [timeLeft, setTimeLeft] = reactExports.useState(10800);
  const goalSubjects = reactExports.useMemo(() => {
    return getSubjectsForGoal(selectedGoal);
  }, [selectedGoal]);
  const [activeSubject, setActiveSubject] = reactExports.useState(() => getSubjectsForGoal(selectedGoal)[0] || "Physics");
  const [showInstructions, setShowInstructions] = reactExports.useState(false);
  const [showSummary, setShowSummary] = reactExports.useState(false);
  const [quizName, setQuizName] = reactExports.useState("");
  const [timeSpent, setTimeSpent] = reactExports.useState({});
  const [reviewMode, setReviewMode] = reactExports.useState(false);
  reactExports.useEffect(() => {
    const fresh = getSubjectsForGoal(selectedGoal);
    if (fresh.length > 0 && !fresh.includes(activeSubject)) {
      setActiveSubject(fresh[0]);
    }
  }, [selectedGoal]);
  const subjects = reactExports.useMemo(() => {
    const uniqueSubjects = Array.from(new Set(questions.map((q) => q.subject).filter(Boolean)));
    const standardOrder = goalSubjects;
    return uniqueSubjects.sort((a, b) => standardOrder.indexOf(a) - standardOrder.indexOf(b));
  }, [questions, goalSubjects]);
  const [savedQuizzes, setSavedQuizzes] = reactExports.useState(() => {
    try {
      return JSON.parse(localStorage.getItem("jee_saved_quizzes_v1") || "[]");
    } catch {
      return [];
    }
  });
  reactExports.useEffect(() => {
    localStorage.setItem("jee_saved_quizzes_v1", JSON.stringify(savedQuizzes));
  }, [savedQuizzes]);
  const [isGenerating, setIsGenerating] = reactExports.useState(quizGeneratorManager.isGenerating);
  const [activeJobs, setActiveJobs] = reactExports.useState(Array.from(quizGeneratorManager.activeJobs.values()));
  reactExports.useEffect(() => {
    let prevJobCount = quizGeneratorManager.activeJobs.size;
    const unsubscribe = quizGeneratorManager.subscribe(() => {
      setIsGenerating(quizGeneratorManager.isGenerating);
      setActiveJobs(Array.from(quizGeneratorManager.activeJobs.values()));
      const currentJobCount = quizGeneratorManager.activeJobs.size;
      if (currentJobCount < prevJobCount) {
        try {
          setSavedQuizzes(JSON.parse(localStorage.getItem("jee_saved_quizzes_v1") || "[]"));
        } catch {
        }
      }
      prevJobCount = currentJobCount;
    });
    return () => {
      unsubscribe();
    };
  }, []);
  const [renamingQuizId, setRenamingQuizId] = reactExports.useState(null);
  const [renameVal, setRenameVal] = reactExports.useState("");
  const totalSources = sources.pdfs.length + sources.saves.length + sources.videos.length + sources.urls.length + (sources.internetSearch.enabled ? 1 : 0);
  const finalQuestionCount = numQuestions === "custom" ? customNum : numQuestions;
  const detectedBriefPlan = reactExports.useMemo(() => {
    return planSubjects(briefInstructions, difficulty, finalQuestionCount, sources, availablePdfs, availableSaves, goalSubjects);
  }, [briefInstructions, difficulty, finalQuestionCount, sources, availablePdfs, availableSaves, goalSubjects]);
  reactExports.useEffect(() => {
    if (typeof Notification !== "undefined" && Notification.permission === "default") {
      Notification.requestPermission();
    }
  }, []);
  reactExports.useEffect(() => {
    try {
      const pdfs = JSON.parse(localStorage.getItem("jee_pdf_sections_v3") || "[]");
      const vids = JSON.parse(localStorage.getItem("jee_vid_sections_v1") || "[]");
      const saves = JSON.parse(localStorage.getItem("jee_saves_subjects_v1") || "[]");
      const pItems = [];
      pdfs.forEach((sec) => {
        sec.subsections?.forEach((sub) => {
          if (sub.pdfKey || sub.pdfUrl || sub.imageKey) pItems.push({ id: sub.id, name: sub.name, path: `${sec.name} > ${sub.name}`, mediaKey: sub.pdfKey, url: sub.pdfUrl });
          sub.subsubsections?.forEach((ss) => {
            if (ss.pdfKey || ss.pdfUrl || ss.imageKey) pItems.push({ id: ss.id, name: ss.name, path: `${sec.name} > ${sub.name} > ${ss.name}`, mediaKey: ss.pdfKey, url: ss.pdfUrl });
          });
        });
      });
      setAvailablePdfs(pItems);
      const vItems = [];
      vids.forEach((sec) => {
        sec.subsections?.forEach((sub) => {
          if (sub.video) vItems.push({ id: sub.id, name: sub.name, path: `${sec.name} > ${sub.name}` });
          sub.subsubsections?.forEach((ss) => {
            if (ss.video) vItems.push({ id: ss.id, name: ss.name, path: `${sec.name} > ${sub.name} > ${ss.name}` });
          });
        });
      });
      setAvailableVideos(vItems);
      const sItems = [];
      saves.forEach((sub) => {
        sub.chapters?.forEach((chap) => {
          chap.sources?.forEach((src) => {
            sItems.push({ id: src.id, name: src.name, path: `${sub.name} > ${chap.name}` });
          });
        });
      });
      setAvailableSaves(sItems);
    } catch (e) {
    }
  }, []);
  const handleStartAnalysis = () => {
    quizGeneratorManager.generate({
      sources,
      difficulty,
      finalQuestionCount,
      readMediaAsArrayBuffer,
      availablePdfs,
      availableVideos,
      model: "",
      useFree: true,
      brief: briefInstructions,
      subjectsList: goalSubjects
    });
  };
  const handleGenerateExplanation = async (idx) => {
    const q = questions[idx];
    if (!q) return;
    setAiExplanations((prev) => ({
      ...prev,
      [idx]: { text: "", loading: true }
    }));
    const apiKey = localStorage.getItem("jee_openrouter_api_key") || "";
    if (!apiKey) {
      setAiExplanations((prev) => ({
        ...prev,
        [idx]: { text: "", loading: false, error: "OpenRouter API Key is missing. Please configure it in the Admin Panel." }
      }));
      return;
    }
    const explanationModels = [
      "nousresearch/hermes-3-llama-3.1-405b:free",
      "google/gemma-4-31b-it:free",
      "meta-llama/llama-3.3-70b-instruct:free"
    ];
    const promptText = `You are a world-class IIT JEE teacher and expert evaluator. 
Provide a comprehensive, mathematically rigorous, and step-by-step explanation for the following JEE question.

Question:
${q.text}

Options:
${q.options.map((opt, i) => `${String.fromCharCode(65 + i)}) ${opt}`).join("\n")}

Correct Option: Option ${String.fromCharCode(65 + q.correctOptionIndex)}

Please write a detailed derivation, detailing all equations, steps, values, and laws/theorems used. Use LaTeX notation for all formulas, equations, symbols, and chemical formulas. Wrap block equations in $$ and inline equations in $. 

Respond with ONLY the markdown explanation. Do not wrap it in JSON or HTML. Start directly with the steps.`;
    let success = false;
    let lastError = "";
    const fixExplanationMath = (str) => {
      if (!str) return str;
      str = str.replace(/\\ce\{([a-zA-Z0-9\+\-\s\(\)]+)\}/g, (match, chem) => {
        let converted = chem.replace(/([a-zA-Z]+)(\d+)/g, "\\text{$1}_$2");
        converted = converted.replace(new RegExp("(?<!\\d|_|{)([a-zA-Z]+)(?!\\d|_|})", "g"), "\\text{$1}");
        return converted;
      });
      str = str.replace(/\\\(([\s\S]*?)\\\)/g, "$$$1$");
      str = str.replace(/\\\[([\s\S]*?)\\\]/g, "$$$$$1$$$$");
      return str;
    };
    for (const modelName of explanationModels) {
      try {
        const payload = {
          model: modelName,
          messages: [{ role: "user", content: promptText }]
        };
        let response;
        try {
          response = await fetch("https://openrouter.ai/api/v1/chat/completions", {
            method: "POST",
            headers: {
              "Authorization": `Bearer ${apiKey.trim()}`,
              "HTTP-Referer": window.location.href,
              "X-Title": "StudE",
              "Content-Type": "application/json"
            },
            body: JSON.stringify(payload)
          });
        } catch (e) {
          response = await fetch(`https://corsproxy.io/?${encodeURIComponent("https://openrouter.ai/api/v1/chat/completions")}`, {
            method: "POST",
            headers: {
              "Authorization": `Bearer ${apiKey.trim()}`,
              "HTTP-Referer": window.location.href,
              "X-Title": "StudE",
              "Content-Type": "application/json"
            },
            body: JSON.stringify(payload)
          });
        }
        if (!response.ok) {
          throw new Error(`Failed with status ${response.status}`);
        }
        const data = await response.json();
        const text2 = data.choices?.[0]?.message?.content;
        if (text2) {
          setAiExplanations((prev) => ({
            ...prev,
            [idx]: { text: fixExplanationMath(text2), model: modelName, loading: false }
          }));
          success = true;
          break;
        } else {
          throw new Error("No response text");
        }
      } catch (err) {
        console.warn(`Explanation model ${modelName} failed:`, err);
        lastError = err.message || "Unknown error";
      }
    }
    if (!success) {
      setAiExplanations((prev) => ({
        ...prev,
        [idx]: { text: "", loading: false, error: `Could not generate explanation. Fallbacks exhausted. Last error: ${lastError}` }
      }));
    }
  };
  reactExports.useEffect(() => {
    if (reviewMode && questions[currentIndex]) {
      if (!aiExplanations[currentIndex]) {
        handleGenerateExplanation(currentIndex);
      }
    }
  }, [currentIndex, reviewMode, questions]);
  const formatTime = (seconds) => {
    const h = Math.floor(seconds / 3600);
    const m = Math.floor(seconds % 3600 / 60);
    const s = seconds % 60;
    return `${h.toString().padStart(2, "0")}:${m.toString().padStart(2, "0")}:${s.toString().padStart(2, "0")}`;
  };
  const formatTimeSpent = (seconds) => {
    if (seconds < 60) return `${seconds}s`;
    const m = Math.floor(seconds / 60);
    const s = seconds % 60;
    return `${m}m ${s}s`;
  };
  reactExports.useEffect(() => {
    if (phase === "active" && timeLeft > 0 && !showSummary && !showInstructions && !reviewMode) {
      const timer = setInterval(() => {
        setTimeLeft((t) => t - 1);
        setTimeSpent((prev) => ({
          ...prev,
          [currentIndex]: (prev[currentIndex] || 0) + 1
        }));
      }, 1e3);
      return () => clearInterval(timer);
    } else if (timeLeft === 0 && phase === "active" && !reviewMode) {
      finalSubmit();
    }
    return;
  }, [phase, timeLeft, showSummary, showInstructions, currentIndex, reviewMode]);
  const questionsBySubject = subjects.reduce((acc, subj) => {
    acc[subj] = questions.filter((q) => q.subject === subj);
    return acc;
  }, {});
  const handleTabClick = (subj) => {
    setActiveSubject(subj);
    const firstQIndex = questions.findIndex((q) => q.subject === subj);
    if (firstQIndex !== -1) {
      setCurrentIndex(firstQIndex);
      updateStatus(firstQIndex);
    }
  };
  const updateStatus = (index2) => {
    setQuestionStatuses((prev) => {
      if (prev[index2] === "not_visited") return { ...prev, [index2]: "not_answered" };
      return prev;
    });
  };
  const goToNext = () => {
    let nextIndex = currentIndex + 1;
    if (nextIndex >= questions.length) {
      if (reviewMode) {
        setPhase("results");
      } else {
        setShowSummary(true);
      }
    } else {
      const nextSubj = questions[nextIndex].subject;
      if (nextSubj && nextSubj !== activeSubject) setActiveSubject(nextSubj);
      setCurrentIndex(nextIndex);
      updateStatus(nextIndex);
    }
  };
  const handleSaveAndNext = () => {
    const hasAnswer = answers[currentIndex] !== void 0;
    setQuestionStatuses((prev) => ({
      ...prev,
      [currentIndex]: hasAnswer ? "answered" : "not_answered"
    }));
    goToNext();
  };
  const handleClear = () => {
    setAnswers((prev) => {
      const newAns = { ...prev };
      delete newAns[currentIndex];
      return newAns;
    });
  };
  const handleSaveAndMark = () => {
    const hasAnswer = answers[currentIndex] !== void 0;
    if (hasAnswer) {
      setQuestionStatuses((prev) => ({ ...prev, [currentIndex]: "answered_marked_for_review" }));
    } else {
      setQuestionStatuses((prev) => ({ ...prev, [currentIndex]: "marked_for_review" }));
    }
    goToNext();
  };
  const handleMarkAndNext = () => {
    setQuestionStatuses((prev) => ({ ...prev, [currentIndex]: "marked_for_review" }));
    goToNext();
  };
  const handleBack = () => {
    if (currentIndex > 0) {
      const nextIndex = currentIndex - 1;
      const nextSubj = questions[nextIndex].subject;
      if (nextSubj && nextSubj !== activeSubject) setActiveSubject(nextSubj);
      setCurrentIndex(nextIndex);
      updateStatus(nextIndex);
    }
  };
  const startQuiz = (quiz) => {
    setQuizName(quiz.name);
    setQuestions(quiz.questions);
    setCurrentIndex(0);
    setScore(0);
    setAnswers({});
    setTimeSpent({});
    setReviewMode(false);
    const initialStatuses = {};
    quiz.questions.forEach((_, i) => initialStatuses[i] = i === 0 ? "not_answered" : "not_visited");
    setQuestionStatuses(initialStatuses);
    setTimeLeft(quiz.questions.length * 120);
    setActiveSubject(quiz.questions[0]?.subject || "Physics");
    setShowSummary(false);
    setShowInstructions(false);
    setPhase("active");
  };
  const commitRename = (id) => {
    if (renameVal.trim()) {
      setSavedQuizzes((prev) => prev.map((q) => q.id === id ? { ...q, name: renameVal.trim() } : q));
    }
    setRenamingQuizId(null);
  };
  const deleteQuiz = (id) => {
    if (confirm("Are you sure you want to delete this test?")) {
      setSavedQuizzes((prev) => prev.filter((q) => q.id !== id));
    }
  };
  const handleSubmit = () => {
    setShowSummary(true);
  };
  const finalSubmit = () => {
    let calculatedScore = 0;
    questions.forEach((q, i) => {
      const ans = answers[i];
      if (ans !== void 0 && ans === q.correctOptionIndex) {
        calculatedScore += 4;
      } else if (ans !== void 0 && ans !== q.correctOptionIndex) {
        calculatedScore -= 1;
      }
    });
    setScore(calculatedScore);
    setShowSummary(false);
    const past = JSON.parse(localStorage.getItem("jee_quiz_results") || "[]");
    localStorage.setItem("jee_quiz_results", JSON.stringify([...past, {
      id: Date.now().toString(),
      date: (/* @__PURE__ */ new Date()).toISOString(),
      score: calculatedScore,
      total: questions.length,
      difficulty
    }]));
    setPhase("results");
  };
  const getSubjectStats = (subj) => {
    const subjQs = questions.filter((q) => q.subject === subj);
    const subjIndices = questions.map((q, idx) => q.subject === subj ? idx : -1).filter((idx) => idx !== -1);
    let correct = 0;
    let incorrect = 0;
    let skipped = 0;
    let time = 0;
    subjIndices.forEach((idx) => {
      const ans = answers[idx];
      const q = questions[idx];
      time += timeSpent[idx] || 0;
      if (ans === void 0) skipped++;
      else if (ans === q.correctOptionIndex) correct++;
      else incorrect++;
    });
    const sScore = correct * 4 - incorrect * 1;
    const totalSubjQs = subjQs.length;
    return {
      correct,
      incorrect,
      skipped,
      time,
      score: sScore,
      total: totalSubjQs
    };
  };
  const addUrl = () => {
    if (urlInput.trim()) {
      setSources((s) => ({ ...s, urls: [...s.urls, urlInput.trim()] }));
      setUrlInput("");
    }
  };
  const renderCheckboxList = (items, selectedIds, type) => {
    if (items.length === 0) return /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm text-muted-foreground py-8 text-center border-2 border-dashed border-border rounded-xl", children: "No sources found in your workspace yet." });
    return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "space-y-1", children: items.map((item) => {
      const isChecked = selectedIds.includes(item.id);
      return /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "flex items-start gap-3 p-3 bg-muted/40 hover:bg-muted border border-transparent hover:border-border rounded-xl cursor-pointer transition-colors", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "input",
          {
            type: "checkbox",
            checked: isChecked,
            onChange: (e) => setSources((s) => ({ ...s, [type]: e.target.checked ? [...s[type], item.id] : s[type].filter((id) => id !== item.id) })),
            className: "mt-1 w-4 h-4 accent-primary rounded cursor-pointer"
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 min-w-0", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm font-semibold text-foreground truncate", children: item.name }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-[10px] font-medium text-muted-foreground truncate uppercase tracking-wider", children: item.path })
        ] })
      ] }, item.id);
    }) });
  };
  const totalPossibleScore = questions.length * 4;
  const answeredQuestionsCount = Object.keys(answers).length;
  const correctAnswersCount = questions.reduce((acc, q, idx) => {
    return acc + (answers[idx] !== void 0 && answers[idx] === q.correctOptionIndex ? 1 : 0);
  }, 0);
  const incorrectAnswersCount = questions.reduce((acc, q, idx) => {
    return acc + (answers[idx] !== void 0 && answers[idx] !== q.correctOptionIndex ? 1 : 0);
  }, 0);
  const skippedAnswersCount = questions.length - answeredQuestionsCount;
  const accuracy = answeredQuestionsCount > 0 ? Math.round(correctAnswersCount / answeredQuestionsCount * 100) : 0;
  const totalSecondsSpent = Object.values(timeSpent).reduce((a, b) => a + b, 0);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "h-full w-full bg-background overflow-y-auto flex flex-col items-center relative text-foreground", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "w-full relative overflow-hidden bg-gradient-to-br from-primary/10 via-card to-background border-b border-border px-6 py-8 shrink-0", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute inset-0 bg-[radial-gradient(ellipse_at_top_right,_hsl(var(--primary)/0.1),_transparent_50%)]" }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "max-w-4xl mx-auto flex items-center gap-4 relative z-10", children: [
        onBack && /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            onClick: onBack,
            className: "p-2.5 hover:bg-muted rounded-2xl border border-border text-muted-foreground hover:text-foreground transition-all flex items-center justify-center",
            title: "Back to Options",
            children: /* @__PURE__ */ jsxRuntimeExports.jsx(ChevronLeft, { className: "h-5 w-5" })
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "h-12 w-12 rounded-2xl bg-primary/20 flex items-center justify-center border border-primary/30 shrink-0 shadow-lg shadow-primary/20", children: /* @__PURE__ */ jsxRuntimeExports.jsx(BrainCircuit, { className: "h-6 w-6 text-primary" }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("h1", { className: "text-2xl font-black text-foreground tracking-tight", children: "AI Quiz Generator" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm text-muted-foreground mt-0.5", children: "Test your knowledge with an advanced custom exam builder." })
        ] })
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex-1 w-full max-w-4xl px-6 py-8 relative", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(AnimatePresence, { mode: "wait", children: [
      phase === "setup" && /* @__PURE__ */ jsxRuntimeExports.jsxs(
        motion.div,
        {
          initial: { opacity: 0, y: 20 },
          animate: { opacity: 1, y: 0 },
          exit: { opacity: 0, y: -20 },
          className: "space-y-8",
          children: [
            !localStorage.getItem("jee_openrouter_api_key") && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "p-4 rounded-2xl bg-red-500/10 border border-red-500/20 text-red-600 dark:text-red-400 flex items-start gap-3 text-sm font-semibold", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(CircleAlert, { className: "h-5 w-5 shrink-0 mt-0.5" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "font-bold", children: "No OpenRouter API Key configured" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs font-normal opacity-90", children: "Please enter your API Key in the Admin Panel to enable quiz generation." })
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-card border border-border rounded-3xl p-6 shadow-sm", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 mb-4", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Target, { className: "h-5 w-5 text-blue-500" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "text-sm font-bold uppercase tracking-wider text-foreground", children: "Number of Questions" })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-wrap items-center gap-3", children: [
                [5, 10, 15, 20].map((num) => /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "button",
                  {
                    onClick: () => setNumQuestions(num),
                    className: cn(
                      "px-5 py-2.5 rounded-xl text-sm font-bold transition-all border-2",
                      numQuestions === num ? "bg-blue-500/10 border-blue-500 text-blue-500 dark:text-blue-400" : "bg-muted/50 border-transparent text-muted-foreground hover:bg-muted hover:text-foreground"
                    ),
                    children: num
                  },
                  num
                )),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 ml-2", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    "button",
                    {
                      onClick: () => setNumQuestions("custom"),
                      className: cn(
                        "px-5 py-2.5 rounded-xl text-sm font-bold transition-all border-2",
                        numQuestions === "custom" ? "bg-blue-500/10 border-blue-500 text-blue-500 dark:text-blue-400" : "bg-muted/50 border-transparent text-muted-foreground hover:bg-muted hover:text-foreground"
                      ),
                      children: "Custom"
                    }
                  ),
                  /* @__PURE__ */ jsxRuntimeExports.jsx(AnimatePresence, { children: numQuestions === "custom" && /* @__PURE__ */ jsxRuntimeExports.jsx(
                    motion.div,
                    {
                      initial: { opacity: 0, width: 0, marginLeft: 0 },
                      animate: { opacity: 1, width: "auto", marginLeft: 8 },
                      exit: { opacity: 0, width: 0, marginLeft: 0 },
                      className: "overflow-hidden",
                      children: /* @__PURE__ */ jsxRuntimeExports.jsx(
                        Input,
                        {
                          type: "number",
                          value: customNum,
                          onChange: (e) => setCustomNum(Math.max(1, parseInt(e.target.value) || 1)),
                          className: "w-20 text-center font-bold bg-background border-2 border-blue-500/30 focus-visible:ring-blue-500"
                        }
                      )
                    }
                  ) })
                ] })
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-card border border-border rounded-3xl p-6 shadow-sm", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 mb-4", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Zap, { className: "h-5 w-5 text-amber-500" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "text-sm font-bold uppercase tracking-wider text-foreground", children: "Difficulty Level" })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "grid grid-cols-2 sm:grid-cols-4 gap-3", children: ["Easy", "Medium", "Hard", "Mixed"].map((diff) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
                "button",
                {
                  onClick: () => setDifficulty(diff),
                  className: cn(
                    "p-4 rounded-xl text-sm font-bold transition-all border-2 flex flex-col items-center justify-center gap-2",
                    difficulty === diff ? "bg-amber-500/10 border-amber-500 text-amber-600 dark:text-amber-400" : "bg-muted/50 border-transparent text-muted-foreground hover:bg-muted hover:text-foreground"
                  ),
                  children: [
                    diff,
                    diff === "Mixed" && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[10px] font-medium opacity-70", children: "Ratio 2:1:3" })
                  ]
                },
                diff
              )) })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-card border border-border rounded-3xl p-6 shadow-sm", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between mb-4", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(BookOpen, { className: "h-5 w-5 text-purple-500" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "text-sm font-bold uppercase tracking-wider text-foreground", children: "Data Sources" })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs(
                  Button,
                  {
                    variant: "outline",
                    size: "sm",
                    onClick: () => setShowSourceModal(true),
                    className: "gap-2 border-purple-500/30 text-purple-600 dark:text-purple-400 hover:bg-purple-500/10 font-bold",
                    children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx(Settings, { className: "h-4 w-4" }),
                      "Manage Sources"
                    ]
                  }
                )
              ] }),
              totalSources === 0 ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "text-center py-6 border-2 border-dashed border-border rounded-2xl bg-muted/20", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(CircleAlert, { className: "h-8 w-8 text-muted-foreground/50 mx-auto mb-2" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm font-medium text-foreground", children: "No sources selected" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground mt-1", children: "Select PDFs, Saves, Videos, URLs or Internet to ground the AI." }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => setShowSourceModal(true), variant: "secondary", size: "sm", className: "mt-4 font-bold", children: "Add Sources" })
              ] }) : /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-wrap gap-3", children: [
                sources.pdfs.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 bg-red-500/10 text-red-600 dark:text-red-400 px-3 py-1.5 rounded-lg border border-red-500/20 text-sm font-semibold", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(FileText, { className: "h-4 w-4" }),
                  " ",
                  sources.pdfs.length,
                  " PDF Sections"
                ] }),
                sources.saves.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 bg-blue-500/10 text-blue-600 dark:text-blue-400 px-3 py-1.5 rounded-lg border border-blue-500/20 text-sm font-semibold", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(Save, { className: "h-4 w-4" }),
                  " ",
                  sources.saves.length,
                  " Saved Sources"
                ] }),
                sources.videos.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 bg-indigo-500/10 text-indigo-600 dark:text-indigo-400 px-3 py-1.5 rounded-lg border border-indigo-500/20 text-sm font-semibold", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(FilePlay, { className: "h-4 w-4" }),
                  " ",
                  sources.videos.length,
                  " Video Sections"
                ] }),
                sources.urls.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 bg-emerald-500/10 text-emerald-600 dark:text-emerald-400 px-3 py-1.5 rounded-lg border border-emerald-500/20 text-sm font-semibold", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(Globe, { className: "h-4 w-4" }),
                  " ",
                  sources.urls.length,
                  " URLs"
                ] }),
                sources.internetSearch.enabled && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 bg-amber-500/10 text-amber-600 dark:text-amber-400 px-3 py-1.5 rounded-lg border border-amber-500/20 text-sm font-semibold", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(Search, { className: "h-4 w-4" }),
                  " Web Search Active"
                ] })
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-card border border-border rounded-3xl p-6 shadow-sm", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 mb-2", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Pencil, { className: "h-5 w-5 text-indigo-500" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "text-sm font-bold uppercase tracking-wider text-foreground", children: "Explain Briefly / Custom Guidelines" })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground mb-4", children: 'Describe exact ratios, target sub-topics, or subject splits (e.g. "in 30 questions put 10 tough questions of Maths, 16 medium questions of Physics and 4 hard questions of Chemistry").' }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "textarea",
                {
                  value: briefInstructions,
                  onChange: (e) => setBriefInstructions(e.target.value),
                  placeholder: "e.g. put 10 tough question of Maths, 16 medium question of Physics and 4 hard question for Chemistry. Exclude chemistry completely if needed.",
                  className: "w-full min-h-[100px] rounded-2xl bg-muted/30 border border-border p-4 text-sm focus:outline-none focus:ring-1 focus:ring-primary focus:border-primary placeholder:text-muted-foreground/60 transition-all font-medium"
                }
              ),
              briefInstructions.trim() && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mt-3 p-3 bg-indigo-500/5 rounded-xl border border-indigo-500/10 text-xs flex flex-wrap gap-4 items-center font-semibold text-foreground", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-muted-foreground flex items-center gap-1", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(Info, { className: "w-3.5 h-3.5" }),
                  " Detected Plan:"
                ] }),
                Object.entries(detectedBriefPlan).map(([subj, plan]) => {
                  if (plan.count <= 0) return null;
                  return /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-indigo-600 dark:text-indigo-400 font-bold", children: [
                    subj,
                    ": ",
                    plan.count,
                    " Qs (",
                    plan.difficulty,
                    ")"
                  ] }, subj);
                }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-foreground font-black ml-auto border-l pl-3 border-border", children: [
                  "Total: ",
                  Object.values(detectedBriefPlan).reduce((acc, curr) => acc + curr.count, 0),
                  " Qs"
                ] })
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex justify-end pt-2", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
              Button,
              {
                onClick: handleStartAnalysis,
                disabled: totalSources === 0 || !localStorage.getItem("jee_openrouter_api_key"),
                size: "lg",
                className: "gap-2 h-14 px-8 rounded-2xl text-base font-bold shadow-xl shadow-primary/25",
                children: [
                  isGenerating ? "Generate Another Quiz" : "Synthesize & Generate Quiz",
                  " ",
                  /* @__PURE__ */ jsxRuntimeExports.jsx(Zap, { className: "h-5 w-5" })
                ]
              }
            ) }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-card border border-border rounded-3xl p-6 shadow-sm mt-8", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 mb-4", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Trophy, { className: "h-5 w-5 text-green-500" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "text-sm font-bold uppercase tracking-wider text-foreground", children: "Your Generated Tests" })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-3", children: [
                activeJobs.map((job) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "p-4 rounded-xl border border-primary/20 bg-primary/5 flex items-center justify-between", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-3", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(RefreshCw, { className: "h-5 w-5 text-primary animate-spin" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-sm font-bold text-foreground", children: [
                        "Generating ",
                        job.name,
                        "..."
                      ] }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground font-medium", children: job.status })
                    ] })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "outline", size: "sm", className: "h-8 w-8 p-0 text-red-500 hover:text-red-600 hover:bg-red-500/10 border-red-500/20", onClick: () => quizGeneratorManager.stopJob(job.id), children: /* @__PURE__ */ jsxRuntimeExports.jsx(Square, { className: "h-3.5 w-3.5 fill-current" }) })
                ] }, job.id)),
                savedQuizzes.length === 0 && !isGenerating && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "text-center py-6 border-2 border-dashed border-border rounded-2xl bg-muted/20", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm font-medium text-foreground", children: "No tests generated yet" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground mt-1", children: "Configure options above and generate your first quiz." })
                ] }),
                savedQuizzes.map((quiz) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "p-4 rounded-xl border border-border bg-card hover:bg-muted/50 transition-colors flex flex-col sm:flex-row sm:items-center justify-between group gap-4", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 min-w-0 pr-0 sm:pr-4", children: [
                    renamingQuizId === quiz.id ? /* @__PURE__ */ jsxRuntimeExports.jsx(
                      Input,
                      {
                        autoFocus: true,
                        value: renameVal,
                        onChange: (e) => setRenameVal(e.target.value),
                        onBlur: () => commitRename(quiz.id),
                        onKeyDown: (e) => e.key === "Enter" && commitRename(quiz.id),
                        className: "h-7 text-sm font-bold bg-background mb-1"
                      }
                    ) : /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm font-bold text-foreground truncate", children: quiz.name }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-xs text-muted-foreground font-semibold", children: [
                      quiz.questions.length,
                      " Questions • ",
                      quiz.difficulty,
                      " • ",
                      new Date(quiz.createdAt).toLocaleDateString()
                    ] })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 shrink-0", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "outline", size: "icon", className: "h-8 w-8 opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity", onClick: () => {
                      setRenamingQuizId(quiz.id);
                      setRenameVal(quiz.name);
                    }, children: /* @__PURE__ */ jsxRuntimeExports.jsx(Pencil, { className: "h-4 w-4" }) }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "outline", size: "icon", className: "h-8 w-8 opacity-100 sm:opacity-0 sm:group-hover:opacity-100 transition-opacity text-red-500 hover:text-red-600 hover:bg-red-500/10 border-transparent hover:border-red-500/20", onClick: () => deleteQuiz(quiz.id), children: /* @__PURE__ */ jsxRuntimeExports.jsx(Trash2, { className: "h-4 w-4" }) }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "sm", className: "h-8 px-4 font-bold", onClick: () => startQuiz(quiz), children: "Start Test" })
                  ] })
                ] }, quiz.id))
              ] })
            ] })
          ]
        },
        "setup"
      ),
      phase === "active" && questions.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs(
        motion.div,
        {
          initial: { opacity: 0 },
          animate: { opacity: 1 },
          exit: { opacity: 0 },
          className: "fixed inset-0 z-50 bg-background flex flex-col font-sans text-foreground",
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between px-4 py-2 border-b border-border bg-card shrink-0", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-3", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-16 h-16 bg-muted border border-border rounded-full flex items-center justify-center overflow-hidden shrink-0 shadow-sm", children: /* @__PURE__ */ jsxRuntimeExports.jsx(User, { className: "h-10 w-10 text-muted-foreground fill-current" }) }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("table", { className: "text-sm font-semibold text-foreground", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("tbody", { children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("tr", { children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("td", { className: "pr-2 pb-0.5 text-muted-foreground font-bold", children: "Candidate Name" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("td", { className: "pb-0.5", children: [
                      ": ",
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-extrabold text-foreground", children: user || "Student" })
                    ] })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("tr", { children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("td", { className: "pr-2 pb-0.5 text-muted-foreground font-bold", children: "Test Name" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("td", { className: "pb-0.5 flex items-center gap-1", children: [
                      ": ",
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-extrabold text-foreground", children: quizName }),
                      " ",
                      /* @__PURE__ */ jsxRuntimeExports.jsx(Info, { className: "w-3.5 h-3.5 text-muted-foreground" })
                    ] })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("tr", { children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("td", { className: "pr-2 text-muted-foreground font-bold", children: reviewMode ? "Mode" : "Remaining Time" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("td", { children: [
                      ": ",
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: cn("px-2 py-0.5 rounded-full font-bold text-xs shadow-sm text-white", reviewMode ? "bg-emerald-500" : "bg-[#3b82f6]"), children: reviewMode ? "REVIEW SOLUTIONS" : formatTime(timeLeft) })
                    ] })
                  ] })
                ] }) })
              ] }),
              !reviewMode && /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "outline", onClick: () => setShowInstructions(true), className: "border-border text-foreground hover:bg-muted font-bold text-sm", children: "View Instructions" })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center bg-card border-b border-border shrink-0 px-1 relative", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "px-2 py-2.5 text-sm text-muted-foreground hover:text-foreground shrink-0", children: /* @__PURE__ */ jsxRuntimeExports.jsx(ChevronLeft, { className: "w-4 h-4" }) }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex bg-card flex-1 overflow-x-auto scrollbar-hide", children: subjects.map((subj) => {
                if (!questionsBySubject[subj] || questionsBySubject[subj].length === 0) return null;
                return /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "button",
                  {
                    onClick: () => handleTabClick(subj),
                    className: cn("px-5 py-2.5 text-sm font-bold border-b-2 transition-all whitespace-nowrap", activeSubject === subj ? "border-primary text-primary bg-primary/5" : "border-transparent text-muted-foreground hover:text-foreground hover:bg-muted/30"),
                    children: subj
                  },
                  subj
                );
              }) }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "px-2 py-2.5 text-sm text-muted-foreground hover:text-foreground shrink-0", children: /* @__PURE__ */ jsxRuntimeExports.jsx(ChevronRight, { className: "w-4 h-4" }) })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-1 overflow-hidden bg-background text-foreground", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 flex flex-col border-r border-border relative", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between items-center px-6 py-3 shrink-0 border-b border-border bg-card/50", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-4", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "font-bold text-lg text-foreground", children: [
                      "Question ",
                      questions.filter((q) => q.subject === activeSubject).findIndex((q) => q === questions[currentIndex]) + 1,
                      ":"
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-muted-foreground border border-border bg-muted/30 px-2.5 py-0.5 rounded-full text-xs font-bold", children: [
                      "Marks: ",
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-emerald-500 font-extrabold", children: "+4" }),
                      " ",
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-red-500 font-extrabold", children: "-1" })
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-muted-foreground border border-border bg-muted/30 px-2.5 py-0.5 rounded-full text-xs font-bold", children: "Type: Single Option Correct" })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "text-muted-foreground hover:text-foreground", children: /* @__PURE__ */ jsxRuntimeExports.jsx(EllipsisVertical, { className: "w-5 h-5" }) })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 overflow-y-auto p-6 md:p-8 space-y-6", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "prose dark:prose-invert max-w-none text-base md:text-lg text-foreground leading-relaxed font-serif", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Markdown, { remarkPlugins: [remarkMath, remarkGfm], rehypePlugins: [rehypeKatex], children: questions[currentIndex].text }) }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    QuestionImage,
                    {
                      imageKey: questions[currentIndex].imageKey,
                      imageUrl: questions[currentIndex].imageUrl
                    }
                  ),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "mt-8 space-y-3", children: questions[currentIndex].options.map((opt, i) => {
                    const isSelected = answers[currentIndex] === i;
                    const isCorrect = i === questions[currentIndex].correctOptionIndex;
                    return /* @__PURE__ */ jsxRuntimeExports.jsxs(
                      "label",
                      {
                        className: cn(
                          "flex items-start gap-4 p-4 rounded-2xl border transition-all cursor-pointer select-none",
                          reviewMode ? isCorrect ? "bg-emerald-500/10 border-emerald-500/30 text-emerald-950 dark:text-emerald-300" : isSelected ? "bg-red-500/10 border-red-500/30 text-red-950 dark:text-red-300" : "border-border bg-card/30 hover:bg-card" : isSelected ? "bg-primary/5 border-primary text-primary" : "border-border bg-card/30 hover:bg-card hover:border-border/80"
                        ),
                        children: [
                          /* @__PURE__ */ jsxRuntimeExports.jsx(
                            "input",
                            {
                              type: "radio",
                              name: `question-${currentIndex}`,
                              checked: isSelected,
                              disabled: reviewMode,
                              onChange: () => setAnswers((prev) => ({ ...prev, [currentIndex]: i })),
                              className: "mt-1 w-4 h-4 accent-primary cursor-pointer border-border"
                            }
                          ),
                          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "font-semibold prose dark:prose-invert max-w-none text-sm md:text-base text-foreground flex-1", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Markdown, { remarkPlugins: [remarkMath, remarkGfm], rehypePlugins: [rehypeKatex], components: quizOptionComponents, children: `(${String.fromCharCode(65 + i)}) ${opt}` }) }),
                          reviewMode && isCorrect && /* @__PURE__ */ jsxRuntimeExports.jsx(Check, { className: "h-5 w-5 text-emerald-500 shrink-0 mt-0.5" }),
                          reviewMode && isSelected && !isCorrect && /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "h-5 w-5 text-red-500 shrink-0 mt-0.5" })
                        ]
                      },
                      i
                    );
                  }) }),
                  reviewMode && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-6 mt-8", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "p-6 bg-primary/5 border border-primary/15 rounded-2xl space-y-4", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 text-primary font-bold text-sm uppercase tracking-wider", children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsx(BrainCircuit, { className: "h-4 w-4" }),
                        " Comprehensive Scientific Derivation"
                      ] }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "prose dark:prose-invert max-w-none text-sm md:text-base text-foreground leading-relaxed font-sans", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Markdown, { remarkPlugins: [remarkMath, remarkGfm], rehypePlugins: [rehypeKatex], children: questions[currentIndex].explanation }) }),
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "text-xs text-muted-foreground pt-3 border-t border-border flex flex-wrap gap-4 justify-between font-semibold", children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                          "Time spent: ",
                          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-foreground", children: formatTimeSpent(timeSpent[currentIndex] || 0) })
                        ] }),
                        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
                          "Difficulty: ",
                          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-foreground", children: questions[currentIndex].difficulty })
                        ] })
                      ] })
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "p-6 bg-purple-500/5 border border-purple-500/20 rounded-2xl space-y-4", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between flex-wrap gap-2", children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 text-purple-600 dark:text-purple-400 font-bold text-sm uppercase tracking-wider", children: [
                          /* @__PURE__ */ jsxRuntimeExports.jsx(Sparkles, { className: "h-4 w-4 animate-pulse" }),
                          " Premium AI Explanation (Multi-Model)"
                        ] }),
                        aiExplanations[currentIndex]?.model && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[10px] bg-purple-500/10 text-purple-600 dark:text-purple-400 px-2 py-0.5 rounded font-mono font-bold", children: aiExplanations[currentIndex].model })
                      ] }),
                      !aiExplanations[currentIndex] || aiExplanations[currentIndex].error ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-3", children: [
                        aiExplanations[currentIndex]?.error && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-red-500 font-semibold", children: aiExplanations[currentIndex].error }),
                        /* @__PURE__ */ jsxRuntimeExports.jsxs(
                          Button,
                          {
                            onClick: () => handleGenerateExplanation(currentIndex),
                            className: "bg-purple-600 hover:bg-purple-700 text-white font-bold rounded text-xs px-4 h-9 flex items-center gap-1.5",
                            children: [
                              /* @__PURE__ */ jsxRuntimeExports.jsx(Sparkles, { className: "h-3.5 w-3.5" }),
                              " Generate Premium AI Explanation"
                            ]
                          }
                        )
                      ] }) : aiExplanations[currentIndex].loading ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2.5 py-4", children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsx(RefreshCw, { className: "h-4 w-4 text-purple-500 animate-spin" }),
                        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-xs font-semibold text-muted-foreground", children: "Generating premium derivation using nousresearch/hermes-3-llama-3.1-405b:free..." })
                      ] }) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "prose dark:prose-invert max-w-none text-sm md:text-base text-foreground leading-relaxed font-sans", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Markdown, { remarkPlugins: [remarkMath, remarkGfm], rehypePlugins: [rehypeKatex], children: aiExplanations[currentIndex].text }) })
                    ] })
                  ] })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "p-3 border-t border-border flex flex-wrap gap-3 justify-between items-center bg-card shrink-0", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex gap-2", children: !reviewMode && /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: handleSaveAndNext, className: "bg-emerald-600 hover:bg-emerald-700 text-white font-bold rounded px-5 h-9 text-sm", children: "SAVE & NEXT" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "outline", onClick: handleClear, className: "font-bold border-border text-foreground hover:bg-muted rounded h-9 text-sm", children: "CLEAR" })
                  ] }) }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex gap-2", children: !reviewMode && /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: handleSaveAndMark, className: "bg-blue-600 hover:bg-blue-700 text-white font-bold rounded px-5 h-9 text-sm", children: "SAVE & MARK FOR REVIEW" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: handleMarkAndNext, className: "bg-amber-600 hover:bg-amber-700 text-white font-bold rounded px-5 h-9 text-sm", children: "MARK FOR REVIEW & NEXT" })
                  ] }) })
                ] })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "w-[340px] flex flex-col bg-card shrink-0 select-none", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "p-4 grid grid-cols-2 gap-y-3 gap-x-2 border-b border-border text-[11px] text-muted-foreground font-bold", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(AnsweredShape, { children: Object.values(questionStatuses).filter((s) => s === "answered").length }),
                    " ",
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Answered" })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(NotAnsweredShape, { children: Object.values(questionStatuses).filter((s) => s === "not_answered").length }),
                    " ",
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Not Answered" })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(NotVisitedShape, { children: Object.values(questionStatuses).filter((s) => s === "not_visited").length }),
                    " ",
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Not Visited" })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(MarkedShape, { children: Object.values(questionStatuses).filter((s) => s === "marked_for_review").length }),
                    " ",
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: "Mark for review" })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "col-span-2 flex items-center gap-2 mt-1", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx(AnsweredMarkedShape, { children: Object.values(questionStatuses).filter((s) => s === "answered_marked_for_review").length }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "leading-tight", children: "Answered & Marked (evaluated)" })
                  ] })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 overflow-y-auto bg-muted/20", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "bg-muted text-foreground font-bold text-sm px-4 py-2 border-b border-border", children: activeSubject }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "p-4 grid grid-cols-5 gap-3", children: questionsBySubject[activeSubject]?.map((q, localIndex) => {
                    const globalIndex = questions.findIndex((x) => x === q);
                    return /* @__PURE__ */ jsxRuntimeExports.jsx(
                      "button",
                      {
                        onClick: () => {
                          setCurrentIndex(globalIndex);
                          updateStatus(globalIndex);
                        },
                        className: "focus:outline-none flex justify-center hover:opacity-80 transition-opacity",
                        children: reviewMode ? /* @__PURE__ */ jsxRuntimeExports.jsx(ReviewShape, { index: globalIndex, answers, questions, children: localIndex + 1 }) : (() => {
                          const status = questionStatuses[globalIndex];
                          let ShapeComponent = NotVisitedShape;
                          if (status === "not_answered") ShapeComponent = NotAnsweredShape;
                          else if (status === "answered") ShapeComponent = AnsweredShape;
                          else if (status === "marked_for_review") ShapeComponent = MarkedShape;
                          else if (status === "answered_marked_for_review") ShapeComponent = AnsweredMarkedShape;
                          return /* @__PURE__ */ jsxRuntimeExports.jsx(ShapeComponent, { children: localIndex + 1 });
                        })()
                      },
                      globalIndex
                    );
                  }) })
                ] })
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between px-4 py-3 bg-card border-t border-border shrink-0 shadow-md z-10", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex gap-2", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "outline", onClick: handleBack, className: "font-bold border-border text-foreground hover:bg-muted rounded h-10 px-4 text-sm", disabled: currentIndex === 0, children: "< BACK" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "outline", onClick: goToNext, className: "font-bold border-border text-foreground hover:bg-muted rounded h-10 px-4 text-sm", disabled: currentIndex === questions.length - 1, children: "NEXT >" })
              ] }),
              reviewMode ? /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => setPhase("results"), className: "bg-primary hover:bg-primary/95 text-primary-foreground font-bold rounded px-8 h-10 text-sm", children: "EXIT REVIEW" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: handleSubmit, className: "bg-emerald-600 hover:bg-emerald-700 text-white font-bold rounded px-8 h-10 text-sm", children: "SUBMIT" })
            ] }),
            showSummary && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "fixed inset-0 z-[60] flex items-center justify-center bg-black/60 backdrop-blur-sm p-4 font-sans text-foreground", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-card border border-border rounded-2xl shadow-2xl p-8 w-full max-w-3xl", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "text-2xl font-black mb-6 text-center bg-muted py-3 rounded-lg", children: "Test Submission Summary" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "grid grid-cols-2 sm:grid-cols-5 gap-4 mb-8", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "text-center p-4 bg-muted/40 border border-border rounded-xl shadow-sm", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-3xl font-black text-foreground mb-1", children: questions.length }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-xs font-bold text-muted-foreground uppercase", children: "Total" })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "text-center p-4 bg-green-500/10 border border-green-500/20 rounded-xl shadow-sm", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-3xl font-black text-emerald-500 mb-1", children: Object.values(questionStatuses).filter((s) => s === "answered" || s === "answered_marked_for_review").length }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-xs font-bold text-emerald-600 uppercase", children: "Answered" })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "text-center p-4 bg-red-500/10 border border-red-500/20 rounded-xl shadow-sm", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-3xl font-black text-red-500 mb-1", children: Object.values(questionStatuses).filter((s) => s === "not_answered").length }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-xs font-bold text-red-600 uppercase", children: "Not Answered" })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "text-center p-4 bg-purple-500/10 border border-purple-500/20 rounded-xl shadow-sm", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-3xl font-black text-purple-500 mb-1", children: Object.values(questionStatuses).filter((s) => s === "marked_for_review").length }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-xs font-bold text-purple-600 uppercase", children: "Marked" })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "text-center p-4 bg-muted/60 border border-border rounded-xl shadow-sm", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-3xl font-black text-muted-foreground mb-1", children: Object.values(questionStatuses).filter((s) => s === "not_visited").length }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-xs font-bold text-muted-foreground uppercase", children: "Not Visited" })
                ] })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("p", { className: "text-center mb-8 font-bold text-lg text-foreground", children: [
                "Are you sure you want to submit the test for final marking?",
                /* @__PURE__ */ jsxRuntimeExports.jsx("br", {}),
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-sm font-medium text-muted-foreground", children: "No changes will be allowed after submission." })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-center gap-6", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "outline", size: "lg", onClick: () => setShowSummary(false), className: "font-bold px-8 h-12 rounded-xl border-border text-foreground hover:bg-muted", children: "Return" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { size: "lg", onClick: finalSubmit, className: "font-bold px-10 h-12 rounded-xl bg-blue-600 hover:bg-blue-700 text-white shadow-lg shadow-blue-500/30", children: "Submit" })
              ] })
            ] }) }),
            showInstructions && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "fixed inset-0 z-[60] flex items-center justify-center bg-black/60 backdrop-blur-sm p-4 font-sans text-foreground", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-card border border-border rounded-xl shadow-2xl w-full max-w-4xl max-h-[90vh] flex flex-col", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "px-6 py-4 border-b border-border bg-muted/40 rounded-t-xl shrink-0", children: /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "text-lg font-black text-center text-foreground uppercase tracking-widest", children: "General Instructions" }) }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 overflow-y-auto p-6 md:p-8 text-sm text-muted-foreground leading-relaxed", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "font-bold text-foreground mb-4 text-center", children: "Please read the instructions carefully" }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("ol", { className: "list-decimal pl-5 mb-6 space-y-2 font-medium", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("li", { children: "Total duration of examination is 180 minutes." }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("li", { children: "The countdown timer in the top right corner of screen will display the remaining time available for you to complete the examination. When the timer reaches zero, the examination will end automatically." }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("li", { children: "The Question Palette displayed on the right side of screen will show the status of each question." })
                ] })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "px-6 py-4 border-t border-border bg-muted/40 rounded-b-xl flex justify-end shrink-0", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => setShowInstructions(false), className: "bg-blue-600 hover:bg-blue-700 text-white font-bold px-8 h-10 rounded", children: "Close" }) })
            ] }) })
          ]
        },
        "active"
      ),
      phase === "results" && /* @__PURE__ */ jsxRuntimeExports.jsxs(
        motion.div,
        {
          initial: { opacity: 0, y: 20 },
          animate: { opacity: 1, y: 0 },
          exit: { opacity: 0, y: -20 },
          className: "space-y-8 max-w-4xl mx-auto pb-12 text-foreground",
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-card border border-border rounded-3xl p-8 shadow-sm flex flex-col md:flex-row items-center justify-between gap-6 relative overflow-hidden", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute inset-0 bg-gradient-to-r from-yellow-500/5 via-transparent to-transparent pointer-events-none" }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-6", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "h-20 w-20 bg-yellow-500/10 rounded-2xl flex items-center justify-center border border-yellow-500/20 shadow-lg shadow-yellow-500/5 shrink-0 animate-bounce", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Trophy, { className: "h-10 w-10 text-yellow-500" }) }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "text-3xl font-black tracking-tight", children: "Test Analysis Report" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm text-muted-foreground mt-1", children: "Detailed subject breakdown and time analysis." })
                ] })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex gap-4 w-full md:w-auto", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs(Button, { onClick: () => {
                  setReviewMode(true);
                  setPhase("active");
                  setCurrentIndex(0);
                }, className: "flex-1 md:flex-initial h-12 px-6 rounded-xl font-bold bg-primary text-primary-foreground shadow-lg hover:bg-primary/95 transition-all gap-2", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(Eye, { className: "w-5 h-5" }),
                  " View Solutions"
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "outline", onClick: () => setPhase("setup"), className: "flex-1 md:flex-initial h-12 px-6 rounded-xl font-bold border-border hover:bg-muted", children: "Dashboard" })
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "grid grid-cols-2 md:grid-cols-4 gap-4", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-card border border-border rounded-2xl p-5 shadow-sm text-center", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-xs font-bold text-muted-foreground uppercase tracking-wider block", children: "Score Obtained" }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "text-3xl font-black text-primary mt-2", children: [
                  score,
                  " ",
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-sm font-normal text-muted-foreground", children: [
                    "/ ",
                    totalPossibleScore
                  ] })
                ] })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-card border border-border rounded-2xl p-5 shadow-sm text-center", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-xs font-bold text-muted-foreground uppercase tracking-wider block", children: "Accuracy Rating" }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "text-3xl font-black text-emerald-500 mt-2", children: [
                  accuracy,
                  "%"
                ] })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-card border border-border rounded-2xl p-5 shadow-sm text-center", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-xs font-bold text-muted-foreground uppercase tracking-wider block", children: "Total Time Spent" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-3xl font-black text-amber-500 mt-2", children: formatTimeSpent(totalSecondsSpent) })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-card border border-border rounded-2xl p-5 shadow-sm text-center", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-xs font-bold text-muted-foreground uppercase tracking-wider block", children: "Questions Breakdown" }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "text-xl font-black text-foreground mt-2.5 flex items-center justify-center gap-1.5 font-sans", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-emerald-500", children: correctAnswersCount }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-muted-foreground text-xs font-medium", children: "C" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-muted-foreground text-xs font-medium", children: "•" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-red-500", children: incorrectAnswersCount }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-muted-foreground text-xs font-medium", children: "W" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-muted-foreground text-xs font-medium", children: "•" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-muted-foreground", children: skippedAnswersCount }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-muted-foreground text-xs font-medium", children: "S" })
                ] })
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-4", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("h3", { className: "text-lg font-black tracking-tight flex items-center gap-2", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Target, { className: "h-5 w-5 text-primary animate-pulse" }),
                " Subject Analysis"
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "grid grid-cols-1 md:grid-cols-3 gap-6", children: subjects.map((subj) => {
                const stats = getSubjectStats(subj);
                if (stats.total === 0) return null;
                return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "bg-card border border-border rounded-2xl p-6 shadow-sm flex flex-col justify-between hover:border-primary/20 transition-all", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("h4", { className: "text-base font-extrabold text-foreground border-b border-border pb-2 mb-4 flex items-center justify-between", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: subj }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-xs bg-primary/10 text-primary px-2.5 py-0.5 rounded-full", children: [
                      stats.total,
                      " Qs"
                    ] })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-3", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between text-sm font-semibold", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-muted-foreground", children: "Subject Score" }),
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-foreground", children: [
                        stats.score,
                        " / ",
                        stats.total * 4
                      ] })
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between text-sm font-semibold", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-muted-foreground", children: "Accuracy" }),
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-emerald-500", children: [
                        stats.correct + stats.incorrect > 0 ? Math.round(stats.correct / (stats.correct + stats.incorrect) * 100) : 0,
                        "%"
                      ] })
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between text-sm font-semibold", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-muted-foreground", children: "Time Spent" }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-amber-500", children: formatTimeSpent(stats.time) })
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between text-sm font-semibold", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-muted-foreground", children: "Correct / Wrong" }),
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-foreground", children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-emerald-500", children: stats.correct }),
                        " / ",
                        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-red-500", children: stats.incorrect })
                      ] })
                    ] })
                  ] })
                ] }) }, subj);
              }) })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "bg-card border border-border rounded-3xl p-6 shadow-sm space-y-5", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "text-lg font-black tracking-tight", children: "Question Grid & Performance" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "space-y-6", children: subjects.map((subj) => {
                const stats = getSubjectStats(subj);
                if (stats.total === 0) return null;
                return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-3", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between items-center bg-muted/40 px-4 py-2.5 rounded-xl border border-border/50", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-sm font-bold text-foreground", children: subj }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-xs text-muted-foreground font-semibold", children: [
                      stats.correct,
                      " Correct • ",
                      stats.incorrect,
                      " Wrong • ",
                      stats.skipped,
                      " Skipped"
                    ] })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "grid grid-cols-5 sm:grid-cols-10 gap-3", children: questions.map((q, idx) => {
                    if (q.subject !== subj) return null;
                    const subjQs = questions.filter((x) => x.subject === subj);
                    const localIndex = subjQs.findIndex((x) => x === q) + 1;
                    const ans = answers[idx];
                    const isCorrect = ans !== void 0 && ans === q.correctOptionIndex;
                    const isWrong = ans !== void 0 && ans !== q.correctOptionIndex;
                    let bgClass = "bg-muted/40 text-muted-foreground border-border";
                    if (isCorrect) bgClass = "bg-emerald-500/10 border-emerald-500/30 text-emerald-600 dark:text-emerald-400";
                    if (isWrong) bgClass = "bg-red-500/10 border-red-500/30 text-red-600 dark:text-red-400";
                    return /* @__PURE__ */ jsxRuntimeExports.jsxs(
                      "button",
                      {
                        onClick: () => {
                          setReviewMode(true);
                          setPhase("active");
                          setCurrentIndex(idx);
                        },
                        className: cn(
                          "h-11 border rounded-xl flex flex-col items-center justify-center font-bold text-xs hover:opacity-80 transition-all select-none",
                          bgClass
                        ),
                        children: [
                          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: localIndex }),
                          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-[8.5px] font-semibold opacity-70 mt-0.5", children: formatTimeSpent(timeSpent[idx] || 0) })
                        ]
                      },
                      idx
                    );
                  }) })
                ] }, subj);
              }) })
            ] })
          ]
        },
        "results"
      )
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(AnimatePresence, { children: showSourceModal && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "fixed inset-0 z-50 flex items-center justify-center p-4", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        motion.div,
        {
          initial: { opacity: 0 },
          animate: { opacity: 1 },
          exit: { opacity: 0 },
          className: "absolute inset-0 bg-background/80 backdrop-blur-sm",
          onClick: () => setShowSourceModal(false)
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(
        motion.div,
        {
          initial: { opacity: 0, scale: 0.95, y: 20 },
          animate: { opacity: 1, scale: 1, y: 0 },
          exit: { opacity: 0, scale: 0.95, y: 20 },
          className: "relative w-full max-w-2xl bg-card border border-border rounded-3xl shadow-2xl p-6 flex flex-col max-h-[85vh]",
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between mb-4 shrink-0", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { className: "text-lg font-black text-foreground", children: "Manage Data Sources" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { onClick: () => setShowSourceModal(false), className: "p-2 hover:bg-muted rounded-full transition-colors text-muted-foreground", children: /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "h-5 w-5" }) })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex gap-2 border-b border-border pb-2 mb-4 shrink-0 overflow-x-auto scrollbar-hide", children: ["pdfs", "videos", "saves", "urls", "internet"].map((tab2) => /* @__PURE__ */ jsxRuntimeExports.jsx(
              "button",
              {
                onClick: () => setSourceTab(tab2),
                className: cn("px-4 py-2 text-sm font-bold rounded-lg transition-colors whitespace-nowrap", sourceTab === tab2 ? "bg-primary text-primary-foreground" : "text-muted-foreground hover:bg-muted"),
                children: tab2 === "pdfs" ? "PDFs" : tab2.charAt(0).toUpperCase() + tab2.slice(1)
              },
              tab2
            )) }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 overflow-y-auto min-h-[250px] pr-2", children: [
              sourceTab === "pdfs" && renderCheckboxList(availablePdfs, sources.pdfs, "pdfs"),
              sourceTab === "videos" && renderCheckboxList(availableVideos, sources.videos, "videos"),
              sourceTab === "saves" && renderCheckboxList(availableSaves, sources.saves, "saves"),
              sourceTab === "urls" && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-4", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex gap-2", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { value: urlInput, onChange: (e) => setUrlInput(e.target.value), onKeyDown: (e) => e.key === "Enter" && addUrl(), placeholder: "https://youtube.com/... or any document url", className: "flex-1 text-sm bg-muted/50" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: addUrl, size: "sm", className: "px-6 font-bold", children: "Add URL" })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-2", children: [
                  sources.urls.length === 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm text-muted-foreground text-center py-6 border-2 border-dashed border-border rounded-xl", children: "No URLs added." }),
                  sources.urls.map((url, i) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex justify-between items-center p-3 bg-muted/40 border border-border rounded-xl text-sm", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "truncate flex-1 mr-2 text-sm font-medium", children: url }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("button", { onClick: () => setSources((s) => ({ ...s, urls: s.urls.filter((_, idx) => idx !== i) })), className: "p-1.5 hover:bg-red-500/20 rounded-md text-red-500 transition-colors", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Trash2, { className: "w-4 h-4" }) })
                  ] }, i))
                ] })
              ] }),
              sourceTab === "internet" && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-6", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "p-5 rounded-2xl border border-amber-500/30 bg-amber-500/5 transition-colors hover:bg-amber-500/10", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "flex items-center gap-4 cursor-pointer", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    "input",
                    {
                      type: "checkbox",
                      checked: sources.internetSearch.enabled,
                      onChange: (e) => setSources((s) => ({ ...s, internetSearch: { ...s.internetSearch, enabled: e.target.checked } })),
                      className: "w-5 h-5 accent-amber-500 rounded cursor-pointer"
                    }
                  ),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-base font-bold text-foreground block mb-0.5", children: "Fetch questions from the Internet" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-xs text-muted-foreground block", children: "Allow AI to crawl the web for up-to-date or broader questions." })
                  ] })
                ] }) }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(AnimatePresence, { children: sources.internetSearch.enabled && /* @__PURE__ */ jsxRuntimeExports.jsxs(motion.div, { initial: { opacity: 0, height: 0 }, animate: { opacity: 1, height: "auto" }, exit: { opacity: 0, height: 0 }, className: "space-y-2 overflow-hidden", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs font-bold text-muted-foreground uppercase tracking-widest ml-1", children: "Search term or Topic" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx(
                    Input,
                    {
                      value: sources.internetSearch.query,
                      onChange: (e) => setSources((s) => ({ ...s, internetSearch: { ...s.internetSearch, query: e.target.value } })),
                      placeholder: "e.g. Thermodynamics JEE Advanced Previous Year Questions",
                      className: "h-12 bg-muted/50"
                    }
                  ),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-[10px] text-muted-foreground mt-1 ml-1", children: "Specify exactly what kind of questions the AI should look for online." })
                ] }) })
              ] })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "mt-6 shrink-0 pt-4 border-t border-border", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { onClick: () => setShowSourceModal(false), className: "w-full rounded-xl h-12 text-base font-bold shadow-lg shadow-primary/20", children: "Confirm Sources" }) })
          ]
        }
      )
    ] }) })
  ] });
}
function QuizPage() {
  const [activeTab, setActiveTab] = reactExports.useState("select");
  if (activeTab === "select") {
    return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "min-h-full w-full bg-background flex flex-col items-center justify-center p-6 md:p-12 overflow-y-auto relative text-foreground", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute top-1/4 left-1/4 -translate-x-1/2 -translate-y-1/2 w-[350px] h-[350px] bg-blue-500/10 rounded-full blur-[120px] pointer-events-none" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute bottom-1/4 right-1/4 translate-x-1/2 translate-y-1/2 w-[350px] h-[350px] bg-purple-500/10 rounded-full blur-[120px] pointer-events-none" }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "max-w-4xl w-full z-10 space-y-10 text-center py-8", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          motion.div,
          {
            initial: { opacity: 0, y: -20 },
            animate: { opacity: 1, y: 0 },
            transition: { duration: 0.6 },
            className: "space-y-4",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "inline-flex items-center gap-2 px-3 py-1 rounded-full border border-primary/20 bg-primary/5 text-primary text-xs font-semibold tracking-wide uppercase", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Sparkles, { className: "h-3.5 w-3.5 animate-pulse" }),
                "Intelligent Study Suite"
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("h1", { className: "text-4xl md:text-5xl lg:text-6xl font-black tracking-tight text-foreground bg-clip-text text-transparent bg-gradient-to-r from-foreground via-foreground/90 to-muted-foreground", children: "Empower Your Prep" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-muted-foreground text-sm md:text-base max-w-xl mx-auto font-medium", children: "Choose from our advanced suite of AI learning tools designed to optimize your preparation and maximize performance." })
            ]
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          motion.div,
          {
            initial: { opacity: 0, y: 20 },
            animate: { opacity: 1, y: 0 },
            transition: { duration: 0.6, delay: 0.1 },
            className: "grid grid-cols-1 md:grid-cols-3 gap-6 text-left",
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs(
                motion.div,
                {
                  whileHover: { scale: 1.03, y: -4 },
                  whileTap: { scale: 0.98 },
                  onClick: () => setActiveTab("chat"),
                  className: "group relative cursor-pointer overflow-hidden rounded-3xl border border-border bg-card/45 backdrop-blur-md p-6 transition-all hover:border-blue-500/50 hover:shadow-2xl hover:shadow-blue-500/5",
                  children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute -right-8 -bottom-8 w-24 h-24 bg-blue-500/10 rounded-full blur-2xl group-hover:bg-blue-500/20 transition-all duration-500" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col h-full justify-between gap-8", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-4", children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "h-12 w-12 rounded-2xl bg-blue-500/10 border border-blue-500/20 flex items-center justify-center text-blue-500 group-hover:scale-110 transition-transform duration-300", children: /* @__PURE__ */ jsxRuntimeExports.jsx(BrainCircuit, { className: "h-6 w-6" }) }),
                        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                          /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "text-xl font-bold text-foreground group-hover:text-blue-500 dark:group-hover:text-blue-400 transition-colors", children: "AI Chat" }),
                          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground mt-2 leading-relaxed", children: "Interactive companion for solving doubts, clarifying concepts step-by-step, and exploring formulas with deep context." })
                        ] })
                      ] }),
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-1.5 text-xs font-bold text-blue-500 dark:text-blue-400", children: [
                        "Launch Chat",
                        /* @__PURE__ */ jsxRuntimeExports.jsx(ArrowRight, { className: "h-3.5 w-3.5 group-hover:translate-x-1.5 transition-transform" })
                      ] })
                    ] })
                  ]
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsxs(
                motion.div,
                {
                  whileHover: { scale: 1.03, y: -4 },
                  whileTap: { scale: 0.98 },
                  onClick: () => setActiveTab("quiz"),
                  className: "group relative cursor-pointer overflow-hidden rounded-3xl border border-border bg-card/45 backdrop-blur-md p-6 transition-all hover:border-purple-500/50 hover:shadow-2xl hover:shadow-purple-500/5",
                  children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute -right-8 -bottom-8 w-24 h-24 bg-purple-500/10 rounded-full blur-2xl group-hover:bg-purple-500/20 transition-all duration-500" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col h-full justify-between gap-8", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-4", children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "h-12 w-12 rounded-2xl bg-purple-500/10 border border-purple-500/20 flex items-center justify-center text-purple-500 group-hover:scale-110 transition-transform duration-300", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Target, { className: "h-6 w-6" }) }),
                        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                          /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "text-xl font-bold text-foreground group-hover:text-purple-500 dark:group-hover:text-purple-400 transition-colors", children: "AI Quizzes" }),
                          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground mt-2 leading-relaxed", children: "Custom quiz generator designed to build mock exams using your uploaded PDFs, video notes, and targeted topics." })
                        ] })
                      ] }),
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-1.5 text-xs font-bold text-purple-500 dark:text-purple-400", children: [
                        "Launch Quizzes",
                        /* @__PURE__ */ jsxRuntimeExports.jsx(ArrowRight, { className: "h-3.5 w-3.5 group-hover:translate-x-1.5 transition-transform" })
                      ] })
                    ] })
                  ]
                }
              ),
              /* @__PURE__ */ jsxRuntimeExports.jsxs(
                motion.div,
                {
                  whileHover: { scale: 1.03, y: -4 },
                  whileTap: { scale: 0.98 },
                  onClick: () => setActiveTab("engin"),
                  className: "group relative cursor-pointer overflow-hidden rounded-3xl border border-border bg-card/45 backdrop-blur-md p-6 transition-all hover:border-indigo-500/50 hover:shadow-2xl hover:shadow-indigo-500/5",
                  children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute -right-8 -bottom-8 w-24 h-24 bg-indigo-500/10 rounded-full blur-2xl group-hover:bg-indigo-500/20 transition-all duration-500" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col h-full justify-between gap-8", children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-4", children: [
                        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "h-12 w-12 rounded-2xl bg-indigo-500/10 border border-indigo-500/20 flex items-center justify-center text-indigo-500 group-hover:scale-110 transition-transform duration-300", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Search, { className: "h-6 w-6" }) }),
                        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
                          /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "text-xl font-bold text-foreground group-hover:text-indigo-500 dark:group-hover:text-indigo-400 transition-colors", children: "Strome" }),
                          /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground mt-2 leading-relaxed", children: "Advanced search engine for indexing web resources, querying study papers, and crawling complex academic notes." })
                        ] })
                      ] }),
                      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-1.5 text-xs font-bold text-indigo-500 dark:text-indigo-400", children: [
                        "Launch Strome",
                        /* @__PURE__ */ jsxRuntimeExports.jsx(ArrowRight, { className: "h-3.5 w-3.5 group-hover:translate-x-1.5 transition-transform" })
                      ] })
                    ] })
                  ]
                }
              )
            ]
          }
        )
      ] })
    ] });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex flex-col h-full bg-background overflow-hidden relative w-full text-foreground", children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex-1 overflow-hidden relative", children: activeTab === "chat" ? /* @__PURE__ */ jsxRuntimeExports.jsx(AIChatInterface, { onBack: () => setActiveTab("select") }) : activeTab === "quiz" ? /* @__PURE__ */ jsxRuntimeExports.jsx(AIQuizInterface, { onBack: () => setActiveTab("select") }) : /* @__PURE__ */ jsxRuntimeExports.jsx(AIEngin, { onBack: () => setActiveTab("select") }) }) });
}
export {
  QuizPage as default,
  quizGeneratorManager
};
