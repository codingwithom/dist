import { c as createLucideIcon, i as useAppContext, w as useWorkspaceContext, u as useLocalStorage, F as Flame, aE as Moon, j as jsxRuntimeExports, C as Card, V as Volume2, B as Button, M as Music, m as motion, A as AnimatePresence, X, S as Search, I as Input, v as LoaderCircle, P as Pause, o as Play } from "./index-DCXNzjIX.js";
import { b as reactExports } from "./vendor-charts-Du7SI2xx.js";
import { R as Radio, L as Library } from "./radio-FB_IzLfa.js";
import { G as Globe } from "./globe-BvJs1oYe.js";
import { U as Upload } from "./upload-aIVzR2d7.js";
import { T as Trash2 } from "./trash-2-CuPzRtqX.js";
import "./vendor-firebase-C_hAKoH5.js";
const __iconNode$7 = [
  ["path", { d: "M6 16.326A7 7 0 1 1 15.71 8h1.79a4.5 4.5 0 0 1 .5 8.973", key: "1cez44" }],
  ["path", { d: "m13 12-3 5h4l-3 5", key: "1t22er" }]
];
const CloudLightning = createLucideIcon("cloud-lightning", __iconNode$7);
const __iconNode$6 = [
  ["path", { d: "M4 14.899A7 7 0 1 1 15.71 8h1.79a4.5 4.5 0 0 1 2.5 8.242", key: "1pljnt" }],
  ["path", { d: "M16 14v6", key: "1j4efv" }],
  ["path", { d: "M8 14v6", key: "17c4r9" }],
  ["path", { d: "M12 16v6", key: "c8a4gj" }]
];
const CloudRain = createLucideIcon("cloud-rain", __iconNode$6);
const __iconNode$5 = [
  ["path", { d: "M10 2v2", key: "7u0qdc" }],
  ["path", { d: "M14 2v2", key: "6buw04" }],
  [
    "path",
    {
      d: "M16 8a1 1 0 0 1 1 1v8a4 4 0 0 1-4 4H7a4 4 0 0 1-4-4V9a1 1 0 0 1 1-1h14a4 4 0 1 1 0 8h-1",
      key: "pwadti"
    }
  ],
  ["path", { d: "M6 2v2", key: "colzsn" }]
];
const Coffee = createLucideIcon("coffee", __iconNode$5);
const __iconNode$4 = [
  [
    "path",
    {
      d: "M7 16.3c2.2 0 4-1.83 4-4.05 0-1.16-.57-2.26-1.71-3.19S7.29 6.75 7 5.3c-.29 1.45-1.14 2.84-2.29 3.76S3 11.1 3 12.25c0 2.22 1.8 4.05 4 4.05z",
      key: "1ptgy4"
    }
  ],
  [
    "path",
    {
      d: "M12.56 6.6A10.97 10.97 0 0 0 14 3.02c.5 2.5 2 4.9 4 6.5s3 3.5 3 5.5a6.98 6.98 0 0 1-11.91 4.97",
      key: "1sl1rz"
    }
  ]
];
const Droplets = createLucideIcon("droplets", __iconNode$4);
const __iconNode$3 = [
  ["rect", { width: "16", height: "16", x: "4", y: "3", rx: "2", key: "1wxw4b" }],
  ["path", { d: "M4 11h16", key: "mpoxn0" }],
  ["path", { d: "M12 3v8", key: "1h2ygw" }],
  ["path", { d: "m8 19-2 3", key: "13i0xs" }],
  ["path", { d: "m18 22-2-3", key: "1p0ohu" }],
  ["path", { d: "M8 15h.01", key: "a7atzg" }],
  ["path", { d: "M16 15h.01", key: "rnfrdf" }]
];
const TramFront = createLucideIcon("tram-front", __iconNode$3);
const __iconNode$2 = [
  ["path", { d: "M10 10v.2A3 3 0 0 1 8.9 16H5a3 3 0 0 1-1-5.8V10a3 3 0 0 1 6 0Z", key: "1l6gj6" }],
  ["path", { d: "M7 16v6", key: "1a82de" }],
  ["path", { d: "M13 19v3", key: "13sx9i" }],
  [
    "path",
    {
      d: "M12 19h8.3a1 1 0 0 0 .7-1.7L18 14h.3a1 1 0 0 0 .7-1.7L16 9h.2a1 1 0 0 0 .8-1.7L13 3l-1.4 1.5",
      key: "1sj9kv"
    }
  ]
];
const Trees = createLucideIcon("trees", __iconNode$2);
const __iconNode$1 = [
  [
    "path",
    {
      d: "M2 6c.6.5 1.2 1 2.5 1C7 7 7 5 9.5 5c2.6 0 2.4 2 5 2 2.5 0 2.5-2 5-2 1.3 0 1.9.5 2.5 1",
      key: "knzxuh"
    }
  ],
  [
    "path",
    {
      d: "M2 12c.6.5 1.2 1 2.5 1 2.5 0 2.5-2 5-2 2.6 0 2.4 2 5 2 2.5 0 2.5-2 5-2 1.3 0 1.9.5 2.5 1",
      key: "2jd2cc"
    }
  ],
  [
    "path",
    {
      d: "M2 18c.6.5 1.2 1 2.5 1 2.5 0 2.5-2 5-2 2.6 0 2.4 2 5 2 2.5 0 2.5-2 5-2 1.3 0 1.9.5 2.5 1",
      key: "rd2r6e"
    }
  ]
];
const Waves = createLucideIcon("waves", __iconNode$1);
const __iconNode = [
  ["path", { d: "M12.8 19.6A2 2 0 1 0 14 16H2", key: "148xed" }],
  ["path", { d: "M17.5 8a2.5 2.5 0 1 1 2 4H2", key: "1u4tom" }],
  ["path", { d: "M9.8 4.4A2 2 0 1 1 11 8H2", key: "75valh" }]
];
const Wind = createLucideIcon("wind", __iconNode);
const DEFAULT_TRACKS = [
  { id: "rain", name: "Rain", icon: CloudRain, src: "https://assets.mixkit.co/active_storage/sfx/2391/2391-preview.mp3", color: "text-blue-400" },
  { id: "thunder", name: "Thunder", icon: CloudLightning, src: "https://assets.mixkit.co/active_storage/sfx/2399/2399-preview.mp3", color: "text-indigo-400" },
  { id: "cafe", name: "Cafe", icon: Coffee, src: "https://assets.mixkit.co/active_storage/sfx/444/444-preview.mp3", color: "text-amber-600" },
  { id: "fire", name: "Fire", icon: Flame, src: "https://assets.mixkit.co/active_storage/sfx/2394/2394-preview.mp3", color: "text-orange-500" },
  { id: "wind", name: "Wind", icon: Wind, src: "https://assets.mixkit.co/active_storage/sfx/2400/2400-preview.mp3", color: "text-slate-400" },
  { id: "river", name: "River", icon: Droplets, src: "https://assets.mixkit.co/active_storage/sfx/2500/2500-preview.mp3", color: "text-cyan-400" },
  { id: "ocean", name: "Ocean", icon: Waves, src: "https://assets.mixkit.co/active_storage/sfx/118/118-preview.mp3", color: "text-blue-500" },
  { id: "forest", name: "Forest", icon: Trees, src: "https://assets.mixkit.co/active_storage/sfx/2502/2502-preview.mp3", color: "text-green-500" },
  { id: "night", name: "Night", icon: Moon, src: "https://assets.mixkit.co/active_storage/sfx/2506/2506-preview.mp3", color: "text-indigo-300" },
  { id: "train", name: "Train", icon: TramFront, src: "https://assets.mixkit.co/active_storage/sfx/2996/2996-preview.mp3", color: "text-stone-500" },
  { id: "whitenoise", name: "White Noise", icon: Radio, src: "https://assets.mixkit.co/active_storage/sfx/2513/2513-preview.mp3", color: "text-gray-300" }
];
function AmbientMixer() {
  const { theme } = useAppContext();
  const { writeMedia, readMediaAsBlob, deleteMedia } = useWorkspaceContext();
  const [customTracks, setCustomTracks] = useLocalStorage("jee_ambient_custom_tracks", []);
  const [activeTrackIds, setActiveTrackIds] = useLocalStorage("jee_ambient_active", DEFAULT_TRACKS.map((t) => t.id));
  const allTracks = reactExports.useMemo(() => [...DEFAULT_TRACKS, ...customTracks], [customTracks]);
  const activeTracks = reactExports.useMemo(() => allTracks.filter((t) => activeTrackIds.includes(t.id)), [allTracks, activeTrackIds]);
  const [volumes, setVolumes] = reactExports.useState({});
  const [playing, setPlaying] = reactExports.useState({});
  const [isFullscreen, setIsFullscreen] = reactExports.useState(false);
  const audioRefs = reactExports.useRef({});
  const [showLibrary, setShowLibrary] = reactExports.useState(false);
  const [searchQuery, setSearchQuery] = reactExports.useState("");
  const fileInputRef = reactExports.useRef(null);
  const [libraryTab, setLibraryTab] = reactExports.useState("local");
  const [onlineResults, setOnlineResults] = reactExports.useState([]);
  const [isSearchingOnline, setIsSearchingOnline] = reactExports.useState(false);
  const [onlineError, setOnlineError] = reactExports.useState("");
  const [resolvingId, setResolvingId] = reactExports.useState(null);
  const [onlineSource, setOnlineSource] = reactExports.useState("mixkit");
  const [previewUrl, setPreviewUrl] = reactExports.useState(null);
  const previewAudioRef = reactExports.useRef(null);
  const filteredTracks = reactExports.useMemo(() => {
    const q = searchQuery.toLowerCase();
    return allTracks.filter((t) => t.name.toLowerCase().includes(q));
  }, [allTracks, searchQuery]);
  reactExports.useEffect(() => {
    return () => {
      Object.values(audioRefs.current).forEach((audio) => {
        if (audio) {
          audio.shouldBePlaying = false;
          audio.pause();
          audio.src = "";
        }
      });
      if (previewAudioRef.current) {
        previewAudioRef.current.pause();
      }
    };
  }, []);
  reactExports.useEffect(() => {
    if (!showLibrary && previewAudioRef.current) {
      previewAudioRef.current.pause();
      setPreviewUrl(null);
    }
  }, [showLibrary]);
  const togglePreview = (url) => {
    if (previewUrl === url) {
      previewAudioRef.current?.pause();
      setPreviewUrl(null);
    } else {
      if (previewAudioRef.current) previewAudioRef.current.pause();
      const audio = new Audio(url);
      audio.play().catch(() => setOnlineError("Failed to play preview."));
      audio.onended = () => setPreviewUrl(null);
      previewAudioRef.current = audio;
      setPreviewUrl(url);
    }
  };
  const loadAudio = async (track) => {
    if (audioRefs.current[track.id]) return audioRefs.current[track.id];
    const audio = new Audio();
    audio.loop = true;
    audio.volume = volumes[track.id] !== void 0 ? volumes[track.id] / 100 : 0.5;
    audioRefs.current[track.id] = audio;
    if (track.isCustom && track.src.startsWith("http")) {
      audio.src = track.src;
    } else if (track.isCustom) {
      const blob = await readMediaAsBlob(track.src);
      if (blob) audio.src = URL.createObjectURL(blob);
    } else {
      audio.src = track.src;
    }
    return audio;
  };
  const togglePlay = async (id) => {
    const isPlaying = playing[id];
    let audio = audioRefs.current[id];
    if (!audio) {
      const track = allTracks.find((t) => t.id === id);
      if (track) audio = await loadAudio(track);
    }
    if (audio) {
      if (isPlaying) {
        audio.shouldBePlaying = false;
        audio.pause();
      } else {
        audio.shouldBePlaying = true;
        audio.play().catch(() => {
          const playOnLoad = () => {
            if (audio.shouldBePlaying) audio?.play().catch(console.error);
            audio?.removeEventListener("canplay", playOnLoad);
          };
          audio?.addEventListener("canplay", playOnLoad);
        });
      }
      setPlaying((prev) => ({ ...prev, [id]: !isPlaying }));
    }
  };
  const handleVolume = async (id, val) => {
    setVolumes((prev) => ({ ...prev, [id]: val }));
    let audio = audioRefs.current[id];
    if (!audio) {
      const track = allTracks.find((t) => t.id === id);
      if (track) audio = await loadAudio(track);
    }
    if (audio) {
      audio.volume = val / 100;
      if (val > 0 && !playing[id]) {
        audio.shouldBePlaying = true;
        audio.play().catch(() => {
          const playOnLoad = () => {
            if (audio.shouldBePlaying) audio?.play().catch(console.error);
            audio?.removeEventListener("canplay", playOnLoad);
          };
          audio?.addEventListener("canplay", playOnLoad);
        });
        setPlaying((prev) => ({ ...prev, [id]: true }));
      } else if (val === 0 && playing[id]) {
        audio.shouldBePlaying = false;
        audio.pause();
        setPlaying((prev) => ({ ...prev, [id]: false }));
      }
    }
  };
  const toggleMix = (id) => {
    setActiveTrackIds((prev) => {
      if (prev.includes(id)) {
        if (playing[id]) {
          const audio = audioRefs.current[id];
          if (audio) {
            audio.shouldBePlaying = false;
            audio.pause();
          }
          setPlaying((p) => ({ ...p, [id]: false }));
        }
        return prev.filter((x) => x !== id);
      }
      return [id, ...prev];
    });
  };
  const handleFileUpload = async (e) => {
    const file = e.target.files?.[0];
    if (!file) return;
    e.target.value = "";
    const id = `ambient_${Date.now()}`;
    const name = file.name.replace(/\.[^/.]+$/, "");
    await writeMedia(id, file);
    const newTrack = { id, name, src: id, color: "text-primary", isCustom: true, icon: void 0 };
    setCustomTracks((prev) => [newTrack, ...prev]);
    setActiveTrackIds((prev) => [id, ...prev]);
  };
  const handleDeleteCustom = async (id, src) => {
    if (playing[id]) {
      const audio = audioRefs.current[id];
      if (audio) {
        audio.shouldBePlaying = false;
        audio.pause();
      }
      setPlaying((p) => ({ ...p, [id]: false }));
    }
    setActiveTrackIds((prev) => prev.filter((x) => x !== id));
    setCustomTracks((prev) => prev.filter((x) => x.id !== id));
    if (audioRefs.current[id]) {
      audioRefs.current[id].src = "";
      delete audioRefs.current[id];
    }
    if (!src.startsWith("http")) {
      await deleteMedia(src);
    }
  };
  const handleOnlineSearch = async () => {
    if (!searchQuery.trim()) return;
    setIsSearchingOnline(true);
    setOnlineError("");
    setOnlineResults([]);
    try {
      if (onlineSource === "archive") {
        const q = encodeURIComponent(searchQuery);
        const res = await fetch(`https://archive.org/advancedsearch.php?q=mediatype:audio+AND+title:(${q})&fl[]=identifier,title,creator&sort[]=downloads+desc&rows=50&output=json`);
        if (!res.ok) throw new Error("Failed to fetch from Internet Archive");
        const data = await res.json();
        const docs = data.response?.docs || [];
        if (docs.length === 0) {
          setOnlineError("No results found. Try a different search term.");
        } else {
          setOnlineResults(docs.map((doc) => ({
            id: `ia_${doc.identifier}`,
            originalId: doc.identifier,
            title: doc.title || "Unknown Audio",
            author: doc.creator || "Unknown Author",
            thumbnail: `https://archive.org/services/img/${doc.identifier}`,
            source: "archive"
          })));
        }
      } else {
        const q = encodeURIComponent(searchQuery.replace(/\s+/g, "-"));
        const fetchMixkit = async (type) => {
          let html = "";
          const targetUrl = `https://mixkit.co/free-${type}/${q}/`;
          try {
            const res = await fetch(`https://api.allorigins.win/get?url=${encodeURIComponent(targetUrl)}`);
            const data = await res.json();
            if (data.contents) html = data.contents;
          } catch (e) {
          }
          if (!html) {
            const res = await fetch(`https://corsproxy.io/?${encodeURIComponent(targetUrl)}`);
            html = await res.text();
          }
          const doc = new DOMParser().parseFromString(html, "text/html");
          const playerEls = doc.querySelectorAll("[data-audio-player-preview-url-value]");
          return Array.from(playerEls).map((el) => {
            const url = el.getAttribute("data-audio-player-preview-url-value");
            if (!url) return null;
            const card = el.closest(".item-grid-item, .item-grid-card, .music-track-row") || el.parentElement?.parentElement;
            const titleEl = card?.querySelector(".item-grid-card__title, .item-grid-music-preview__title, .music-track-row__title, h2");
            const authorEl = card?.querySelector(".item-grid-card__author, .item-grid-music-preview__author, .music-track-row__author");
            let title = titleEl?.textContent?.trim();
            if (!title) {
              const match = url.match(/\/([^/]+)-preview\.mp3/);
              title = match ? match[1].replace(/-/g, " ") : "Mixkit Sound";
            }
            const idMatches = url.match(/\/(\d+)\//);
            const id = idMatches ? `mixkit_${idMatches[1]}` : `mixkit_${Math.random().toString(36).slice(2)}`;
            return {
              id,
              title,
              author: authorEl?.textContent?.trim().replace(/^By\s+/i, "") || "Mixkit",
              url,
              source: "mixkit"
            };
          }).filter(Boolean);
        };
        const [sfx, music] = await Promise.all([
          fetchMixkit("sound-effects"),
          fetchMixkit("stock-music")
        ]);
        const combined = [...sfx, ...music].filter((v, i, a) => a.findIndex((t) => t?.url === v?.url) === i);
        if (combined.length === 0) {
          setOnlineError("No results found on Mixkit. Try a different term.");
        } else {
          setOnlineResults(combined);
        }
      }
    } catch (err) {
      setOnlineError("Search failed. Please try again.");
      console.error(err);
    } finally {
      setIsSearchingOnline(false);
    }
  };
  const handleAddOnlineTrack = async (result) => {
    if (result.source === "archive") {
      const id = `ambient_ia_${result.originalId}`;
      if (customTracks.some((t) => t.id === id)) {
        if (!activeTrackIds.includes(id)) setActiveTrackIds((prev) => [id, ...prev]);
        return;
      }
      setResolvingId(result.originalId);
      try {
        const res = await fetch(`https://archive.org/metadata/${result.originalId}`);
        const data = await res.json();
        const audioFile = data.files?.find((f) => f.format?.includes("MP3") || f.format?.includes("Ogg") || f.format?.includes("Flac"));
        if (!audioFile) {
          setOnlineError(`No playable audio format found for "${result.title}".`);
          return;
        }
        const streamUrl = `https://archive.org/download/${result.originalId}/${audioFile.name}`;
        const newTrack = { id, name: result.title, src: streamUrl, color: "text-blue-500", isCustom: true, icon: void 0 };
        setCustomTracks((prev) => [newTrack, ...prev]);
        setActiveTrackIds((prev) => [id, ...prev]);
      } catch (e) {
        setOnlineError(`Failed to resolve audio for "${result.title}".`);
      } finally {
        setResolvingId(null);
      }
    } else {
      const id = result.id;
      if (customTracks.some((t) => t.id === id)) {
        if (!activeTrackIds.includes(id)) setActiveTrackIds((prev) => [id, ...prev]);
        return;
      }
      const newTrack = { id, name: result.title, src: result.url, color: "text-blue-500", isCustom: true, icon: void 0 };
      setCustomTracks((prev) => [newTrack, ...prev]);
      setActiveTrackIds((prev) => [id, ...prev]);
    }
  };
  reactExports.useEffect(() => {
    const handleFullscreenChange = () => {
      setIsFullscreen(!!document.fullscreenElement);
    };
    document.addEventListener("fullscreenchange", handleFullscreenChange);
    return () => document.removeEventListener("fullscreenchange", handleFullscreenChange);
  }, []);
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(Card, { className: `p-6 border-border backdrop-blur-xl transition-all duration-500 flex flex-col ${isFullscreen ? "fixed inset-0 z-[100] bg-black/95 rounded-none" : "bg-card/50 h-full border-0 rounded-none"}`, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between mb-6 w-full max-w-4xl mx-auto shrink-0", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("h2", { className: "text-xl font-bold flex items-center gap-2 text-foreground", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(Volume2, { className: "h-5 w-5 text-primary" }),
          " Ambient Mixer"
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground mt-1", children: "Zen mode for deep focus" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex items-center gap-2", children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Button, { variant: "outline", size: "sm", onClick: () => setShowLibrary(true), className: "gap-1.5 h-9 text-xs", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Library, { className: "h-4 w-4" }),
        " Library"
      ] }) })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: `grid gap-4 w-full max-w-4xl mx-auto overflow-y-auto pb-4 pr-1 ${isFullscreen ? "grid-cols-2 md:grid-cols-3 lg:grid-cols-4" : "grid-cols-2 sm:grid-cols-3"}`, children: activeTracks.map((track) => {
      const isTrackPlaying = playing[track.id];
      const volume = volumes[track.id] ?? 50;
      const Icon = track.icon || (track.src.startsWith("http") ? Globe : Music);
      return /* @__PURE__ */ jsxRuntimeExports.jsxs(
        motion.div,
        {
          layout: true,
          className: `p-4 rounded-2xl border transition-all ${isTrackPlaying ? "bg-primary/5 border-primary/30" : "bg-muted/30 border-border/50"}`,
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between mb-4", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-3 min-w-0", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: `p-2 rounded-xl shrink-0 ${isTrackPlaying ? "bg-primary/20" : "bg-muted"}`, children: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon, { className: `h-6 w-6 transition-colors ${isTrackPlaying ? track.color : "text-muted-foreground"}` }) }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-semibold text-sm text-foreground truncate", children: track.name })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "button",
                {
                  onClick: () => togglePlay(track.id),
                  className: `h-8 w-8 rounded-full flex items-center justify-center border transition-all ${isTrackPlaying ? "bg-primary text-primary-foreground border-primary" : "border-border text-muted-foreground hover:border-primary/50"}`,
                  children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: `w-3 h-3 rounded-full transition-all ${isTrackPlaying ? "bg-primary-foreground animate-pulse" : "bg-muted-foreground"}` })
                }
              )
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "input",
              {
                type: "range",
                min: "0",
                max: "100",
                value: volume,
                onChange: (e) => handleVolume(track.id, parseInt(e.target.value)),
                className: "w-full h-1.5 rounded-lg appearance-none cursor-pointer bg-muted outline-none accent-primary",
                style: {
                  background: `linear-gradient(to right, hsl(var(--primary)) ${volume}%, hsl(var(--muted)) ${volume}%)`
                }
              }
            )
          ]
        },
        track.id
      );
    }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(AnimatePresence, { children: showLibrary && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "fixed inset-0 z-[200] flex items-center justify-center p-4", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute inset-0 bg-black/60 backdrop-blur-sm", onClick: () => setShowLibrary(false) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(
        motion.div,
        {
          initial: { opacity: 0, scale: 0.95, y: 10 },
          animate: { opacity: 1, scale: 1, y: 0 },
          exit: { opacity: 0, scale: 0.95, y: 10 },
          className: "relative bg-card border border-border rounded-2xl shadow-2xl w-full max-w-2xl max-h-[85vh] flex flex-col",
          children: [
            /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between p-4 md:p-6 border-b border-border shrink-0", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-4", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs("h2", { className: "text-lg font-bold flex items-center gap-2", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(Library, { className: "h-5 w-5 text-primary" }),
                  " Audio Library"
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex bg-muted rounded-lg p-0.5", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("button", { onClick: () => setLibraryTab("local"), className: `px-3 py-1 text-xs font-medium rounded-md transition-colors ${libraryTab === "local" ? "bg-background shadow-sm text-foreground" : "text-muted-foreground hover:text-foreground"}`, children: "Local" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx("button", { onClick: () => setLibraryTab("online"), className: `px-3 py-1 text-xs font-medium rounded-md transition-colors ${libraryTab === "online" ? "bg-background shadow-sm text-foreground" : "text-muted-foreground hover:text-foreground"}`, children: "Online Search" })
                ] })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("button", { onClick: () => setShowLibrary(false), className: "text-muted-foreground hover:text-foreground", children: /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "h-5 w-5" }) })
            ] }),
            libraryTab === "local" ? /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "p-4 border-b border-border flex gap-2 shrink-0", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "relative flex-1", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Search, { className: "absolute left-3 top-1/2 -translate-y-1/2 h-4 w-4 text-muted-foreground" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { value: searchQuery, onChange: (e) => setSearchQuery(e.target.value), placeholder: "Search tracks...", className: "pl-9 h-9 text-xs bg-muted/50 border-border" })
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsxs(Button, { size: "sm", className: "h-9 gap-1.5 text-xs shrink-0", onClick: () => fileInputRef.current?.click(), children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Upload, { className: "h-4 w-4" }),
                " Add Track"
              ] }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "file", accept: "audio/*", className: "hidden", ref: fileInputRef, onChange: handleFileUpload })
            ] }) : /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "p-4 border-b border-border flex gap-2 shrink-0 flex-col", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex gap-2", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsxs(
                  "select",
                  {
                    value: onlineSource,
                    onChange: (e) => setOnlineSource(e.target.value),
                    className: "h-9 text-xs bg-muted border border-border rounded-md px-2 outline-none focus:ring-1 focus:ring-primary/50",
                    children: [
                      /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "mixkit", children: "Mixkit" }),
                      /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "archive", children: "Archive" })
                    ]
                  }
                ),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "relative flex-1", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(Globe, { className: "absolute left-3 top-1/2 -translate-y-1/2 h-4 w-4 text-blue-500" }),
                  /* @__PURE__ */ jsxRuntimeExports.jsx(Input, { value: searchQuery, onChange: (e) => setSearchQuery(e.target.value), onKeyDown: (e) => e.key === "Enter" && handleOnlineSearch(), placeholder: onlineSource === "mixkit" ? "Search Mixkit for ambient audio..." : "Search Internet Archive...", className: "pl-9 h-9 text-xs bg-muted/50 border-border" })
                ] }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs(Button, { size: "sm", className: "h-9 gap-1.5 text-xs shrink-0 bg-blue-600 hover:bg-blue-700 text-white", onClick: handleOnlineSearch, disabled: isSearchingOnline, children: [
                  isSearchingOnline ? /* @__PURE__ */ jsxRuntimeExports.jsx(LoaderCircle, { className: "h-4 w-4 animate-spin" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Search, { className: "h-4 w-4" }),
                  " Search"
                ] })
              ] }),
              onlineError && /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-red-400", children: onlineError })
            ] }),
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "flex-1 overflow-y-auto p-4 grid grid-cols-1 sm:grid-cols-2 gap-2", children: libraryTab === "local" ? /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
              filteredTracks.map((track) => {
                const inMix = activeTrackIds.includes(track.id);
                const Icon = track.icon || (track.src.startsWith("http") ? Globe : Music);
                return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between p-3 rounded-xl border border-border/50 bg-muted/30 hover:bg-muted/50 transition-colors", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-3 min-w-0", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: `p-2 rounded-lg bg-primary/10 ${track.color || "text-primary"}`, children: /* @__PURE__ */ jsxRuntimeExports.jsx(Icon, { className: "h-4 w-4" }) }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-sm font-semibold truncate", children: track.name })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2 shrink-0 ml-2", children: [
                    track.isCustom && /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: "ghost", size: "icon", className: "h-7 w-7 text-muted-foreground hover:text-destructive", onClick: () => handleDeleteCustom(track.id, track.src), children: /* @__PURE__ */ jsxRuntimeExports.jsx(Trash2, { className: "h-3.5 w-3.5" }) }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: inMix ? "secondary" : "outline", size: "sm", className: "h-7 text-[10px] px-3 shrink-0", onClick: () => toggleMix(track.id), children: inMix ? "Remove" : "Add to Mix" })
                  ] })
                ] }, track.id);
              }),
              filteredTracks.length === 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "col-span-full py-10 text-center text-muted-foreground", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Music, { className: "h-10 w-10 mx-auto mb-3 opacity-20" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm", children: "No tracks found." })
              ] })
            ] }) : /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
              isSearchingOnline && onlineResults.length === 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "col-span-full py-12 flex flex-col items-center justify-center text-muted-foreground", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(LoaderCircle, { className: "h-8 w-8 animate-spin mb-4 text-blue-500" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm", children: "Searching the web..." })
              ] }),
              !isSearchingOnline && onlineResults.length === 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "col-span-full py-10 text-center text-muted-foreground", children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx(Globe, { className: "h-10 w-10 mx-auto mb-3 opacity-20" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm", children: "Search to find ambient tracks online." })
              ] }),
              onlineResults.map((result) => {
                const trackId = result.source === "archive" ? `ambient_ia_${result.originalId}` : result.id;
                const inMix = activeTrackIds.includes(trackId);
                const isPreviewing = previewUrl === result.url;
                const isResolving = resolvingId === result.originalId;
                return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-3 p-3 rounded-xl border border-border/50 bg-muted/30 hover:bg-muted/50 transition-colors", children: [
                  result.source === "archive" ? /* @__PURE__ */ jsxRuntimeExports.jsx("img", { src: result.thumbnail, className: "w-10 h-10 rounded object-cover shrink-0 bg-muted", alt: "", onError: (e) => e.currentTarget.style.display = "none" }) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "w-10 h-10 rounded-full bg-primary/10 flex items-center justify-center shrink-0", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Music, { className: "h-5 w-5 text-primary" }) }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex-1 min-w-0", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-sm font-semibold text-foreground truncate", children: result.title }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground truncate", children: result.author })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex flex-col gap-1.5 shrink-0", children: [
                    result.source === "mixkit" && /* @__PURE__ */ jsxRuntimeExports.jsxs(Button, { variant: isPreviewing ? "default" : "secondary", size: "sm", className: "h-6 text-[10px] px-2", onClick: () => togglePreview(result.url), children: [
                      isPreviewing ? /* @__PURE__ */ jsxRuntimeExports.jsx(Pause, { className: "h-3 w-3 mr-1" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Play, { className: "h-3 w-3 mr-1" }),
                      isPreviewing ? "Stop" : "Preview"
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx(Button, { variant: inMix ? "secondary" : "outline", size: "sm", className: "h-6 text-[10px] px-2", onClick: () => handleAddOnlineTrack(result), disabled: isResolving, children: isResolving ? /* @__PURE__ */ jsxRuntimeExports.jsx(LoaderCircle, { className: "h-3.5 w-3.5 animate-spin" }) : inMix ? "Added" : "Import" })
                  ] })
                ] }, result.id);
              })
            ] }) })
          ]
        }
      )
    ] }) })
  ] });
}
export {
  AmbientMixer
};
